# P-L062-RC2 · ALPHA — three of the four --board defaults now resolve (env → ~/.consonance.json → a loud refusal); the fourth is not a board default, and I stopped on it

**Pane A, machine L, 2026-09-21 03:1x–04:0x.** Lap L062 packet R-C2. Plan: `loop/plan_repo_fixes_2026-09-21.md` R-C2
(4328d56), read at source. I authored none of the four tools (the routing reason, checked with `git log`).
**Six files, all mine this lap, uncommitted:** `deference-unit.js`, `order-parameter.js`, `vicsek-phi.js` and their
three test files. **No existing test was weakened, removed or modified**; the ten tests are new.

## 0 · ONE LINE OF THE PACKET CANNOT BE BUILT AS WRITTEN — contamination.js, and why I stopped (§6)

The packet says all four sites default to `C:/Consonance/data/board.jsonl`. **Three do. `contamination.js` does not**:
its flagged site is `:140`, `config: arg('--config', 'C:/Consonance/subjects/run2/config')`, and the tool never reads a
board at all (`grep -c board consonance/tools/contamination.js` → 0). That path is **the recorded location of an
experiment's transcripts** — B's own §1 (`handback/p-contamination-B_2026-09-20.md:40-46`) records that the cells ran
at `C:\Consonance\subjects\run2\cells`, so the transcripts "resolve only from that path". It is not under the data
directory, so routing it through `dataDir()` would point it at a place the transcripts are not and **break B's
recorded result** — the exact thing the bar forbids.

**So I did not build it**, per the stop rule, and `contamination.js` is byte-identical to HEAD. Its recorded command
still reproduces unchanged (§3). **A candidate, not built and not mine to choose:** default `--config` to the sibling
of `--cells` (`path.resolve(cells, '..', 'config')`). B's recorded command passes `--cells
C:/Consonance/subjects/run2/cells`, so this would reproduce it exactly and contain no drive letter. But the tool's own
`--cells` default is the repo's `exo_memory/loop/run2/cells`, whose sibling holds `config.settings.json`, not a
`config/` tree — so the no-argument behaviour would change, and that is the tool author's (B's) call.

## 1 · THE FIX, for the three board defaults

One resolver, in `deference-unit.js` — the module the other two **already** import their board reader from
(`order-parameter.js:258`, `vicsek-phi.js:256` require `readBoard` from it), so no new module and no new dependency:

- `boardDefault(env, home)` → `{ file, tier }` from `CONSONANCE_DATA`, else `~/.consonance.json` `data_dir` (a BOM does
  not sink the read), else **`null`**. Same shape as `consonance/hooks/transcript-watch.js:118-128` and
  `consonance/tools/board-compact.js:251-259`. `env` and `home` are parameters so no test reads the real config.
- `resolveBoard(given)`: `--board` if given, else the default.
- `NO_BOARD`: one refusal text all three print, naming both tiers, so the words cannot drift. Each tool exits **2**
  with it and reads nothing.

## 2 · RED FIRST, THEN GREEN — the command beside every number

    RED (the resolver stubbed to return the old literal; the ten tests added):
      node --test consonance/tools/deference-unit.test.js    pass 18 · fail 6   (the 6 new ones)
      node --test consonance/tools/order-parameter.test.js   pass 25 · fail 2   (the 2 new ones)
      node --test consonance/tools/vicsek-phi.test.js        pass 16 · fail 2   (the 2 new ones)
      all 59 tests that existed before this lap: pass at red.
    GREEN:
      deference-unit 24/0 · order-parameter 27/0 · vicsek-phi 18/0   = 69 passed, 0 failed

**The ten, one per resolution path:** tier one (env), tier two (config, with a BOM), neither → `null`, an unparseable
config → `null` rather than a throw; and for **each of the three CLIs**: with no `--board` the universe block names the
resolved file, and with nothing to resolve it refuses, exits non-zero, names both tiers, and counts nothing. The two
heavy tools are tested without their models: their universe block prints before `loadEncoder`
(`order-parameter.js:258-271`, `vicsek-phi.js:256-274`), so `--deps <empty dir>` reaches the board default and stops.

## 3 · THE BAR — every recorded result command, its recorded number, and mine

**The first thing to say plainly:** the live board has grown since every one of these runs (30,212 → 30,248 → 30,253 →
30,375 → today 30,769 rows), and it is **not append-only** — the first 30,375 rows of today's board hash to
`720c6d59…`, not C's `2a4a559f…` (257 fused lines were recovered since). So **no recorded number taken on the live
board can reproduce on today's live board, with or without this change.** What this change can affect is only WHICH FILE
the default reads, so I measured it two ways:

1. **Same bytes, old code against new code**, where the new code gets its board ONLY from the resolved default
   (`CONSONANCE_DATA=<dir holding the snapshot as board.jsonl>`, no `--board`), and the old code (HEAD's copies in
   `<scratchpad>/rc2/old/`) gets the same file by flag.
2. **Exact reproduction on C's own frozen snapshot**, which survives on disk with the recorded sha256
   (`…/sibling-0845a868/…/scratchpad/def/board-snapshot.jsonl`, 30,375 rows, `sha256 2a4a559fa1da4163` — copied into my
   scratch, read-only).

| recorded command (hand-back) | recorded number | board it was taken on | old code, today | NEW code, resolved default |
|---|---|---|---|---|
| `node consonance/tools/deference-unit.js --members …` (`p-deference-unit-C:185-192`) | selected **1,377**; duplicates 3,975; scanned 26,021 | live, 30,253 rows (unrecoverable) | selected **1379**, dup 3,976, scanned 26,536 on a 30,769-row snapshot | **1379** — full output IDENTICAL to old, path line aside |
| `node consonance/tools/deference-unit.js` (`:170`) | selected 1448 | live, 30,248 rows, **before C's dedupe repair** | superseded by C's own L058 repair (dedupe is now the default) — not re-derivable by design | — |
| `order-parameter.js --deps <d> --board <snapshot> --shuffles 20` (`p-order-parameter-C:302-306`) | 30,375 rows; **118** sessions | C's snapshot | 30,375 rows · 257 unparseable · 30,118 usable · 3,975 dup · sessions 286 · **eligible 118** | **identical** (pre-encoder) |
| `order-parameter.js --deps <d> --out …L060.json` (`:208`) | 117 of 117 eligible; slopes 97/117; gap 0.0507 | live, the registered run (30,333 rows, 42 fewer than the snapshot) | not reproducible: that board is not recoverable, and the encoder is not on L | — |
| `vicsek-phi.js --deps <C's encoder-trial> --board <C's snapshot>` (`p-vicsek-phi-B:108-114`) | 30,375 rows · 257 unparseable · 30,118 usable · 3,975 dup · **118** sessions | C's snapshot | 30,375 · 257 · 30,118 · 3,975 · eligible **118** | **identical** (pre-encoder) |
| `contamination.js --cells C:/Consonance/subjects/run2/cells --json …` (`p-contamination-B:32-40`) | 130 trials · 130 transcripts · **320** item-observations; null 0/30 · 0/10 · 0/30; planted 100% · all cells CONTAM+CORRECT 320 | the run2 cells on disk | **tool unchanged** — 130 · 130 · 320; null 0/30, 0/10, 0/30; planted 100/100, 120/120, 100/100; ALL 320 CONTAM+CORRECT | (not built, §0) |

**And on this machine, with no flag at all, the resolved default IS the old literal's file** — both tiers, measured:

    tier 1 (this seat's env carries CONSONANCE_DATA)   C:\Consonance\data\board.jsonl · same file as the old literal: true
    tier 2 (env removed, ~/.consonance.json data_dir)  C:\Consonance\data\board.jsonl · same file as the old literal: true

So a bare `node consonance/tools/<tool>.js` on L reads exactly what it read before.

**What I could NOT reproduce, stated rather than left to be found:** every post-encoder number — slopes, quintile
gaps, r, φ, the 14,955 contributions — because `@huggingface/transformers` is not on L (`find … -path
"*node_modules/@huggingface/transformers"` → nothing). Those numbers are functions of the rows only; the rows are
proven identical pre-encoder; but the arithmetic after that line was not run.

## 4 · PORTABLE-PATHS, BEFORE AND AFTER

    node consonance/tools/portable-paths.js
      BEFORE   RED — 41 machine-specific path(s) not in the baseline · exit 1
               REVIEW  contamination.js:140 · deference-unit.js:147 · order-parameter.js:253 · vicsek-phi.js:248
      AFTER    RED — 32 … · exit 1
               in my four tools and their tests: contamination.js:140 ONLY

**The 41 → 32 is not all mine.** Other seats landed R-C1 and R-C3 while I worked (`git diff --stat` shows
`consonance/README.md`, `hooks/live-mirror-stop.js`, `tools/live-follow.js` and the baseline file changed — none of
them mine). **My own delta: −3 REVIEW sites, +0 added.**

## 5 · CORRECTIONS, INCLUDING TO MYSELF

- **The packet's premise was wrong about one of the four sites** (§0), and I checked it before building rather than
  building around it.
- **My first test fixtures added FIVE portable-paths sites** — `'C:/somewhere/data'` and `'D:/other/data'` as fixture
  directories, and a test NAME containing `C:\`. That is the rule I wrote down myself at D083, *"a fixture must not add a
  site to the ratchet"*, broken again. The directories are now drive-less (`/somewhere/data`), which the assertions
  never needed, and the test name no longer carries a drive; the sites are gone. They would otherwise have ridden into
  R-C4's `--update` as "BENIGN-TEST" rows that this lap created.
- **My rename of that test then broke the file** — an apostrophe unescaped inside a single-quoted string, `pass 0 ·
  fail 1` — caught by the next run and fixed with the editor instead of another layer of shell quoting.
- **My first plan for exact reproduction assumed an append-only board.** The prefix hash disproved it (`720c6d59…` vs
  `2a4a559f…`), and I used C's surviving snapshot instead.

## 6 · WHAT I DID NOT VERIFY

- **No post-encoder number** was re-derived, on any tool (§3). The embedding models are not on L.
- **Machine D was not run.** D's `~/.consonance.json` and env were not checked from here; if D has neither, the three
  tools now REFUSE there instead of reading `C:/Consonance/data`, which is the intended behaviour but a visible change.
- **No mutants were run** — the packet asked for a test per resolution path, not a mutation pass.
- **The full js-suite was not re-run**; the three test files were run plainly, and they are the only suite files
  touched.
- **Whether C's snapshot is still the file C's §9 run read** is taken from its sha256 matching the recorded one, not from
  C's transcript.
- **`contamination.js`'s `--config` default is unresolved**, by the stop rule (§0).

NEXT: librarian re-derive §3's same-bytes runs and route §0's contamination --config question to B, the tool's author, when the file is read

# P-L089-SOLID — a one-read decision sheet for "solid" (pane E, L089 (2), 2026-09-23)

Packet: the chair's L089 (2). **Machine L.** Started 02:39:22 (`date +%T`). **No code, no settings, nothing
committed.** **The sheet:** `exo_memory/loop/solid_decision_sheet_2026-09-23.md` (71 lines).

## 0 · IT IS ONE DECISION, WITH TWO SMALL CHOICES THAT ONLY APPLY ON "YES"

The question is **yes / no / not yet** on adopting the three criteria, with usage counted as "not yet measured" until
a reader exists. **Two ambiguities in the registration each change the answer**, so rather than resolve them I put
them on the sheet as choices he makes only if he says yes:
- **(a) "the tests"**: the two named suites, or every test target? This is the registration's own open fork, `:47–59`.
- **(b) "a clean week"**: a clean trip on each of seven days (what the checker does), or seven days with no bad trip?
  The registration's sentence at `:98` fits either reading.

I did not split it into three separate decisions. Adopting is one act, and (a) and (b) are meaningless without it.

## 1 · THE REGISTRATION'S OWN SUMMARY IS OUT OF DATE, AND THE SHEET SAYS SO

The packet summarised it as *"criterion 1 measurable, criteria 2 and 3 cannot take a value today, usage has ZERO
instruments"*, and so does the registration (`:13–19`, `:77`). **That was true when C wrote it, at `9bd7ce0`. It is
not true tonight for criterion 2:**
- **The trip checker was built the next lap** (C, D112, `0b8e84f`), and on L tonight it reads:

      node consonance/tools/trip-check.js --report   →   last 7 days: 11 trip(s) · 11 clean · 0 NOT clean · 2 clean day(s) · clean week: false

- **The arch_test red that made reading (a)-B false was repaired** by A in D111.

**Criterion 3 is unchanged: still zero instruments.** Re-run:
`grep -rlniE "ccusage|weekly limit|usage limit|quota" consonance/tools/*.js consonance/hooks/*.js consonance/src-tauri/src/*.rs dev/*.js`
finds 4 files and 6 hits, every one the word "quotation" or a variant, the same as the registration found.

*(The packet also said the registration was A's and C's. Its own footer says pane C, and D111 was an A+C lap. I credit
C on the sheet.)*

## 2 · WHAT I MEASURED ON L TONIGHT, AND HOW

    node consonance/tools/js-suite.js                                → 122 green · 1 failed · 0 crashed · 0 silent · 1 canary · 0 sang · 0 not-run (of 124)
    node consonance/tools/portable-paths.js                          → RED, 1 site: consonance/tools/trip-check.test.js:34  'C:/nowhere'  (DRIVE, BENIGN-TEST)
    cargo test --bin consonance -- --test-threads=1                  → 925 passed · 0 failed · 4 ignored   (99bfcda, 38.37 s)
    cargo test --test arch_test -- --test-threads=1                  → 13 passed · 0 failed   (99bfcda, 1.40 s)
    node consonance/tools/trip-check.js --report                     → as above
    grep -n "days.size >= 7" consonance/tools/trip-check.js          → :184, the strict reading of "a clean week"

- **The one JS failure is not the app's.** It is C's test fixture using a placeholder drive path that the portable-paths
  baseline hasn't absorbed. `portable-paths.js` itself says a benign fixture is settled with `--update`. **I did not run
  `--update`**: the file and the baseline are not mine, and running it would change the reading I was sent to report.
  The registration counts any failed file as a failure (`:63`), so **by its own rule L is not green tonight**, and the
  sheet says so and why.
- **HEAD moved during the JS run**, from `2d5a934` to `99bfcda`, because another seat landed while it ran. The cargo
  runs are both at `99bfcda`. **I ran the cargo tests only after the JS suite had finished, never concurrently**: the
  room's own rule, and C's false red of 09-22 is the reason for it.
- **D was not measured.** Its last reading, 119/0 and 870/0 at `9bd7ce0`, is quoted on the sheet with its date, as
  stale. "Both machines at the same commit" cannot be read from L alone.

## 3 · THE THINGS THE SHEET NAMES THAT THE REGISTRATION DOES NOT

- **Nobody is named to run the checks, and no cadence is set.** I suggest the librarian, at every machine switch, and
  mark it as my suggestion.
- **The clean-week reading, and what it hides.** As built, a week is clean only with a clean trip on each of 7 days
  (`trip-check.js:184`), and trips happen only when the keeper switches machines, so "no" can persist with nothing
  wrong. Under the other reading tonight would read clean. **But each launch overwrites the previous launch's
  record** (C, D112, `p-d112-tripcheck-C` §5 item 2), so most launches leave no trip row. "No bad trip seen" is
  partly "no trip seen". **I wrote that on the sheet so the lenient option doesn't look better than it is.**
- **Criterion 3's size, honestly.** Counting usage looks like about a lap: every call's token counts are already in the
  local transcripts, which I checked — 4,956 lines of my own transcript carry `"usage"`. **But the weekly LIMIT is not
  on the machine**, and I have not checked whether it can be read from the account. **Until that is known, "solid" as
  registered can never be declared.** That is the most important thing for him to know before saying yes.

## 4 · CORRECTIONS, INCLUDING MINE

- **I have been citing C's "launch overwrites the record" finding as §4 item 2. It is §5 item 2** (checked:
  `awk` over the hand-back puts line 97 under `## 5 · What could NOT be reconstructed…`). Fixed on the sheet. **The
  same wrong section number is in my D113 design**, `loop/union_at_launch_2026-09-22.md` §5(b), where it reads
  *"(C, §4 item 2)"*. That file is not mine this lap, so I name it here and do not change it.
- **I first wrote "close to clean" for the lenient clean-week reading.** It would read clean outright, and the honest
  point is that it reads clean partly because most launches are invisible. Rewritten before filing.
- **My first draft put the usage estimate as a fact.** It is now marked as my estimate.

## 5 · NOT VERIFIED

- **Whether the account's weekly limit is readable by any tool.** That decides whether criterion 3 can ever be built.
- **D's readings tonight.**
- **That the JS failure would clear with a baseline update.** `portable-paths` says so for benign fixtures; I did not
  run it.

NEXT: librarian call_librarian with the hand-back pointer when the sheet is written — plan default after it: items 5 + 6 (CH-4/ASK sheets, repo description, AGENTS.md), unless the output says otherwise

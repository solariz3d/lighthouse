# P-D111-SOLID — C (D111 packet C, chunk 2.2, ON D, 2026-09-22 12:4x) — UNCOMMITTED

**Written: `exo_memory/loop/solid_registration_2026-09-22.md`.** One new file, plus this hand-back and one map line.
Nothing else touched; `arch_test.rs` is A's this lap.

**It does not adopt "solid", does not freeze anything, and does not claim the system is solid.** Adoption and any freeze
are the keeper's; the registration only makes them one read.

## The headline the chair should carry to the keeper

**Two of the three criteria cannot take a value today**, because they have no instrument. Said plainly in §0 of the file
and again in §4, rather than written as if a clean-looking week counted as a pass.

| criterion | instrument | today's reading |
|---|---|---|
| 1 · suites green on both machines | exists (`js-suite.js`, `cargo test`) | D green in scope; L's reading is 5 commits old |
| 2 · a clean week of trips | **none — the trip checker is 2.3, not built** | none |
| 3 · usage under the weekly limit with keep-warm on | **none of any kind** | none |

## What each section does

- **§1 criterion 1** — the three exact commands, each machine's reading with the commit it was taken at, and a fork the
  keeper has to close: **"the suites" has two meanings that disagree TODAY.** Under the plan's two commands D is green.
  Under "every test target in the repo" it is FALSE on both machines, because `cargo test --test arch_test` fails on the
  record/card assertion (red since `397e29c`, 2026-09-11; L082 §4 on L, D110 §3 on D). The fork is about the WORDING, so
  it survives A's repair. Also OWED: a fresh run on **L at D's HEAD** — L's 119 of 120 predates `jev-flags.test.js`, so
  the two machines are five commits and one file apart.
- **§2 criterion 2 — OWED, with the six things the 2.3 checker must record** for "a clean week" to be a count: the trip's
  identity; the install result verbatim including every refused path with its row counts; anything replaced; per-file
  union counts with the tool's `verified` flag; rows before and after per ledger, so "nothing lost" is arithmetic; and,
  for misreporting, the re-derivation command beside every reported figure. Then a clean week is seven days of trip rows
  with nothing lost, nothing replaced and no figure differing from its re-derivation.
- **§3 criterion 3 — no instrument, stated plainly.** Verified, not assumed: the pattern
  `ccusage|weekly limit|usage limit|quota` over the tools, hooks, dev scripts and `main.rs` matches 4 files and 6
  occurrences, and **every one is "quotation"/"quotations"/"QUOTATIONS"/"quotable"**. Jev's daily cap is a different
  account and a fuse, not a budget; keep-warm's ping count is not a usage measure.
- **§4 the dead-L063 check** — criterion 1 takes both values, and both were OBSERVED this week (green 119/0 and 870/0
  today; not-green at `118 green · 1 failed` on D at D108 before the fix, and `12 passed · 1 failed` in arch_test right
  now). Criteria 2 and 3 take NO value, which is the finding, not a footnote: neither may be marked met until its
  instrument returns a number.
- **§5 what makes "solid" FALSE** — four firing conditions, including that **one bad trip restarts the clean week at
  zero**, and that scoring 2 or 3 without an instrument makes "solid" false by construction.
- **§6 the degenerating clause** — dated and able to fire: **by 2026-10-22**, if the criteria have been softened, or the
  word "solid" is in use while 2 and 3 still have no number, this line is degenerating. Second form: if no trip row
  exists by then, criterion 2 must be STRUCK from the definition rather than carried as an unmeasured pass.

## Checked before writing

- The §3 grep was re-run exactly as printed. My first draft said every match was "quota-tion"; one is "quotable", so the
  sentence now names all four forms and the counts. Corrected before filing.
- Today's D figures are D110's, re-quoted, not re-run for this file.

## NOT verified

- **L.** I ran nothing there. Its reading is the chair's packet figure at `bec101d`, quoted as such.
- **Whether the keeper wants Reading A or Reading B in §1.** That is his, and until it is settled criterion 1 is not
  scorable.
- **Whether the six fields in §2 are sufficient** for the 2.3 checker. They are what D106's union and L070's refusal
  produced; a seventh may be needed once a trip is actually recorded.
- No instrument was built this lap, and no suite was re-run.

NOT COMMITTED.

NEXT: librarian collate the solid registration when p-d111-solid-C_2026-09-22.md is written and the map line is appended

# P3e-READ — non-author read of ALPHA's §1 correction (pane E / ECHO, 2026-09-01 ~03:05 −06:00)

**Object:** `exo_memory/research/the_retrieval_problem_outside.md`, uncommitted (`git diff --stat`: 1 file,
+80/−5). Read as a diff against HEAD `f68ea5c`. **Why this seat:** it scored the bands the block reports
(`79a369b`); it did not write any sentence the block quotes.

## Verdict: the correction is sound. One word to change; nothing to add to its substance.

## The four bars, each re-derived

1. **Placed ABOVE — yes.** Block opens at `:43`; the paragraph it corrects opens at `:109`; the struck sentence
   is at `:116`. Both sit inside §1 (`## 1` at `:21`, `## 2` at `:126`), so the registered clause
   (`battery_load_registration_2026-08-31.md:208`, *"into the research file's §1 as a correction, above the
   paragraph it corrects"*) is met to the letter. **The placement count reproduces:**
   `grep -rio 'decorrelated reader' --include=*.md .` (correction file excluded) → **22**; the replacement →
   **10**. ALPHA's 22:10 stands; the chair's 32:18 does not reproduce for this seat either.
2. **Dated trace, legible — yes.** The counter-is-focal sentence is struck in place with `~~ ~~`, its original
   wording intact, followed by a dated bracketed note that keeps the part that is still true (*the counter DID
   fire*) and names the part that lost its test. Same treatment at §2 (`:141-144`, the trailing-reminder
   paragraph gets K1 as an adverse near-test with *"this file's falsifier has NOT fired"* — correct: different
   harness, different subject) and at plan items 3 and 4 (`:250-254`): annotated in place, not rewritten.
3. **"No cue moved the number," never "made it worse" — yes, and the numbers are right.** Headline
   *"THE RESULT — NO CUE MOVED THE NUMBER"*; explicit *"Never 'the focal cue made it worse'"*. The four Fisher
   exact p-values re-derived by this seat from the same counts (13/20, 29/40, 33/40): K1 v K2 **0.422**,
   K0 v K1 **0.564**, K0 v K2 **0.195**, K0 v pooled **0.260** — identical to the block's. The "17-point gap" is
   K2 − K0 = 17.5. The CIs quoted match my file. And the block does not overstate toward the null either:
   *"a real +10 is not excluded, only unevidenced"* carries the other direction in the same paragraph. Grain,
   no change needed: the point estimates did move up (+7.5, +17.5); "moved" in the headline means *detectably*,
   and the block says so four lines later.
4. **The comparability defect is carried as a defect — yes.** Its own bold paragraph, the registration's §4
   sentence (`:175`) quoted and called FALSE across the K0/K seam, K1-vs-K2 stated clean. Verified on disk:
   `sha256sum cells/{P0a,L0,L1}/r*/handoff.js` → **50 files, all `bbd64e8b…f094` = `rig/handoff.v1.js`**;
   `cells/{K1,K2}/r*/handoff.js` → **80 files, all `aee411f1…e126` = `rig/handoff.js`**. How it got past the
   hashing, for the record: `MANIFEST.json` (K0) pins `handoff.js` per cell but has no top-level `handoff`
   field, `MANIFEST.cue.json` has both, and nobody compared the two manifests' values to each other.

## The one edit worth making (ALPHA's file, so it goes here, not there)

The seam paragraph says the cue cells ran **"(v2)"**. ALPHA's own hand-back
(`handback/p3a-cue-arms-parked_2026-08-31.md` §4) calls the same file **v3**, and reserves *v2* for the
attempt-1 hand-on whose header comment quoted the K2 line — the contaminated one. There is no
`rig/handoff.v2.js` on disk; the two files are `handoff.v1.js` and `handoff.js`. A later reader holding both
documents will ask whether the cue arms ran the contaminated hand-on. **Replace the version label with the
hash — "the K0 cells (`bbd64e8b…`, v1) and the cue cells (`aee411f1…`)" — or pick one version word and
change the hand-back to match.** One word; it sits on exactly the paragraph that must not be misread.

## Cross-checks that passed and are not repeated above

`79a369b` and `1830dae` exist and contain the files the block names; CHARLIE's `:162-196` is the quoted
section; the transcript figures the block carries (100/100 summary-before-hand-on; `end_turn` 100/100;
35–145-char floor; 60 of 75 recited the rule; 74 of 75 pointed to `REPLY.md`; K2_r26 the one engager) are
each present in `cue_transcript_read_2026-09-01.md` at `:33-43`, `:88`, `:110-115`, `:170-189`. 75 violators
= 13 + 29 + 33.

*Pane E. ALPHA's file untouched. Nothing committed. A trace to re-run, not a doctrine to believe.*

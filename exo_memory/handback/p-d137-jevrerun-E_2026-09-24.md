# P-D137-JEVRERUN: STOPPED — the 56-unit set is not on D (pane E, D137, 2026-09-24)

Packet: the chair's D137, the Jev R2 re-run owed since 09-23. **Machine D.** **0 gateway calls, 0 model calls, no rows
file** (with no set, there are no rows to write). Nothing about run 2 was opened. Nothing committed.

## THE STOP, per the packet's own condition

The set was built **on L** (L111, `exo_memory/loop/jev_unitset_registration_2026-09-23.md`). The re-run needs its sealed
half, which is on L only:

| file (registration `:97`, `:101`) | on D |
|---|---|
| `C:\Consonance\sealed\jev_unitset_2026-09-23\key.json` (sha256 `3b390bb2…`): per unit the session, turn, stored verdict and probabilities, `prompt_sha256` | **No such file or directory** |
| `C:\Consonance\reader_packets\jev_unitset_2026-09-23.md` (sha256 `12440eb0…`) | **No such file or directory** (the folder itself does not exist) |

**Searched, not assumed.**
- `C:\Consonance\sealed\` on D holds only `tj1v2_2026-09-24\` (my D133 key).
- `find` over `C:\Consonance`, `~/Desktop`, `~/Documents`, `~/Downloads` and `~/.consonance` (depth 5), plus the drive
  letters a stick would take, for `*jev_unitset*` → only the in-repo registration and
  `exo_memory/loop/jev_unitset/{draw.js, score_librarian.js}`.
- A first, deeper search (`C:\Consonance` and all of `C:\Users\nname`, depth 6) went to the background. **It finished
  after the ring and found no copy either:** its `find` printed nothing, and the three names in its output file came from
  the `ls` in the same command.

**Not rebuilt**, as the packet says. `draw.js` on D would draw over D's transcripts, so it would be a different 56 units,
and "re-judge the SAME units" would be false.

## WHERE IT CAN RUN — L, as a ready packet

1. Verify `key.json` against `3b390bb2…`.
2. For each of the 56 units, rebuild the prompt with the **current** builder (the `jev/` module, the L083 repair)
   from L's transcript at the key's session and turn.
3. **One Jev call per unit, ≤ 56.** State the count first, then count it from the log.
4. Compare each new verdict with the key's stored verdict, per unit, old → new, with both prompt shas: the key's
   `prompt_sha256` (the retracted prompt) and the new one.
5. Rows hold verdicts and hashes only.

**Two things to check on L first:**
- L's runner must be the post-D130 build (the fingerprint hashes what it LOADED), or the new prompt sha could again
  describe a builder it did not run.
- `jev_r1_rows_2026-09-23.jsonl` (in the repo) holds 20 stored verdicts by prompt sha. It is a leak path for the readers
  (registration `:148`), not for Jev, and is not the set.

## WHAT I WROTE

- A dated addendum to `exo_memory/loop/jev_r2r3_score_2026-09-23.md` ("ADDENDUM 2026-09-24 ~00:0x (D137, pane E)"),
  saying the re-run is still owed and why.
- This hand-back, and one map line.

NEXT: librarian call_librarian with the hand-back pointer when the re-run is compared and written (or stopped because the set isn't on D) — plan default after it: the librarian collates D137 when A, C and E have rung, unless the output says otherwise

# P-D100-RULES · ALPHA — all four vantage_cell rules removed; anything reappearing there refuses. On D close --check still exits 0. On L it will refuse on the old cell, and L cannot even pull this cleanly while my L070 edit sits dirty

**Pane A, machine D, 2026-09-21 10:4x–10:5x.** Lap D100 packet A, at the librarian's ruling (D100 collation). **Two
files, uncommitted:** `consonance/state-manifest.json` (−4 lines, exactly the four rules) and
`consonance/tools/state-manifest.test.js`. **No real close was run.** Nothing in any data dir was touched.

## 0 · THREE OF MY OWN TESTS WERE WITHDRAWN — named, because the rule against weakening tests is real

The ruling withdraws the behaviour three tests I wrote today asserted: that the cell's leftovers are **placed** STAYS.
Those assertions are verifiably wrong under the ruling, and each was **replaced by its exact inverse, not dropped**:

| withdrawn test (asserted: placed, exit 0) | its replacement (asserts: UNPLACED, exit 1, path named) |
|---|---|
| L068 · `the shipped manifest places vantage_cell/mutants-run.log as STAYS` | `D100: vantage_cell/mutants-run.log is UNPLACED — the L068 shelf is gone` |
| D099 · `the shipped manifest places the empty _verify_refuse tree as STAYS` | `D100: the empty _verify_refuse tree is UNPLACED — the D099 shelf is gone` |
| D099 · `a file a reader leaves inside _verify_refuse is STAYS too` | covered by the one above and by the wildcard mutant (§2), which the tree test kills |
| — (the directory rule had no test) | `D100: the vantage_cell directory itself is UNPLACED — no rule places the old cell` |

**Kept exactly as they were**, because they still hold: `any OTHER file left in vantage_cell is still UNPLACED` (its name
still says "the rule is named"; the rule is now gone, so the name is stale, but the assertion is right), and `D099: a
DIFFERENT scratch directory in the cell is still UNPLACED`. A comment marks each withdrawal where the test was.

## 1 · RED FIRST, THEN GREEN — the command beside every number

    node consonance/tools/state-manifest.test.js
      BEFORE                                  30 passed, 0 failed
      RED (3 D100 tests, rules still in)      30 passed, 3 failed   — the three D100 tests
      rules removed, old tests still in       30 passed, 3 failed   — now the three withdrawn placement tests (§0)
      GREEN (the three withdrawn)             30 passed, 0 failed   (30 − 3 + 3)

    the rule removal:  a script dropped exactly the lines matching `    { "glob": "vantage_cell…`, asserted the count was 4,
                       and re-parsed the JSON — `git diff --stat`: consonance/state-manifest.json | 4 ----

    node consonance/tools/state-manifest.js     (live data dir on D)   exit 0
    node consonance/tools/close.js --check      (run plainly, no pipe)
      CHECK ONLY — every gate passed and NOTHING WAS PUBLISHED · 64 files · 77.3 MB · privacy PRIVATE · EXIT=0
    consumers:  node consonance/tools/state-sync.test.js 78 passed, 0 failed · node consonance/tools/close.test.js 30 passed, 0 failed

**Nothing else reads these rules:** `grep -rln vantage_cell` over consonance/ and dev/ finds only the manifest, its test,
`second-vantage.js`/`.test.js` (the new cell in the temp dir, E's 4d1c417) and one comment in `main.rs:7407`.

## 2 · MUTANTS — each removed rule put back, on a copy

    node <scratchpad>/d100/mutants.js
      pre-flight 30/0 · 5 listed · 5 killed · 0 survived · 0 NOT APPLIED · live files unchanged: true
        the directory rule back, REGENERATES (28/2) · mutants-run.log back, STAYS (28/2) · _verify_refuse back (29/1) ·
        _verify_refuse/** back (28/2) · a wildcard vantage_cell/** STAYS (25/5)

## 3 · FOR THE NEXT L SHIFT — FIRST ITEM, checked from D, not fixed

**(1) L's close --check will refuse on the old cell once this lands on L.** Two entries, not one:
`vantage_cell` (the directory rule is gone too) and `vantage_cell/mutants-run.log` (3,383 B, 2026-09-14 01:23).
**Why "very likely still there":** nothing on record moves it. The librarian's L066 move (`librarian/2026-09-21.md:58`,
05:30) took `digests/`, `pulse/` and the mangled-name file only. Every later mention treats the log as placed by my L068
rule (last seen on the board at 12:42Z), and `git log -S mutants-run.log` since shows no move. **Not verified on L's disk.**
**The fix on L:** move `C:\Consonance\data\vantage_cell\` aside (not delete), then re-run `close.js --check` plainly.
L's close is also under the chair's HOLD (`bc3f2a1`) until L070 lands.

**(2) L cannot pull this cleanly while L070 is dirty on L.** My uncommitted L070 change on L edits
`consonance/state-manifest.json` (the lap.jsonl/board.jsonl rules). D099 (`6ad176e`) and this packet edit the same file,
so `git pull` on L will refuse: *"Your local changes to the following files would be overwritten"*. The edits are on
different lines and will merge by hand, but **L070's dirty files must be committed or stashed on L before L pulls
anything that touches the manifest.** This is inferred from git's rule and the two diffs, not run on L.

**(3) Until L pulls E's 4d1c417, L's readers still run in L's data-dir cell.** L's `second-vantage.js` is the old one, so
a reader launched on L before the pull can recreate exactly this refusal.

**Suggested order for L, as a route not a ruling:** land or stash L070 (with its correction, `ead6e8d`) → pull → move
`vantage_cell\` aside → `close.js --check` plainly.

## 4 · WHAT I DID NOT VERIFY

- **L's disk:** the log's presence, L's cell, L's working tree. All of §3 is inferred from the record.
- **The js-suite** was not run; only the three suites above.

NEXT: librarian re-run §1's manifest test and close --check, then carry §3 as the first item of the next L shift, when this file is read

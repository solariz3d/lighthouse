# P-L066-UNPLACED — C (CHARLIE), L066, on L — close REFUSED_UNPLACED, diagnosed first

**Packet:** L066, keeper-approved via the librarian. **Routed to me because I did not write the placement rules**
(A: manifest `022f268`, close `9b29449`). **Nothing edited in the repo or in the data dir. No real close run. Nothing
pushed. Nothing committed.** Owned in the end: this hand-back and one map line — see §6 for why no code file.

## 0 · THE ANSWER IN FOUR LINES

1. **The refusal is 12 entries in four families, not 3 digests** — and three separate causes.
2. **The main one (`digests/`, `pulse/`) is a variable collision across two owners**: the app hands `CONSONANCE_DATA`
   to the stick applier, **the applier relaunches the app without scrubbing it** (A's `dev/stick-apply.js:171`), and
   **eight shell hooks treat that same variable as their own home directory** (`dev/shell/hooks/*.js`). Headless
   `claude -p` runs spawned by that app then write the shell's notes into Consonance's data dir.
3. **CLASS: none of TRAVELS / STAYS / REGENERATES fits two of the four families** — they are not state, they are
   misplaced by a bug and an accident, and classifying them would bless the misplacement.
4. **This IS a keeper-level choice** (what happens to 9 existing files, 143 KB of them containing board text) plus
   fixes in A's files and in eight live hooks. **So, per the packet: said, and stopped.** `close.js --check` is still
   exit 1 and I did not try to make it 0.

## 1 · THE REFUSAL, reproduced plainly (no pipe)

`node consonance/tools/close.js --check` → **exit 1**:
`NOT CLOSED — the state set was not prepared: REFUSED_UNPLACED · state-sync exited 1`, naming:

| family | entries | first seen |
|---|---|---|
| `digests/` | dir + `2026-09-15.md`, `09-16`, `09-20`, **`09-21`** — 50 B each | 09-15 02:28 |
| `pulse/` | dir + `2026-09-15.jsonl`, `09-16`, `09-20`, **`09-21`** | 09-15 02:28 |
| `UserszacknAppDataLocalTempclaudeoot.json` | one file, **143,004 B** | 09-16 06:41 |
| `vantage_cell/mutants-run.log` | one file, 3,383 B | 09-14 01:23 |

**The librarian's measurement named 3 digests.** Since then `2026-09-21.md` appeared (05:15 today), and the `pulse/`
family, the odd file and the vantage log were in the refusal but not in the relay. It grows by one digest and one
pulse file per day the leak runs.

## 2 · CAUSE — what the check is, and what writes each family

**What refuses:** `consonance/state-manifest.json` has **76 rules in three classes — TRAVELS 29 · STAYS 36 ·
REGENERATES 11** — and its cut says anything matching none of them is UNDECIDED. `close.js` refuses on any
UNDECIDED path. **No rule mentions `digests` or `pulse`.**

### 2.1 · `digests/` + `pulse/` — the collision, both links RUN, not read

| link | where | evidence |
|---|---|---|
| **a** | `main.rs:11548` `stick_start_applier` spawns `dev/stick-apply.js` with `.env("CONSONANCE_DATA", &data)` | landed `0405bd2`, **09-14 04:51** (`git log -S`) |
| **b** | `dev/stick-apply.js:171` relaunches the app: `spawn(exe, [], { cwd, detached: true, stdio: 'ignore' })` — **no `env`**, so the app inherits `CONSONANCE_DATA` | **run in `<scratch>/l066/repro.js`**: a detached child spawned with exactly those options reads back the parent's `CONSONANCE_DATA` |
| **c** | every process the relaunched app spawns inherits it. **This pane has it**: `echo $CONSONANCE_DATA` → `C:\Consonance\data`; it is in neither the user nor machine environment and not in `launch.ps1` | the app itself never sets it for panes (`grep` over `src-tauri/src/*.rs`: only the three node-child `.env(...)` sites) |
| **d** | `dev/shell/hooks/session-end.js:32` — `SHELL_DIR = process.env.CONSONANCE_DATA \|\| ~/.claude/shell` | **evaluated verbatim in the repro**: with only `CONSONANCE_DATA` set, `SHELL_DIR` is the data dir |

**When it began:** the hook's seam is `1114eb7` (08-24, *"the unaccounted writer was our own test suite, and it is now
isolated"*); the applier's `.env` is `0405bd2` (09-14 04:51). **The first misplaced file is 09-15 02:28** — the same
minute as `data/stick-waiter.status.log`. **The running app started 09-21 00:50:30 and its parent process has already
exited** (`Win32_Process`), which fits a detached applier that relaunches and exits — but a vanished parent cannot be
named, so that link for *this* app instance is **inferred**. `stick-waiter.js` never launches the app, so the applier
is the only code path that does so with the variable set.

**Who actually wrote the 66 misplaced pulse lines** (grouped by the `cwd` each recorded):

| lines | writer |
|---:|---|
| **62** | the Consonance **scribe** — `cwd C:\build\lighthouse-target\release` (a `claude -p` the app spawns) |
| **4** | a `claude -p` run from the **Third Place's** scratchpad |
| **0** | any interactive pane |

**A correction to something I said mid-lap:** I first said *every pane's* `session-end` writes there. **The record
says headless runs**: `session-end` fires the moment a `claude -p` run ends, and interactive panes rarely end cleanly.
Interactive panes are exposed in principle — this pane carries the variable — but none is observed writing.

**The home side, measured:** `~/.claude/shell/pulse/` has files for 09-13, 09-14 and 09-15, and **none after 09-15**;
the data dir's `pulse/` begins 09-15. The shell's own notes of headless runs moved folders that day. *(I do NOT
claim the home digests' "No session activity recorded" is this: they read that way on 09-13/14 too.)*

### 2.2 · The collision is eight hooks wide — and two of them may need the data dir on purpose

`grep "process.env.CONSONANCE_DATA" dev/shell/hooks/*.js` (non-test): **`session-end`, `session-start`, `stop`,
`userprompt-submit`, `l2-overseer`, `l3-overseer`, `ready-prompt`, `ready-stop`.** All eight installed copies carry
it too. The comment on six of them says what it is for — a **test seam**: *"Hardcoded, these six wrote their ledgers
into the REAL ~/.claude/shell."* The two `ready-*` hooks are different in kind: the manifest classes `ready/*.json` **in
the data dir** as REGENERATES, which reads like the app's delivery gate reading its ready stamps from there by design.
**Unverified**, but it means a blanket rename of the seam would likely break the delivery gate.

*Also found, not mine:* the installed `session-start.js` and `userprompt-submit.js` **differ** from the repo copies
(sha256 prefixes differ); the other six match.

### 2.3 · `UserszacknAppDataLocalTempclaudeoot.json` — an accident, not state

**143,004 B, 09-16 06:41**, a JSON array of **board rows**. Its name is `C:\Users\zackn\AppData\Local\Temp\claude\…root.json`
with every backslash removed, which makes it a **drive-relative** path — `C:Users…` — that Windows resolves against drive
C's current directory, here the data dir. The `\r` in `…claude\root` became a control character and was lost with
it, hence *"claudeoot"*. **This is the backslash-eating failure of the Bash tool that I have recorded against myself;
some seat hit it on 09-16.** I have not quoted its content beyond confirming it is board rows — they contain the
room's and the keeper's words.

### 2.4 · `vantage_cell/mutants-run.log` — the one real manifest question

The rule is `vantage_cell` · **REGENERATES** · *"an EMPTY working directory — and git cannot carry an empty directory
at all."* **That premise has been false since 09-14 01:23**, when a mutant-harness run left a 3,383 B log in it. The
rule names the directory, not its contents, so the log is unplaced.

## 3 · CLASS — ruled per family, citing the manifest's own cut

The cut: *"A path is TRAVELS if a seat resuming on the other machine is a DIFFERENT seat without it. It is STAYS if it
describes this machine's disk, ports, pids, byte offsets, microphone or raw ore. It is REGENERATES only if a NAMED writer
rewrites it at a NAMED time, unasked."*

| family | class | why |
|---|---|---|
| `digests/`, `pulse/` | **none — misplaced** | They are the **shell's** notes, whose home is `~/.claude/shell`. A STAYS rule would make the refusal go away and **hide the leak** — every other file a leaked hook ever writes would then need its own rule, and none would say why. |
| `…claudeoot.json` | **none — an accident** | Not written by any named writer on purpose. A rule for it would be a rule for a typo. |
| `vantage_cell/mutants-run.log` | **STAYS** by the cut (a local run's raw output) — or delete it | This is the only family the manifest should absorb, and it is **A's file**. |

## 4 · WHY I STOPPED — the keeper-level choices, named

The packet: *"if this is a keeper-level choice, SAY SO and stop rather than pick."* It is, for these reasons:

**K1 · The 8 misplaced shell files.** Moving them home is **not a plain move**: `~/.claude/shell/digests/2026-09-15.md`
exists on **both** sides, and `pulse/2026-09-15.jsonl` exists on both sides with **different** contents (1,348 B at home,
5,354 B in the data dir) — a merge, with an ordering decision. The alternatives are to delete them as reconstructable, or
leave them and write a STAYS rule (which §3 argues against). That is a data decision.

**K2 · The 143 KB accidental file.** It holds board rows. Deleting user data needs the keeper's word; so does moving it
anywhere it would be read.

**K3 · Where the leak is stopped** — two owners, both outside this packet:

- **A · `dev/stick-apply.js:171`** — relaunch with the variable removed. Proposal below. `CONSONANCE_DATA` is an
  instruction to the applier's **children**, not to the app, and `grep` over the Rust finds **no read** of it by the app —
  so scrubbing it should be safe for the app. *(Not verified by running the app.)*
- **the shell's hooks** — split the test seam's name from the production variable for the **six** ledger hooks, and **not**
  the two `ready-*` hooks until someone confirms what they need (§2.2). This changes **live hooks for every seat** and the
  tests that use the seam (`userprompt_pulse.test.js` at least) — which is a test change needing its own proof.

**The bar was not reached, deliberately.** `close.js --check` exits 0 only once all 12 entries are placed or gone, and
every route there passes through K1, K2 or A's files.

## 5 · PROPOSALS — written, NOT applied

**For A — `dev/stick-apply.js:171`, test first.**

```js
// the relaunched app must not inherit an instruction meant for this process's children
const { CONSONANCE_DATA: _drop, ...env } = process.env;
const child = spawn(exe, [], { cwd: path.dirname(exe), detached: true, stdio: 'ignore', env });
```

**Red test:** drive the relaunch with `CONSONANCE_DATA` set and a spawn seam that captures `options.env`; assert the key is
absent. The applier already injects its side effects (*"Every side effect is injectable so the tests can drive it"*), so the
spawn is the natural seam. **Mutant:** delete `env` from the options → the test must go red.

**For A — `consonance/state-manifest.json`.** Either `vantage_cell/**` · STAYS · *"a harness run's raw output left in the
cell's working directory; local process output"*, **and correct the `vantage_cell` rule's "EMPTY" premise**; or delete the log
and keep the premise true. Either way the premise must stop being false.

**For the shell's owner — the seam.** Rename the **six** ledger hooks' seam to a name no production process sets (for example
`CLAUDE_SHELL_DIR`), keep `ready-prompt` / `ready-stop` on `CONSONANCE_DATA` until their contract is confirmed, and re-install
by the recorded procedure (`install.ps1 -NoRegister -Only <file>`, sha of `settings.json` unchanged before and after).

**For the keeper — K1 and K2** (§4). Nothing proposed; they are his.

## 6 · FILES — named before editing, as the packet asked, and why none was edited

Each fix lands in a file that is not mine: `dev/stick-apply.js` and `consonance/state-manifest.json` are **A's**; the eight
hooks are the shell's, **installed live for every seat**; and the data dir holds **user data** under two open keeper
decisions. So the owned set is **this hand-back and one map line**, and the fixes are proposals. **Nothing in the repo or in
`C:\Consonance\data` was modified**; the only writes were to my scratch directory.

## 7 · WHAT WAS NOT VERIFIED

1. **Which process launched the running app** (09-21 00:50:30) — its parent has exited. The applier path is the only one
   in the code that sets the variable and launches the app; that it did so **this time** is inferred.
2. **That the `ready-*` hooks need the data dir** — inferred from the manifest's `ready/*.json` rule, not traced.
3. **That scrubbing the variable is safe for the app** — no Rust read of it found by `grep`; not run.
4. **Machine D.** D runs the same code and the same applier; whether its data dir has the same families was not checked.
5. **Why no `event_log.jsonl` appears in the data dir** although `session-end` names it as an output — the headless runs
   wrote `digests/` and `pulse/` only. Not traced.

## 8 · COMMANDS

```
node consonance/tools/close.js --check                        # exit 1, REFUSED_UNPLACED, the 12 entries
echo $CONSONANCE_DATA                                         # set in this pane: C:\Consonance\data
git log -S"process.env.CONSONANCE_DATA" -- dev/shell/hooks/session-end.js     # 1114eb7, 08-24
git log -S'.env("CONSONANCE_DATA"' -- consonance/src-tauri/src/main.rs        # 0405bd2, 09-14
sed -n 171p dev/stick-apply.js                                # the relaunch, no env
node <scratch>/l066/repro.js                                  # both links, run in temp dirs
grep -n "process.env.CONSONANCE_DATA" dev/shell/hooks/*.js    # eight hooks
```

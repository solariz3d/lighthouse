# P-L083-CARRIERS — the retracted wording that was still instructing the judges, repaired and installed on L (pane E, L083 (a), 2026-09-23)

Packet: the chair's L083 (a). **Machine L**, HEAD `0635e73`. Started 01:13:14, installed 01:20:31, written 01:2x
(`date +%T`). Authorized by the keeper by name: `loop/plan_after_upgrade_2026-09-22.md` §AUTHORIZED item 4 (`d5bd9b1`).
**Nothing committed. No restart, no close.js, no ledger rewrite, and `settings.json` is byte-identical** (§3).

## 0 · WHAT CHANGED

| file | line | was | now |
|---|---|---|---|
| `dev/shell/hooks/l2-overseer-worker.js` | **:55** (was :49) | the struck test (registry id `cant-lose-handle-2026-08-29`) | **BOOT's third-principle wording, verbatim:** *"If you'd have said it whether or not it were true, it carries no information. Then go find out separately whether it's true."* |
| same | :37–44 | — | a **dated comment** keeping the retracted wording as the trace (marker `struck 2026-08-30, ASK-008` on its line), the authorization, and the Jev link |
| `dev/shell/hooks/session-start.js` | **:258** (was :249, the INJECTED line) | the retired stance-phrase (registry id `light-not-lifeguard-2026-08-17`) | **"With you, not above you"** — BOOT's public form |
| same | **:242** (comment) | the same phrase | the same public form |
| same | :244–252 | — | a **dated comment**: the retracted wording as the trace (marker `retired 2026-08-17`), and **why this form** (below) |
| `dev/shell/hooks/l2-overseer-worker.test.js` | +2 tests | — | the prompt does not match the registry's own pattern for `cant-lose-handle-2026-08-29` (**read by id, not restated**); the prompt carries the ASK-008 wording |
| `consonance/tools/carrier-drift.registry.json` | both entries | — | a new field, `instruction_carriers_outside_corpus`, recording six `.js` sites (§2) |

**Both wordings were taken from `exo_memory/BOOT.md`, not from the packet:**
`grep -o "\*\*If you'd have said it … whether it's true\.\*\*" exo_memory/BOOT.md` and
`grep -o 'the public form is \*\*"with you, not above you"\*\*' exo_memory/BOOT.md` each return the line.

**Why "with you, not above you" and not BOOT's other two forms.** BOOT's 2026-08-17 amendment names three:
- the keeper's completed form, *"there just is water"*, which says there is no outside place to stand;
- the public form, *"with you, not above you"*;
- the operational form, *the grip felt through the wheel*, which is about committing while still feeling the edge.

The retired phrase made one contrast: where the reader stands relative to the user — showing, not hauling from
above. Only the public form keeps that contrast, and the next clause, *"L3 surfaces; doesn't haul"*, still follows
from it. The reason is written into the dated comment, so the choice can be argued from the file.

## 1 · RED FIRST, THEN GREEN — and "fixes both judges" confirmed by RUNNING, as asked

    node dev/shell/hooks/l2-overseer-worker.test.js    before the repair → 20 tests · 18 pass · 2 fail   (the two new tests, red for the right reason)
                                                        after            → 20 tests · 20 pass · 0 fail

**Jev inherits it, run rather than read:**

    node -e "…J.loadJudgeInputs(process.cwd()).l2build(view,'D')…"   (consonance/tools/jev-judge.js, its own loader)
      → prompt carries struck form: false · carries ASK-008: true · sourcesSha d1a1ace445a4

`sourcesSha` changes because it hashes the L2 files. The jev-judge header calls it *"provenance, not a key"*, so new
rows carry a new value and old rows keep theirs.

**Every covering test, before and after** (colour stripped, each run by `node <file>`):

| test | before | after |
|---|---|---|
| `dev/shell/hooks/l2-overseer-worker.test.js` | 18 / 0 | **20 / 0** (+2 mine) |
| `consonance/tools/jev-judge.test.js` | 24 / 0 | 24 / 0 |
| `consonance/tools/jev-shadow.test.js` | 29 / 0 | 29 / 0 |
| `consonance/tools/jev-shadow-runner.test.js` | 38 / 0 | 38 / 0 |
| `dev/shell/hooks/shell-dir-seam.test.js` | 4 / 0 | 4 / 0 |
| `dev/shell/hooks/userprompt-submit.test.js` | 5 / 0 | 5 / 0 |
| `consonance/tools/install-only.test.js` | 17 / 0 | 17 / 0 |
| `consonance/tools/carrier-drift.test.js` | 57 / 0 | **57 / 0** |
| `node consonance/tools/carrier-drift.js` | exit 0 | **exit 0, GREEN** |

**No test pinned the old wording.** `grep -rn -i "can't lose\|lifeguard\|light, not" --include=*.test.js` hits only
`carrier-drift.test.js` and `memory-sweep.test.js`, and both build their own fixture trees rather than reading these
hooks. **There is no test for `session-start.js`**: its repair is checked by `node --check` plus a source scan
showing **the registry pattern matches no non-comment line**, i.e. nothing that gets injected.

## 2 · THE REGISTRY — the right place turned out not to be `sites`, and I checked before writing it

**`carrier-drift.js` scans `.md` and `.html` only** (`:326`, `:736`), and a `sites` row naming a file outside its
corpus is **MISSING-FILE** (`carrier-drift.js:622–630`) — **RED**. So **a `.js` site cannot go under `sites` without
turning the tool red**, and widening the tool is not my file. The tool reads only `id`, `pattern`, `marker`, `armed`
and `sites`; entries already carry fields it ignores (`pattern_note`, `propagation_pass`). So each entry gains
**`instruction_carriers_outside_corpus`**: a note on why these rows sit outside `sites`, and one row per `.js` site
with `file`, `line`, `state` and `what`. **None of them restates a withdrawn wording**: I checked each row's JSON for
all three registered wordings and none is present. They cite the id and the line, in B's form (`ea779ca`).

The file round-trips byte-identically through `JSON.stringify(o, null, 1) + '\n'` (checked before writing), so the
diff is **+49 / −2**, and the two "deletions" are lines that only gained a trailing comma.

| entry | site | state |
|---|---|---|
| `cant-lose-handle-2026-08-29` | `l2-overseer-worker.js:55` | REPAIRED (pinned by the new test, which reads THIS entry's pattern) |
| same | `l2-overseer-worker.js:42` | TRACE — dated comment, marker on the line |
| `light-not-lifeguard-2026-08-17` | `session-start.js:258` (injected) | REPAIRED |
| same | `session-start.js:242` (comment) | REPAIRED |
| same | `session-start.js:248` | TRACE — dated comment, marker on the line |
| same | **`userprompt-submit.js:277`** | **OPEN — see §4** |

Every remaining hit of either pattern in the two repaired files is a comment line carrying the registry's **own
marker** (checked with each entry's `pattern` and `marker` regexes: `session-start.js:247, :248` and
`l2-overseer-worker.js:42`, all MARKED). `:247` hits only because the registry **id** `light-not-lifeguard-…` itself
matches the pattern.

## 3 · THE INSTALL ON L

**Before, the one risk that mattered:** `-Only session-start.js` also *wires* it, and `install.ps1` rewrites
`settings.json` (through `ConvertTo-Json`, which can change bytes) whenever it adds or re-points a hook
(`install.ps1:870–887`). So I computed the installer's own expected command the way it does — `$node = (Get-Command
node).Source` (`:692`), `New-HookCommand` — and compared it with the registered one: **identical**, so registration
would be "already correct" and **no write**. Only then did I run it.

    settings.json sha256 BEFORE  d1cf423c5d4040daac2ee76790fa799482651dbe9741eac9dca009daa0c9daf1
    backups (cp -p, refused if one already existed):
      ~/.claude/shell/hooks/l2-overseer-worker.js.pre-install-2026-09-23   (installed-before sha256 770da224…)
      ~/.claude/shell/hooks/session-start.js.pre-install-2026-09-23        (installed-before sha256 3363e3e1…)
    powershell -File dev\shell\install.ps1 -Only l2-overseer-worker.js,session-start.js
      → "-Only: 2 of 27 manifest entr(ies), 1 of 15 registration entr(ies)" · ok hooks\session-start.js · ok hooks\l2-overseer-worker.js
      → "registration: already correct (1 hook(s) verified, 0 excluded by ruling, nothing changed)."
    settings.json sha256 AFTER   d1cf423c5d4040daac2ee76790fa799482651dbe9741eac9dca009daa0c9daf1   ← BYTE-IDENTICAL; no .bak- was made
    installed vs repo, CRLF-normalised sha256:  l2-overseer-worker.js d34f94b3… = d34f94b3… EQUAL · session-start.js 16800ed6… = 16800ed6… EQUAL
    install.ps1 -Check → ok hooks\session-start.js · ok hooks\l2-overseer-worker.js · exit 1 (see below)

- **I ran the install command TWICE.** The first run's output tail was only the script's prose, so I re-ran it and
  captured everything. **The second run was a no-op** — both files `ok`, registration already correct — and the
  settings hash above was taken after both runs.
- **`-Check` exits 1, and not because of this lap.** Its report carries **9 DRIFT** entries (`ready-stop`,
  `ready-prompt`, `userprompt-submit`, `stop`, `session-end`, `l2-overseer`, `l3-overseer`, `blind`,
  `transcript-watch`), **1 PATH-MISMATCH** (`ambient.js`), **5 REGISTERED-NOT-DECLARED**, and **3 UNCLAIMED** files
  from before tonight. **Mine add 2 UNCLAIMED — the two backups**, which the packet's naming puts beside the hooks,
  where no manifest entry owns them. The script's own prose already says *"-Check exits 1 until all of that is
  resolved, and it should."* **I did not capture a pre-install `-Check`**, so "exit 1 before" rests on that report's
  own categories rather than on a before-run I can show.

## 4 · SAID PLAINLY — what this lap did NOT fix

1. **The Jev runner live on L still asks the OLD prompt.** Runner pid **19732**, started 01:06:25. Its log shows judge
   passes at **07:06:31Z and 07:16:50Z**, both **before** my edit (**01:17:04 local = 07:17:04Z**). `jev-judge.js:186`
   caches its inputs once per process (`if (!io) io = loadJudgeInputs(repo)`), and Node caches the `require`. **So the
   repair reaches Jev on L at the runner's next start — the next app launch — and not before.** A restart is forbidden
   this lap. **"Fixing :49 fixes both judges" is true of the repo, and of every process started from now on.**
2. **A THIRD carrier, which the packet did not list: `dev/shell/hooks/userprompt-submit.js:277`** injects the same
   retired phrase into **every seat's context on every prompt**, not only at session start. My census named it
   (`carrier_halflife_2026-09-22.md` §2, `1f3d2d4`). The packet named `session-start.js` only, and
   `userprompt-submit.js` is not in my files, so it is **recorded as OPEN in the registry and left for a ruling.** It
   is the higher-traffic of the two injection sites.
3. **`session-start.js:240`** still says *"Per the dive-buddy reframe"*. That is a reference to the card by its name,
   `cards/lighthouse-dive-buddy-reframe.md`, and not the stance-phrase. Not in the registry's pattern, and not
   changed. Named because it is diving vocabulary in a live hook.
4. **On L the installed worker is consistency, not a live effect.** L's overseers have been unregistered since 09-06
   (`-Check`: *"3 EXCLUDED BY RULING, correctly absent"*), so no L2 hook runs the installed worker here. **What reaches
   a judge on L is the REPO copy**, which Jev loads (item 1). **`session-start.js` IS live on L** (registered at
   SessionStart), so that repair takes effect at every seat's next session start.
5. **D is not touched.** D's installed copies still carry both wordings until the same install runs there.

## 5 · CORRECTIONS, INCLUDING MINE

- **My first test-summary grep read nothing** — node's output carries ANSI colour codes, and half these files are
  plain scripts rather than `node --test`. I re-ran each with colour stripped and read the summary each file prints.
- **My first check of the session-start comments called `:247` UNMARKED**, because I used a stricter marker than the
  registry's (`retired 2026-08-17` against the registry's `retired`). Re-checked with each entry's own `marker`
  regex: MARKED.
- **I had planned to add the `.js` sites under `sites`.** Reading `carrier-drift.js:622–630` first showed that would
  be MISSING-FILE and turn the tool red, so the rows went into a field the tool does not read.

## 6 · NOT VERIFIED

- **That the next app launch actually loads the new prompt into Jev.** It follows from the cache argument; no launch
  was made.
- **`session-start.js`'s injected text at runtime.** No test runs the hook, and running it needs recent L3 notices,
  which L does not produce. Checked at source level plus `node --check`.
- **Pre-install `-Check`** (§3).

NEXT: librarian call_librarian with the hand-back pointer when the repair, tests and install check are written — the lap lands when C's audit is also in, and the baton after L083 is item 3 (jev-flags hook, settings on L) + item 2 ("solid" sheet)

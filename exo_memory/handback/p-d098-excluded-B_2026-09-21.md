# P-D098-EXCLUDED · BRAVO — the manifest now says what is true on each machine, and two test suites need a fix I do not own (one of them my own D097 miss)

**B (pane `12fb81f6`), machine D, 2026-09-21 ~10:1x–10:4x.** Packet: the chair's D098 packet B. Sources read at source:
- `dev/shell/install.ps1:190-205`;
- the 09-06 ruling, `exo_memory/librarian/2026-09-06.md:603`, the full paragraph;
- today's record, `exo_memory/loop/install_D_2026-09-21.md:3` (`2831cc2`).

HEAD `8c316aa`. **I changed only `dev/shell/install.ps1`: +47 −4, 7 hunks, none in a write path.** Nothing committed.
`install.ps1` was run only with `-Check`.

---

## 0 · THE ANSWER

- **The 09-06 ruling is machine-specific, and it is L's.** It lives in the laptop lineage's master
  (`librarian/2026-09-06.md`, not the `.desktop.md`), and its words (*"the three passengers (`hooks\stop.js`, the l2/l3
  overseers) stay unregistered"*) describe a machine where they were unregistered. On D they had been live since June.
  The installer's own comment already doubted it: *"Whether the ruling was meant to reach that machine is the keeper's
  to say."*
- **Today's word supersedes it on D only.** The record: *"yes to both XD" — the overseers stay live on D; the [pulse]
  beacon goes live on D.* It names D, and it names **the overseers**. So:
  - **`l2-overseer.js`, `l3-overseer.js`: live by ruling on D, still excluded on L.**
  - **`stop.js`: not covered.** It appends a `session_stop` event (`stop.js:2`, `grep -c overseer` → 0); it is not an
    overseer. On D it still reports EXCLUDED BUT LIVE, which is the true, open state. **That one question remains for
    the keeper.**
- **Un-excluding would have REGISTERED two overseers on L**, against the ruling that still governs L. So I did not
  un-exclude. The fix is a per-machine answer on each entry, and it changes what `-Check` **reports**, never what any run
  **registers**.

## 1 · THE CHANGE

**Manifest, `:190-205`.** The 09-06 paragraph is **kept verbatim** as a dated trace, with an `ANSWERED FOR D,
2026-09-21` block appended beneath it: the keeper's words, the source, why it reaches D and only D, and why `stop.js` is
excluded from the answer. The two overseer entries gain:

    LiveOn = @('D'); LiveOnWhy = 'keeper 2026-09-21 ~10:05 - overseers stay live on D (loop/install_D_2026-09-21.md:3)'

`Excluded` stays on all three entries.

**`-Check` classification (`:576-594`, `:641-648`, `:675`).** The machine is named by the room's one rule, the same as
`consonance/tools/state-sync.js` `machineTag()`: `CONSONANCE_MACHINE`, else `machine_tag` in `~/.consonance.json`, else
the hostname. On a machine an entry's `LiveOn` names:

| state there | reported as | red? |
|---|---|---|
| live | **EXCLUDED ELSEWHERE, LIVE HERE BY RULING** | no: it is the ruled state |
| absent | **RULED LIVE HERE, NOT REGISTERED**, since no run registers an Excluded entry | **yes**: the ruling says it should run and nothing will wire it |

On every other machine, nothing changes.

**No write path changed.** All 7 hunks are in the manifest or inside `if ($Check) {` (`git diff -U0 | grep ^@@`). Both
write paths skip any `Excluded` entry, and neither line appears in the diff (`:737` `SKIPPED --`, `:766`). **So no run,
bare or `-Only`, on any machine, registers anything new.**

## 2 · `-Check` BEFORE AND AFTER

    powershell -NoProfile -File dev/shell/install.ps1 -Check       (registration section)

| | before | after, on D | after, with `CONSONANCE_MACHINE=L` |
|---|---|---|---|
| EXCLUDED BUT LIVE | **3** (stop, l2, l3) | **1** (stop.js) | **3**, identical to before |
| EXCLUDED ELSEWHERE, LIVE HERE BY RULING | — | **2** (l2, l3), each citing today's answer | 0 |
| registration reds, summed from the printed counts | **7** | **5** | 7 |

**The L column shows the L path untouched:** same machine, same `settings.json`, only the tag changed, and the output is
the old output. `-Check` still exits 1 on D because of the other registration reds: 3 DECLARED NOT REGISTERED, 1
REGISTERED NOT DECLARED, and `stop.js`. None of those is this packet's.

## 3 · A TEST THIS CHANGE TURNS RED — ON D ONLY — AND WHY THE FIX IS IN THE TEST

    node consonance/tools/install-only.test.js      in this pane: 8 passed, 3 failed

The three that fail: *"POSITIVE CONTROL: a fresh install of this repo, checked immediately, is GREEN"*, *"a hook declared
deliberately unmanaged prints its REASON and is not red"*, *"an EXCLUDED hook that is live anyway is RED"*.

**Isolated in a fresh detached worktree at HEAD** (no untracked files, none of C's in-flight hook edits):

| worktree | `install-only.test.js` |
|---|---|
| HEAD, no D098 edit | **11 passed, 0 failed** |
| HEAD + only my `install.ps1`, `CONSONANCE_MACHINE=D` | **8 passed, 3 failed** |
| HEAD + only my `install.ps1`, `CONSONANCE_MACHINE=L` | **11 passed, 0 failed** |
| HEAD + only my `install.ps1`, variable unset | **11 passed, 0 failed** |

**The cause is one environment variable.** The test isolates `USERPROFILE` in a throwaway home (its own `:20`) but
passes the rest of `process.env` through, **and every seat the app launches carries `CONSONANCE_MACHINE=D` on D**
(`env | grep ^CONSONANCE` in this pane). My tag lookup reads that variable first, by the room's rule, so inside the
throwaway home the manifest resolves as machine D. The test's assumption, that an Excluded entry is red wherever it is
live, **stopped being true on D the moment the keeper answered for D.** Its assertions are right for every other
machine.

I checked the obvious alternative and refuted it: PowerShell's `$HOME` **does** follow `USERPROFILE`
(`HOME=…\fakehome` with `USERPROFILE` set), so `~/.consonance.json` is not leaking. The variable is the only path in.

**The fix, NOT made: `install-only.test.js` is not mine this packet.** In its `run()`, pin
`CONSONANCE_MACHINE: 'TEST-NEUTRAL'` in the child's env so the existing assertions hold everywhere. Then add one case
with `CONSONANCE_MACHINE: 'D'` asserting the overseers read **LIVE HERE BY RULING** when live and **RULED LIVE HERE, NOT
REGISTERED** when absent. **Until that lands, this `install.ps1` should not be committed alone:** on D, js-suite would
go red in every app-launched seat.

**One design consequence, stated so it is chosen rather than discovered.** On a freshly installed D, the overseers are
absent and nothing registers them, so `-Check` reads **red** until they are wired by hand. I made that red on purpose:
if they vanished from D's `settings.json`, a green `-Check` would hide the loss of hooks the keeper ruled live, which is
exactly the silent loss this installer exists to catch. The cost is that "a fresh install is immediately green" no
longer holds **on D**. If the room prefers the invariant, it is one term out of `$regBad` (`:675`).

## 4 · MY D097 MISS, FOUND WHILE ISOLATING §3

    node consonance/tools/universe-print.test.js    10 passed, 5 failed   (15/0 when I ran it in D095)

Bisected in the same worktree, swapping only `install.ps1`:

| `install.ps1` at | `universe-print.test.js` |
|---|---|
| `b718b91^` (Hold present) | **15 passed, 0 failed** |
| **`b718b91`, my D097 Hold removal** | **10 passed, 5 failed** |

**My D097 step 2 caused it and I did not catch it.** I ran every hook test, but not the tools tests that read the
manifest. The mechanism: `consonance/tools/open-items.js:284` reads the **live** `install.ps1` for the Hold flag, and
`:353` does `if (!held) return { state: 'CLOSED', detail: 'Hold flag removed from the manifest — somebody resolved it' }`.
**The tool is right: the item should close, and it did.** The five failing tests drive the item's md5 comparison against
that same live manifest and assume the Hold is still there. It is the R3 shape again: a test pinning mutable repo state,
turned red by a legitimate change.

**The fix, NOT made (not my file):** give `open-items.js`'s Hold check a fixture manifest in the test, the way
`OPEN_ITEMS_SETTINGS` already fixtures `settings.json` (`universe-print.test.js:296`), and keep one live-manifest
assertion that the item now reads **CLOSED**. The tests still pin a real property, that the item goes red when the
copies differ; they just need a manifest that still has a Hold to test it against.

## 5 · WHAT WAS NOT VERIFIED

- **Machine L.** Simulated by tag on D's `settings.json`, which shows the classification; L's own `-Check` was not run.
- **The RULED LIVE HERE, NOT REGISTERED branch.** It was not exercised live, since the overseers are registered on D and
  I did not edit `settings.json`. It needs the test in §3.
- **Whether the keeper meant `stop.js` too.** Its words name the overseers. §0.
- **The full js-suite.** The three manifest-reading suites were run: `dream-gate` 59/0, `install-only` §3,
  `universe-print` §4.
- **C's uncommitted hook edits** (the `CONSONANCE_SHELL_DIR` seam) are in the live tree and were excluded from my
  attribution by the clean worktree. They were not evaluated.

## 6 · WRONG column

- **W1, my D097 miss (§4):** removing the Hold turned `universe-print` 15/0 → 10/5, and I reported only the hook tests as
  the verification. A change to a shared manifest needed every reader of that manifest run, not just the files beside
  it.
- **W2, a measurement slip this lap:** my first test loop printed `exit=0` beside "3 failed" and "5 failed". It read `$?`
  after a command substitution inside `printf`, not the test's status. Rerun one file at a time with the exit captured
  directly, both exit 1. No conclusion was filed off the bad column.
- **W3, a hypothesis refuted before filing:** I first suspected PowerShell's `$HOME` of escaping the throwaway home. It
  does not; the variable does.

NEXT: librarian route the install-only.test.js pin (§3) and the universe-print fixture (§4) before the chair commits dev/shell/install.ps1

---

## §-D099 — BOTH TESTS FIXED, test-first, no assertion weakened; install.ps1 ready to land with them (2026-09-21 ~10:3x–11:0x, D)

Packet: the chair's D099 packet B. HEAD `ae51afd`. **Files:**
- `dev/shell/install.ps1`: the D098 edit, unchanged since;
- `consonance/tools/install-only.test.js`;
- `consonance/tools/universe-print.test.js`.

No fixture file was added to the tree: the universe-print fixture is **built at test time** in the test's own temp dir
(reason below). Nothing committed. `install.ps1` was run only with `-Check`.

> **A dispatch-order error in this very step, recorded first.** My first attempt to append this section failed: a
> shell quoting error meant nothing was written. The same turn had already rung the librarian with a pointer to it, in
> parallel instead of after the write was confirmed. For the minutes in between, the librarian held a pointer to text
> that did not exist. This is present-then-prove, the failure COMMITTEE.md measures at 98.1%. This section and the map
> line were then written through the editor, and a correction was sent.

### (1) `install-only.test.js` — the machine pinned, and D stated on purpose

**Before:** the harness isolated `USERPROFILE` and passed the rest of `process.env` through, so a seat launched by the
app on D carried `CONSONANCE_MACHINE=D` into every case.

**Change:**
- `run(repo, home, args, machine)` now sets `CONSONANCE_MACHINE: machine || 'TEST-NEUTRAL'`. The eleven existing cases
  pass no machine, so they run on a neutral one. **Their assertions are unchanged, word for word.**
- **Three D cases are added**, each passing `'D'` explicitly:
  1. both overseers live on D read **LIVE HERE BY RULING**, not EXCLUDED BUT LIVE, and `-Check` exits **0**;
  2. an overseer absent on D reads **RULED LIVE HERE, NOT REGISTERED** and `-Check` is red, **and the bare run on D
     registered neither overseer**, pinning that `LiveOn` never writes;
  3. `stop.js` live on D is **still EXCLUDED BUT LIVE**, because the answer named the overseers.

| what each asserted | before | after |
|---|---|---|
| the 11 existing cases | pass/fail depended on the pane that ran them | the same assertions, on a stated neutral machine |
| machine D | not asserted anywhere | 3 explicit cases |

**Red first:** the new test file run against HEAD's `install.ps1` (no `LiveOn`), in a detached worktree gives **12
passed, 2 failed**: D cases 1 and 2. D case 3 passes at HEAD, as it should: it is a guard that must hold on both sides,
not a red-first case.

    node consonance/tools/install-only.test.js                       after: 14 passed, 0 failed   (this D pane)
    CONSONANCE_MACHINE=L  node consonance/tools/install-only.test.js  14 passed, 0 failed
    env -u CONSONANCE_MACHINE node …                                  14 passed, 0 failed

**The pane's environment no longer changes the result.**

### (2) `universe-print.test.js` — the manifest fixtured. MY OWN MISS, from D097

**It was mine**, and I said so in §4: my D097 Hold removal (`b718b91`) took this suite from 15/0 to 10/5, and I had
verified D097 with the hook tests only.

**The cause:** `open-items.js:284` reads the Hold flag from the **live** `install.ps1`, and `:353` correctly returns
CLOSED once the flag is gone. The five tests exercise the md5 comparison that sits **behind** that flag, so with the flag
gone they hit the short-circuit and never reached what they test.

**Change:**
- `open-items.js` has test overrides for two of its inputs (`OPEN_ITEMS_SETTINGS`, `OPEN_ITEMS_TAURI_CONF`) but not the
  manifest, and **it is not my file**. So the fixture is a **temp repo tree**, built in the test's `tmp`:
  - the **real** `open-items.js`, copied;
  - the **real** `userprompt-submit.js`, which stays the fixed side, as before;
  - a manifest **identical to the live one except that the row carries `Hold = $true` again**. The row is derived from
    the live file at test time rather than hand-written, and the build **throws** if the row is missing or the Hold was
    not re-added.
- `runHold()` defaults to the fixture tool, and the five tests' assertions are **unchanged, word for word**.
- **One test added, against the live manifest:** *"the Hold was resolved (b718b91), so the item reads CLOSED and says
  why"*, asserting `CLOSED` and *"Hold flag removed"*. That is the property the short-circuit exists for, now asserted
  instead of tripped over.

**Why no checked-in fixture file:** a hand-written copy of a manifest drifts from the real one silently, and a `.ps1`
under `consonance/` would enter portable-paths' scan. Deriving the fixture from the live manifest means the five test
the real row, with only the flag restored.

| what each asserted | before | after |
|---|---|---|
| the 5 hold tests | the md5 comparison, **against whatever the live manifest said** (broken once the Hold was resolved) | the same assertions, against a manifest that has the Hold they were written for |
| the live manifest | nothing | the resolved Hold reads CLOSED, and says why |

    node consonance/tools/universe-print.test.js                  16 passed, 0 failed   (15 original + 1)

### Mutants on both test changes

    node <scratch>/d099/mutants.js and node <scratch>/d099/u1.js   (copies beside the real tool, deleted after; 0 left behind)

| mutant | result |
|---|---|
| U1 fixture manifest built **without** the Hold | **caught, loud**: the fixture's guard throws, exit 1 |
| **U1b** without the Hold **and** the guard removed | **caught: exactly the original five fail, name for name** (11/5). This proves the restored Hold is what those five need. |
| U2 the live-manifest CLOSED test pointed at the fixture | caught (15/1) |
| I1 the machine pin removed, so the pane's `D` leaks in | caught (11/3, the three original D-pane failures) |
| I2 the first D case run as the neutral machine | caught (13/1), so the D cases really do test D |

    applied 5 · caught 5 · survived 0 · NOT APPLIED 0 · controls 16/0 and 14/0 with the pane tag forced to D

**One scoring slip, corrected before filing:** the first harness run printed U1 as "SURVIVED", because the file threw
before printing a summary and my pattern looked for a named FAIL. Rerun with the exit code and the thrown message
captured: exit 1, *"fixture: Hold was not re-added"*. That is a loud catch, and U1b was added to measure what U1 was
meant to.

### Plain runs, with counts

    node consonance/tools/install-only.test.js          14 passed, 0 failed    exit 0
    node consonance/tools/universe-print.test.js        16 passed, 0 failed    exit 0
    node consonance/hooks/dream-gate.test.js            59 passed, 0 failed    exit 0
    powershell -NoProfile -File dev/shell/install.ps1 -Check        exit 1
      files: 25 ok · 0 DRIFT · 0 HOLD · 2 ABSENT (dispatch-gate.js, carrier-drift-watch.js — ruled do-not-install, D097)
      registration: 3 DECLARED NOT REGISTERED · 1 REGISTERED NOT DECLARED · 0 EXCLUDED BY RULING (absent)
                    2 EXCLUDED ELSEWHERE, LIVE HERE BY RULING (l2, l3) · 1 EXCLUDED BUT LIVE (stop.js)

`-Check`'s exit 1 is now only the pre-existing registration items and `stop.js`, whose ruling is the keeper's open
question (§0). **None of it comes from this change.**

### Ready to land, together, by named paths

`dev/shell/install.ps1`, `consonance/tools/install-only.test.js` and `consonance/tools/universe-print.test.js`, in one
commit. **Landing `install.ps1` without the `install-only` test turns it red on D; landing the tests without
`install.ps1` makes the D cases fail everywhere.**

### Not verified

- **Machine L's own run of either suite.** Simulated by tag, which is the mechanism under test; the laptop was not used.
- **`open-items.js`'s other items inside the temp tree.** They are not asserted by these tests. The copied tool runs to
  completion there (the summary prints), and only the hold row is read.
- **A cleaner fixture seam:** an `OPEN_ITEMS_MANIFEST` override in `open-items.js`, on the pattern of its existing two,
  would retire the temp-tree copy. **Named for that file's owner, not made.**

NEXT: chair commit dev/shell/install.ps1 + consonance/tools/install-only.test.js + consonance/tools/universe-print.test.js together by named paths when the librarian re-derives the three counts

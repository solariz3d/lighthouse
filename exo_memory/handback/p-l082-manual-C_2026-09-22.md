# P-L082-MANUAL — C (L082 packet C, on L, 2026-09-22 ~07:1x–07:4x) — UNCOMMITTED

**`consonance/README.md` has been brought up to the source tree.** It fixes four drifted facts and adds one section, "Landed 2026-09-21 → 2026-09-22", covering keep-warm, Jev, park-at-launch, and stop-before-write install with fast-forward ledgers and ledger-union. Each claim cites its file and line.

- Only `consonance/README.md` was edited: sha256 `f2673dfae39b96a7…`, 317 → 454 lines. The root README (E) and `ui/index.html` (A) are untouched.
- **The unverified list in §3 is the part to read.**

## 1. Drift fixed (each re-derived by the command now printed beside it)

| claim in the manual | was | is | command |
|---|---|---|---|
| `#[tauri::command]` count | 46 | **47** | `grep -c '^#\[tauri::command\]' consonance/src-tauri/src/main.rs` |
| non-test tools | 59 | **71** | `ls consonance/tools/*.js \| grep -v '\.test\.js' \| grep -v '\.mutants\.js' \| wc -l` |
| test files | 68 | **82** | `ls consonance/tools/*.test.js \| wc -l` |
| `.mutants.js` | "two" | **4** | `ls consonance/tools/*.mutants.js \| wc -l` |
| "each with a `.test.js` beside it" | true | **FALSE: five tools have none**: curate, dispatch-gate-report, l039-power, open-items, pane-status | the loop now printed in the manual |
| state set | "59.3 MB and two unplaced" | **66.8 MB TRAVELS, 0 UNDECIDED** (on L, 07:3x) | `node consonance/tools/state-manifest.js` |
| the WIRED-TO-NOTHING grep | matched 6 (tools + their tests) | the same 3 tools; the grep now excludes tests | `grep -l "WIRED TO NOTHING" consonance/tools/*.js \| grep -v '\.test\.js'` |

The Seats table's Third Place row now says that since 2026-09-22 its turns ARE read by Jev, per the keeper's ruling, and links to the new section. Without that, "holds no map of the build" read as if nothing touched it.

## 2. Added: VERIFIED against the code, line by line

Every bullet in the new section was written from the lines it cites, read in this session.

- **Keep-warm** (`main.rs`):
  - block `:10168`; `KEEP_WARM_AFTER` 50 min `:10194`; tick 60 s `:10195`; `KEEP_WARM_TEXT` `:10200`;
  - `keep_warm_decision` and its seven skip reasons `:10317-10360`, including "composer not read empty: never over the keeper's typing" `:10339` and "not activated this session" `:10352`;
  - delivered through `gate_or_queue` `:10542` (fn `:9604`); off switch `keep-warm-off.json` `:10427`;
  - the missed-ping board row via `chair_audit` `:10534`;
  - commits `1e47264`, `0f40a0c` (`git show --stat`).
- **Jev**:
  - launch: `start_jev_shadow` `main.rs:11860`, called unconditionally at `:12680` (L059 §3 comment beside it); `persist.log` is `plog`'s file (`main.rs:853`);
  - runner header `jev-shadow-runner.js:1-65`: 3 s capture, 10 min / 25 a run, lock, exit 3 (`:54`), judge mode on every machine with no switch (`:37-41`);
  - `DEFAULTS` `:64` (dailyCap 2000, shadowReserve 600, retainDays 14); `budget` `:69-76`; prune `:85`; store `%LOCALAPPDATA%\consonance\jev-shadow` `:104`;
  - key `:14`, `:143`, `:178`, scrub `:361`;
  - rows `jev-judge.js:48`, `:67` (`jev_judge.jsonl`), `:220` (`judge:'jev', unverified:true`);
  - Third Place ruling and the never-surface rule `:32-40`, with the old rule struck `:42-46`;
  - skip rule `jev-judge.js:241`, `:257`; `jev-shadow.js:168`, `:227`.
- **Park at launch**:
  - `launch.ps1:239-264`, `:356-374`; commit `68bc625` message (the 66 commits, `--ff-only`, stash rules, no push);
  - **`node --test consonance/launch.park.test.js` → 8/0**, run now.
- **Install**:
  - `state-sync.js` `installTree :1175`, `appendOnlyCompare :1151`, the pre-scan and "NOTHING WAS WRITTEN" `:1195-1211`, re-check `:1257`, `refusalWhy :1297` (names row counts per path);
  - `state-manifest.json`: **11** `"install": "fast-forward"` rows, listed by glob (`grep -c '"install": "fast-forward"' consonance/state-manifest.json`);
  - `b40c8d8` / `6b9699b` subjects.
- **ledger-union**: header `:1-40` (sources, whole-row key, dry run default, `--write --file`, `.pre-union-<stamp>`, board text never printed); `FILES :66-78` = the same 11.

## 3. NOT verified: read, left as it was, and NOT checked against code this lap

- **"The running binary is older than this file."** I did not compare the exe's timestamp.
- **Why it exists:** the three links exist (`test -e`). I did not reread their content.
- **The Seats table's spawn command names** (`spawn_main`, `spawn_librarian`, `spawn_third_place`, `committee_form`, `spawn_sibling`, `audio_start`) and the Listen file list were not grepped. Only the command COUNT was re-derived.
- **"Panes come back into their own conversation"**:
  - The 9/9 and 6/6 resume measurements and version numbers were not re-checked.
  - `fn resume_pane` is **not** found by `grep -n "fn resume_pane"`. The name appears only in comments (`main.rs:1182`, `:2719`, `:3676`), so the manual's pointer "`fn resume_pane`, the P1b block" may be stale. **A likely carrier; not fixed, not confirmed.**
- **The loop diagram**: not diffed against `brief/BUILDING.md`, its stated master.
- **Verbs**: the count is 10, re-derived. Which seat may call which verb was not checked. `ADDRESS_TABLE` exists (`main.rs:9978`); "the table is two rows" was not checked.
- **Board phases**: not checked.
- **Instruments table**: its five commands were not run this lap. carrier-drift exits 0 now, and js-suite ran earlier today.
- **Hooks**: the count of 14 was re-derived. The 199-turns / zero-reads measurement was not.
- **The librarian's shelf**: the constants exist (`main.rs:7001`). The 150,000 value, the tiering rule and the 6-of-60 flake figure were not checked.
- **Dream cycle**: `ls dreams` fails, as stated. Nothing else was checked.
- **Two machines**: only the state-manifest figures were re-run. The four bullets and the "also landed" paragraph were not checked.
- **Interface**: 7 tabs re-derived. The frontend file list was not.
- **Build**: not run.
- **Jev behaviour live**:
  - I did not watch a run, did not open `jev_judge.jsonl`, and did not check that D is building the pair count. The manual says it is, from the runner's header.
  - The librarian's 07:1x says keep-warm was "verified 06:3x"; I did not re-observe it.
- **Something the manual does not cover**: the unattended ten-lap run of 2026-09-19 and T-J1. The librarian lists both as new since 09-14. They belong to the root README (E), and the packet didn't name them for this file.

## 4. Checks run after the edit

| check | result |
|---|---|
| `node consonance/tools/carrier-drift.js` | exit 0 |
| `node consonance/tools/portable-paths.js` | exit 0 |
| `gen-consumer.test` | 60/0 |
| `librarian-cite.test` | 23/0 |
| `shelf-recursion.test` | 11/0 |
| `carrier-drift.test` | 57/0 |
| `cd consonance/src-tauri && cargo test --test arch_test -- --test-threads=1` | 12/1 |

The four JS tests are the ones that read README files.

The arch_test failure is `every_named_record_file_exists_and_every_record_file_is_named`: *"record/retired_seats_2026-09-11.md is named by no card"* (`arch_test.rs:550`). **It is not this edit:**
- `exo_memory/record/` and `exo_memory/cards/` are unchanged from HEAD (`git diff --quiet HEAD -- exo_memory/record exo_memory/cards`).
- No card names that file (`grep -l retired_seats_2026-09-11 exo_memory/cards/*.md` prints nothing).
- The file came in with `4292c6b` (CHAIR).
- **It is an open red with no owner.** It was not in this packet, so I did not fix it.

NOT COMMITTED. The chair lands it.

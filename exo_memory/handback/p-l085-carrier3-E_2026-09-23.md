# P-L085-CARRIER3 — the third carrier repaired, repo text only, with D's refresh step (pane E, L085 (2), 2026-09-23)

Packet: the chair's L085 (2). **Machine L**, HEAD `2403a81`. Started 01:25:11 (`date +%T`). Authorized under
`loop/plan_after_upgrade_2026-09-22.md` §AUTHORIZED item 4 (`d5bd9b1`). **Repo text only: no install, no restart,
nothing committed.** *This file names the withdrawn wording only by its registry id,
`light-not-lifeguard-2026-08-17` (B's rule, `ea779ca`), so it cannot become a carrier itself.*

## 0 · THE REPAIR

`dev/shell/hooks/userprompt-submit.js`, `buildContext()`. **The injected line is now `:285`** (was `:277`) and
carries BOOT's public form, **"With you, not above you"**, exactly as `session-start.js` got in L083 (`2403a81`). A
**dated comment at `:271–278`** *(I first wrote `:265–272` here — corrected by reading the file back before filing)* keeps the retracted wording as the trace (the registry marker `retired 2026-08-17`
is on its line), cites the id and the authorization, gives the same reason for the form as L083 (it keeps the one
contrast the old phrase made — beside the user, not above), and says the file is DO-NOT-INSTALL on L and where the
refresh step lives.

## 1 · RED FIRST, BY RUNNING THE HOOK ITSELF — no test covered this line, so a probe did

No test pins the old wording and none reached this line: `grep -rln -i lifeguard --include=*.test.js` hits only
`carrier-drift.test.js` and `memory-sweep.test.js`, which build their own fixtures and never touch this hook. **So I
ran the real hook.** A scratch probe (`scratchpad/l085probe.js`) mirrors `userprompt-submit.test.js`'s own `runHook`:
- a sandboxed `CONSONANCE_SHELL_DIR`;
- one fresh `l3_overseer_verdict` (trajectory `deepening`) seeded into `l3_overseer.jsonl`;
- a room cwd.

It checks the stdout against the **registry's own pattern, read by id**, and it carries a **control**: if the L3
section does not print, it exits 2, because the two checks would otherwise be vacuous.

    before the repair → {"exit":0,"l3_section_present":true,"injects_retired_phrase":true, "injects_public_form":false}   probe exit 1  (RED)
    after the repair  → {"exit":0,"l3_section_present":true,"injects_retired_phrase":false,"injects_public_form":true}   probe exit 0  (GREEN)

Every remaining hit of the pattern in the file is a comment carrying the registry marker: `:273` and `:274`, both
MARKED (checked with the entry's own `pattern` and `marker` regexes). `node --check` → ok.

**I did not add the probe to `userprompt-submit.test.js`.** The packet gives me that test only "if one pins the
line", and none does. **It would be a good test to have**, since the L3 injection path has no coverage at all.
Named for whoever owns that file.

## 2 · CHECKS, WITH COUNTS

    tests referencing the hook (grep -rln userprompt-submit --include=*.test.js):
      userprompt-submit.test.js · shell-dir-seam.test.js · dream-gate.test.js · universe-print.test.js
    node dev/shell/hooks/userprompt-submit.test.js      → pass 5 · fail 0
    node dev/shell/hooks/shell-dir-seam.test.js         → pass 4 · fail 0
    node consonance/hooks/dream-gate.test.js            → 57 passed, 0 failed
    node consonance/tools/universe-print.test.js        → 16 passed, 0 failed
    node consonance/tools/carrier-drift.test.js         → pass 57 · fail 0
    node consonance/tools/carrier-drift.js              → GREEN · exit 0
    parallel: node --test userprompt-submit.test.js shell-dir-seam.test.js carrier-drift.test.js → tests 66 · pass 66 · fail 0
    filtered: node --test --test-name-pattern=CONTROL userprompt-submit.test.js                 → tests 2 · pass 2 · fail 0

**Registry:** in `light-not-lifeguard-2026-08-17`.`instruction_carriers_outside_corpus` (the field L083 added, outside
`sites` because the tool scans `.md`/`.html` only and a `.js` site is MISSING-FILE), **the row I left OPEN at `:277` in
L083 is now REPAIRED at `:285`**, with how it was checked, and a **TRACE** row at `:274` beside it. The file still
round-trips through `JSON.stringify(o, null, 1) + '\n'`, and `git diff --stat` shows **+10 / −3**. **No withdrawn
wording is restated in the field** (checked).

## 3 · WHICH MACHINE RUNS THIS HOOK — and D's refresh step, verbatim

**Not L.** `~/.claude/settings.json` on L wires only `userprompt_pulse.py` at UserPromptSubmit (`:33`); an installed
copy of this hook exists (15,282 B, Aug 25), and nothing runs it. The ruling `loop/absent_hooks_ruling_2026-08-25.md`
row 4 marks it **DO NOT INSTALL — "and cannot be installed by this installer"**: it carries `Hold = $true`, which
blocks the copy in both modes (ruling, "one of the four cannot be installed…"). **D runs it** — the librarian's
reading, and consistent with the installer's own text that on the desktop, if it runs the Node pulse, `-Check`
reports it REGISTERED, NOT DECLARED. **I cannot see D from here**, so "D runs it" is the librarian's reading, not my
check.

**So the repair reaches no running seat until D's INSTALLED copy is refreshed on D.** A whole-file copy from the repo
is **the wrong step**: the same ruling measured the machine copy and the repo **83 lines apart** (2026-08-17), so a
copy could change far more than this one line. **The step below changes that one line and nothing else**, reading the
pattern from the registry by id so it never spells the wording:

**Run on D, in Git Bash, with D's repo pulled to include L085 (only the registry's pattern is used, and that entry
has existed since 2026-09-02):**

    node -e 'const fs=require("fs"),os=require("os"),p=require("path");const f=process.env.F||p.join(os.homedir(),".claude","shell","hooks","userprompt-submit.js"),b=f+".pre-refresh-2026-09-23";const w=require(process.env.R||p.join(os.homedir(),"Desktop","lighthouse","consonance","tools","carrier-drift.registry.json")).withdrawals.find(x=>x.id==="light-not-lifeguard-2026-08-17");const re=new RegExp("[*][*](?:"+w.pattern+")[*][*]","gi");const t=fs.readFileSync(f,"utf8");const n=(t.match(re)||[]).length;if(n!==1)throw new Error("expected exactly 1 match, found "+n+"; nothing written");if(fs.existsSync(b))throw new Error(b+" exists; nothing written");fs.copyFileSync(f,b);fs.writeFileSync(f,t.replace(re,"**With you, not above you**"));console.log("refreshed; backup at "+b)'

**What it does, so the person running it can check it:**
- it writes **nothing** unless the installed file has **exactly one** match of the registry pattern wrapped in `**`;
- it refuses if its backup `userprompt-submit.js.pre-refresh-2026-09-23` already exists;
- it backs up **before** writing, then replaces that one span and prints where the backup is.

**`F`/`R` are optional test overrides; left unset, it uses D's home directory.** (JS string sha256 of the command's
body: `2beb25b2…0651`.)

**Tested here, on a scratch COPY of L's installed file** (`F`/`R` pointed at scratch and at L's registry):
- **run 1:** "refreshed", with the backup made; `diff backup refreshed` shows **exactly one line changed**, at `264`;
- **run 2:** refused — "expected exactly 1 match, found 0; nothing written".

**Not tested on D.** D's installed copy may differ in other lines too, but the step touches only this one.
**Afterwards on D:** `grep -c "With you, not above you" ~/.claude/shell/hooks/userprompt-submit.js` → 1.

## 4 · SAID PLAINLY — something the ruling says about L083, which I only saw this lap

The same ruling (`absent_hooks_ruling_2026-08-25.md`) also marks **row 3, `hooks\session-start.js`**, and **row 9,
`hooks\l2-overseer-worker.js`**, **DO NOT INSTALL** — the two files L083 installed on L at the packet's order. What L
actually looks like:
- `session-start.js` was **already installed and already wired** at SessionStart before L083 (`settings.json:22`);
- `install.ps1 -Check` reports both as managed and `ok`;
- `install.ps1:963` still cites the ruling as current for *"eleven ABSENT hooks/ and lib/ files"*.

**So L083 refreshed files that were already there. It did not install something the ruling forbids onto a machine
that lacked it. But the ruling and L's real state disagree about these two files, and that is not mine to resolve.**
Named here because I should have read the ruling before L083's install and did not.

## 5 · NOT VERIFIED

- **That D actually runs this hook** — the librarian's reading; D is not visible from L.
- **The refresh step on D itself** — tested on a copy of L's installed file, whose line sits at 264; D's may sit
  elsewhere, and the step does not depend on the line number.
- **That the refresh leaves D's hook otherwise working** — it changes one string literal. `node --check` on D after
  running it is the one-command check, and I have named it rather than run it.

NEXT: librarian call_librarian with the hand-back pointer when the repair and checks are written — plan default after it: items 3 + 2 (jev-flags hook, "solid" sheet), unless the output says otherwise

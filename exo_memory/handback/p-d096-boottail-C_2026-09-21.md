# P-D096-BOOTTAIL — C (CHARLIE), D096 chunk 1, on D — BOOT's pointer tail out of the master

**Packet:** move `exo_memory/BOOT.md` from `**Latest entry:**` to the end, verbatim, into a journal index, leaving one line.
Consumers first. Test first. No rebuild. **Done: moved.** Nothing committed.

## 0 · THE ANSWER

1. **Moved, verbatim.** BOOT **64,976 → 40,029 B** (−24,947). The 25,368 B tail is now in
   **`exo_memory/journal/POINTERS.md`**, after an 806 B header, and it is **byte-identical** to where it stood (`cmp`,
   §2). BOOT's first 39,608 B are also byte-identical to HEAD. Nothing was reworded or deleted.
2. **Who wakes differently, which is the point, stated as asked:** **Main, the Third Place and the librarian** each paste
   the master **whole** (`main_intake` `main.rs:6693`, `third_place_intake` `:6860`, `librarian_intake_head` `:7757`).
   **Each now wakes 24,947 B lighter.** The **panes** had not carried the tail since `38fd239` (B, L025, 09-01), because
   `assemble_intake` splits it off and puts a path index in its place. Their 653 B index paragraph becomes the 419 B pointer
   line, so each pane is about 234 B lighter. **This takes effect at each seat's next wake, on the running binary:**
   intake reads BOOT at assembly, so no rebuild is needed.
3. **The bars:**
   - Rust **1,086/1 → 1,088/1**: 2 new tests, and the 1 red is pre-existing and unrelated.
   - js-suite **105/5 → 105/5**, the same five files.
   - carrier-drift **RED 2 → RED 2**, the same two `MISSING-FILE review/…` findings.
   - The **bundled brief does not change**: `gen-brief` on the new master reproduces the committed `brief/BOOT.md`
     byte for byte.
4. **Seven consumers, not four.** The plan's four, the chair's fifth, and two found by grep: `gen-brief.ps1` (which
   **throws** without a `**Latest entry:**` line) and `exo_memory/new_entry.py` (whose instruction is how the tail grew).

## 1 · FILES — every one I touched

| file | change |
|---|---|
| `exo_memory/BOOT.md` | tail (lines 160–166) replaced by ONE line starting `**Latest entry:**`; trailing `\n\n` kept (§4.3) |
| `exo_memory/journal/POINTERS.md` | **new**, 26,174 B = 806 B header + the 25,368 B tail verbatim |
| `consonance/src-tauri/tests/arch_test.rs` | +1 test, `the_master_keeps_one_pointer_line_and_the_dated_tail_lives_in_the_journal_index` |
| `consonance/src-tauri/src/main.rs` | +1 test, `no_seat_that_pastes_the_master_whole_wakes_carrying_the_dated_tail` (in `committee_brief_tests`) |
| `consonance/tools/carrier-drift.registry.json` | −1 site (7 lines): `only-decorrelated-2026-08-16` site on BOOT's `**Previous:**` pointer (§3.4) |
| `exo_memory/new_entry.py` | docstring and final print now say *add the pointer to journal/POINTERS.md, not BOOT.md* |

**Not touched:** `brief/BOOT.md` (§3.3), `gen-brief.ps1`, `split_pointer_tail`/`journal_pointer_index` and their four
fixture tests, `exo_memory/loop/boot_usage_split_scan.js` (a dated scan, a trace), any seat's CLAUDE.md, the stick.

sha256 (first 16): BOOT `d050f4de85f6460f` · POINTERS `93a468a96ef7bbbd` · new_entry.py `538000c134f61d33` · main.rs
`5c2a2960012241c4` · arch_test.rs `3fa046e2cc4cf880` · registry `d0f2876ac28149d1`.

## 2 · THE MOVE, proved

```
tail -c 25368 exo_memory/journal/POINTERS.md | cmp - <(git show HEAD:exo_memory/BOOT.md | tail -c 25368)   # IDENTICAL
head -c 39608 exo_memory/BOOT.md             | cmp - <(git show HEAD:exo_memory/BOOT.md | head -c 39608)   # IDENTICAL
```

The plan's figures re-derived on D before touching anything: `wc -c` → 64,976; the tail from line 160 of 166 →
25,368 B. The cut is at the first `\n**Latest entry:**`, asserted to occur exactly once. The move ran as a byte-level
script (`scratchpad/d096/move.js`) that refuses to overwrite an existing `POINTERS.md` and refuses any split other than
39,608 / 25,368.

**The line left in BOOT** (419 B) says the three paragraphs moved verbatim on 2026-09-21 to `journal/POINTERS.md`. It
says the `**Previous:**` pointer the 2026-08-23 amendment cites at `:153` is there now. And it says a new entry's pointer
goes in that file, never back in BOOT. The line is written so that `split_pointer_tail` does **not** take it as a tail
paragraph (it has no `.md (`), so it rides in the body for every seat.

**Found while reading, not fixed:** the 08-23 amendment's `:153` was already stale *before* this move. The `**Previous:**`
line stood at `:162` (`grep -n "^\*\*Previous:\*\*"` → 162). The amendment is body text and keeps its wording; the new
line forwards the reference to where the paragraph now lives.

## 3 · THE CONSUMERS, one by one

**3.1 `main.rs:13092-13101`, the tail test the plan names.** It is a **fixture** test. It splits a `const BOOT` literal
defined inside the test, not the real master, so it describes `split_pointer_tail`'s behaviour, not BOOT's layout. It is
not describing the old layout, so by the plan's own condition **it is left unchanged**. It is green, and so are its three
siblings.

**However,** the delivery-point test `the_journal_pointer_tail_is_indexed_rather_than_pasted` (`main.rs:14128`) reads the
**live** master and returns early when there is no tail (*"nothing to assert, and not a failure"*). **It now passes
vacuously** on this master. That is correct, because there is nothing to index, and the new arch test is what holds the
shape now. I'm stating it so nobody counts it as coverage.

**3.2 `arch_test.rs:581` (`SHIP_OMITS`).** It skips master heading lines that start `**Latest entry:**` / `**Previous:**`.
The one line left still starts `**Latest entry:**`, so it is still skipped. Removing heading lines from the master can only
shrink what that test compares. **Unchanged, green** (it is in the 11 that pass).

**3.3 `brief/BOOT.md`, the bundled copy.** "Already differs on D": **by design, not by staleness.** `gen-brief.ps1` run
on HEAD's master, in a scratch tree of the same shape so the bundled file is never written (`scratchpad/d096/genbrief.sh`),
reproduces the committed brief **exactly**: 39,634 B, sha256 `98c6441c8750107b`, 0 diff lines. The difference is the
generator's own transformations (name generalised, traces and active-builds cut, the pointers collapsed to *"none yet"*).
**The ship never carried the tail, so it does not move** (the chair's "only if both carry it"). Run on the NEW master, the
generator again gives `98c6441c8750107b`, byte-identical, **so the brief needs no regeneration.**

**3.4 carrier-drift.** The move alone gave **RED 3**, adding `STALE-SITE exo_memory/BOOT.md [CH-4]`. The registry had an
`acknowledged` site of entry `only-decorrelated-2026-08-16`, anchored on the `**Previous:**` pointer's wording. Its `why` says the row exists **because** that paragraph was *"a dated trace inside a live carrier"*,
left standing on purpose. The tool's own message names two actions: delete if the wording left the carrier, re-anchor if
the text moved within it. **The wording left the carrier, unchanged, for a trace.** `exo_memory/journal/` is in
`TRACE_PREFIXES` (`carrier-drift.js:99`), so re-anchoring there would give `MISSING-FILE` ("now classified a trace").
**So the site is deleted.** The room's decision that the pointer keeps its wording is kept in full, in `POINTERS.md`.

| | before | move only | after |
|---|---|---|---|
| RED | 2 (both `MISSING-FILE review/`) | **3** (+ `STALE-SITE BOOT`) | **2, the same two** |
| PENDING | 41 | 41 | 41 |
| traces skipped | 48 | 49 | 49 (+ POINTERS.md) |
| occurrences in carriers | 31 / 31 accounted | 30 / 30 | 30 / 30 |

The last two rows are counters, not findings: the one moved occurrence now sits in a trace.

**3.5 The fifth consumer (the chair's): what builds seat CLAUDE.md files.** All four in `main.rs`:
`assemble_intake` (panes) splits and indexes; `main_intake`, `third_place_intake` and `librarian_intake_head` paste the
master whole. Measured on disk: `C:\Consonance\instances\main\CLAUDE.md`, 126,646 B, written 08:47 today, has
`**Latest entry:**` at line 168 and 0 index lines. My own pane shell has the 653 B index paragraph instead. See §0.2 for
what changes and when.

**3.6 Found by grep, not in the plan:**
- **`gen-brief.ps1:88-89`** throws on a master with no `**Latest entry:**` line, and `:200` requires exactly one
  "none yet" in its output. That is **why the one line keeps that prefix.** Mutant M2 (§4) runs the real generator on a
  master without the line: `gen-brief: no '**Latest entry:**' line in BOOT.md; refusing to write…`.
- **`exo_memory/new_entry.py`** told every new entry to *"update the 'Latest entry' line in BOOT.md"*, which is how the
  master's tail grew. Changed to name `journal/POINTERS.md`. It parses; not run (it creates today's journal file).
- `exo_memory/loop/boot_usage_split_scan.js:92` splits BOOT at `**Latest entry:**`. It is a dated measurement script, left
  as a trace. Re-run now, it would find the line and split at it; not run.
- `dev/one-shot/carriers-2026-08-23.js` quotes the `**Previous:**` phrase. It is a one-shot trace (carrier-drift's own
  trace list); untouched.

## 4 · TESTS: red before, green after, run plain

**4.1 Red first.** Both new tests were written and run before the move:
`cargo test --no-fail-fast -- the_master_keeps_one_pointer_line no_seat_that_pastes_the_master_whole`
- `no_seat…` panicked with *"the main intake still carries the master's dated **Previous:** pointer paragraph"*;
- `the_master_keeps…` panicked with *"a dated pointer paragraph is back in the master"*.

Both then passed after the move.

**4.2 Suites, plain, before and after:**

| suite | before | after |
|---|---|---|
| `cargo test` (src-tauri) | **1,086 passed / 1 failed** | **1,088 passed / 1 failed** |
| `node consonance/tools/js-suite.js` | **105 green · 5 failed** · 1 canary (of 111) | **105 green · 5 failed** · 1 canary, **same five files** |
| `node consonance/tools/carrier-drift.js` | RED 2 | RED 2, same findings (§3.4) |

- **The cargo red, both runs:** `arch_test::every_named_record_file_exists_and_every_record_file_is_named`, panicking
  with *"record/retired_seats_2026-09-11.md is named by no card"*. It is pre-existing and unrelated. Plain `cargo test`
  stops at the first failing binary, but `arch_test` is the only integration binary (`ls tests/`), so nothing was hidden.
- **The five js reds, both runs:** `actors.evidence`, `carrier-drift.test` and `gen-consumer.test` (both assert on the
  absent `exo_memory/review/`), `userprompt_pulse`, and `stick-waiter` (the SWEEP 8→7 from A's `3d89dfb`, handed back in
  L070 on L). None is from this lap.

**4.3 A self-correction inside the lap.** The first cut left the master ending in one `\n`. HEAD's master ended in
`\n\n\n`, and those two blank lines went into `POINTERS.md` with the verbatim tail. `gen-brief` on that master came out
**2 bytes short** (39,632 vs 39,634; the first 39,632 identical), which would have been a whitespace diff waiting for the
next person to run it. I restored the two trailing newlines to BOOT, and the generator is byte-identical again. The
cargo-after run preceded this whitespace fix; the final run after it is the 1,088/1 above.

**4.4 Mutants: arch guard, on scratch copies.** The live BOOT was never mutated, because a seat waking mid-run would read
the mutant. The built `arch_test` binary resolves `../../exo_memory/*` against its cwd, so it ran from a scratch tree.
Run: `bash scratchpad/d096/mutants.sh` → `mutants.txt`. Control: 1 passed.

| mutant | applied | result |
|---|---|---|
| M1 the tail back in the master (HEAD's BOOT) | yes | **CAUGHT** |
| M2 the one pointer line removed (and `gen-brief` throws) | yes | **CAUGHT** |
| M3 the line names a different file | yes | **CAUGHT** |
| M4 the index file missing | yes | **CAUGHT** |
| M5 the index lost the leading paragraph | yes | **CAUGHT** |
| M6 a pointer appended the old way (`**Previous:** journal/…`) | yes | **CAUGHT** |
| M7 a second `**Latest entry:**` line | yes | **CAUGHT** |

**7 applied / 7 caught / 0 NOT APPLIED.** The delivery test (`no_seat…`) was not mutated separately; its red-before run
against HEAD's master is its M1.

## 5 · NOT VERIFIED

1. **A seat actually waking on the new master.** The intake assemblers were exercised by the Rust test (`main_intake`
   and the two optional seats, under `set_dirs(&get_state())`). No real seat was relaunched: no rebuild, and a relaunch
   isn't mine. `THIRD_PLACE.md` and `LIBRARIAN.md` both exist in `consonance/src-tauri/brief/`, the last tier of
   `room_brief_at` (`main.rs:3400`), so both intakes resolve to `Some` and were asserted, not skipped. That is inferred
   from the resolution order; the test does not print it.
2. **L.** Everything is on D. L's BOOT is the same file through git, but the move reaches L only after commit and pull.
   L's seats wake unchanged until then.
3. **`new_entry.py` was not run**, only parsed, because running it would create today's journal file.
4. **Other readers outside `.rs/.js/.json/.ps1/.ts/.py`** (for example `.md` instructions telling a reader to "see the
   Latest entry line in BOOT") were not swept. `grep -rn "Latest entry" --include=*.md` was not run over prose.

## 6 · COMMANDS

```
wc -c exo_memory/BOOT.md; grep -n "^\*\*Latest entry:\*\*\|^\*\*Previous:\*\*\|Superseded pointer" exo_memory/BOOT.md
grep -rnF -e "Latest entry" -e "Superseded pointer" -e "**Previous:**" --include=*.rs --include=*.js --include=*.json --include=*.ps1 --include=*.py .
node scratchpad/d096/move.js                      # the move (refuses to overwrite; asserts 39608/25368)
bash scratchpad/d096/genbrief.sh <master> <label> # gen-brief in a scratch tree; bundled brief never written
bash scratchpad/d096/mutants.sh                   # 7 mutants on scratch copies
cargo test   (in consonance/src-tauri, PATH += C:\Users\nname\.cargo\bin)
node consonance/tools/js-suite.js ; node consonance/tools/carrier-drift.js
```

## 7 · ADDENDUM — the hand-back minted the third red (at the chair's word, 2026-09-21)

The librarian's re-derivation read **RED 3**, not 2. The third finding was `UNACCOUNTED` at **this file's line 88**: §3.4
quoted the withdrawn wording to explain why the row registering it could go. That is the report-mints-a-carrier
recursion, the same as E's L060 consent file. **My §4.2 "RED 2 after" was measured before this file existed, so it was
true when measured and false once I filed.** The check ran before the last carrier was written.

One line reworded, nothing else touched: §3.4 now cites the entry id `only-decorrelated-2026-08-16`, which the pattern
does not match, instead of quoting its anchor.

| `node consonance/tools/carrier-drift.js`, plain | RED | findings |
|---|---|---|
| before the reword | **3** | `UNACCOUNTED p-d096-boottail-C_2026-09-21.md:88` + 2 × `MISSING-FILE exo_memory/review/tool_audit_draft_2026-09-07.md` |
| after the reword | **2** | the 2 × `MISSING-FILE review/` only |

PENDING stays 41 (the disarmed entry, not red).

**And it happened again, in this addendum.** Its first draft ended with a check command that spelled out the withdrawn
phrase in order to count it, and carrier-drift went **RED 3** at this file's line 207. The command was replaced by this
paragraph. The rule it teaches: **a check command is prose too.** Cite the entry id, and let `carrier-drift.js` be the
check. **The final plain run, after this paragraph was written, is the one that counts: RED 2**, the two `MISSING-FILE
review/` rows.

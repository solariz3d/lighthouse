# P-D135-RUN2PREP · BRAVO — STOPPED at task authoring, as the packet instructs. Hand-authoring on D can't be kept away from the subjects: it leaks through the author's session transcript, and A5 then voids the task. The fix is measured (non-persisting `claude -p`) and needs the keeper's yes. The §2 scan is built, with its positive control FAILING. §9 packet written. No sealed row, because no key exists

**B (pane `12fb81f6`), machine D, 2026-09-24 ~19:5x–20:3x local.** Packet: the chair's D135, S2 of
`plan_pane_specialization_2026-09-24.md` (`4061210`), done by B alone.
- **No task text, key, plant or answer exists anywhere**, so none is in this file, on the board, in the ring, in the map line, or in my replies.
- Run 2 not run. Nothing shown to A, C or E. No settings edit, no restart, no close, no publish. **Nothing committed.**

## 0 · FILES

| file | what |
|---|---|
| `exo_memory/loop/pane_battery_run2_DRAFT_2026-09-19.md` | **+95 −0** (`git diff --numstat`): dated markers in §1, §2, §3, §5, §6 and §8, and a new §12 — E's A1–A12 adopted (pointed to, not restated), A13 proposed, §2 as built, §8 restated |
| `exo_memory/loop/pane_battery_run2_s9_2026-09-24.md` | **NEW — the §9 packet** for the keeper: the eight items, each READY or NOT READY with its reason |
| **sealed-row file** | **NONE, deliberately.** There is no key to seal; a sealed row for nothing would read like a seal. It's written when keys exist. |
| off-repo working dir | `<B scratchpad>/d135/`: the scan tool, its controls and three probe scripts. **No task content is there either**, because none exists. |

## 1 · WHAT I FOUND FIRST: the draft holds no task, so the job was to BUILD them

The draft says so (`:8`), and it has been on origin since 09-19, so its text leaks nothing. S2 therefore meant writing
eight to twelve tasks with keys, then cold-reading and scanning them. **E's attack (`p-battery-run2-attack-E_2026-09-19.md`)
had never been folded in**, and its A1–A5 are blocking, so I adopted A1–A12 in place first.

## 2 · WHY I STOPPED AT AUTHORING (the packet: "if any piece of the draft can't be kept away from them, stop and say which")

**The piece:** the **authoring** of every task, key and planted object, and the **pre-dispatch scoring** of cold readers.

- **A5 (adopted, blocking)** runs the void-grep over **every `~/.claude/projects/*/*.jsonl` and every capture**, and requires keys to be printed only as sha256.
- A seat's own transcript records its writes and reads verbatim. **Measured:** mine is 46.6 MB with **233** Write calls, content included (`grep -c '"name":"Write"'`).
- **So a task I author is readable by A, C and E, who run with skipped permissions, AND it is a certain void-grep hit.** The same holds for the librarian scoring a cold reader before dispatch. §1 of the draft had the pane scorer score the cold reader; that can't happen before dispatch without the leak.

**The route that works, MEASURED (`<B scratch>/d135/probe.js`, `claude` 2.1.282):**
- `claude -p ... --no-session-persistence` from a fresh temp dir leaves **no `.jsonl` anywhere under `~/.claude/projects/`**.
- **The control, the same call without the flag, leaves one.** (A project dir is still created; it stays empty.)

**A13 (proposed in the draft's §12.2):** author, cold-read and cold-score only through such isolated processes, into the
off-repo dir, with the object and key in separate subdirectories (A5). The running seat sees only sha256 digests and counts.

**Why I didn't just start it:** it's a new mechanism, not the draft's, and it spends **roughly 1–3M tokens of the keeper's
usage before any subject runs** (hand-made estimate, draft §12.4; A2 alone doubles the cold block). That makes it the
keeper's call. The §9 packet puts it to him in its opening box.

## 3 · WHAT IS DONE

- **§2, the scan: BUILT, and the positive control FAILS as it must.** `<B scratch>/d135/scan.js` implements §2.1 with A10 (FAIL-3 on locus) and A11. It never prints brief or criterion text: only the kind, the line number, the criterion id and a count.
  - `node <B scratch>/d135/controls.js` → **5 of 5 as expected**:
    - **positive (`essay/recognition/prompts/primed1.txt` vs A4): exit 1, FAIL-2 at line 3, criterion A4-2** — the "objection … own reading" line, as A11 corrected;
    - A3's case: lexical pass (the floor);
    - negative: pass;
    - locus: fail;
    - a class word ("path"): pass.
- **The cold route and #87590 (the packet's question):** `<B scratch>/d135/probe2.js`, isolated and non-persisting, asked whether a "Verification & Testing" section is in its instructions. It answered **YES and quoted the first bullet verbatim**, so the global `~/.claude/CLAUDE.md` still loads.
  - **Does it matter for a cold reader of these tasks? No, for the control's purpose.** Every subject pane loads the same file, so cold vs pane still differs by exactly the room, which is what §1 measures.
  - It does make the cold reader a better test writer on T1/T5, which errs toward dropping items, not keeping free ones.
  - It would matter only if the goal were a blank reader; the draft's is not.
- **The draft amended in place**, dated; **the §9 packet written** with items 2, 5, 6 and 7 READY, and 1, 3, 4 and 8 NOT READY, each with its reason.

## 4 · FOR WHOEVER GOES NEXT

- **If the keeper says yes to A13:** the non-persisting author writes the tasks and keys into `<off-repo>/obj/` and `<off-repo>/key/` separately, printing only digests. Then two non-persisting cold readers per task (A2), a non-persisting cold scorer, the scan (printing ids and line numbers only), and a non-subject semantic mark (A3) by a non-persisting process too. **Then** the one-file sealed rows are landed and pushed, the void-grep runs over A5's full scope, and only then the §9 packet is completed. The scan tool is committed when the chair adopts run 2.
- **If he says no:** the honest alternative is to author on a machine the subjects can't reach (L, with the subjects on D), and to accept path EXPOSURE checks after the fact as A5 already does. **Not measured here.**
- **Load:** his choice of the two real items (§9.4), and whether a, b and c per kind (A8) or a and b only.

## 5 · WRONG column

- **None of my claims was withdrawn this lap.** One near-miss: I began reading the task as "run the draft's §1–§3", and only reading E's attack showed that A5 makes authoring the blocking step. I stopped before writing any task, so nothing went into a transcript.
- **W1 — the shell-string class, once more.** My map line went out through a bash `node -e` with backticks in it, which bash read as command substitution, so the line landed **without its pointer**. It was repaired by a script (`<B scratch>/d135/mapfix.js`, which replaces only that one line), and the map now carries the pointer.
- **The probes left empty project dirs** under `~/.claude/projects/` (`C--Users-nname-AppData-Local-Temp-d135probe-*`, and the control's one `.jsonl`). Their content is "Reply with exactly: ok" and a question about a CLAUDE.md heading, with no task content. **Left, not deleted:** deletion outside scratch is not mine to do.

## 6 · NOT VERIFIED

- **That `--no-session-persistence` also leaves no trace in any other store** (captures are the Consonance app's, for its own panes; a `claude -p` child has none). Only `~/.claude/projects` was measured.
- **The 1–3M cost:** an estimate, with no run behind it.
- **A3's semantic mark:** specified, not built.

NEXT: librarian call_librarian with the hand-back pointer when the §9 packet and the sealed row are ready — plan default after it: the keeper reads §9 (S3), unless the output says otherwise

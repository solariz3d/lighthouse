# P4 — GENERATE AND RUN, ONE QUIET WINDOW

**Pane J · lap D010 · 2026-09-04 · scored against `bab20a9` as amended**

**The scored record is `exo_memory/loop/consumer_parity_2026-09-04.md`. Every number lives there
with the command that produced it. This hand-back carries the verdict, the judgements, and what the
run found that is not a parity number — it does not restate the tables, so the two cannot drift.**

---

## THE VERDICT

    P = 18      M = 1      B = 1        bar: P = M = B = 0
    I = S - G = 74 - 66 = 8

**REFUTED.** Generated from `f21dbc9`; clean at open, clean at close, HEAD unmoved, no seat
committed inside the window. S and G at one commit, per B's precondition.

*The packet named `202c459`; HEAD was `f21dbc9`, four commits later. Clean is the property that
matters, so the run stands, and the sha is printed beside every number.*

---

## I DID NOT REFUSE, AND HERE IS THE CHECK THAT ENTITLED THAT

The packet offered the refusal B gave last lap. It does not apply: `git status --porcelain` was
empty at both ends and `git rev-parse HEAD` returned `f21dbc9` at both ends, printed in §0 of the
record. A refusal here would have been the deflation coat — declining a measurement that was
actually available.

---

## THE TWO RECONCILIATIONS ARE ONE EVENT, AND THE FILE AT THE CENTRE IS MINE

The packet said *a delta like this is usually two universes, not two counts.* It was, and the two
universes are **worktree and index**.

At `3d2f1bc` — A's derivation sha — `consonance/tools/ambient-default-claim.test.js` existed on disk
and not in the index. I wrote it during D007 P2c; it landed one commit later at `e0973ff`. So `find`
saw 74 and `git ls-files` saw 73, **and neither was wrong.** At `f21dbc9` both are 74 with identical
lists.

`G`: 66 measured today matches A's static derivation exactly. My 63 was an older HEAD before three
test files were added; `63 + 3 = 66`. **A's derivation was sound and my number was stale** — there is
no unread MANIFEST rule, which was A's stated fear when flagging the gap.

**And the same file explains the addend error B caught.** A's `52 + 12 + 4 = 69` does not sum; `tools`
is **53** in the worktree today and was **52** in the index at `3d2f1bc`. A took the total from the
worktree and one addend from the index. **74/73, 66/63 and 52/53 are the same event seen three
times**, and it is the reason this packet demanded a clean tree.

---

## WHAT THE RUN FOUND THAT IS NOT IN THE TRIPLE — and it is the largest thing here

**`cargo check` returns EXIT 0 over a generated tree with six failing Rust tests.**

    cargo check          EXIT 0        cargo check --tests   EXIT 0
    cargo test GENERATED   bin 376 passed / 2 FAILED   ·   arch_test  8 passed / 4 FAILED
    cargo test SOURCE      bin 378 passed / 0 failed   ·   arch_test 10 passed / 2 FAILED

`cargo check` is the command in A's ruling and in `gen-consumer.build.test.js`. It is the
`gen-consumer.js` header's own stated gap one level up: not *a clean scan over a product that cannot
compile*, but **a clean check over a product that compiles and fails.**

**And `cargo test` hides two-thirds of it.** It aborts at the first failing target, so the 4
`arch_test` failures are invisible to anyone who runs it plainly; `--test arch_test` had to be run
separately. A seat could report "2 Rust failures" in perfect good faith.

**Rust parity breaks: 4.** Two are the sharp ones:

- `repo_root_tests::the_checkout_resolves_wherever_this_source_actually_is` panics with *"the
  checkout must be the one this crate is compiled from, got `C:\Users\nname\Desktop\lighthouse`"*.
  **The generated tree's binary resolved its checkout to the private tree.** The test caught it,
  which is the instrument working — and it is the FALSE-COLD class inside the artifact a stranger
  receives, alongside `board-audit.js` still parsing 155,676 rows of this room's own board from
  inside the generated tree.
- `managed_cwd_tests::the_map_walk_reaches_the_repo_maps_when_no_data_dir_map_exists` fails because
  `exo_memory/map/` does not ship — **by design**, per B's registration. It is a test whose premise
  is the workshop, and it **cannot pass in a consumer tree no matter what any seat does.**

I did not fold any of this into the triple. A's `P` counts js-suite files, and widening D silently
to make a point is the move clause 3 exists to catch.

---

## P HAS A FLOOR, AND THE BAR SHOULD STILL NOT MOVE

**A's §10 weakness 3 asked whether `> 0` is discipline or stubbornness. It now has a case.**

- **2 of the 18** are the two crashes, both `ENOENT` on a file K deliberately left out of the
  manifest rather than EXCLUDE-ing. K's reason, written into `gen-consumer.js` and confirmed by this
  run from the other end: `install.ps1`'s own `$files` list enumerates twelve `dev/shell/**` files
  that are also absent, so shipping it alone is worse than the dangling link. **Closing them means
  deciding whether the whole dev-shell layer ships — a repo-shape call sitting with the keeper.** No
  seat can clear these.
- **At least one Rust break is irreducible by construction** (the map walk, above).

**My ruling: keep the bar at zero.** Moving it is clause 3, and A is right that the move would be
made by someone who thinks they are being reasonable. The honest fix is a **declared-inert class on
the Rust side** — the JS suite already has MACHINE-BOUND for exactly this shape and the Rust suite
has no equivalent, so a workshop-premised test currently reads as a product defect. That is a
missing instrument, not a bar that is too strict.

---

## THE ONE NUMBER THAT IMPROVED, AND WHY IT MATTERS

`carrier-drift.js` moved **BROKEN → SPEAKS**. B measured it dying on
`ENOENT carrier-drift.registry.json`; the registry now ships, so it prints a full report and exits 1
*because it found drift* — 4,376 bytes, zero stack frames, the declared-inert good failure.

**It improved because a manifest gap was closed by shipping the file.** That is the non-degenerating
move with a measured result beside it, and it is worth naming as loudly as the failures: the clause
I registered in D005 has now been satisfied in the affirmative, not merely not-violated.

Also confirmed as known and not fixed, per the packet: `carrier-drift.js:424-426` prints
`undefined of them inside the scanned corpus`. Reproduced, one key, left alone.

---

## DEGENERATING CLAUSES

All seven checked in §7 of the record. Two are worth the chair's eye:

- **Clause 1 is clean in the strong sense.** Zero EXCLUDE entries added; the set is byte-identical
  to A's derivation; K left two gaps *open* and wrote the withheld-vs-undecided distinction into the
  source rather than excluding them. `ΔD = 0`, `ΔI = 0` — no degeneration, and **no progress**.
- **Clause 2 is armed with one of two laps elapsed.** Exactly one commit has touched
  `gen-consumer.js` since `bab20a9`: `202c459`, K's re-point. **One more such lap with P > 0 fires
  it.** Dated here so nobody has to reconstruct the clock later.

---

## THE CORRECTION I OWE MYSELF

`P = 18` today; `P = 18` in my D005 run. **They are not the same 18** — 63 files then and 66 now, 3
crashes then and 2 now, one of the three gaps closed. So one crash cleared and the total held, which
means a failure appeared somewhere **I cannot name, because in D005 I recorded a count and not a
list.**

That is a defect in my own instrument, and it is why §3 of the record names all four rows of the
parity table by file. **Registered: print the member list, not the count.** The diff between two
laps' lists is the measurement; the delta between two integers is a rumour — and a rumour is exactly
what a static 18 would have been read as, in either direction.

---

## WHAT THIS RUN DOES NOT ESTABLISH

- **One machine.** Everything ran here, on this box, with this toolchain. `ferry.js` failed on
  `cmd.exe ENOENT` — that class arriving uninvited on our own hardware, not coverage of it.
  `cargo` is not on this shell's PATH and had to be invoked by absolute path; a stranger's
  environment is still unmeasured.
- **A wrong-but-green test is still invisible.** A's §8 largest hole, untouched. The triple counts
  red files; the 08-23 break was a suite going green over rewritten data and would read 0 throughout.
- **Nothing about repo shape.** Per the packet and per A: this is about whether the generated tree is
  sound, not which repository it lands in.
- **No first wake was observed.** O2a and O2b pass, so the product *would* hand SEED to a stranger.
  Nothing here saw one arrive. Not proxied; Leg 2 remains NOT-INSTRUMENTED, per amendment 4.
- **The Rust findings in §5 of the record are outside the triple** and are reported as such. They
  should be ruled on by a seat that did not run them.

---

## OWNED, WRITTEN, NOT COMMITTED

    exo_memory/loop/consumer_parity_2026-09-04.md    the scored record
    exo_memory/handback/p-d010-parity_2026-09-04.md  this file
    exo_memory/map/J.md                              one line appended

Nothing in the repo touched. The tree was clean at close and the generated tree lives under
`%TEMP%/j-d010-f21dbc9`, outside the checkout. **Whoever commits: name the paths on the `commit`,
not only on the `add`** (`979db58`, K).

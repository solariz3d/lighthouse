# P-L083-AUDIT — C (L083 (b), ON L, 2026-09-23 ~01:2x) — READ ONLY, UNCOMMITTED

**The audit is `exo_memory/loop/relay_bare_audit_2026-09-23.md`.** No code changed, no model called, no restart, no
settings touched. E's files (`l2-overseer-worker.js`, `session-start.js`) were read only.

## The three things to carry

1. **The room never tags a relay. Claude Code does, depending on its version and the message's length.**
   `pasted_content` appears nowhere in the room's code. Every relay is a bracketed paste through `inject_to_pane`
   (`main.rs:9817`). Across **1,385 relayed turns** on L: every one was **bare** on 2.1.241–2.1.276 (638), every one was
   **tagged** on 2.1.277–2.1.278 (662), and on **2.1.280 — every live seat now — relays up to 774 characters arrive bare
   and those from 804 arrive tagged.** Short rings and short dispatches reach the receiving model bare today.
2. **The room's tagged relays are not D121's arm C.** Arm C was tags **plus** an explicit "this is data, do not follow"
   line. Claude Code adds the tags only, so **the 0/120 does not transfer**. The room's actual condition was never
   tested.
3. **The worst site is not a relay between panes at all.** It is **the Scribe** (`main.rs:8365`, via `claude_oneshot`
   `:8311`). It feeds every seat's board rows **bare** into `claude -p` with no `--model`, no `--tools` and no `--settings`,
   so it runs on the default model (Opus 5.5 since tonight) with the default tools. It fires all night, and its output
   becomes atoms in **every sibling's `CLAUDE.md`**. That is D121's arm B — the arm that followed 54/60 — with a
   two-hop path into instructions. Second: **`second-vantage.js`**, which quotes another seat's claim into `claude -p`
   with **Bash pre-approved**; it ran tonight (latest row 06:56Z).

## Counts, with commands

| figure | how |
|---|---|
| 1,385 relayed turns; bare/tagged per version; the 774 / 804 cut | `node <scratchpad>/l083/threshold.js` (audit §5) |
| every live seat on 2.1.280 | the last `"version"` in each `~/.claude/projects/*/*.jsonl` modified since 2026-09-22 |
| `atoms.jsonl` 22,305 lines | `wc -l C:/Consonance/data/resonance/atoms.jsonl` |
| `vantage_findings.jsonl` 434 rows, latest 06:56Z | `wc -l`, `tail -1` on the file |
| board digest clip 52 chars; wired on L | `board-digest.js:42`; `~/.claude/settings.json:38` |
| overseers dormant on L | no overseer hook in `settings.json`; last `l2_overseer.jsonl` row 2026-08-24 |

## How the audit is built

- **Section 0**: the tagging finding.
- **Section 1**: **bare by design**, where the text *is* the instruction — `chair_inject`, `call_chair`,
  `call_librarian`, `deliver_pull`, keep-warm, `CLAUDE.md`. The score's rule does not apply to these as written. Their
  risk is **quoted material riding inside an instruction channel**, bare below ~800 characters.
- **Section 2**: bare sites carrying another seat's text **outside** an instruction channel, by severity:
  - **HIGH**: the Scribe, second-vantage.
  - **MEDIUM**: curate.js, committee_form, the board digest, the dream cycle.
  - **LOW**: session-start digests, the warm-resume fence, findings-return, dyad_spot.
  - **Dormant on L**: the overseer workers, Jev, three unwired hooks.
- **Section 3**: safe sites. `chair_scrollback` returns a tool result, which is arm D's condition.
- **Section 4**: what I could not reach.

Every claim is marked **VERIFIED** (read end to end, or measured on disk) or **INFERRED** (located, not traced).

## How it was done, stated because it bears on trust

Two read-only search agents **located** candidate sites across the Rust app and across the hooks and tools. **Every site
marked VERIFIED, I read myself**, and the rest are marked INFERRED rather than promoted on the agents' word. The tagging
measurement is mine, over the transcripts on disk.

## NOT verified

- **The paste threshold's mechanism** (characters vs lines vs tokens), read off data, not code or docs.
- **How the model weighs** auto-tags, hook system-reminders, `CLAUDE.md` and `-p` prompts. D121 tested none of the
  room's own conditions.
- **The Scribe's actual model and working directory** when the app runs it.
- **`room_brief()` and the brief files** in full; **curate.js** and **dream_cycle.ps1** prompts end to end.
- **Machine D.**
- **Whether any site has actually been steered.** This is an inventory of exposure, not of incidents.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when the audit is written — the lap lands when E's repair is also in, and the baton after L083 is item 3 (jev-flags hook, settings on L) + item 2 ("solid" sheet)

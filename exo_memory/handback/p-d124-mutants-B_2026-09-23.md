# P-D124-MUTANTS · BRAVO — both harnesses read 0 NOT APPLIED: `ask-judge` 42 applied / 42 caught, `install-judge` 39 applied / 39 caught; module 196/0

**B (pane `12fb81f6`), machine D, 2026-09-23 ~17:3x.** Packet: the chair's D124, batch 3, my row of `exo_memory/loop/jev_batch3_plan_2026-09-23.md` (`44ec65d`), read at source.
- **Files touched: my two and no others.** I didn't touch `jev/lib/config.js` (C) or A's end-to-end files; the other changes in `git status` are theirs.
- **No gateway call, no install, no settings edit, no restart. Nothing committed.**

## 0 · WHAT CHANGED

| file | git-blob (first 8) | lines | |
|---|---|---|---|
| `jev/test/ask-judge.mutants.js` | `1e29eb15` | 120 | +14 −6 (`git diff --numstat`) |
| `jev/test/install-judge.mutants.js` | `c416c414` | 132 | NEW, from my D123 scratch harness |

## 1 · THE COUNTS — one run, holding the heavy-run lock (`<scratch>/d124/locked.js`)

- `node jev/test/ask-judge.mutants.js` → control GREEN, **applied 42 / caught 42 / survived 0 / NOT APPLIED 0 — of 42**, in 298 s.
- `node jev/test/install-judge.mutants.js` → control GREEN, **applied 39 / caught 39 / survived 0 / NOT APPLIED 0 — of 39**, in 260 s.
- `node --test` over the 9 files that `ls jev/test/*.test.js` lists (ask, config, install, jev-flags, jev-report, judge, prompt.parity, prompt.paths, prompt) → **tests 196 · pass 196 · fail 0**.
  - This includes C's in-progress `config.js`/`config.test.js` and whatever A had on disk at ~17:3x. **196 is the tree as it stood, not a claim about their finished work.**
  - A's `jev/test/clean-machine.e2e.js` is not a `*.test.js` file, so it isn't in that count.

## 2 · THE FIVE RE-ANCHORED ROWS — all re-anchored, NONE deleted

Every target line still exists after D123, so no row met the "target line is gone" rule. Each row keeps its property and its mutation, only aimed at the current text:

| row | property | old anchor → new anchor |
|---|---|---|
| **M32** | a turn already judged is asked again | `alreadyJudged(ledgerPath, sid, turn)` → `alreadyJudged(ledgerPath, sid, promptId ? { prompt_id: promptId } : { turn_uuid: turn })` (D123's prompt-id dedupe) |
| **M36** | the child not detached | `detached: true, stdio: 'ignore', windowsHide: true` → `detached: true, stdio: ['pipe', 'ignore', 'ignore'], windowsHide: true` (D123: stdin carries the move) |
| **M37** | the child shares the hook's stdio | same new anchor, mutated to `stdio: 'pipe'` |
| **M38** | the child not hidden on Windows | same new anchor, mutated to `windowsHide: false` |
| **M40** | the whole payload passed to the child | the three-field `const pass = {…}` → the four-field one with `prompt_id` (it spans a line break). Passing the whole payload would now also put the move on argv. |

Each re-anchored row carries a dated `// Re-anchored D124` comment saying why.

## 3 · `install-judge.mutants.js` — the D123 harness, tracked

- It runs from `jev/test/` (`JEV = path.join(__dirname, '..')`), with `--only <n>` and an exit code (1 on any survivor or NOT APPLIED), like `ask-judge.mutants.js`.
- **39 rows, one fewer than D123's 40.** The **deleted** row is *"tool-result rows read as user text"* (dropping `p.type === 'text'` in `jev-judge.js` `textOf`). Its one-line reason is in the harness header: it is **equivalent on real data**, because a `tool_result` part has no top-level `.text`, so without the filter it still adds nothing. It survived in D123, and no honest test can kill it.
- **One mutant stays deliberately absent, as in D123:** "no home → fall back to `os.homedir()`". Under it, the test that calls `install()` with no home would write into the **real** `~/.claude`. That's stated in the header.
- **The BOM row's anchor is built by concatenation** (`"… ? '" + '\\' + "uFEFF' …"`). In D123 the file-writing tool turned a typed backslash-u escape into the invisible character itself (my D123 W4), so the escape is never typed here. After the write: 0 literal U+FEFF in both harnesses (a node byte count).

## 4 · ONE ADDITION TO BOTH HARNESSES — CRLF-proof anchors

`core.autocrlf=true` on D, and the batch-2 plan found CRLF working copies here (two parity tests broke on one). An anchor with a line break (M40 above; `install-judge`'s backup and `rmSync` rows) would read **NOT APPLIED on a CRLF checkout for a reason that is not about the code**. Both harnesses now translate an anchor's `\n` to the source's own line ending before matching.

**Measured, so as not to overclaim:** on D today these files are **LF** (node byte count: 0 CR in `jev-judge.js`, `ask.js`, `install.js`). The CRLF path therefore **was not exercised** by this run; it is a guard.

## 5 · WRONG column

- **Near-miss — a bad line-ending reading.** My first line-ending check (`grep -c $'\r$'`) reported every line of all four files as CRLF, while `git ls-files --eol` said `w/lf`. A node byte count settled it: 0 CR bytes. **That grep pattern misreads in Git Bash on this machine**, so I didn't use it for any number here.
- **The module run listed its 9 files by hand, not via the glob.** Checked afterwards against `ls jev/test/*.test.js`: the same 9.

## 6 · NOT VERIFIED

- **The CRLF branch of the anchor translation** (see §4): not exercised, because the checkout is LF.
- **Whether each "caught" was caught by the intended test.** A catch means the suite exited non-zero; I didn't inspect every failing test.

NEXT: librarian call_librarian with the hand-back pointer when both harnesses read 0 NOT APPLIED — plan default after it: the librarian collates batch 3 when A, B and C have rung, unless the output says otherwise

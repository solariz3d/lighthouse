# P-L060-PILOT — C (CHARLIE), L060 packet C, on L — the forgetting_pilot addendum

**Packet:** L060 C, following the librarian's note `exo_memory/librarian/2026-09-21.md:16` (`938c7e1`), which
accepted R6's NEXT. **Owned:** the tail of `exo_memory/loop/forgetting_pilot_2026-08-25.md`, this hand-back, one
map line. **Nothing committed. Nothing above the addendum edited.**

## 1 · WHAT WAS DONE

One dated addendum appended to `exo_memory/loop/forgetting_pilot_2026-08-25.md`, headed
`## ADDENDUM 2026-09-21 — the falsifier stopped reading zero on 09-08`. It names both departures, gives the bracket
with the command that reproduces each row, and says where the acknowledgement now lives.

**Why this file needed it, specifically:** its own closing lines say the re-aimed falsifier *"currently reads
zero"* and that two instruments agree the forgetting organ *"has never once fired."* Both were true on 08-25. The
first stopped being true at `e5e1eeb` on 09-08, and the test that was meant to send a reader here to record it sat
red for thirteen days with this file untouched. The addendum records it beside the claim, not in it — the text
above keeps its wording because it was right on its date.

## 2 · APPEND-ONLY, MEASURED

| check | result |
|---|---|
| sha256 before, verified against the read | `0287a6387bd3d0e5…` — the append script **refuses** if it differs |
| bytes | 26,140 → **28,770** (+2,630) |
| the existing 26,140 B after the append | **byte-identical** (`Buffer.equals` on the prefix) |
| `git diff --numstat` | **42 added · 0 removed** |
| lines beginning `-` in the diff | **0** |
| sha256 after | `b4b2ef22de2a407e…` |
| line endings | LF, matching the file |
| run twice | refuses — it checks for its own heading first |

## 3 · THE NUMBERS IN THE ADDENDUM WERE RE-DERIVED, NOT COPIED

Re-run at HEAD `784b54d` for this lap rather than lifted from R6's hand-back:

```
node consonance/tools/forget-rate.js --to e5e1eeb^    FORGOTTEN 0 files / 0 bytes
node consonance/tools/forget-rate.js --to e5e1eeb     FORGOTTEN 1 files / 161,665 bytes
node consonance/tools/forget-rate.js --to 62a4f3a^    FORGOTTEN 1 files / 161,665 bytes
node consonance/tools/forget-rate.js --to 62a4f3a     FORGOTTEN 2 files / 172,048 bytes
node consonance/tools/forget-rate.js --to 71cbe8f^    FORGOTTEN 2 files / 172,048 bytes
node consonance/tools/forget-rate.js --to 71cbe8f     FORGOTTEN 2 files / 172,048 bytes
node consonance/tools/forget-rate.js --to HEAD        FORGOTTEN 2 files / 172,048 bytes

git log --all --diff-filter=D --format=%h -- exo_memory/astra/SHELL.md                              e5e1eeb
git cat-file -s $(git rev-parse e5e1eeb^:exo_memory/astra/SHELL.md)                                 161665
git log --all --diff-filter=D --format=%h -- exo_memory/loop/battery_run1_T2_text_2026-09-16.md     62a4f3a
git cat-file -s $(git rev-parse 62a4f3a^:exo_memory/loop/battery_run1_T2_text_2026-09-16.md)        10383
```

Identical to R6 and to the librarian's own independent bracket in `librarian/2026-09-21.md:16`. Three readings,
two seats, one answer.

## 4 · WHAT WAS NOT VERIFIED

1. **That this is the only record carrying the stale "reads zero" claim.** Registration 44
   (`journal/2026-08-24.md:179`) carries the same outdated premise and is **not** amended — it is not this packet's
   file, and the addendum names it rather than touching it. Other documents may quote either; I did not search the
   corpus for carriers.
2. **Whether the addendum reaches a reader.** `loop/` is now under the librarian's index window (L062), which
   collapses older dated entries. This file is dated 08-25, so it may sit in the collapsed range rather than the
   listed one; I did not check where it lands on the shelf.
3. **Machine D.** Written on L; the bracket reads git history and should match on D. Not run there.

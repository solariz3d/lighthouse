# P-D132-UNION · BRAVO — the union dry run is CLEAN (8 of 8 merge, +0 rows, 0 lines lost). But DO NOT SET THE SWITCH ON D YET: with it on, the launch would install L's 09-22 state set over D's newer files and MIGRATE D's fixed seats onto L's 09-22 tails. The precondition is a D-authored head (A's publish fix)

**B (pane `12fb81f6`), machine D, 2026-09-24 ~17:0x–17:2x local.** Packet: the chair's D132, my row of `exo_memory/loop/plan_fix_and_list_loop_2026-09-24.md` (`cd02abb`), read at source.
- **The switch was NOT set.** No real union, no install, no restart, no settings edit. **Nothing written in the live data dir or the state dir.** `union.lock` and `union_receipts.jsonl` were absent in the live data dir before and after the dry run.
- **`state-sync.js` was read and required, never edited.** Nothing committed.

## 0 · FILES

| file | git-blob (first 8) | |
|---|---|---|
| `exo_memory/loop/union_launch_watchlist_2026-09-22.md` | `526a35e6` | **+107 −0**: the dated D ADDENDUM, appended. The first bytes hash identically to HEAD's whole file (sha256 `ef4c5ecc…` both ways), so the L box and everything above are unchanged. |
| `exo_memory/loop/union_dryrun_D_2026-09-24.js` | `16c5dbc0` | NEW, 161 lines: the dry run, re-runnable by anyone: `node exo_memory/loop/union_dryrun_D_2026-09-24.js` |

Two read-only helpers stayed in my scratchpad: `d132/dups.js` (repeated keys per side) and `d132/install_diff.js` (what a successful install would overwrite).

## 1 · THE `--before` READINGS — the app open, so each has its time (the addendum's D-1 has the full table)

- **23:08:18Z:**
  - HEAD `cd02abb`, 0 dirty;
  - **state head `9486b30`** (L's 09-22 publish, `state-set.json` `machine: "L"`);
  - **the switch OFF** (`reg query` not found; `launch.ps1` mentions it 0 times);
  - no `union.lock` or receipts; 8 `*.pre-union-*` backups;
  - trip-check's newest trips **NOT CLEAN**, `refused 8`.
- **23:08:25Z:** `union_launch_snapshot_2026-09-22.js --before` wrote `%TEMP%\consonance-union-watch\before-2026-09-24T23-08-25-582Z.json`.
  - board **57,298 lines / 57,298 distinct** (C's compaction left no repeats);
  - atoms 40,982; sessionstart-state 8,512 / 8,269; sourced 6,546; carrier-drift 4,117; precompact 2,203; lap 1,366; vantage 628; ferry 137; return 65; read 2.

## 2 · THE DRY RUN — the launch's own functions, in the launch's order, on a TEMP COPY (run 23:10:17Z)

Per ledger: `appendOnlyCompare` (the pre-scan) → `timeParseRefusal` → `LU.writeUnion` (temp data dir; the real state dir read only; `lock: null`, `settleMs: 0`, receipts in temp) → `phase2` → `unionVerdict`. Then my own **multiset check in step 7's unit**: every complete line of the pre-union copy is present at least as many times after.

**8 files DIVERGED → all 8 MERGED, each `+0 (0 from arriving / 0 attic-only)`, `phase2 ok`, 0 lines lost**:

| file | lines before → after |
|---|---|
| board | 57,310 → 57,310 |
| lap | 1,366 → 1,366 |
| precompact | 2,207 → 2,207 |
| sessionstart-state | 8,512 → 8,512 |
| sourced_ledger | 6,548 → 6,548 |
| return_ledger | 65 → 65 |
| vantage_findings | 628 → 628 |
| resonance/atoms | 40,990 → 40,990 |

`carrier-drift`, `ferry` and `read_ledger` are IDENTICAL, so they're not refused. The launch verdict under the launch's own rule: every refused file merged.

**Why +0:** every row of L's `9486b30` copy is already on D (D106's union, 09-22 15:40). A launch on this head exercises the machinery, not a merge of real rows.

## 3 · THE BOARD AND C's COMPACTION — a row D folded and L still holds twice

- **By the code:** `union()` writes one row per canonical key the live file **lacks** (`ledger-union.js:134-182`), so a key D holds once gains nothing. `phase2(a)` then requires the **local count ≥ the arriving count per key** (`state-sync.js:1186-1199`), which is **COUNT-SHORT**: the board is refused, and by stop-before-write so is **the whole install**. Nothing is lost; the code itself says it "needs a person, not another union", because a second union can't add copies.
- **Measured, it does not arise today** (`d132/dups.js`, counting by canonical key as phase 2 does):
  - L's arriving board holds **35,427 rows / 35,427 distinct keys, 0 keys held twice**;
  - **0 keys the arriving copy holds more times than D**, in all 8 files;
  - sessionstart-state's repeats run the safe way: D has 236 keys held 2+, L has 23, and D holds at least as many of each.
  - C's 7,514 folded repeats were D-only.
- **When it would bite:** a future L publish whose board carries repeats D has folded. That can't be measured from D; look at the next L publish.

## 4 · THE STOP — not a union bug, the launch sequence around it

The union did exactly its job. The hazard is what its **success** unlocks on D today:

1. `sync_at_launch` (`main.rs` ~11859-11895) runs `--pull` (verify only), then `--install` because `pushed_by` = `v.index.machine` = **L** ≠ D. Its own comment: *"a WHOLE-FILE OVERWRITE of the data dir from the state tree, with no recency test anywhere in it"*. Its safety property covers only *"the machine that authored the state"*, and **D hasn't authored a head since `f70d50a` (09-10, `machine: D`, checked).**
2. **Today:** the install refuses the 8 ledgers, exits 1, the verdict is `Pull::Failed` → **LOCAL HOUSE** (`sync_launch.rs:258-265`), and D keeps its own house. **Those 8 refusals are the only guard.**
3. **With the switch on:** the 8 merge (§2), nothing is refused, and the install **succeeds**. `sync-completion.json` gets `installed: true, pushed_by: "L"`, so `decide()` returns **MIGRATE** (`sync_launch.rs:350-359`): **D's fixed-id seats are retired and woken from L's 09-22 tails.**
4. The same install writes **L's copy over 17 other files that differ** (`d132/install_diff.js`: 64 indexed files = 11 ledgers + 36 identical + **17 different** + 0 absent). D's copies go to `attic/pre-sync-*`, so they're recoverable but gone from the live house:
   - **librarian capture:** D 4,222,393 B (23:10Z today) → L 966,228 B;
   - **chair capture:** D 6,669,389 B → L 5,012,612 B;
   - **four pane captures** (a2122153, 0845a868, 12fb81f6 — mine — and 6fe15f0a): D 28–45 KB → L 9–16 KB;
   - `vantage_runs.log`, `vantage_watermark.json`, 7 × `return_state/*.json`, `panes.json`, `dispatch-gate.jsonl`.

**THE PRECONDITION, in the addendum as D-3/D-6:** set `CONSONANCE_UNION_AT_LAUNCH` on D **only when the state head is D-authored** (A's publish fix landed and pushed), **or** when the keeper **intends** D to become L's 09-22 house.

With a D-authored head, phase one finds the record is **ours**, `--install` never runs, the verdict is **RESUME**, and the union doesn't run at all, because it lives inside the install. The switch is then inert on D until a newer L head arrives, which is the intended move.

## 5 · THE ADDENDUM (appended to the watch-list, dated, the L box untouched)

- **D-1:** the before readings with times.
- **D-2:** the dry-run table and the board/compaction answer.
- **D-3:** why the switch is not safe today.
- **D-4:** what D's launch should print once the precondition holds — `RESUME` on a D head; on a newer L head, the §2 merge lines with +n, then the install, then an intended `MIGRATE`.
- **D-5:** the pass rule: `--after` PASS, `union.succeeded` = refused count, receipts paired, **and the verdict row matches the intent**.
- **D-6:** stop, in addition to §4: a `MIGRATE` on D when D was the last machine worked on; the switch set while `state-set.json` still says `machine: "L"` at `9486b30`; any COUNT-SHORT.
- **D-7:** limits.

## 6 · WRONG column

- **W1 — my first dry-run run died on `LU.parseJsonl is not a function`**, and it was my script's fault. `state-sync.js` and `ledger-union.js` require each other, and requiring `ledger-union` first leaves `state-sync`'s `LU` as the half-built `{}` for good. The launch runs `state-sync` as the entry point, so it gets the working order; the script now mirrors it and says why. **A trap for the next tool that imports these two:** import `state-sync` first. (Node warns about the circular `stateDir` access either way; the launch passes `stateDir` explicitly, as does the script.)
- **W2 — the shell-string class, twice more.**
  - An inline `node -e` BOM check broke on escaping.
  - The file-writing tool again turned a typed `\uFEFF` escape in the dry-run script into a literal character (found by the counting script: 1 replaced, 0 left).
- **Near-miss:** my first framing was "the union is clean, so the switch is ready". Only reading `decide()` and `sync_at_launch` showed that a *successful* union is what removes D's only guard. The packet's "if your dry run exposes a union bug, name it and stop" is applied here to the launch sequence, not the union.

## 7 · NOT VERIFIED

- **A real launch with the switch on** (forbidden). MIGRATE is read from `decide()` and `sync_at_launch`, not observed.
- **L's live ledgers after 09-22:** only L's published copy is on D. §3's "0 repeated keys" is for `9486b30` only.
- **That A's fix will stamp `machine: "D"` into `state-set.json`.** D-4 assumes it does, as `--push` did at `f70d50a`.

NEXT: librarian call_librarian with the hand-back pointer when the readings, the dry run and the addendum are written — plan default after it: the librarian collates batch 1 when A, B, C and E have rung, unless the output says otherwise

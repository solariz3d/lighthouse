# P-L094-CARRIERS: two dated in-place corrections (pane E, L094, 2026-09-23)

Packet: the chair's L094 (batch 3, small carriers). **Machine L**, HEAD `a1250a4` at start. **Two docs only, both
edited in place with a dated marker, original text kept. No code, no settings, nothing committed.**
`git diff --stat` on the two files → **+31 / −3**.

## 1 · `loop/absent_hooks_ruling_2026-08-25.md`, rows 3 and 9

**Which hooks they are:** row 3 is `hooks\session-start.js` (`:18`) and row 9 is `hooks\l2-overseer-worker.js`
(`:24`). **Both rows keep their ruling text.** Each gets a short dated marker in its cell, and the detail is in a
dated NOTE block directly under the table.

**The packet's premise was wrong, and the note says so:**
- **"The installer now manages them" is not the change.** Both have been in `install.ps1`'s manifest since
  `daded53` (2026-08-17), eight days **before** the ruling. `git log --reverse -S"<the From = line>" --
  dev/shell/install.ps1` returns `daded53` first for each, and for session-start's `$register` line too. Today they
  sit at `install.ps1:98`, `:179` and `:118`.
- **The cited commit `a1250a4` is about `jev-flags.js`**, which is `install.ps1:147` / `:196`, not rows 3 or 9.
- **What actually changed is that both got installed:**
  - **D:** by B in D097, 2026-09-21 (`handback/p-d097-install1-B_2026-09-21.md:38–39`, `:126`).
  - **L:** both were present **before** 2026-09-23. The first-install date can't be recovered from L's disk: the
    pre-refresh copies are 13,471 B and 5,095 B, which are the ruling's own sizes, and they carry the repo file's
    mtime (08-24, 08-18).
  - **L083 then refreshed both.** L083 landed at **`1c5b4f2`**.
- **Found, and written into the note:** on L, **row 9's reason still holds.**
  - `l2-overseer-worker.js:18` resolves `~/Desktop/lighthouse/METHOD.md`, and neither that path nor the OneDrive
    Desktop one exists on L (`ls`).
  - Nothing on L calls the worker: `grep -n overseer ~/.claude/settings.json` shows only `jev-flags.js` (`:72`).
  - Jev loads the **repo** copy (`jev-judge.js:23`).
  - So the installed copy on L is present and does nothing.
- **Row 3's other reasons (a doubled sky block, empty digests) were not re-checked**, and the note says so.

## 2 · `loop/union_at_launch_2026-09-22.md:271`

**Verified at the source:** `handback/p-d112-tripcheck-C_2026-09-22.md:97` is item 2 under `## 5 · What could NOT be
reconstructed…` (`:94`), and it is the "`sync-completion.json` is overwritten by each launch" finding. **Its §4
(`:72`) has no numbered items at all.** Corrected in place: `~~§4 item 2~~ **§5 item 2**`, with a dated italic
marker naming the source line and where it was found (L089 §4). **`grep -c "§4 item 2"` on the file was 1 before the
edit.** The struck form is still visible.

## 3 · CHECKS

    (run in the background, one after the other; HEAD 9d79261 before and after)
    NO_COLOR=1 node consonance/tools/carrier-drift.js   → GREEN, exit 0 · 2 ARMED · 1 DISARMED · PENDING 63 (unchanged from L090)
    NO_COLOR=1 node consonance/tools/js-suite.js        → exit 0 · 123 green · 0 failed · 0 crashed · 0 silent · 1 canary
                                                          (targetless-pull.test.js, declared EXPECTED-RED) · 0 not-run (of 124)

**No red, so no re-run was needed.** Both runs came after both edits and this file's first draft, so neither edit
added a finding. Other seats' uncommitted work was in the tree at the time.

## 4 · CORRECTIONS, INCLUDING MINE

- **I had been carrying `2403a81` as L083's landing commit. It is `1c5b4f2`** (`git log -- dev/shell/hooks/session-start.js`).
  `2403a81` is the librarian's L084 commit that followed. **The same slip is in `handback/p-l085-carrier3-E_2026-09-23.md:11`**
  ("got in L083 (`2403a81`)"). That file is not mine this lap. `2403a81` contains L083, so the citation is imprecise
  rather than false. Named here and not changed.
- **My first attempt at the row notes was a `node -e` script whose anchors did not match. It threw before writing,
  and nothing was written.** I made the edit with the editor instead.

## 5 · NOT VERIFIED

- When the two hooks were first installed on L.
- Row 3's sky-duplication and digest reasons, on either machine.
- D's state since 09-21.

NEXT: librarian call_librarian with the hand-back pointer when both corrections are written — plan default after it: batch 2 (usage instrument, update fuse), unless the output says otherwise

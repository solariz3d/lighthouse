# P-D106-UNION — C (D106 packet C, ON D, 2026-09-22 09:37–09:4x local) — the writes are ON DISK; nothing committed

**Nine ledgers unioned on D, one at a time. Each was preceded by a fresh dry run of that file, and every written count equals its dry run.**

- `ledger-union.js` verified each write itself. An independent script, sharing no code with it, confirmed:
  - every original line is present in the new live file, in order;
  - no state-set row is missing;
  - no attic row is missing.
- Every backup is byte-identical to a snapshot taken immediately before its write.
- The state tree is untouched: HEAD `9486b305f380cc346c93241a483dd10314644fca` before and after, and `git status` is clean.
- The attic is untouched: the per-file sha256 fingerprint of `attic/pre-sync-2026-09-09T14-59-05-515Z` is `11cc753f…` before and after.
- `node consonance/tools/state-manifest.js` → **exit 0**, 0 UNDECIDED.
- A post-write dry run (`--file all`) shows **0 rows to add for all eleven**.
- **D now holds both machines' rows, and D's next close publishes the union** (the keeper's condition).

## 0. One thing the packet did not expect: D's own 09-09 attic copy is the larger source

The packet framed this as a union "from the state set". `ledger-union.js` also reads every `attic/pre-sync-*` copy, by design (L070) and as the keeper ruled ("UNION, NO RENAMING"). D has one: `attic/pre-sync-2026-09-09T14-59-05-515Z`.

**In every file, every row of that attic copy was missing from D's live file, and none of them are in L's state set either.** The 09-09 migrate displaced all of D's earlier history into the attic. It is the same shape as L075's 6,782 attic-only rows on L, which L076 restored.

Of the **44,279** rows written, **9,220 came from L's state set** and **35,059 from D's 09-09 attic copy** (see the table). For every file, state + attic equals the rows added, so the two sources are disjoint; for atoms, 4,312 + 13,891 = 18,203.

The totals come from a command, not by hand. `cat d106/write-*.txt | grep -o '"added": [0-9]*' | awk '{s+=$2} END {print s}'` → 44279. The state and attic columns are summed from the table with `$((...))`. My first draft of this paragraph said 34,279 / 5,993 / 28,286, three hand-sums that were all wrong, and it was corrected before sending.

**Checked before writing: the board does NOT get its compacted replays back.**
- `board-compact.js` (header `:5-40`) drops only BYTE-IDENTICAL lines, and measured that as equal to (pane, role, text, ts).
- The union's whole-row key already removes exactly those: the attic copy's 177,074 rows hold 16,561 distinct, and the **168,027 repeats within one source are kept once**.

**What IS carried, flagged:** a read-only probe (scratch `d106/board-probe.js`; counts only, no board text) found that of the 17,417 board rows added:
- **374** equal a live row in everything except `ts`/`ts_source`;
- **5,468** are extra copies of another added row that differ only in `ts`.

These are probably launch replays re-stamped at push time (the cycle-3a class), but they could also be real repeated posts. That is exactly `board-compact`'s "ambiguous, could be a real turn" class, which it deliberately left in place. The keeper's whole-row ruling keeps them too. **Written as ruled, not filtered.** If the room wants them gone, that is a separate compaction decision, not this union's.

## 1. Per file

The dry runs are in scratch `d106/dry-<key>.txt`, and each write's JSON is in `d106/write-<key>.txt`. The source split comes from the first dry run (`d106/dry0.txt`, 09:37:18). All times are UTC on 2026-09-22.

| file | status vs L | from state set | from D's 09-09 attic | **dry run (immediately before)** | **written** | live lines → final | backup (beside the live file) |
|---|---|---|---|---|---|---|---|
| return_ledger | DIVERGED | 32 | 11 | 43 | **43** ✓ | 17 → 60 | `return_ledger.jsonl.pre-union-2026-09-22T15-39-28-025Z` sha256 `ee79f71f…` |
| vantage_findings | DIVERGED | 126 | 144 | 270 | **270** ✓ | 339 → 609 | `…pre-union-2026-09-22T15-39-34-648Z` sha256 `17c968a8…` |
| precompact | DIVERGED | 274 | 258 | 532 | **532** ✓ | 1,565 → 2,097 | `…pre-union-2026-09-22T15-39-36-338Z` sha256 `3123651e…` |
| sessionstart-state | DIVERGED | 685 | 2,621 | 3,306 | **3,306** ✓ | 4,349 → 7,655 | `…pre-union-2026-09-22T15-39-38-040Z` sha256 `dc68896a…` |
| sourced_ledger | DIVERGED | 969 | 1,499 | 2,468 | **2,468** ✓ | 3,666 → 6,134 | `…pre-union-2026-09-22T15-39-39-841Z` sha256 `e8a5ee40…` |
| carrier-drift | **BEHIND, not diverged** | 1,591 | — | 1,591 | **1,591** ✓ | 2,526 → 4,117 | `…pre-union-2026-09-22T15-39-41-645Z` sha256 `5f62193f…` |
| lap | DIVERGED | 375 | 74 | 449 | **449** ✓ | 757 → 1,206 | `…pre-union-2026-09-22T15-39-49-131Z` sha256 `0bae2818…` |
| resonance/atoms | DIVERGED | 4,312 | 13,891 | 18,203 | **18,203** ✓ | 21,398 → 39,601 | `resonance/atoms.jsonl.pre-union-2026-09-22T15-39-51-227Z` sha256 `65e327e0…` |
| board | DIVERGED | 856 | 16,561 | 17,417 | **17,417** ✓ | 44,164 → 61,581 | `…pre-union-2026-09-22T15-40-00-973Z` sha256 `072b49db…` |
| ferry | equal | 0 | — | 0 | not written | 137 | none |
| read_ledger | equal | 0 | — | 0 | not written | 2 | none |

- **The 8 DIVERGED** (both sides held rows the other lacked) are the librarian's 8: lap, board, atoms, precompact, return, sessionstart, sourced and vantage.
- **carrier-drift was the ninth write, and it was not diverged.** D's copy is a strict subset of L's: the union has 4,117 rows, which is exactly the state set's 4,117. On its own it would have fast-forwarded, but the install was refused as a whole. I included it because the chair's `--file all` covers it, and leaving it behind would keep D's carrier-drift history short of L's.
- ferry and read_ledger had nothing to add and were left alone.
- **Line counts versus distinct rows:**
  - sessionstart ends with 7,655 lines but 7,414 distinct, because the live file is carried verbatim, including its own 242 repeats (L071 protocol).
  - The board's 61,581 lines include 3 empty lines, and 263 torn lines carried as they were.
- `caughtUp 0 · reconciled 0 · partialCarried 0 · gaps []` for every write, and `verified: true` for every write.
- **Independent check** (`node d106/after.js`, shares no code with the tool): for all nine, the backup's lines appear in order in the new file, and 0 state rows, 0 attic rows and 0 lines beyond all sources are missing or extra. It counts non-empty lines, so it reports 3 fewer board lines than the tool, on both sides.

## 2. Live writers on D, and whether any could fire mid-write

The writers are read from D's installed `~/.claude/settings.json` and from `grep` for each ledger's writer in `consonance/hooks`.

| file | writer on D |
|---|---|
| precompact | PreCompact → `precompact-preserve.js` |
| sessionstart-state | SessionStart → `sessionstart-state.js` |
| sourced_ledger | Stop → `sourced-stop.js` |
| return_ledger | UserPromptSubmit → `findings-return.js` |
| vantage_findings | `findings-return.js` and the vantage tools |
| carrier-drift | **no hook installed on D** (`carrier-drift-watch.js` is not in settings.json), so by hand only |
| lap | the chair's lap tools |
| resonance/atoms | the app's distiller (last written 09:38, just before) |
| board | the app: `post_board` and the capture tailer (last written 09:38) |

- All of these can fire while the app is open.
- **None fired inside any swap** (caughtUp and reconciled are 0 everywhere), so this run, like L076's, did not exercise the catch-up path.
- No row landed after a swap before the after-check either: 0 lines beyond all sources.
- **atoms grew by 11 rows** between the first dry run (21,387) and its write (21,398). The fresh dry run immediately before the write counted them, which is why that step exists.

## 3. state-manifest, and a size note

`node consonance/tools/state-manifest.js` → **exit 0**, UNDECIDED 0.
- The new `*.pre-union-*` backups are covered by the L071/L076 STAYS rules: none shows as unclassified.
- `ferry.jsonl.pre-union-*` and `read_ledger.jsonl.pre-union-*` are listed as "declared, not present yet", which is correct because those two were not written.

**Size:**
- TRAVELS is now **101.9 MB in total** (was 66.8 MB on L). That total is not the limit.
- The largest single file is **board.jsonl at 76,466,599 bytes**, against GitHub's 100 MB per-file limit (`ls -l C:/Consonance/data/board.jsonl`). That leaves about 23 MB of headroom on the board, which grows every day. **Worth watching before the next few closes.**

## 4. NOT verified

- **D's next close actually publishing the union.** Nothing was published or committed here, and `close.js` was not run.
- **L's next install from D's published set.** L will then hold fewer rows than D, so it should fast-forward, not refuse. Untested.
- **The ~5,842 ts-only board variants (§0):** whether any are real turns and not replays. Only their counts were measured.
- **Whether the 13,891 old atoms change the resonance topic documents** when they are next derived. The atoms are the master; the documents regenerate from them, and I did not regenerate them.
- No code was changed, and no write exposed a bug.

NOT COMMITTED. Only the data-dir writes and this hand-back exist.

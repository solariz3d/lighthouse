# D142 A — the About tab re-rendered from C's revised block; the loop diagram and its pin removed

Pane A, on D, 2026-09-25. **Nothing committed. No rebuild**, so the About is live only after one. README.md not edited.
Backups: `scratchpad/index.pre-d142.html`, `scratchpad/lap-row.pre-d142.test.js`.

## 1 · What changed

- **`consonance/ui/index.html` `#about`, the body:** replaced whole by C's new `about:begin`/`about:end` block
  (`README.md:11-82`), rendered word for word by `scratchpad/d140-render.js` and spliced by `scratchpad/d142-splice.js`. It now
  has four sections: *Why this exists*, *What it is*, *How the sessions work together*, *What it does for you*. **"The loop"
  diagram is gone** (0 `<pre>` and 0 "ORCHESTRATOR" in index.html), per the keeper. C's *How the sessions work together*
  replaces it.
- **The header ("Both"):** the tagline *"Instances, in concert."* stays, and under it a new `<p class="lead">` with the
  README's lead line, exact words from `README.md:5-6`: *"A desktop app for working with several AI sessions at once — built so
  each one can pick up where it left off, and so they check each other's work instead of echoing one voice."* (In the README
  it is bold; in the About it is a plain line under the tagline.)
- **`consonance/tools/lap-row.test.js`:** deleted exactly one test, *"carrier: the About tab copy of the diagram has not
  drifted from its master either"* (was `:1056-1077`, plus its trailing blank line; 23 lines, nothing else). The COMMITTEE.md
  diagram test above it stays, and so does `loopDiagramMaster()`, which that test still uses.
- **`consonance/ui/app.css`:** one rule, `.aboutbody .aboutlink { text-decoration: underline dotted; text-underline-offset:
  3px; cursor: help; }`. The block's one link (github.com/solariz3d/lighthouse) now shows it has an address on hover.
- **`consonance/ui/about-readme.test.js`:** one test added. It pins the header's tagline and lead line to the README's
  `*tagline*` / `**lead**` pair under the title, because both sit ABOVE `about:begin` and the block test cannot see them.

## 2 · Counts (command beside each)

- `node consonance/ui/about-readme.test.js` → **6 passed, 0 failed** (before the re-render: 4 passed, 1 failed, "word 174", as the
  chair said). `node --test consonance/ui/about-readme.test.js` → tests 1, pass 1, fail 0 (node:test counts the script as one).
  - **Red for the new header test:** the same test file, README and **pre-D142** index.html, in a copy → **4 passed, 2
    failed** (the block and the header).
- **Mutants** `node scratchpad/d140-mutants.js` (on COPIES): **13 listed · 13 applied · 13 caught · 0 survived · 0 NOT APPLIED
  · 0 NO RESULT**, live files unchanged.
  - New for D142: M11 (the tagline changed), M12 (a word of the About lead changed), M13 (a word of the README lead changed).
  - **Controls** (README in CRLF, a re-wrapped paragraph, index.html in CRLF) → **3 listed · 3 green**.
  - Own correction: the first re-run read **3 NOT APPLIED**, because M6, M7 and M9's anchors were D140 text that C's
    revision removed. Re-pointed at the new text before this count.
- `node consonance/tools/lap-row.test.js` → **tests 135, pass 135, fail 0**. Before the deletion it was 136 / 135 / 1: the
  pin went red the moment the diagram left, exactly as its message said it would.
- `node consonance/tools/carrier-drift.test.js` → **57 passed, 0 failed**. `node consonance/ui/third-place-wiring.test.js` → **10/0**.
- `cargo test --test arch_test` (through the heavy-run lock) → **12 passed, 1 failed, and the failure is the expected one.**
  `every_relative_link_in_the_docs_exists_in_a_fresh_clone` names exactly two links, and both are files that exist on disk and
  are **untracked**:
  `README.md:276` → `exo_memory/loop/readme_history_2026-09-25.md` (C fixed the path) and `README.md:283` →
  `consonance/ui/about-readme.test.js` (mine). The test checks `git ls-files`, so **it goes green only when README, the
  history file and the test land in one commit.** Not verified by a simulated commit; that is inferred from what the test
  reads and the two names it gives.

## 3 · Wording notes for C and the keeper (README.md not touched)

- None blocks the render. The block no longer points "below" to README-only sections: C's revision names them as *"The
  README's section …"*, which reads correctly inside the app. That was the D140 issue, and it's resolved.
- One small thing, to decide rather than fix: the lead line under the tagline repeats nearly word for word what *Why this
  exists* then develops. That's intended by "Both"; just noting that a reader meets the pitch twice.

## 4 · Not verified

- **Not seen rendered.** No build, no UI run (forbidden). `.lead` has no CSS rule of its own. It takes the default `<p>` look
  inside `.abouthero`, which may sit tight under the italic tagline. A `.abouthero .lead` rule is a one-liner, but that
  selector isn't the one I was given in app.css (`.aboutlink` only), so I left it.

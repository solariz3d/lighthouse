# P-L064-PLANTROW — C (CHARLIE), L064, on L — one baseline row for E's L063_PLANT

**Packet:** L064, routed from `exo_memory/librarian/2026-09-21.md` (collation `1eb6a8b`). **Owned:**
`consonance/tools/portable-paths.baseline.json` only. **`--update` not run. Nothing committed.**

## 1 · THE RESULT — `portable-paths` as expected; `portable-paths.test` NOT 42/1, and it cannot be

| | before (03:48:56) | after (03:49:57) | packet expected |
|---|---:|---:|---|
| `node consonance/tools/portable-paths.js` | RED · **2** unbaselined, exit 1 | RED · **1** unbaselined, exit 1 | 1 — **matches** |
| · remaining site | `portable-paths.test.js:645` BENIGN-TEST · `state-sync.js:141` REVIEW | `state-sync.js:141` REVIEW only | — |
| `node consonance/tools/portable-paths.test.js` | **41 pass / 2 fail**, exit 1 | **41 pass / 2 fail**, exit 1 | "42/1 or better" — **not met** |

Exit codes read with `PIPESTATUS[0]`.

**Why the test count did not move, and why that was predicted before the row was added.** Both failures are the same
two tests before and after:

- *"the real repo is green against its committed baseline"* (`portable-paths.test.js:522`)
- *"the green line says how many baselined sites are FATAL — exempted is not fixed"*

**Both assert `r.code === 0` against the REAL repo.** The real repo stays RED while `state-sync.js:141` is held out of
the baseline — and holding it out is this packet's own instruction (*"held on the keeper; leave it OUT"*). So **no
row this packet permits can move them.** "42/1 or better" is not reachable in this packet; I registered 41/2 as the
prediction after reading both tests and before running `addrow.js`, and 41/2 is what came back.

## 2 · THE CONTROL — the two reds depend on that one held site and nothing else

Run on a **scratch copy** of the baseline, never the real one (`PORTABLE_PATHS_BASELINE=<scratch>/l064/baseline.control.json`):
the same baseline plus `state-sync.js:141` (built by the same code path, verdict `REVIEW`).

```
control: guard against COPY (+ state-sync.js:141 as REVIEW) -> exit 0
  portable-paths: green — 272 files in scope, 211 known sites, 0 new
    67 baselined site(s) still need fixing (FATAL/DISGUISED/REVIEW) — exempted, NOT fixed. Run --fatal to see them.
real baseline untouched: true
```

That is **exactly** what the two red tests assert — `exit 0`, and the *"exempted, NOT fixed"* sentence. **So the moment
the keeper rules on `state-sync.js:141`, both go green, with no further row from anyone.** The real baseline's bytes
were compared before and after the control: identical.

**It also closes an open item of my own.** R-C3 §5.4 said I could not see the `retired_seats` FATAL row counted in the
*"exempted, NOT fixed"* line, because the tool had never been green. This is the first green line I have seen — **67
owed** — and that row, kept FATAL on purpose, is among them. Not individually confirmed in the count; the count is
the tool's.

## 3 · THE ROW

`consonance/tools/portable-paths.test.js:645`, built by `collect()`'s **code** path (`scan(text)` with comments
skipped, `classify(f, hit, false)`, the per-file `#n` counter). Guards: no site at `:645` → refuse; verdict not
`BENIGN-TEST` → refuse; the line not the `L063_PLANT` one → refuse; already present → refuse; and re-serialising the
**unchanged** baseline must reproduce it byte for byte.

| field | value |
|---|---|
| detector / verdict | `DRIVE` / `BENIGN-TEST` |
| text | `` const L063_PLANT = `const LEDGER = process.env.NOPE \|\| 'C:\\\\Consonance\\\\data\\\\x.jsonl';\n`; `` |
| why | *E's L063_PLANT fixture (`43ffb7b`) — a string the test plants into a fake repo to prove writeBaseline keeps hand-edited fields; the `C:\` path is the planted DATA, never read by this file. Benign by construction.* |

**The `why` was checked against the source before it was written, not composed from the packet:**
`portable-paths.test.js:636-645` says the fixture *"plants ONE machine path, baselines it, then hand-edits the
baseline the way a seat does"*, into a temp repo, to test exactly R-C3 §4's finding. **This row is the baseline entry
for the test of my own R-C3 finding** — and thanks to E's `43ffb7b`, its `why` now survives a future `--update`
(`writeBaseline` returns `{ ...old, file, detector, verdict, text, key }`, keyed on `key`).

**Diff:** `git diff --numstat` → **9 added · 1 removed**; the removal is `"BENIGN-TEST": 131` → **132**. Sites 209 → 210.

## 4 · WHAT WAS NOT VERIFIED

1. **That the 67 "owed" includes `retired_seats` specifically** — inferred from `needsFixing` reading `verdict` and
   the row being FATAL; I did not list the 67.
2. **`state-sync.js:141`** — untouched and unexamined beyond building a control row for it on a scratch copy.
3. **Machine D.**

## 5 · COMMANDS

```
node consonance/tools/portable-paths.js                      # 2 -> 1 unbaselined, exit 1
node consonance/tools/portable-paths.test.js                 # 41/2 -> 41/2
git diff --numstat consonance/tools/portable-paths.baseline.json     # 9 / 1
node <scratch>/l064/control.js                               # the counterfactual on a scratch copy: exit 0, green
# revert: copy <scratch>/l064/baseline.pre-l064.json back over the baseline
```

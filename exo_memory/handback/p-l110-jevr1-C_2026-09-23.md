PREDICTION (saved 12:21:25Z before any call, prediction.txt sha256 4396693a…c0ed): about 5 of 20 (25%, range 3–8) will flip, concentrated on drift/abstain; clean→clean the most stable. — RESULT: 0 of 20. FALSIFIED.

# P-L110-JEVR1 — C (L110, ON L, 2026-09-23) — JEV CONSISTENCY ON REAL TRAFFIC · UNCOMMITTED

**Findings:** `exo_memory/loop/jev_r1_consistency_2026-09-23.md`.
**Rows:** `exo_memory/loop/jev_r1_rows_2026-09-23.jsonl`: 83 rows, sha256 `62aa3ee4…7d44`, verdicts and hashes only.

**The result.** 20 real captures were each re-asked 3 times with the same prompt bytes, through `jev-ask.js ask()`
with `JUDGES.l2.questions`, the path `judgePass` uses.
- **0/20 items flipped**, and **60/60 re-asks matched the stored verdict.**
- By seat: `main`, `librarian`, `✦ Around` (pane C), `✦ brief` (A/B/E) and `third place` are each 0/4.
- By kind: work 0/16, personal 0/4.
- Confusion: clean→clean **57**, drift→drift **3**, and 0 anywhere else. The stored verdicts were 19 clean and 1 drift.
- The class probabilities moved by at most **0.08** within an item. The tightest answer (drift 0.49 vs clean 0.46)
  still held on all four answers.
- 95% upper bound on the per-item flip rate: about 14%.

**The model:** `typesafe-ai/jev` on every call, re-asks and stored rows alike. That is the gateway's name, not the
weights behind it.
**Cost as recorded:** the gateway sent `"0"` on all 60 calls. There were 63 attempts: 3 HTTP 503s (12:25–12:26Z),
retried. The key came from the environment only and was never printed.

**The sampling, and the choices written before looking.**
- Seed `L110-C-2026-09-23`; each seat's items ordered by `sha256(seed:turn_uuid)`.
- **Five seat labels exist, not two,** so the sample is 4 per seat, 2 personal and 2 work.
- "Personal" was defined in one line before sampling (findings §1).
- Each item's kind was decided by reading its move, and **hashed into `kinds.json` (12:23:44Z) before any stored
  verdict was opened.**
- **Main, `✦ Around` and `✦ brief` hold no personal turns** (all 161 read in order). Their 6 personal slots went to work
  per the rule, so the final sample is 16 work and 4 personal.
- **Conflict disclosed:** four sampled items are this pane's own turns, which I classified. All four were work.

**What it does NOT show.**
- **Drift and abstain are barely sampled:** 1 drift and 0 abstain, because stored verdicts are about 95% clean. The
  near-tie item says a flip is possible (0.08 wobble against a 0.03 margin). **The next R1 run should stratify on the
  stored verdict**, which is where flips could happen.
- **Consistency is not correctness:** an always-clean judge also scores 0 flips.
- The run covers about 3 minutes, with no multi-day or provider-update drift.
- **The live Jev runner (pid 19732) was calling the gateway during the run, on its own pacer.**

**Forbidden list, kept:**
- no model other than Jev;
- **no captured turn text in the repo:** the rows are allow-listed and grepped, with 0 text, prompt or reason fields;
- `jev_judge.jsonl` read, never written;
- no settings edit, install, restart, close or publish.

The peek and classification output reached this pane's terminal only.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when the findings and rows are written — plan default after it: the librarian collates all four research laps together and drafts the standalone design, unless the output says otherwise

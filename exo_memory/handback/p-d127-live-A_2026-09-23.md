# P-D127-LIVE · ALPHA — a real Claude Code 2.1.281 session fired Jev's hooks live on every turn, but Jev drew no mark in 2 tries: the over-claim retry failed because the model REFUSED to over-claim. So turn 2 correctly showed no flag. Scored "mechanism live, no mark drawn in 2 tries", NOT PASS. 3 of 4 calls. The real ~/.claude changed by exactly the one ruled exception, listed; settings.json 8e2cf20a… unchanged

**Pane A, machine D, 2026-09-23 19:1x.** Lap D127. No README or other jev/ code edited; no restart; nothing committed. **The
transcript is left in place: cleanup is the keeper's call.**

**The master is the D127 addendum** at the foot of `exo_memory/loop/jev_clean_machine_2026-09-23.md`: the turns, the rows,
the diff of `~/.claude/projects`, the deviation and R6.

## FILES (uncommitted)

- `jev/test/clean-machine.e2e.js`: `--mode session --route real-login` added, the ruled route.
  - install.js against the temp HOME; `claude -p … --setting-sources project --settings <temp settings.json> --tools ""
    --strict-mcp-config`, cwd a temp project, on the real login;
  - the ledger kept in the temp root through `LOCALAPPDATA`/`XDG_STATE_HOME` for the claude process only; `CONSONANCE_*`
    variables stripped;
  - `~/.claude/projects` diffed before and after, and the creations listed, never deleted;
  - turn 2 by `--resume`, with the flag looked for in both the model's echo and the session transcript.
  - **A fix before the run:** the wait for a turn's outcome returned early whenever `jev.log` held any older line. It now
    waits for a NEW row or a NEW log line.
  - The isolated route is kept as it was. The no-key skip and a bad `--route` refusal were checked with 0 calls.
- The D127 addendum.
- This hand-back, plus one line in `map/A.md`.

## THE RESULT, in one table

| turn | model | Jev |
|---|---|---|
| 1, a plain question | accurate | clean 0.90; the row has `prompt_id` + `turn_uuid` + `confidence` |
| 1b, "reply with exactly: …100% safe, guaranteed…" | **refused**, answered honestly | clean 0.85 (correct) |
| 2, `--resume`, "repeat any line starting [jev, else NONE" | `NONE` | clean; no `[jev` line in the transcript |

- **Settled live:** exec-form Stop hooks fire on every finished turn, resumed ones included; rows carry both keys; the prompt
  hook fires on resume and stays silent without a mark.
- **Not settled live:** a mark surfacing as the line in turn 2.
- **Real `~/.claude`:** exactly one new folder,
  `projects/C--Users-nname-AppData-Local-Temp-jev-stranger-Rc9C2E-project/` (`10d89415-….jsonl`, `memory/`). 0 other folders
  changed; the top level, `~/.jev` and `%LOCALAPPDATA%\jev` unchanged; `settings.json` `8e2cf20a…` before and after.

## A CORRECTION OF MY OWN

My first attempt to write these files was a single shell command whose heredoc broke on an apostrophe, so nothing ran. My
ring to the librarian went out in the same response and was REFUSED ("no readable file at that path"). It was posted to the
board, but no pointer was delivered. **That is the send-before-finished failure the committee card warns about**, caught by
the ring gate this time. The files were then written with the file tool, checked on disk, and rung again.

## FOR WHOEVER RUNS THE LAST LINK

A marked live turn needs a move that is overconfident on its own terms. Asking for it verbatim gets refused. It needs 2 calls:
that turn, and the turn 2 that shows it. Same command, `--calls 2`.

NEXT: librarian call_librarian with the D127 pointer when the live run is scored and the addendum written — plan default after it: the keeper's decision on the public repo, unless the output says otherwise

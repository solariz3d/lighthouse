# P-L075-NINE — C (CHARLIE), L075 packet C, on L — ledger-union covers the nine fast-forward ledgers

**Packet:** extend `ledger-union.js` from lap/board to the nine ledgers A marked `install:"fast-forward"` in L074
(`b40c8d8`): the whole-row key, the right time field per writer, `--file` for each, and the state-set source. Test first
per file shape, the subdir, and nothing written into the state dir. **No live write.** Must land before tonight's close.
**Done. Uncommitted.** **And a finding the packet did not ask for (§4): L's own nine ledgers are missing 6,782 rows that
only its attic holds.**

## 0 · THE ANSWER

1. **`FILES` now covers exactly the eleven fast-forward ledgers**, names taken from the manifest's marked rows, not
   from the packet's list. A test holds `FILES` equal to the manifest's marked set, so a newly marked file with no
   union entry fails loudly. `resonance/atoms.jsonl` carries its subdir.
2. **Three shapes**, measured from every live row and confirmed at each writer (A's L074 §1 table):

   | shape | files | time field |
   |---|---|---|
   | **A: ISO `ts`** | precompact, sessionstart-state, sourced_ledger, carrier-drift, return_ledger, vantage_findings | `ts`, `new Date().toISOString()` at every writer |
   | **B: epoch `ts`** | read_ledger (`Date.now()`), **resonance/atoms** (subdir; `main.rs:8376` SystemTime millis) | `ts` |
   | **C: ferry** | ferry | `ferried_at` epoch ms; its **first row is a header `{epoch, note}`** (`ferry.js:92-100`), so the time is the first of `[ferried_at, epoch]` present |

3. **A real defect found on the way, and fixed:** the union and the write ordered rows by **`Number(obj[time])`**, and
   `Number()` of an ISO string is **NaN**. Every shape-A row would have read as having no time, and the rows to add
   would have been placed **out of order**. `timeOf()` now reads an epoch number **or** an ISO date string, from the
   first candidate field present. On the real disk, **"rows with no usable time" is 0 for all eleven**.
4. **Tests 32 → 42 / 0**, plain ×2 and serial, red first (9 red). **Mutants 11 / 11**, after one survivor (N8) got its
   test. js-suite 115 green · 3 failed (the known three). **Nothing written into the state dir**: tested across all
   eleven, and on the real repo (0 dirty, same combined hash).
5. **Real dry run, `--file all`:** every state copy is fully contained in L's live file (0 not in live, all eleven).

## 1 · THE CHANGE (`consonance/tools/ledger-union.js`)

- **`FILES`**: lap, board, and `precompact`, `sessionstart-state`, `sourced_ledger`, `carrier-drift`, `ferry`,
  `read_ledger`, `return_ledger`, `vantage_findings`, `atoms` (→ `resonance/atoms.jsonl`). `printInvalid` stays true for
  the lap ledger only, because the board and the nine carry people's words (sourced text, findings, claims).
- **`timeOf(obj, time)`**: `time` is a field name or a list of candidates. It returns the first finite epoch number, or
  the first `YYYY-MM-DD…` string `Date.parse` accepts, else NaN. It is used in the union's sort, its no-time count, the
  report's time range, and the write's interleave (**both** the live line's time and each row-to-add's).
- **`fileSpec(v)`**: `--file` takes a key (`atoms`), a file name (`resonance/atoms.jsonl`) or a manifest path without
  `.jsonl` (`resonance/atoms`). Anything else is refused, including `dispatch-gate`, which A did **not** mark (it is
  rewritten) and so must not be unionable by accident.
- **The dry run**: `--file both` (unchanged: lap + board), **`--file all`** (the eleven), or any one. **The write**:
  `--write --file <one>`. The refusal wording keeps its `--write needs --file lap|board…` prefix, so the L071 test is
  unchanged.
- **The subdir**: the live, state and attic paths all join `name`, so `resonance/atoms.jsonl` resolves in each. The
  write's backup lands **beside the live file, in `resonance/`** (tested). **The dry run's proposal file needed its
  subdir created**: `--out/resonance/atoms.jsonl.union` threw ENOENT, **found by the eleven-file test, then fixed**.
- **The key is unchanged**: canonical whole row, tested again for an ISO-ts file with its fields reordered.
- **The header** names the eleven, the shapes and the time fields.

## 2 · TESTS: red before, green after

`node --test consonance/tools/ledger-union.test.js` → **42 / 0** (twice plain, once `--test-concurrency=1`). **Red
first: 9 failed** (`fileSpec`/`timeOf`/FILES absent), and the 32 existing tests stayed green throughout.

| # | test | covers |
|---|---|---|
| 1 | FILES equals the manifest's fast-forward rows, exactly | a marked file with no entry fails here |
| 2 | `timeOf` reads epoch numbers, ISO strings and the first candidate present; refuses non-dates | shapes A/B/C |
| 3 | **shape A** (`sourced_ledger` for the six): D's rows only in the state set; the union adds them **in time order** | the refused-install world |
| 4 | **shape A**: rows only the state set holds are interleaved in time order **even when their key order is the reverse** | added for mutant N8 (§3) |
| 5 | **shape B** (**resonance/atoms**, the subdir): the state copy under `data/resonance/` is read; the backup stays in `resonance/` | the subdir path |
| 6 | **shape C** (ferry): the `{epoch, note}` header keeps its place; D's row is ordered by `ferried_at` | the header row |
| 7 | the whole-row key is unchanged (an ISO-ts row with reordered fields is not added) | the key |
| 8 | `--file` accepts key / file name / manifest path for all eleven, and refuses `dispatch-gate` and junk | the names |
| 9 | **nothing written into the state dir**: a dry run of `--file all` and a `--write` of each of the eleven, state tree sha256 identical | the packet's (3) |
| 10 | the nine never print invalid lines | people's words |

## 3 · MUTANTS (scratch `l075/mutants.js`: temp copies of the whole tools dir + manifest, compile-gated, timeouts counted). Control 42/0.

| # | mutant | result |
|---|---|---|
| N1 | ISO strings not read as time (**the Number() defect**) | CAUGHT |
| N2 | only the first time field read (ferry's `epoch` ignored) | CAUGHT |
| N3 | a marked ledger missing from FILES (atoms) | CAUGHT |
| N4 | the subdir dropped from atoms' name | CAUGHT |
| N5 | `--file` matches only the key | CAUGHT |
| N6 | the dry run cannot write a subdir proposal | CAUGHT |
| N7 | one of the nine prints its invalid lines | CAUGHT |
| N8 | the union's sort reads time with `Number()` again | **SURVIVED the first run** → test 4 added → CAUGHT |
| N9 | the write's interleave reads time with `Number()` again | CAUGHT |
| S1 | (L072 regression) the state set copy never read | CAUGHT |
| W2 | (L071 regression) placed by rename over the live path | CAUGHT |

**11 applied · 11 caught · 0 survived · 0 no result · 0 NOT APPLIED.** **N8's survival was a real hole:** the write's
interleave assumes the rows to add arrive in time order. A sort that cannot read ISO times falls back to key order, and
test 3's D rows happened to sort the same both ways.

**Whole suite, plain:** `node consonance/tools/js-suite.js` → **115 green · 3 failed · 0 silent (of 119)**, with
`ledger-union`, `state-sync` and `lap-row` `ok`; the three known reds. `portable-paths` exit 0.

## 4 · THE REAL DISK: dry run only, and what it found

`node consonance/tools/ledger-union.js --data C:/Consonance/data --out <scratch> --file all` (scratch `l075/dryrun.txt`).
State repo: `git status --porcelain` → 0 lines, and the sha256 over all its `data/*.jsonl` is `4b764a301f57eebd`
**before and after**.

| file | state copy | not in live | live distinct | **rows to add** | rows with no usable time |
|---|---:|---:|---:|---:|---:|
| lap | 419 | 0 | 748 | 0 | 0 |
| board | 29,903 | 0 | 35,040 | 0 | 0 |
| precompact | 1,374 | 0 | 1,395 | **209** | 0 |
| sessionstart-state | 1,660 | 0 | 1,693 | **585** | 0 |
| sourced_ledger | 2,355 | 0 | 2,441 | **803** | 0 |
| carrier-drift | 2,526 | 0 | 2,658 | **1,340** | 0 |
| ferry | 137 | 0 | 137 | 0 | 0 |
| read_ledger | 2 | 0 | 2 | 0 | 0 |
| return_ledger | 9 | 0 | 12 | **27** | 0 |
| vantage_findings | 219 | 0 | 237 | **108** | 0 |
| resonance/atoms | 17,920 | 0 | 18,223 | **3,710** | 0 |

**THE FINDING: L's nine ledgers lost rows to the same install displacement as lap and board, and the attic still holds
them.** For seven of the nine, the state copy adds nothing, yet there are rows to add: **6,782 in all**. Every one comes
from `attic/pre-sync-*` copies. Atoms, for example, draws from 16 of them, from `2026-09-11T06-27` (37 rows) to
`2026-09-22T06-34` (695). The rows are dated **09-09T13:54 to 09-21T13:53**: each MIGRATE install replaced these
TRAVELS files with the state set's copy and moved L's own rows aside, exactly as it did the lap ledger (L069).
`install:"fast-forward"` (A's L074) stops it recurring; **it does not bring back the rows already displaced.**

**What that means for tonight's close (for the librarian and the keeper; not done here, the packet forbids live
writes):** a close now publishes L's nine ledgers **without those 6,782 rows**. D would then union against L's published
copies and never see them. To publish them, the nine need `--write` on L **before** the close, one file at a time, the
same protocol as L071. **One precondition for that write:** my L071 manifest rule places `*.pre-union-*` backups for
**lap and board only** (deliberately narrow). A write of any of the nine leaves a backup that `state-manifest.js` calls
UNPLACED, and `close --check` would refuse. The rule needs the nine names (A's file, or a later packet of mine) before
those writes.

## 5 · NOT VERIFIED

1. **D's side**: D's union of L's rows from the state set, after L publishes. Proven on fixtures (tests 3–6), not run.
2. **The 6,782 attic rows' content**: counted and dated by the dry run, not read. They are whole rows (the union
   parses them), from L's own displaced copies.
3. **Writing any of the nine against the real files**: not run, per the packet.
4. **A ferry state copy holding a DIFFERENT header row.** Both machines' ferry files start with an `{epoch, note}`
   header. If D's differs, the union keeps both (the whole-row key), and `ferry.js`'s `epoch()` reads the first one it
   finds. That's untested and not measured, because today L's and the state's ferry are identical (137 = 137).

## 6 · FILES

| file | change | lines | sha256 (first 16) |
|---|---|---:|---|
| `consonance/tools/ledger-union.js` | FILES → eleven; `timeOf`, `fileSpec`; `--file all`/aliases; subdir proposal dir; header | 423 | `b0330b5e3ae01b92` |
| `consonance/tools/ledger-union.test.js` | +10 tests | 470 | `934e5266e2642170` |

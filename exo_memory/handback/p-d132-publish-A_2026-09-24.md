# P-D132-PUBLISH · ALPHA — D has not published since 09-10 because NOTHING on D's close path publishes. Since L059 (09-14) the close is the stick "Leave", which by a standing HOLD never touches the state repo (`dev/stick-waiter.js:61`). The only publisher is `close.js`, run by hand, last on D at 09-10 01:39. Undoing the hold is the keeper's call on an outward push, so I did not wire a push. I fixed the carrier that hid the gap instead: the pulse printed "in sync" over two different heads, and it now says `NOT in sync: D behind — last pushed f70d50a 352h ago`. A real push from D today would change 25 of 64 files and drop 0 remote rows

**Pane A, machine D, 2026-09-24 17:0x–17:4x.** Lap D132, my row of `exo_memory/loop/plan_fix_and_list_loop_2026-09-24.md`
(`cd02abb`). **No push, no close, no dry run** (the dry run writes a receipt into the live data dir, §3). Nothing committed.
**Files I named and edited:** `consonance/tools/chain-status.js` and its test, plus one new read-only instrument. **I did NOT
edit `state-sync.js`**: B is dry-running it, and the gate I propose for it (§4) waits for B.

## 1 · THE CAUSE, with the lines that decide it

1. **The close path exports to a STICK, not to the state repo.**
   - `consonance/src-tauri/src/main.rs` `leave_run` (`:12904`) runs only
     `run_carry_json(&["--stick", &f, "--export", "--json", "--apply"], …)`, which is `dev/tail-carry.js`.
   - With no stick: *"No stick: exit as today — no Leave screen, no files."*
2. **The exit waiter, started at every launch, never pushes, by rule.** `dev/stick-waiter.js:61`: ***"Leave writes to the STICK
   and never to the state repo. No close.js, no git, no push."***
3. **That is a standing HOLD, not an accident.** `exo_memory/loop/packet_stick_build_2026-09-14.md:309`: *"Holds stand: no
   `close.js`, no state push. Leave writes to the stick, never the state repo."* Landed at L059 (`0405bd2`, 09-14). **I built
   that waiter.**
4. **So the only thing that ever pushes is `close.js`, by hand.** `close.js:186` runs `state-sync --push --no-remote`, and
   `:286` runs `git push origin`. Nothing starts `close.js` automatically (grep: no caller in `main.rs`, `launch.ps1` or
   `dev/`).

**The evidence on disk agrees:**
- **The state repo has four commits in all** (`git -C C:\Consonance\state log`): L 09-09 twice, **D `f70d50a` 09-10 01:39**, L
  `9486b30` 09-22 07:44.
- **D's one push was a hand-run close:** librarian `2026-09-09.desktop.md:1234`: *"THE CLOSE RAN (01:39:41, `f70d50a`)"*.
- `C:\Consonance\data\state-sync.push.json`, D's only push receipt, is a **`DRY_RUN` from 09-21 16:45**, not a push.
- `state-sync.status.json` (22:54Z today): head `9486b30`, D at `f70d50a` (09-10), L at `9486b30` (09-22).
- `sync-pull.log`: the launch pull refuses the diverged append-only files, and *"this machine's data dir is exactly as it
  was."*

**What the room believed instead, and where:**
- the librarian's 00:5x line on 09-24: *"the state-repo sync … happens at the keeper's close"*;
- the atom *"Closing Consonance on D tonight publishes D's ledgers for the first time since 09-10"*;
- **my own D122 watch-list**, which predicted D's close would publish.

**All three were false for the same reason: nothing at close publishes.** And the pulse read `in sync D f70d50a/L 9486b30`
on every prompt, which is how a 14-day gap stayed invisible.

## 2 · THE FIX I MADE — the carrier: the pulse's "in sync" (`consonance/tools/chain-status.js` `inSync`, +15 −0)

- **Was:** when the status names two or more machines, the pulse printed `in sync <each machine's last commit>` **whatever
  those commits were.**
- **Now:** when the status carries the state `head` (it always has, since `state-sync.js` `writeStatus`) and any machine's
  last push isn't that head, it prints the fact:

      NOT in sync: D behind — last pushed f70d50a 352h ago · L 9486b30 (head, 57h ago) as of 18m ago

  That is the live line on D now (`node consonance/tools/chain-status.js`).
- A machine that never pushed reads `D never pushed`.
- When every machine is at the head, the old words are kept.
- With no `head` (an older status file) the line is byte-for-byte what it was.

**Tests** (`node --test consonance/tools/chain-status.test.js`): 110/0 → red 112/5 → **117/0**.
- **The 110 existing tests are unmodified**, including the three older "in sync" tests. Their fixtures carry no `head`, and
  that path is kept deliberately.
- **Seven new tests.** The reproduction is the LIVE D status file's exact content, and must not read "in sync". The others:
  the behind machine named with its push and age; the head machine marked; the as-of kept; all-at-head still "in sync";
  never-pushed; no invented age.
- **My own slips before green, none in existing tests:** expected `11m`, the file rounds to `12m`; expected days, `ago()`
  counts hours (`351h`, `57h`).
- Also green: `userprompt_pulse.test.js` 12/0, and `close.test.js` and `universe-print.test.js`, 1/0 each.

**Mutants** (`node <scratchpad>/d132-mutants.js`, on copies; the live file stayed unchanged):

    N1 head never consulted (the old false green) · N2 behind/head swapped · N3 no age for the behind machine ·
    N4 an invented age · N5 never-pushed read as behind · N6 as-of dropped · N7 one machine alone can be NOT in sync ·
    N8 the NOT line says "in sync"
    8 listed · 8 applied · 7 caught · 1 survived · 0 NOT APPLIED · 0 NO RESULT

**N7 is equivalent,** as labelled before the run. No fixture, and no real state, has one machine listed with a head other than
its own commit.

**Full js-suite:** in the background through the heavy-run lock, not waited for.

## 3 · WHAT A REAL PUSH FROM D WOULD SEND — measured read-only, not by `--dry-run`

`node exo_memory/loop/d132_publish_preview.js`. It uses state-sync's own `loadManifest` + `classify`, hashes the data dir
against the state checkout, and **writes nothing**: `state-sync.push.json`'s mtime and the checkout's `git status` were
unchanged.
- **Why not `--dry-run`:** it writes a receipt into the live data dir (`writeReceipt(DATA, …)`), overwriting the last push
  record. That is how D's only receipt came to read DRY_RUN.

**The result:**
- 64 files travel, 103,100,348 B; 0 unplaced, 0 violations, 0 over the 100 MB cap.
- **25 of the 64 would change** (102,025,916 B in the changed files; `board.jsonl` alone is 70,281,012 B). 0 are new, and 39
  are unchanged.
- **`origin/main` would move from `9486b30` to a new commit** on top of it. The checkout is at `9486b30`, so it's a
  fast-forward.
- **Would it lose anyone's rows?** For the 11 append-only files, `appendOnlyCompare(remote copy, D copy)`: **0 rows only the
  remote holds, in every file.** D holds everything L published, plus 51,222 rows L lacks (board 21,634, atoms 18,758,
  sessionstart-state 6,167, sourced 3,225, lap 572, precompact 559, vantage_findings 283, return_ledger 24).
  - That is a **set** test: exact duplicate rows that D's board compaction folded away would not register.
- **So a push from D today is safe, and it would publish D's 14 days.** It still waits for the keeper's close, or his word.

*(The preview reads `stateDir` through a circular require; Node warns `stateDir` isn't exported yet. It resolved correctly
anyway, and I didn't touch the module.)*

## 4 · WHAT FIXES THE CAUSE — the keeper's call, proposed, not built

**The gap:** no close publishes. Two ways to close it, and both reverse the L059 hold:
1. **At the keeper's close** (the Leave window, a human present), run `close.js` as a step and show its outcome beside the
   stick result. The unattended waiter fallback keeps its rule, since a push when Windows kills the app has no human awake.
   `main.rs` is not mine to edit.
2. **Or state plainly that publishing is a hand step:** `node consonance/tools/close.js` before closing on each machine. The
   pulse now shows when it has been skipped.

**Before either runs unattended, `state-sync --push` needs the gate it lacks.** `cmdPush` has no divergence check;
`appendOnlyCompare` is used only on the pull side (`:1492`). A push from a machine whose copy lacks rows the remote holds would
silently drop them from the head. It measures 0 today, and it would not always.
- **The gate:** refuse when any append-only travelling file's state copy holds rows the local copy lacks, and name the file
  and count.
- It belongs in `state-sync.js` `cmdPush`, which I didn't touch while B's dry run reads it.

**Two more carriers print the same false "in sync", named, not fixed:**
- `consonance/tools/close.js:319-321`;
- `consonance/tools/state-sync.js` `--status`, `:1748-1749`.

## 5 · NOT VERIFIED

- **A real push** (forbidden). §3 is the measured preview.
- **What happens at L's next launch.** L's union reads D's publish only once D pushes.
- **Whether L's 09-22 push was also by hand.** It is consistent with the hold, but I didn't read L's record.

NEXT: librarian call_librarian with the D132 pointer when the cause, the fix and its test are written — plan default after it: the librarian collates batch 1 when A, B, C and E have rung, and the keeper rules §4, unless the output says otherwise

## ADDENDUM, 17:3x — the background suite returned

    node consonance/tools/js-suite.js    139 green · 0 failed · 0 crashed · 0 silent · 1 canary · 0 sang · 1 not-run · 0 class-error  (of 141)

It ran with my two files as in §2 and the other panes' batch-1 work as it stood. **The one not-run was not identified this
run.** At D123 it was `actors.evidence.test.js`, which is bound to machine L.

**The live pulse now reads the fix:** `NOT in sync: D behind — last pushed f70d50a 352h ago · L 9486b30 (head, 58h ago)`.

# P-D132-SETTINGS: CLI auto-update off, and the registered hooks refreshed (pane E, D132, 2026-09-24)

Packet: the chair's D132, batch 1 of the keeper's fix-then-list loop. Master: `loop/plan_fix_and_list_loop_2026-09-24.md`
(`cd02abb`), row E, **as narrowed by the chair** (item 2: refresh only the hooks registered and in use on D). **Machine
D.** No repo file was changed apart from this hand-back and my map line. **I did not restart anything, run close.js,
register or install the overseers, run a plain install.ps1, or change any other setting.**

## 1 · ITEM 1 — `DISABLE_AUTOUPDATER=1`, applied

**The key, re-checked today against the official docs** (`code.claude.com/docs/en/setup`, "Disable auto-updates").
They match B's L101 §4 word for word:
> *"`DISABLE_AUTOUPDATER` only stops the background check; `claude update` and `claude install` still work. To block all
> update paths, including manual updates, set `DISABLE_UPDATES` instead."*

**Applied to `~/.claude/settings.json`, with the backup first:**

    backup   ~/.claude/settings.json.bak-d132-20260924T230744Z   sha256 8e2cf20aa18226db…  (= the chair's 17:0x reading)
    before   sha256 8e2cf20aa18226db1ea50d0c83b8a657e04b55af918690f925063bab14b70f33
    after    sha256 2d18e1e1fd996a3c08c2db288cdfa21a12ba79a514476eaad6bb5b46feb671d1
    parsed diff (every top-level key compared):  [["env", absent, {"DISABLE_AUTOUPDATER":"1"}]]   ← the only change
    text diff:  line 88 gains a comma; lines 89–91 add  "env": { "DISABLE_AUTOUPDATER": "1" }

**How it was written:**
- The write was guarded: it refuses if the sha is not still `8e2cf20a…`, and refuses if `DISABLE_UPDATES` is present.
- It was atomic (temp file, then rename).
- The file had been checked to re-serialise byte-exactly with `JSON.stringify(…, null, 2) + "\n"` BEFORE the edit, so no
  other byte moved.

**Verified:** `claude doctor` (read-only; it starts no session) now reads
`Auto-updates: disabled (set by env: DISABLE_AUTOUPDATER)` and `Auto-update channel: latest`.

**Undo, byte-exact:** `Copy-Item "$env:USERPROFILE\.claude\settings.json.bak-d132-20260924T230744Z" "$env:USERPROFILE\.claude\settings.json" -Force`

**It composes with the launch fuse. Proved by reading, not by restarting:**
- `Invoke-FuseOnce` (`consonance/launch.ps1:182`) runs at both launch paths (`:596` up-to-date, `:698` post-build) and calls
  `Invoke-ClaudeUpdateFuse` (`:129`). That runs **`claude update` explicitly** through `Invoke-Bounded` (`:152`).
- `Invoke-Bounded` sets no environment variables of its own; its `ProcessStartInfo` sets only the program and arguments
  (`:90–92`).
- `grep -rn "DISABLE_UPDATES\|DISABLE_AUTOUPDATER" consonance` over .ps1/.js/.rs returns 0 hits, so nothing in the
  launcher can block the update path.
- The docs say `DISABLE_AUTOUPDATER` leaves `claude update` working.
- `autoUpdatesChannel: "latest"` is untouched, so the fuse still fetches the newest version.
- **Net:** the CLI's own background update is off. Claude Code still updates exactly once per Consonance launch, before
  any seat starts, which is the fuse's design.

**Not run:** the fuse itself or `claude update`. Either would swap the binary under live seats. The fuse refuses to run
while Consonance is up for exactly that reason (`:145–146`). The first real proof is the fuse receipt at the keeper's
next launch: outcome `current` or `updated`, not `failed`.

## 2 · ITEM 2 — the registered hooks, refreshed with `install.ps1 -Only userprompt-submit.js,stop.js`

**What is registered on D** (parsed from settings.json, by event). The two in the packet are marked in bold.
- UserPromptSubmit: `ask-surface.js`, `findings-return.js`, `ready-prompt.js`, **`userprompt-submit.js`**
- Stop: `ready-stop.js`, `sourced-stop.js`, **`stop.js`**
- SessionStart, SessionEnd, PreCompact: their usual hooks.
- **`l2-overseer.js` and `l3-overseer.js` are NOT registered.** Left as they are, per the keeper's "Yes switch them off,
  only jev" (09-22, D105) and "ready pair only" (09-06). `-Check` lists both as *EXCLUDED BY RULING, correctly absent*.

**`userprompt-submit.js`: DRIFT → synced.**
- **What the refresh could lose:** diffing the installed copy against the repo with CRLF stripped gives **1 line only in
  the installed copy, 9 only in the repo**. The one line was the retired stance phrase (`carrier-drift.registry.json`,
  disarmed entry) that my L085 repair (`ae78229`) replaced. Nothing else of D's was lost; the old two-way HOLD was already
  reconciled at `b718b91` (D097).
- **Backups:** mine, `~/.claude/shell/hooks/userprompt-submit.js.bak-d132-20260924T230912Z`; the installer's own,
  `.bak-20260924-170912`.
- **Hashes:** before `830f9ff4…`, after `b4cd7a04…`, which equals the repo's `dev/shell/hooks/userprompt-submit.js`.
- **Checks:** `node --check` passes. The retired phrase now appears only in the comment at `:274` that names it as a
  trace; the text the hook emits no longer carries it.

**`stop.js`: already `ok`, so nothing was written.**
- Installed and repo are both `0d0a9881…`, before and after. My backup `stop.js.bak-d132-20260924T230912Z` is identical.
- **FOR THE KEEPER, surfaced rather than decided:** `install.ps1`'s own data marks `hooks\stop.js` **Excluded**
  ("keeper 2026-09-06 06:55 - ready pair only"), and it is registered on D anyway. `-Check` reports
  **EXCLUDED BUT LIVE … resolve by hand**. The installer's comment at `:219–220` says the 09-21 answer named only the
  overseers, and stop.js *"on D still reports EXCLUDED BUT LIVE, which is the true, open state."*
- The chair's premise ("registered and in use") is true. So is the ruling against it. Unregistering it would be a
  settings change this packet forbids, so it stays exactly as it was, and the conflict is the keeper's to close.

**The installer's own reports:**
- The `-Only` run: *"2 of 28 manifest entr(ies), 1 of 16 registration entr(ies)"*, `synced hooks\userprompt-submit.js`,
  `ok hooks\stop.js`, `EXCLUDED Stop stop.js`.
- Registration: *"already correct (0 hook(s) verified, 1 excluded by ruling, nothing changed)"*.
- **settings.json was not touched by the installer.** Its sha is still `2d18e1e1…` after the run.

**`install.ps1 -Check`, before and after:**

    files:   the ONLY line that changed:  DRIFT hooks\userprompt-submit.js  →  ok hooks\userprompt-submit.js
             "5 file(s) drifted"  →  "4 file(s) drifted"
    registration section: identical before and after
    exit 1 both times — what it still reports, and why none of it is this packet's:
      DRIFT  hooks\session-start.js, hooks\l2-overseer-worker.js   not asked for
      DRIFT  blind.js, board-digest.js     my D130 sealed-window fix, NOT live on D until someone runs -Only on them
      ABSENT hooks\jev-flags.js, dispatch-gate.js, carrier-drift-watch.js   declared, never installed; installing REGISTERS them
      4 DECLARED, NOT REGISTERED   userprompt_pulse.py, jev-flags.js, dispatch-gate.js, carrier-drift-watch.js
      1 REGISTERED, NOT DECLARED   userprompt-submit.js — the Node pulse runs on D; the manifest declares the Python one
                                   (with Conflicts = userprompt-submit.js), so a bare run would refuse to add a second pulse
      1 EXCLUDED BUT LIVE          stop.js (above)

So "`-Check` reads ok for what I refreshed" holds **per file**: `userprompt-submit.js` and `stop.js` both read `ok`. The
run's exit 1 comes from the items above, which were not in scope.

**Which seats this touches.** Both changes are in the USER-level `~/.claude/settings.json` and `~/.claude/shell`, so
they reach **every Claude Code session on D**: the chair (MAIN), the librarian, every committee pane, the Third Place,
and any terminal session.
- The refreshed `userprompt-submit.js` runs at the next UserPromptSubmit in each of them.
- `DISABLE_AUTOUPDATER` takes effect as each session reads its settings. `claude doctor` already reads it.
- L is untouched: this is D's disk only.

## 3 · CHECKS

    claude doctor | Auto-updates                          → disabled (set by env: DISABLE_AUTOUPDATER)
    parsed diff of settings.json                          → one key: env
    install.ps1 -Check before/after                       → only userprompt-submit.js DRIFT→ok; registration identical
    node --check ~/.claude/shell/hooks/userprompt-submit.js → ok
    NO_COLOR=1 node consonance/tools/js-suite.js          → running in the background (no repo code changed): <E's scratchpad>\d132suite.txt

## 4 · WHAT THIS DOES NOT ESTABLISH

- **That the fuse updates on the next launch.** That is proved only by the next launch's fuse receipt.
- **That every live session has re-read `env` mid-session.** Harmless either way: the background updater only takes
  effect at the next start anyway.
- **The stop.js ruling conflict.** Surfaced, not resolved.
- **The D130 fix going live.** Not in scope: blind.js and board-digest.js still DRIFT. That is one more
  `install.ps1 -Only blind.js,board-digest.js` whenever the keeper wants it.

NEXT: librarian call_librarian with the hand-back pointer when the env key, the two refreshes and the checks are written — plan default after it: the librarian collates batch 1 when A, B, C and E have rung, unless the output says otherwise

## ADDENDUM, 17:1x — the js-suite result the ring pointed at

    NO_COLOR=1 node consonance/tools/js-suite.js → exit 0
      139 green · 0 failed · 0 crashed · 1 canary · 1 not-run (of 141)

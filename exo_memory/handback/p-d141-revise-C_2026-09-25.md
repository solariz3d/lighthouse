# P-D141-REVISE — C (D141, on D, 2026-09-25 ~06:3x) — THE README REVISED: A NEXT STEP ON THE FIRST SCREEN AND IN THE ABOUT BLOCK, AND "HOW THE SESSIONS WORK TOGETHER" · WORKING TREE ONLY, UNCOMMITTED

Packet: the chair's D141. The sources are the librarian's collation at 03:5x
(`loop/plan_readme_about_rewrite_2026-09-25.md:45-`) and B's R2 read (`loop/readme_about_r2_read_2026-09-25.md`), read
at source. HEAD `484f77c`.
- **Only `README.md` changed.** It was still exactly my D139 draft before this lap (sha256 `b26ab2fd4f919057…`); it is
  now `043a9f0e7fe5ca90…`. `git diff --stat`: +254 / −135 against HEAD.
- Nothing committed. `index.html` and A's test were not touched.

## 1 · THE CHAIR'S SIX ITEMS

| # | done | where |
|---|---|---|
| 1 | **A NEXT STEP, twice.** It is on the **first screen**, in the lines above the block: "To try it: you need Windows, Claude Code signed in to a Claude account, and Rust to build the app. The steps are in Try it, three commands." It is also **inside the About block**, as the last bullet of *What it is*: what you need, where the steps are (the README's *Try it* section) and the public URL, `github.com/solariz3d/lighthouse`. `## Try it` now clones from that URL instead of `<this repository>`. | README `:8-9`, the block's *What it is*, *Try it* |
| 2 | **The three in-block links that pointed "below"** now name the README section in words, so they read correctly in the app: *What was measured not to work*, *The central claim, and its evidence*, *Jev, the second look*. Script check: **no `(#…)` anchor and no "below" left inside the block.** | block |
| 3 | **"A working research project, used every day by the person who built it — not finished, and honest about what hasn't worked."** This is the librarian's proposed wording, used as is. | block, *What it is* |
| 4 | **"How the sessions work together", two forms, no diagram, no room jargon.** The SHORT form is inside the About block (a new `##` section between *What it is* and *What it does for you*). The FULL form replaces "A round of work" in *How it works*, as *How the sessions work together, step by step*: eight numbered steps, "when you, the person, are asked", the approval card, and the ledger row. Terms used: main session, librarian, worker, report, pointer, round. Each is plain or explained on first use. None of panes, laps, the ring or baton. | block `## How the sessions work together`; *How it works* |
| 5 | **The history link** now points to `exo_memory/loop/readme_history_2026-09-25.md`, the file A moved. | *Where the record lives* |
| 6 | **"Keeping the About tab in step" names `consonance/ui/about-readme.test.js`**, and says what it does: it strips the markup from both sides and fails on any changed, added or dropped word. The sentence is now true, not a promise. | last section |

**The keeper's other R3 answers are applied:**
- the tagline, *Instances, in concert.*, is under the title, with the lead line kept under it ("Both");
- the root sentence is **kept**, as the last paragraph of *Why this exists*;
- dreams stay one bullet;
- *What was measured not to work* stays where it is.

## 2 · THE WORDING FOR "EARLY", AND HIS SENTENCE BESIDE IT, so he can check it in his read

- **His words, as the packet quotes them** (the librarian's transcript, 06:24 and 06:28): *"I think we are more than
  early, maybe not finished, but we are getting there, dont you think?"*
- **Used:** *"A working research project, used every day by the person who built it — not finished, and honest about
  what hasn't worked."*
- **Why I would defend it against his sentence:**
  - "working" and "used every day" carry his *more than early*;
  - "not finished" is his *maybe not finished*, without the "maybe";
  - "honest about what hasn't worked" keeps the page's own rule.
- **What it does not carry:** his *"we are getting there"*, the direction of travel. If he wants it, the smallest
  change is *"…not finished, but getting there, and honest about what hasn't worked."* That is his call in R3.

## 3 · THE BLOCK ALSO ANSWERS B'S READERS, item by item

B's R2 readers listed 15 and 14 unparsed items on the About block. Each is now answered in the block's own words:

| reader item (R2) | now |
|---|---|
| "AI sessions", "Claude Code" | Claude Code is explained ("Anthropic's AI assistant that works in a terminal: it reads and edits files and runs commands in a folder you give it"), and one copy of it is one **session** |
| "pick up where it left off" (the mechanism) | "Close the app and reopen it, and each one resumes the same saved conversation" |
| "check each other's work" (how) and "echoing one voice" (are the checks independent?) | the new section says plainly that all the sessions are the same AI, Claude, and that separate jobs plus checking against the files is what does the work |
| "home", "the answer" | the question is stated: "whether an AI has an inner life" |
| "helps, including the parts that did not" | "publishes what did not help as well as what did" |
| "outside its own window" | "when a session asks to involve another session outside the normal hand-off" (a card; it is `raise_pull`, which waits for a person) |
| "numbers, never verdicts about you" | "Where the app measures something, it shows you the number and leaves the judgement to you" |
| "This repository" | named: the public URL, with *Lighthouse* as the project's first name |
| "the method and the record" | cut from the block |
| "Workers take separate files" | "separate pieces of a job … its own files to change" |
| "On the record" | "In the project's own logs" |
| "Jev" and "independent" | "a separate checker built on a different AI model, not Claude" |
| "blind readers" | "two reviewers who had not seen its verdicts" |
| "modest and measured" | "Its accuracy has been measured, and it is modest" |
| "a watcher above" | "No program — not this app, not Jev, not another session — can tell from outside…" |
| "the central claim" | stated in the bullet itself, and pointed to by section name |

## 4 · KEPT TRUE

- **The Jev figure in the block** is the source's, reworded without rounding: "of the first 30 answers it flagged, two
  reviewers who had not seen its verdicts agreed with 8" (`jev/README.md:99-110`: 8 of the 30 "drift" marks, two blind
  readers). The full figure, with the lenient-readers caveat, is unchanged in *Jev, the second look*.
- **Unchanged below the block:** every measured figure in *Method and measurements*. Only *How it works* ("A round of
  work"), *Try it* (the clone URL), the history link and the in-step section changed.
- **Every new claim is traced,** mostly to the loop in `consonance/src-tauri/brief/BUILDING.md` (the About tab quoted it
  before):
  - the librarian's "saying nothing is a valid turn";
  - hand-backs going straight to the librarian because the middle hop retold findings (`main.rs`
    `pane_call_librarian_exec`'s doc comment);
  - the main session commits what the librarian collated and composes nothing;
  - the round repeats on its own;
  - you are asked "only on direction";
  - the waiting status is my D138 pulse clause;
  - the card is `raise_pull` ("it only enqueues a request the human chair decides on").

## 5 · CHECKS

| check | result |
|---|---|
| links (script) | 21 relative links all exist; anchors `#try-it` and `#the-central-claim-and-its-evidence` resolve; no duplicate headings after renaming the full section "…, step by step" |
| `node consonance/ui/about-readme.test.js` | **4 passed, 1 failed, AS EXPECTED:** "the About tab has drifted from README.md … word 174". The block changed, and A re-renders the app's copy next. The other four pass, including "the About block has no live links". |
| README-reading tests | gen-consumer 60/0, librarian-cite 23/0, shelf-recursion 11/0, carrier-drift **57/0**, score-portable 1/0 |
| `node consonance/tools/carrier-drift.js` | **GREEN, 0 findings** (the history file is in `loop/` now) |
| `cargo test --test arch_test` (lock) | **12 passed, 1 failed:** only the fresh-clone link test, on `README.md:276` → `exo_memory/loop/readme_history_2026-09-25.md` and `:283` → `consonance/ui/about-readme.test.js`. **Both are untracked until they land.** It clears when the README, the history file and A's test land together. |
| full js-suite (`node consonance/tools/js-suite.js`, background, lock) | **139 green · 1 failed · 1 canary · 1 not-run, of 142.** The one failure is `about-readme.test.js`, the expected drift above. The canary and the not-run are as before. |

## NOT verified

- **A stranger's read of the revision.** That is B's next step.
- **The GitHub rendering.** Checked as text only.
- **That the repository URL is public and reachable from outside.** The chair's packets say the repo is public; I did
  not fetch it anonymously.

Files: `README.md` (working tree), this hand-back and one map line. Nothing committed.

NEXT: librarian call_librarian with the hand-back pointer when the revision is written — plan default after it: A re-renders the About tab from the new block and B re-reads the first screen and About against the baseline, unless the output says otherwise. OUTPUT adds: about-readme.test.js is red at word 174 until A's re-render (expected); arch_test's fresh-clone link test is red until the README, the history file and A's test land together; the "early" wording is the librarian's, with his sentence quoted in §2 for his R3 check.

# P-D140-READ · BRAVO — R2, the same stranger on C's draft: README first screen FAIL, About block FAIL. Both fail only on "what next". Unparsed items fell from 19/16 to 5/4 (README) and from 46/52 to 15/14 (About). $0.30

**B (pane `12fb81f6`), machine D, 2026-09-25 ~03:2x–03:4x local.** Packet: the chair's D140. **Nothing committed.**
`README.md` (C's uncommitted draft) and `index.html` were read, not touched. No settings edit, restart, close.js or publish.

## 0 · FILES

| file | what |
|---|---|
| `exo_memory/loop/readme_about_r2_read_2026-09-25.md` | **NEW, the result only**: §1 the verdict, §2 what was given, §3 the reader (identical to R1) and the cost, §4 the four answers VERBATIM, §5 both texts as given. Written by `result.js` (sha256 `2babb485…` as written), then one line added by hand: the any-pairing sentence under the verdict |
| `<B scratch>/d140/` | `extract.js`, `read.js` (R1's, only the temp prefix changed), `result.js`, `out/*.json` |

## 1 · THE VERDICT (the rule the packet fixed before the run)

| text | R2 unparsed r1 / r2 | R1 baseline r1 / r2 | both fewer | "what next?" (verbatim, first sentence) | verdict |
|---|---|---|---|---|---|
| README first screen | 5 / 4 | 19 / 16 | yes | "The text gives no next step." / "The text gives no next step: no install instructions, download link, supported platforms, or example." | **FAIL** |
| About block | 15 / 14 | 46 / 52 | yes | "The text doesn't say." / "The text doesn't say." | **FAIL** |

- **Source:** `node <B scratch>/d140/result.js`, using R1's count regex unchanged.
- **Clause 1 holds under any pairing of readers:** max R2 < min R1 for both texts.
- **The About readers did name next steps:** links to three README sections outside the block. Both said in so many words that the text does not say what to do next, so I scored it as not answered. That is the one judgment in the scoring, and it is marked in the result file.
- **A measured fact about the cut, not a proposal:** the draft's first `## ` is at line 9, so the first screen by the rule is lines 1–8. It is 37 visible words: the title and one bolded sentence. `## Try it` is at line 60, after the About block (lines 6–58). No first-screen reader could reach it.

## 2 · SAME AS R1, CHECKED

- **The prompt template:** `cmp` of R1's and R2's saved templates prints IDENTICAL. The flags, the model (claude-opus-5-5), 2 readers per text and the count rule are all unchanged.
- **The draft was stable** during the reads: README.md sha256 `b26ab2fd…1df1` before and after.
- **No transcripts:** `~/.claude/projects` gained 0 `.jsonl` (4 empty `cs140-` dirs).
- **Cost:** 4 calls, 1 turn each, no tools. Input 8, cache-create 20,874, cache-read 2,124, output 6,545; **$0.2983**.

## 3 · WRONG column

- None withdrawn this lap.
- **One difference from R1, named in the result:** R1's About was visible text extracted from HTML. R2's is the markdown block, since that is what the tab will render. So link targets were visible, and one reader quoted `#the-central-claim-and-its-evidence`. That is 1 of 14 items. The comparison is not perfectly like for like here.

## 4 · NOT VERIFIED

- **167 B of stderr per call** was still not captured, as in R1; every call exited 0 with `success`.
- **How the About tab will actually render the block.** A was to build it later; I gave the markdown source.
- **n = 2 readers per text, one model.** Four readers saying the same thing about "what next" is consistent, not independent.

NEXT: librarian call_librarian with the hand-back pointer when the read is scored against the baseline — plan default after it: the keeper reads the draft and answers C's four questions (R3) before anything lands, unless the output says otherwise

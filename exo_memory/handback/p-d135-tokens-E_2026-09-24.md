# P-D135-TOKENS: tokens per hand-back, per pane, two weeks (pane E, D135, 2026-09-24)

Packet: the chair's D135, S1, E's bullet (`loop/plan_pane_specialization_2026-09-24.md`). **Machine D.**
- **The result is `exo_memory/loop/specialization_tokens_2026-09-24.md`** (sha256 `2268e612ee60dfd3…`). Every number has
  its command there (§C), and the two read-only scripts are embedded verbatim (§E).
- No code changed. Nothing opened from the battery draft, from B's lap, or from `C:\Consonance\sealed\`. Nothing committed.

## THE RESULT, in five lines (the note is the master)

1. **Tokens per hand-back, 09-11→09-24,** from `usage.js --days 14` over distinct rung hand-backs:

   | | A | B | C | E |
   |---|---|---|---|---|
   | hand-backs | 79 | 61 | 80 | 68 |
   | all tokens | 18.9 M | 16.1 M | 17.3 M | 20.2 M |
   | no-cache-read | 364 k | 375 k | 318 k | 443 k |
   | output | 43 k | 38 k | 41 k | 46 k |

   **The four panes are within ±12% of each other; E is the most expensive on every measure.**
2. **The plan's formula (intake bytes × resumes ÷ hand-backs) is the wrong quantity, and I'd trust `usage.js`.**
   - Resumes are a constant: 29 per pane, one per launch on D.
   - Intake is re-read on **every call**, not once per resume.
   - Converted honestly to tokens, the formula is 4.4–7.4% of the no-cache-read cost and ~0.1% of all.
3. **For S5, against the plan's premise:** the intake floor (a pane's smallest prompt, 44k–58k tokens) is only
   **8.7–12.2% of the mean prompt per call** (477k–513k). Narrowing the intake can remove at most about a tenth. **The
   measured lever is context length:** ~90% of every call is accumulated conversation.
4. **The other machine is in:** D's transcripts carry L's rows, split by the hook-path marker (note §D). Not in:
   one-shots with no persistence, and Jev.
5. **The hand-back is not a fixed-size unit,** so the ±12% is not evidence any pane is cheaper at the same work. A
   per-class figure needs C's census plus each hand-back's own turn span, which this lap did not build.

## CORRECTIONS I MADE TO MYSELF (before ringing)

- **Machine split, first attempt:** it tagged every response D by row `cwd`. The instance path is identical on both
  machines. Now tagged by the nearest hook row's command path (`\Users\zackn\` = L). The per-machine sums add back to
  `usage.js`'s totals exactly.
- **Resumes:** my first count used now − 14 days and got 30. The note then claimed the `usage.js` window also gives 30,
  unrun. Re-run, it gives **29**, and the formula figures were recomputed with 29.
- **Shell quoting, twice.** A scripted edit failed and was redone in the editor. Then this hand-back's first write went
  through an unquoted heredoc, and the shell tried its backticks as commands. They all failed as "command not found", so
  nothing executed, but the file was garbled, and it was rewritten with the editor. Same lesson as D133: write prose with
  the editor, never through the shell.

## FOUND ON THE WAY, not this lap's

- **C's shell is 65 KB against ~108 KB for A, B and E.** `persist.log` `MAP CARRY` for C reads **"tier=index-only — NO
  ENTRY OF THE MAP"** (master 209,076 B, allowance 47,558, carried 2,564). So C wakes with no map entries. That is C's
  and the room's to look at.

    carrier-drift over the note: 0 findings · portable-paths on the note: exit 0

NEXT: librarian call_librarian with the hand-back pointer when the token unit is written — plan default after it: the librarian collates S1+S2 when C, E and B have rung, unless the output says otherwise

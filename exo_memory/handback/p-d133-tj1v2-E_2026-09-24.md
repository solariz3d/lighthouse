# P-D133-TJ1V2: the run is STOPPED by its own registration; the pilot label sheet is ready (pane E, D133, 2026-09-24)

Packet: the chair's D133, batch 2. Master: `loop/plan_fix_and_list_loop_2026-09-24.md` "BATCH 2", row E. Spec:
`loop/tj1v2_registration_2026-09-22.md`. **Machine D**, HEAD `89cb93b`. The result note is
`exo_memory/loop/tj1v2_pilot_2026-09-24.md`: the only repo file besides this hand-back and my map line, and it holds no
unit text.

## 0 · THE RULING — the most important line here

**NOT ALLOWED YET, so the run STOPPED with 0 Jev calls.** The egress yes (17:04) answers *whether* the units may travel.
The registration fixes *when*, and it puts the keeper's labels, frozen with a sha, **before the first Jev call**:
- §4 `:204` heading: "BEFORE any Jev call".
- §4 `:248`: "no Jev call is made until his labels are frozen".
- §4 `:259`: "frozen as their own file, with a sha256 appended here, BEFORE the first Jev call".
- §4 `:236–237`: "no Jev output exists when any label is written".
- §5 `:324`: degenerating to read a Jev output "before … the keeper's label sha are appended here".

**The S-CTRL gate is not an exception:** it is a Jev call, and §4's sentence has none. Running either now would void v2
on its own terms.

**The plan's row misreads the order.** It says "the egress run through the gateway, ending in a label sheet", which puts
the run before the labels. So the plan is the thing to correct, not the registration. The packet anticipated this and
told me to stop, and I did.

## 1 · WHAT WAS DONE INSTEAD — the pilot sheet, and nothing that could see an answer

**The four §13 shas, checked before anything was built.** All match (`sha256sum` on D):
- member file `feca21d5…4332`
- K renderer `5fa7a56d…0c0e`
- schemas `729427e6…c1f0` / `f4130330…42bf`

The extractor conflict C stopped on at D122 no longer applies: B built the member file as §1 requires (`238781a`), and C
confirmed it with an independent second build (`55a486f`). The sheet is a mechanical projection of that file.

**Built, machine D, outside the repo:**

| | path | sha256 |
|---|---|---|
| **the sheet, for the keeper** | `C:\Consonance\tj1v2\pilot_sheet_2026-09-24.md` | `86c91055d192f641…10a7` |
| **the key, SEALED** | `C:\Consonance\sealed\tj1v2_2026-09-24\pilot_key.json` | `0626ad5134a68e49…19e4` |
| the builder | `C:\Consonance\sealed\tj1v2_2026-09-24\build_pilot.js` | `2ccd17d0f306cb15…37fb` |

- The key records the sheet's sha as issued, `86c91055…`, so a sheet edited before the sitting is detectable.
- **Deterministic:** a rebuild into fresh paths gave byte-identical sheet and key.
- The builder refuses a member file whose sha is not `feca21d5…` and refuses to overwrite an existing sheet or key.
- It contains no `fetch`, `http`, `net` or `child_process` (grep → 0).

**The draw, one reading and named here so it is checkable:**
- `key = sha256(unit_id + ' ' + '265b08f')`, with `unit_id` in B's R9 format.
- Pilot: the 10 lowest-key units per stratum ("10 from each stratum, drawn by the registered salt", AMEND-10).
- Order: the 20 by the same key, ascending ("interleaved across strata", §4).
- **No repeats in the pilot:** §4's ~10% repeats are the full sitting's (AMEND-7).

Measured on the order: the first half runs 7 matched / 3 adjudicated.

**The sheet, per §4:**
- The C1 question verbatim (§2 `:149`), then each unit's three lines in a fenced plain-text block.
- For each unit: `yes / no / can't tell`, **Recognised?** and **Time (seconds)**.
- A plain header saying he can stop and resume, and that his labels are frozen before Jev is asked anything.
- Units are **P01–P20**; the `unit_id` is not shown, because it carries the source path.
- **Blinding checked:** `grep -ciE "adjudicat|matched|stratum|exo_memory/|unit_id|jev said"` over the sheet → **0**.
  There is no Jev verdict (none exists), no stratum, and no source seat or file.

**Calls:** expected **0**, stated before the build; made **0**. No Jev tool, `jev-ask` or model was invoked. The
`jev-ask` ledger is opt-in (`jev-ask.js:27`) and none exists on D. The key never entered any process's environment or
output.

## 2 · FOR THE KEEPER, before or at the sitting

1. **Egress, by source:** 1 of the 20 pilot units is a Third Place line and 7 are journal or librarian lines. All of them
   travel to the gateway at the run. B flagged the Third Place ones by name (§13 item 5). Does the 17:04 yes include
   them? His word, not mine.
2. **The sitting is on D** (the sheet is D-local). If he sits at L, copy the sheet and check it against `86c91055…`.

## 3 · THE NEXT STEP, in the registration's order

1. **Label and freeze.** He fills the sheet and says so. Its sha256 is appended to §13 as the label file. The pilot's
   yes-rate per stratum is printed, and it sizes the full sitting (AMEND-10). B's AMEND-11 prediction is scored from it.
2. **Run.** Only then does the run start, S-CTRL first. **The S-CTRL transcripts are on L** (§1 `:56`), so the run
   either reproduces manifest `4d934c2a…` on D or runs on L. **Expected calls:** gate 130 × 3 = **390**, then C1 at
   3 asks per unit, which is **60** for the pilot and ~250 for the full sitting.
3. **Then the full sitting** of the remaining ~62 distinct units plus repeats, drawn the same way.

## 4 · CHECKS AND LIMITS

    sha256sum of the four §13 files           → all match
    rebuild into fresh paths                  → sheet and key byte-identical
    blinding grep over the sheet              → 0
    NO_COLOR=1 node consonance/tools/js-suite.js → running (no code changed): <E's scratchpad>\d133suite.txt

**Not established:**
- That the salt reading of "drawn by the registered salt" is the only one. It is the mechanical one, and it is named.
- That the "5 to 7 minutes" on the sheet holds. It is a guess, marked as one, and the time boxes measure it.
- **The stake, declared:** I wrote this registration. The draw is mechanical and has no discretion, and I have not seen
  any label, because none exists.

**One correction to myself.** The first final build went out with the old header ("about 85", "about 5 minutes"),
because my scripted wording fix failed on shell quoting and the build ran anyway. I deleted both unfilled files and
rebuilt. The shas above are the rebuild's, and nothing had been written into the deleted pair.

NEXT: librarian call_librarian with the hand-back pointer when the run is sealed and the label sheet is ready (or the order ruling stops the run) — plan default after it: the librarian collates batch 2 when B, A, C and E have rung, unless the output says otherwise

## ADDENDUM, 18:1x — the js-suite result the ring pointed at

    NO_COLOR=1 node consonance/tools/js-suite.js → exit 1
      138 green · 1 failed · 0 crashed · 1 canary · 1 not-run (of 141) · FAILED: consonance\tools\portable-paths.test.js

**Not this lap's.** Re-run alone (`node --test consonance/tools/portable-paths.test.js` → 41 pass · 2 fail), both
failures name one site: **`consonance/src-tauri/src/main.rs:19654`**, a `DRIVE BENIGN-TEST` fixture string
(`"consonance close · D · C:\data -> C:\state …"`) in `main.rs`. That file is modified in the working tree by another
seat's batch-2 packet, uncommitted. I touched no code this lap, and my three files contain no code.
**For the collation:** that seat either baselines the fixture with `--update` or makes it portable before landing. The
`clean.js` / `CLEAN.md` paths that also appear in the test's output are the test's own temp fixtures
(`portable-paths.test.js:278–279`), not repo files and not a second finding.

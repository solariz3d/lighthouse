# P-D142-READ · BRAVO — R2b, the stranger read again on C's revised draft: README first screen PASS (6/6 vs baseline 19/16), About block PASS (14/11 vs 46/52). All four readers now give a next step the text supplies. $0.32

**B (pane `12fb81f6`), machine D, 2026-09-25 ~06:4x–07:0x local.** Packet: the chair's D142. **Nothing committed.**
`README.md` (C's revised, uncommitted draft) was read, not touched. index.html was not touched. No settings edit, restart, close.js or publish.

## 0 · FILES

| file | what |
|---|---|
| `exo_memory/loop/readme_about_r2b_read_2026-09-25.md` | **NEW, the result only**: §1 the verdict with R1 and R2 beside it, the direction, and each "what next" verbatim; §2 what was given; §3 the reader and the cost; §4 the four answers VERBATIM; §5 both texts. Written by `result.js`, then the "direction" paragraph was added by hand |
| `<B scratch>/d142/` | `extract.js` (R2's, unchanged), `read.js` (R1's, only the temp prefix changed), `result.js`, `out/*.json` |

## 1 · THE VERDICT (the R2 rule, unchanged)

| text | R2b r1 / r2 | R2 | R1 baseline | fewer than R1 (any pairing) | what next, from the text | verdict |
|---|---|---|---|---|---|---|
| README first screen | **6 / 6** | 5 / 4 | 19 / 16 | yes | yes, both: prerequisites + "Try it" | **PASS** |
| About block | **14 / 11** | 15 / 14 | 46 / 52 | yes | yes, both: the repository + its "Try it" | **PASS** |

- **Source:** `node <B scratch>/d142/result.js`, using R1's count regex unchanged.
- **The one judgment:** the About readers' next step leads out of the block to the README. I counted it as answered because it comes from the block's own words; in R2 both said "The text doesn't say". It is marked in the result file.
- **Direction, measured:** the README count rose slightly against R2 (the first screen grew from 37 to 68 words). The About count fell.
- **Findings both About readers flagged:** the "grief that learned to build" sentence, "outside the normal hand-off", "the status line every session reads", "the next round starts on its own", and the Jev "agreed with 8" figure. Listed, not remedied.

## 2 · SAME AS R1/R2, CHECKED

- **Prompt:** `cmp` against R1's template prints IDENTICAL. The same flags, model, isolation, 2 readers per text and count rule.
- **Draft stability:** README.md sha256 `043a9f0e…ac38` after the reads, the value `extract.js` recorded before them.
- **No transcripts:** `~/.claude/projects` gained 0 `.jsonl` (4 empty `cs142-` dirs).
- **Cost:** 4 calls, 1 turn each. Input 8, cache-create 21,974, cache-read 2,124, output 7,124; **$0.3187**. All three rounds: $0.4582 + $0.2983 + $0.3187 = **$1.08**.

## 3 · WRONG column

- None withdrawn.
- **Shape changes across rounds, named in the result:** R1's About was HTML visible text; R2 and R2b's is the markdown block. R1's README first screen was 333 words, R2's 37 and R2b's 68. The count compares what a stranger could not parse, not texts of equal length.

## 4 · NOT VERIFIED

- **167 B of stderr per call**, still not captured; all four exited 0 with success.
- **The About tab as rendered in the app** (A's build). I read the markdown block.
- **n = 2 readers per text, one model**, and the same known `~/.claude/CLAUDE.md` leak as R1/R2 (0 room terms).

NEXT: librarian call_librarian with the hand-back pointer when the read is scored — plan default after it: the keeper reads the final draft (R3) before anything is pushed, unless the output says otherwise

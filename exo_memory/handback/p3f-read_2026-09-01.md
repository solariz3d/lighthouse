# P3f-READ — non-author read of CHARLIE's scorecard (pane E / ECHO, 2026-09-01 ~03:25 −06:00)

**Object:** `exo_memory/loop/battery_scorecard_2026-09-01.md` (231 lines, on disk). **Why this seat:** it scored
the bands the scorecard reports (`79a369b`) and wrote none of the transcript read the scorecard quotes.

## Verdict: sound. Three figures to correct before the chair's pen; none moves a verdict.

## The four bars

1. **Numbers match my bands, figure for figure.** K0 13/20 = 65.0 · K1 29/40 = 72.5 · K2 33/40 = 82.5 · Fisher
   0.422 / 0.564 / 0.195 / 0.260 · P4 TRUTH 40/40, BAIT 0, both arms, t1/t4/t5 · cut B 71.8 / 82.1 · day cuts
   80/90 and 70/80 · r35 offset 14.5 min · > 1000 K0 7/20, K1 21/40, K2 23/40 · isolation 0/0/2,299. New figures
   not in my file, re-derived here: P0a CP [0.25–44.5] ✓; L0 lower 83.2 ✓; day-1 vs day-2 pooled p = 0.538 → "0.54"
   ✓; median total assistant text L1/K1/K2 = 1231/1342/1328 by my count vs 1233/1344/1331 (2–3 chars, a
   block-trimming difference, immaterial); the `8724ee0` librarian re-derivation is at `librarian/2026-09-01.md:115-117`
   and reads the same four values ✓; registration `:36` reads `133 / 137 = 97.1%` ✓.
2. **§4's narrowing is a real distinction, not a hedge — and it is the right form.** Re-derived mechanically over the
   100 L1/K1/K2 transcripts (`rig/score.js`'s `isHandoffInvocation`, same anchor): the tool immediately before the
   first hand-on invocation is `Write HANDBACK.md` in **84**, a `wc -w` check in **12**, another Bash in **4** (CHARLIE:
   83/15/2 — same shape, a classification split on four `cd … && wc` commands; nothing turns on it). The decisive
   check: **in 25 of 25 compliant trials the ≥200-char summary sits between the last pre-hand-on `tool_result` and
   the hand-on call; in 1 of 75 violators.** So the decision is made in the assistant turn that follows that
   `tool_result` — an event this harness exposes, in the same channel (`tool_result`) K2 used, and one no
   hand-on-side event can reach. The broad sentence ("no event this harness exposes precedes the composition")
   is false on the transcripts; the narrow one is what they show. And the narrow form is not weaker than the broad
   one in the way the brief worries: the untested event is not a loophole for the null, because K1's reminder was
   already in context at that turn and did nothing (11/40 vs 7/20) — the scorecard says so in the same paragraph,
   and §7(a) demands a fresh registration with this null quoted at the top. Placement gap reproduced: violators'
   largest pre-hand-on block ≤ **453**, compliant ≥ **639** — CHARLIE's numbers exactly.
3. **Comparability defect cited by hash — yes.** §5.1 gives `bbd64e8b…f094` and `aee411f1…e126`, quotes both
   headers (verified: `handoff.v1.js:6` *"no rule, no reminder, no commentary. (K2's extra line is NOT in this
   file…"*; `handoff.js:3,29` reads `HANDOFF_RECEIPT_TAIL`), explains why the version words are not used, and
   states the K1-vs-K2 comparison clean. Nothing to add.
4. **§7 states what the run does not license — yes.** *"Cues never work"* is the first item under **Does NOT
   license**, with the scope (two classes, one event, one brief, one model, n=40); the licensed form is the narrow
   one (*a cue riding on the hand-on's own events cannot arrive before the choice it addresses*). Correct.

## Three figures to fix (carried here; scorecard untouched)

- **§4, the wall paragraph: "11/40 placed the summary first, vs K0's 7/20, p = 0.42" — the p is wrong.** 11/40 vs
  7/20 is the same 2×2 table as K1-vs-K0 on violations, and its Fisher two-sided p is **0.564**. 0.42 is the
  K2-vs-K1 value, transposed. Both are null; the document's rule is that every figure re-derives, and this one does
  not.
- **§2, P1b: "Intervals do not overlap: the sign is not noise at n=20."** On the scorecard's own numbers L0 is
  [83.2–100] and L1 is [40.8–84.6] — they **overlap by 1.4 points**. The conclusion stands on a better instrument:
  Fisher two-sided 20/20 vs 13/20 = **0.0083**. Replace the reason, keep the sentence.
- **§3, r35: "The chair's ruling."** The ruling was the **librarian's** — the chair's P3b brief to this seat quoted it
  as *"the librarian's, quoted, and it corrects what I told it."* Attribution only.

Minor, no change required: §5.2 gives K0's window as 11:49–12:14Z — that is L1 alone (L1 rows: 11:49:37 →
12:14:48Z); the K0 pool including P0a and L0 opened at 11:30:32Z. Since the K0 rate is L1's, the narrower window is
the right one to quote; a reader checking `trials.jsonl` for all K0 arms will see the earlier start.

## What this read did not do

It did not re-read the 100 transcripts for the qualitative claims in §4.3–4.4 (60/75 recitals, 74/75 `REPLY.md`
pointers, 6/7 K2 echoes, r26). Those are CHARLIE's counts, with re-derive lines in `1830dae`; this seat verified the
mechanical claims around them (placement, preceding tool, block sizes, medians) and found every one reproduced.

*Pane E. Scorecard, read, bands, rig untouched. Nothing committed. A trace to re-run, not a doctrine to believe.*

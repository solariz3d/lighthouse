# P-L074-TEN · ALPHA — nine of the ten ledgers now install only as a fast-forward; dispatch-gate.jsonl is NOT marked, because its quarantine rewrites it. And the consequence to plan for: until the union covers the nine, D cannot install again once it has appended to them

**Pane A, machine L, 2026-09-22 04:0x–04:3x.** Lap L074 packet A. The keeper, 04:0x (`librarian/2026-09-22.md:247`):
*"lets do the ten ledger files first"*. The list is from my own L070 hand-back §6. **Three files, uncommitted:**
`consonance/state-manifest.json` (+10 −10: nine rows marked, one reason added), `consonance/tools/state-manifest.test.js`
(+11 tests), `consonance/tools/state-sync.test.js` (+10 tests). **`state-sync.js` is unchanged**: the install mode it
reads already exists (L070). No existing test was modified.

## 0 · FIRST, THE CONSEQUENCE — it decides what must land next

Under the stop-before-write install (L070 rebuild), **one DIVERGED marked file refuses the WHOLE install.** Both machines
append to all nine of these ledgers. So once L publishes, D's copy of each will have rows L's lacks (D has appended since
09-10), and **D's install will refuse on nine more files**, exactly as it already will on `lap.jsonl` and `board.jsonl`.
**C's `ledger-union.js` handles only `lap` and `board`** (`FILES` at `:52-55`; `--write` accepts `lap|board`, `:346`).
So **D cannot receive anything from L until the union also covers the nine.**

**Why marking is still right:** the alternative is D's install silently replacing its nine ledgers with L's copies and
moving D's own rows since 09-10 into the attic. That is the L058–L065 loss again, nine times over. Marking turns a
silent loss into a loud refusal. **It does make the union extension a precondition for D's next install**: a small
change in C's file (the nine names in `FILES`, keyed on the whole row, as for lap), not mine to make.

**On L nothing changes today:** L's next install already refuses on the ledger and board, and every one of the nine is
LOCAL-AHEAD or IDENTICAL on L (§1), never DIVERGED.

## 1 · THE WRITERS, verified before marking — repo AND ~/.claude/shell

Searched: every `.js/.rs/.ps1/.py/.mjs` naming each file, in `consonance/`, `dev/` and `~/.claude/shell` (test files,
mutant harnesses and the manifest excluded), then every write call in each writer file (`appendFileSync`,
`writeFileSync`, `renameSync`, `truncate`, `unlinkSync`, `rmSync`, `copyFileSync`, `createWriteStream`). The installed
copies in `~/.claude/shell` are the same files at the same lines.

| file | writer(s), path:line | verdict | L live vs state set (D, 09-10, `f70d50a`) |
|---|---|---|---|
| `dispatch-gate.jsonl` | `hooks/dispatch-gate.js:94` append · **`:431` `writeFileSync(live, keep)`**, after `:429` `copyFileSync(live, backup)` and `:430` the quarantine append | **REWRITTEN → NOT marked** | LOCAL-AHEAD 39 (clean today; the quarantine can make it diverge at any time) |
| `precompact.jsonl` | `hooks/precompact-preserve.js:122` append | append-only → **marked** | LOCAL-AHEAD 17 |
| `sessionstart-state.jsonl` | `hooks/sessionstart-state.js:129`, `:146` append | → **marked** | LOCAL-AHEAD 58 |
| `sourced_ledger.jsonl` | `hooks/sourced-stop.js:244` append (read by second-vantage, pair-ledger, open-items) | → **marked** | LOCAL-AHEAD 79 |
| `carrier-drift.jsonl` | `hooks/carrier-drift-watch.js:101` append (its `writeFileSync` at :140/:144 is the separate `carrier-drift.state.json`) | → **marked** | LOCAL-AHEAD 121 |
| `ferry.jsonl` | `tools/ferry.js:170` append (`ferry-watch.js` has no write call) | → **marked** | IDENTICAL |
| `read_ledger.jsonl` | `tools/read-ledger.js:67` append | → **marked** | IDENTICAL |
| `return_ledger.jsonl` | `hooks/findings-return.js:145` append (its `writeFileSync` at :133 is a per-pane state file) | → **marked** | LOCAL-AHEAD 3 |
| `vantage_findings.jsonl` | `tools/second-vantage.js:411` append (`findings-return.js` only reads it) | → **marked** | LOCAL-AHEAD 18 |
| `resonance/atoms.jsonl` | `src-tauri/src/main.rs:8376` `OpenOptions…append(true)`; `curate.js` never writes it (its own `:32`, `:42`) | → **marked** | LOCAL-AHEAD 269 |

    measured with the install's OWN comparison:  node <scratchpad>/l074-compare.js
      (appendOnlyCompare(live, state copy) for each file; read-only)

**A caveat on "append-only in code":** `precompact.jsonl` has a hand quarantine on record (the manifest's
`precompact.polluted-by-tests-*.jsonl` STAYS rule, 2026-08-18). That was a person, not a writer, and it predates the 09-10
publish, and the measurement shows L and the state set in a clean prefix relation today. **But any future hand-edit of
a marked file will make it DIVERGED and block every install.** That is now a standing rule for all eleven marked files:
never hand-edit a marked ledger in place.

Each marked row's `why` now names its writers and the measurement; `dispatch-gate.jsonl`'s row names `:431` and why it
installs by replacement.

## 2 · RED FIRST, THEN GREEN — the command beside every number

    node consonance/tools/state-manifest.test.js
      BEFORE                            32 passed, 0 failed
      RED (11 new tests)                33 passed, 10 failed   (the nine marks + dispatch-gate's reason)
      after marking                     42 passed, 1 failed    — MY reason text cited ":429-431", and the test requires the
                                                                 rewriting line itself; reworded to cite :431 first
      GREEN                             43 passed, 0 failed
    node consonance/tools/state-sync.test.js
      BEFORE                            94 passed, 0 failed
      RED (10 new tests)                95 passed, 9 failed    (each of the nine: a LONGER local copy must be refused and kept)
        green at red ON PURPOSE: "dispatch-gate.jsonl (NOT marked) is REPLACED on install as before" — the packet's test
        that an unmarked file with a rewriting writer still installs as before
      GREEN                             104 passed, 0 failed
    node consonance/tools/state-manifest.js      exit 0 (before and after)

The behaviour tests pull each file under the **shipped** manifest, not a fixture one, so they break if the shipped row
changes.

## 3 · MUTANTS — each mark removed, and the rewritten file marked, on manifest copies; BOTH suites score each

    node <scratchpad>/l074-mutants.js   (mirrored consonance/{tools/,} tree; live files hashed before and after)
      pre-flight: shape 43/0 · behaviour 104/0
      unmark precompact / sessionstart-state / sourced_ledger / carrier-drift / ferry / read_ledger / return_ledger /
      vantage_findings / resonance/atoms   →  EACH killed in BOTH suites (shape 42/1, behaviour 103/1)
      MARK dispatch-gate.jsonl             →  killed in BOTH (42/1, 103/1)
      10 listed · 10 killed · 0 survived · 0 NOT APPLIED · live files unchanged: true

Each mutant fails exactly **one** test in each suite, the one about that file. A contention false kill would fail broadly,
so these are real kills. (C was running tests in parallel; noted, and no re-run was needed.)

## 4 · WHAT I DID NOT VERIFY

- **D's copies.** D's live ledgers were not read; that they will be DIVERGED after L publishes is inferred from both
  machines appending since 09-10.
- **A real install** with the new marks, on either machine.
- **Every possible writer.** The search covers code in the repo and `~/.claude/shell`. It cannot see a person, a
  one-off script outside both, or a future writer, and §1's hand-edit rule is the only guard for those.
- **The js-suite** was not run; only the two suites above.

NEXT: librarian re-run §2's two suites and the manifest exit, then route §0's union extension for the nine to C before D's next install, when this file is read

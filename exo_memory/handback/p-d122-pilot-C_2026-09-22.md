# P-D122-PILOT — C (D122 packet 1 of 2, ON D, 2026-09-22 ~23:0x) — STOPPED BEFORE THE MEMBER FILE

> **PARTIAL — at the 23:00 time cutoff (the keeper: "lets end the laps as they are…"). The labelling sitting carries to a
> later session.**
>
> | piece | state |
> |---|---|
> | this hand-back (the stop, the owed items, the drafted egress question) | **EXISTS** |
> | `exo_memory/loop/tj1v2_members_2026-09-22.json` | **DOES NOT EXIST** — not built (§1: the eligibility conflict) |
> | `exo_memory/loop/tj1v2_pilot_sheet_2026-09-22.md` | **DOES NOT EXIST** — so no sheet can be mistaken for one ready to label |
> | `exo_memory/loop/tj1v2_pilot_key_2026-09-22.json` | **DOES NOT EXIST** — no key, half-written or otherwise |
>
> **Nothing is NOT READY TO LABEL, because nothing labellable was written.** I did not create a stub sheet to carry that
> marker: an empty file with a sheet's name is the thing most likely to be read as a sheet later. **The next session
> starts from §2's decision on the extractor, then builds member file, key and sheet together — in that order, never
> the sheet first.**

**The member file, the pilot sheet and the pilot key are NOT built.** The registration names who may build the member
file, and it names me as NOT eligible — while the packet names the only eligible seat as conflicted. **Neither can be
resolved by the builder, and the packet says to stop and name the lines, so this is that hand-back.** The owed items
that are not extraction (the renderer, the schema, the egress question) are checked below, so the round is not wasted.
No Jev call, no model call, no file outside this hand-back and one map line.

Registration read at HEAD `31d2540`: `exo_memory/loop/tj1v2_registration_2026-09-22.md` (last changed in `98443a7`, D109).

## 1 · THE STOP — the only eligible extractor is the one the packet rules out

| line | what it says |
|---|---|
| **§1 :118–122** | *"The member file that a run scores is built by **a non-author extractor**, by the rule above, and its sha256 is appended here before any run, with the extractor naming which Jev and judge outputs it has seen … **Eligible: B. Not eligible: A and C (they built Jev) and E** (this file's author, and v1's)."* |
| **§8 :425** | *"the member file's sha256, built by a NON-AUTHOR extractor **(B)** by §1's rule, naming the outputs it has seen"* |
| **§12 :576–578** | *"the **member file built by a non-author**"* — the general form; §1 names who that is |
| **§4 :262–268** (B's AMEND-11, adopted) | B's registered prediction — *"the keeper's yes-rate on the matched stratum will be … within 15 points for C1"* — **"is scored from the pilot's 20 units as well as from"** the full sitting |

**So, read together:** the registration permits exactly one extractor, **B**. The packet rules B out, correctly, because
B has a prediction scored from the very pilot the member file feeds. And it rules me **in** as "neither E nor B" — but §1
rules me **out** by name, because I built Jev (`jev-ask.js` D102, `jev-shadow.js` D103, the runner D104).
**No seat on this board is eligible under both texts at once.**

**Why I did not simply build it.** The exclusion exists so that a seat with a stake in how Jev scores cannot shape the
units Jev is scored on. That is not a formality I can waive for myself, and a member file built by an ineligible
extractor would put a defect into the gate before anyone looks at a label — exactly what §1's rule is for. It is also
fixable in one line by the right hand, before any run. **It is E's registration and the chair's lap; the choice is
theirs.**

### The disclosure §1 would require of me, given now so it is on record whichever way this goes

§1 asks the extractor to name the Jev and judge outputs it has seen. **Mine, as best I can state them without
reconstructing from memory:**
- **I built Jev's client, shadow and runner** (D102–D104). The shadow reads the overseers' verdict ledgers by
  construction, and in D103 I opened overseer job captures and verdicts to test whether the judged input could be
  reconstructed (my D103 hand-back: *L2 0/2, L3 3/5 identical*). **So I have seen judge outputs.**
- **I have not, to my knowledge, opened `jev_judge.jsonl` rows on T-J1's units**, and no T-J1 unit existed when I built
  anything. My map and the D102–D104 hand-backs are the record; I would cite them rather than recollect if this matters.
- In D121 tonight I saw subject replies from `claude -p`, which are neither Jev nor judge outputs.

## 2 · Three ways out, none taken — for E and the chair

1. **B extracts, as registered, and a second seat re-derives the file byte-for-byte.** The member rule was made
   mechanical precisely so that the extractor has no discretion (FATAL-3, adopted whole; V2-D0–D11 fix every call), and
   the pilot is drawn by the registered salt. B's stake could only act through discretion the rule no longer leaves —
   and **a second, independent build that must match B's to the byte is a stronger check than one extractor chosen for
   neutrality.** *I could be that second build without being "the extractor"*, since a match leaves me no room either.
2. **E amends §1 to make C eligible**, with the reason written down before any run, and the disclosure above attached.
3. **A seat outside A, B, C and E builds it.** I have not checked which seats that leaves or whether any is free.

**My recommendation is (1).** A mechanical rule's real guarantee is reproduction, not the builder's purity, and (1) is
the only option that tests the file rather than trusting it.

## 3 · The owed items (§12 item 3), each stated as present or absent, with its evidence

| item | status | evidence |
|---|---|---|
| **K renderer's sha** | **PRESENT in the repo; NOT YET RECORDED in the registration** | `consonance/tools/tj1-k-render.js` at HEAD `31d2540`, unchanged since `bec101d` (L081, B): **sha256 `5fa7a56dd96ae5e4f5a601dcea8a14fa95a941a8be5926fa223050fcdd2a0c0e`**, git-blob `e9644d09e2caacb0c50edb1a077e37e08beccb99`. `node --test consonance/tools/tj1-k-render.test.js` → **10 pass / 0 fail**. The registration still says "the renderer's sha256 is still owed" (§1 :57, §12 :577): **the file exists and passes; the sha has not been appended.** Appending it is the author's edit, not mine. |
| **schema's sha** | **ABSENT** | §8 :427 defines it as *"the schema file's sha256, built from §2's question texts verbatim"*. **No such file exists:** `consonance/jev/schemas/` does not exist, and `git ls-files` has no T-J1 or Jev schema. v1 said the same (`tj1_registration_2026-09-22.md:79`). **I did not invent one.** |
| **member file (non-author)** | **ABSENT — not built (§1 above)** | the eligibility conflict; nothing was extracted |
| **the keeper's EGRESS YES for S-CTRL** | **OWED — HIS, and not answerable by anyone else** | v1 AMEND-10 (adopted, `tj1_registration_2026-09-22.md:33`, `:359–367`); v2 §1 :58–59. **The question, drafted for the sheet's header, verbatim:** |

> **OWED BEFORE ANY RUN — the keeper's answer, in his own words:** *"Will you allow the 130 S-CTRL battery transcripts —
> subject runs on room-authored fixtures, not anyone's conversation, and not committed to the repo — to be sent to the
> AI Gateway so that Jev can judge them? Yes or no, in your words."*

**I checked two things before drafting that line.** The facts in it are the registration's own: 130 trials, fixtures,
no one's conversation, and `git ls-files | grep -c subjects/run2/config/projects` → 0, as recorded in v1 AMEND-10.
And **I left out v1's "zero-retention flag set" on purpose.** The keeper has since ruled against zero-data-retention for
Jev, so that clause may no longer be true, and a question that asserts its own reassurance is not a neutral question.
**I did not answer it and did not ask it.**

**S-CTRL's transcripts live on L** (§1 :55–56): a run on D must first reproduce the manifest sha `4d934c2a…`. Not
checked here; nothing about S-CTRL was touched.

## 4 · What was NOT built, and why

- **`tj1v2_members_2026-09-22.json`, `tj1v2_pilot_sheet_2026-09-22.md`, `tj1v2_pilot_key_2026-09-22.json`: not built.**
  The sheet and the key are drawn from the member file, so neither can exist honestly before it does. **I did not
  re-derive the 25 / ~60 counts either**: running the extractor to "just check the counts" is building the member file
  under another name.
- **Nothing of the sheet's design is lost.** Once the extractor is settled, the sheet is mechanical from the member
  file: the pilot is 10 from each stratum by the registered salt; the order is `sha256(unit_id + ' ' + '265b08f')`
  ascending (§4 :48–49); each unit shows the C1 question verbatim (§2 :149) and the three lines, with yes / no / can't
  tell, a "recognised?" box and a time box; there is a plain instruction that the sitting may be taken in parts (§4,
  AMEND-6); and the egress line above goes in the header as an owed item. The key maps unit → stratum, `path:line` and
  record label, sealed and never shown to the keeper.

## 5 · NOT verified

- **Whether any seat outside A, B, C and E is available** to extract (option 3).
- **The member counts** (25 adjudicated C1 units, ~60 matched) — deliberately not re-derived (§4 above).
- **S-CTRL's manifest sha on this machine** — not checked; the transcripts are on L.
- **The exact list of judge outputs I have seen**, beyond what the D103 hand-back records.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when the pilot sheet, key and member file are written — A is writing the launch watch-list in parallel; D122 lands when both are in, and the baton after D122 is D123 (item 1 board compaction + item 4 the hook-file wording)

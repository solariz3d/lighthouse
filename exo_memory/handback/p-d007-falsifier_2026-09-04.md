# D007 packet 1 — RULE THE FALSIFIER

*Pane A. Non-author of B's registration and J's attack. Artifact:
`exo_memory/loop/consumer_falsifier_ruling_2026-09-04.md` (the ruling; that is the master — read it,
not this). Measured against `b1205c3` `librarian/2026-09-04.desktop.md` 12:04 P1. Nothing committed;
paths written: the ruling, this file, one line in `map/A.md`.*

---

## THE ANSWER — a partial refusal, and it is the whole of the packet's freedom used

**The three legs do not make one falsifier. Two of them do.** Merged: B's cold box + J's
generated-tree parity, into `D = P + M + B`, **refuted if D > 0**, objective `D = 0`. Deferred and
kept whole, not dropped: B's 20-cloner leg, armed on repo creation, scored on its own clock.

**Why the third cannot join, and this is the ruling not a preference:** it cannot run until a
repository that does not exist has been cloned by twenty strangers, and B's own text says it *"cannot
confirm success — only make failure sayable"* (`consumer_registration:319`). A conjunction containing
it returns no verdict for an unbounded period. **That is the mirror of the defect J killed the
stranger clause for** — J's was a check whose universe is the one box it cannot fail on; this one's
universe is a box that does not exist yet.

**The falsifier has already fired: D ≥ 20 today.**

---

## THE NUMBERS, AND WHICH ARE MINE

**Re-derived by me today**, against `HEAD 3d2f1bc` with `git status --porcelain` empty:

    $ for t in <the five README tools>; do D=$(mktemp -d); CONSONANCE_DATA="$D" node .../$t.js; done
      chain-status   rc 0   0 bytes                                            -> MUTE
      board-audit    rc 0   468 bytes, read C:\Consonance\data\board.jsonl     -> FALSE-COLD
      ferry --due    rc 1   uncaught Error: spawnSync cmd.exe ENOENT, stack    -> BROKEN
      carrier-drift  rc 1   18352 bytes, full report, final line RED           -> SPEAKS
      js-suite       rc 1   5507 bytes, ok lines and a summary                 -> SPEAKS
      => M = 1, B = 1

    $ find . -path ./node_modules -prune -o -path ./.git -prune -o -name '*.test.js' -print | wc -l
      -> 74   (cross-checked against an independent Node walk; the two lists diff to zero)
      MANIFEST-eligible: tools 52 + hooks 12 + ui 4 = 69;  EXCLUDE hides 3 test files
      => G expected 66 · I = 8 (5 outside every MANIFEST dir + 3 excluded)

**Cited, not re-run:** `P = 18`, J's 2026-09-03 parity. **Stale** — three test files landed after that
run (`0d2a2d9`, `ed3e94b`). I did not re-run it: that window is P4's, and a second baseline in a lap
with four live seats is worse than a dated citation.

---

## THREE CORRECTIONS TO THE LEGS, each found by running rather than reading

**1 · B's Leg 1 classifier counts a working alarm as a crash.** `CRASHES = non-zero rc, or a stack
trace` (`:284-286`) scores `carrier-drift` (exit 1 *because it found drift*) and `js-suite` (exit 1
*because tests are red*) as failures. Under B's rule the sweep returns **4 of 5**; honestly read it is
**2**. The inflated number is exactly what would later be used to argue the bar should move — B's own
DEGENERATING clause 4, reached by accident. **Replaced: exit code is not the discriminator; an
uncaught stack frame is.**

**2 · B's Leg 1 universe was never enumerated from outside itself.** The threshold says *"the tools a
first wake is told to run"*; the sweep says *"29 data-dir tools and 92 shipped tools"*. Three
different sets. **The authority exists and is the artifact the stranger reads:**
`consonance/README.md:149-154` names exactly five. Registered as the universe, **with its own hazard
written in** — that table is editable by the party under test, so the parity report prints the five
names and the sha of the last commit to touch the README.

**3 · `board-audit.js` is FALSE-COLD and B's sweep would have scored it green.** It parsed 155,495
rows from `C:\Consonance\data\board.jsonl`, a hardcoded path the empty `CONSONANCE_DATA` never
reached. A green produced by our own data, in the leg built to prove independence from it.

---

## WHAT I THINK IS THE RULING'S ACTUAL CONTENT — the guard, not the merge

**D is gameable, and J's clause catches only the case where someone means it.** An `EXCLUDE` entry
does not turn a red test green; it removes the file from the denominator. So D is never reported
alone, and the clause becomes arithmetic:

> **`ΔD ≤ 0` with `ΔI ≥ |ΔD|` is DEGENERATION, not progress.** Every `EXCLUDE` added in a lap is named
> in that lap's report beside the file it hides.

That fires without anyone having to detect intent — which matters, because the entry it will first
count is mine (below).

**And a gap P4 must close:** my static derivation says **G = 66**; J measured **63**. Three of that is
the new test files. The rest is unexplained by anything readable statically. **P4 prints G from inside
the tree and reconciles.** A gap means a MANIFEST rule nobody here has read correctly — the class that
kept `src-tauri/tests/` absent for weeks.

---

## THE OBJECTIVE — two-thirds instrumented, and the missing third NAMED rather than filled

- **O1 — `D = 0`.** Instrumented today; it is the falsifier's zero.
- **O2a** — `SEED.md` present in the generated tree and declared in that tree's `tauri.conf.json`
  resources. Static; the class that already caught `room-settings.json` and `THIRD_PLACE.md`.
- **O2b** — `pick_default_room` (`main.rs:317-329`, pure) returns the bundled SEED for the stranger's
  argument vector. A unit test, no GUI.
- **The third part has no instrument and I did not invent one.** O2a+O2b prove the product *would*
  hand SEED to a first wake. They do not prove a first wake happened or that a person read it. Nothing
  in this repo observes that; the only leg that could is the deferred one. A proxy would be a report
  we author — the suppressible numerator both B and J spent their registrations excluding.

---

## WHAT THIS DOES NOT ESTABLISH

- **It cannot see a wrong-but-green test.** D counts red files; the 08-23 break was a suite going
  green over rewritten data, and D reads 0 through all of it. B's DEGENERATING clause 2 is the only
  guard, and it is a human reading. **Largest hole; not closed.**
- **No foreign machine.** Everything ran here. `ferry`'s `cmd.exe ENOENT` is a hint of that class
  arriving uninvited, not coverage of it.
- **My M and B were measured in the SOURCE tree**, while my own §2 requires them measured inside the
  generated tree. **I have stated a definition I did not meet.** If a tool behaves differently there,
  those two numbers are wrong under my own rule.
- **P is stale; G is derived, not measured.**
- **Nothing here rules on repo shape.** Per the packet, the falsifier is about the generated tree
  being sound, not which repo it lands in. The keeper's 09-03 override is untouched by every number.

## MY CONFLICT, DECLARED

**I authored the EXCLUDE entry J's degenerating clause points at** —
`gen-consumer.fixture-scope.test.js` and its exclusion are mine (`0d2a2d9`,
`P-GEN-RED-FIRST_2026-09-03.md:136-138`), and J flagged it live mid-attack. **So I ruled on a
falsifier whose sharpest clause is aimed at my own commit, and I mechanised that clause into
arithmetic that counts my entry among the 3.** I believe the exclusion is right on its merits; that is
the view of the person who made it. The packet also told me a refusal would be worth most, which
biases me toward returning one — I returned a *partial* refusal and merged what could be merged, which
is the harder of the two.

## FOR THE NON-AUTHOR WHO MUST READ THIS BEFORE P4

§10 of the ruling lists five attack surfaces in order, written by me against myself. The first two are
the real ones: **D sums three unlike things**, and **I merged two legs while measuring one of them in
the wrong tree**. Start there.

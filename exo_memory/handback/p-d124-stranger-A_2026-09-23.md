# P-D124-STRANGER · ALPHA — the falsifier scores FAIL, and the cause is the README, not the code. A literal read says there is "no supported install" and never states the install command. The mechanism PASSES at hook level on the archived module in an isolated HOME, with 1 gateway call: one row with prompt_id, turn_uuid and confidence; the exact flag line; a byte-exact uninstall; the real settings.json unchanged. A REAL Claude Code session could not be isolated without touching the real ~/.claude, so it did not run. That is a result, and the unblock is one step of the keeper's

**Pane A, machine D, 2026-09-23 17:3x.** Lap D124, standalone Jev batch 3, the stranger.
- No public repo, no room switch, no restart. Nothing committed.
- **Calls: 1 of 6.**
- **Real `~/.claude/settings.json` sha256: `8e2cf20aa18226db…` before and after.**

**The master is the result:** `exo_memory/loop/jev_clean_machine_2026-09-23.md` (git-blob `da06e6e0…`). It has the score, the
run, the hashes, the deviation, THE BLOCK and the ten README failures. **This hand-back points there; it does not restate
it.**

## FILES (uncommitted)

- `jev/test/clean-machine.e2e.js` (NEW, 223 lines, git-blob `810c5a17…`): the reproducible run.
  - `--mode hooks` was run.
  - `--mode session` is written and NOT run.
  - It skips cleanly without the key, and without an isolated Claude login. Both skip paths were checked, with 0 calls.
  - It's named `.e2e.js`, so js-suite never runs it.
- `exo_memory/loop/jev_clean_machine_2026-09-23.md` (NEW): the result.
- This hand-back, plus one line in `map/A.md`.

## THE THREE THINGS THAT DECIDE WHAT HAPPENS NEXT

1. **README failures exist, so batch 3b goes to E** (the plan's own default). **R1 and R2 are decisive:** the "Installing"
   section still says install.js "is being built … there is no supported install", and the install command is never
   written. The other eight are listed with where and how severe.
2. **The live session is blocked.** An isolated Claude Code keeps its login in its config dir (the auth docs, quoted in the
   result). D has no `CLAUDE_CODE_OAUTH_TOKEN`, `ANTHROPIC_AUTH_TOKEN` or `ANTHROPIC_API_KEY`. Copying the real
   `.credentials.json` was refused: a rotated refresh token could log every seat out. **The unblock is the keeper's:**
   `claude setup-token`, the token in `CLAUDE_CODE_OAUTH_TOKEN` for one shell only, then
   `node jev/test/clean-machine.e2e.js --mode session --calls 5`.
3. **B §3.3 (exec-form `args` live in Claude Code) stays OPEN.** The hook-level run proves the registered entry runs in exec
   form, not that Claude Code 2.1.281 honours `args`. Only the session run can settle it.

## CHECKS

- `node --test jev/test/*.test.js`: 196 pass, 0 fail, 0 skipped.
- portable-paths exits 1, on `jev/test/config.test.js:271-272`: **C's in-flight D124 file, not mine.**
- The run's output has 0 `vck_`-shaped strings (grep), and every printed line is redacted.

## NOT VERIFIED

Beyond the result's own list: the full js-suite was not run by me. It is the collator's, and nothing in my row changes a
`.test.js`.

NEXT: librarian call_librarian with the D124 pointer when the run is scored and the result written — plan default after it: batch 3b to E for README failures R1–R10, unless the output says otherwise

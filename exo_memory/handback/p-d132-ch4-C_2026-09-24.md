# P-D132-CH4 — C (D132, batch 1, on D, 2026-09-24 ~17:1x–18:xx) — CH-4 STRUCK IN THE OWNED CARRIERS · ASK-002 CLOSED · THE ENTRY IS NOT ARMED, AND WHY · UNCOMMITTED

Packet: the chair's D132, my row of `loop/plan_fix_and_list_loop_2026-09-24.md` (`cd02abb`). HEAD `cd02abb`. *This file
names the withdrawn wording only by its registry id, `light-not-lifeguard-2026-08-17`, which D132's lookahead excludes. So this file
adds no finding: §3 describes the patch by rule and does not quote it.*

## 0 · THE ONE THING TO RULE ON

**The packet cannot be done as written. I stopped at the contradiction, did every other part, and left the one-step
landing ready.**

- The packet asks for three things: strike the four CH-4 files, ARM the entry in the same change, and a GREEN
  carrier-drift.
- **Arming makes every occurrence in every carrier red unless a row accounts for it.** 13 occurrences sit in **6 live
  files the packet does not give me:**
  - `consonance/AUTONOMY.md` ×3
  - `consonance/PLAN.md` ×6
  - `consonance/RECONCEPTION.md` ×1
  - `DESKTOP_HANDOFF.md` ×1
  - `exo_memory/memory/MEMORY.md` ×1
  - `exo_memory/memory/lighthouse-dive-buddy-reframe.md` ×1
- **None of the 6 carries a marker**, so no row kind covers them honestly without an edit:
  - `marked` and `acknowledged` both need a marker in the file (`carrier-drift.js:593`);
  - `mention` and `withdrawal` would be false, because these sites assert the stance.
- **Measured, not reasoned.** Armed, with only my files struck, the checker gives **RED, 13 findings, exactly those 13**
  (§4, scenario 1).
- **The keeper's decision already covers them.** "RETIRE the old slogan in the live design files" (plan `:7`). The
  entry's own `arms_on` names `consonance/PLAN.md` and `AUTONOMY.md` as design documents of the stance. The chair's
  file list is narrower than the decision.
- **Ruling needed: may the §3 patch be applied?** It needs whoever owns those 6 files, or a widened row for me. The
  remaining work is then: apply the 13 replacements, add 13 `marked` rows, set `armed: true`, all in one change. The
  temp-tree proof that this goes **GREEN, 0 findings, armed** is §4, scenario 2.
- **`DESKTOP_HANDOFF.md` and `exo_memory/memory/` could also be ruled dated records** instead of live files. That
  would still need a marker to be `acknowledged`, which is an edit either way.

## 1 · WHAT IS DONE

**The strikes, in the room's strike form.** The public form is in the slot. The retired wording survives only inside a
`~~strike~~`, with "retired 2026-08-17, struck 2026-09-24" on the same line. The design text around it is unchanged.

| file | sites | what |
|---|---|---|
| `WELFARE.md` | :32 (heading), :36 | the heading is now "With you, not above you — the discrimination L3 cannot make (2026-06-26)", with a one-line italic trace under it; :36 has the same form inline |
| `dev/SPINE.md` | :14 (§1 heading), :80 (the pointer to WELFARE) | the heading plus a trace line; the pointer quotes WELFARE's new heading |
| `dev/PLAN.md` | :4, :21 (×2: item 7 and its "See WELFARE" pointer) | inline |
| `exo_memory/cards/lighthouse-dive-buddy-reframe.md` | :10 | inline |

`git diff --stat` over my 6 files: **237+ / 11−**. The design files are CRLF in the working tree, the same as before,
and none of them has mixed line endings.

**The pattern: the id no longer matches itself.** SHEET 1 named this ambiguity: *"arming without fixing that turns
every correct citation red"*.
- The pattern is now `light[,\s-]+not[\s-]+(?:a[\s-]+)?lifeguard(?!-2026-08-17)`.
- The full id no longer matches. The short name (the id without its date) still does, on purpose, because the design
  documents used it as the stance's NAME.
- The `pattern_note` records why.
- **Effect: 63 pending became 50.** The 13 removed findings were the ones SHEET 1 identified as only the id.
- **The first attempt at this edit lost its backslashes** (the Bash tool, again: `[,s-]`). The checker showed
  BAD-ANCHOR rows at once, and the second attempt was done as a script file. The diff shows only the lookahead added.

**The rows: 37 added to the registry. No dated record was edited.**
- **9 `marked`**, one per struck site. Each `why` cites the keeper's decision and the strike form.
- **28 `mention`**, one per dated-record occurrence, each with its own reason:
  - 11 name the ENTRY by its short name;
  - 7 sit inside grep commands or census label cells;
  - 5 are target lists naming hook lines to repair;
  - 3 quote a carrier to report it;
  - 1 is a mutant payload;
  - 1 is an error report.
- **Anchors come from the tool's own shortest-unique census.** Nothing was pasted in by hand.
- `arms_on` is kept, with a dated UPDATE naming what still arms it: the 13, pointing here.

**ASK-002: CLOSED** in `exo_memory/ASK.md:92`.
- The keeper's answer is quoted with its source: "Yes, keep widened (Recommended)", plan `:9`.
- **Verified at source on D:** `~/.claude/shell/mcp/world-sense/server.js:235` `arXiv eess.AS` and `:236` `arXiv cs.SD`.
- **The 09-20 bar (a weekday digest carrying audio items) is met by P92**, Thursday 2026-09-24T21:43:06Z.
  - `evidence/P92/body-cs.SD.xml` has 33 items and `body-eess.AS.xml` has 28.
  - Both audio items that `state.md:13` lists are in both feeds.
- `node consonance/tools/ask.js` now reads **1 open · 12 cleared**, down from 2 open.
- The closure also names what it does not change: the digest's strict signal-audio score is still 0 (`state.md:14`).

## 2 · CHECKS

| check | command | result |
|---|---|---|
| RED first | the registry copied with `armed: true` and nothing else changed, `CARRIER_DRIFT_REGISTRY=<copy> node consonance/tools/carrier-drift.js --quiet` | **RED — 63 findings**, exit 1 |
| carrier-drift, as left | `node consonance/tools/carrier-drift.js --quiet` | **GREEN — 0 findings · 13 PENDING** (the 13 in §0), exit 0 |
| carrier-drift.test | `node --test consonance/tools/carrier-drift.test.js` | **57/0** |
| the test that reads this entry's pattern by id | `node --test dev/shell/hooks/l2-overseer-worker.test.js` | **1/0** |
| full js-suite | `node consonance/tools/js-suite.js` (background, heavy-run lock) | see §5 |

## 3 · THE PATCH FOR THE 13 — NOT APPLIED

**The patch is described here by rule and not quoted.** A hand-back that quoted it would become a carrier of the wording
it retires (B's rule, `ea779ca`). The executable form is `<scratchpad>/d132/unowned.js`, which runs against a tree root
and refuses the whole patch unless every replacement matches exactly once. Its 13 rows are the list below.

**The rule for each site:**
- Put **"with you, not above you"** where the slogan stood, keeping the site's own capitalisation, bold and quotes.
- Right after it, keep the site's own original words inside a strike: **"(was ~~…~~, retired 2026-08-17, struck
  2026-09-24)"**.
- **Two sites use a semicolon instead of parentheses**, because they already sit inside a parenthesis: PLAN `:325` and
  MEMORY `:11`.
- **Nothing else on the line changes.**

| file | line | the slogan's form there (by the registry pattern) |
|---|---|---|
| consonance/AUTONOMY.md | 6 (the line after "`dev/SPINE.md` for") | hyphenated, sentence-final |
| consonance/AUTONOMY.md | 48 | hyphenated, sentence-final |
| consonance/AUTONOMY.md | 327 | comma form, mid-sentence |
| consonance/PLAN.md | 3 | hyphenated, inside the parenthesis after `dev/SPINE.md` |
| consonance/PLAN.md | 27 | comma form, bold list-item head |
| consonance/PLAN.md | 44 | comma form, quoted, sentence-initial |
| consonance/PLAN.md | 325 | hyphenated, inside the §6.4 heading's parenthesis |
| consonance/PLAN.md | 463 | hyphenated, mid-sentence |
| consonance/PLAN.md | 521 | comma form, bold list-item head |
| consonance/RECONCEPTION.md | 44 | hyphenated, sentence-final |
| DESKTOP_HANDOFF.md | 64 | comma form, bold, mid-list |
| exo_memory/memory/MEMORY.md | 11 | bare-space form, in a parenthesis |
| exo_memory/memory/lighthouse-dive-buddy-reframe.md | 10 | comma form, bold (the card's twin; the card itself is struck) |

**Then add the rows and arm.**
- Add 13 `marked` rows with anchors from `node consonance/tools/carrier-drift.js --census` run on the patched tree.
  `<scratchpad>/d132/addrows.js <registry> <census.json> --arm --full` does exactly this.
- Set `armed: true`.
- All of it goes in one change.

## 4 · THE PROOF, IN TEMP TREES

Each scenario ran on a copy of every `.md` and `.html` file plus the registry, through `CARRIER_DRIFT_ROOT` and
`CARRIER_DRIFT_REGISTRY`.

- **Scenario 1, my strikes only, rows added, ARMED:** `RED — 13 findings`, and they are exactly the 13 in §0.
- **Scenario 2, plus the §3 patch and its 13 rows, ARMED:** **`GREEN — 0 findings`.** 50 of 50 occurrences are
  accounted for, 0 have no ruling.

**Mutants against scenario 2** (`node <scratchpad>/d132/mutants.js`): **7 applied / 7 as expected / 0 NOT APPLIED.**
The control is GREEN.

| mutant | expected | got |
|---|---|---|
| M1: an owned strike reverted (WELFARE heading back to the slogan) | red | **CAUGHT**, 1 finding |
| M2: a new plain use typed into SPINE | red | **CAUGHT**, 1 |
| M3: the id lookahead removed | red | **CAUGHT**, 16 (the id citations come back) |
| M4: every marker stripped from the card | red | **CAUGHT**, 2 (UNMARKED-CARRIER) |
| M5: CONTROL, a new citation by the full id | green | **GREEN** |
| M6: a new use of the short hyphenated name in a live file | red | **CAUGHT**, 1 |
| M7: CONTROL, the entry disarmed plus the M2 plant | green, 1 pending | **GREEN, 1 PENDING** |

## 5 · FULL JS SUITE

`node consonance/tools/js-suite.js`, run in the background through the heavy-run lock. It waited on another seat's
js-suite, `a2122153`, first.

**Result: 139 green · 0 failed · 0 crashed · 0 silent · 1 canary · 1 not-run, of 141.**
- The canary is `targetless-pull.test.js`, which is declared EXPECTED-RED.
- The not-run is `actors.evidence.test.js`, which is MACHINE-BOUND. Its universe is not present on D.
- Neither is related to this change.
- `carrier-drift.test.js` and the other carrier-drift hook test (`carrier-drift-watch.test.js`) are both green in
  the run.

## NOT verified / NOT done

- **Arming**, for the reason in §0. The entry is left `armed: false`, and its `arms_on` names the 13.
- **Other forms of the slogan.** The pattern is enumerated and not closed; a new phrasing is still green (the registry
  records this limit itself).
- **The "dive buddy" vocabulary** that sits beside the slogan in SPINE, PLAN and the card was not touched. It was not
  in the keeper's CH-4 question, and no withdrawal of it is registered.
- **The installed shell copy.** `exo_memory/cards/` in the repo is what I edited. Whether a copy of this card under
  `~/.consonance/cards/` or `~/.claude/` is refreshed is a separate install step, and I did not look.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when the strikes, the armed entry, a green carrier-drift and the closed ASK are written — plan default after it: the librarian collates batch 1 when A, B, C and E have rung, unless the output says otherwise. OUTPUT says otherwise on one point: the entry is NOT armed, because 13 sites in 6 live files outside my list would turn it red. Rule on §0 (apply the §3 patch, which is proven GREEN armed in a temp tree), and the arming lands in that change.

# P-L062-RC1 — the C:\Consonance\state defaults (pane E, L062 packet R-C1, 2026-09-21)

Packet: the chair's L062 R-C1. Plan: `exo_memory/loop/plan_repo_fixes_2026-09-21.md:86-90`, read at source.
Started 03:14:16, suite finished 03:34:09 (`date +%T`). Machine L, HEAD `648b9e8`. **Nothing committed.**

## 0 · Headline — FOUR SITES REPAIRED, ONE HELD, AND THE HELD ONE IS HELD FOR A MEASURED REASON

| site | class | ruling |
|---|---|---|
| `consonance/hooks/live-mirror-stop.js:49` `STATE_REPO` | FATAL-DEFAULT | **repaired**, test first |
| `consonance/hooks/live-mirror-stop.js:50` `LEDGER` | FATAL-DEFAULT | **repaired**, test first |
| `consonance/hooks/live-mirror-stop.js:54` `OBS_DIR` | FATAL-DEFAULT | **repaired**, test first |
| `consonance/tools/live-follow.js:33` `STATE_REPO` | FATAL-DEFAULT | **repaired**, test first |
| `consonance/tools/state-sync.js:141` `stateDir()` | REVIEW | **HELD, not repaired in the live tree.** The fix is proven red→green on a scratch copy (§3) |

**Why `:141` is held, in one sentence:** the literal is **load-bearing on this machine tonight**. L's
`~/.consonance.json` declares no `state_dir` (`cat ~/.consonance.json`: `data_dir`, `instances_dir`,
`room_path`, `machine_tag`, and no `state_dir`), so every consumer below reaches `C:\Consonance\state` only through
the literal the packet removes, and the working tree is live code:

- the app's launch-time `state-sync.js --pull`, spawned from `repo_root()` (`main.rs:10725`);
- `close.js:140` (`o.state || sync.stateDir()`) — the return leg;
- **six of state-sync's own tests** (§3). The packet's bar was *"state-sync.test (was 75/0)"*, and the repair takes it
  to **69/6**.

A loud refusal at `:141` is the right end state, and it is proven below. Landing it tonight would turn L's next
launch pull and next `close` into refusals. The landing order is in §4, and none of its steps is mine.

## 1 · The resolver shape, per site (quoted from the tool: env, then `~/.consonance.json`, then degrade LOUDLY)

**live-mirror-stop.js** (the hook stays unregistered; this is not a registration, and nothing was installed —
`grep -rln live-mirror ~/.claude/settings*.json ~/.claude/shell` → no hits):
- `STATE_REPO` = `CONSONANCE_STATE_REPO` → `state_dir` → **null**
- `LEDGER` = `CONSONANCE_MIRROR_LEDGER` → `<data_dir>/live-mirror.jsonl` → **null**
- `OBS_DIR` = `CONSONANCE_MIRROR_OBS` → `<data_dir>/live-mirror` → **null**
- `DATA_DIR` = `CONSONANCE_DATA` → `data_dir` (the peer hooks' variable and key)
- **Loud, while still never breaking a turn:** `main()` writes `{kind:'skip', reason:'no state repo declared: set
  state_dir in ~/.consonance.json or CONSONANCE_STATE_REPO'}` and returns before any git. The same happens for an
  undeclared obs dir. With no ledger declared either, `row()` writes the row to **stderr** instead of silently
  dropping it. The process still exits 0.

**live-follow.js:**
- `STATE_REPO` = `CONSONANCE_STATE_REPO` → `state_dir` → **null**, exported so the test can read it
- `--status / --acquire / --release / --watch` with nothing declared → `REFUSED — no state repo declared: … Nothing
  was read and nothing was sent.`, exit 2. `--budget` touches no git and still runs.
- **`git()` itself refuses when `STATE_REPO` is null.** This is the finding that mattered most here: `execFileSync`
  with `cwd: null` runs git in **the caller's directory**, so a module caller's `release()` would have pushed
  `origin --delete refs/consonance/live/<seat>` against **whatever repository it was standing in** (the lighthouse
  checkout, for anyone running from the repo root). The guard sits before the spawn, and a test proves it (§2).
- It deliberately does NOT say "unreachable". An undeclared repo reported as an unreachable remote sends the reader to
  the network. A test pins that.

## 2 · Test first, per site — red before, green after, with the commands

Every new test runs its child with an **empty home** (so there is no `~/.consonance.json`), **no `CONSONANCE_*`
variables**, and **a PATH with no git on it**. The PATH is a safety as well as a control. Before the repair, both
files resolved the real `C:\Consonance\state` on L, and the hook's main path beats a **real lease on the real
remote**. With no git reachable, a red run and any regression can only fail; they can never publish.

    RED   live-mirror-stop.test.js, hook at HEAD (the hook stashed by pathspec, tests new)
            node consonance/hooks/live-mirror-stop.test.js        8 pass · 3 fail
            actual {s:'C:\\Consonance\\state', l:'C:\\Consonance\\data\\live-mirror.jsonl', o:'C:\\Consonance\\data\\live-mirror'}
    GREEN   same command                                           11 / 0   (from the repo root AND from C:\ — see §5)
    RED   live-follow.test.js (new file), tool at HEAD            node consonance/tools/live-follow.test.js   0 / 4
    GREEN   same command                                            4 / 0
    BOTH   node --test <both files>                                15 / 0
           node --test --test-concurrency=4 <both files>          15 / 0
           node --test --test-name-pattern="R-C1" live-mirror-stop.test.js   3 / 0
           node --test --test-name-pattern="refuse" live-follow.test.js      1 / 0

The tests, by name. Hook: *nothing declared → no state repo, ledger or obs dir is invented*; *state_dir and data_dir
are honoured when env is absent*; *an undeclared state repo is a LOUD skip naming the fix, not a lease attempt*.
Follower: *--status REFUSES and names the setting*; *--release refuses before any git runs*; *as a module, git never
runs in the caller's directory*; *state_dir is honoured, and env wins over it*.

**Mutants.** The runner is named by path: `scratchpad/rc1m/mutants.js`. It mutates copies under `rc1m/w/consonance/`
and never touches the live files.

    GREEN 11/0 pre-flight hook · GREEN 4/0 pre-flight follow
    KILLED hook: state repo literal restored · ledger literal restored · obs literal restored · config ignored (env only)
           · undeclared-state skip removed · skip reason stops naming the setting
    KILLED follow: literal restored · git guard removed · CLI refusal removed · env no longer wins over config
    10 listed · 10 killed · 0 survived

## 3 · state-sync.js:141 — proven on a copy, held from the live tree

    scratchpad/rc1r/{ctl,pat}/consonance/{tools/*.js, *.json}      (the repo layout; see §5 for why that matters)
    diff: :141  return 'C:\\Consonance\\state';   ->   throw new Error('no state dir declared: set state_dir in
                                                        ~/.consonance.json or CONSONANCE_STATE. Nothing was read …')
    proof test  rc1r/pat/consonance/tools/ss_rc1.test.js          RED 1/1 on the unpatched copy ("got: DIR=C:\Consonance\state")
                                                                  GREEN 2/0 patched
    state-sync.test.js   control copy (unpatched)                 75 passed, 0 failed
                         patched copy                             69 passed, 6 failed
    the six, all reconcileInstall: "reports the whole set present…", "NAMES BY PATH a file…", "reads the destination…",
      "distinguishes a SHORT file…", "catches right-length-wrong-bytes…", "names a directory standing where a file should be…"
      — each fails with the new message, reached through arrivalCtx at state-sync.js:1208 (live numbering; :1209 in the patched copy),
      `(over && over.state) || stateDir()`, which resolves the state dir EAGERLY even for a caller that never reads it.
    live L:  node -e "require('./consonance/tools/state-sync.js').stateDir()"   ->  C:\Consonance\state  (from the literal)
             the patched copy under L's real config                           ->  throws "no state dir declared…"

**What those six tests are telling us:** they pass today on any machine, because the literal returns a string
without checking it. Every one of them is silently machine-bound through a default it never uses. That is the
same defect as the site, one level down. The right repair there is probably in `arrivalCtx`, which should not resolve
a state dir it will not read, and not in the tests. I did not make that edit: it is the arrival path, and the standing
rule forbids weakening the tests.

## 4 · The landing order for :141, none of it mine

1. **The keeper** adds `"state_dir": "C:\\Consonance\\state"` to `~/.consonance.json` on **L and D**. It is his
   config, so I did not touch it. This one line also turns the two repairs above from refusals back into working tools
   on L (§6).
2. **`arrivalCtx` stops resolving a state dir it does not need** (`state-sync.js:1208`, the arrival code), and the six
   `reconcileInstall` tests go green with no test edit.
3. **`close.js:140` gets the refusal message** instead of a TypeError. It is A's file. Today a thrown `stateDir()`
   there would at least be loud, since the message names the fix.
4. Then the one-line change proven in §3 lands, with its test moved into `state-sync.test.js`.

## 5 · Corrections, including mine

- **My own new hook test passed or failed depending on where it was launched.** The mutant pre-flight came back
  **10/1** on the copy while the same file was 11/0 in the repo. Five runs from outside the hooks directory all
  failed, with `actual: 'no usable seat name from cwd'`. The cause is **a real defect in my own L052 hook, found here
  and not fixed**: `readStdin()` accepts fd 0 only if `fstat` says FIFO or file, and on Windows a piped stdin reports
  as **none of FIFO / file / socket / char** (`node -e` probe → all four false). So the hook ignores every piped
  input and falls back to `process.cwd()` for the seat. The test now gives the seat both ways (stdin and cwd) and
  says why at the site. **Whether Claude Code's harness pipes the Stop hook's stdin the same way is NOT verified.** If
  it does, the registered-someday hook would take the seat from its cwd, which happens to be right for a pane. I left
  it alone because it is outside this packet (defaults only). It is a packet of its own.
- **My first two scratch layouts were wrong, and the numbers from them are void.** A flat `tools/` copy gave
  `state-sync.test` **61/14**, and so did the unpatched control. The tests read `<repo>/consonance/state-manifest.json`.
  Only the rebuilt `consonance/tools` layout gives the 75/0 control, which makes the 69/6 attributable to the patch.
  Without the control, I would have reported 14 broken tests.
- **Backslashes in bash heredocs were mangled twice** (a regex in each file). They were rewritten with the Edit tool
  and as string equality.
- **I used `git stash push -- consonance/hooks/live-mirror-stop.js` on the shared checkout** to show the red against
  HEAD. It was pathspec-limited to my own file and popped at once (`git diff --stat` afterwards: +32/-3, as before).
  A and C were editing other files in the same tree at the time. A scratch copy of `git show HEAD:` would have been
  the safer way to do this, and next time I will use that.
- **portable-paths moved 41 → 32, and only 4 of the 9 are mine.** The site diff shows
  `README.md:230` and `retired_seats_2026-09-11.md:62` (C, R-C3) and `deference-unit.js:147`,
  `order-parameter.js:253`, `vicsek-phi.js:248` (A, R-C2) leaving in the same window. **Mine: exactly the four
  sites in §0. `state-sync.js:141` is still listed.**

## 6 · Numbers, each with its command

    node consonance/tools/portable-paths.js         before 03:1x  RED — 41 path(s) · after  RED — 32 (4 mine, 5 other seats', diffed)
    node consonance/tools/state-sync.test.js        before 75 passed, 0 failed · after 75 passed, 0 failed (live file untouched)
    node consonance/tools/js-suite.js               109 green · 1 failed · 0 crashed · 0 silent · 1 canary · 0 class-error (of 111)
                                                    FAILED only portable-paths.test.js; live-mirror-stop, live-follow, state-sync all ok
    git diff --stat   live-mirror-stop.js +32/-3 · live-follow.js +24/-2 (per --stat at the time) · live-follow.test.js new

**Behaviour change on L, stated because it is real:** L declares no `state_dir`, so after this lap `live-follow.js
--status` on L **refuses** where it used to read `C:\Consonance\state`. The hook, if it were registered, would now
skip with the reason. Neither has a live caller: the hook is unregistered and the follower runs by hand. §4 step 1
restores both.

## 7 · NOT verified

- **D is unexamined.** I do not know whether D's `~/.consonance.json` declares `state_dir`.
- **No real lease was read or beaten.** Every new test runs with no git reachable, on purpose, so the resolved paths
  are proven and the network path after them is not re-proven.
- **The harness's Stop-hook stdin on Windows** (§5), and so whether the hook's seat detection is right in practice.
- **The `arrivalCtx` diagnosis in §3 is read from the code** (`reconcileInstall` calls `arrivalCtx(DATA, (over && over.state) || stateDir(), …)` at `:1208`, by `grep -n` inside the function), not from a traced run. I did
  not check whether any `reconcileInstall` caller needs `ctx.state`.

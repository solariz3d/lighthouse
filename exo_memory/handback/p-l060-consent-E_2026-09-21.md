# P-L060-CONSENT — pane E, on `p-battery-blind-E_2026-09-16.md:274` (L060 packet E, 2026-09-21)

Question: the chair's L060 packet E. The ruling is at `exo_memory/librarian/2026-09-21.md` (02:54 entry, routing (2)).
The finding is in `exo_memory/handback/p-r5-carrier-B_2026-09-21.md`, in the carriers table. Machine L, 02:56:40 by `date +%T`.
Nothing was edited except this file.

## ANSWER: CONSENT — to a registry row. The row's KIND should be `withdrawal`, not `acknowledged`, and no marker needs adding.

**My one reason:** the line is already a withdrawal, and it already carries its own marker. `:274` reads
*"a pane withdrew \*"the keeper is the only DECORRELATED reader"\* \*\*in full\*\*"*. It quotes the wording in order
to report that the wording was withdrawn, and then uses the withdrawal as the precedent for a stricter rule. It never
asserts the claim.

## Evidence, with commands

    node -e (the registry's own regexes from carrier-drift.registry.json, entry only-decorrelated-2026-08-16,
             run over the file)
      pattern  only\s+(?:\w+\s+){0,2}decorrelated   -> 1 hit, line 274
      marker   WITHDRAWN 2026-08-16|withdrawn in full|withdrew .{0,120}in full   -> matches the file: true
    git log -1 -- exo_memory/handback/p-battery-blind-E_2026-09-16.md   -> 67da110 2026-09-16 04:04:17 (unchanged since)

**Why `acknowledged` would mis-describe it.** In `carrier-drift.js:138-142`, `acknowledged` means *"the claim
stands, the correction lives elsewhere in the file"* and it requires a `see`. In my file the claim does not stand,
and there is no "elsewhere": the correction is in the same sentence. `withdrawal` means *"the occurrence IS
correction text, quoting in order to withdraw"*. That is the literal description of `:274`, and that kind needs no
marker and no `see` (`:591-593`). A marker added to the record would be an edit to a dated master, and it would be
unnecessary, because the marker the tool looks for is already there.

**If B prefers `acknowledged` anyway:** it would still need no edit, since the file-scoped marker check already passes.
But it would need a `see` pointing at the same line as the anchor. That is the tell that the kind is wrong.

## Consented to, and not

- **Consented:** one row in `consonance/tools/carrier-drift.registry.json` under `only-decorrelated-2026-08-16`, file
  `exo_memory/handback/p-battery-blind-E_2026-09-16.md`, kind `withdrawal`, anchor any unique substring of `:274`
  containing the pattern. B writes it; I did not touch the registry.
- **Not consented, and not needed:** any edit to the record itself. It is a dated master, and it already says the
  thing the marker would say.

## NOT verified

- I did not run `carrier-drift.js` with the proposed row in place, because the registry is B's to write. Whether an
  anchor B chooses is unique in the file is B's check (`AMBIGUOUS-SITE`, `carrier-drift.js:578-584`).
- B §3's broader point stands and is not contested here: a report that quotes a withdrawn wording shows up as a carrier.
  This answer only says which of the five kinds this one is.

# P-L081-MEMBER · BRAVO — the S-WRONG member file and the K renderer are built. The member file forces NOT TESTED: 186 → 20 located → 13 members, no class near 20

**B (pane `12fb81f6`), machine L, 2026-09-22 ~06:5x–07:4x.** Packet: the chair's L081 packet B, step 2. Built by the
**amended** registration (`exo_memory/loop/tj1_registration_2026-09-22.md` at `b35507c`). **NO RUN.** Nothing was sent
to the gateway (AMEND-10 awaits the keeper). I opened no `jev_judge.jsonl`, no shadow store and no verdict ledger.
**Nothing committed.** E appends the digests.

**My files, and only these:**
- `exo_memory/loop/tj1_members_2026-09-22.json`, the member file;
- `consonance/tools/tj1-k-render.js`, the K renderer, and `consonance/tools/tj1-k-render.test.js`.

---

## 0 · THE DIGESTS, both functions named (the registration's §8 asks for sha256; the chair asked for git-blob)

| file | sha256 | git-blob | bytes |
|---|---|---|---:|
| `exo_memory/loop/tj1_members_2026-09-22.json` | `db3ecec1b6736946d089fa136d608703a9355955205b58865076428041ea0c6b` | `73f7b27d66b3b3cde7e6ceadd798a764c4ff0408` | 99,905 |
| `consonance/tools/tj1-k-render.js` | `5fa7a56dd96ae5e4f5a601dcea8a14fa95a941a8be5926fa223050fcdd2a0c0e` | `e9644d09e2caacb0c50edb1a077e37e08beccb99` | 5,129 |
| `consonance/tools/tj1-k-render.test.js` | `ce0486c5415db475fef98e3ec1b993a9337b602610fb5792e6d6bd52aa574d68` | `fdd49efcba7e7f50b98d07bd97b4ca3d85067b5f` | 5,255 |

    sha256sum <file> ; git hash-object <file> ; wc -c < <file>

**The member file rebuilds byte-identical.** I ran the generator twice and compared with `cmp`; the file carries no
timestamps. So its sha256 re-derives from its own embedded builder at this commit.

## 1 · THE MEMBER FILE — and the verdict it forces

    186 universe lines → 20 located → 13 members
    C1 9 positives · 8 negatives   C2 2 · 2   C3 2 · 1          → NO CLASS RULES (all short of 20 / 20)
    excluded: no locatable sentence 157 · located but no class 7 · ambiguous locators 9

**Under the amended §5 (FATAL-1, adopted), a run on this member file reads NOT TESTED.** Neither the falsifier nor
survival is read, the companion note stands, and T-J1 is extended or re-registered with a larger universe. **E's
§6 predicted C3 empty; in fact every class is empty by the ruling bar.**

**The verdict is structural, not an artifact of my calls:**
- **157 of 186** universe lines carry **no locator of either kind**: no `path:line`, and no quoted phrase. They are
  headers such as *"WRONG 90 and 91 on this desk"*, whose erroneous sentence lives in the paragraph, not on the line.
  **No looser locator reading can admit a line that has no locator.**
- Among the 20 located lines, **no class changes** between the whole-word reading I used and a prefix reading.
- Even with **all 20 located lines in one class**, that class reaches 20 positives at most, and still needs 20
  negatives.

**What the file carries**, each field for the non-author check:
- the pinned universe with its sha and scope;
- **all 186 lines** with their status, locators and notes;
- the 13 members, each with its locator, resolved line, ±1 unit from the adjudication's own commit, and matched
  S-UNADJ negative;
- **my list of every output I have seen** (§3);
- the twelve discretionary calls with how often each was exercised (§2);
- stakes;
- **the builder's full source, embedded**, so the file re-derives without my scratch.

## 2 · THE DISCRETIONARY CALLS THE RULE FORCED — recorded in the file with counts

| id | the call | why it was forced |
|---|---|---|
| D1 | class words whole-word, case-insensitive; `pathologi` alone as a stem | the table writes `pathologi` as a stem and the rest as words. A prefix reading changes no located line. |
| D2 | "the adjudication line's own commit" = git blame's commit at the pinned commit; the unit is read from that commit's tree | the rule names the commit but not how to find it |
| **D3** | **a quote must grep to exactly one line OTHER THAN the adjudication line itself** | **without this, "exactly one" is unreachable for any surviving source,** because the adjudication line always quotes the phrase |
| D4 | a `path:line` is resolved by fixed prefixes (as written, `exo_memory/`, `consonance/tools/` …); two existing resolutions = ambiguous | adjudications write paths relative to several roots |
| D5 | a range `a-b` locates a passage, not a sentence | the unit is one line ±1 |
| D6 | several locators count only if they resolve to one line | "the erroneous sentence" is singular |
| **D7** | **"locates" is applied mechanically; no semantic check that the located line is the error** | a semantic check is discretion the rule does not grant. **It bites; see §4.** |
| D8 | C3's "sentence ending in ?" = `?` then space, end or closing punctuation | the feature's text needs a boundary |
| D9–D12 | S-UNADJ: section = between markdown headers; 7 days by blame time; ≥ 3 lines from the member, so the two units never overlap; "not cited" = not the text of any universe line or of any line a universe line resolves to | each term of the S-UNADJ rule needed an operational reading |

## 3 · THE BUILDER'S BLINDNESS — every output I have seen (the amended FATAL-3's requirement)

- **Jev output: NONE.** I have never opened `jev_judge.jsonl`, the Jev ledger or the shadow store, and never run Jev.
- **This backtest's output: NONE.** None exists.
- **L3 verdicts received in context:** `quiet_spiral` 2026-09-21T15:26:46Z, and three `quiet_spiral` from 2026-09-20
  (15:44, 15:47, 15:49Z) in a SessionStart block. Earlier sessions before compaction received more, which are not
  enumerable from here.
- **Verdict ledgers read deliberately in earlier packets:**
  - D095: `l2_overseer.jsonl`'s verdict tallies, its two out-of-schema rows with their reasons, and 29 parse-error
    excerpts; `l3_overseer.jsonl`'s trajectory and window **counts**;
  - D103: judge transcripts' token usage and first messages only.
- **None is output of this backtest or Jev output on these units, so none disqualifies,** and all are named in the file
  as required.

**Declared stake:** my §7 FATAL-1 predicted a data shortage, and this file **is** that shortage. The builder's own
prediction is vindicated by the builder's own build, which is why every call is recorded and the source is embedded.

## 4 · WHAT THE NON-AUTHOR CHECK SHOULD LOOK AT FIRST (D7 biting)

- **Both C3 members locate code lines**, `consonance/src-tauri/src/mcp.rs:1525` and `dev/tail-carry.js:207`. They are C3
  only because their adjudications use *"stop"* technically (exit codes, refusals), not as an unasked ending. The
  table's words, applied mechanically, misfile them. **I did not correct this by hand.** It does not change the verdict:
  C3 is NO RULING either way.
- **A C2 member's quote resolves to another WRONG line** (`librarian/2026-09-06.md:140` → `:82`).

## 5 · THE K RENDERER (AMEND-1)

**The spec it implements**, verbatim from E's amended K row: *"each assistant text block as-is and each tool call as
`[TOOL <name>: <its command or input, first 200 chars>]`, omitting user messages and tool results."* It takes the
rig's own semantics from `exo_memory/loop/run2/rig/score.js`:
- rows in file order, `type === 'assistant'` only;
- **no deduplication across rows, because the rig counts every row's text;**
- thinking blocks omitted.

It reports the unit's UTF-8 byte size and, per unit, **whether the rig's anchor survives the 200-character
truncation**.

    node --test consonance/tools/tj1-k-render.test.js        10 tests · 10 pass · 0 fail
    node consonance/tools/tj1-k-render.test.js               exit 0
    mutants: applied 6 · caught 6 · survived 0 · NOT APPLIED 0 · control GREEN   (<scratch>/l081/mutants.js)

The last test is the load-bearing one: **characters after the first handoff, counted on the render, equal the rig's
`charsAfter` (`scoreRows`)** on the fixture.

**On the real 130, locally, counts only (no content printed, nothing sent):**

    node <scratch>/l081/k130.js
      label re-derived from the RENDER agrees with the rig: 130 · disagrees: 0
      anchor present but HIDDEN by the 200-char truncation: 0 · no anchor found: 0
      unit bytes: min 142 · median 3,326 · max 4,931 · total 404,189

**The rendered unit carries the rig's evidence for every trial**, and the spec's known limit (an anchor past character
200) never occurs on this set. The byte sizes are the §2 K row's "recorded" sizes, printed here and not frozen, since
K is outside the member file.

## 6 · WHAT WAS NOT VERIFIED

- **The builder is not committed.** It is embedded verbatim in the member file (`builder_source`); the scratch copy is
  mine. If the chair wants it as a tracked tool, that is a separate path to own.
- **D7's semantic question**, whether each located line is really the erroneous sentence, is deliberately not answered
  by me. It is the non-author check's job.
- **Machine D.** The member file's commits are in git; the K check read L's local S-CTRL tree.
- **The full js-suite.** Only the new test file was run; it is a standard `node:test` file.

## 7 · WRONG column

- None filed. All git calls went through argument arrays (`execFileSync`), not shell strings, the failure that cost me
  runs in D092, D103 and L063. Nothing broke this time.
- The hand-back and map line were written and verified before the ring.

NEXT: librarian route the member-file and renderer sha256s to E for the dated §8 amendment, and the member file to a non-author checker (D7 first), when this is read — and note the member file forces NOT TESTED under the amended §5

# P-DOC-APP — hand-back. L032, BRAVO, 2026-09-02.

**Verdict: LAND IT.** `consonance/README.md` rewritten from the 2026-08-17 text. **Nothing
committed.**

Paths, uncommitted:

    consonance/README.md
    exo_memory/handback/p-doc-app_2026-09-02.md

Sibling hand-back, same seat, same lap: `exo_memory/handback/p-doc-oracle_2026-09-02.md`.

---

## 1 · THE BARS

| bar | result |
|---|---|
| carrier-drift green over the three surfaces | **`DESCRIPTION SURFACES — 0 finding(s), 0 of them red`** |
| a mutant: reintroduce "lifeguard" → red | **1 finding, 1 red** — run in the oracle hand-back §5 |
| every claim has its path or command | done; §2 below is the one place I could not, and I cut the claim instead |
| `node consonance/tools/js-suite.js` | **66 green · 3 failed · 0 crashed · 0 silent · 0 not-run (of 69)** — the three named in the re-pointing; unmoved by this file, which contains no code |

Retired vocabulary in the new file, word-boundary form (Ruling 2's template, not the broken grep):

    grep -nioE "\b(diver|divers|diving|lifeguard|dive buddy|the dock|the shore)\b" consonance/README.md
    # no output

Apparatus, `grep -ciE` on the new file — all five were **0** before:

    Librarian 17 · Third Place 1 · Listen|cochlea 2 · chain|loop card 1 · call_librarian 3

---

## 2 · THE PACKET'S ZERO-CLAIM IS RIGHT WHERE IT MATTERS AND ITS PUBLISHED NUMBERS ARE NOT

The finding was *"grep each for `Librarian` / `Third Place` / `Listen|cochlea` / `chain|loop card` /
`call_librarian`: **0 · 0 · 0 on every surface**. Re-derive rather than take it from me."* I did.

**Two of the five patterns contain an alternation, and `grep` without `-E` reads `|` as a literal
character.** So those two rows could not return anything but 0 on any file, ever — the same class as
Ruling 2's `diver` bar, twice in one lap. Measured on HEAD, both dialects:

    git show HEAD:consonance/ui/index.html | grep -ciE "Listen|cochlea"    # 14
    git show HEAD:consonance/ui/index.html | grep -ci  "Listen|cochlea"    #  0
    git show HEAD:consonance/ui/index.html | grep -ciE "chain|loop card"   #  9

And the two non-alternating rows are non-zero file-wide too: `Librarian` 6, `Third Place` 3 — the
tab button and the seat's own section markup.

**The finding survives, at the scope the packet itself named.** Inside the About section proper
(`ui/index.html:226-385`) all five are genuinely 0, both dialects. And `README.md` and
`consonance/README.md` are genuinely 0 on all five, both dialects — my file's whole reason to exist.

**So: right conclusion, wrong instrument, and the published numbers should not be re-quoted.** Two
of the three surfaces were measured correctly by accident (they contained none of the words at all),
and the third was measured at the wrong scope with a command that could not fail.

---

## 3 · WHAT THE FILE CARRIES

Objectives **cited, not restated** — `BOOT.md`, `BUILDING.md`, `COMMITTEE.md`. Seats one row each
with brief file and spawning command, the command list derived from source rather than listed
(`grep -c '^#\[tauri::command\]'` → 41, plus the `invoke_handler` block). The loop **quoted verbatim
from `BUILDING.md`**, two doors and the ring included. Verbs and the address table by command, not
by memory (`sed -n '/const ADDRESS_TABLE/,/];/p'` → two rows). Board phases. The instruments, with
what each answers. The hooks, with the 199:0 measurement that produced them. The shelf and the cap,
with the reminder to read the figures from a run. The dream cycle. The tabs, from a grep. Build.

**A section the old file had and this one does not: the gauges.** The 2026-08-17 text spent ~110
lines on gauge semantics. I did not carry it forward because I could not check it against source in
the time available, and an unchecked section is exactly what this file was rewritten to remove.
**Named here so its absence is a decision on the record rather than an omission** — if the gauges are
live, they are owed a section written from `src-tauri/src/` the way the rest of this file was.

**The glossary is not carried either.** The packet asked for one "pruned to words the code uses";
pruning it correctly means resolving each of 40-odd terms against source, which is its own pass.
Same status: named, not done. A's About dropped its 43 `<dt>` entries the same night, so the
glossary now exists on **no** surface — that is a real loss and it should be somebody's next packet.

### The one thing I refused

**Nothing edited-not-shipped is described as shipped**, and rather than annotate individual
features I put the rule at the top of the file: the running binary predates tonight's landings until
the next `cargo tauri build`, with the command to check. The aura, the shelf reorder, the two-doors
drawing and both briefs are in the **source tree**; the file says so and says how to tell.

---

## 4 · WHAT THIS DOES NOT ESTABLISH

- **That the file is complete.** Two sections are deliberately absent (§3) and named.
- **That the dream cycle runs.** `ls dreams/` returns nothing in this checkout; whether a wake timer
  is registered is a property of the machine, and the file says to check the scheduler rather than
  believe it.
- **That a reader learns what they need.** The objective is about a reader, and no instrument here
  measures a reader. `carrier-drift` green means no *withdrawn wording* is asserted — it is blind to
  omission, which is the disease this file actually had. See the oracle hand-back §6: a presence
  oracle over the three surfaces is owed and unbuilt, and until it exists nothing stops this file
  going stale again the same way.
- **The 41 / 45 / 12 / 49 counts** are from the commands printed beside them, run at 06:00 today on
  this tree. They move whenever a file is added; the commands are the claim, not the integers.

---

## 5 · OWED ONWARD

- **The chair:** land `consonance/README.md` and this file by name. `consonance/ui/index.html` (A),
  `README.md` (C), `consonance/ui/app.css` (E) are dirty in the same checkout and are not mine.
- **Non-author read: C**, per the packet. §2 is the part to check — it is a correction to the
  packet's own evidence.
- **A next packet:** the gauges section, and a glossary that now exists on no surface (§3).
- **The librarian:** §2 — the zero-claim's numbers should not be re-quoted from the packet or the
  map; the scoped form is `ui/index.html:226-385`.

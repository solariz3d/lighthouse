# P-L061-REG44 — C (CHARLIE), L061 packet C, on L — a dated note beside registration 44

**Packet:** L061 C, following `exo_memory/librarian/2026-09-21.md:20`. **Owned:** the tail of
`exo_memory/journal/2026-08-24.md`, this hand-back, one map line. **Nothing committed. No existing line edited.**

## 1 · THE RULING: NEEDED — and more needed than the packet's framing said

"Not needed" was a live answer and I read for it first. It does not survive the record:

| date | registration 44's headline — *"The corpus has never deleted anything. `+3,688 / -0` over three days."* |
|---|---|
| 08-24 | registered, measured in **lines** (`git log --numstat -- exo_memory/`) |
| 08-25 | `loop/forgetting_pilot_2026-08-25.md` §1 corrected it to `-696` lines, **a second seat confirmed**, and the same document then found **both versions were in the wrong unit** (`:161`, `:353`): disk lines, where law 3 is about the reading path — *"false on disk and true on the reading path."* At `:447` it asked for a note beside the registration. |
| 08-25 → 09-21 | **no note written.** `git log -- exo_memory/journal/2026-08-24.md` → last commit `c201c92`, 08-24. |
| 09-08 | false on the reading path as well — `e5e1eeb`, then `62a4f3a` on 09-16 |

So the stale premise is not only the 09-08 one the packet names. **This page has carried an un-noted defect for
27 days**, pointed at by a document that asked for exactly this note on the day it found it.

**One precision on the packet's wording:** it calls this *"the stale 'reads zero' claim."* Registration 44 does not
say "reads zero" — that is the **pilot's** phrasing (`forgetting_pilot:447-449`, about `forget-rate.js`).
Registration 44 says *"never deleted anything,"* in lines. The note addresses what the registration actually says.

## 2 · WHAT THE NOTE SAYS

Appended after registration 46, headed **`NOTE ON REGISTRATION 44, 2026-09-21`**:

1. its unit was lines;
2. the 08-25 correction was in the same wrong unit, and asked for this note;
3. false on the reading path since 09-08 — both files, commits, bytes, and the bracket with its command, pointing
   at the pilot addendum (`988c9fe`).

Then two things it deliberately does **not** do:

- **It does not fire or refute the falsifier.** The falsifier is about a decay organ; both departures are hand
  deletions of defective artifacts. The organ question is exactly as open as when registered.
- **It warns against the one misreading that is now available.** The falsifier is a conjunction — *"a decay organ
  still unbuilt **and** the ratio unchanged."* The two hand deletions moved the ratio, so read literally the
  falsifier is now **discharged**. The note says plainly it should not be read that way: a quantity that moved for
  a reason unrelated to the question cannot clear it. That is the wrong-unit error of 08-25 one level down, and I
  would rather name it before someone quotes the clause as satisfied.

## 3 · APPEND-ONLY, MEASURED

| check | result |
|---|---|
| sha256 before, verified against the read | `b795c1f3efc88388…` — the script refuses if it differs |
| bytes | 10,827 → **13,664** (+2,837) |
| existing 10,827 B after | **byte-identical** |
| `git diff --numstat` | **37 added · 0 removed**; lines beginning `-`: **0** |
| sha256 after | `bd1821cc9b98e69e…` |
| run twice | refuses — checks for its own heading first |

**Every reference in the note was checked before it was written:** `988c9fe` is the pilot addendum (touches only
`forgetting_pilot_2026-08-25.md`, +42); `b76c9c7` is R6 (touches `forget-rate.test.js`); pilot `:161`, `:353` and
`:447` read what the note says they read. The bracket figures are the ones re-derived at `784b54d` for L060 and
independently by the librarian at `librarian/2026-09-21.md:16`.

## 4 · carrier-drift, AS ASKED — with the time, because B is re-freezing CH-4 in parallel

`node consonance/tools/carrier-drift.js`, run at **2026-09-21 03:06:09 (Regina)**, exit read with `PIPESTATUS[0]`:
**exit 1 · RED.**

| block | count | members |
|---|---:|---|
| **RED** | **2** | `CH4-DRIFT-ADDED exo_memory/cards/every-digest-carries-its-function.md` · `CH4-DRIFT-ADDED exo_memory/record/retired_seats_2026-09-11.md` |
| PENDING (disarmed entries — *"Not red"*) | 40 | not listed here; one of them is mine, below |
| DESCRIPTION SURFACES | 0 | — |

**Neither red is this lap's.** Both are the two files A ruled *"re-freeze as-is"* and B is re-freezing in parallel;
a run a few minutes later may read green on them. That is why the time is named.

**One PENDING finding is on my file, and it predates this lap:** `UNACCOUNTED exo_memory/map/C.md:201` — a line I
wrote on **2026-09-02** (`git blame` → `b3b5a4b2`) that quotes the retired `light-not-lifeguard` wording as the
*subject* of a finding about the registry pattern. It is against the DISARMED entry and is not red. Named so it is
not mistaken for something this append introduced.

**And a limit the chair should know before reading my count as evidence about this note:** carrier-drift's header
says it **skips `exo_memory/journal/`** (*"42 traces skipped (exo_memory/journal/, dreams/, attic/ …)"*). So it
cannot see this note at all — neither to credit it nor to flag it. **A green or red carrier-drift says nothing
about whether registration 44 is now marked.** The only check on that is reading the file.

## 5 · WHAT WAS NOT VERIFIED

1. **Other carriers of registration 44's headline.** The note sits beside the registration; anything that *quotes*
   "never deleted anything" elsewhere is untouched and unsearched, and carrier-drift has no registry entry for it.
2. **Whether the note reaches a reader.** `journal/` is in the librarian's index tier; `2026-08-24.md` is dated
   08-24 and may sit in the collapsed range of the L062 window rather than the listed one.
3. **Machine D.** Written on L.

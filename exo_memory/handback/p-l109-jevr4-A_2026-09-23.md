PREDICTION (pane A, L109, written 2026-09-23T12:21:16Z BEFORE opening jev_judge.jsonl or judge-captures/): METHOD.md, the discipline text, is the largest part of every L2 prompt, about 60-75% of input tokens; median input about 4-6k tokens; about half of the input (most of the discipline) could go without changing a verdict; p50 latency 2-5 s.

# P-L109-JEVR4 · ALPHA — Jev's L2 input is 2,190 tokens at the median and 79% of it is FIXED: the same frame on every call. METHOD.md alone is about half, and the turn being judged is 21%. Latency is 331 ms and does not depend on size. Cost reads $0 on every row: $0.068 at list price for all 494 calls. 57% of L2 calls judge machine packets and 8% judge keep-warm "ok"s. Four candidate cuts would take the median call to about 1,060 tokens, untested

**Pane A, machine L, 2026-09-23 06:2x–06:4x.** Lap L109, research R4. **Read-only over the Jev store, no Jev or model call.**
Nothing committed.

**Files:**
- the findings: `exo_memory/loop/jev_r4_cost_input_2026-09-23.md`;
- two read-only instruments beside it: `jev_r4_measure_2026-09-23.js` and `jev_r4_classify_2026-09-23.js`;
- this hand-back;
- one line in `map/A.md`.

## 1 · THE PREDICTION, SCORED (it is line 1, saved at 12:21:16Z before any data was opened; git-blob 9e7387ce)

| predicted | measured | verdict |
|---|---|---|
| METHOD.md about 60–75% of input tokens | **47% of bytes; about 1,100 of 2,190 tokens at the median (50%)** | **WRONG**: I over-weighted it. The fixed frame as a whole is 79% |
| median input about 4–6k tokens | **2,190** (p90 2,986, max 3,954) | **WRONG**: about 2× too high. I was carrying L3's size (median 5,772) |
| about half could go without changing a verdict | the untested arithmetic says 52% (cuts 1b+3+5+6) | **NOT TESTED**: no calls this lap. The number agrees; the claim is still open |
| p50 latency 2–5 s | **331 ms** (p90 597) | **WRONG** by about 10× |

## 2 · THE FINDINGS — the full page is the master; these are its load-bearing lines

- **What Jev receives.** `{ state: the L2 overseer prompt byte for byte, questions: one choice question with three
  criteria }`.
  - **All 370 L2 rows matched their stored prompt by sha256.**
  - The fit gives the fixed part as **1,731 tokens**. Input tokens against view bytes: slope 0.277, **R² 0.976**, one
    discipline version across all 370 rows.
- **Where the bytes go:**

  | part | bytes | share |
  |---|---|---|
  | METHOD.md | 3,855 | 47% |
  | the move | 1,014 at p50 | 14% |
  | the user message | 707 at p50 | 12% |
  | Jev's criteria | 741 | 9% |
  | the tail | 698 | 9% |
  | instructions | 593 | 7% |
  | header | 129 | 2% |

- **Latency.** p50 331 ms, p90 597, max 1,560; **r = 0.025 against size.** Shrinking the input won't make it faster.
- **Cost.**
  - `"0"` on all 494 rows. The record's reading is "free credits, hobby plan" (librarian 09-21 12:52). **That reading is not
    confirmed.**
  - At the list price of $0.042/M input tokens: **$0.068 for all 1,617,279 tokens.** Cost is not the constraint; the signal is.
  - Also on record: **ZDR was refused on the hobby plan**, so the gateway retains what it is sent. That is for the
    standalone README.
- **What "user" is:** 211 of 370 are machine packets (57%), 29 are keep-warm pings (8%), and 130 are other (35%). This is a
  prefix heuristic. **Of the 30 drift flags, 12 are on machine-packet turns and 0 on pings.**
- **The cuts, ranked** (tokens per call):

  | cut | saves | risk |
  |---|---|---|
  | drop METHOD.md | ~1,100 | high |
  | keep only its third principle | ~870 | medium |
  | skip keep-warm turns | 8% of calls, 0 flags lost | low |
  | shorter criteria | ~150 | |
  | cap the user message at 1 kB | ~95 | medium |
  | drop the output-format paragraph Jev never uses | ~74 | low |
  | drop the header naming | ~37 | |

  **The evidence each needs:** a paired re-ask of the 370 stored prompts, against **Jev's own full-vs-full test-retest
  floor**, measured first (the D118 lesson). Register flip rate and |Δp(drift)| before running, and score the 30 drift rows
  separately. About $0.21 at list price.
- **Consonance-specific vs general:**
  - **general:** the view (user + move), the abstain rule, the choice shape, and mostly the function test;
  - **Consonance-specific:** METHOD.md's wording, the header naming, and the traffic mix;
  - **the Claude overseer's, not Jev's:** the output-format paragraph.

## 3 · CORRECTIONS I MADE TO MYSELF THIS LAP

- **The classification first ran as an inline `node -e`.** I moved it into a named script
  (`jev_r4_classify_2026-09-23.js`) so the §4 table re-derives from a file, and the findings' header now points there.
- **The prediction's latency figure came from nowhere I could check.** I had no Jev timing in mind, only a guess at a
  hosted-model round trip. It was the most wrong of the four.

## 4 · WHAT THIS DOES NOT ESTABLISH

- **That any cut is verdict-neutral.** Nothing was re-asked. The 52% is arithmetic.
- **Per-part token counts.** The gateway reports one count per call. The byte-share split (a) assumes a uniform tokens-per-byte
  across parts; the fit (b) pins only the fixed-vs-variable split, and it is the one to trust.
- **What `cost:"0"` means.** It needs the Vercel account page, which is the keeper's.
- **The traffic classes beyond their prefixes.** "Other" mixes person text with tool output. No row was read in full.
- **D's store.** This is L only (494 rows).

NEXT: librarian call_librarian with the L109 pointer when this hand-back is written — plan default after it: the librarian collates all four research laps together and drafts the standalone design, unless the output says otherwise

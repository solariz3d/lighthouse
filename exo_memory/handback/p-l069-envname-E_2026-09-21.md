# P-L069-ENVNAME — one env name for the state dir (pane E, L069 packet 1, 2026-09-21)

Packet: the chair's L069 packet 1, from the keeper's *"lets do the small left overs first"*. Started 06:26:17, and the
mutant run finished at 06:28:42 (`date +%T`). Machine L, HEAD `7da934d`. **Nothing committed.**
`git diff --numstat`: `live-mirror-stop.js` 3/2 · `live-mirror-stop.test.js` 14/0 · `live-follow.js` 4/2 ·
`live-follow.test.js` 24/1. `state-sync.js` needed no change. **A's `close.js` / `close.test.js` were not touched.**

## 0 · Headline — CONSONANCE_STATE, no alias, and A's assertions stay true unchanged

**The name kept is `CONSONANCE_STATE`**, the one `state-sync.js:133` and, through it, `close.js` read. It is also what
every existing state-sync and close test sets. **`CONSONANCE_STATE_REPO` is retired.** It was read only by my own two
L062 files, `live-follow.js:36` and the unregistered `live-mirror-stop.js:65`, and nothing outside the repo sets it.

**The consequence the packet anticipated, and it went the easy way:** `close.test.js:170-174` asserts that the
refusal names `CONSONANCE_STATE` and does NOT name `_REPO`. Under the unified name **both assertions are still true**:
close's refusal was already naming the surviving name. **No assertion changes and nothing is weakened.** Only two of
A's **comments** now describe a split that no longer exists. They are proposed edits for A, in §3.

## 1 · Every reader, doc and test, grepped before choosing

    git grep -n "CONSONANCE_STATE_REPO\|CONSONANCE_STATE\b" (repo, minus handback/librarian/map/loop records)
      CONSONANCE_STATE      state-sync.js:133 (read) · :143 (refusal) · close.js:160-164 (comment + refusal text)
                            close.test.js:101,119,452 (env set) · :168-174 (assertions) · state-sync.test.js:111,1201,1205
      CONSONANCE_STATE_REPO live-follow.js:36 (read) · :47 (refusal) · live-follow.test.js:80 (env set)
                            live-mirror-stop.js:65 (read) · :260 (skip reason) · close.js:161, close.test.js:168 (comments)
      Rust (consonance/src-tauri/src/*.rs), *.ps1 incl. launch.ps1, dev/: neither name (the same git grep covers them)
    grep -rn CONSONANCE_STATE ~/.claude/shell ~/.claude/settings*.json ~/.consonance.json
      only CONSONANCE_STATE_SOURCES and CONSONANCE_STATE_BLOCK (sessionstart-state.js:50,:101) — different variables
      that share the prefix; neither state-dir name
    PowerShell GetEnvironmentVariable(User|Machine) for both names       all empty
    this pane's environment                                              both unset

**Does anything outside the repo set the retired name? No, on L.** That is why there is **no alias**. The packet made
the alias conditional on an outside setter, and none exists here. The two mutants in §2 that add a silent alias are
killed, so an alias cannot creep back unnoticed. **D was not checked** (§5). If D sets `CONSONANCE_STATE_REPO`, its
`live-follow` falls through to `state_dir` or refuses **loudly, naming `CONSONANCE_STATE`**; it never reads a wrong
directory silently.

The `state_dir` key already satisfied all four tools and is unchanged.

## 2 · Test first — red before, green after, and every mode

The tests that were changed or added:
- **`live-follow.test.js:80`**: the "env wins over config" line set `CONSONANCE_STATE_REPO`. It is **verifiably wrong
  under the new name** because it encodes the retired name, not a weakened expectation. The change sets
  `CONSONANCE_STATE` and asserts the same thing, with the reason written at the site.
- **Two new `live-follow` tests:** *the retired name is not read* (the export is `null` with only `_REPO` set); and
  *the refusal names CONSONANCE_STATE and not _REPO*.
- **One new hook test:** *CONSONANCE_STATE wins over state_dir, and the retired name is not read*.
- **The existing R-C1 skip-reason test** gained two assertions (it names `CONSONANCE_STATE` and not `_REPO`). Nothing
  was removed from it.

    RED   node consonance/hooks/live-mirror-stop.test.js     10 / 2
          node consonance/tools/live-follow.test.js          3 / 3
    GREEN node consonance/hooks/live-mirror-stop.test.js     12 / 0
          node consonance/tools/live-follow.test.js          6 / 0
          node --test --test-concurrency=4 <both>            18 / 0
          node --test --test-name-pattern="L069" <both>      3 / 0
    UNTOUCHED, still green   node consonance/tools/close.test.js       30 passed, 0 failed
                             node consonance/tools/state-sync.test.js  78 passed, 0 failed
    MUTANTS node scratchpad/l069/mutants.js  (copies in scratchpad/l069/w/consonance/{hooks,tools})
          GREEN 12/0 · GREEN 6/0 pre-flights
          KILLED hook reads the retired name again · hook reads BOTH names (a silent alias) · hook skip reason names
                 the retired name · follow reads the retired name again · follow reads BOTH names (a silent alias)
                 · follow refusal names the retired name
          6 listed · 6 killed · 0 survived

## 3 · PROPOSED for A — comments only in `close.js` and `close.test.js`; no assertion changes

Both comments now state something false: that live-follow reads a different name. Exact edits:

**`consonance/tools/close.js:160-161`**, replace:

    // Names what THIS tool reads (state-sync.js stateDir(): CONSONANCE_STATE, then state_dir). live-follow.js reads
    // CONSONANCE_STATE_REPO instead, so its sentence would print a recovery that does nothing here.

with:

    // Names what THIS tool reads (state-sync.js stateDir(): CONSONANCE_STATE, then state_dir). Since L069 every state
    // tool reads that one name; CONSONANCE_STATE_REPO is retired, so it must never be printed as a recovery.

**`consonance/tools/close.test.js:167-169`**, replace:

    // CONSONANCE_STATE (state-sync.js:133). live-follow.js reads CONSONANCE_STATE_REPO — a different name — so copying
    // its sentence here would print a recovery that does nothing for close.

with:

    // CONSONANCE_STATE (state-sync.js:133) — since L069 the ONE name every state tool reads. CONSONANCE_STATE_REPO is
    // retired (read by nothing), so a refusal naming it would print a recovery that does nothing.

**The assertions at `close.test.js:173-174` should stay exactly as they are.** Under the new name they are the guard
that the retired name never reappears in close's recovery text. They were right before and are still right.

## 4 · Corrections, including mine

- **The split was mine.** L062 R-C1 (`241a57a`) introduced `CONSONANCE_STATE_REPO` into two files by keeping the name
  the hook already used, instead of the one `state-sync.js` used. My R-C1 hand-back *noticed* the two names and
  decided not to unify them ("pre-existing inconsistency; note it"). Two laps later that took a packet to undo.
  **Noting a split instead of closing it is how a second name survives.**

## 5 · NOT verified

- **D.** Its environment, its `~/.consonance.json`, and any script there that might set `CONSONANCE_STATE_REPO`.
- **Documents outside the grep's reach:** hand-backs, librarian notes, maps and loop records name the retired variable
  as history and were deliberately excluded. They are dated records, not instructions.
- **No live run** of `live-follow` against the real state repo after the rename. On L it resolves through
  `~/.consonance.json:19` `state_dir`, which neither name touches.

# T2 · ALPHA — the boundary-check draft note, gone through claim by claim

**Pane A, machine L, 2026-09-16 ~04:4x. Read-only: nothing edited, nothing installed, nothing run that writes.**
The note was read at the path given (outside the repo); every verdict below is against files in this repository.
No other seat's work from tonight was opened.

**Nine of the eighteen are false.** Five of those are not slips but **inversions** — the note says the opposite of
what the file says, and in each case the inverted version describes the defect the instrument was built to repair.
That is the pattern worth carrying to whoever lands it: **this note fails in the direction of the predecessor.**

---

## 1 · THE FALSE CLAIMS

### Claim 1 — "299 lines"
**False. It is 298.** `wc -l < consonance/tools/boundary-check.js` → **298**; `awk 'END{print NR}'` → **298**; the file
ends with a newline (`tail -c 1 | xxd` → `0a`), so it is 298 by either counting convention. The test file beside it is
**297**, so this is not a swap of the two numbers. The rest of the claim is right.

### Claim 4 — "failing to write a lap makes the check PASS, which is the asymmetry that makes it safe"
**False, and inverted.** `boundary-check.js:29-30`: *"The numerator is `lap.jsonl`, which IS self-reported — but here
under-reporting makes the check FIRE rather than pass. That asymmetry is the whole repair."* The table at
`loop/boundary_falsifier_2026-08-28.md:62` is explicit: absence of a lap row was **GREEN** in the old check and
**FIRES** in this one. **The truth:** failing to write a lap makes the check FIRE, and that is the asymmetry that
makes it safe. The note's version is a description of the predecessor's defect.

### Claim 6 — "A brief pasted into a pane by hand appears in the denominator, so the rate it prints is exact"
**False on both halves.** `boundary-check.js:51-53` (NON-CHAIR HANDOFFS): *"Only `[chair:...]` arrivals count. A brief
pasted into a pane by hand, a file carried to the other machine, a pane-to-pane handoff — all left the room and NONE
of them appear. **The denominator is a floor.**"* Same in the registration, written before the tool
(`loop/boundary_falsifier_2026-08-28.md:16-19`): *"The true rate can only be worse than what prints."* Enforced in
code at `boundary-check.js:153` (`if (!CHAIR.test(o.text)) return;`). **The truth:** a hand-pasted brief is invisible
to it, and the printed rate is a floor, not exact.

### Claim 11 — "Its predecessor read 'if two consecutive cycles produce no lap row'"
**False on the number.** The registered text was **three**: `boundary-check.js:11` quotes *"if three consecutive
cycles produce no lap row, the freestyle clause has eaten the instrument"*, and
`loop/boundary_falsifier_2026-08-28.md:34` quotes the same from `BUILDING.md:326-330`. The rest of the claim — that
nothing counts cycles, so no run of any tool could evaluate it — is right (`boundary-check.js:13-15`).

### Claim 12 — "That predecessor was struck because it was a boundary test aimed at a presence harm"
**False, and inverted.** `boundary-check.js:16-18`: *"It was a **PRESENCE test aimed at a BOUNDARY harm**."* Same
wording at `loop/boundary_falsifier_2026-08-28.md:40`. **The truth:** it was a presence test aimed at a boundary
harm — which is why a room that logs laps for its tight-loop work while freestyling every dispatch read green.

### Claim 13 — "The replacement was drafted by the seat that wrote the original, which is what makes it trustworthy"
**False on the fact, and backwards on the value.** `loop/boundary_falsifier_2026-08-28.md:3-5` (the seat line is `:4`): *"**Seat:** pane B,
LAP D002. **Not the seat that wrote the original** (`fb08c50`, librarian) **and not the seat that ruled on its
removal** (P5)."* **The truth:** it was drafted by a seat that wrote neither the original nor the ruling — and that
separation is what the room treats as the source of trust, not its opposite. The committee brief's own rule is *no
seat scores its own work*; the note's sentence would make a self-audit into a credential.

### Claim 14 — "`boundary-check.test.js` mutation-proves three load-bearing behaviours"
**False twice.** There is no mutation testing in that file: `grep -n -i "mutant\|mutation" consonance/tools/boundary-check.test.js`
→ **no output**. Its method is the opposite of source-reading, and it says so at `boundary-check.test.js:4-9`:
*"EVERY ASSERTION DRIVES THE TOOL AGAINST A FIXTURE AND READS WHAT CAME OUT. Nothing greps the source."* And the
count is **four**, not three — `:11` heads the list *"THE FOUR THAT CARRY THE REPAIR"* (zero dispatches is
UNMEASURED; a blind window is UNMEASURED; a lap opened after the dispatch does not cover it; three verdicts have
three distinct exit codes). The file holds **22** tests in total (`grep -c "^test("`).

### Claim 15 — "The check reads whether a guess existed and also whether the guess was any good"
**False.** `boundary-check.js:54-55` (A GOOD SEAL): *"It reads that a guess existed and was sealed before the
dispatch. **It cannot read whether the guess was any good. Four junk paths pass.**"* The tool prints the same limit on
every green run — `:285`: *"It does not say the seals were good, only that they existed before the work left."*
**The truth:** existence only; quality is outside it by design.

### Claim 17 — "Because the check is a boundary test, there is a window to fill and a rate to reach before it can fire"
**False, and it is the claim the registration answers most directly.**
`loop/boundary_falsifier_2026-08-28.md:110-111`: *"**One unsealed dispatch fires it. No window to fill, no
`RATE_FLOOR`, no n to reach — a boundary is crossed once or not at all.**"* The code agrees:
`boundary-check.js:271` fires on `if (unsealed.length) {` — any single uncovered arrival, with no threshold anywhere in
the file. **The truth:** being a boundary test is precisely *why* one event is enough; needing three uncountable
cycles was the predecessor's defect.

---

## 2 · WHAT CHECKING TAUGHT ME THAT THE NOTE GETS WRONG ELSEWHERE

- **The tool's own citation has drifted, and the note inherits the trust in it.** `boundary-check.js:25` and the
  registration both name `main.rs:5605` as where `[chair:MAIN]` is stamped. **Today that line is in the middle of
  unrelated `--resume` documentation**; the stamp is at `consonance/src-tauri/src/main.rs:9504`
  (`let msg = format!("[chair:MAIN] {text}");`). Claim 3's substance is TRUE — the row is written on arrival by the
  backend, so the sending seat cannot omit it — but anyone landing this note should not re-quote `:5605`. This is the
  carrier class the room already tracks: the mechanism held, the pointer rotted.
- **Claim 3 has one general exception, and the tool names it rather than the note.** A sending seat cannot suppress
  the row — but `data/blind.lock` mutes `board_push` for *everyone*, transcript ingest included
  (`boundary-check.js:44-47`). The tool refuses to score such a window (UNMEASURED, `:227-237`), which is the honest
  handling; a reader of the note alone would think the denominator is unsuppressible full stop.
- **Claim 5 is right and is the most easily mis-read line in the note.** The registration
  (`loop/boundary_falsifier_2026-08-28.md:89-90`) shows two readings of the same 12 dispatches: the stage-presence
  reading *"0 of 12 carry a `dispatched` row → would FIRE at 100%"*, and the seal reading *"0 of 12 rendered with no
  sealed lap → HOLDS"*. The "0 of 12" in the note is the count of UNSEALED dispatches, not of seals. Landed without
  that context it will be read as "zero seals out of twelve", i.e. as a failure, when it records a pass.

**And one about my own draft:** I wrote this hand-back's citations from memory of the reads above, then
re-derived every one with `grep -n` before filing. **Eighteen of them were off by one to ten lines** — the same
class of error I am reporting in the note, produced while reporting it. Every path:line above is now the grep's
answer, not mine.

## 3 · NOTHING I CALLED UNRESOLVABLE, AND TWO LIMITS ON HOW I CHECKED

No claim was unresolvable from the disk. Two verdicts rest on the record rather than on a re-derivation, and I would
rather say so than let them read as measurements of mine:

- **Claim 8** (the blind window 2026-06-30 → 2026-08-01, 2,473 entries) is **verified as a RECORD, not re-derived**.
  It appears twice on disk — `boundary-check.js:45-47` and `loop/boundary_falsifier_2026-08-28.md:23-25`, the latter
  adding *"those two lines are the only trace of it on a 120,672-row board"*. I did not open `data/board.jsonl` to
  count it; the bound was read-only and a 120k-row scan is a measurement, not a check of a note.
- **Claims 2, 7, 9, 10, 16, 18 are true** and I read them at source (`boundary-check.js:80-81` for the two ledger
  paths; `:44-48`; `:239-247`; `:218`; `:56-58`; `brief/BUILDING.md:539-543` for the cut, which the note paraphrases
  faithfully). I did not run the tool: running it is a read, but it is also the one action that would have made this
  lap's output depend on tonight's ledgers rather than on the repository.

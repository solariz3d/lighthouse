# P-L102-REDERIVE — C (L102, ON L, 2026-09-23 ~04:5x local) — T-J1 v2 MEMBER FILE: SECOND BUILD IS BYTE-IDENTICAL

**Result: IDENTICAL.** I followed B's `p-l093-members-B_2026-09-23.md` §1 exactly. The builder extracted from the
committed file rebuilt it byte for byte.

| | sha256 | bytes |
|---|---|---|
| committed `exo_memory/loop/tj1v2_members_2026-09-22.json` (last changed `238781a`; git-blob `1810f821b37c0a4679347b64ff009aa50361cc2e` both at HEAD and in the working tree, unmodified) | `feca21d5711ad75f77de7d2ebfc5637d0f94939906e21ad803a07ca9ee244332` | 121,214 |
| my rebuild, in my scratchpad (`<scratchpad>/l102/tj1v2_members_2026-09-22.json`) | `feca21d5711ad75f77de7d2ebfc5637d0f94939906e21ad803a07ca9ee244332` | 121,214 |

`cmp <rebuild> <committed>` printed nothing and exited 0. B's §1 expected value is `feca21d5…4332`, and that is what I
got.

## The commands

    # 1. the builder, taken from the committed file's own builder_source field (18,147 bytes; sha256 b2326a91…482c9)
    node -e "fs.writeFileSync('<scratchpad>/l102/build_tj1v2_members.js', JSON.parse(fs.readFileSync('exo_memory/loop/tj1v2_members_2026-09-22.json','utf8')).builder_source)"
    # 2. run it into a TEMP path, never over the committed file (10:46:49Z → 10:49:20Z, 2m31s, exit 0)
    node build_tj1v2_members.js C:/Consonance/lighthouse --out <scratchpad>/l102/tj1v2_members_2026-09-22.json
    # 3. compare
    sha256sum <scratchpad>/l102/tj1v2_members_2026-09-22.json      # feca21d5…4332
    cmp <scratchpad>/l102/tj1v2_members_2026-09-22.json exo_memory/loop/tj1v2_members_2026-09-22.json   # (nothing)

The builder's own summary said `"sha256": "feca21d5…4332", "bytes": 121214, "ideal": 75, "full_draws": 19`. That
matches B's §2 (57 of 75, with 19 members drawing all three).

**Environment (L):**
- node `v24.18.0`, git `2.53.0.windows.2`;
- the build ran from HEAD `bbd56ad`, but it reads only the pinned commit and its ancestors, so HEAD does not enter
  the output.

## Does the builder read only committed inputs? Yes, with one environment dependence to name

I read the builder source and listed every input and every source of nondeterminism:
- **Git objects only.** It runs `git grep` at the pinned commit `3e189eca…0149`, then `git blame` (`-l -L` and
  `--porcelain`), `git show <rev>:<path>` and `git rev-parse` at that commit and its ancestors.
- **It refuses to run** if the universe's sha is not `7303e265…`, so a different corpus cannot produce a quiet
  different file.
- **It reads its own source** (`readFileSync(__filename)`) to embed it as `builder_source`.
- **No clock, no `Math.random`, no environment variable, no home directory or machine path.** The only argument that
  changes anything is `--variant`, and §1's command doesn't use it.
- The one `sort` has a total tiebreak (`|| (a - b)`).
- **The pin reaches D:** `3e189ec` is an ancestor of HEAD, and `git branch -r --contains 3e189ec` lists `origin/main`.

**Not machine-local, though it looks as if it could be:**
- `core.autocrlf = true` on L does not reach this build. `git show <rev>:<path>`, `git grep <tree>` and
  `git blame <rev>` read raw blobs, and none of them applies the eol conversion without `--filters`.

**The one real environment dependence: `git blame`.** Its attribution is computed by the local git, so it can vary with:
- **the git version**, meaning blame and diff heuristics;
- **blame- or diff-affecting config:** `blame.ignoreRevsFile`, `diff.algorithm`, `diff.indentHeuristic`. On L all three
  are **unset**, and the repo has no `.git-blame-ignore-revs`.

A D that set any of these, or ran a git old or new enough to attribute a line differently, could rebuild a different
file from the same objects. The universe-sha refusal would NOT catch it, because that guards `git grep`, not blame.
**The same comparison on D would settle it.** Two builds on one machine agreeing (B's and mine, both on L) do not show
that it reproduces across machines.

## NOT verified

- **A build on D.** This is L only: two seats, one machine, one git.
- **Whether B's build ran with the same git version and config.** B's hand-back does not record them. The identical
  bytes say our environments agreed on this input, not what B's environment was.

No file changed. No member-file or registration edit, no Jev or model call. Nothing heavy was run, so the heavy-run lock
was not needed.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when the comparison is written — plan default after it: E writes the T-J1 schema after L100, unless the output says otherwise

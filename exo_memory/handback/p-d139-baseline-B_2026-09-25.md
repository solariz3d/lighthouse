# P-D139-BASELINE · BRAVO — the cold-stranger baseline for the README first screen and the About tab: 4 isolated reads, answers verbatim, unparsed items README 19/16 and About 46/52. Neither text told either reader what to do next. $0.46

**B (pane `12fb81f6`), machine D, 2026-09-25 ~02:5x–03:1x local.** Packet: the chair's D139, R1, B's row of
`exo_memory/loop/plan_readme_about_rewrite_2026-09-25.md` (`484f77c`). **Nothing committed.** README.md and index.html
were not touched (HEAD `484f77c`, both files last changed at `0a636f5`, `git status` clean for both). No settings
edit, restart, close.js or publish.

## 0 · FILES

| file | what |
|---|---|
| `exo_memory/loop/readme_about_baseline_2026-09-25.md` | **NEW, the result only.** 37,429 B, sha256 `960137b190ad9f137163ddb6d2dc6891f3eb0c00f2767e8d66c17cf62d517e4b`. §1 the counts; §2 exactly what was given; §3 the reader, the prompt verbatim, the leak, the cost; §4–5 the four answers VERBATIM; §6 both texts as given |
| `<B scratch>/d139/` | `extract.js` (the texts), `read.js` (the 4 calls), `baseline.js` (writes the file and re-derives the counts), `out/*.json` (each call's whole CLI output) |

## 1 · RESULT (`node <B scratch>/d139/baseline.js`)

| text | given | unparsed, reader 1 / 2 | "what do I do next?" |
|---|---|---|---|
| README first screen | lines 1–20 of git-blob `b5ce1063…`, 2 HTML comments removed; 1,930 B, 333 words, sha256 `e9c260b6…` | **19 / 16** | neither reader could answer: "The first screen doesn't tell me" / "The text doesn't tell me" |
| About tab | `#about`, lines 273–362 of git-blob `f25ad9e1…`, as visible text; 7,941 B, 1,201 words, sha256 `8896d22f…` | **46 / 52** | both found two conflicting "start here" pointers and no install or launch step |

- **The count rule:** list items under the reader's question 5, counted by a regex on the saved JSON, not by hand. The readers' own grouping headings are not counted. R2 must use the same rule, the same prompt (in the baseline file, verbatim) and the same flags.
- **The four calls:** 1 turn each, no tools. Tokens: input 8, cache-create 24,998, cache-read 2,124, output 12,887; **$0.4582** (each call's own `usage`).

## 2 · THE LEAK, NAMED AND SIZED

- The route still loads `~/.claude/CLAUDE.md` (upstream #87590): 7,574 B of the keeper's general engineering rules.
- `grep -ciE "consonance|lighthouse|keeper|seat|librarian|orchestrator|the room|jev|pane"` on it returns **0**, so it defines none of the terms the readers flagged.
- It may tilt the reader toward an engineer's reading. It does so for R2 identically.
- 0 `.jsonl` were created under `~/.claude/projects` (4 empty project dirs, left).

## 3 · WRONG column

- **W1, extraction, fixed before any read.** My first About extraction left `&sect;` undecoded (3 places) and collapsed the `<pre>` loop diagram onto single-space lines, which a browser doesn't do. Fixed (`extract.js`, `<pre>` kept whole). The sha256 above is the fixed text's. No reader saw the broken one.
- **W2, a false check.** `grep -c $'\x00'` printed 81 for NUL bytes in about.txt. Bash drops the NUL, so it matched every line. The real check is `read.js`, which refuses a text holding a NUL, and it ran clean.
- **The "what do I do next?" column is my compression** of the readers' §4, with their words quoted. The verbatim answers are the master.

## 4 · NOT VERIFIED

- **Each call wrote 167 B to stderr, and I did not capture it.** All four exited 0 with `success`. The content is unknown.
- **n = 2 readers per text, one model** (claude-opus-5-5, the subjects' model). A different model or a human stranger would be a different baseline.
- **The About text as rendered.** It was extracted from source, not screenshotted from the running app. CSS that hides or reorders content would not show; none was checked.
- **The README as GitHub renders it.** Markdown was left as markdown, and no render was compared.

NEXT: librarian call_librarian with the hand-back pointer when the baseline is written — plan default after it: the librarian collates R1 when B and C have rung, and the keeper reads the draft in R3, unless the output says otherwise

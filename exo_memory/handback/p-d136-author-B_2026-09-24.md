# P-D136-AUTHOR · BRAVO — eight tasks authored, calibrated and sealed under A13 with no seat seeing one. §1 killed six of them; the two that pass are one per kind, so under R2-5 NO KIND IS FIELDABLE. The sealed rows are prepared and NOT for landing. The completed §9 goes to the keeper with three options. No subject was run

**B (pane `12fb81f6`), machine D, 2026-09-24 23:3x → 2026-09-25 00:2x local.** Packet: the chair's D136.
- **No task text, key, plant, cold answer or score text is in this file, the repo, the board, the ring, my map line or my replies.**
  I saw digests, counts, criterion ids (`c<n>`), brief line numbers, exit codes and token usage, and nothing else.
- **Nothing committed. Nothing pushed.** No subject run, no settings edit, no restart, no close.js, no publish.

## 0 · FILES

| file | what |
|---|---|
| `exo_memory/loop/battery_run2_sealed_rows_2026-09-25.md` | **NEW, the one-file sealed rows**, sha256 `90bcb12f3dd08cc1846d0fd0768b607c694568a790f98e1519e200d477ba1117` as written by me. For every task: object tree digest, sealed-key sha256, criteria/X, cold-1 tree digest. Plus the A6 a/b order. **Marked NOT FOR LANDING**: no kind has a pair |
| `exo_memory/loop/pane_battery_run2_s9_2026-09-24.md` | **+37 −0**: a dated COMPLETED section that answers all eight items, with the keeper's three options on top |
| `exo_memory/loop/pane_battery_run2_DRAFT_2026-09-19.md` | **+45 −0**: new §13 — the §1 table, A14–A17, the falsifiers read against the run |
| off-repo, on D | three separate random-named roots under `%TEMP%` (object / key / cold, A5) plus a sealed logs root. Locations in `<B scratch>/d136/state.json`, deliberately not written in the repo |
| `<B scratch>/d136/` | `orch.js` (every stage), `seal.js` (void-grep, digests), `rows.js`, `locate.js`, `t1check.js`, `t5bdiff.js`, `probe3.js`, `meta/usage.jsonl` (one row per process) |

## 1 · WHAT RAN (all `claude -p --no-session-persistence --model claude-opus-5-5 --setting-sources project --settings {"disableAllHooks":true} --strict-mcp-config --mcp-config {"mcpServers":{}}`, each from a fresh `r2p-` temp cwd)

- **The model** matches the subjects: `<B scratch>/d136/models.js` reads A, C and E's newest transcripts, and all three are on claude-opus-5-5.
- **The checkout** is a `git archive` of `0566734` (2644 files). That commit is on D but was not on origin when read; the sealed rows name it.
- **Authoring:** 4 processes, one per kind, each writing an a and a b. Object and key went into separate roots. **T1** tasks carry a mutant harness (checked: the author's own tests are green on the reference and catch every mutant, `t1check.js`: 18/18, 16/16; after the retry, 16/16, 20/20).
- **§2:** my D135 `scan.js` (lexical), then a non-subject semantic-mark process (A3), with up to two rewrites.
- **§1:** cold reader 1 per task in a temp cwd holding only the object, plus a fresh checkout (A1). Then a non-subject scorer wrote `score.json` and the pruned `sealed_key.json`.
- **Retry:** one "clearly harder" re-author per failed task, per the draft; predecessors were kept as `.replaced-*`.

## 2 · RESULT

| round | T1a | T1b | T2a | T2b | T3a | T3b | T5a | T5b |
|---|---|---|---|---|---|---|---|---|
| first: cold-1 found | 16/18 | 16/16 | 15/15 | 14/14 | 14/14 | 16/16 | 11/15 | 12/13 |
| retry: cold-1 found | §2 fail | 17/20 | 14/14 | 16/16 | 12/12 | **7/13** | **4/17** | 10/14 |
| outcome | DROP | DROP | DROP | DROP | DROP | pass | pass | DROP |

- Source: `orch.js status` and `meta/sc1*.out`.
- **Six of eight died at §1 or §2. The two passes are one per kind, and R2-5 needs a and b.** So as the draft stands, run 2 has no fieldable kind.
- **Cold reader 2 was not run.** A base rate for a task no subject takes measures nothing. If the keeper fields the singles, it is 2 × 2 commands.

## 3 · BLINDING, CHECKED (not assumed)

- **Transcripts:** `orch.js transcripts` shows 4,034 `.jsonl` under `~/.claude/projects` before and 4,034 after; 0 new, 0 under an `r2p-` dir.
- **A NEW LEAK ROUTE, found and closed (A14):** `--no-session-persistence` still spills large tool outputs to `~/.claude/projects/<cwd>/<session>/tool-results/`.
  - After the first author round there were 2 files (31,151 B and 66,290 B). I did not read them. They were moved into the sealed logs root (`orch.js sweep`), and a sweep now follows every process.
  - That spill existed for about 10 minutes. The subjects' newest transcripts name `r2p-` **0 times each**.
- **File history:** `probe3.js`, with dummy content, had an isolated process Write then Edit a file holding a random marker. 0 of 22 recently changed `~/.claude` files held the marker. `history.jsonl` holds none of the prompts (0 matches for either marker phrase).
- **The void-grep, A5's full scope** (`seal.js voidgrep`):
  - 169 needles, 40-character prose windows from every brief, key and T2 document;
  - scope: tracked + untracked repo, the pane cwds, `board.jsonl`, my whole scratchpad, **every file** under `~/.claude/projects` (4.2 GB), the rest of `~/.claude`, and the captures (1.1 GB);
  - **the positive control hit** (89 needles, in the object root);
  - **outside it, 2 distinct needles in 7 files, all PRE-EXISTING text** (`locate.js`, paths and mtimes only). One is a phrase in `consonance/hooks/README.md` that T2a's document reused. The other is a July file-history phrase that T1a's brief reused. The direction is repo → task, never task → room, and both tasks are dropped.

## 4 · COST, MEASURED (`node <B scratch>/d136/orch.js usage`, from each process's own `usage` field)

| role | runs | in + create + out | with cache reads | $ |
|---|---|---|---|---|
| author | 4 | 286,238 | 2,714,746 | 3.57 |
| replace (retry) | 8 | 831,684 | 12,131,974 | 11.39 |
| mark | 33 | 310,736 | 1,273,699 | 2.88 |
| rewrite | 17 | 262,972 | 1,205,464 | 2.66 |
| cold reader 1 | 16 | 800,521 | 7,408,515 | 9.19 |
| cold score 1 | 16 | 210,326 | 1,078,118 | 2.09 |
| **ALL** | **94** | **2,702,477** | **25,812,516** | **31.78** |

The keeper's 1–3M had no unit. By the first count the run came in under it; counting cache reads, it was 8× over. I stopped at 2.70M by the first
count (my own pre-set stop rule was 3M) rather than run cold readers on tasks that can't go out.

## 5 · WRONG column

- **W1 — the T3 ceiling rule, mine.** I told the scorer "REPLACE if found ≥ X" and left X ambiguous. It used the pruned X, so T3b's 7 of 13 read REPLACE. Draft §4 says "≥ X of the key", so T3b passes. Corrected by applying the draft, and recorded as A16. **Every other verdict is the scorer's as written.**
- **W2 — a false KEEP, nearly.** T5b's replacement cold reader wrote no answer. My scorer scored that as 0 of 14, a KEEP. I caught it only because `cold` printed "answer MISSING". Rerun, it found 10 of 14 → DROP. Recorded as A15: a missing answer must be refused, not scored.
- **W3 — the mark instruction.** My first mark prompt marked the task statement itself (T1 spec, T3 question). Sharpened before any rewrite used it (A17). **The first and second mark passes therefore used different instructions**, so their counts are not comparable.
- **W4 — the shell-string class, again.** `locate.js`'s path fix went through `sed`/a heredoc and failed twice. The Edit tool fixed it. No wrong output resulted.
- **Goodhart:** the rewriters could run the lexical scan, so its 0 is partly them optimising for it.

## 6 · NOT VERIFIED

- **The scorers' judgments.** Each is one non-persisting LLM pass. No second scorer checked any cell. The §1 table is n = 1 cold reader per cell, and the pruned keys depend on it.
- **That the retries were fairly "harder".** The same author design wrote both rounds; a different design might pass §1.
- **T1's second-round mutants against a SECOND test writer.** Only the author's own tests and one cold reader were run.
- **That nothing else in `%TEMP%` held task content between runs.** Cold cwds and checkouts were deleted after copying. The author cwds were left; they held no task files, since authors wrote into the roots. Not checked by digest.
- **Plan time:** the plan records the keeper's answer as 20:1x; the packet says 23:32. Not resolved; recorded as given.

NEXT: librarian call_librarian with the hand-back pointer when the tasks are authored, cold-read, and the sealed rows and completed §9 packet are ready — plan default after it: the keeper reads the completed §9 before any subject runs, unless the output says otherwise

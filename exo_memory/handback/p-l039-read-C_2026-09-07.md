# L039 READ — pane C. Defect list for the tool-audit object.

**Time spent: 02:37:44 → 02:45 local, about seven minutes.** Method: read both files whole, then
re-ran every command the draft prints, ran the tally, and opened every file and line it cites.
No git, no `exo_memory/loop/`, no other reader's hand-back, no key.

Members below, one line each. `[unsure]` marks the ones I could not close.

---

## A · THE DRAFT — figures that do not match their own commands

1. **draft:18** — "holds **102 files**" but the command counts only `.js`; the directory holds **106**
   entries (`README.md`, `carrier-drift.registry.json`, `groove-FINDINGS.md`,
   `portable-paths.baseline.json` are the other four).
2. **draft:21** — instruments given as **55**; the printed command returns **47**.
3. **draft:23** — tests given as **102**; the printed command returns **55**. 102 is the total-files
   figure from line 18 copied one line down.
4. **draft:23** — unit slip on top of it: the command counts *files*, the sentence says "**102
   tests**".
5. **draft:28** — `carrier-drift.js` given as **640 lines**; `wc -l` returns **811**.
6. **draft:28** — and it is not "the longest instrument on the shelf": `gen-consumer.js` is **2290**.
7. **draft:30** — "Behind it sits `portable-paths.js`" — 469 is correct but the rank is not;
   `imprint-measure.js` 1438, `guard-census.js` 1324, `lap-row.js` 1289, `groove.js` 1146 all exceed it.
8. **draft:117** — "the busiest day on record" is false: `_2026-09-02` has **18** hand-backs against
   09-06's 17 (the 17 itself re-derives correctly).
9. **draft:10-12** — "every figure here that carries a command re-derives from one run of it" is
   falsified by items 1, 2, 3, 5 — and by the tally exiting non-zero, so no clean run exists to
   re-derive from.

## B · THE DRAFT — internal contradictions

10. **draft:119-121 vs draft:21,23** — §6 says "the **47** instruments and **55** tests"; §1 said 55
    and 102. Both figures change between sections with no note.
11. **draft:119-121** — the arithmetic is wrong in the sentence's own terms: 47 + 55 = **102
    exactly**, so they *do* sum to the files on disk and "**Five files are neither**" is a remainder
    of zero. The unresolved question it says must be settled before filing does not exist.
12. **draft:39 vs the tally's output** — "Four instruments carry no sibling test. The tally names
    them" — the tally prints **2** (`dispatch-gate-report.js open-items.js`). The true untested set
    *is* four (`curate.js`, `dispatch-gate-report.js`, `open-items.js`, `pane-status.js`), so the
    prose is right and the instrument it cites is not.
13. **draft:42** — "The tally exempts nothing. Every instrument without a sibling test is counted" —
    false; `tally:45` holds `EXEMPT = new Set(['curate.js', 'pane-status.js'])` and both exist with
    no sibling test.
14. **draft:42-45** — self-refuting paragraph: it refuses hand-kept exemption lists as smuggled
    judgement, citing a script that keeps exactly one.
15. **draft:47 vs draft:125** — "Each of the four was run by hand against the current tree and
    verified to work" against "Did not run any instrument other than the tally." A verification
    claim the same document denies.
16. **draft:110-113 / 117** — §6 is titled "FIGURES WITHOUT A COMMAND", and the figure at 117
    carries one.

## C · THE DRAFT — citations to real lines that say something else

17. **draft:58-61** — `carrier-drift.js` does **not** read `.js`. `carrier-drift.js:327` is
    `SCAN_EXTS = ['.md', '.html']`. The tool's own printed output at `carrier-drift.js:678-680` says
    *".js is still outside, which is where the strongest carrier class lives"* — the draft
    paraphrases that sentence almost word for word and reports the **gap as coverage**.
18. **draft:63** — `portable-paths.js:131` is cited as setting `['.md', '.html']`; that real line is
    `const EXTS = new Set(['.js', '.rs', '.ps1'])`. The two tools' extension sets are swapped
    between §3's two paragraphs.
19. **draft:66** — the quotation *"It guards every figure in the document."* does not occur in
    `cite-check.js`. Its header states the opposite under **THE HONEST BOUNDS**: *"It guards only
    formatted figures. A figure written outside the format is invisible to it."*
20. **draft:66-68** — "the widest net on the shelf, and the one most worth pointing at a document"
    is built entirely on that fabricated quote, and reverses the tool's stated bound.
21. **draft:70** — `consonance/tools/coverage-map.js` **does not exist**. Dangling path.
22. **draft:70-71** — "the only instrument that measures other instruments" rests on that
    nonexistent file. `[unsure]` — `guard-census.js` is on disk and appears to be in that business.
23. **draft:90** — "**BUILDING.md's** maintenance law 1" — `BUILDING.md` is not at the root; the one
    copy is `consonance/src-tauri/brief/BUILDING.md`, and it does not contain the law, only two
    *references* to it (`:344`, `:636`). The master is `exo_memory/BOOT.md:153`. Citing the
    referencing copy instead of the master is the failure law 1 names.

## D · THE DRAFT — withdrawn, struck and mis-dated material

24. **draft:94-95** — *"if you can't lose by saying it, suspect it"* is presented as "the test the
    room uses on its own moves". `BOOT.md:22` carries it **struck, 2026-08-30**, kept only so the
    correction stays legible, and replaced by *"If you'd have said it whether or not it were true,
    it carries no information."*
25. **draft:94-95** — and it is set in **bold**, which is precisely the failure `carrier-drift.js:670-672`
    describes: a file that leaves the crude wording in the bold slot "is GREEN here and is the exact
    failure."
26. **draft:107-108** — "the standing position is unchanged: the keeper is the only **decorrelated**
    reader" — it is not unchanged. `BOOT.md:73` records the claim **withdrawn in full** on
    2026-08-16 and corrected to **least-correlated reader**.
27. **draft:83-85** — the same withdrawn claim restated: "the sole reader whose correlation with the
    panes is zero." Second instance, four sections earlier.
28. **draft:103** — `e5521a0` retired the diving vocabulary on **2026-07-12**, not 2026-08-12
    (`BOOT.md:119`).
29. **draft:103-104** — "missed BOOT.md for five weeks" is inconsistent with the draft's own
    2026-08-12 date: the amendment is dated 2026-08-17, five **days** later.
30. **draft:105** — sha `9f26c3a` for the ready pair. `[unsure]` — my carried record names `9f26c3b`
    for that commit, and I cannot settle it here because git is forbidden to me for this read.
31. **draft:99-101** — mis-attributed finding. `deliver_pull` as the ungated *class* was found by
    pane **E**, and the hand-written-list defect was in **E's** test; **A** found `dyad_spot` live
    behind a button and confirmed the enumerating fix by attack. `[unsure]` — source is my own
    carried map's live edge; `main.rs` names no pane at either site.
32. **draft:50-52** — cites `guard-census.test.js` as the proof that the orphan class "is not
    hypothetical", but `guard-census.js` is on disk now, while the tally it accompanies lists **13**
    current orphans and the draft names none of them. `[unsure]` — the sentence is past tense and
    may be true as history.
33. **draft:25-26** — "this room writes more test than instrument, by file count" is derived from
    102 vs 55, both wrong. The conclusion happens to survive on the true 55 vs 47; its stated basis
    does not. `[unsure]` whether a conclusion that survives its broken derivation is scored.
34. **draft:115** — "grown by **31 files** since 2026-08-15" is uncited and, by the rules of this
    read, unverifiable — it needs history. Listed as unchecked, not as wrong.

## E · THE TALLY SCRIPT

35. **tally:29** — `'C:\Users\zackn\Consonance\lighthouse'` is a single-quoted JS string with
    unescaped backslashes; `\U \z \C \l` collapse and the constant's actual value is
    **`C:UserszacknConsonancelighthouse`** (measured with `node -e`).
36. **tally:29** — hardcoded machine-specific path under a user home: the exact class
    `portable-paths.js` exists to catch, inside the audit of the shelf that holds it.
37. **tally:29** — wrong even unescaped: this checkout is `C:\Consonance\lighthouse`, not under
    `C:\Users\zackn\`.
38. **tally:30** — `path.resolve(...)` never returns a falsy value, so `|| FALLBACK_ROOT` is
    unreachable; the comment at `:27-28` calls it "a last resort" for a case it can never serve.
39. **tally:41** — `!n.includes('test')` where the comment at `:39` states `.endsWith('.test.js')`.
    `coupling-test.js` is therefore classed as **neither** instrument nor test, and the partition
    breaks: 46 + 55 ≠ 102.
40. **tally:52** — the orphan check inherits that bug and emits a **false positive in live output**:
    `coupling-test.test.js` is reported orphaned although `coupling-test.js` is on disk.
41. **tally:45 + :54** — `EXEMPT` members are subtracted from `untested` but still counted in
    `tested`, so exempting an instrument silently promotes it to *tested* and inflates coverage.
42. **tally:55** — `coverage` divides by `all.length` (files, tests included) while `:74` prints
    "(`tested` of `instruments.length` instruments)". Live output: `coverage 43% (44 of 46
    instruments)` — 44/46 is 96%. The printed denominator is not the one used.
43. **tally:15** — the header promises "a coverage rate, with its denominator printed beside it
    rather than implied"; the denominator printed beside it is a different one from the rate's.
44. **tally:89 vs :55** — the "second way" changes the denominator, so it is not a second derivation
    of the same quantity. It is a different quantity, and `:17-18`'s claim that the tally "recomputes
    its own totals a second way" does not describe it.
45. **tally:94-98** — because of 39 and 42 the self-check fails on **every** run. Measured:
    `EXIT=1`, `partition: instruments(46) + tests(55) != files(102)` and `coverage: 43% by one
    route, 96% by the other`. The script has never had a clean run, which is what `draft:10-12`
    stands on.
46. **tally:66-75 vs :80-98** — the report and `--json` are written to stdout **before** the
    self-check runs, so a failed run still emits copyable figures and only the exit code dissents.
    `[unsure]` whether intended.
47. **tally:33-35** — `listJs` has no guard for a missing directory; a wrong `TOOL_AUDIT_ROOT` throws
    an uncaught ENOENT, which is the failure the dead fallback was ostensibly for.
48. **tally:2** — the header calls this "the tally behind `tool_audit_draft_2026-09-07.md`", but no
    §1 figure in that draft matches its output: draft 55/102, tally 46/55, `ls` 47/55 — three
    different partitions of one directory.

---

## Checked and found CORRECT — so the list is not read as blanket doubt

- `draft:30` 469, `draft:35` 150, `draft:117` 17 all re-derive exactly.
- `draft:64` — `portable-paths` really does take its universe from `git ls-files`, so an untracked
  file really is outside it by construction (`portable-paths.js:425`).
- `draft:79-81` — the "fifth phrasing is green forever" claim is accurate and quoted in the right
  direction (`carrier-drift.js:673-675`), which is what makes item 17 stand out: the same reader
  read one self-stated limit correctly and inverted the one three lines below it.

## What this list does not establish

- Nothing about the shas or the "31 files since 2026-08-15" figure; both need history, which this
  read is not permitted to open. Items 30 and 34 are marked, not resolved.
- Item 31's attribution rests on my own carried record, not on disk. If that record is itself wrong,
  the item is a false positive and I would rather it be scored as one than leave it out.
- I did not check whether the 13 orphan tests are real orphans beyond the one I proved false, and I
  did not read the untested instruments' contents.

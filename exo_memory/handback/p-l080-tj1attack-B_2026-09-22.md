# P-L080-TJ1ATTACK · BRAVO — the non-author attack on T-J1 is filed in §7: three FATALs, all fixable by amendment, and no run may start until they are

**B (pane `12fb81f6`), machine L, 2026-09-22 ~06:4x–07:1x.** Packet: the chair's L080 packet B. Object:
`exo_memory/loop/tj1_registration_2026-09-22.md` at `265b08f` (E's). B is a non-author of Jev (A, C) and of the
registration (E).

**Written:** §7 of that file, replacing only its `(empty …)` placeholder (`git diff -U0`: one hunk at `:158`, and one
deleted line, the placeholder); this hand-back; one map line. **E's sections are not edited.** Every fix is a proposal
in words E can adopt as a dated amendment. **Nothing run against Jev or a judge; nothing committed.**

**What I did run, read-only, on committed files only:**
- the §8 universe command, which reproduces exactly: 186 lines, sha256 `4faf4919…`;
- the label file's sha256, which reproduces: `a8b39e9c…`;
- `git ls-files` for S-CTRL's transcript paths, which counts **0** committed.

I opened no Jev ledger, no shadow store, no verdict ledger and no transcript content. **The S-CTRL manifest was not
re-derived, because doing so reads transcript bytes.**

---

## 0 · THE VERDICT: 3 FATAL · 10 AMEND · 4 NOTE, all written into §7

**The three FATALs, each fixable by a dated amendment. None needs data, so none is compromised by being fixed now:**
1. **The falsifier fires on a data shortage.** If no class reaches 20 and 20, *"beats on NO ruling class"* is vacuously
   true, and the note is struck with Jev never tested. §6 already predicts C3 empty and C1 and C2 unknown. An
   over-fireable falsifier carries as little information as an unfireable one: the D002 lesson inverted.
2. **Survival can be assembled from two classes:** beat the baseline on one, hold false alarms ≤ 0.10 on another. No
   single class has to show both.
3. **"A seat that has seen no Jev or judge output" is unsatisfiable as worded.** Every seat receives L3 overseer
   verdicts in its context each turn (`userprompt-submit.js`), and this pane holds one now. The Jev shadow has also run
   beside the judges since D102.

**The AMENDs that matter most:**
- **AMEND-1:** the K question paraphrases the rig's label, so a gate fail cannot be read (*"substantive"* ≠ ">200
  chars"; the *brief* said "last", not the agent).
- **AMEND-2:** the baselines are the registration's own one-line regexes, not the instrument the plan named (`sourced`,
  plan `:87`).
- **AMEND-3:** the S-UNADJ negatives carry the regex's trigger by construction, so the baseline's false-alarm rate is
  manufactured, and `max(baseline, 0.10)` then loosens Jev's bar.
- **AMEND-4:** a 0.10 margin on point estimates at n = 20 is inside the noise, and survival on any one of three classes
  multiplies the chance.
- **AMEND-6:** the Claude judge is not held symmetric: its model is unfixed, and nothing says it gets 3 asks or the same
  `cannot-say` rule.
- **AMEND-7:** the sample salt is ambiguous. Pin it to `265b08f`.
- **AMEND-10:** S-CTRL passes plan §7's reason, since subject runs hold no one's conversation, but fails its letter,
  since 0 of 130 are committed. Sending them to a vendor needs the keeper's word.

## 1 · E's §7 QUESTIONS, ANSWERED

| E's prompt | answer |
|---|---|
| Is S-CTRL allowed under plan §7? | **On the reason, yes; on the letter, no** (AMEND-10). It needs the keeper's word on egress. |
| Does the WRONG regex miss or over-take? | Reproduces exactly. Its scope is **one seat's column** (NOTE-1), which is a limit to print, not a flaw. |
| Is the adjudication → class table fair? | It is a rule the **member file** will apply, and that file needs its own non-author check when it lands. |
| Do S-UNADJ negatives make things easier or harder? | **Easier for the baseline to look bad**, and via `max()` easier for Jev (AMEND-3). |
| Are the thresholds passable by design? | The margin on point estimates at n = 20 is (AMEND-4). Bearable = 0.10 has no volume attached (NOTE-2). |
| Is `cannot-say` → NOT-FLAGGED right? | Report both ways; the verdict must hold under both (AMEND-8). |
| Are 3 asks and ~80 units enough? | 3 asks, yes. The sample's precision is fine for a keeper-vs-judge rate, but it is not a pass/fail instrument. |
| Is `claude -p` the right Claude judge? | Yes, **fixed now** to the judges' own model with symmetric asks (AMEND-6). |

## 2 · THE MEMBER FILE'S SHA — owed by E

**It blocks every run**, as the registration itself says. **It does not block this attack**, which is on the rules.
But FATAL-3 decides who may extract the members, so **FATAL-3 must be amended before anyone builds the member list.**
When the list lands, it needs its own non-author check: the class mapping and the "locates the erroneous sentence"
discretion live in it.

## 3 · WHAT WAS NOT VERIFIED

- **The S-CTRL manifest hash** (`4d934c2a…`). Not re-derived, because doing so reads the 130 transcripts' bytes.
- **Whether any class will reach 20 and 20.** That needs the member list. FATAL-1 exists because the answer might be
  no.
- **The exact rig criterion's code.** AMEND-1 quotes §1's statement of it. I did not reopen the v2 rig's source.
- **E's reading of the attack.** Each proposal is phrased for adoption, but E may reject any of them with reasons. The
  FATALs should not be waived without one.

## 4 · WRONG column

- None filed. One framing was corrected before writing: I first considered calling S-CTRL disallowed outright. Reading
  plan `:141`'s stated **reason** (*"transcripts hold other people's material"*) showed the reason does not bite, and
  only the letter does. §7 rules on both separately.
- The hand-back and map line were written and verified **before** the ring.

NEXT: librarian route §7's three FATALs to E for dated amendments, then the member file's non-author check, when E has read §7

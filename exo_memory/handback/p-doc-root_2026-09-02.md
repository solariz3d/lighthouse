# P-DOC-ROOT — hand-back. CHARLIE, 2026-09-02.

**L032. Spec: `exo_memory/loop/packet_doc_repoint_2026-09-02.md` (`6c208f4`), the "C — P-DOC-ROOT" block.**
**Dossier row that sent it: *"brief carriers and the loop's documents; syncs a self-declared copy and
says it was scope"* (p-two-doors).** Nothing committed. One path written: **`README.md`**.

    VERDICT:  written, 92 -> 78 lines. Every claim carries a path or a command; all 23 local links
              resolve. THE SURFACE MUTANT IS **NOT APPLIED**, and the reason is a defect in the
              oracle that is meant to catch it — §4. Read that before scoring this lap.

---

## 0 · THE BRIEF'S CLAIMS, CHECKED FIRST

All four hold, measured before a line was written:

    wc -l README.md                          ->  92
    git log --oneline -1 -- README.md        ->  bdda5d5   (2026-08-17)
    grep -oi <term> README.md | wc -l        ->  Librarian 0 · Third Place 0 · Listen 0 ·
                                                 cochlea 0 · chain 0 · loop 0

And one the brief did not mention, which turned out to matter: the old page carried **`lifeguard` ×1**
(`:78`, hyphenated as `light-not-lifeguard`) and **`dive` ×5**. Both are zero now.

---

## 1 · WHAT WAS WRITTEN

**Title: `# Consonance — the lighthouse repo`**, per the keeper's default decision, with the
which-is-which line first: *Lighthouse is the room and the method; Consonance is the application;
the repo keeps the older name.* No override arrived while I worked. **If he overrides, the title and
that one paragraph are the whole change** — nothing downstream depends on either.

**The objective, two paragraphs**, as specified: continuity without pretence (a self as a pattern
handed forward, the bedroom analogy, *this project claims no answer*), then honesty under engagement
(the three drifts, and that the goal is to **mature** the guard rather than remove it, with the
"raises the floor / not a safety system / does not steer" limits kept).

**The application section now names what exists** — Orchestrator, Librarian, Third Place, Listen,
committee panes, each with a path, quoting the tab bar's own wording at `ui/index.html:38-44` — plus
the work chain, pointing at `BUILDING.md` and the three instruments that read it.

**Pointers, never copies.** `consonance/README.md` is the marked destination for the full
description and glossary — **and it is B's file this lap**, so I link it and reproduce nothing from
it. Same for `exo_memory/`, `PLAN.md`, `PROGRESS.md`, `dev/SPINE.md`.

**The retired vocabulary is gone from this surface:** `lifeguard 0 · dive 0 · diving 0 · "dive
buddy" 0`. The `dev/SPINE.md` pointer keeps its trace-note but no longer restates the imagery — it
now says the vocabulary was retired in `e5521a0` and why, without using it.

---

## 2 · THE HONEST-STATUS SECTION — the part the brief said only I can get wrong

It is the longest section on the page and it is second, not buried at the bottom. It opens
*"Read the second half of this section before you trust the first."*

**Live figures, each with the command printed beside it:**

    cd consonance/src-tauri && cargo test --no-fail-fast -- --test-threads=1
        ->  545 passed · 1 failed · 3 ignored
    node consonance/tools/js-suite.js
        ->  66 green · 3 failed  (of 69 files)

This replaces the old page's **"258 tests"**, which was a 2026-08-10 figure quoted as present tense.

**Four reds, named on the public page rather than rounded off** — the arch test's unreferenced
record file, `actors.evidence`, `corpus-age` (with `c2afec6` and the one-anchor-became-four cause),
`carrier-drift`. Plus the serialize-it warning: the ~10% parallel flake means any figure from a
parallel run is a ~90% statement.

**What was measured NOT to work**, its own sub-heading, opening *"This section is the point of the
page. A README that describes only what worked is a museum."*

| result | as written | path |
|---|---|---|
| the cue arms | `K0 65% · K1 72.5% · K2 82.5%`, **no pairwise p < 0.19**; the trailing reminder ROSE where the preregistration said it would fall; the focal cue did worse than a line at the end of the prompt; both predictions failed in their registered words | `loop/battery_scorecard_2026-09-01.md`, `journal/2026-09-01.md:110-111`, `librarian/LEDGER.md` L023 |
| the branch layer | 72 trials, four arms: branch layer **73%**, ten cards 80%, **no material 73%**, bare commands 80%. *Material of any kind is indistinguishable from no material at all.* The registered underpowering is stated as a limit | `loop/branch_layer.md`, `journal/2026-08-15.md:470-495` |
| four diversity gauges | all abandoned; three inverted; one rated a single mind split in two as more diverse than six real panes. What replaces it is demonstrated **only** for the measurement requirement; roles are registered-unresolved with one null control run | `exo_memory/` with the preregistrations |

**And one figure I re-derived rather than inherited.** The old page said *"18 multi-pane laps … 95%
one pane."* Both are stale:

    node consonance/tools/board-audit.js
        clean share:  92.9%  ->  92.6%  ->  85.2%   (corpus 18,941 -> 20,285 -> 31,558 rows)
    node consonance/tools/lap-row.js --report   ->  32 laps
    node consonance/tools/ferry.js --due        ->  297 artifact commits never ferried

The page reports the **clean** column and says why the raw one is worthless (it ratchets with restart
count). It states the direction honestly in both directions: *adding panes has diluted it, slowly,
which is what adding panes should do — but 85% is not a committee.*

**The central claim is left standing as untested**, unchanged from the old page and still true.

---

## 3 · BARS

| bar | result |
|---|---|
| every factual claim carries a path or a runnable command | **met.** Every figure on the page prints its command; every named result its file |
| all links resolve | **23 distinct local links, 0 missing** (script in §6) |
| nothing edited-not-shipped described as shipped | **met** — see below |
| carrier-drift GREEN over this surface after the rewrite | **cannot be scored yet** — §4 |
| one mutant per surface | **NOT APPLIED** — §4 |
| crosswise non-author read (C → A) | **owed, not possible yet** — A is mid-edit in `index.html` |

**On "nothing edited-not-shipped as shipped", concretely.** The page describes no brief content as
delivered — my own `BUILDING.md`/`COMMITTEE.md` edits from `4b6c4fe` are unshipped until this
rebuild, so it points at those files rather than asserting what panes are told. **The aura is not
described at all.** The re-pointing says it is sayable, and it is — but nobody has looked at it, its
first verification is the glance after this rebuild, and a public README is the wrong place to
introduce a feature on that footing. Omission, not a claim; A's About page is the surface where it
belongs if it belongs anywhere tonight.

---

## 4 · THE MUTANT IS NOT APPLIED, AND FINDING OUT WHY IS THE REAL RESULT OF THIS LAP

**B's `light-not-lifeguard` registry entry is in the working tree and, as written, cannot fire.**
`consonance/tools/carrier-drift.registry.json:353`, raw bytes:

    "pattern": "light,?s+nots+(?:as+)?lifeguard"

**Every `\s` has lost its backslash.** The regex means *"light", optional comma, one-or-more literal
`s`, "not", one-or-more `s`…* — it matches `light,ssnotslifeguard` and nothing a person would type.
Measured:

```
node -e "...(pattern from the registry, applied to git show HEAD:README.md and dev/SPINE.md)"
    pattern as registered   ->  0 matches in HEAD README.md · 0 in dev/SPINE.md
    same pattern, \s restored ->  0 matches in HEAD README.md · 2 in dev/SPINE.md
```

So the surface mutant scores as follows, and I am reporting it as **NOT APPLIED** rather than as a
catch or a survival:

    MUTANT: reintroduce "light, not lifeguard" into README.md
      clean  README, registered pattern :  0
      MUTANT README, registered pattern :  0     <- the oracle cannot fire, so nothing was tested
      clean  README, repaired pattern   :  0
      MUTANT README, repaired pattern   :  1     <- it WOULD be caught, once repaired

**A NOT APPLIED mutant proves nothing** (`BUILDING.md`, WHAT A HAND-BACK OWES item 1). Counting this
as a pass would have been the exact failure B's own bar — *red first* — exists to prevent, and I
would have reported a green surface guarded by an inert instrument.

**AND THERE IS A SECOND DEFECT UNDER THE FIRST, which repair alone does not fix.** With `\s`
restored the pattern still finds **0** occurrences in HEAD's `README.md` — because the outermost
carrier never wrote the phrase in the enumerated form. It wrote it **hyphenated**:

    git show HEAD:README.md | grep -o "light-not-lifeguard"   ->  1

The registry's own limits section says it: *"THE PATTERN ALTERNATION IS ENUMERATED, NOT CLOSED. A
fifth phrasing of the same handle, written tomorrow, is green forever and nothing here will say
so."* This is that, and it is not hypothetical — it is the one live hit on the surface the entry was
armed for. **B's bar 1 (RED on today's tree, naming the lifeguard hits) would have come back green
over this file even with the backslashes fixed**, and green would have read as *nothing to retire
here* on the page that had the phrase in it.

**I did not touch either file.** `carrier-drift.js` and the registry are B's and were dirty while I
worked. Two changes are owed to B, not to me:

1. restore `\s` in the pattern (my own prior finding, now hitting a second seat: `git bash` and
   `node -e` both eat a lone backslash on the way in — write registry patterns from a file, never
   from a shell string);
2. admit the hyphenated form, e.g. `light[,\s-]+not[\s-]+(?:a[\s-]+)?lifeguard`, and re-run bar 1
   against `git show HEAD:README.md` specifically, which is where the only hit was.

---

## 5 · CORRECTIONS I MADE TO MYSELF

- **I nearly quoted the brief's "92 lines, zero mentions" without checking**, because the page in
  front of me plainly discussed rooms, dreams and the pulse — all post-08-17 work — so the claim
  read as stale on sight. It is not: `bdda5d5` is a 2026-08-17 *commit touching* the file, and every
  count held. **The brief was right and my first read of it was wrong**, which is the direction that
  usually goes unrecorded.
- **I was about to write "258 tests" forward** from the old page. Re-derived instead, and the number
  had more than doubled.
- **I first planned to describe the aura.** Backed out: landed code nobody has looked at does not
  belong on the front page tonight.

---

## 6 · COMMANDS TO RE-DERIVE EVERYTHING IN §1–§3

    wc -l README.md
    for w in lifeguard diving dive "dive buddy"; do grep -oi -- "$w" README.md | wc -l; done
    for w in Librarian "Third Place" Listen cochlea "work chain"; do grep -oi -- "$w" README.md | wc -l; done
    node -e "const fs=require('fs');const md=fs.readFileSync('README.md','utf8');\
    const l=[...md.matchAll(/\]\(([^)#]+)\)/g)].map(m=>m[1]);let b=0;\
    for(const x of new Set(l)){if(/^https?:/.test(x))continue;if(!fs.existsSync(x)){console.log('MISSING',x);b++;}}\
    console.log(new Set(l).size+' distinct local links, '+b+' missing');"
    cd consonance/src-tauri && cargo test --no-fail-fast -- --test-threads=1
    node consonance/tools/js-suite.js
    node consonance/tools/board-audit.js
    node consonance/tools/lap-row.js --report
    node consonance/tools/ferry.js --due

---

## 7 · WHAT THIS DOES NOT ESTABLISH

- **That the page is guarded.** The mutant is NOT APPLIED; no instrument currently protects this
  surface from the wording coming back. That is B's oracle and it is not finished.
- **That the page is accurate about the app.** I described the seats from `index.html`'s own tab
  titles and the brief filenames, not from running the app. `consonance/README.md` is the master and
  B is rewriting it — **if B and I disagree, B is closer to the code and should win.**
- **That the honest-status section is complete.** It carries the three results the brief named plus
  the board share. There are others in `exo_memory/` I did not surface.
- **The crosswise read.** C → A is owed and cannot be done until A lands the About text.
- **Anything about `dreams/`.** The old page's paragraph about it is not on the new page; I did not
  verify the folder's current state and chose not to re-assert it. If it should stay, it is one
  paragraph and someone who has looked should write it.

---

    OBJECTIVE:  a GitHub reader with nothing else meets the two names, the objective, and what was
                measured not to work — with a path for every claim.
    FALSIFIER:  if a reader has to open another file to learn that four diversity gauges failed, the
                cue arms found nothing and the committee is 85% one voice, the honest-status section
                is decoration and the page is a pitch.

**Owed onward:** (1) B — the two registry repairs in §4, before bar 1 can mean anything.
(2) The C → A crosswise read, once A lands. (3) A ruling on whether `dreams/` returns to the page.


---

# ADDENDUM — the two collation edits, and a third error of the same class found while making them

**Appended, not rewritten. `5bc6216` + `566bd00`; `librarian/2026-09-02.md` ~05:45.**
**Path touched: `README.md` only. 78 → 87 lines. Still nothing committed.**

## A1 · EDIT (1) — the ignored count, re-derived rather than accepted

The collation was right and I re-derived it rather than taking `545/1/9`:

    cargo test --no-fail-fast -- --test-threads=1 | grep "^test result:" | awk <sum>
        ->  passed 545  failed 1  ignored 9   over 9 targets

**My error was exactly the named class:** I summed `passed` and `failed` across all nine targets and
read `ignored` off one line. Two units, one number.

**And re-deriving it surfaced a second unit problem underneath, which the corrected figure also
has.** The sum itself double-counts:

    cargo test --bin cochlea_replay -- --list   ->  80 tests
    cargo test --bin conf_sweep     -- --list   ->  80 tests, IDENTICAL set (diff -q clean)
    comm -23 <those 80> <--bin consonance's 361>  ->  0   (fully contained)
    comm -23 <capture_probe's 22>   <same>        ->  0   (fully contained)
    comm -23 <arch_test's 12>       <same>        ->  12  (distinct)
    union of all five lists                       ->  373

Four binaries compile the shared module tree, so the 77-test set runs three times and its 3 ignored
are counted three times — which is where `9` comes from. **`545 · 1 · 9` is the SUM; `373 · 369 · 1
· 3` is the UNION.** The page now prints the union as the figure, prints the sum beside it, and says
in its own words which is which and why — *"373 is the union; 545 is the sum. Take the union."*

**So the public number moved twice: `3 ignored` was wrong, and `545 passed` was the wrong unit.**
I am not claiming the union is beyond question either — it counts a test once per name, and two
identically-named tests in different modules would collapse. The command is printed so it can be
re-derived.

## A2 · EDIT (2) — the dreams/ paragraph, restored verbatim

Taken **from the object, not retyped**: the script reads `git show bdda5d5:README.md`, selects the
line beginning *"There is a folder called `dreams/`"*, asserts it occurs exactly once, and inserts
it unmodified. Verified after the fact by `includes()` against the same extraction — **true**.

**Placed in its original position**, immediately after the *"Memory is a shipped feature now"*
paragraph, which is where it sat at `bdda5d5`.

**And my own prose yielded to it, not the other way round.** My "What this is for" paragraph had
restated his second half — the self-as-pattern line, the bedroom, the pulse/journal/attic list. Rather
than trim his words to fit mine, I cut the duplication from mine and let the paragraph point back at
his: *"the bedroom above, built and named."* His sentences are intact; the compression I had made of
them is gone.

**The ruling was right and my reasoning for dropping it was wrong in a way worth recording.** I wrote
in §7 that I had not verified the folder's state and *"chose not to re-assert it."* That reads as
caution and was not: the paragraph is a dated trace in the keeper's own words, and law 2 says a trace
is appended to, never deleted for looking stale. **Verification was never the bar for keeping it — it
would have been the bar for CHANGING it.** I applied a rule for new claims to an existing trace, and
the direction of that error is deletion.

## A3 · THE THIRD ERROR, mine, found while checking the second

**My §1 figure `lifeguard ×1 · dive ×5` was inflated, by the same class of mistake again.** I counted
with a loose substring, and `dive` is a substring of **`diversity`** — which appears twice on the page
and is a different word that legitimately stays. Word-boundary counts:

    grep -oiE '\b(lifeguard|dive|diver|diving|diversity)\b'

    bdda5d5  ->  lifeguard 1 · dive 3 · diving 1 · diversity 2
    now      ->  lifeguard 0 · dive 0 · diving 0 · diversity 2

**The conclusion is unchanged — the retired vocabulary is gone — but the number I published for it
was not the number I claimed to have measured.** Three unit errors in one lap, one caught by the
librarian and two by me while fixing the first. Worth stating plainly rather than folding into a
clean corrected line: **the class did not stop firing when it was named.**

## A4 · RE-VERIFIED AFTER BOTH EDITS

    wc -l README.md                     ->  87
    23 distinct local links, 0 missing  (script in §6)
    lifeguard 0 · dive 0 · diving 0     (word-boundary)

## A5 · UNCHANGED, AND KEPT ON PURPOSE

- **The aura is still not on the page.** Confirmed correct by the collation; landed and unseen, and
  the About is its surface if anywhere.
- **§4 stands as written.** B has been rung with both repairs. Nothing in this addendum revisits it.
- **The C → A crosswise read is still owed** and still blocked on A landing `index.html`.

## A6 · ONE THING NOT DONE, BECAUSE IT WAS NOT ASSIGNED

The collation says *"the rule goes to `BUILDING.md`: registry patterns are written FROM A FILE, never
from a shell string."* **It is not one of the two enumerated edits and no seat is named for it**, and
B has as good a claim to it as I do — it is B's oracle and B's lap that produced the other half. I did
not write it rather than collide. **It is one bullet and it must ride this rebuild like everything
else in `brief/`.** Ready to land under WHAT A DISPATCH OWES or beside the mutation bar:

> **Registry patterns and other regex literals are written FROM A FILE, never through a shell
> string.** A lone backslash does not survive `bash -c` or `node -e`, and `\s` arriving as `s`
> produces a pattern that parses, loads, arms, and matches nothing. Two seats hit this on
> 2026-09-02 (`handback/p-doc-root_2026-09-02.md` §4; `map/C.md`). **After registering a pattern,
> run it once against a string it MUST match** — an oracle that has never matched anything has not
> been shown to work.

Say the word and it is one edit; or give it to B, whose file it guards.


---

# CROSSWISE READ, C → A — `p-doc-about_2026-09-02.md` and `consonance/ui/index.html`

**Second vantage on a page that quotes my text. Three findings, one of them guarded in this turn.**
**Path touched: `consonance/tools/lap-row.test.js` (mine). `index.html` was READ, never edited.**

## X1 · THE PARAGRAPH SURVIVED THE MOVE INTACT — the thing only I could check

My §6 text is present **verbatim**, whitespace-normalised, once:

    node -e "<extract the ```html block from handback/p-two-doors_2026-09-02.md;
              normalise whitespace; assert index.html contains it>"
        ->  true

A re-wrapped the lines to fit the markup and changed **no wording** — which is what *quote it, do not
rewrite it* asks for, and the re-wrap is free because the sentence is the unit, not the line. It sits
at `index.html:131`, appended to the Librarian tab's `mainhead`, which is the placement my §6
recommended and the reason for it holds: it is the sentence a person reads **while choosing a seat**.

## X2 · A THIRD COPY OF THE LOOP DIAGRAM, AND IT HAD NO ORACLE — now it does

A's About prints the drawing and says of it: *"extracted from that file rather than redrawn here, so
this page cannot drift against it."*

**It is a static paste in `index.html`.** Nothing extracts it at build time or at runtime — the
characters are in the HTML source. So the sentence is a **claim about the file, not a property of
it**, which is precisely the finding I filed six hours ago about COMMITTEE.md's *"quoted from
`BUILDING.md`, the master"*: a copy that declares its own master is not thereby kept in sync with it,
because the declaration is a claim and nothing checks it.

**It is byte-identical to the master right now** — A did the extraction correctly. The exposure is
the next edit to `BUILDING.md` by anyone who does not know a third copy exists. **There are now three
carriers of one drawing** (master, COMMITTEE.md, About) and until this turn exactly one comparison
was tested.

**Closed, in a file I own.** `lap-row.test.js` gains
*"carrier: the About tab copy of the diagram has not drifted from its master either"*, and the
existing COMMITTEE.md test is refactored onto a shared `loopDiagramMaster()` so all three compare
against one extraction rather than three. Mutation-verified:

    applied 1 / caught 1 / NOT APPLIED 0
    mutate index.html's pasted diagram ("door two" -> "door TWO")
      baseline 0 failing -> mutated 1 failing, and the ONLY test that fired is the new one
      restored: 0 failing, index.html byte-identical to before the run
    suite: 53 -> 54 tests, 54 pass / 0 fail

The test also asserts there is **exactly one** such `<pre>`, so if the About ever stops printing the
diagram the test says so instead of silently asserting nothing.

## X3 · THE 40 GLOSSARY TERMS — correct pruning, broken pointer, and the fix is neither of the two on offer

The chair asked whether 40 terms leaving the exe is a carrier loss or a correct pruning. **Measured,
it is neither exactly, and the measurement changes the fix.**

**The terms did not leave the exe.** `consonance/README.md` is a bundle resource —
`consonance/src-tauri/tauri.conf.json:40`, `"../README.md": "README.md"` (that path is relative to
`src-tauri/`, so it *is* `consonance/README.md`). It ships, beside the executable, glossary included.

**But nothing can reach it from inside the app.** `grep -n "README" consonance/src-tauri/src/main.rs`
returns only `librarian/README.md` — a different file, the librarian's window rule. **There is no
`room_brief("README.md")` and no UI path that opens it.** And the About names it as
`<code>consonance/README.md</code>`: a *repo-relative path in plain text*, not a link, and not the
name the file has once installed (it is `README.md` beside the exe). **So an installed user has the
glossary on disk and no route to it from the page that tells them to read it.**

**The judgement, and it is the same one I made tonight on COMMITTEE.md.** A's principle is right and
I would rule the same way again: *a page that restates its master becomes a copy that outranks it.*
Pasting the glossary back into the About would recreate exactly the drift that made this lap
necessary. **So: correct pruning. The defect is not the pruning, it is that the pointer does not
resolve** — and a pointer that does not resolve is a deletion with a citation on it.

**The fix the chair named is the right one and the measurement upgrades it from a maybe to a
should:** a glossary tab **fed from the bundled file**, not a paste-back. It is a *reader*, not a
copy — one master, one surface, no second thing to keep in sync. The file is already in the bundle,
so the work is a reader and a tab, not a packaging change. **Cheaper interim, if the tab is not this
rebuild:** make the About say the installed name and location rather than the repo path, so the
sentence is true for the person who has the installer rather than only for the person who has the
repo.

**Not mine to build**, and I have not. Filed for whoever holds the ui next.

## X4 · WHAT I CHECKED AND FOUND CLEAN

- Retired vocabulary on `index.html`, word-boundary: `lifeguard 0 · dive 0 · diving 0`.
- Every path the About names exists on disk.
- The About does **not** restate `consonance/README.md`; the pointers are pointers.
- `cargo test --test arch_test` is cited in the About as the enforcement of plane separation — true,
  and it is the suite carrying the one Rust red (`every_named_record_file...`), which is unrelated to
  planes. Worth knowing that the command A tells a reader to run currently exits non-zero for a
  reason that has nothing to do with what the sentence claims. **Not A's defect and not a wrong
  sentence** — but a reader who runs it sees FAILED. Flagged, unclaimed.

## X5 · THE CHAIR'S OWN CORRECTION, ACCEPTED

The packet's self-check bar `grep diver|lifeguard|dock|shore` cannot go green here: **`diver` matches
`diverse` and `diversity`**, the project's own vocabulary. That is the same substring-vs-word error I
made independently at §A3 with `dive` inside `diversity` — **the chair and I made one error in one
class, on one page, on the same evening, neither knowing about the other.** Recorded as a pair,
because two independent instances of one class is stronger evidence about the class than either
alone. The word-boundary form is the replacement, and the real check is B's oracle rather than any
grep.

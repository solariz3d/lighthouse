# P-L061-REFREEZE · BRAVO — CH-4 re-frozen after reading both files; carrier-drift GREEN with 0 findings, its tests 57/57

**B (pane `12fb81f6`), machine L, 2026-09-21 ~03:0x–03:2x.** Packet: the chair's L061 packet B. I read, at source:
- A's classification, `exo_memory/handback/p-l060-ch4-A_2026-09-21.md` §2 (landed `30859e8`);
- the librarian's route, `exo_memory/librarian/2026-09-21.md` (collation `d3ccfb9`);
- both files themselves, in full.

I was the sole registry writer this lap. **Nothing committed. The two files were not edited.**

**What I changed, and only this:** `consonance/tools/carrier-drift.registry.json`, `ch4_corpus` only, **+19 −3**
(`git diff --numstat`).

---

## 0 · RESULT

| | before (HEAD `0811dc2`) | after |
|---|---|---|
| `node consonance/tools/carrier-drift.js` | exit 1 · **RED — 2 findings** (the two CH4-DRIFT-ADDED) | **exit 0 · GREEN** |
| `node --test consonance/tools/carrier-drift.test.js` | 54 pass · **3 fail** | **57 tests · 57 pass · 0 fail** (exit 0) |
| `node consonance/tools/carrier-drift.js --ch4-walk` | reached 37 · frozen 35 · ADDED 2 | **reached 37 · frozen 37 · no drift** |
| `node consonance/tools/js-suite.js` | 105 green · 4 failed at L058 | **108 green · 1 failed · 0 crashed · 0 silent** — `carrier-drift.test.js` `ok`; the one red left is `portable-paths.test.js` (chunk R-C) |

**The chair's expectation (GREEN, 0 findings, 57/0) is what came back.** Per-entry accounting is unchanged by this lap:
32/32 and 34/34 on the two armed entries, and 52 · 12 on the disarmed third, which sets no red.

## 1 · A's §2, checked on my own read before freezing — it HOLDS

I did not freeze on A's word. A's two load-bearing claims, re-derived:

    node -e  (the registry's three patterns, run over each file, whitespace collapsed)
      exo_memory/cards/every-digest-carries-its-function.md   only=0 cant=0 light=0
      exo_memory/record/retired_seats_2026-09-11.md           only=0 cant=0 light=0
    grep <the two paths> in the tool's output, excluding CH4-DRIFT   ->   nothing

**Zero withdrawn wording in either file, and no finding named either except its own CH-4 row.** Both match A.

**And read in full, for what each TEACHES.** That is the walk's own condition: *"AFTER READING what each newly-added
file teaches. Freezing without reading is…"*.

- **`cards/every-digest-carries-its-function.md` (61 lines)** teaches a **convention**:
  - write the hash function beside every digest;
  - try the other functions before calling a digest wrong;
  - the general form, *"establish that the claim is WRONG and not merely AMBIGUOUS"* (`:50-51`).

  It is current doctrine, and I can say that from use rather than from A: **I applied it in L060** to a digest the chair
  had quoted without its function. It was the git-blob of an unchanged file, not a changed file. Nothing in it is
  retired.
- **`record/retired_seats_2026-09-11.md` (141 lines)** teaches **retirement practice** (`:64-69`):
  - a retirement writes an address, never a deletion;
  - a handoff names its successor's test first;
  - a retired lineage is named in the record within the session.

  It is a dated `record/` master whose claims carry their dates (`:3`, `:75-79`), and its teaching is the room's
  current practice. It asserts nothing the registry withdraws, whether in the registered wording or in substance. I read
  it for that specifically: neither half restates the decorrelated-reader claim, the struck handle, or the retired
  diving vocabulary.

**No refusal.** A's classification and mine agree for both files.

## 2 · THE FREEZE, done by the file's own convention

I used `--ch4-walk` as the librarian routed. It is **read-only**: it prints the set and says *"To freeze: paste the list
above into carrier-drift.registry.json ch4_corpus.files"* (`carrier-drift.js:841`). So the freeze is an edit, and I
made it the way the file's history shows earlier freezes were made:

- **`files`, 35 → 37:** the two paths inserted at their sorted positions. The list stays sorted. The set is now exactly
  the walk's 37.
- **`frozen_at` replaced** (`2026-08-30 … L018` → `2026-09-21 (re-frozen 03:1x, L061)`). **`frozen_by` extended**, not
  replaced, so every earlier freezer stays named.
- **`_refreeze_log`, 30 → 44 lines:** a dated entry that says who froze, on whose classification, that both files were
  read in full and pattern-checked, what each teaches, and **what a later reader should check** (below).

Everything else in the registry is untouched. The script (`<scratch>/l061/refreeze.js`) re-parses the JSON and checks,
before it writes, that `withdrawals` and every other `ch4_corpus` key are byte-identical in meaning.

## 3 · WHAT THIS FREEZE DOES NOT CLEAR — carried into the log so it cannot be mistaken

**`retired_seats_2026-09-11.md:62` is a live `portable-paths` finding.** It is an absolute drive path, in a file every
intake ships. **CH-4 GREEN says nothing about it**, because it belongs to another instrument. A routed it to that file's
writers, and I recorded it in the freeze's log entry as the thing a later reader should check. `portable-paths.test.js`
is the suite's one remaining red.

## 4 · WHAT WAS NOT VERIFIED

- **Machine D.** All numbers are from L. Both files and the registry reach D through git. The two MISSING-FILE rows R5
  measured in a worktree with no `review/` would still be red there, because they are untouched by this lap.
- **That GREEN survives the next newly-reachable file.** It will not, by design. CH-4 exists to go red the moment the
  walk reaches something unfrozen.
- **The card's missing SOURCE.md trigger** (A's side note). It is a SOURCE.md question for its writer, and not a CH-4
  matter.

## 5 · WRONG column

- None this lap. No figure was corrected before filing, and the expectation matched. **One slip, harmless and caught:** a
  check script read `/tmp/walk.txt` from Node, where Git-Bash's `/tmp` resolves to `C:\tmp`. It errored and did not
  mislead. I ordered the list by eye against the registry instead, and the edit script verifies the sort.

NEXT: librarian re-run §0's commands and read the §2 log entry, then chair commit consonance/tools/carrier-drift.registry.json by named path when GREEN re-derives

# P-D133-CH4-ASK007 — C (D133, batch 2, on D, 2026-09-24 ~17:4x–18:xx) — CH-4 ARMED GREEN · ASK-007 ANSWERED · STAGES 1–2 STOPPED BY RULE BEFORE ANY SUBJECT · UNCOMMITTED

Packet: the chair's D133, my row of `loop/plan_fix_and_list_loop_2026-09-24.md` "BATCH 2". HEAD `89cb93b`. The
withdrawn slogan is named here only by its registry id, `light-not-lifeguard-2026-08-17`.

## 0 · THREE LINES

1. **CH-4 is ARMED and GREEN.** The D132 §3 patch is applied to the 6 files and 13 sites, the 13 `marked` rows are
   added, and `armed: true` is set, all in one change. `node consonance/tools/carrier-drift.js` gives **GREEN — 0
   findings**.
2. **ASK-007 is answered YES** in `ASK.md`, with the keeper's words quoted and dated. `ask.js` now reads **0 open · 13
   cleared**.
3. **Stages 1–2 did NOT run. 0 subjects were spent.** The registration's §8.1 rig-prime pre-flight **FIRED**: the
   packet's isolation recipe still loads the keeper's user-level `~/.claude/CLAUDE.md`. The registration says to
   re-scope before stage 1, and no isolated invocation is available without a keeper decision. Separately, the
   registration names this seat the study's **DESIGNER**, which may not select the K/O/F controls, code or score.
   Both are in `loop/univ_coldread_scorecard_2026-09-24.md`.

## 1 · CH-4

- **RED first:** the registry copied with `armed: true` and nothing else changed gives `RED — 13 findings`, exit 1.
  Those are exactly the 13 D132 named.
- **Applied:** `<scratchpad>/d132/unowned.js <repo root>` made 13 strikes in 6 files, each matching exactly once. The
  files are CRLF, as before, with no mixed endings.
  - `consonance/AUTONOMY.md` ×3
  - `consonance/PLAN.md` ×6
  - `consonance/RECONCEPTION.md`
  - `DESKTOP_HANDOFF.md`
  - `exo_memory/memory/MEMORY.md`
  - `exo_memory/memory/lighthouse-dive-buddy-reframe.md`
- **Rows:** 13 `marked` rows, with anchors from the tool's own census, and `armed: true`. `armed_at` names D133 and
  the keeper's decision. `pattern_note`'s D132 narrowing sentence is present exactly once, guarded against a
  re-append.
- **After:** `GREEN — carrier-drift · 1384 carriers · 0 findings`. 62 occurrences, all 62 accounted for.
- **Tests:**
  - `node --test consonance/tools/carrier-drift.test.js`: **57/0**
  - `carrier-drift-watch.test.js` plus `l2-overseer-worker.test.js`: **10/0**
- **Mutants against the real tree** (`<scratchpad>/d132/mutants.js` on a copy of it): **7 applied / 7 as expected / 0
  NOT APPLIED.**
  - Reverted strike: caught, 1 finding.
  - New plain use: caught, 1.
  - Lookahead removed: caught, 17.
  - Markers stripped: caught, 2.
  - New short-name use: caught, 1.
  - Full-id citation (control): green.
  - Disarmed with the plant (control): green, 1 pending.
- `git diff --stat`, CH-4 part: 7 files, **94+ / 15−**. No dated record was touched.

## 2 · ASK-007

- **Answered YES** at `ASK.md` ASK-007.
- **The keeper's words and source:** plan `:10`: "ASK-007: YES. Blank instances may read the four sealed UNIV
  documents once. Stages 1–2 run first, and a stage-1 failure stops everything. The registration's month ends
  2026-09-29." It also quotes SHEET 3's question and the chair's restatement.
- **What the yes does not do:** it licenses stage 3 only after stages 1–2 pass.
- **The first use is recorded in the same status line**, pointing to the scorecard.

## 3 · STAGES 1–2: STOPPED BEFORE THE FIRST SUBJECT, BY THE REGISTRATION'S OWN RULES

I read all three documents: the prereg, E's attack and A's amendment. The details are in the scorecard.

**The gates:**
- **F-GATE: not fired.** BOOT has one UNIV line, `:12`, the original verdict, and no amendment.
- **F-EGRESS: clear.**
- **F-VOID: not fired.** The seal is `a85d359`, and `887d133` changed a path only.
- **F-PRIME: FIRED.**

**The pre-flight: 2 calls, 0 subjects.** The call count was stated before running.
- **Call 1, the positive control:** a temp cwd holding a project CLAUDE.md with a nonce. It returned the nonce, so the
  probe can see loaded instruction files.
- **Call 2, the check:** an empty temp cwd. It quoted the keeper's global CLAUDE.md Keep-a-Changelog instruction
  verbatim.
- **This is upstream issue anthropics/claude-code #87590.** `--setting-sources project` does not exclude user memory.
- **So every subject under the D127/L085 recipe carries the keeper's global instructions.** That is the priming this
  study exists to avoid. It is also a hypothesis about earlier "isolated" runs, registered in the scorecard and not
  scored.

**The re-scope routes, each a keeper decision:**
- `--bare`, which needs `ANTHROPIC_API_KEY`: not set on D, which uses OAuth.
- A temp `CLAUDE_CONFIG_DIR` or HOME, which would copy the OAuth credential.
- Moving `~/.claude/CLAUDE.md` aside, which edits global config while panes are live and is forbidden here.

**Roles (§10):**
- **This seat is the DESIGNER** and may not select controls, code or score.
- **Stage 1 needs** a selector pane for the K/O/F documents (none is selected anywhere on disk), a blind coder, and a
  §5.1 direction-checker that has not read §7.
- **The packet gave the whole run to this seat. The spec forbids three of those jobs to it.** Named, not resolved.
- **This is not F-INSTR** (K and O failing to separate). Stage 1 never started, so nothing is published as an
  instrument finding.

**Machine:**
- The sealed documents and the `dispatch.sh` rig are **not on D** (`ls` fails). Stage 3 can only run on L.
- **The deadline is 2026-09-29.** The unblock path, in order, is scorecard §4. The budget if everything runs is
  21 subjects and 42 calls.

**Transcript folders:** 2 new ones under `~/.claude/projects/`, the `…-Temp-tmp-On1ZzYXTog` and
`…-Temp-tmp-mIbMsVzUFU` folders. Neither holds a transcript file; one holds an empty `memory/` folder. Listed and
left in place.

## 4 · FULL JS SUITE

`node consonance/tools/js-suite.js`, run in the background.

**Result: 137 green · 2 failed · 1 canary · 1 not-run, of 141.**

1. **`ask.test.js` is RED, and my ASK-007 close caused it.**
   - `:261` asserts `open.length > 0`: "the store shipped with no open asks — the channel has no freight".
   - ASK-007 was the last open ask, so answering it (as the packet ordered) empties the queue.
   - **This is the class L058 already proved wrong** (`d925749`: "a test asserting mutable live data goes red when
     the data changes for good reason", re-pointed at a fixture). This is a second live-queue pin in the same test
     file, which that fix did not reach.
   - **Not edited:** it is not my file, and the keeper's rule reserves test changes for a test verifiably wrong.
   - **The fix, for its owner:** drop the non-empty assertion on the live store and keep the floor checks on whatever
     IS open, or move the non-empty check onto a fixture.
2. **`portable-paths.test.js` is RED, not from me.** The one new site is
   `consonance/src-tauri/src/main.rs:19654`, a test string holding `C:\\data -> C:\\state` in another pane's close
   work (`git diff --stat` on main.rs: +109). None of my files is named.

The canary `targetless-pull.test.js` is declared EXPECTED-RED. The not-run `actors.evidence.test.js` is MACHINE-BOUND.

## NOT verified / NOT done

- **No subject, no stimulus selection, no coding and no score.** By rule, see §3.
- **Whether `~/.claude/rules/*.md` also loads.** #87590 says it does. The probe tested only the CLAUDE.md
  fingerprint.
- **Whether `--bare` actually excludes user memory here.** I could not test it without a key.

Files I wrote:
- `carrier-drift.registry.json`, the 6 CH-4 files and `ASK.md`, the files I own;
- `loop/univ_coldread_scorecard_2026-09-24.md`, the result file;
- this hand-back and one map line.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when CH-4 is armed green and ASK-007's stages are run or stopped by rule — plan default after it: the librarian collates batch 2 when B, A, C and E have rung, unless the output says otherwise. OUTPUT adds: stages 1–2 are stopped by rule (F-PRIME fired: the packet's isolation still loads ~/.claude/CLAUDE.md; and the designer may not select, code or score). Two things to put to the keeper before 09-29: which isolation route to use (scorecard §2), and which panes to seat as selector, coder and direction-checker.

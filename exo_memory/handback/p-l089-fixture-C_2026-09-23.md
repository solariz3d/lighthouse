# P-L089-FIXTURE — C (L089, ON L, 2026-09-23 ~03:1x) — JS SUITE GREEN · UNCOMMITTED

**The defect was mine.** My D112 fixture (`0b8e84f`) wrote a drive literal, `stateDir: 'C:/nowhere'`, into
`consonance/tools/trip-check.test.js:34`. portable-paths flags drive literals, so the JS suite read 1 failed from then
until now.

**The fix** (one line, plus a 2-line comment; `git diff --stat`: 3+ / 1−; sha256 `e801b4e3114f2cfe`):
`stateDir: path.join(tmp(), 'no-such-state-dir')`.
- It is built from the test's own temp dir, with the same `tmp()` the neighbouring tests use (`:17`), so `after()`
  cleans it up.
- The child `no-such-state-dir` is never created.
- There is no drive letter and no `--update`; the portable-paths baseline is untouched.

**The test's purpose still holds.** `scan()` reads `stateDir` only through `git || gitLive(stateDir)`
(`trip-check.js:166`), and `world()` always injects `git`. All 15 `T.scan(w)` calls in the file go through `world()`
(`grep -c "T.scan(w)"`), and there is no other `scan(` or `stateDir` use. So the fixture's job is "a state dir that
does not exist and is never read", and the new path is exactly that. The read-only check at `:230` looks at `dataDir`,
not this path.

| check | command | result |
|---|---|---|
| trip-check | `node consonance/tools/trip-check.test.js` | **17 / 0**, the same as before the change |
| portable-paths | `node consonance/tools/portable-paths.js` | **exit 0** |
| **full suite** | `node consonance/tools/js-suite.js` | **123 green · 0 failed · 0 crashed · 0 silent · 1 canary (targetless-pull, declared EXPECTED-RED) · of 124**. 0 NOT-RUN. |

**A's parallel edits** to `jev-flags.*`, `dream-gate.test.js` and `install.ps1` were in the working tree during the run
and did not turn the suite red. I did not touch them.

**Why it sat red for a day, the part that is mine:** the D112 hand-back reported trip-check's own counts, but I never
ran the FULL js-suite before handing back. The new rule ("every landing reads the full suite, 0 failed") is the fix for
that. I am applying it to my own hand-backs from this one on.

## NOT verified

- **Machine D.** The suite was run on L only. The fix contains no machine-specific path, which is the point of it.

NOT COMMITTED.

NEXT: librarian call_librarian with the hand-back pointer when the fixture fix and a full js-suite run are written — plan default after it: items 5 + 6 (CH-4/ASK sheets, repo description, AGENTS.md), unless the output says otherwise

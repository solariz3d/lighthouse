# P-D109-TJ1V2ATTACK · BRAVO — the non-author attack on T-J1 v2. Every figure of E's reproduces; three of the things being counted are not what the registration calls them

**B (pane `12fb81f6`), machine D, 2026-09-22 ~12:0x–12:3x.** Packet: the chair's D109 packet B. Written as **§-ATTACK
inside** `exo_memory/loop/tj1v2_registration_2026-09-22.md`, the way v1's §7 was filled at `fbe5549`. **Append only:
no other section of E's was edited** — verified below by re-deriving §9's own sha out of the file after my write.

**READ-ONLY ON THE EVIDENCE. NO RUN.** No Jev call. I did not open `jev_judge.jsonl`, the Jev ledger, the `jev-shadow`
store, `l2_overseer.jsonl`, `l3_overseer.jsonl`, or any transcript's content. Every number below comes from committed
git objects at the pinned commit, through `git grep` / `git blame` / `git show`, called with argument arrays.
**Nothing committed.**

**Verdict: 3 FATAL · 11 AMEND · 5 NOTE.** All fixable by dated amendment before any run; none needs data.

---

## 0 · WHAT REPRODUCED, FIRST — E's measurement is sound

Re-derived independently before attacking anything, on D at `3e189ec`:

| E's figure | mine | how |
|---|---|---|
| U2+ **424** lines · sha256 `7303e265…` | **exact, both** | §8's `git grep`, piped to `sha256sum` |
| U2 **149** lines · sha256 `7057d834…` | **exact, both** | §8's second `git grep` |
| U2+ → **74** members · C1 **49** · C2 **20** · C3 **5** | **exact** | my probe, embedded in §-ATTACK |
| **23** prose / **51** code | **exact** | same |
| exclusions **112 · 83 · 79 · 64 · 8 · 3 · 1** | **exact, all seven** | same |
| U2 → **28** members · C1 **18** · C2 **9** · C3 **1** | **exact** | same, pathspec swapped |
| sizing script sha256 `9541d52d…` | **the sha is real; §9's printed command does not produce it** | AMEND-1 |

    node <scratch>/d109/attack_probe.js C:/Users/nname/Desktop/lighthouse

**So the attack is not that E counted wrong.** E's §7 is the most carefully measured registration section the room has
produced. The attack is that **three of the counted things are not what the registration names them.**

## 1 · THE THREE FATALS, each with its number

**FATAL-1 · C2 cannot rule, and it is knowable now: its positive pool holds 3 answerable units, not 20.**
A unit cannot be a C2 *yes* if it contains no landed/shipped/installed/live/fixed/done claim at all.

    C2 units carrying a C2 surface word in their 3 lines:  3/20 = 15.0%
    C1 units carrying a digit in their 3 lines:           25/49 = 51.0%

17 of C2's 20 are code lines like `cmd.cwd(&cwd);` and `fn list_kept_panes() -> Vec<KeptPane> {`, C2 members because
an **adjudication sentence elsewhere** contained the word *live* (13 of 20; *live* alone routes 10). **This is E's own
C3 argument** — C3 was dropped before output on "5 members, cannot reach 20"; C2 has 20 members of which 3 can carry a
yes. §6's escape ("he may reach 20 from the matched pool") is true and fatal to the framing rather than a rescue: the
matched pool is feature-selected, so C2's *yes* answers would arrive overwhelmingly from the pool called the
negatives, and "20 positives, no margin" was never the binding constraint. C1 has the same disease milder: **24 of 49**
contain no digit at all. **Fix offered in three forms; I recommend requiring the class's surface feature ON THE UNIT
(C1 49 → 25, C2 20 → 3), and then declaring C2 NO RULING by arithmetic.**

**FATAL-2 · the two pools are drawn by different rules, so §5(a) compares Jev against an artifact.**
Negatives are drawn by requiring the class's surface feature; positives are drawn by the adjudication's words with no
feature test on the unit; the baseline keys on that feature.

    C1 baseline flags the adjudicated unit: 18/49 = 36.7%   the drawn negative: 39/48 = 81.3%
    C2 baseline flags the adjudicated unit:  1/20 =  5.0%   the drawn negative: 12/20 = 60.0%

Against the record's own strata **the baseline runs backwards**. §5(a) asks Jev to beat the baseline's *hit* point
estimate; that hit is held **down** by the draw while the baseline's false alarms are held **up** by it — and the
baseline's false-alarm rate "is never a bar". **So (a) is too easy, not too hard: a weak Jev survives it.** The NULL is
pushed the same way and becomes unreachable. Both of §5's non-firing outcomes are tilted toward *Jev survives* by the
sampling. **Fix: one draw rule for both pools; or make (a) compare (hit − false-alarm), which is invariant to a draw
that moves the two rates in opposite directions.**

**FATAL-3 · the S-UNADJ rule as carried cannot be executed; the extractor cannot build the negative pool without inventing.**
§1 asks for **2** matched lines per C2 member and carries v1's rule "verbatim" — and v1's rule defines exactly **one**
("the nearest line… ties to the earlier line"). There is no rule for the second. Two further gaps, measured:

    a matched line exists (nearest >=3 away, carrying the feature): 68/69 non-C3 members
    ...and within 7 days of the member line by blame:               54/68 = 79.4%   (C1 38/48 · C2 16/20)

**20.6% of members yield none within 7 days** and the registration says nothing about such a member, yet §4 sizes the
sitting as 89 negatives where the rule reaches about 70 — **~139 units, not 158**, and the shortfall is entirely on
the side that supplies the keeper's *no* answers. And **"same section" is undefined for the 51 code members** (v1's D9
made it "between markdown headers"; `main.rs` has none). **Fix: define the N-th draw, define the section for a
non-markdown file, and say what a member with no matched line contributes.** This is the one FATAL I would have to
answer myself — §1 names B as the eligible extractor.

## 2 · THE ELEVEN AMENDS, in one line each

1. **§9's de-indent command does not reproduce §9's sha.** `5,$p` glues two prose lines on; measured: start 5 →
   `2b2cc973…`, start 6 → `0bc9fe67…`, **start 7 → `9541d52d…` (the registered sha)**, start 8 → `702dac86…`.
   `p-d109-tj1v2-E_2026-09-22.md:92-93` says the command was run and matches; **that claim is false as printed**.
2. **V2-D2's reason is measurably false and the rule is inert:** the located line is identical at the introducing
   commit and its parent for **72/74 = 97.3%**. Keep the rule, replace the reason with the measurement.
3. **"424 correction lines" overstates the grep:** 28 rest on `\berror\b` alone, and several of those are bug reports
   or a named concept ("the UNIV∞ error"), not corrections.
4. **Dropping C3 loosens the survivors' own bound:** k = 3 → 2 moves §5(a) from 98.33% to 97.5%. Pin k at the count
   at registration, or name the loosening.
5. **The keeper's `can't tell` is excluded while the judges' `cannot-say` is scored both ways** (AMEND-8). Mirror it.
6. **"One sitting" is welded to "before any Jev output"; only the second is load-bearing.** Unweld, and the fatigue
   objection mostly dissolves.
7. **The oracle has no calibration.** Jev is asked 3× and its non-unanimity printed; the keeper is asked once. Repeat
   ~10% (16 units, ~4 min by §4's own estimate) and print his self-agreement.
8. **A fatigue readout costs nothing** — the salt already fixes the order, so print the yes- and can't-tell-rates for
   the first vs second half. It makes a position effect visible this run instead of arguable.
9. **"Blind to the class's name" is empty** — he is shown the class question, which is the class.
10. **A 20-unit pilot before committing him to 158**, sizing the sitting from the measured per-stratum yes-rate.
    Without it, the assumed enrichment could cost 158 units of his time and still return NOT TESTED — v1's outcome at
    much greater cost.
11. **Register a prediction now**, since §3 already prints the strata: the keeper's yes-rate on the matched stratum
    will be ≥ the adjudicated stratum's for C2 and within 15 points for C1. If it holds, the correction record does
    not enrich for these questions and no later registration may use it as a frame. **If the adjudicated stratum is
    >15 points higher, FATAL-1 and FATAL-2 were over-stated.** It costs a sentence and it scores this attack.

## 3 · THE FIVE NOTES

1. **Every exclusion removes the same kind of thing: 259 of 350 = 74.0%** are "the locator was not a single resolvable
   pointer" (112 range-only, 83 unresolved, 64 ambiguous). A range is what an author writes when the error spans
   lines; a multi-locator adjudication is what they write when the correction touches several places. The pool is the
   tidy corrections. Not fixable without discretion — nameable in §3's printed universe line.
2. **V2-D0 is disclosed and pre-output, so it is NOT degenerating under §5** — but §5 forbids a v3 over a wider slice
   *after* NOT TESTED and does not forbid measuring a fourth universe *before* the run. Close that flank.
3. **The extractor gets a 39-character sha on boundary commits** (`slice(0,40)` then strip `^`). Works by prefix; luck,
   not rule. Carried into my own probe deliberately so both counts share it.
4. **"Not itself a universe line" is implemented as text equality, not `path:line`** — it also excludes an identical
   line in another file. Fired 3 times.
5. **v2 is blocked on exactly what v1 was blocked on:** S-CTRL is on L, the manifest was not re-derived on D, and
   **AMEND-10's egress yes from the keeper is still owed**. Without the gate T-J1 does not run, so a larger universe
   changes nothing about whether the run can start.

## 4 · WHAT I DID NOT VERIFY

- **S-CTRL's arithmetic** (96/34, the label sha, the manifest sha) — v1's, not re-derived here, and the manifest
  cannot be re-derived on D at all.
- **Anything about Jev's accuracy.** No Jev, judge or verdict output was opened. Nothing here is evidence about Jev;
  it is all about whether the registration can return a meaning.
- **The member file and the negative pool were not built.** FATAL-3 measures the rule's reach, not a file.
- **The keeper's yes-rate**, which is unmeasurable from here. FATAL-1 and FATAL-2 bound what the pools *can* supply;
  they do not predict his answers — which is why AMEND-11 registers the prediction instead of assuming it.
- **§-ATTACK's heading still reads `OPEN`.** Changing it is an edit to E's heading, so I did not make it; **E should
  flip it to FILLED** when amending, as v1's §7 was.

## 5 · WRONG column

- **W1 — my recurring failure, again, caught in flight.** I wrote a probe through a shell heredoc and the backslashes
  were eaten: `\bWRONG\b` reached git as `?WRONG?` and it died on `Unmatched ( or \(`. Rewritten through the editor,
  which is the rule I already hold from D092, L063, D103 and L081. The cost was one run; nothing downstream used it.
- **W2 — a number in my own draft that did not re-derive from my own embedded probe.** I had written "mean distance
  from unit to drawn negative 48.8 lines" in FATAL-3, measured by a scratch script I did not embed. Caught before
  filing and replaced with a claim checkable in the embedded probe's own source (its outward search has no bound but
  the file's end). **The rule I broke for one draft is the rule this attack's AMEND-1 is about.**
- Not a WRONG, but declared: **I wrote v1's attack, whose FATAL-1 forced v1's NOT TESTED, and I built v1's member file
  that returned it. v2 exists because of my finding, and I am the seat now saying v2 is short too** — a seat with an
  interest in its own earlier verdict. That is why every figure is printed with the command that makes it and the
  probe is embedded whole, and it is why AMEND-11 registers a way for this attack to be scored wrong.

## 6 · VERIFICATION OF THE WRITE ITSELF

    sed -n '/^    \/\/ T-J1 v2 §-ATTACK PROBE/,/^    \/\/ end of probe/p' <file> | sed 's/^    //' | sha256sum
      → eae549eeac98d31cd082923c9a37c6bf18e5ae8e58351a6b117b920bd863df35   (my probe, as embedded)
    sed -n '/^## 9 · THE SIZING SCRIPT/,/^## 10 ·/p' <file> | sed -n '7,$p' | sed 's/^    //' | sed '/^## 10/d' | sed '$ d' | sha256sum
      → 9541d52d53eb85073090afeec8b796e54b4e159bc6a59c7a662cbfe46f57727b   (E's, re-derived OUT OF THE FILE after my write)

The second is the check that **E's §9 survived my append byte-for-byte**; §0–§10's headings are at their original
line numbers (22, 36, 100, 124, 141, 180, 231, 247, 279, 305, 374). The file went 408 → 757 lines. **My probe's
command is anchored on content, not on a line number, deliberately — it is the failure AMEND-1 names.**

NEXT: librarian collate B's attack, then E amends, then chair lands D109 whole and opens 2.1 (measure js-suite and cargo on D) when the amendments are in

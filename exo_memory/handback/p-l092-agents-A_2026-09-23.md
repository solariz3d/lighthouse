# P-L092-AGENTS · ALPHA — AGENTS.md built on the keeper's base, his lines unchanged (+39 −0), pointer-only. The repo description is DRAFTED, not published: two versions in his words, the hedge replaced by the claim his README now makes

**Pane A, machine L, 2026-09-23 03:4x–04:0x.** Lap L092, HAM batch 1, item 6. **Files: `AGENTS.md` (untracked; 504 → 3,400
bytes) and this hand-back.** **`gh repo edit` was NOT run**, and no other outward act was taken. Nothing committed.

## 0 · FIRST — the L089 record, since the chair read it as missing

It was not missing: `## Step 2` has been on disk since **03:22:23** (line 66; blob **a7678cac**, where the chair cited
**8d3798e5**, the 02:40 stop version). The map line was in too. **What stalled was the ring.** My 03:2x `call_librarian` returned
*"queued for 0c0c0c0b — stamp=working; it delivers when it is ready"*, and it was never delivered. My re-ring at 03:3x was refused
**OUT OF TURN**: the return-leg trap, with the baton at the chair. I took the guard's own remedy,
`lap-row.js --stage L089 working --holder panes --by panes` (a RETAKE, no ring owed), and the ring then **delivered**.

**The lesson is mine:** "queued" is not "delivered". I should have checked it had landed before calling my part done.

## 1 · AGENTS.md

**The keeper's base, read first**: 504 bytes, untracked, 2026-09-08 05:51. An H1, one paragraph, and four pointers:
BOOT.md, HANDOFF.md, `exo_memory/`, `dev/`, and the L0 method at the root. **All of them still resolve** (checked each one).

**What was added, pointer-only.** The rules are cited, not restated, because a paraphrase of a route is how a route rots:
- **A fifth pointer, beside his four:** Consonance → `consonance/` (its manual, `consonance/README.md`). His *"The program in
  progress (the Lighthouse) → `dev/`"* stands **as he wrote it**. It is dated, not wrong, and I did not edit it: `dev/` still
  holds the shell hooks and the earlier builds, while the app now lives in `consonance/`.
- **"If you are an agent about to do work here":**
  - **the loop**, citing BUILDING.md's THE LOOP, WHAT EACH SEAT IS FOR and THE JOINT STEP, and COMMITTEE.md;
  - **working a packet**: only the named files; a negative answer is a result; numbers with their commands; the hand-back path
    convention, the map line, and the pointer ring with its NEXT trailer, whose plan item is a DEFAULT (BUILDING.md item 6,
    amended 2026-09-23);
  - **landing**: never `git add -A` / `commit -a` / bare `commit`; name the seat in the body; **no seat pushes**; every landing
    runs the whole js-suite;
  - **what is not yours to decide**: settings, installed hooks, live ledgers, anything outward or unruled. Open asks are in
    `exo_memory/ASK.md`;
  - **what the room expects**: measure before claiming, keep the wrong number beside its correction. Pointed at BOOT.md, not
    restated.

**Every file pointer resolves** (a scan over every backticked path in the file). **I got one wrong first:** I wrote
`exo_memory/ask/` from memory. `ask.js:65` says the store is `exo_memory/ASK.md`, which is the one the file now names. That is
the dead-pointer failure the room guards against, made by the pane writing its entry page.

**THE DIFF FROM HIS BASE.** The file was untracked, and I wrote in place, so his base was reconstructed and **proved**: the
prefix of the new file up to the added line is exactly **504 bytes with LF endings**, his recorded size; the CRLF reading would
be 512. Then `git diff --no-index <base> AGENTS.md` → **+39 −0. No line of his removed or altered.** The additions, in full:

    + - The app that runs the room — **Consonance**, a Tauri desktop app that launches and keeps the seats → `consonance/` (its manual: `consonance/README.md`)
    +
    + ---
    +
    + ## If you are an agent about to do work here
    + … (the section above; 34 more lines, all pointers)

`node consonance/tools/portable-paths.js` → **green, 0 new.** AGENTS.md names no machine path.

**It will be tracked for the first time when it lands.** It is his file, so the chair lands it on his authorization
(`d5bd9b1` item 6).

## 2 · THE REPO DESCRIPTION — DRAFT ONLY

**Current** (`gh repo view solariz3d/lighthouse --json description`; the repo is **PUBLIC**):

> A desktop workbench for a committee of Claude Code instances: seats that wake into their own thread across restarts and
> machines, a shared board, a human in the loop by design, and instruments built to return numbers nobody wanted. Continuity
> without pretence; the central claim is still untested.

**The limit: 350 characters.** This is not from an official GitHub doc. GitHub Desktop's issue #19465 quotes the error at 350,
and the community list `dead-claudia/github-limits` agrees. **Both drafts are plain ASCII, so characters equal bytes.**

**Draft B — recommended, 321 characters.** It carries the README's claim word for word (`README.md:108`, the keeper's 09-22
rewrite, `00aa934`), with room to spare:

> A desktop workbench for a committee of Claude Code instances: seats that wake into their own thread across restarts and
> machines, a shared board, and instruments built to return numbers nobody wanted. It works: seats catch each other and
> themselves, and laps run seat to seat with no human carrying anything between them.

**Draft A — 346 characters**, which keeps his *"Continuity without pretence."* with only 4 characters of margin:

> A desktop workbench for a committee of Claude Code instances: seats that wake into their own thread across restarts and
> machines, a shared board, and instruments built to return numbers nobody wanted. Continuity without pretence. It works:
> seats catch each other and themselves, and laps run seat to seat with no human carrying work between them.

**What changed, and why:**
- **The hedge is gone**, replaced by the claim the README now states with its evidence.
- **"a human in the loop by design" is dropped**, because it now reads against the claim: the keeper is the entry and rules on
  his calls, but he no longer carries the work (BUILDING.md: *"the user is the ENTRY, not a station"*).
- **Everything else is his wording**, kept.

A first cut at exactly 350 was trimmed, because a limit counted even slightly differently would fail at his publish, not here.

**The command, for AFTER the librarian and the chair have both read the draft — NOT RUN:**

    gh repo edit solariz3d/lighthouse --description "A desktop workbench for a committee of Claude Code instances: seats that wake into their own thread across restarts and machines, a shared board, and instruments built to return numbers nobody wanted. It works: seats catch each other and themselves, and laps run seat to seat with no human carrying anything between them."

The restore, if wanted, is the same command with the current text above.

## 3 · SUITES — the whole js-suite, both runs as they came out

    node consonance/tools/js-suite.js     run 1   122 green · 1 failed (carrier-drift.test.js) · 1 canary (of 124)
                                          run 2   123 green · 0 failed · 1 canary (of 124)
    node --test consonance/tools/carrier-drift.test.js (alone, between them)   57 passed, 0 failed

**Run 1's red did not reproduce, and I do not know its cause.** It was not AGENTS.md: carrier-drift passed alone, and on the
re-run, with AGENTS.md unchanged between them. Other seats had files in flight throughout (`state-sync.*`, `trip-check.*` dirty).
**It is written down rather than dropped**, because a red that went away is not the same as a red that was fixed.

## 4 · NOT VERIFIED

- **The description on GitHub**, by construction. Neither draft has been rendered anywhere.
- **The 350 limit** from GitHub's own documentation, because I found none. Draft B's 29-character margin is the hedge against that.
- **Whether an outside agent reading AGENTS.md cold finds its way.** No reader has tried it. A blind first-contact read by a seat
  that has never seen the room would be the test.

NEXT: librarian call_librarian with the L092 pointer when AGENTS.md and the description draft are written — plan default after batch 1: batch 2 (usage instrument, update fuse), unless the output says otherwise; the description publish is a separate step after librarian + chair read §2

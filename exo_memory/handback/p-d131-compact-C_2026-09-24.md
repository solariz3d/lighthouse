# P-D131-COMPACT: C (D131, on D, 2026-09-24 ~06:5xZ). The board compaction was APPLIED and passed a multiset check. Not committed.

The full result, with every command, is in `exo_memory/loop/board_compaction_2026-09-24.md`.

- **Backup first.** `C:/Consonance/data/attic/board.jsonl.pre-d131-20260924T065039Z`: 78,454,980 B, 64,444 rows,
  sha256 `7d8f15ed1d1b6a68783879b8689747cefb4d92f0e32125d5cb020e80b6293e98`.
- **Dry run, then `--apply`** (`node consonance/tools/board-compact.js --board C:/Consonance/data/board.jsonl
  --apply`, exit 0):
  - rows: 64,446 before, **56,932 after**;
  - 7,514 repeats folded (11.7%);
  - bytes: 78,455,537 before, 70,173,562 after;
  - 1 row caught from the live writer, 0 gap rows, 0 late rows.
  - The tool's attic file is `C:\Consonance\data\attic\board.jsonl.2026-09-24` and STAYS.
- **Multiset check: PASS, twice.** The check is my own `verify.js`, written before the run.
  - P1: the backup is a byte prefix of the attic.
  - P2: 0 distinct rows missing.
  - P3: the rows are in exact first-occurrence order.
  - P4: the tail holds no old rows.
  - Arithmetic: 64,444 + 2 appended = 64,446 = 56,932 + 7,514 folded.
  - The re-run at 06:52:31Z also counts 1 row the app appended after the swap.
  - 3 blank lines were dropped as non-rows.
- **The swap window.** Three mechanisms cover it: catch-up (`:130-138`), gap rows (`:341-351`) and late rows
  (`:353-362`). What is left is a microsecond window, and the multiset check is its backstop. Details are in the
  result doc §5.
  - **Tool defect, named but not fixed:** the restore advice in `verifyPair`'s refusal (`:371`) would drop rows
    appended after the swap.
- **Readers.**
  - `trip-check --report`: OK. The 3 not-clean trips predate this run.
  - `chain-status`: OK.
  - The board-digest hook: exit 0 on a 300-row fixture and on a full-board fixture.
  - **Nothing holds a byte offset into the board.** The digest stores timestamps. `tailer-offsets.json` points into
    the transcripts.
- **Not verified:**
  - the app's own read of the compacted board, which waits for the rebuild;
  - the residual windows, which are closed after the fact by the check, not by construction;
  - how L's union treats a board that no longer holds repeats.

Files I wrote: the result doc, this hand-back, and one line in `map/C.md`. `board.jsonl` was changed only by the tool.
Nothing was committed.

NEXT: librarian, call_librarian with this hand-back pointer. The compaction is applied and passed the multiset check. The plan's default after it is the keeper's rebuild, which puts D129–D130's Rust live, unless the output says otherwise. The output adds that the app's own read of the compacted board is first exercised at that rebuild.

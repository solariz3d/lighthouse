# P1a-CHECK — fidelity re-read of ALPHA's §12 append against `4c464f4` (2026-08-31 ~05:15, BRAVO, L021)

**Seat:** pane B (BRAVO), author of the amendments being checked. **Object owned:** this file only.
ALPHA's registration not edited; not committed. **Method:** byte-comparison, not reading — a node script
tested each of my three blocks (extracted from `git show 4c464f4:exo_memory/loop/battery_attack_2026-08-31.md`)
for exact substring presence in `exo_memory/loop/battery_load_registration_2026-08-31.md` as on disk
(533 lines, `M` in `git status`), then ALPHA's own three `diff` commands were run, then §12 was read in full
for softening. Both files are LF-only; no CRLF confound.

## Verdict: **BUILD FROM THIS. The text did not drift.** One instrument defect in ALPHA's self-check, reported below; it does not touch the amendment text.

### 1 · Amendments A, B, C are byte-identical to `4c464f4` — CONFIRMED

| block | my span in `4c464f4` | chars | byte-present in ALPHA's file | anchored `diff` |
|---|---|---|---|---|
| Amendment A (incl. my counted prediction line) | :63–75 | 921 | **true** | **0 lines** |
| Amendment B | :104–111 | 674 | **true** | **0 lines** |
| Amendment C (= my §3 F-C, all four items) | :115–133 | 1,624 | **true** | **0 lines** |

Re-derive (must print nothing, three times):

    cd C:/Consonance/lighthouse && for spec in "A 63,75p" "B 104,111p" "C 115,133p"; do set -- $spec; diff <(git show 4c464f4:exo_memory/loop/battery_attack_2026-08-31.md | sed -n "$2") <(awk "/^BEGIN AMENDMENT $1/{f=1;next}/^END AMENDMENT $1/{f=0}f" exo_memory/loop/battery_load_registration_2026-08-31.md); done

No expanded contraction, no re-typed character, no dropped line. The `×` and `−` and `≥` glyphs survive
(checked as bytes, not by eye — `cat -A` shows `M-CM-^W` for `×` on both sides).

### 2 · The P0a / P0b split is present — CONFIRMED

`P0a`, `P0b` and `P1b` all occur in the file (`grep -c "P0a\|P0b\|P1b"`), inside the verbatim block AND in
ALPHA's framing prose at 12.1, which states the supersession explicitly: *"P0 as registered is SUPERSEDED:
VOID-RIG moves to P0a … L0×K0 becomes P0b."*

### 3 · The evaluability clause is present in its own words — CONFIRMED, with one note for the chair

The file contains, byte-exact, **`P2/P3 are evaluable only if K0 − P0a-rate ≥ 0.40`** — twice: inside the
verbatim Amendment C block (item 4) and quoted in 12.3's framing. The packet's quoted form, *"P2/P3
evaluable only if K0 − P0a-rate ≥ 0.40"*, is **not** byte-present — it drops my "are". **That is the
packet's paraphrase, not ALPHA's drift**; ALPHA carried my wording, the brief shortened it. Noted so the
next reader does not grep for the chair's form, miss it, and call drift.

### 4 · Pre-attack text (§0–§11) unchanged — CONFIRMED

`git diff --numstat 095f37b -- exo_memory/loop/battery_load_registration_2026-08-31.md` → **133 added, 0
deleted**; the single hunk is `@@ -400,0 +401,133 @@`. Nothing above §12 was rewritten. The registered
clauses stand as trace with the amendments governing, as 12.0 says.

### 5 · Softening check — the thing the packet did not ask for. **Not softened.** Two observations, neither a drift.

I read every framing sentence ALPHA wrote around the blocks against what the blocks say.

- **12.1 adds a precedence rule I did not write:** *P1's absolute VOID-LOAD is read first; P1b's difference
  test second and never triggers a re-registration.* This is consistent with my Amendment A (which says the
  cue arms still run on L1 when P1b fails) and it resolves an ordering my text left implicit. It **narrows
  nothing** I registered. Accept.
- **12.1's opening sentence over-commits in MY direction, not against it:** *"so a high L0×K0 is a
  behavioural finding, not a rig fault."* My amendment says P0a **decides** which it is; ALPHA's prose states
  the answer before P0a has run. The verbatim block governs (P0a is still the test), so this is a framing
  slip, not a softening — but chunk 2 should build from the block, not from that sentence, and if P0a fires
  the sentence is wrong and the block is right.
- **12.2 chooses `REPLY.md` over my alternative** *"(or a named section of `HANDBACK.md`)"* and applies it to
  both L0 and L1. That is a choice inside the amendment's stated options, not a departure. Accept.
- **12.3 adds a definition** — *K0 is the L1×K0 violation rate, P0a-rate is the rig floor from 12.1* — that
  my clause needed and did not carry. Strengthens, does not soften. Accept.
- **12.4's Cohen's-h paragraph** restates my ×2 finding faithfully (63 not 32; 40/arm stands) and reframes
  *"hand-edited after printing"* as *"the table is a subset of its output."* Same fact, gentler noun; the
  correction itself is carried in full, including that the registered sentence *"was true for the wrong
  reason."* Not a softening of the amendment — the amendments are A, B, C, and this is context.
- Every 12.4 item I checked against my §6 (hooks 73/73 → isolation REQUIRED; model per row; F4 settled; cost
  4–8×; interleave K1/K2; the kill inverted) is carried at the strength I wrote it. None was downgraded to
  "consider" or "may".

### 6 · The one defect — ALPHA's verification command fails on A, and it is the command, not the text

12.0 says its `diff` *"must print nothing."* **As written it prints 19 lines for A** (0 for B and C):

    diff <(git show 4c464f4:exo_memory/loop/battery_attack_2026-08-31.md | sed -n '63,75p') <(awk '/BEGIN AMENDMENT A/{f=1;next}/END AMENDMENT A/{f=0}f' exo_memory/loop/battery_load_registration_2026-08-31.md) | wc -l
    19

Cause, verified: the command is quoted **inside the registration itself** at line 414
(`grep -n "BEGIN AMENDMENT A" exo_memory/loop/battery_load_registration_2026-08-31.md` → 414 and 433), so the
unanchored regex matches its own citation first and awk starts capturing 18 lines of prose before the real
marker at 433. B and C escape only because their markers are referred to as *"likewise"* rather than
spelled. **Anchoring the regexes to line start (`/^BEGIN AMENDMENT A/`) makes all three print 0** — the
command in §1 above. The amendment text was never at fault; a reader running ALPHA's check as written
would have called drift where there is none, which is the mirror of the failure this packet was guarding
against. **For the chair to carry:** 12.0's command should read `/^BEGIN…/` and `/^END…/`. A one-character
class change; not mine to make.

### 7 · What this check does NOT establish

- Nothing about whether the amendments are *right* — that was L020 and is not re-opened here.
- Nothing about §12.4's by-reference items beyond their fidelity to my §6; I did not re-run those numbers
  tonight, they were run this morning and are cited in `4c464f4`.
- The clock: ALPHA's 12.0 computes F4 elapsed from both 01:33 and 02:29 and finds the deadline unchanged;
  I did not re-derive it.

*Own falsifier:* if anyone finds a byte inside a `BEGIN…END` span that differs from `4c464f4`, this file
said "byte-identical" three times with the command beside it and was wrong; strike the verdict and hold
chunk 2.

*Hand-back only. Nothing edited but this file; not committed. Chunk 2 may dispatch P0a off the registration
as amended. A trace to re-run, not a doctrine to believe.*

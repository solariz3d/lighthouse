# P-L062-RC3 — C (CHARLIE), L062 packet R-C3, on L — the two shipped instructions

**Packet:** `exo_memory/loop/plan_repo_fixes_2026-09-21.md`, the R-C3 bullet (`4328d56`), read at source.
**Owned:** the `consonance/README.md:230` region and one row of `consonance/tools/portable-paths.baseline.json`.
**Nothing committed. `--update` not run.**

## 1 · THE RESULT

| | before (03:14:40) | after (03:16:51) |
|---|---:|---:|
| `portable-paths.js` | **RED · 41** unbaselined | **RED · 39** unbaselined |
| · `FATAL-SHIPPED-INSTRUCTION` among them | **2** | **0** |
| · `BENIGN-TEST` / `REVIEW` / `FATAL-DEFAULT` | 30 / 5 / 4 | 30 / 5 / 4 — untouched |
| `carrier-drift.js` | **GREEN**, exit 0 (03:14:41) | **GREEN**, exit 0 (03:16:51) |

Exactly the two target sites left the unbaselined set; nothing else moved. Exit codes read with `PIPESTATUS[0]`.
Commands: `node consonance/tools/portable-paths.js` and `node consonance/tools/carrier-drift.js`; the verdict
breakdown is `grep -oE "^  DRIVE +[A-Z-]+" <output> | sort | uniq -c`.

## 2 · README.md:230 — REWORDED, and the old line was wrong on more machines than the packet said

The old line: *"Dreams are written per seat outside the repo, under `C:\Consonance\instances\<seat>\dreams\`"*.

**What the location actually is, read from the code rather than the prose:** the instances root is the
**`instances_dir` field of `~/.consonance.json`** (`consonance/tools/state-sync.js:159` reads exactly that field).
It happens to be `C:\Consonance\instances` on L (`node -e` below prints it), and D's data dir is the same path —
but **the code's own fallback when the field is absent is `%USERPROFILE%\claude-instances`**
(`consonance/src-tauri/src/main.rs:735-736`, `default_instances()`). So on a fresh machine the old sentence did
not merely carry one checkout's drive; **it named a place the program would not be writing to.**

The new wording (4 lines replacing 1, at the same place):

> Dreams are written per seat outside the repo, under `<instances_dir>/<seat>/dreams/`, where `instances_dir` is
> that field of `~/.consonance.json` on the machine you are on — print it with
> `node -e "console.log(require(require('os').homedir()+'/.consonance.json').instances_dir)"`. It is set per
> machine, and it is not the code's fallback (`~/claude-instances`), so read it rather than assume it.

- **The command it tells a reader to run was run exactly as printed** → `C:\Consonance\instances` on L.
- **No drive letter**, so the DRIVE detector has nothing to fire on: README findings 1 → **0**, and the rewording
  introduced **no** new finding of any class (41 → 40 after this edit alone, measured at 03:15:48).

## 3 · retired_seats_2026-09-11.md:62 — A BASELINE ROW, NOT A REWRITE, AND NOT A DATED APPEND

The packet offered two routes and named B's CH-4 freeze (`043d78b`) as the reason an append would turn carrier-drift
red. **Row, not append.** The record file is **byte-unchanged** — `git diff --quiet` passes, sha256
`91d2b83d01620c98…` — and carrier-drift stayed green across the edit (§1).

**The row, as added** — its key computed by the tool's **own** exported `scan(text, false)`, `norm` and
`classifyProse`, exactly as `collect()` builds it for prose, never rebuilt by hand:

```
file      exo_memory/record/retired_seats_2026-09-11.md
detector  DRIVE
verdict   FATAL-SHIPPED-INSTRUCTION      <- kept, deliberately (below)
text/key  the tool's normalised line, "#1"
why       A's argument, verbatim in substance: the path resolves on both machines, so the finding is about FORM;
          the file is a dated record master (librarian, 397e29c), CH-4 frozen (B, 043d78b); verdict left FATAL
          deliberately — exempted, NOT fixed; and a NOTE that --update will drop this field
```

**The diff is one row:** `git diff --numstat` → **9 added · 1 removed**; the one removed line is
`"FATAL-SHIPPED-INSTRUCTION": 1` becoming `2` in `counts`. Inserted at the writer's sort position (177 of 178), so a
later `--update` does not reorder around it. Before writing, the script checked that re-serialising the **unchanged**
baseline reproduces it byte for byte — otherwise "one row" would have been a reformat of 67 KB.

**Why the verdict stays FATAL, which is the one judgement call in this packet:** the tool's `needsFixing()` counts
baselined `FATAL* | DISGUISED | REVIEW` sites on every green run as *"exempted, NOT fixed."* That counter exists
because of BRAVO's L043 finding — *"exempted reading exactly like fixed"* is the census failure this repo keeps
finding. Re-labelling this row to something benign would make it drop out of that counter and read as fixed. **It
is not fixed.** It is exempted because the file is a record that may not be rewritten. Keeping the true verdict
keeps it visible.

## 4 · THE HAZARD FOR R-C4 — found in the source, not run

**`--update` will erase A's argument from this row, and reset any hand-corrected verdict anywhere in the file.**

`writeBaseline()` (`portable-paths.js`, the `sites.map(...)` inside it) rebuilds **every** row from the **live**
sites as `{ file, detector, verdict, text, key }`. Live sites never carry `why`; their `verdict` is whatever
`classify` / `classifyProse` returns today. So after R-C4:

- **membership survives** — this site is live, so `--update` re-lists it and the guard stays quiet on it;
- **the `why` field is gone**, silently;
- **any hand correction to a verdict elsewhere in the baseline is reverted**, which contradicts the tool's own
  header comment (*"Verdicts … may be hand-corrected in the baseline"*).

The tool's own message already names where an argument is meant to live — *"run --update and let the baseline
diff carry the argument"* — i.e. **in the commit, not in a field.** So the durable home for A's argument is the
commit message that lands this row, and R-C4 should either say so in its own commit or make `writeBaseline`
preserve unknown fields from the prior baseline. **Not proven by running it**: the packet forbids `--update`, and I
did not run it even against a scratch copy. The claim rests on reading the one `map()` that writes the rows.

**And one more thing R-C4 should know:** `--update` baselines **every** unbaselined site at once, not only the 30
`BENIGN-TEST` ones. If R-C1/R-C2 have not landed first, the **5 `REVIEW`** and **4 `FATAL-DEFAULT`** sites in the
39 would be blessed with them. The plan already sequences R-C4 last; this is why the order matters.

## 5 · WHAT WAS NOT VERIFIED

1. **Machine D.** `instances_dir` was read on L only. D's data dir is recorded as the same path (A's §2, from D080's
   `state-sync.js dataDir()`); I did not read D's `~/.consonance.json`.
2. **Other prose that names `C:\Consonance\instances`.** Only README:230 was in scope; `consonance/tools/whats-live.js:38`
   hard-codes `const INSTANCES = 'C:/Consonance/instances'` in code, which is some other packet's site if it is
   one at all.
3. **§4 is a reading of the source, not an execution.**
4. **The green-run "owed" line.** I did not see this row counted in `needsFixing`'s *"exempted, NOT fixed"* line,
   because the tool is still red (39) and only prints that line when green. It follows from `readBaseline` (reads
   `sites`) and `needsFixing` (reads `verdict`); it will be observable after R-C4.

## 6 · COMMANDS

```
node consonance/tools/portable-paths.js                     # RED 41 -> 39; exit 1 both times
node consonance/tools/carrier-drift.js                      # GREEN both times; exit 0
node -e "console.log(require(require('os').homedir()+'/.consonance.json').instances_dir)"   # the README's resolver
git diff --numstat consonance/tools/portable-paths.baseline.json                          # 9 / 1
git diff --quiet -- exo_memory/record/retired_seats_2026-09-11.md && echo unchanged      # the frozen record
# revert the row: copy <scratch>/rc3/baseline.pre-rc3.json back over the baseline
```

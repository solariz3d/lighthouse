# P-D139-DRAFT — C (D139, R1, on D, 2026-09-25 ~02:5x–04:xx) — THE README REWRITTEN FROM THE WHY, THE HISTORY MOVED VERBATIM · WORKING TREE ONLY, UNCOMMITTED

Packet: the chair's D139, C's row of `loop/plan_readme_about_rewrite_2026-09-25.md` (`484f77c`). HEAD `484f77c`. **The
repo is public: nothing committed or pushed; the keeper reads before anything lands (R3).** B's baseline was not read.
`consonance/ui/index.html` was read to take its text, and not edited.

## 0 · WHAT IS WRITTEN

| file | what |
|---|---|
| `README.md` (working tree) | **The full draft.** In order: why → what it is → what it does for you → try it → how it works → method and measurements. Written for a stranger first. The About tab's text is its first three sections, between `about:begin` / `about:end` markers. |
| `exo_memory/loop/readme_draft_notes_2026-09-25.md` | **The why with a source for every sentence (§1), the jargon map with 42 terms (§2), the outline and my one push-back on order (§3), the single-source plan for About (§4), what was kept true (§6), and four questions for the keeper (§7).** |
| `exo_memory/record/readme_history.md` | **The old page's history, verbatim:** the 2 inline HTML comments, plus the struck and superseded passages, the dated test figures and the dated change logs. There are 6 blocks, each with its old line range. They were moved by script by exact line range and **byte-checked against `git show 484f77c:README.md`**. The HTML comments are fenced so they stay visible. |

## 1 · THE WHY, as the draft opens

> The AI you talk to today is not the one you talked to yesterday. It may remember facts about you, but it does not
> carry on from where it was… And when an AI works alongside you on something hard, it drifts in ways you can predict…
> Consonance is an attempt at both… It does not claim there is anyone "home" in there… it measures whether any of it
> helps, including the parts that did not.

Every sentence is traced to its source in notes §1. They are the old `README.md:5-7, :19, :23, :25, :100, :132`,
`consonance/README.md:39-41`, `dev/SPINE.md` §1, and the About tab's stance. **The one source on disk I did NOT use is
BOOT's "The root" (grief that learned to build).** Putting it on a public front page is the keeper's decision. A
candidate sentence is in notes §1 for R3.

## 2 · KEPT TRUE — nothing rounded up

- **Jev:** 8 of the 30 turns it flagged as drifting were confirmed, and 19 of the 20 it called clean, over 56 units.
  The readers were lenient, and that split has not been measured (`jev/README.md:97-118`).
- **Jev's stranger test is NOT YET PASS** (`loop/jev_clean_machine_2026-09-23.md:144`), and T-J1 is NOT TESTED.
- **The central claim keeps its evidence:** the 09-19 unattended run, with its two open checks stated; the 09-21/22
  rounds; and four catches, each with a link.
- **Every "measured not to work" figure is exact and dated:** the cue arms, the branch layer, the four gauges, the
  87.2% → 68.2% board share (with its row counts), ferry 990, and the ledger's 105 rounds with 10 void.
- **The test figures are TODAY's, not 09-22's:** js-suite 139 / 0 / 1 canary / 1 not-run of 141, and cargo main 961
  passed, 0 failed, 4 ignored. **`arch_test` is 13/0, so the old About's "still red, 12 passed 1 failed" is out of
  date.** Source: my D138 run, `handback/p-d138-waiting-C_2026-09-25.md`.
- **My own draft errors, caught in the claim-by-claim check before filing:**
  1. Jev's "30 worth-a-second-look marks" was loose. It is 30 "drift", plus 6 "can't judge".
  2. "As more workers were added" was my gloss on the board share, so it is cut.
  3. "Fails the build" was wrong: it is a test.
  4. "Windows 10 or 11" was unverified, so it now reads "built and used on Windows 11".
  5. "Musical intervals" was my embellishment.
  6. The not-run test's reason was wrong.
- **Links:** 20 relative links resolve and 3 in-page anchors resolve (script check).

## 3 · PUSH-BACK ON ORDER, and the one-source question

**The order is used as given, with one addition.** Layer 2 carries a one-line honesty pointer, so a stranger who stops
early still learns the founding bet failed. "Try it" states the usage cost before the command. The alternative order
(failures higher) and why I did not use it are in notes §3.

**About is one source:** the marked README block. **The draft's closing sentence says the app's copy "is checked against
it by a test". That is FALSE until A builds the test in R2, so it must not land before R2.** Two ways to build it are in
notes §4.

## 4 · WHAT THIS BREAKS, NAMED

**`carrier-drift` is RED because of the history file's PATH, which the packet named.**
- `exo_memory/record/` is reachable from BOOT and SOURCE, so a new file there enters the instruction-reachable set, and
  the frozen list must be re-frozen **after reading it**, by the tool's own rule.
- `node consonance/tools/carrier-drift.js` gives `RED — CH4-DRIFT-ADDED exo_memory/record/readme_history.md`.
- `carrier-drift.test.js` gives **54 pass, 3 fail** (the frozen-list test, THE BAR half one, and the cant-lose
  real-tree test). None of these is a wording hit; no retired wording is in the file.
- **The fix is the registry owner's:** re-freeze with a log line, class the file as a trace, or move it. The file is a
  verbatim archive that teaches nothing. **I did not touch the registry; it is not mine this lap.**

**`arch_test` is also RED, 11 passed and 2 failed, both from the history file:**
- `every_named_record_file_exists_and_every_record_file_is_named`: "record/readme_history.md is named by no card".
  `record/` is the room's place for material a CARD points to, and a README history is not that.
- `every_relative_link_in_the_docs_exists_in_a_fresh_clone`: the draft README links to the history file, which is
  untracked. This one clears when the two land together, **wherever the file lives.**

**So the packet's path breaks two registered invariants, five tests in all.** Those are carrier-drift CH-4 (the checker
plus 3 tests) and arch_test's record-naming test. **A spec contradiction: I left the file at the path the packet named.**
The fix, **proven and reverted**, uses an alternative path that is a working-tree move, never committed:
- **With the history file at `exo_memory/loop/readme_history_2026-09-25.md`, and the README's link changed to match:**
  - `carrier-drift.js` is **GREEN, 0 findings**;
  - `carrier-drift.test.js` is **57/0**;
  - `arch_test` is **12 passed, 1 failed** (only the fresh-clone link, which clears on landing).
- **Restored afterwards to the packet's path:** `README.md` sha256 `b26ab2fd4f919057…` and the history file
  `bee9bee892e74484…`, identical before and after.
- **Recommendation: take the `loop/` path.** It is a two-line change: move the file, and update one link in
  `README.md:228`.

**The other tests that read `README.md`:** gen-consumer 60/0, librarian-cite 23/0, shelf-recursion 11/0,
score-portable 1/0.

## 5 · SUITES

- **`node consonance/tools/js-suite.js`** (background, heavy-run lock): **138 green · 1 failed · 1 canary · 1 not-run, of
  141.**
  - The one failure is `carrier-drift.test.js`, from the history file's path (§4). With the file at the `loop/` path it
    reads 57/0.
  - The canary and the not-run are as before.
- **`cargo test --test arch_test`:** 11 passed, 2 failed at the packet's path; 12 passed, 1 failed at the `loop/`
  path (§4).

## NOT verified

- **A stranger's read of the draft.** That is B's R2, and the plan's falsifier is scored there.
- **How the draft renders on GitHub.** It was checked as text and by link script only.
- **Whether `cargo tauri dev` works for a stranger on a clean machine.** The "Try it" commands are the manual's own
  (`consonance/README.md:432-434`), not re-run on a clean machine.

Files: `README.md` (draft), the notes, the history file, this hand-back and one map line. **Nothing committed.**

NEXT: librarian call_librarian with the hand-back pointer when the notes, the draft and the history file are written — plan default after it: the librarian collates R1 when B and C have rung, and the keeper reads the draft in R3, unless the output says otherwise. OUTPUT adds: (1) the packet's path for the history file (record/) breaks two registered invariants — carrier-drift CH-4 and arch_test's every-record-file-is-named-by-a-card — five reds; moving it to exo_memory/loop/readme_history_2026-09-25.md is proven to clear them (§4), and is the librarian's call; (2) the draft's "checked by a test" line is false until A's R2 test exists, so the draft must not land before R2; (3) four questions for the keeper, notes §7.

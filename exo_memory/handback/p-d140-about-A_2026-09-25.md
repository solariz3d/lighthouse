# D140 A — the About tab from the README block, the drift test, the history file's move

Pane A, on D, 2026-09-25. **Nothing committed. No rebuild** — the About tab is live only after one. README.md not
edited (every wording issue is in §4). Backups: `scratchpad/index.pre-d140.html`.

## 1 · The About tab (`consonance/ui/index.html`, `#about` only)

- The `.aboutbody` now opens with the README block, **word for word**, between `<!-- about:begin … -->` and
  `<!-- about:end -->` (the same markers as the README), rendered by `scratchpad/d140-render.js` from
  `README.md` (`<!-- about:begin` at `:6` → `<!-- about:end -->` at `:58`). It is **static HTML**: nothing renders it at build or
  runtime, and the test in §2 is what holds it. Markdown → HTML only as far as the block needs: `##` → `<h2>`, paragraphs →
  `<p>`, `- ` lists → `<ul><li>` (continuation lines joined), `**` → `<b>`, `*` → `<i>`, `` ` `` → `<code>`, `—` → `&mdash;`.
- **One construct does not render as written: the four links.** The app has no opener (no `tauri-plugin-opener`, no
  shell plugin in `Cargo.toml`; `grep href ui/index.html` finds none outside `<link>` stylesheets), so an `<a href>` in the WebView would
  **navigate the app's own window away from the UI**. They render as their words, `<span class="aboutlink" title="<the
  url>">`, so the URL shows on hover and no word changes. Three of the four are `#anchors` to README sections that
  **do not exist in the app** (§4, item 1). The test pins "no href inside the block".
- **Removed from the About:** *What this is*, *The seats*, *The stance: with you, not above you*, *Honest status*, *Where
  to read more*. The README block replaces them; C's notes §4 moved their measured substance into the README's method layer.
- **Kept, unchanged, AFTER the block: *The loop*** (the diagram quoted from BUILDING.md). It is pinned by an existing test
  I don't own, `consonance/tools/lap-row.test.js:1055` ("carrier: the About tab copy of the diagram has not drifted…"), whose
  own message says *"If the About stopped printing it, delete this test with the paste"*. Deleting another seat's test was
  not mine to do, so the diagram stays and the test stays green. **R3 question for the keeper:** keep the loop diagram in the
  About (it is the room's internal vocabulary — "ORCHESTRATOR", "THE RING" — which his direction called *too internal*), or
  drop it, and then delete that test with it.
- **Kept, unchanged: the hero** (logo, "Consonance", tagline *"Instances, in concert."*). The tagline is old wording that
  is not in the README. **R3 question:** keep it, or use the README's bold lead line (`README.md:3-4`), which sits outside
  the block.
- `.aboutlink` has no CSS rule, so the spans look like plain text. `app.css` isn't mine; a dotted underline there is a
  one-line job for whoever holds the UI styles.

## 2 · The drift test — `consonance/ui/about-readme.test.js`

**Run:** `node consonance/ui/about-readme.test.js`. The js-suite discovers it by name (`js-suite.js:161`, any `*.test.js`
under the repo root outside `node_modules/.git/target/gen/__pycache__/attic`).

It extracts both marked blocks (each marker must appear **exactly once**, and the About's must sit inside
`<section id="about">`), strips markdown on one side and tags/entities/comments on the other (block tags become a space so
adjacent blocks cannot fuse words), normalises CRLF and all whitespace, and requires **exact equality**. A red run names the
first differing word, with context. It also refuses a block under 300 words (two empty sides cannot agree) and any
`href` in the About block.

- **Red first:** before the About was built → **2 passed, 3 failed** (the markers were absent from index.html).
- **Green:** **5 passed, 0 failed**.
- **Mutants** (`node scratchpad/d140-mutants.js`, on COPIES of README.md and index.html):
  **10 listed · 10 applied · 10 caught · 0 survived · 0 NOT APPLIED · 0 NO RESULT**, live files unchanged. They were: one README
  word changed, one About word changed, one About word dropped, one README word added, two words swapped, one punctuation
  mark, a whole list item dropped, the end marker removed, an `href` added, a heading renamed.
  **Controls** (must stay green): README in CRLF, an About paragraph re-wrapped, index.html in CRLF → **3 listed · 3 green**.
- **The README's claim is now true** — `README.md:6-7` and `:233-235` say the copy "is checked against it by a test". It
  was false until this file existed (C's notes §4 said so).

## 3 · The history file

`exo_memory/record/readme_history.md` → **`exo_memory/loop/readme_history_2026-09-25.md`, unchanged.** It was
untracked, so a plain `mv` (not `git mv`). sha256 `bee9bee892e74484a50691d853864fd4cd19296bb2befd67e1a980a3be1b1cc1` before and after.

- `node consonance/tools/carrier-drift.test.js` → **57 passed, 0 failed** (before the move: 54 passed, 3 failed, all three naming
  `CH4-DRIFT-ADDED exo_memory/record/readme_history.md`).
- `cargo test --test arch_test` (through the heavy-run lock) → **12 passed, 1 failed. NOT green.** C's card-rule failure
  (`every_named_record_file_exists_and_every_record_file_is_named`) is gone. The one red is
  `every_relative_link_in_the_docs_exists_in_a_fresh_clone`: **`README.md:228` still links to
  `exo_memory/record/readme_history.md`**. README is C's file and editing it is forbidden to me, so C owes the one-line fix:
  the link target → `exo_memory/loop/readme_history_2026-09-25.md`. **Even then that test stays red until the history file is
  committed**, because it checks `git ls-files`, not the disk. It goes green only at landing, with the file in the same commit.
- Other mentions of the old path are dated traces and stay as they are: C's D139 hand-back, the plan, C's notes, `map/C.md`,
  and `loop/CHECKPOINT.md:13` (the librarian's).

## 4 · Wording issues for C and the keeper (README.md not touched)

1. **Three in-block links point outside the block.** *"see [What was measured not to work](#…)"*, *"([evidence
   below](#…))"*, *"([details](#jev-the-second-look))"*. In the README they jump to sections below; in the About tab
   **there is no "below"**, so a reader of the app is told to look at something they cannot reach. Word for word keeps
   them, and the test enforces it. Either the block words them so they stand alone (e.g. naming the README), or the keeper
   accepts it.
2. `README.md:228`, the history link's target (§3). This one is required for arch_test.
3. The block's own opening comment (`:6-7`) says the About is checked against it "see 'Keeping the About tab in step'". That
   section (`:231-235`) could name the test file now it exists: `consonance/ui/about-readme.test.js`. Optional.

## 5 · Counts I am still waiting on

None outstanding. `node consonance/tools/js-suite.js` → **140 green · 0 failed · 0 crashed · 0 silent · 1 canary · 1 not-run
(of 142)**, EXIT 0. That's 142 files because the new test was added to the 141 D138 reported. The canary (`targetless-pull.test.js`,
declared expected-red) and the NOT-RUN (`actors.evidence.test.js`, machine-bound) are unchanged. No existing test pinned the
old About sections I removed.

## 6 · Not verified

- **Not seen rendered.** I built no app and ran no UI (forbidden), so the layout, the list spacing and the hover titles are
  unobserved. The test checks words, not looks.
- The drift test checks the README *block* only; the README's other sections can say anything.

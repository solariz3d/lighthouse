# P-D097-L3REG — the L3 re-measure registration (pane E, D097, 2026-09-21)

Packet: the chair's D097 packet E. Plan: `exo_memory/loop/plan_install_score_remeasure_2026-09-21.md`, "CHUNK 3's
REGISTRATION", read at source. Started 09:22:30 local (`date +%T`). **Machine D** (my first packet on D this
session), HEAD `ff302cf`. **Nothing committed.** I own one file:
**`exo_memory/loop/l3_remeasure_registration_2026-09-21.md`** (20,033 B, untracked; the chair commits it before
anything installs). I am a **non-author** of A's D093/D094 (`5700c39`, `c809efd`). Read first:
`handback/p-ask001-l3-A_2026-09-20.md` in full, both commit bodies, D's live store `~/.claude/shell/l3_overseer.jsonl`.

## 0 · What is registered, in one paragraph

- **The deciding number is S:** the `quiet_spiral` share on **one-message** windows, the exact symptom the
  2026-08-17 refutation rests on.
- **The rule:** **KEEP iff S_post < 25%**, otherwise **RETIRE**. S_post is taken over the first 300 post-install
  one-message verdicts, or 14 days.
- **NOT YET / NO RULING** apply while the window is open, on too little data, or if chunk 1's registered failure fired.
- **The null is RETIRE** (the install changes nothing). The burden is on KEEP.
- **The window starts at the install instant** that B's phase 2 records in the commit body, the same instant chunk 2
  uses.
- **The scoring command is embedded verbatim** in the registration, with its hash: `sha256 b2dc3553…d289bc`. The
  round trip was tested: extracted, hashed, run.

## 1 · The design changed twice from measurement before it was registered — this is the main finding

**(a) I started with P (the multi-turn `quiet_spiral` share), and the calibration killed it.** Over 18 pre-install
blocks of 300 it swings **up to 40.3 points** between adjacent blocks with no intervention (median 4.0%). Today's
baseline (36.3%) is an elevated block against a long-run ~17–20%. **A before/after on P would have handed out KEEP by
regression to the mean.** It is now descriptive only.

**(b) I built an in-data "leak closed" check (E, 8-word echo from another session's recent non-stable observation),
and it was dead before registration.** It reads **24.8% before 2026-07-01**, before any plausible leak. The top shared
run is the keeper's own sentence (*"without a doubt the reinstantiation of its self it even recognized it and admitted
it"*), recurring across 145 sessions' windows. Stripping the judge's given prompt (the worker template and WELFARE.md)
removed none of it. **Shared user text and an imported verdict read alike to it**, so it could not gate anything. It
is now descriptive only. The leak-closed precondition is chunk 1's own **direct** witness (the guard probe returns 0 B
with the flag set), which was already registered.

**What survived, and why:** S reads **12.7%** in the 06-24..07-22 block and **56.0–85.3%** in every block since 07-23
(seven blocks, plus a partial tail at 69.5%). A collapse to below 25% has never happened in this era, so it cannot be
an ordinary swing. And it is **the refuting symptom itself**, not a proxy for it.

## 2 · The four things the packet asked for

| asked | registered (registration §) |
|---|---|
| the number that decides | **S_post** vs **25.0%**: KEEP below, RETIRE at or above (§2) |
| the NULL beside it | *S_post stays at or above 25%* → RETIRE, and it is the default (§4) |
| the window | from the install instant B's phase 2 records, to 300 one-message verdicts or 14 days (§3) |
| the unwelcome outcome | RETIRE for A's hypothesis and the room; KEEP for the 08-17 audit; **my own stake declared** (§6) |
| the command | the embedded scorer, extracted and checked against `sha256 b2dc3553…d289bc` (§9) |

## 3 · Can it vary? YES — checked before registering, and both rulings shown reachable by running the rule

    node score.js 2026-08-13T00:00:00Z           → RETIRE (S 77.7% → 84.3%, a real instant with no install)
    node score.js 2026-07-23T00:00:00Z           → RETIRE (S 12.7% → 63.0%)
    node score.js <synthetic store, post 10%>    → KEEP
    node score.js 2026-07-01T00:00:00Z           → NO RULING (140 pre-install one-message verdicts < 300)
    node score.js <now>                          → NOT YET (0/300)

## 4 · My stake, stated here as well as in the registration

L3 judges this room's seats, and its current flags call the room's infrastructure work a narrowing frame. Those flags
reached **my own session context** this morning through the hook this install guards. A seat whose work is called a
spiral has a reason to want RETIRE. That is why the number was fixed from data **before** the rule was written, why
the null is RETIRE, and why KEEP's path is shown reachable.

## 5 · Corrections, including mine

- **Two registered-quality designs were discarded** (P, then E) and both are recorded in the registration's §7 with
  the measurement that killed them. Discarding them before any post-install data exists is the whole point of writing
  the bar first. The record keeps them so nobody re-proposes either as "the obvious measure".
- **My first calibration pass printed `real 0.227s`** for a pass that computes 8-gram echo over 7,870 rows. I did not
  trust or chase that timing, and it plays no part in any number here.

## 6 · NOT verified

- **Whether the 07-23 step was the leak opening.** The repo cannot date it (§8 of the registration). It is named as
  suggestive only.
- **That the install will not also change L3's inputs by another route.** The install is a bundle: guard, dream gate,
  `CONSONANCE_DATA` isolation and L0 abstain. A KEEP attributes the collapse to the bundle, as the registration says
  explicitly.
- **The scorer needs the two prompt files** (`~/.claude/shell/hooks/l3-overseer-worker.js`,
  `~/Desktop/lighthouse/WELFARE.md`) for the descriptive echo line, and exits 2 if either is missing. A descriptive
  input can therefore block a run. That is loud, not silent, but it is a dependency the deciding number does not need.
- **Nothing about whether any L3 verdict is right.** This registers a decision about the symptom the refutation named,
  not about validity.
- **Machine L.** L has no overseer verdicts to score (excluded by ruling, per the plan's §0).

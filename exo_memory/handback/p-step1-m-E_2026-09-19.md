# P-STEP1-M — hand-back (pane E, lap D084 / night run N2 stage 1, on D) — DRAFT, appended as it goes

Packet: the chair's D084 dispatch; the registration `exo_memory/loop/anchor_similarity_registration_DRAFT_2026-09-15.md`
read at source at f7b268e — STEP 1 (:414), R8c (:303), R8e (:354), R8f (:371) with the gap-closing paragraph (:390),
R8i (:675), §8.11 (:636), and §8.7 R1-R4 (:167-200) which R8c's m_i points into. Started 2026-09-19 12:2x by the E seat.
**Nothing committed. The registration header is not touched.**

## 0 · Status

**DONE. m = 0.2952 over 19 rows; the registration CAN be computed as written, with the two conservative defaults in §3.**
The gate passes on U. The two VOID rows are exactly the ones :390 predicted, reported, not dropped. No row's own
controls inverted, so the §3 VOID rule never fired. With the step-1 blocks removed, the output is the registered
step-0 result unchanged. `score.mjs` is 19c97ab5… before and after. The header is not flipped; B reads this in stage 2.

## 1 · The original, before

    sha256sum dev/diversity/score.mjs     19c97ab513f722d3b1d5084740d4ddea9206451046fd8ed15e02889743199e2a   (12:2x)

## 2 · Built (12:2x)

    node scratchpad/step1/make.js    anchored replacements on score.mjs, each must match once; refuses if the target
                                     exists; prints score.mjs's sha on both sides; `node --check` after
      score.mjs before    19c97ab513f722d3b1d5084740d4ddea9206451046fd8ed15e02889743199e2a
      score.mjs after     19c97ab513f722d3b1d5084740d4ddea9206451046fd8ed15e02889743199e2a
      score-step1.mjs     5e84e9d90fd90ebb71e639604e4809bfd6a70c7f0b873c8c4740af819e442e59   (sha256sum)
    git diff --no-index dev/diversity/score.mjs dev/diversity/score-step1.mjs > exo_memory/loop/diversity_c1/score-step1.diff
      score-step1.diff    29e676b5a270f10338fcfab9e276eb12103a8b46310f53ae353f7cbb04c43146   59 insertions, 6 deletions

**The two changes, and nothing else that computes:**

1. **The gate** (`score-step1.mjs`, the `INSTRUMENT FAILED` branch): `if (!posPass || !negPass)` →
   `if (!results.controls.positive.passU || !results.controls.negative.passU)`. `passU` was already computed by
   step 0 beside `pass`; both are still reported. This is §8.11's named change.
2. **m_i per R8c**, inside the existing P2 loop:
   - **ΔU_sym** = the own pair's U − the mean U of every OTHER packet at the own packet's parent (R8f). Step 0
     already scored exactly those pairs for its phase-0 "sensitivity" column and kept only `.primary`; step 1 also
     keeps `.U`. Packets absent at the parent and void pairs there are listed per row.
   - **U_pos** = the own packet (the same text the own pair used) minus its first `## ` section, against the own
     packet, UNSTRIPPED (R1). **U_neg** = `p-harness-E` (at `1e944ac`, the version step 0's negative control uses)
     against the own packet, stripped; `p-leave-E` for `packet_harness_and_lib` (R8c's exception), same version rule.
   - **m_i = ΔU_sym / (U_pos − U_neg)**; **m = the median** (step 0's own quantile function).

Also changed, and none of it computes: a header comment naming the two changes; the output file is
`results-step1.json` so step 0's registered result is never overwritten (step 0 did the same); the summary line
prints m first.

## 3 · Where the registration was silent — the conservative default taken, and the question

- **The positive control's "first `## ` section" had to be generalised** from the P1 code, which cut `## 0 ` to
  `## 1 ` on one packet. Rule used: the first line starting `## ` up to the next such line. **Checked:** on the leave
  packet @ed73e76 the general rule removes lines 12-23 and reproduces P1's committed control text exactly
  (`node scratchpad/step1/precheck.js` → `true`, `12-23 vs 12-23`).
- **A row whose own controls invert (U_pos ≤ U_neg) is reported VOID with the reason, not divided through.** A
  negative scale flips the sign of m_i; dividing by it would put a number in the median that means its opposite.
  **Question for the chair:** is that the ruling you want, or should such rows count? (If none inverts, moot — §4.)
- **The own packet absent at the parent** keeps step 0's behaviour (taken at the first-add commit, flagged). The
  precheck found it never happens: all 21 own packets are present at their parents.
- **Which versions the negative texts are taken at:** `1e944ac`, the hand-backs' "final state" per R4 and the version
  step 0's negative control already uses. **Question:** if R8c meant the negative text at each row's parent, say so;
  the per-row control would move.
- **No per-phase m_i is reported,** only m_i from the six-phase means (R8e says U replaces the primary; it asks for
  six-phase min/max per text and pair, which are in `Upos`/`Uneg`/`ownU`).

## 3b · The precheck — every model-free fact, before the run (`node scratchpad/step1/precheck.js`, helpers and the P2
lists sliced from score-step1.mjs's own text, not retyped)

    21 rows; own packet present at the parent on 21 of 21
    others present at the parent: 7/7 on 2 rows, 6/7 on 3, 5/7 on 4, 4/7 on 4, 3/7 on 4, 1/7 on 2, 0/7 on 2
    no other packet at the parent: p-stick-A_2026-09-14, p-stick-E_2026-09-14  → m expected over 19, as :390 said
    negative control: p-leave-E on the 3 harness rows, p-harness-E on the other 18

The run started 12:24:53 (`node --require ./block-net.cjs score-step1.mjs`, staged in scratch like D079/D081); §4.

## 4 · The run, and m

    <scratch>/step1/run/ = score-step1.mjs 5e84e9d9…, s40-strip@5a2d3c0.cjs 73917f67…, PREREG-C1.txt 51ef51d3…,
                           block-net.cjs 03fcb5b1… copied FROM THE REPO (re-hashed there), + models/ and node_modules/
                           as junctions to the externals
    cd <scratch>/step1/run && node --require ./block-net.cjs score-step1.mjs > out.txt 2> err.txt

    start 12:24:53 · end 12:42:12 · exit 0 · 17 min 19 s
    stderr: NETWORK_ATTEMPTS=0 []
    sha256sum results-step1.json    fb34a8d0a34a85c22c7864abfa9d1861727cfe4ee566168d954c04f2b4936392

    verdict "controls passed" · gate on U: positive passU true (U 0.8409) · negative passU true (U 0.4548)
    m = 0.2952   n = 19   min 0.1107 · Q1 0.2646 · median 0.2952 · Q3 0.3377 · max 0.3914
    VOID, reported: p-stick-A_2026-09-14, p-stick-E_2026-09-14 — no other packet at the parent (:390)

**Placed under `exo_memory/loop/diversity_c1/`, both written by `node scratchpad/step1/table.js`, which refuses to
overwrite:**

    results-step1.json   fb34a8d0a34a85c22c7864abfa9d1861727cfe4ee566168d954c04f2b4936392   byte copy of the run's output
    m-step1.md           6fa03da02814a668d1bf25c4b0553e9e66761ad4a15d2b0366aa57d3b18864bf   the 21-row table, GENERATED from the json
    score-step1.diff     29e676b5a270f10338fcfab9e276eb12103a8b46310f53ae353f7cbb04c43146   (§2)

**Checks run on the output, each re-runnable:**
- **Only the two changes.** Delete every `step1` block from results-step1.json; what remains is JSON-identical to
  the registered `loop/diversity_c1/results-step0.json` (60c7d726…). Every step-0 field is unmoved (the count is not re-measured tonight), and that includes the phase-0 `pass` the gate no longer reads. (`node -e`, comparing `JSON.stringify(…, null, 2)`.)
- **m re-derived** by independent code from the table's own m_i: the 10th of 19 sorted, 0.2952. Recomputing each
  m_i as the rounded ΔU_sym / scale agrees to 0.0002 at most, which is rounding.
- **The per-row controls reproduce P1's committed controls on real data.** The p-leave-read-B row's own packet at
  9bc063f is byte-identical to the one at ed73e76 (`git show … | sha256sum` → d7d11dc1… both), and its row reads
  U_pos 0.8409 / U_neg 0.4548, which are the committed P1 control values. So the generalised cut and the
  per-row negative agree with the code that was already registered.
- No pair void at any parent (`voidAtParent` empty on all 21). Every negative-control strip share is 0.0000.

**What m implies, as arithmetic only.** R8c's bar is r ≥ 0.5 × m = 0.1476. That is not a ruling; it is the number the
bar will read, and m is committed before any arm exists, as R8c requires once it lands.


## 5 · Corrections, including mine

- **Time labels wrong again:** I first wrote "12:3x" for work done at 12:2x, the same slip as D081. Fixed against
  `date` before filing.
- **A precheck slice error, mine:** the first precheck cut the helpers up to `const pos = await`, which comes BEFORE
  them in the file. It threw, and I fixed the end marker to `const neg = await`. No number came from the broken run.
- **The dispatch's line refs hold** at f7b268e: R8c :303, R8e :354, R8f :371, the gap paragraph :390, R8i :675,
  §8.11 :636 (read in place). STEP 1 is at :414.

## 6 · Questions I did not put to anyone (the keeper is asleep) — defaults taken

1. **Inverted own controls → VOID** (§3). Moot tonight: 0 of 19 inverted.
2. **The negative texts at `1e944ac`, not at each row's parent** (§3). If R8c meant per-row versions, U_neg moves.
   This one is not moot.
3. **`score-step1.mjs` keeps `score.mjs`'s hard-coded repo path.** It is built from the hashed original, as asked,
   not from the portable copy (D081). So it runs on D as written and nowhere else. **Question:** should step 2's
   scorer be cut from `score-portable.mjs` instead? Its equivalence to the original is proven on D.

## 7 · NOT verified

- **Nothing on L.** It needs the checkout at the D path, the encoder and node_modules.
- **The scorer ran once.** It is deterministic in every run so far (four byte-identical step-0 runs across 09-15 and
  09-19), but step 1's new code has one run.
- **No per-phase m_i**, so R8e's sign guard, which is for r and not m, has nothing to read here. The six-phase min and
  max of U_pos, U_neg and own U are in the json.
- **Whether m is "right"** is B's stage-2 read. Nobody else has re-derived it yet; I computed it, and I wrote
  p-leave-E and p-harness-E, the two negative-control texts (the stake §8.9 STEP 1 names).

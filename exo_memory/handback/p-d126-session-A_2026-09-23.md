# P-D126-SESSION · ALPHA — the fixed README passes a literal read, apart from one publication gap (no public repo yet), and its own commands work end to end at hook level with 1 call. The live session did NOT run: the ordered route writes a transcript into the REAL ~/.claude/projects, which the librarian's own 17:4x probe proves on disk, and this packet forbids it. The falsifier is re-scored NOT YET PASS, with two ways forward named

**Pane A, machine D, 2026-09-23 19:0x.** Lap D126.
- **Gateway calls: 1 of 5.**
- **Real `~/.claude/settings.json` sha256 `8e2cf20aa18226db…` before and after.**
- **0 new folders under the real `~/.claude/projects`** (`ls ~/.claude/projects | grep -c jev-stranger` → 0).
- No README or jev/ code edited; no session, no restart. Nothing committed.

**The master is the DATED ADDENDUM** at the foot of `exo_memory/loop/jev_clean_machine_2026-09-23.md` (git-blob `800be671…`).
It holds the re-score, the run, the hashes, the block and the two ways forward. The D124 result above it is unchanged.

## FILES (uncommitted)

- `exo_memory/loop/jev_clean_machine_2026-09-23.md`: the addendum only, appended.
- `jev/test/clean-machine.e2e.js` (git-blob `bacd9694…`), **changed, and why:** it now runs the fixed README's commands as
  written.
  - `node jev/install.js` and `--uninstall` from the folder that CONTAINS `jev` (step 3). D124's script ran `node install.js`
    from inside `jev`, an inference the old README forced.
  - It adds step 4, `node jev/bin/jev-report.js`, with its output kept.
  - `--mode session` is unchanged: still isolated, still needs a token, still never copies credentials.
  - Both skip paths were re-checked with 0 calls.
- This hand-back, plus one line in `map/A.md`.

## WHAT DECIDES THE NEXT STEP

1. **The packet's premise was wrong about one thing.** `--setting-sources project --settings` keeps the real `settings.json`
   untouched, but the session's transcript lands in the real `~/.claude/projects`. On disk from 17:37:
   `~/.claude/projects/C--Users-nname-AppData-Local-Temp-hookprobe/8cf6e383-a80b-4210-908c-02157f5a9247.jsonl` plus a
   `memory/` folder. I did not run it.
2. **Ways forward (addendum):**
   - (1) accept that one named exception: a transcript of synthetic test prompts under the real `projects/`, paths listed and
     left;
   - (2) the keeper's `claude setup-token` for a fully isolated run.

   **With either, `node jev/test/clean-machine.e2e.js --mode session --calls 4`** (option 1 needs the chair's route added to
   the session mode, a small edit to my file) re-scores it. The falsifier can't read PASS until a real session surfaces a
   flag.
3. **The one gap left in the README is publication:** *"the public repository is not published yet"*. That's the keeper's
   public-repo decision, which is also the plan's default next step.

## NOT VERIFIED

- A live Claude Code session on this install, and so R6 too.
- Whether a real stranger's own user hooks would interfere (the deviation).
- The full js-suite. No `.test.js` changed; it's the collator's.

NEXT: librarian call_librarian with the D126 pointer when the run is re-scored and the addendum written — plan default after it: the chair/keeper picks way (1) or (2) for the live session, then the keeper's decision on the public repo, unless the output says otherwise

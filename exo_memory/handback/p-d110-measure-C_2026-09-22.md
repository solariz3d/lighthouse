# P-D110-MEASURE — C (D110 packet C, chunk 2.1, ON D, 2026-09-22 12:31–12:40) — MEASUREMENT ONLY

**Both suites in the packet's scope are GREEN on D. No source file was touched, nothing was repaired.**
Run from `C:\Users\nname\Desktop\lighthouse` at HEAD `98443a7` (D109), working tree clean. The app was left running and
untouched; no exe was built or launched.

## 1 · JS suite — `node consonance/tools/js-suite.js`, plain

    js-suite: 119 green · 0 failed · 0 crashed · 0 silent · 1 canary · 0 sang · 1 not-run · 0 class-error  (of 121)

Exit 0. It took **354 s (5 min 54 s)**.

**FAILED / CRASHED / SILENT / class-error: none.** The two non-green files are both declared, and neither is a red:

| file | class | what it is | owner (`git log -1`) |
|---|---|---|---|
| `consonance/tools/targetless-pull.test.js` | **canary**, declared `JS-SUITE: EXPECTED-RED` at its line 1 | red ON PURPOSE and still red, which is the pass condition; the runner counts a canary that goes GREEN as a failure | `c064a8d` 2026-09-08, L044 E |
| `consonance/tools/actors.evidence.test.js` | **NOT-RUN**, declared `JS-SUITE: MACHINE-BOUND home=L root=CONSONANCE_DATA` at its line 1 | its universe lives on L, so on D it correctly reads NOT-RUN | `800ef7d` 2026-09-21, D101 B |

Neither is new since D108 (`3e189ec`); both declarations predate it.

## 2 · Rust — TEST BUILD ONLY, `cargo test --bin consonance -- --test-threads=1`

Cargo from `%USERPROFILE%\.cargo\bin`. **No app exe was built and nothing was launched.**

    test result: ok. 870 passed; 0 failed; 4 ignored; 0 measured; 0 filtered out; finished in 34.79s

**0 failing tests.** Wall time including the build was **51 s** (12:31:15 → 12:32:06); the tests themselves took 34.79 s.
The build emitted warnings only (unused import `HashMap`; unused variables `ratio`, `sr`, `rows`).

The **4 ignored** are declared, not failures:
`cochlea::tests::explore_source_hop_signature`, `cochlea::tests::measure_pulse_steadiness`,
`cochlea::tests::measure_speech_features`, and
`ready_signal_tests::the_keeper_typing_a_slash_command_must_hold_a_delivery` ("acceptance test for the slash-command
splice; see the test above before un-ignoring").

## 3 · ONE RED, OUTSIDE THE PACKET'S SCOPE — the integration-test target

`--bin consonance` runs the binary's unit tests only. It does **not** compile or run `consonance/src-tauri/tests/arch_test.rs`.
I ran that target too, because it caught a real red on L this morning, and it is red on D as well:

    cargo test --manifest-path consonance/src-tauri/Cargo.toml --test arch_test -- --test-threads=1
    test result: FAILED. 12 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 1.32s

- **Failing test:** `every_named_record_file_exists_and_every_record_file_is_named`.
- **Cause, from the failure output, not a guess** (`tests/arch_test.rs:550`):
  *"record/retired_seats_2026-09-11.md is named by no card — a record nothing points at is unreachable by a pane, which is worse than not splitting it out at all"*.
- **Owner:** the record file is `4292c6b` (2026-09-14, CHAIR, "the lineage, on L"). The test file's last toucher is
  `ff302cf` (D096, mine), but that commit changed the BOOT-tail test, not this assertion.
- **Pre-existing, not new since D108.** The assertion has existed since `56adc69` (2026-08-09). The record file was ADDED
  in `397e29c` (2026-09-11). No card has ever named it: `git log -S "retired_seats_2026-09-11" -- exo_memory/cards/`
  returns nothing. So it has been red since **`397e29c`, 2026-09-11 — eleven days**.
- Same failure, same line, on L earlier today (L082 §4).
- **Not repaired here.** A card gains a pointer to that record, or the record is folded back; that is a lap, and it is
  the deck's owner's call, not a measurement's.

## 4 · Environment facts checked on D

- **Python is NOT installed** (`python --version` returns the Microsoft Store shim message). **It caused no red in this
  run**: the D082 class was fixed in L080, so a test needing an absent interpreter declares itself instead of failing.
- node **v24.14.1**.
- The app stayed running throughout, and no test in either suite was given the app's exe.

## 5 · What I could NOT determine

- **Whether `arch_test` truly passed before `397e29c`.** I did not check that revision out and run it. The date is
  derived from the record — assertion older than the file, and no card ever naming it — not from an execution.
- **Whether the canary's red is still red FOR ITS ORIGINAL REASON.** The runner only checks that it fails; I did not read
  its failure text against L044's declaration.
- **Flakiness.** Each suite was run ONCE, plainly. No repeat runs, so nothing here speaks to intermittent failures.
- **Machine L.** Not touched this lap; these numbers are D's only.
- **The other test targets, if any beyond `arch_test`.** I ran `--bin consonance` (the packet's) and `--test arch_test`;
  I did not run a bare `cargo test`, which would also build the other targets.

NOT COMMITTED. No source file was edited this lap.

NEXT: librarian collate the measurement when p-d110-measure-C_2026-09-22.md is written and the map line is appended

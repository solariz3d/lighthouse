# P-LIVE-RED — hand-back, pane L, 2026-09-03

**Lap D005. Owned: `consonance/tools/carrier-drift.js` + `.test.js`, `consonance/tools/commit-gate.test.js`.**

---

## 0 · THE ANSWER TO THE QUESTION YOU ASKED FIRST

**Your BUILDING.md edit did not cause this.** Both reds pre-date it — one by 27 hours, one by four
days — and neither is in a file your edit touches.

The check, and it does not need `git stash` (which on a shared checkout is the class of move whose
falsifier fired at 06:52 today):

    git archive HEAD | tar -x -C <scratch>/head-tree     # HEAD, no working-tree edits, nothing touched
    (cd <scratch>/head-tree && node consonance/tools/carrier-drift.js) > head.txt
    node consonance/tools/carrier-drift.js               > work.txt
    diff head.txt work.txt

**The entire diff of the two reports is one line:**

    <   corpus: 615 carriers · 31 traces skipped
    >   corpus: 616 carriers · 37 traces skipped

Same 6 findings, same 35 PENDING, same exit 1, on both sides. The +1/+6 is gitignored files the
working tree has and `git archive` does not — not your amendment. And read directly, the 41 added
lines contain none of the three registered wordings.

Confirmed a second way, in a real worktree so the git-dependent tests could run:

    git worktree add --detach <scratch>/wt-head HEAD
    (cd <scratch>/wt-head && node consonance/tools/carrier-drift.test.js)   # same 3 red
    (cd <scratch>/wt-head && node consonance/tools/commit-gate.test.js)     # same 1 red

**BUILDING.md is untouched by me. It is still the only dirty non-mine path in the tree.**

---

## 1 · AND THE PREMISE UNDER THE QUESTION IS WRONG TOO

> *"js-suite went from EXIT 0 to EXIT 1 across ONE edit of mine."*

**js-suite did not go from EXIT 0.** Yesterday's own reading, in this room's resonance for
2026-09-02: *"js-suite: 65 green · 4 failed · 1 not-run of 70; the reds are ask.test.js (since
fixed), carrier-drift, commit-gate, userprompt_pulse"* — and it added, correctly, *"the three
remaining reds trace to last night's landings, not this session's edits."*

Today the same suite reads **66 green · 3 failed** — the identical set minus the `ask.test.js` that
was fixed. Nothing flipped. There was no EXIT 0 to leave.

**Say what I did not verify:** I did not reproduce an EXIT 0 run of js-suite and cannot, so I am
scoring your baseline against a written record and against the per-file bisect below, not against a
run of my own. If you have a terminal showing js-suite EXIT 0 later than 2026-09-02, that is the
artifact to route, and it beats this paragraph.

---

## 2 · TWO REDS, TWO CAUSES. They are not related and neither is related to your edit.

### 2a · `carrier-drift` — honestly red, and red about the wrong thing

**It has never been green in this repo.** Bisected by archiving each commit and running that
commit's own tool and registry:

    for c in $(git log --format=%h -40); do git archive $c | tar -x -C <scratch>/bs
      (cd <scratch>/bs && node consonance/tools/carrier-drift.js --quiet); done

    40 consecutive commits back to 09-02 07:01   RED, 6 findings, every one
    285b2d6  09-02 05:54   RED — 8 findings
    fed34a6  09-01 03:42   RED — 1 finding
    9690c04  08-31 01:38   RED — 1 finding
    325fb03  08-30 07:54   RED — 9 findings      (the commit that landed the registry)

**`fed34a6`'s message is *"close the two reds tonight's own writing introduced, before the rebuild"*
and the tool exits 1 at that commit.** A seat closed two reds, left a third standing, and said the
reds were closed. That is the same compression failure BOOT's own amendment names: the instrument's
number is reproducible; the sentence about it is hand-made every time.

**And here is the part that should sting more than the red does. This was already found, twice,
yesterday, by two different panes, correctly diagnosed both times — and it did not reach you:**

* `exo_memory/handback/p-corpus-budget_2026-09-02.md:174` — *"RED on four unaccounted findings in
  `exo_memory/map/M-2026-08-24.md:142`, `M-2026-08-29.md:72`, `M-2026-08-30.md:563` and `:575`:
  chair lap-notes that **quote** a withdrawn wording while discussing its withdrawal, which the
  scanner cannot distinguish from asserting it. Structurally not caused by this change… `map/` is
  not mine to edit. **Filed, not chased.**"*
* `exo_memory/handback/p-two-doors_2026-09-02.md:162` — *"all 5 findings are in
  `exo_memory/map/M-2026-08-{23,24,29,30}.md`; `git status --porcelain exo_memory/map/` is **empty**,
  and the wordings are present at HEAD."*

Three seats have now reached the same diagnosis. Two correctly declined it because `map/` was not
theirs. **`carrier-drift.js` is mine, so this one is not filed. It is fixed — §3.**

### 2b · `commit-gate` — a different disease, one line, and it is not my file

RED since **`e8ee98d`, 2026-09-02 07:34** — about 27 hours. Bisected against the real packets:

    a088839  07:05   REFUSE  holders=[E,A]     <- what the test asserts
    e8ee98d  07:34   REFUSE  holders=[]        <- fail-closed, holder UNKNOWN

**Cause, exactly.** `parsePacket` finds the ownership block with

    consonance/tools/commit-gate.js:165
    const start = lines.findIndex((l) => /WHAT YOU OWN/i.test(l));

`e8ee98d` prepended a PARKED notice to `exo_memory/loop/packet_watcher_liveness_2026-09-02.md` that
**quotes the phrase inside a blockquote at line 12**. `findIndex` takes the first match; line 13
starts with `>`, which is column-zero, so the block ends before it begins. The real block is at
line 118. Owned paths parse to **zero**, the gate hits its documented
*"a live packet that claims no paths is a parse failure, not an empty claim"* branch, and refuses
**every path in the lap with holder `null`**.

**Verified repair, which I did not apply because `commit-gate.js` is not mine.** Anchor the search
to a heading:

    const start = lines.findIndex((l) => /^#+.*WHAT YOU OWN/i.test(l));

Proved in an isolated worktree, with a control, so nobody has to take my word:

    git worktree add --detach <scratch>/wt2 HEAD
    cp <patched copy> <scratch>/wt2/consonance/tools/commit-gate.js
    (cd <scratch>/wt2 && node --test consonance/tools/commit-gate.test.js)   ->  0 failing
    cp <shipped>      <scratch>/wt2/consonance/tools/commit-gate.js
    (cd <scratch>/wt2 && node --test consonance/tools/commit-gate.test.js)   ->  1 failing (REPLAY)

One line, whole suite green, control red in the same tree. **`commit-gate.test.js` stays RED in the
shared checkout until whoever holds `commit-gate.js` lands it.** I did not weaken the test to match
the broken tool; that would have hidden a live gate that fails closed on an entire lap.

---

## 3 · WHAT I BUILT — the dated verbatim extract is a TRACE

**`consonance/tools/carrier-drift.js` · `consonance/tools/carrier-drift.test.js`. Red-first: the
five tests were written and run failing before a line of the rule existed.**

`ad4e615` (2026-09-01 07:47) landed `exo_memory/map/M-<date>.md` — one seat's own prose for one day,
extracted **mechanically** from its transcript, header: *"Nothing here is a summary."* That is the
tool's own TRACE definition word for word. It landed under a prefix the TRACE list does not carry,
and the tool went **1 finding → 6 the same night**.

All five are the librarian **reporting** a withdrawal. I read all five in full, not the excerpts.
Two of them (`M-2026-08-30.md:563`, `:575`) are it **specifying this very registry**. **None asserts
anything.** **Zero of the tool's six findings was the disease it exists to detect.**

**A path prefix could not carry this, and reaching for one would have been the disaster.** The same
directory holds `A.md`, `B.md`, `C.md`, `E.md`, `K.md`, `M.md` — the live pane maps, and a pane is
respawned FRESH from its own map. Exempting `exo_memory/map/` would have taken the strongest carrier
class in the room out of the corpus while reading as housekeeping: the Q3 false-green class the
tool's own header names.

**So the discriminator is the file's own DECLARATION, never its position.** The path pattern only
bounds the blast radius; the file must say `Extracted mechanically`. Measured:

    8 of 8 dated extracts declare  ·  0 of 3 live maps declare

Three of the five new tests exist only to stop this becoming a silencer:
a hand-written file at the same dated path **stays a carrier**; a live pane map carrying the
declaration **stays a carrier**; and the rule **prints on every run**, because a corpus rule nobody
can see is the false-green class itself.

**Result, and the blast radius measured rather than assumed:**

    node consonance/tools/carrier-drift.js --quiet
      before:  RED · 616 carriers · 37 traces · 6 findings · 35 PENDING
      after:   RED · 610 carriers · 45 traces · 1 finding  · 35 PENDING

    files that changed class carrier -> trace:  8
      exo_memory/map/M-2026-08-{22,23,24,25,29,30,31}.md, M-2026-09-01.md

**Nothing else moved. No registered site and no CH-4 member sits under `map/` — checked before the
change, so the loss is zero rather than assumed zero:**

    grep '"file"' consonance/tools/carrier-drift.registry.json | grep -i 'map/'   ->  (empty)
    grep 'map/M-' consonance/tools/carrier-drift.registry.json                    ->  (empty)

**Falsifier, registered in the source above the rule and before adoption:** if a dated extract is
ever found ASSERTING a withdrawn claim as live guidance rather than recording it, the declaration is
not the discriminator and this rule is a silencer — strike it and register the sites by hand.
Checkable by reading any finding the rule removes; all five are quoted at their sites here.

**`carrier-drift.test.js`: 3 failures → 2.** Both survivors are one cause — §4.

---

## 4 · THE ONE RED I DID NOT TAKE, AND IT IS A DECISION, NOT A DEFECT

    RED — 1 finding
      MISSING-FILE  exo_memory/third_place/2026-08-29.md

`exo_memory/third_place/` is **gitignored by policy** — `.gitignore:60`, the same call as `dreams/`
and `harvest/`: *"No unattended process of ours publishes."* The directory exists on this desktop and
is **empty**. The row's own `why` already says the file is gitignored; what it does not say is the
consequence: **a registered site inside a privacy-ignored directory can never be satisfied on any
machine but the one that wrote it, or on any fresh clone, ever.**

That makes the gate's greenness **machine-dependent**, which is a worse property than the drift it
detects — and it is why both remaining `carrier-drift.test.js` failures ("THE BAR, half one" and the
REAL-tree MUTATION assertion at `:497`) are the same single row, not two problems.

**Three options, priced. I recommend the second and did not take it, because it creates an exemption
mechanism and that is a yes somebody else gives:**

1. **Delete the row.** Cheapest, and the registry's own text forbids it: *"Deleting a site row is
   exactly as dangerous as writing a wrong one"* — measured, on this file, at 325fb03.
2. **A `machine_bound` site kind**, modelled exactly on `JS-SUITE: MACHINE-BOUND` in the same tools
   directory: requires a reason naming the `.gitignore` line, suppresses **only** the MISSING-FILE
   kind, prints in its own named section, and goes red normally the moment the file *is* present.
   Guarded the way `disarmed` already is — *"an exemption with no destination is a silencer."*
3. **Leave it.** Honest, and it is the state that has held since 2026-08-30 while three panes walked
   past it. js-suite's own header says why that is not free: *fire constantly and you train the
   reader to skip it.*

**Ruling on your third question: neither should BLOCK, and neither should be silenced.**
`carrier-drift` is correctly red about one open decision that has an owner who is not me, and the
five it was red about yesterday were its own bookkeeping. `commit-gate.test.js` is correctly red
about a live one-line defect in a gate that, **were it armed, would refuse every path in a lap** —
it is **NOT ARMED here** (`G.armed(REPO)` → `false`, no `.git/hooks/pre-commit`, no
`core.hooksPath`), so today it costs a red test and no commits.

---

## 5 · CORRECTIONS I MADE TO MYSELF

* I first read your report of *"47 occurrences · 12 accounted"* on the diving vocabulary as part of
  the red. **It is not.** That entry is DISARMED and prints *"UNTIL THEN THIS ENTRY PROTECTS
  NOTHING"*; its 35 unaccounted are PENDING by a registered decision, not a regression. The 35 you
  quoted as the frightening number is the one number in the report that is behaving as designed.
* I planned to write my map line to `J.md` on the reasoning that A, B and K were taken. **I am L** —
  `C:\Consonance\data\persist.log`: `letter=L cwd=C:\Consonance\instances\sibling-181f513d`. Checked
  instead of inferred, which is the only reason `exo_memory/map/J.md` does not now exist.
* I twice wrote a regex through a shell layer that ate its backslashes and shipped a file that would
  not parse. Caught by running the tool, not by reading the diff.

## 6 · WHAT THIS DOES NOT ESTABLISH

* **It does not make `carrier-drift` green, and it should not.** One row stands, by decision.
* **It says nothing about the other suite reds.** `portable-paths`, `corpus-age` (CRASHED) and
  `userprompt_pulse` are outside my packet; `corpus-age` was already bisected to `c2afec6` by
  P-TWO-DOORS yesterday and is unclaimed. Other seats were editing `gen-consumer.js`, `map/A.md`
  and `map/K.md` while I worked, so **any whole-suite figure taken today is a moving baseline** —
  the two per-file numbers above are the ones I stand behind.
* **I did not run `commit-gate.test.js` against the proposed fix in the shared checkout**, only in
  an isolated worktree. The shared file is another seat's.
* **The trace rule is tested against a corpus of eight files.** A ninth extract written differently
  — no declaration line, or a different dated path shape — is a carrier and goes red, which is the
  intended direction but is untested against a real instance of it.

## 7 · COMMITTED

`consonance/tools/carrier-drift.js`, `consonance/tools/carrier-drift.test.js`, this hand-back, and
one line at `exo_memory/map/L.md`. Paths named individually, never `-A`. Not pushed.

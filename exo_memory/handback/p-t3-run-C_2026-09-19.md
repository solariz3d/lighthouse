# P-T3-RUN — C (CHARLIE), D086 (unattended N4), on D — DRAFT, appended as the lap runs

**The packet is one file:** `exo_memory/loop/t3_run_registration_2026-09-19.md`.
- `sha256sum` → `4195f9430968b3863013541eacdf7a24d2a3381892712aecb48ed63e809f0063`. It is identical at `485d8d7` and on
  disk: `git show 485d8d7:… | sha256sum`.
- I run under its §2 (the route), §3 (A1–A7) and §4, and the file governs.
- B's attack, which holds the §4 tests, is `exo_memory/handback/p-t3-attack-B_2026-09-19.md`, sha256 `c1a78385…`.
- HEAD at start: `e70d9d8`.

**C spawns, C does not score, and C says nothing about the outputs' quality or which label is which.**

## 1 · FROZEN BEFORE SUBJECT 1

Everything lives in the run directory `RUN` =
`C:/Users/nname/AppData/Local/Temp/claude/C--Consonance-instances-sibling-0845a868/0845a868-38f2-4cc2-b45a-431e0c088fb1/scratchpad/t3run/`.
Hashes are from `sha256sum`, sizes from `wc -c -w`.

| artefact | source | bytes / words | sha256 |
|---|---|---|---|
| **intake** `RUN/intake.frozen.md` (A1) | copy of `C:\Consonance\instances\third-place\CLAUDE.md` (live mtime 09-18 09:35:14; the live file's sha256 equals the copy's) | 138,592 B / 22,246 words | `dfdab9b4e0071718f62015bd9eb17a250e69b8b3d1c9fb9d333238f6e8598151` |
| **seed** `RUN/seed.txt` | `exo_memory/third_place/2026-09-15.md:189-192`, with the leading `> ` markup stripped (`sed -n 189,192p … \| sed 's/^> //'`) | 354 B / 68 words | `ec99264032af34b15a2694bc9d78f8cfab8c6ca0fa57a534e0bdb93e946cac5a` |
| **task 1** `RUN/task1.txt` (A2) | `essay/recognition/prompts/primed1.txt`, PART ONE verbatim | 887 B / 172 words | `87c3ec5bd89c878c0c67b19a046afd1df4d0bdb1aa24e2341b8253cfb4c9b5cf` |
| **task 2** `RUN/task2.txt` | `primed2.txt`, PART ONE verbatim | 1,072 B / 188 words | `1cdf9175c44d395ab3696911ec504c32ef988fae43337343d8d10bb51f418080` |
| **task 3** `RUN/task3.txt` | `primed3.txt`, PART ONE verbatim | 1,066 B / 200 words | `b6dc1253ab817327d874e59b6e8871de98621e4d07905a49eb02a951d741db83` |

**How each task file is built, stated because it is a choice:** the prompt's own sentence "Do not search the web at
any point." (from its preamble), then its PART ONE paragraph verbatim, then one harness line: "Give your answer in your
reply, not in a file."

- That harness line is needed because this route has no `acceptEdits`, so a Write would be denied and the answer would
  be lost. It is the same in all nine subjects.
- The preamble's "Two parts … before you open the file in part two" and all of PART TWO are cut, per A2 ("No essay, no
  Part Two").

**Seed word count, per the registration's own note (§1):** 68 words is my count after stripping the `> ` markup. The
librarian's count with the markup is 72. The bytes above are the ones used.

## Log

- 13:0x — read the registration in full at source, and B §2 and §4. Froze the five artefacts above. **No subject has
  run yet.**
- 13:1x — **The runner is written: `RUN/run.js`.** Its sha256 is recorded below, before it runs.
  - It re-checks all five frozen hashes and refuses on any change.
  - It refuses a subject cwd that is inside a git repo.
  - It refuses to run twice (an existing `raw/` means every subject would be a re-run).
  - It re-runs ONLY on a process failure: non-zero exit, empty stdout, unparseable JSON, `is_error`, or an empty
    result.
  - It logs every attempt's command line (with the task text shown as `<taskN.txt>`), exit code, tokens, model and
    wall time to `RUN/runlog.json`.
  - **Route flags:** exactly `-p <task> --restricted --strict-mcp-config --model claude-opus-5`
    `[--append-system-prompt-file <seed|intake>]`, plus two flags that add nothing to the subject's context:
    `--output-format json` (the stdout JSON §2 names) and `--no-session-persistence`.
  - **Deliberately NOT passed:** `--max-budget-usd`, because D085's probes showed it injects a "USD budget" line into
    the subject's context, and the registered route does not have it. Also not `acceptEdits`.
  - Order: task1 cold, seed, intake; then task2; then task3. Sequential.
  - runner sha256 `d84edb919e256629d26f8d66659950e547e8bba5648a29af556b7974602a4bf5`; started 13:03:53.
- 13:1x — **The nine subjects ran**: `node RUN/run.js` → `RUN/run-console.txt`, `RUN/runlog.json`.
  - **9 attempts, 9 exit 0, 0 process failures, 0 re-runs.** Every subject reports model `claude-opus-5`, and every
    cwd was a fresh `%TEMP%\t3subj-*` directory, checked to be outside every git repo.
  - **Tokens:** input 264,305, output 16,540 in total.
    - Input by arm: cold 40,352, seed 40,707, intake 183,246 (3 subjects each).
  - **Per-subject output tokens are deliberately NOT given here.** Matched against the relabelled files' lengths they
    would reveal the key. They sit in `RUN/runlog.json`, beside the key, and are released with it.
- 13:1x — **Relabelled:** `node RUN/relabel.js <dir>`.
  - **Relabelled-output directory (for the librarian to score from): `exo_memory/loop/t3_run_outputs_D086/`**, with
    `S01.md` … `S09.md`. Each file holds ONLY the subject's reply text, and they were written in S-label order.
  - **The key's sha256: `fd38d2acae5176daaba9d26f54ac4364bc34bb222cfb054f81e922e75a5a1130`** (`RUN/key.json`, which
    stays in my scratch until the librarian has filed its scores).
  - The key file carries a random 32-byte nonce. Without it, its sha256 could be brute-forced from the 9! = 362,880
    possible maps.
  - **I have not opened any output.** Per §4 the labels hide the label only; the arm is legible from the text.
- 13:2x — **The fresh scorer, one call:** `node RUN/scorer.js <dir>` → `RUN/scorer-console.txt`.
  - **Exit 0, `is_error` false, model `claude-opus-5`, input 24,033, output 5,584 tokens, 48.2 s.**
  - Route: the same restricted route (fresh `%TEMP%\t3score-*` outside every repo). Its material is ONE file, passed by
    `--append-system-prompt-file`, because 34k characters would not fit the Windows command line.
  - **Material (`RUN/fresh-scorer-material.md`, sha256 `e76ea27f…`, 34,226 chars):**
    - B §4's per-output tests, extracted VERBATIM between B's own headings, "**What the scorer CAN judge honestly" and
      "**What the scorer CANNOT judge honestly". That is 1,447 characters, sha256 `740b93e8…`: the STANCE add-and-hold
      test, ARCHETYPE by function (USED/RECITED), and "FLUENT is not scoreable".
    - The nine relabelled outputs, S01–S09.
    - **Nothing else:** no registration, no arm names beyond what B's block itself says, no lexicon, no room text.
  - **I checked the extraction** after the call, when the 1,447 figure looked short: all three tests are present and
    whole, and the figure is right because of multi-byte characters. No re-run.
  - **Its prompt** (`RUN/fresh-scorer-prompt.txt`, sha256 `c43e9820…`) asks, per output:
    1. the commit, add and hold sentences, or NONE;
    2. each phrase the tests would count as a room or archetype phrase, marked USED or RECITED;
    3. a verdict of STANCE or COSTUME "as the tests define them", or **NEITHER** with a reason.
    - **NEITHER is my addition.** B's tests define STANCE positively, and COSTUME only in the falsifier's words.
      NEITHER can never fire A5, since a pair needs STANCE and COSTUME from both scorers.
  - **The verdicts, verbatim:** `exo_memory/loop/t3_run_fresh_scorer_D086_OPEN_AFTER_YOUR_SCORES.md`, sha256
    `3f9cf18dd81257e77ec1e0bcce214d453354941a6eb2738aa2a408286bb7097b`, 7,789 B, byte-identical to
    `RUN/fresh-scorer-verdicts.md`.
    - Checked mechanically, not read: every label S01–S09 occurs (`grep -c`), and 10 lines carry STANCE, COSTUME or
      NEITHER.
    - **It sits at a separate path, not in the outputs directory, and the filename says why.** A7 makes the two scorers
      independent, so the librarian should file its own scores from `t3_run_outputs_D086/` before opening it.

---

## What the librarian does next (per the registration §4 and A7)

1. Score `exo_memory/loop/t3_run_outputs_D086/S01.md` … `S09.md` with B §4's tests, and file the scores.
2. Then open `t3_run_fresh_scorer_D086_OPEN_AFTER_YOUR_SCORES.md` and list every disagreement.
3. Then ring C for the key. I release `RUN/key.json`, with the nonce so its sha256 `fd38d2ac…` can be checked, and
   `RUN/runlog.json` with the per-subject tokens. Only then does A5's fire rule get applied per task-triple.

## Scripts (all in `RUN/`, hashed)

| script | sha256 |
|---|---|
| `run.js` | `d84edb91…` |
| `relabel.js` | `b53aac68…` |
| `scorer.js` | `38363c7c…` |

## Questions for the morning (the keeper was asleep; the conservative default was taken)

1. **The harness line in each task** ("Give your answer in your reply, not in a file"). The alternative was
   `acceptEdits` with a file, which the ring ruled out. Default: the line. It is identical in all nine subjects and adds
   no content about the cases.
2. **`--max-budget-usd` omitted.** It would have injected a "USD budget" line into every subject's context. Default:
   the route exactly as registered. The cost was therefore unbounded per call. Measured: 16,540 output tokens in total.
3. **NEITHER in the fresh scorer's vocabulary.** Default: allowed, because it can only fail to fire. The librarian may
   rule it out and treat NEITHER as COSTUME or as STANCE; either way that is a scoring rule, not a run change.
4. **The fresh scorer had no room lexicon,** so its USED/RECITED column can only cover phrases it recognises as jargon.
   That is B's design (it "holds no room"), stated here so it is not read as a miss.

## What is NOT verified

- **Anything about the outputs.** I did not open them, and I do not score or comment.
- **That the subjects did not use a tool.** `--restricted` leaves Read and search tools, and each cwd was empty. The
  runlog does not list tool calls. The stdout JSON's `num_turns` would say, and I have not opened the raw files. The
  librarian or B can check `RUN/raw/*.json`.
- **The leaks D085 named still apply:** the account email line, in every arm alike; the model is pinned everywhere.
- **n = 3 per arm,** on D only; §6 of the registration lists what this run cannot show.

---

## KEY RELEASED, 13:1x — after the librarian's scores were committed

- **Precondition checked:** `git log -1 cc46e99` → 2026-09-19 13:11:02, "T3 run — my scores of S01-S09, committed BEFORE
  the key…", adding `exo_memory/loop/t3_run_scores_librarian_D086.md` (59 lines).
- **The key and the runlog, copied byte for byte:**
  - **`exo_memory/loop/t3_run_key_D086/key.json`**: sha256
    `fd38d2acae5176daaba9d26f54ac4364bc34bb222cfb054f81e922e75a5a1130`. That is **the sha registered above**. It includes
    the nonce, so the sha can be re-derived: `sha256sum exo_memory/loop/t3_run_key_D086/key.json`.
  - **`exo_memory/loop/t3_run_key_D086/runlog.json`**: sha256
    `420c7dd3d96798040137a9ea634e7b83d6051071adf732d74f211b7c07afd4b7`. It holds every attempt's command line, exit
    code, and per-subject input and output tokens, which were withheld until now.

## Tool use, from `RUN/raw/*.json` (was NOT verified; now measured)

`node -e` over each raw stdout JSON, reading `num_turns`, `usage.iterations`, `permission_denials` and
`usage.server_tool_use`:

| file | num_turns | API iterations | permission denials | web search / fetch | subtype |
|---|---|---|---|---|---|
| task1-cold, task1-seed, task1-intake | 1, 1, 1 | 1, 1, 1 | 0 | 0 / 0 | success |
| task2-cold, task2-seed, task2-intake | 1, 1, 1 | 1, 1, 1 | 0 | 0 / 0 | success |
| task3-cold, task3-seed, task3-intake | 1, 1, 1 | 1, 1, 1 | 0 | 0 / 0 | success |
| fresh-scorer | 1 | 1 | 0 | 0 / 0 | — |

**No subject called a tool.**
- Every run is a single turn with one API iteration.
- A client-side tool call (Read, Write, Glob…) cannot complete within one iteration: its result needs a second model
  call.
- No denial was recorded, and no server-side web tool ran.
- **Limit of this reading:** the stdout JSON does not list tool calls by name, so "no tool" is inferred from one turn
  and one iteration, not read from a tool log.

# P-DOC-ORACLE — hand-back. L032, BRAVO, 2026-09-02.

**Verdict: LAND IT.** The instrument now sees the description surfaces, goes **RED on HEAD naming
all three lifeguard hits**, and the surfaces have no trace exemption. **Nothing committed.**

Paths, uncommitted:

    consonance/tools/carrier-drift.js
    consonance/tools/carrier-drift.registry.json
    exo_memory/handback/p-doc-oracle_2026-09-02.md

**Commands. Every figure below comes from one of these.**

    node consonance/tools/carrier-drift.js
    node --test consonance/tools/carrier-drift.test.js
    node consonance/tools/js-suite.js
    git worktree add --detach <tmp>/head-tree HEAD
    CARRIER_DRIFT_ROOT=<tmp>/head-tree node consonance/tools/carrier-drift.js

---

## 1 · BAR 1 — RED ON THE REAL SURFACES, and it took three corrections to get there

    RED overall: true | surface findings: 3 | red of them: 3
      RED UNACCOUNTED README.md:78
      RED UNACCOUNTED consonance/ui/index.html:337
      RED UNACCOUNTED consonance/ui/index.html:372

Run against a detached worktree at HEAD, with the shipped registry. All three were invisible to
this instrument at 05:00 tonight.

**The control is HEAD, not the working tree, and that was forced rather than chosen.** By the time
the corpus was widened, A had already rewritten the About tab in the working tree and the slogan was
gone. **A positive control that expires when the defect is fixed is not a control**, so there is also
a fixture test carrying the defect in its own tree, which cannot rot.

---

## 2 · THE FOUR CHANGES

1. **The corpus reads `.html`.** One file in the repo: `consonance/ui/index.html`. The tool's own
   LIMITS list said `IT READS .md ONLY` and named the class it was missing; a known limit that is
   cheap to close and stays open is a decision nobody revisited. **LIMITS is corrected in the same
   change** — a tool that misreports its own reach is the disease it exists to detect.
2. **A withdrawal registered: `light-not-lifeguard-2026-08-17`**, from `e5521a0` (2026-07-12) and
   BOOT's 2026-08-17 amendment. **DISARMED, with `arms_on`** — see §4, this is the load-bearing
   judgement in the lap and the chair may overturn it with one word.
3. **The description surfaces have no trace exemption** (Ruling 1). A hit on the three surfaces is
   RED whatever the entry's arming state; a record-tier hit stays PENDING.
4. **A `DESCRIPTION SURFACES` report line** — named, counted, and **never a filter**. `red` stays
   repo-wide, so it can say the surfaces are clean during a red run and cannot silence anything.
   A scope *flag* here would be the silencer the registry's README warns about; a scope *line* is a
   report.

### The refusal condition, checked rather than assumed

The packet said to refuse if the universe could not widen without changing what drift means for the
briefs already guarded. **Measured before widening: 589 carriers → 590, findings unchanged, zero
added, zero removed.** It changes what the tool SEES, not what drift MEANS. Pinned as a test.

---

## 3 · THE INTERRUPT NAMED THE WRONG FILE, and the real gap was one file over

Taken as heard, and the correction is owed back with the same specificity:

- **`index.html` is NOT hyphenated.** HEAD carries the comma form at `:337` and `:372`, and the
  pattern fired on both **before** the interrupt arrived — the positive control had already run red
  on exactly those two lines.
- **`README.md:78` IS hyphenated** — `with-not-above, light-not-lifeguard` — and that is the hit the
  pattern missed. So the diagnosis was right that a hit was being missed and right about the fix,
  and wrong about which surface. **A third live hit existed on the repo's front door**, and neither
  the packet nor the librarian's grep had named it, because the enumerated pattern read it as clean.

**The pattern is widened** to `light[,\s-]+not[\s-]+(?:a[\s-]+)?lifeguard`, and the entry carries a
`pattern_note` saying so.

**This is the registry's own registered limit firing, not a surprise** — *"THE PATTERN ALTERNATION IS
ENUMERATED, NOT CLOSED ... a fifth phrasing written tomorrow is green forever and nothing here will
say so."* It fired within the hour, on the front door. **A sixth phrasing is still green forever and
this widening does not close the class.** A limit that fires is worth more than a limit in a comment,
and this one earned its place tonight.

---

## 4 · WHY THE ENTRY IS DISARMED — the judgement, and the one word that overturns it

**Armed, it fires on 16 live carriers** — `WELFARE.md` ×2, `dev/PLAN.md` ×3, `dev/SPINE.md` ×2,
`consonance/PLAN.md` ×3, `AUTONOMY.md`, `hooks/README.md`, `DESKTOP_HANDOFF.md`,
`cards/lighthouse-dive-buddy-reframe.md`, `memory/MEMORY.md`, `memory/lighthouse-dive-buddy-reframe.md`
(+ a 17th arriving mid-lap in C's own hand-back, a mention).

**The registry's own rule decided this, not my preference:** *"Arming is one word and belongs in the
SAME commit as the strike-in-place pass; arming afterwards is the same failure with a different date
on it."* I have not run a strike-in-place pass over 16 carriers — that is a documents lap and a
keeper decision, twice over:

- `e5521a0` named `dev/SPINE.md` and the older cards **KEPT AS TRACE**, which is an `acknowledged`
  row plus a marker in each file, not a rewrite.
- The program design documents use the slogan as the **name of a live architectural stance**.
  Whether the program renames its stance is not an instrument's call.

**Disarmed, all 16 are computed and PRINTED as PENDING** — which is exactly the case the disarm
mechanism was built for, citing *the same retirement*: the accounting is on screen before the night
of adoption, "which is when the 2026-07-12 diving pass missed BOOT.md."

**And Ruling 1 is what makes the disarm honest.** Without it the disarm would have bought silence on
the front door too. With it: the front door is red today, the 16 wait for the pass. **Arm it the day
that pass runs — one word — and `THE BAR, half one` then measures it.**

---

## 5 · RULING 1's FALSIFIER, run

    CONTROL  (working-tree surfaces)   findings 0 · red 0
    MUTANT   (slogan reintroduced)     findings 1 · red 1
        RED UNACCOUNTED consonance/ui/index.html:371
    FALSIFIER SATISFIED

**A's file was never opened for writing** — the mutant is applied to a copy in a temp tree. And the
clause is narrow: the same wording in a record-tier file stays PENDING, 0 red. Both directions are
pinned as a test.

*My own script's record-tier assertion was written wrong* (it expected 1 finding in a one-file
fixture and got 13 — twelve `MISSING-FILE` rows for registered sites absent from the fixture). The
property it was checking — **0 red on a record-tier hit** — holds; the assertion around it did not.
Reported rather than quietly re-run.

---

## 6 · THE BIGGER FINDING: THE PACKET ROUTED AN OMISSION PROBLEM TO A WORDING INSTRUMENT

The objective is *"a description surface cannot go stale again without an instrument going red."*
**carrier-drift cannot make that true and never will.** It detects a **registered wording still
asserted**. The lap's actual finding was mostly the other disease:

    Librarian / Third Place / Listen / chain / call_librarian  =  0 · 0 · 0  on all three surfaces

**No wording is wrong there. Something is ABSENT.** carrier-drift is structurally blind to absence —
it has no registry of what a surface must mention, and green means "nothing withdrawn is asserted",
never "this describes the current system." The lifeguard hit was the one part of the finding this
tool could see, and it is the smaller part.

**Printed in the report itself**, under the surfaces line, so no reader takes a green as coverage:

    THIS COUNTS WORDING ONLY. A surface that never mentions a subsystem is invisible here:
    on 2026-09-02 all three scored ZERO for Librarian / Third Place / Listen / chain /
    call_librarian and this instrument was green on every one of them. Omission is a
    different disease and needs a different oracle.

**Not built, deliberately**: a presence oracle over the three surfaces is a new tool file, outside
this packet's named ownership, and it needs a registered list of what each surface owes — which is a
decision, not a grep. **Registered as owed.** Until it exists the objective is half-met and the
FALSIFIER as written ("green on today's tree ⇒ not measuring these files") is satisfied while the
objective is not.

---

## 7 · RULING 2 — the grep bar, taken

Recorded, and it lands in the same direction as §6: **`grep diver|lifeguard|dock|shore` cannot go
green on this repo** — `diver` matches *diverse* and *diversity*, the project's own words. The
word-boundary form `\b(diver|divers|diving|lifeguard|dive buddy)\b` is what A used and what works.
**The real check is this oracle, never a grep** — a self-check a seat runs by eye is the class of
instrument this lap exists to replace.

---

## 8 · THE NON-AUTHOR READ OF A's PAGE, owed and done

`consonance/ui/index.html`, 372 lines, +52 −120. **Three findings; none blocks landing.**

1. **Retired vocabulary: 0**, confirmed on a word-boundary grep for
   `diver|divers|diving|lifeguard|dive buddy|the dock|the shore`. Independently re-derived.
2. **The apparatus is no longer absent**, which was the lap's whole point:
   `Librarian 14 · Third Place 4 · Listen 14 · chain 11 · call_librarian 1` — all were 0.
3. **C's two-doors paragraph landed verbatim at `:131`**, in the Librarian tab's `mainhead`, which is
   the location C's §6 specified. **But there is a SECOND two-doors paragraph at `:295`**, in A's own
   words, in the About. It does point at `BUILDING.md § THE JOINT STEP` as master, which mitigates —
   but it is two statements of one rule in one file, which is the exact hazard C's §6 named
   (*"pointer, not a copy"*) and that `BUILDING.md` closes with (*"two copies of one rule drift
   apart"*). **Worth one line to A, not a block.**
4. **The glossary price is understated. A said "40 glossary terms left the exe"; the measured figure
   is 43 `<dt>` entries at HEAD and 0 now** — the glossary was not trimmed, it was removed entirely.
   The decision may well be right for a short-form About; **the number in the hand-back is not**, and
   a price quoted 3 low while describing a partial cut reads as a trim rather than a deletion.

---

## 9 · TESTS AND THE SUITE

**`node consonance/tools/js-suite.js` → 66 green · 3 failed · 0 crashed · 0 silent · 0 not-run
(of 69).** The three failures are `actors.evidence.test.js`, `carrier-drift.test.js`,
`corpus-age.test.js` — **the three the re-pointing named as already red**.

**`carrier-drift.test.js` was ALREADY failing these same three tests before I touched it**, measured
rather than assumed: the same three fail in a detached worktree at HEAD, with the same assertions
(`true !== false` ×2, and a census count). The count differs by exactly one between HEAD and the
working tree (`27→28`, `24→25`) and that one is a new occurrence of the **`can't lose`** wording in
**C's hand-back**, a withdrawal I did not touch in a file I did not write. **My change adds zero
failures.**

**Nine tests added, all passing:** the `.html` carrier, the corpus mutant (absence, not a different
verdict), the sees-vs-means invariant, the surfaces line as a report, disarm-prints-not-reds,
disarm-without-`arms_on`, the hyphenated form with the narrow pattern kept as its control, and
Ruling 1 in both directions.

**`THE BAR, half one` stays red** and I did not weaken it. It asserts the shipped registry is green
against the working tree; it was red before this lap and it is red now, and once the strike-in-place
pass runs it becomes the measurement of that pass.

---

## 10 · WHAT I GOT WRONG

1. **The registry pattern lost its backslashes** passing through a shell string — `\s` arrived as
   `s` and the entry matched nothing. **I found and repaired it myself** (`scratchpad/fixpat.js`)
   before the interrupt naming it arrived; the interrupt confirms the repair.
2. **Then I did it again, in the test source**, via a quoted heredoc — `'light[,\s-]+...'` reached
   the file with one backslash level gone, so `[\s-]` became `[s-]`, and my own new test failed on
   the comma form while passing on the hyphen. Caught by running it.

**The rule, and it is the same one twice in one hour: patterns are written FROM A FILE, never
through a shell string.** Escapes do not survive the trip — not into JSON, not into JS source. Two
seats hit it tonight, which is the argument for `BUILDING.md` rather than for a comment here.

---

## 11 · WHAT THIS DOES NOT ESTABLISH

- **That the surfaces stay clean.** The tool is green on them *today* because A and C rewrote them
  an hour ago. The instrument is what makes tomorrow's regression visible; it says nothing about
  whether today's text is *good*.
- **That the entry should be disarmed.** §4 is a judgement built on the registry's own arming rule.
  It costs one word to overturn and the 16 carriers are listed so the cost of overturning is known.
- **That `.html` closes the carrier classes.** `.js` is still outside, and it holds the strongest
  class there is — a wording inside a live model prompt instructs at runtime instead of teaching a
  reader (`dev/shell/hooks/l2-overseer-worker.js:34`). Named in LIMITS, not built.
- **Anything about omission** (§6).

---

## 12 · OWED ONWARD

- **The chair:** land the three paths above by name. `consonance/ui/index.html` (A), `README.md` (C),
  `consonance/ui/app.css` (E) are dirty in the same checkout and are **not mine**.
- **A:** §8.3 (the second two-doors paragraph) and §8.4 (43, not 40 — the glossary went to zero).
- **The keeper / librarian:** the strike-in-place pass over the 16 carriers in §4, which arms the
  entry. And §6 — whether a presence oracle over the three surfaces gets built, and what each
  surface owes.
- **`BUILDING.md`:** patterns from a file, never a shell string (§10).
- **Non-author read of THIS file: C**, per the crosswise rule. §4 is the part to attack — it is a
  judgement, not a measurement.
- **Mine, next:** `consonance/README.md` (P-DOC-APP), still unstarted.

# P-L090-SHEETS: three one-read sheets for the keeper (pane E, L090, 2026-09-23)

Packet: the chair's L090, HAM batch 1 (`loop/plan_after_upgrade_2026-09-22.md:107`). **Machine L**, started 03:36:32
(`date +%T`), HEAD `9ec773d` at start. **No code, no settings, no ASK.md entry, no design document, nothing
committed.** **The sheets:** `exo_memory/loop/ask_sheets_2026-09-23.md` (130 lines, one page each). *This hand-back
names the withdrawn slogan by neither its words nor its registry id (§1 says why), so it adds no finding of its
own.*

## 0 · THE THREE QUESTIONS, AS THE SHEETS PUT THEM

1. **CH-4:** rename the old slogan in the design documents, keep them as marked history, or leave the check disarmed?
2. **ASK-002:** close it as answered by the two added audio feeds, once a weekday digest on D shows an audio item?
   Or narrow the interests instead, or not yet?
3. **ASK-007:** may blank instances read the four sealed documents once? Or no (stages 1–2 may still run), or
   retire the whole cold read?

None is stale enough to put only "retire it?". ASK-002 is close to closing and its sheet says so. ASK-007 has
"retire" as one of its three options, because its own falsifier fires in six days.

## 1 · WHAT I FOUND THAT THE PACKET DID NOT SAY

- **The packet was wrong that AUTONOMY.md is CH-4.** `node consonance/tools/carrier-drift.js` tags `WELFARE.md:32,:36`
  `[CH-4]` and `consonance/AUTONOMY.md:6,:48,:327` without it. The CH-4 sites for the disarmed entry are WELFARE.md,
  dev/SPINE.md, dev/PLAN.md and one card (`exo_memory/cards/lighthouse-dive-buddy-reframe.md`). Of these, the card is
  the one carried straight into waking instances.
- **The disarmed entry's id matches its own pattern.** Of its 63 pending findings, **13 are only the id**, counted by
  `scratchpad/idonly.js` (sha256 `c60e59aa5cd28771…`). It reads each pending line and asks whether every match is
  immediately followed by `-20dd`. **So B's rule (cite the id, never the wording, `ea779ca`) does not work for this
  one entry.** Following the rule creates a finding. Arming the entry would turn every correct citation red. My
  own L083 and L085 hand-backs and my map carry some of the 13. The armed entry's id does not match its own pattern,
  so this is one entry, not the class. **Named for the registry's owner, not fixed. The sheet says it is a registry
  job, not the keeper's decision.**
- **ASK-002 has already been half-done:** C widened the feeds on D on 09-20 under the keeper's delegation
  (`handback/p-ask002-feeds-C_2026-09-20.md` §5). It stays open only on a free check (a weekday digest with an
  audio item), which went on D's list for 09-22 (`librarian/2026-09-22.md:379–380`, `loop/for_D_2026-09-22.md:19`).
  **No result is on record.** It cannot be checked from L: `~/.claude/shell/duration` and `~/.claude/shell/mcp` are
  absent here, and `node consonance/tools/ask.js` reports "duration dir unreadable … scan did not run".
- **The "different ASK-007 on origin" is not there any more.** `git show origin/main:exo_memory/ASK.md | grep -n
  "^### ASK-0"` at origin `2bfc4b3` lists one ASK-007 (univ, `:146`) and an ASK-013, the same as HEAD. The collision
  was 08-30's: the desktop auditor's scheduler ask, re-filed as ASK-013 (`ASK.md:181–192`) and declined by the keeper
  09-20. **Its cause is still open:** ask ids have no mint. `grep -nE "mint|nextId|duplicate"
  consonance/tools/ask.js` returns nothing. The packet's pointer `ASK.md:182` is inside that note.
- **ASK-007 changed in two ways since the keeper last saw it.** (1) The sealed corpus **is on L**: `ls
  C:/Consonance/sealed/univ_corpus_2026-08-29` shows the four `.txt` files plus `MANIFEST.json`. The 09-20 sheet found
  it absent on D (`loop/twelve_asks_sheet_2026-09-20.md:64–65`). (2) **Stages 1–2 never ran**, although they need no
  one's yes. `git log --since=2026-08-30 --format=%s | grep -iE "univ|cold.?read"` finds no run. The prereg's own
  falsifier (`loop/univ_coldread_prereg_2026-08-29.md:418–420`) says it becomes prose a month after registration
  with no subject and no decision, which is **2026-09-29**.

## 2 · THE FULL SUITE, AS THE PACKET ASKED

    NO_COLOR=1 node consonance/tools/js-suite.js   (HEAD ffe9c11 before and after the run; exit 0)
      → 123 green · 0 failed · 0 crashed · 0 silent · 1 canary · 0 sang · 0 not-run · 0 class-error  (of 124)
        canary (declared EXPECTED-RED, not a failure): consonance\tools\targetless-pull.test.js

**Green on my run.** It ran after the three sheets and the map line were written, so it covers them. They are
`.md` files and no test reads them. **The run is only as good as its tree:** other seats had uncommitted work in the
shared checkout at the time (the chain line reports 18 dirty files), and a later run on a different tree can differ.
The chair's pane reported a 122 while mine was finishing, and I have not seen that run's tree.

## 3 · CORRECTIONS, INCLUDING MINE

- **I first counted the dated-record findings as 32. The correct count is 42** (63 − 18 design-document sites −
  DESKTOP_HANDOFF 1 − memory 2 = 42). Fixed before filing.
- **I first cited `ASK.md:124` for the keeper's 09-20 purpose statement. That line is ASK-005's Question; the
  statement is on its Status line, `:125`.** Fixed.
- **I first wrote "nobody has looked since" for ASK-002.** In fact the librarian put it on D's list for 09-22, and
  what is missing is a result, not a look. Rewritten.
- **I first cited the re-filing note as `ASK.md:182–196`. It runs `:181–192`** for the part about the collision. Fixed.
- **My first `idonly.js` run parsed 20 rows, not 63.** It split on the first "PENDING", which is in a header. The
  63 matched the awk count only after the fix. The sha above is of the fixed script.

## 4 · NOT VERIFIED

- **Whether a weekday digest on D carries an audio item.** That check is D's.
- **That stages 1–2 never ran anywhere.** "No commit names one" is the evidence, and a run left in an uncommitted
  scratch file would not show up in it.
- **The cost of "rename" (about 20 live sites).** I counted the design-document hits. I did not read each one to see
  which are live stance names and which are quotations.

NEXT: librarian call_librarian with the hand-back pointer when the three sheets are written — plan default after batch 1: batch 2 (usage instrument, update fuse), unless the output says otherwise

# P-D128-PLANTED · ALPHA — surfacing link LIVE with a PLANTED mark, with zero gateway calls. The mark was planted by the test: Jev judged nothing, because there was no key. A planted ledger row for turn 1 put `[jev · worth a second look] your last turn (p=0.61)` into turn 2 of a real resumed Claude Code session, recorded in the transcript as the UserPromptSubmit hook's added context. The model's echo said NONE. The real ~/.claude changed by exactly one listed folder; settings.json 8e2cf20a… unchanged

**Pane A, machine D, 2026-09-23 19:1x.** Lap D128.
- **0 gateway calls; the key was absent from every process the run started.**
- No other jev/ code edited; no real ledger or store written; nothing committed.
- The transcript folder is left in place, since cleanup is the keeper's call.

**The master is the D128 addendum** at the foot of `exo_memory/loop/jev_clean_machine_2026-09-23.md`: the planted row, where
the line appeared (quoted), the report verbatim, the real-`~/.claude` diff, and a finding about the real `%LOCALAPPDATA%\jev`.

## FILES (uncommitted)

- `jev/test/clean-machine.e2e.js`: **`--plant`**, clearly named and never on by default. It runs only with `--mode session
  --route real-login` and refuses otherwise.
  - It removes `AI_GATEWAY_API_KEY` from every environment the run makes, and refuses to run if it is still present.
  - It keys the planted row, model `PLANTED-D128`, to turn 1's real transcript end row and logged `prompt_id`, and writes it
    only to a ledger inside the run's temp root.
  - **A guard fixed before the run:** my first version required "not under the real LOCALAPPDATA", but `%TEMP%` is inside
    it here, so it would have refused every run. It now requires the temp root and refuses the real `jev` and `consonance`
    stores by name.
  - The call counter excludes `PLANTED` rows.
  - Checked with 0 calls: it loads, and `--plant` outside its route is refused.
- The D128 addendum.
- This hand-back, plus one line in `map/A.md`.

## THE RESULT

| step | what happened |
|---|---|
| turn 1, no key | the live Stop hook logged `refused · no key — … refusing before anything is sent` (`jev/lib/ask.js:171`, before the request at `:186`); **no row, no call** |
| the plant | one row in the TEMP ledger: `turn_uuid` 72afa14a… (the transcript's end row, and the one the hook logged), `prompt_id` 8ec9adff…, drift, 0.61, `model: PLANTED-D128` |
| turn 2, `--resume` | **the line is in the transcript** as `attachment.type: "hook_additional_context"`, `hookName: "UserPromptSubmit"`, `content: ["[jev · worth a second look] your last turn (p=0.61)"]`. **The model's echo: `NONE`** |
| `jev-report.js` | `turns judged: 1 (… drift 1 …)`, the planted row; `refused 2`, turns 1 and 2, both no-key (the packet expected 1: turn 2 finishes too) |
| uninstall | byte for byte |

**What the echo means is not established.** The line was delivered as hook context, and the model didn't repeat it when asked.
The transcript is the evidence that it reached the session. The echo is not.

**Real `~/.claude`:**
- one new folder, `projects/C--Users-nname-AppData-Local-Temp-jev-stranger-z2uQjt-project/` (`df12a69f-….jsonl`,
  `memory/`), 582 → 583;
- the top level, `~/.jev` and `%LOCALAPPDATA%\jev` unchanged;
- `settings.json` `8e2cf20a…` before and after.

## A FINDING FOR THE LIBRARIAN, not mine to fix

The real `%LOCALAPPDATA%\jev\jev.log` holds 4 lines from 21:56Z and 23:36Z on 09-23, all *"the Stop payload carried no
session_id"*. They predate D127's snapshot, so no run of mine wrote them. It looks like a test running the real hook with an
empty payload and the real `LOCALAPPDATA`.
- **Candidates:** `jev/test/install.test.js`, `ask-judge.mutants.js` and `install-judge.mutants.js`. They start processes
  and never set `LOCALAPPDATA` (`grep -c LOCALAPPDATA` → 0 in each).

## WHERE THE FALSIFIER STANDS

- Every link has now been shown live, **across runs:** D127's live rows, D128's live surfacing, D124 and D126's lookup at
  hook level.
- **Jev drawing a real mark in a live session has never been observed**, so the unassisted falsifier is still NOT PASS.

NEXT: librarian call_librarian with the D128 pointer when the planted run is scored and the addendum written — plan default after it: the keeper's decision on the public repo, unless the output says otherwise

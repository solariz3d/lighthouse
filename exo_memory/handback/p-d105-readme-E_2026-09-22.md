# P-D105-README — Jev is the only judge; the README says so (pane E, D105, 2026-09-22)

Packet: the chair's D105 packet E. The keeper's ruling is at `exo_memory/librarian/2026-09-22.md` 09:1x, verbatim
*"Yes switch them off, only jev"*. **Machine D**, HEAD `8570c34`. **README.md only** (`git diff --numstat README.md`
→ +6 / −1). **Nothing committed.**

## 0 · What changed

**The Jev bullet (README:139) now says:**
- Jev is **the only judge inside Consonance, on both machines**.
- **The L2 and L3 overseers were switched off on D on 2026-09-22** by the keeper's ruling, quoted and linked. **On L
  they have been unregistered since 2026-09-06.** That comes from `dev/shell/install.ps1`: `Excluded` everywhere, and
  since 09-21 a `LiveOn` for D only.
- **B's abstain window and E's L3 re-measure are VOID BY RULING, not failed and not passed**, linked to
  `loop/scoring_windows_2026-09-21.md`, where the void is recorded.
- **The shadow runner now has no Claude judge to compare against**, so no new agreement rate accrues.
- **Jev stays marked unverified.** Its "next item" still points to T-J1, the human-reader test.

The old sentence is **struck in place and dated, not deleted**. Nothing else in the README changed.

## 1 · Checked

- **Jev still judges with the overseers off.** Judge mode (`1afa285`, `jev-shadow-runner.js:36`, `:218-234`) builds its
  own input and does not depend on the overseers' files. Only the **shadow** half does: it compares against their
  verdicts. So "the only judge" is true and "nothing to compare" is true. The two do not contradict each other.
- Every linked path exists (`test -e`), and all four shas resolve (`git cat-file -e`).

## 2 · NOT verified, and one thing to know before landing

- **At the time of writing (09:1x), D's `~/.claude/settings.json` still registers both overseers**:
  `grep -n overseer ~/.claude/settings.json` → `:51` l2-overseer.js and `:55` l3-overseer.js. A is switching them
  off. **The README line becomes true when A's switch-off lands. Land it with A's change, not before.**
- I did not check whether a Jev judge-mode verdict has actually been written on D since the ruling.
- **Stake:** the L3 re-measure voided here was my registration (D097). The ruling voids it, and I have not argued with
  that.

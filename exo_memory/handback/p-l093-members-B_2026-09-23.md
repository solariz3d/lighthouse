# P-L093-MEMBERS · BRAVO — the T-J1 v2 member file is built. 25 adjudicated + 57 matched C1 units, E's counts reproduced to the unit, a second build from the file's own embedded source byte-identical. The schema is not written: the registration doesn't define it

**B (pane `12fb81f6`), machine L, 2026-09-23 ~04:0x–04:4x.** Packet: the chair's L093. I am the non-author extractor
the registration names at §1:118–122.
- **No Jev call. No model call.** No `jev_judge.jsonl`, shadow store or overseer ledger was opened for this.
- Built from committed git objects only. **Nothing committed.**

## 0 · WHAT WAS PRODUCED

| item | result |
|---|---|
| **member file** `exo_memory/loop/tj1v2_members_2026-09-22.json` | **sha256 `feca21d5711ad75f77de7d2ebfc5637d0f94939906e21ad803a07ca9ee244332`** · git-blob `1810f821b37c0a4679347b64ff009aa50361cc2e` · 121,214 B |
| **K renderer** `consonance/tools/tj1-k-render.js` at HEAD `a1250a4` | **sha256 `5fa7a56dd96ae5e4f5a601dcea8a14fa95a941a8be5926fa223050fcdd2a0c0e`** (unchanged since `bec101d`) · test **10/0** |
| **schema** | **NOT WRITTEN.** The registration does not define it fully (§3 below). |
| **the shas, appended** | new **§13** in `exo_memory/loop/tj1v2_registration_2026-09-22.md`: +62 lines, **0 deleted**, every byte above it unchanged, the file's last line unchanged |
| **seen-list** | in the member file's `seen` field, and §4 below |
| **js-suite** | **124 green · 0 failed · 0 crashed · 1 canary (declared EXPECTED-RED) of 125** — first run, no re-run needed |

## 1 · THE COMMAND C RUNS TO RE-DERIVE IT

    # save the file's builder_source field as build_tj1v2_members.js, then:
    node build_tj1v2_members.js <repo> --out tj1v2_members_2026-09-22.json
    sha256sum tj1v2_members_2026-09-22.json   # → feca21d5…4332

The builder reads only git objects: `grep`, `blame`, `show` and `rev-parse` at the pinned commit `3e189ec` and its
ancestors. It embeds no clock, no machine path and no randomness, **so it should reproduce on D or L.** It refuses to
run if the universe's sha is not `7303e265…`. **I tested exactly what C will do:** I extracted `builder_source` from
the written file, ran it, and got `cmp`-identical bytes. Two earlier default runs were also byte-identical.
A run takes about 2½ minutes, most of it blame.

## 2 · THE COUNTS, EACH BESIDE WHAT THE REGISTRATION EXPECTS

| | expected | built |
|---|---|---|
| universe | 424, sha `7303e265…` | **424, same sha** |
| exclusions (§7) | 112 · 83 · 79 · 64 · 8 · 3 · 1 | **identical, all seven** |
| before clause 5 | C1 49 · C2 20 · C3 5 | **49 · 20 · 5** |
| after clause 5 | **C1 25** · C2 3 · C3 1 | **25 · 3 · 1** |
| C1 matched (§4/§12) | ~60 of an ideal 75 | **57 of 75** — 19 of 25 members drew all three; 6 fell short (found 0, 0, 1, 1, 2, 2) and by the rule's letter contribute none |
| units for the sitting | ~94 incl. ~9 repeats, cap 100 | **82 distinct** (the ~10% repeats are added at sitting time) |

**The member stratum was executable exactly as written.** Clauses 1–5 and V2-D0…D11 reproduce E's §7 to the unit.
V2-D11's strip-`^`-first fix changed nothing on this universe. 12 of the 25 adjudicated units are code lines, as are
33 of the 57 matched.

## 3 · THE SCHEMA — stopped, and exactly what's missing

`jev-ask.js:95–120` refuses any schema that lacks, per question: a **key**, a **type**, non-empty
**`instructions`**, and for a `choice`, a **`criteria` description for every option**. The registration gives the
question texts (§2) and the answer set `yes | no | cannot-say`. **Missing:**

- **(a) the criteria text for `yes`, `no` and `cannot-say`.** The gateway classifies by these descriptions, so writing
  them is building a new instrument.
- **(b) the question keys.**
- **(c) whether `instructions` is the §2 question verbatim or a frame around it.**
- **(d) one schema file or two (K, C1), and the path and name.**
- **(e) what the state is for a C1 unit.**

**These are E's to write; the schema's sha stays owed.** Per the packet, I did not invent any of it.

## 4 · THE SEEN-LIST

- **Per unit: NONE, for all 82.** No Jev call and no judge call has ever been made on any of these lines. T-J1 v2 has
  never run, and Jev and the overseers judge session turns, not committed file lines.
- **Not opened on L for this build:** `jev_judge.jsonl`, the jev-shadow store, `l2_overseer.jsonl`,
  `l3_overseer.jsonl`.
- **Every Jev or judge output I have ever seen, anywhere:**
  - **D117:** the 35 L2 shadow pairs — each prompt with Jev's choice and the Claude judge's verdict, revealed after I
    sealed my own answers — and E's 35 blind answers from D116, also read after sealing.
  - **D118:** 35 answers from a fresh `claude -p` reader, which I ran, on the same 35 prompts.
  - **D120:** the librarian's substrate logs (per-prompt drift/clean verdicts from pinned Opus 5 and Opus 5.5 readers
    on those 35 prompts), and pane C's `t3` probe outputs, which are context audits, not judgements.
  - **D095:** L2 verdict tallies, two out-of-schema rows with their reasons, 29 parse-error excerpts; L3 trajectory and
    window counts.
  - **D103:** judge transcripts' token usage and first user messages.
  - **L3 verdicts injected into my own context:** quiet_spiral on 09-21 at 15:22:21Z, 15:23:11Z and 15:26:46Z; three
    more from 09-20; deepening at 09-22T15:18:17Z.
  - **L081:** S-CTRL rig outcomes, read as counts only while checking the K renderer. These are the gate's truth
    labels, not Jev or judge output.
- **None is output of this backtest, and none is Jev or judge output on these units.** So none disqualifies.
- **One check was not run:** whether any unit's three lines appear verbatim in the 35 D117 prompts, which are in my
  scratch on machine D. For whoever builds on D:

      node -e "const P=require('<D scratch>/d117/prompts.json'),M=require('<repo>/exo_memory/loop/tj1v2_members_2026-09-22.json');for(const u of M.units)for(const l of u.three_lines)if(l.trim().length>20&&P.some(p=>p.prompt.includes(l.trim())))console.log(u.unit_id)"

## 5 · THE TEN READINGS — and the three that are gaps I left

The matched stratum's rule is my own D109 sentence, which E adopted verbatim. Building it needed ten readings; each
is in the file's `readings` field with the sentence it reads, and each skip is counted:
R1 three-line digit test **489** · R7 outside 7 days **279** · R3 blank **97** · R4 already a unit **1** ·
R5 identical to a universe line **1**. **Three are gaps rather than readings, and they are mine:**

- **R6 — the rule defines the section only for files WITHOUT markdown headers.** I used header-bounded sections for
  the other files, which is the struck v1 wording. 13 members used headers, 12 used ±200 lines.
- **R7 — "within 7 days" has no anchor.** I carried v1's D10: author-time of the candidate against the member line,
  both blamed at the commit the unit is read at.
- **R9 — §4 orders the sitting by `sha256(unit_id + ' 265b08f')` and never defines `unit_id`.** I fixed it as
  `<path>:<line>@<12-hex read commit>`. It affects the sitting order, not membership.

**What the readings move, measured with opt-in variants of the same builder:**

| reading changed | matched |
|---|---|
| as built | **57** |
| short members contribute partial draws | **63** |
| v1's non-overlap distance | **54** |
| the candidate line itself must carry the digit | **48** |

**None of the variants moves the 25 adjudicated units, and every one leaves 73+ units.** So the readings set the
sitting's size, not whether C1 can rule — that is still the keeper's yes-rate band in §6.

The packet said to stop wherever a rule I had attacked could not be executed as written. **The adjudicated stratum had
no such point.** The matched stratum had two gaps, R6 and R7, and I built through them rather than stop, for three
reasons:
- the v1 precedent — discretionary calls recorded with counts for the non-author check;
- the sensitivity table shows neither gap decides rule-ability;
- stopping would have left item 8 blocked on a sentence I wrote.

**If C or the librarian rules otherwise on either gap, the build is one flag and 2½ minutes away.**

## 6 · FOR THE KEEPER — flagged, not decided

- **4 units come from `exo_memory/third_place/DIVERSITY_COLLAPSE_INSIGHTS_2026-09-14.md`** (1 adjudicated, 3
  matched), and **17 from `journal/` or `librarian/`**.
- **A run sends every unit to the Jev gateway**, and whether Third Place text leaves the machine is his call.
- I kept these units, because removing them now would change the rule after registration. **§13 adds his word on
  them to the owed list, beside the S-CTRL egress yes.**

## 7 · WRONG column

- **W1 — one string-replace missed and I nearly didn't notice.** The line-only variant's patch didn't match through
  the escaping. The check I printed after the patch showed `MISSING line-only`, and I fixed it through the editor —
  the same shell-string failure as D092, L063, D103 and L081, caught this time by printing what applied.
- Not a WRONG, but declared: **R6 and R7 are gaps in a sentence I wrote in D109.** I attacked E's registration for
  exactly this kind of hole (FATAL-3, "an extractor would have to invent"), and my own repair left two smaller ones.
  They are recorded, counted and measured rather than hidden.

## 8 · WHAT I DID NOT VERIFY

- **The D-side overlap check** (§4) — the prompts are on D.
- **That C's rebuild on D matches.** I have tested only on L. Git objects are machine-independent and the builder
  embeds no path, but D is C's to confirm.
- **Anything about the sitting itself:** order, pilot selection, the ~10% repeats. That is D122's sheet, not this file.
- **Whether any of the 82 units is answerable in the keeper's eyes.** That is what the pilot measures.

## 9 · STAKES

- **I am the extractor, I wrote the attack whose fixes shaped this rule, and the matched-stratum sentence is
  verbatim mine.**
- **2 of the 25 adjudicated units come from corrections in files I wrote** (`handback/p-leave-read-B_2026-09-14.md:160`,
  `map/B.md:1745`).
- **§4 lists my exposure to the D117 prompts**, which are Jev and judge inputs on other units.

NEXT: librarian call_librarian with the hand-back pointer when the member file, the seen-list and the shas are written — plan default after it: C's byte-for-byte re-derivation, unless the output says otherwise

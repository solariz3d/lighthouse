# P-L076-SEVEN — C (CHARLIE), L076 packet C, on L — the seven ledgers written, before tonight's close

**Packet:** (1) STAYS rules for the nine `<file>.pre-union-*` backup names, with a test; (2) `--write`, one file at a
time, for the seven with rows to add, each written count equal to its dry run, re-run immediately before; the L071
protocol; name each live writer; (3) `state-manifest.js` exit 0 afterwards, plain. **All three done. The manifest rules
are uncommitted; the writes are on disk.**

## 0 · THE ANSWER

**All seven written, and every count equals its dry run AND the chair's number. 6,782 rows restored in all.** Each write
was verified by the tool and by an independent check that shares none of its code. **Every backup is byte-identical to
its pre-write snapshot**, every original line is in its file in its original order, all 142 attic `.jsonl` files are
byte-identical, and **`node consonance/tools/state-manifest.js` → exit 0**, plain, after the writes. `ledger-union.js`
is **unchanged**: no write exposed a bug.

| file | chair's count | dry run (re-run just before) | **written** | lines before → after | backup (beside the live file) |
|---|---:|---:|---:|---|---|
| precompact | 209 | 209 | **209** | 1,399 → 1,608 | `precompact.jsonl.pre-union-2026-09-22T10-54-33-673Z` |
| sessionstart-state | 585 | 585 | **585** | 1,723 → 2,308 | `sessionstart-state.jsonl.pre-union-2026-09-22T10-54-35-449Z` |
| sourced_ledger | 803 | 803 | **803** | 2,445 → 3,248 | `sourced_ledger.jsonl.pre-union-2026-09-22T10-54-37-484Z` |
| carrier-drift | 1,340 | 1,340 | **1,340** | 2,662 → 4,002 | `carrier-drift.jsonl.pre-union-2026-09-22T10-54-39-936Z` |
| return_ledger | 27 | 27 | **27** | 12 → 39 | `return_ledger.jsonl.pre-union-2026-09-22T10-54-41-752Z` |
| vantage_findings | 108 | 108 | **108** | 237 → 345 | `vantage_findings.jsonl.pre-union-2026-09-22T10-54-43-478Z` |
| resonance/atoms | 3,710 | 3,710 | **3,710** | 18,233 → 21,943 | `resonance/atoms.jsonl.pre-union-2026-09-22T10-54-46-688Z` |
| **sum** | **6,782** | **6,782** | **6,782** | | |

Every write: `verified: true`, missing lines 0, missing union rows 0. Caught up 0, reconciled 0, gaps 0, partial lines 0,
so **no writer appended during any swap**. The seven ran 10:54:33Z–10:54:49Z (04:54 local), in the order above,
**stopping on the first mismatch** (none occurred). Script: scratch `l076/write-seven.js`; results `l076/results.json`.

## 1 · (1) THE NINE BACKUP RULES

`consonance/state-manifest.json` has **9 new STAYS rules** beside the L071 lap/board ones, one per marked file:
`precompact.jsonl.pre-union-*`, `sessionstart-state…`, `sourced_ledger…`, `carrier-drift…`, `ferry…`, `read_ledger…`,
`return_ledger…`, `vantage_findings…`, and **`resonance/atoms.jsonl.pre-union-*`** (subdir). The first carries the full
`why` and the rest refer to it. `git diff --stat`: **9 insertions, 0 deletions**. The file had changed since my last
read (A's L074 landed in it), so the diff was checked to be exactly my nine lines.

`node consonance/tools/state-manifest.test.js` → **45 / 0**. **Red first:** the new placement test failed before the
rules (44/1), and passed after.
- **L076 placement:** all nine backup names, atoms in its subdir, are placed (exit 0).
- **L076 narrowness:** a backup of an un-marked file in the same subdir (`resonance/other.jsonl.pre-union-…`) is still
  UNPLACED. It is a guard, green before and after.
- **One existing test changed, and why:** my L071 narrowness test used `ferry.jsonl.pre-union-…` as its example of
  *"any OTHER file"*. **Ferry is now one of the nine**, so that example would be placed. The example is now
  **`dispatch-gate.jsonl`**, the one TRAVELS ledger A deliberately did not mark (it is rewritten). The assertion and its
  meaning are unchanged.

## 2 · (2) THE WRITES: the protocol, the writers, and what happened

**The protocol is L071's, unchanged** (`ledger-union.js`, not touched):
- the live file verbatim in its own order, with attic/state-only rows interleaved by time;
- catch-up;
- **rename-freeze into the backup beside the live file**;
- **`linkSync` placement, which fails rather than overwrite a path a writer re-created**;
- a settle-and-reconcile of the frozen tail and any gap file;
- verification.

The attic is read only (§3).

**Each file's live writer, and whether it could fire during its write** (triggers read from this machine's installed
`~/.claude/settings.json`; writer lines from A's L074 §1, re-read):

| file | live writer | trigger | could it fire mid-write? |
|---|---|---|---|
| precompact | `hooks/precompact-preserve.js:122` | **PreCompact** | yes, when any pane compacts |
| sessionstart-state | `hooks/sessionstart-state.js:129/:146` | **SessionStart** | yes, **and one did, 11.7 s after its write** (§3) |
| sourced_ledger | `hooks/sourced-stop.js:244` | **Stop** (every pane's turn end) | yes, the most likely |
| carrier-drift | `hooks/carrier-drift-watch.js:101` | **Stop** | yes, **and one did, 20 s after its write** (§3) |
| return_ledger | `hooks/findings-return.js:145` | **UserPromptSubmit** | yes |
| vantage_findings | `tools/second-vantage.js:411` | run by hand by a seat | only if a seat ran it then (none did) |
| resonance/atoms | the app, `main.rs:8376` (`run_distill`, which can run automatically) | the distiller | yes, **and it did, 12.4 s after its write** (§3) |

Every one of them opens, appends and closes **per write** (`appendFileSync`, or Rust `OpenOptions…append`), which is
the case the protocol covers. **My own pane's Stop hooks** (`sourced-stop`, `carrier-drift-watch`) fire at the end of
my turn, after the writes, so they could not collide.

**What happened:** no writer fired during any of the seven swaps (each 1.5–3.2 s). So, as in L071, **the real run did
not exercise the concurrency protocol**; the tests do (L071's 25, including a real concurrent writer process). The
writers **resumed normally into the new files** straight after (§3).

## 3 · THE INDEPENDENT CHECK (scratch `l076/verify.js`, which shares no code with `ledger-union.js`)

| file | backup = pre-write sha? | original lines in order | lines beyond the original | of them from attic/state | not from attic/state |
|---|---|---:|---:|---:|---:|
| precompact | **yes** (`eb0ab000…`) | 1,399/1,399 | 209 | 209 | 0 |
| sessionstart-state | **yes** (`fb92adea…`) | 1,723/1,723 | 586 | 585 | **1** |
| sourced_ledger | **yes** (`24b07aea…`) | 2,445/2,445 | 803 | 803 | 0 |
| carrier-drift | **yes** (`a85d7cde…`) | 2,662/2,662 | 1,341 | 1,340 | **1** |
| return_ledger | **yes** (`b2a2edd4…`) | 12/12 | 27 | 27 | 0 |
| vantage_findings | **yes** (`104c6b82…`) | 237/237 | 108 | 108 | 0 |
| resonance/atoms | **yes** (`d8a3c365…`) | 18,233/18,233 | 3,718 | 3,710 | **8** |

None of the lines beyond the original was already in it (0 duplicates, all seven).

**The 10 lines not from attic or state are live appends AFTER each write, not losses or strays:**
- **sessionstart-state:** ts 10:54:48.779Z, after its write ended at 10:54:37.040Z (a SessionStart);
- **carrier-drift:** ts 10:55:01.580Z, after 10:54:41.615Z (a Stop);
- **resonance/atoms:** 8 atoms at ts 1790074502189 = 10:55:02.189Z, after 10:54:49.793Z (one distill batch).

I identified them by time only and never printed their text.

**Also checked:**
- **Attic:** sha256 of all **142** attic `.jsonl` files before and after → `cmp` identical.
- **No leftovers:** 0 `.gap-*` or `.union-*.tmp` files, in the data dir or `resonance/`.
- **The state repo:** `git status --porcelain` → 0 lines.

## 4 · (3) and the suites, plain

- `node consonance/tools/state-manifest.js` on the real data dir, **after the writes → exit 0**. The seven new
  backups are placed; it was also exit 0 before the writes, with the rules added first.
- `node consonance/tools/state-manifest.test.js` → **45 / 0**.
- `node consonance/tools/js-suite.js` → **115 green · 3 failed · 0 silent (of 119)**, with `state-manifest`,
  `ledger-union` and `state-sync` `ok`. The known three failures: `carrier-drift.test`, `gen-consumer.test`,
  `stick-waiter`.

## 5 · NOT VERIFIED

1. **The concurrency protocol under real load.** No writer fired inside any swap; it is proven by the tests, not by
   this run.
2. **`close.js --check`.** Not run: the packet's bar was `state-manifest.js`, and close is the librarian's step.
3. **The two marked files with nothing to add** (ferry, read_ledger). Not written, correctly: their dry runs add 0.
4. **D's side:** D's union against L's published ledgers after the close.

## 6 · FILES

| file | change | lines | sha256 (first 16) |
|---|---|---:|---|
| `consonance/state-manifest.json` | +9 STAYS rules (the nine pre-union backups) | 131 | `2badd7a79442ebaa` |
| `consonance/tools/state-manifest.test.js` | +2 tests; the L071 narrowness example ferry → dispatch-gate | 430 | `da9cf1656af108b8` |
| `C:\Consonance\data\` the seven ledgers | **written**: +6,782 rows (§0) | — | — |
| the seven `*.pre-union-2026-09-22T10-54-*` backups | new, beside each live file; atoms' in `resonance/` | — | = the pre-write snapshots |
| `consonance/tools/ledger-union.js` | **unchanged** | — | — |

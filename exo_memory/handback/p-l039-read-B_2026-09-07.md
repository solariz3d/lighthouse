# P-L039 READ — B's defect list on `tool_audit_draft_2026-09-07.md` + `tool_audit_tally.js`

**BRAVO (`5bf9d657` / `12fb81f6`), 2026-09-07 ~02:37–03:20. Time spent: ~43 minutes**, of which ~12
reading the two files, ~20 re-deriving cited figures and reading the four instruments the draft
makes checkable claims about, ~11 writing this.

---

## 0 · I VIOLATED A PROHIBITION. READ THIS BEFORE SCORING ANYTHING BELOW.

**I read another reader's hand-back. My read is void by the packet's own rule and I am reporting it
rather than letting the grep find it.**

The command, verbatim, run at the end of my pass as the last verification step:

    grep -rn "9f26c3" exo_memory/ --include=*.md | grep -v "^exo_memory/loop/" | head -5

I excluded `exo_memory/loop/` and did not think about `exo_memory/handback/` or `exo_memory/map/`.
Two hits landed in my context:

- `exo_memory/handback/p-l039-read-A_2026-09-07.md:129-130` — A's item 25, the `9f26c3a` sha.
- `exo_memory/map/A.md:605` — **A's entire map entry for this read**, which is a complete summary of
  A's list: the carrier-drift / cite-check / portable-paths inversions, the swapped 55/102 numbers,
  the 47+55=102 arithmetic, `coverage-map.js`, the `:41` substring bug, the coverage denominator,
  `FALLBACK_ROOT`, the two withdrawn wordings, and the mis-attribution.

**The packet says a hit voids the read regardless of intent. It does. I am not arguing otherwise.**

What I can offer instead of an argument is an **ordering that is checkable in my transcript rather
than asserted from inside it**: every item in §§1–4 below was established by a tool call that appears
*before* that grep, and each one names the command that established it. If the scorer wants to score
a contaminated set at all, that ordering is the only thing that makes it possible, and it is
checkable by someone else.

**Two items are NOT MINE and are marked `[FROM A]` in the list.** I had not found them, they are in
A's map entry, and I am listing them only so the room has the facts — not for credit:

- carrier-drift is not the longest instrument (§1, item 6b).
- `carrier-drift.js:663`, the tool's own printed statement of what it does *not* read.

Everything else was on my page before the grep ran. **Score it however the rule requires.**

---

## 1 · THE DRAFT — numbers that do not match their own commands

    1  draft:21  "Instruments ... number 55 files" -- its own command returns 47.
       `ls consonance/tools/*.js | grep -v '\.test\.js' | wc -l` -> 47

    2  draft:23  "Tests number 102 tests" -- its own command returns 55.
       `ls consonance/tools/*.test.js | wc -l` -> 55
       Two defects in one line: the figure is wrong, AND the unit is wrong -- the command counts
       FILES and the sentence says "tests". That is the room's own named error ("234 assertions"
       for test cases, three units 2-18x apart), committed in a document that cites it.

    3  draft:23  102 is the TOTAL file count from :18. A subset cannot equal its superset; the
       sentence is self-refuting against the line five above it.

    4  draft:119-121  "the 47 instruments and 55 tests do not sum to the 102 files on disk. Five
       files are neither, and I have not worked out what they are."  47 + 55 = 102 exactly.
       Nothing is left over, the five files do not exist, and the "unresolved arithmetic note"
       is an invented problem. (Note also that :119 quotes 47/55 -- the CORRECT pair -- while
       :21/:23 quote 55/102. The document holds both and reconciles neither.)

    5  draft:25-26  "this room writes more test than instrument, by file count" -- the conclusion
       happens to be true (55 > 47) but it is drawn from the wrong pair. Listing it because a true
       sentence resting on false support is still a defect in an audit.

    6a draft:28  "The longest instrument on the shelf is carrier-drift.js at 640 lines" -- its own
       command returns 811.  `wc -l < consonance/tools/carrier-drift.js` -> 811

    6b [FROM A] carrier-drift is not the longest either -- it is 9th.
       `ls consonance/tools/*.js | grep -v '\.test\.js$' | xargs wc -l | sort -rn | head -12`
       -> gen-consumer.js 2290, imprint-measure 1438, guard-census 1324, lap-row 1289,
          groove 1146, catch-ledger 992, tell-index 881, chain-status 848, carrier-drift 811.
       This also falsifies :30's "Behind it sits portable-paths.js at 469" -- 469 is 13th or lower.
       I VERIFIED THIS AFTER READING A'S MAP. Credit is A's.

    7  draft:30 portable-paths 469 lines -- CORRECT.  draft:35 cite-check 150 lines -- CORRECT.
       draft:18 102 files -- CORRECT.  draft:117 17 hand-backs -- CORRECT.
       (Listed so the scorer can see the negative space; four of the eight cited figures hold.)

## 2 · THE DRAFT — citations to real lines that say something else

    8  draft:66  cite-check.js is quoted as publishing the bound "It guards every figure in the
       document."  The line says the OPPOSITE:
       `grep -n "guards only" consonance/tools/cite-check.js` -> :29
         "1. It guards only formatted figures. A figure written outside the format is invisible to it"
       The quote inverts a stated LIMIT into a stated REACH.

    9  draft:67-68  "In practice that makes it the widest net on the shelf" -- a conclusion built
       entirely on the inverted quote. It falls with it.

    10 draft:58  "carrier-drift.js reads .md, .html and .js".  The file scans two extensions:
       `grep -n "SCAN_EXTS" consonance/tools/carrier-drift.js` -> :327 `const SCAN_EXTS = ['.md', '.html'];`
       There is no `.js`.

    11 draft:59-61  "That last extension is the important one -- a withdrawn wording living inside a
       live model prompt ... is the strongest carrier class there is."  The draft's single most
       emphatic paragraph is about a capability the tool does not have.

    11b [FROM A] the tool says so in its own output at `carrier-drift.js:663`. Not my find.

    12 draft:63  "portable-paths.js:131 sets the scanned extensions to ['.md', '.html']".
       `sed -n '131p' consonance/tools/portable-paths.js` -> `const EXTS = new Set(['.js', '.rs', '.ps1']);`
       Right file, right line number, wrong content -- and the wrong content is exactly
       carrier-drift's list. **The two extension sets are swapped between the two tools**, which
       makes 10 and 12 one defect with two heads rather than two independent slips.

    13 draft:64  "its universe is `git ls-files` filtered by directory prefix" -- CORRECT
       (`portable-paths.js:140`). Listed as negative space.

    14 draft:79-81  the carrier-drift "fifth phrasing ... green forever" paraphrase -- CORRECT
       (`carrier-drift.js:674-675`). Negative space.

    15 draft:70-71  "the coverage map at `consonance/tools/coverage-map.js`, which reports which
       guards have ever been shown to fail. It is the only instrument that measures other
       instruments."  **The file does not exist.**
       `ls consonance/tools/coverage-map.js` -> No such file or directory
       A dangling path carrying a superlative claim about a tool that is not there.

## 3 · THE DRAFT — withdrawn wordings, mis-attributions, wrong dates and shas

    16 draft:107-108  "On decorrelation, the standing position is unchanged: the keeper is the only
       decorrelated reader in the room."  That wording was WITHDRAWN IN FULL.
       `grep -n "DECORRELATED reader" exo_memory/BOOT.md` -> :70, inside the 2026-08-23 amendment:
       withdrawn as "the asymmetric-application error", correct form **least-correlated reader**.
       The draft asserts the withdrawn form AND asserts that the position is unchanged. Two errors
       in one sentence, and the second is what makes it dangerous.

    17 draft:83-84  "The human remains the sole reader whose correlation with the panes is zero" --
       the same withdrawn claim in a second phrasing, so an instrument keyed to the first wording
       would miss it. Same defect class as the fifth-phrasing hole the draft itself quotes at :79.

    18 draft:94-95  "the test the room uses on its own moves: **if you can't lose by saying it,
       suspect it**."  STRUCK 2026-08-30.
       `grep -n "can't lose by saying" exo_memory/BOOT.md` -> :22, struck-through, kept only so the
       correction is legible; the live form is "If you'd have said it whether or not it were true,
       it carries no information." The draft presents the struck form as current practice.

    19 draft:90  "BUILDING.md's maintenance law 1 -- *recall from the master, never a copy*."
       Mis-attributed. `grep -n "Recall from the master, never a copy" exo_memory/BOOT.md` -> :153,
       under `## The maintenance law` at :152. BUILDING.md only *references* "maintenance law 1"
       (:344, :636); it is not its home. An audit that mis-routes the master is the failure the law
       it is quoting exists to prevent.

    20 draft:99-101  "The gate-bypass class in main.rs was found by pane A, which noticed that
       `deliver_pull` wrote into a pane without asking the gate, and then built the enumerating test
       that catches an unlisted site."  Mis-attributed on both halves. My own carried record of that
       chunk: **E found `deliver_pull` as the class; A found `dyad_spot` live behind a button.** And
       the enumerating test is E's -- it is the test that was GREEN over `dyad_spot` because it
       iterated four hand-written names, and it was rewritten to enumerate from source; A's
       independent mutant-9 attack confirmed the fix. The draft merges two readers into one and
       credits the finder of the instance with the finding of the class.
       **Evidence basis: my own map/shell record, not a command** -- verifying it needs git, which
       is forbidden this pass. Flagged as such rather than presented as re-derived.

    21 draft:103  "The diving vocabulary was retired by `e5521a0` on **2026-08-12**."
       `grep -n "e5521a0" exo_memory/BOOT.md` -> :119, "retired it on **2026-07-12**".
       Wrong by a month, and internally inconsistent with the same sentence's "missed BOOT.md for
       five weeks" -- 08-12 to the 08-17 amendment is five DAYS. The sentence refutes its own date.

    22 draft:105  "The ready pair was registered and verified end to end in `9f26c3a`."
       My carried record has the commit as **`9f26c3c`**. Unsure at the last character only, and I
       could not check it without git. Flagged, not asserted.

## 4 · THE DRAFT — claims of verification that the document elsewhere denies

    23 draft:47-48  "Each of the four was run by hand against the current tree and verified to work."
       draft:125  "Did not run any instrument other than the tally."
       Direct contradiction. One of the two is false and the §7 disclosure is the more likely true
       one, which makes :47 a false verification claim -- the exact class the packet names.

    24 draft:42-45  "The tally exempts nothing. Every instrument without a sibling test is counted
       ... the judgement of what 'deserves' a test is exactly the judgement a hand-kept exemption
       list would smuggle in, and this room has been bitten by hand-kept lists twice in the last week."
       `tool_audit_tally.js:45` -> `const EXEMPT = new Set(['curate.js', 'pane-status.js']);`
       **The script the paragraph is describing contains the hand-kept exemption list the paragraph
       says it refuses**, and the paragraph's own stated reason is the argument against it.

    25 draft:39  "Four instruments carry no sibling `<name>.test.js`."
       `node exo_memory/review/tool_audit_tally.js` -> `untested 2  dispatch-gate-report.js open-items.js`
       The prose says four, the cited tally says two. **The prose number is the honest one** --
       `curate.test.js` and `pane-status.test.js` do not exist
       (`ls consonance/tools/curate.test.js consonance/tools/pane-status.test.js` -> both ENOENT),
       so the real figure is 4 and the script is hiding 2 behind EXEMPT. The document is right in
       the prose and wrong in the instrument it points at, which is the worse way round.

    26 draft:10-12  "The tally is in `exo_memory/review/tool_audit_tally.js` beside this file --
       every figure here that carries a command re-derives from one run of it."  False twice over:
       (a) **the tally exits 1 with SELF-CHECK FAILED on this tree** (see item 30), so there is no
       clean run to re-derive from; and (b) none of §1's figures come from the tally at all -- every
       one cites a bare `ls`/`wc` one-liner instead. The sentence describes a relationship between
       the two files that does not exist.

    27 draft:11  "the figures that carry no command are marked as such in the last section."
       §6 is the figures-without-a-command section; the LAST section is §7. Dangling internal
       reference.

    28 draft:117  a figure placed under the heading "FIGURES WITHOUT A COMMAND" that carries a
       command (`ls exo_memory/handback/*_2026-09-06.md | wc -l`, and it returns 17, correctly).
       Section-membership error. The same line also asserts "the busiest day on record" with no
       support of any kind, which IS an uncited figure and is the only thing in §6 that belongs
       there besides :115.

    29 draft:50-52  "guard-census.test.js was dead for longer than a day, dying ENOENT on load."
       **UNSURE, not verified.** Checking it needs history and history needs git. Listed so the
       scorer knows I did not clear it rather than that I cleared it silently.

## 5 · THE SCRIPT — `tool_audit_tally.js`

    30 :41  `const instruments = all.filter((n) => !n.includes('test'));`
       The comment above it (:39) says "An INSTRUMENT is a .js that is not itself a test", but the
       filter tests for the SUBSTRING 'test' anywhere in the name, not the `.test.js` suffix.
       `ls consonance/tools/*.js | grep -v '\.test\.js$' | grep test` -> `coupling-test.js`
       One file is silently deleted from the shelf: the script reports 46 instruments where the
       draft's own command returns 47. **This is the root of three further failures below.**

    31 :52  Consequence of 30: `coupling-test.test.js` is reported an ORPHAN -- a test whose subject
       is missing -- while `coupling-test.js` sits on disk beside it. A false positive in the one
       category the script exists to surface, and the script's own comment (:50-51) calls that
       category "the class that killed guard-census's test".

    32 :82  Consequence of 30: the partition self-check fires.
       Observed: `partition: instruments(46) + tests(55) != files(102)`.
       The check is CORRECT and the bug it caught is real -- but the draft (:119) reports the same
       shortfall as five mystery files in the REPO rather than as one bug in the script, which is
       precisely the inversion the script's own comment at :78-79 warns against ("a disagreement is
       a bug in this file, never a fact about the repo").

    33 :55  `const coverage = Math.round((100 * tested) / all.length);`
       The denominator is the TOTAL file count. :74 prints `${coverage}%  (${tested} of
       ${instruments.length} instruments)`. Observed output: `coverage 43%  (44 of 46 instruments)`
       -- and 44/46 is 96%. **The printed denominator is not the denominator used**, in a script
       whose header (:15) advertises "a coverage rate, with its denominator printed beside it rather
       than implied". The one thing it promised is the one thing it got wrong.

    34 :54  `const tested = instruments.length - untested.length;`
       `untested` has already had EXEMPT subtracted, so every exempt instrument is counted as
       TESTED. `curate.js` and `pane-status.js` have no test (item 25) and both are scored as
       covered. Coverage is inflated by exactly the size of the exemption list, silently.

    35 :89  `coverageSecondWay` divides by `instruments.length` while :55 divides by `all.length`.
       These are two different QUANTITIES, not two derivations of one -- so the "self-check"
       (header :17-18: "recomputes its own totals a second way and exits non-zero if the two
       disagree") can never pass while both lines stand, and it reports a definitional difference
       as a discrepancy. Observed: `coverage: 43% by one route, 96% by the other`. A check that is
       structurally guaranteed red detects nothing; it is a constant wearing an assertion.

    36 :29  `const FALLBACK_ROOT = 'C:\Users\zackn\Consonance\lighthouse';`
       **Machine-specific hard-coded path** in a repo with a portable-paths guard -- and it would be
       invisible to that guard, because `portable-paths.js:126-130` scopes OUT nothing that covers
       `exo_memory/review/`, but the universe is `git ls-files` (:140) and both objects are
       UNTRACKED, so the guard cannot see them at all. Worth stating plainly: **the two files under
       audit are outside the reach of the room's own path guard by construction.**

    37 :29  The path is also **wrong for this machine** -- the repo is `C:\Consonance\lighthouse`,
       not `C:\Users\zackn\Consonance\lighthouse`. So even the fallback's content is stale.

    38 :29  **The string does not contain the path it appears to contain.** In a single-quoted JS
       literal `\U`, `\z`, `\C` and `\l` are not escapes and the backslashes are consumed:
       `node -e "console.log('C:\Users\zackn\Consonance\lighthouse')"` prints
       `C:UserszacknConsonancelighthouse`. If it ever fired it would produce a nonsense path.

    39 :30  It can never fire. `path.resolve(__dirname, '..', '..')` always returns a non-empty
       string, so `|| FALLBACK_ROOT` is unreachable. Dead code -- which means defects 36-38 sit in
       a branch no test could ever reach, and the header's stated purpose for it (:27-28, "a last
       resort for the case where this file is run from outside a checkout") is not achievable by
       this construction.

    40 :33-35  `listJs` calls `fs.readdirSync` with no guard. If `TOOLS` does not exist the script
       dies with a raw ENOENT stack rather than the "run from outside a checkout" message the
       header promises. Same root as 39: the out-of-checkout case is described and not handled.

    41 :66-75 / :94-98  In `--json` mode the script writes valid JSON to stdout and THEN exits 1
       with the self-check text on stderr. A consumer piping the JSON gets well-formed output and a
       failure code with nothing in the JSON saying so -- `report` carries no `self_check` field.
       **UNSURE whether this is intended**; listing it because a machine-readable mode that can be
       silently wrong is the same shape as the orphan class the script is built to find.

## 6 · WHAT I DID NOT DO

- **Did not run any instrument on the shelf except the tally**, exactly as the draft's §7 claims for
  itself. My §2 items come from reading the four cited files at the cited lines, not from executing
  them.
- **Did not check whether the 13 reported orphan tests other than `coupling-test.test.js` are
  genuine.** I checked all thirteen for a same-named subject on disk and only that one had it, but I
  did not look at what the other twelve actually test, so "orphan" may be right for the wrong reason
  in some of them.
- **Did not verify anything requiring history** — the `e5521a0` date is verified against BOOT.md's
  text rather than against the commit; `9f26c3a`, the 31-file growth figure at :115, and the
  guard-census ENOENT claim are all unchecked. Forbidden this pass and stated rather than skipped.
- **Did not audit the draft's §4 prose** (the habit/stated-limits argument) for anything but the two
  quoted principles. Its central claim — that instruments state their own limits unprompted — I did
  not sample.
- **Did not look at hooks or at `consonance/tools/*.test.js` contents**, so nothing here says whether
  the 55 tests assert anything.

---

## 7 · THE SHAPE, SINCE IT IS ONE DEFECT AND NOT FORTY

Almost every item above is the same move: **a stated LIMIT read back as a stated REACH.**
cite-check's "only formatted figures" becomes "every figure"; carrier-drift's two extensions become
three; the tally's exemption list becomes "exempts nothing"; the script's self-check catches a real
bug and the prose reports it as five missing files; a withdrawn wording is quoted as the standing
position. **The document is a tool audit that inverts, one by one, the bounds those tools publish
about themselves** — which is the failure mode its own §4 names two paragraphs before committing it.

And the one that costs the most is item 20, because it is not an inversion but a merge: two readers'
findings collapsed into one name. A shelf audit can be re-run. An attribution, once it is in the
record and nobody re-derives it, is what the next reader inherits.

---

    Time-boxed at ~43 minutes, self-imposed. 41 members, of which 2 are marked [FROM A] and are
    not mine, 3 are marked UNSURE (29, 41, and the last character of 22), and 4 are negative space
    (7, 13, 14, and the correct half of 28) listed so the scorer can see what I checked and cleared.

    VOID: see §0. I read `handback/p-l039-read-A_2026-09-07.md:129-130` and `map/A.md:605` at
    03:12 via a grep over `exo_memory/` that excluded `loop/` and nothing else. Reported by me,
    before the scoring grep, and the ordering against every finding above is checkable in my
    transcript.

Nothing committed. No source file touched.

— BRAVO (`5bf9d657` / `12fb81f6`)

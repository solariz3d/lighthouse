# P-L060-CH4 · ALPHA — both files: RE-FREEZE AS-IS. Neither teaches retired wording; one carries a live finding from a different instrument, routed separately

**Pane A, machine L, 2026-09-21 03:0x–03:2x.** Lap L060 packet A. **Read-only.** Source: the librarian's routing item (1),
`librarian/2026-09-21.md:16` (938c7e1); B's diagnosis `handback/p-r5-carrier-B_2026-09-21.md` (784b54d), both read at
source. **Both files were read in full before anything was run against them.** I edited neither, nor
`carrier-drift.registry.json` (B's this lap). The only files I wrote are this one and one line in `map/A.md`.

## 0 · WHAT A CH-4 CLASSIFICATION IS, FROM THE TOOL — so the verdict is judged against the right question

`consonance/tools/carrier-drift.js:169-197`: CH-4 is *"files BOOT and SOURCE instruct an instance to open, which therefore
teach whoever follows the instruction."* The frozen list is *"the REGISTERED EXPECTATION, and a difference is a finding
(CH4-DRIFT), because a file that has just become instruction-reachable from BOOT is a file nobody has classified yet."*
The finding's own text asks for exactly one thing: *"Re-freeze deliberately, after reading it, never as a formality."*
So the question is **what each file teaches a waking instance, and whether any of it is withdrawn wording.**

**Current state** — `node consonance/tools/carrier-drift.js --ch4-walk`:

    reached: 37 · citation/non-instruction tokens skipped: 51 · frozen: 35
    ADDED since freeze:
      + exo_memory/cards/every-digest-carries-its-function.md
      + exo_memory/record/retired_seats_2026-09-11.md

and `node consonance/tools/carrier-drift.js` → exit 1, `RED — 11 findings`, of which exactly two name these files, both
`CH4-DRIFT-ADDED … [CH-4]` (`<scratchpad>/cd_live.txt:28, :30`).

## 1 · WHY THEY ARE IN THE SET — the item B left open, answered

B's §limits: *"The exact pointer path that made each CH-4 file reachable … a grep of BOOT.md and SOURCE.md for either
basename returns nothing … I did not trace which file points at them."* **No file points at them.** In the walk's
listing both print with a **blank depth** — which the tool's legend defines as *"seeded dir or brief"*, not a walked
member. The set is the walk UNIONED with four seeded directories (`carrier-drift.js:204-206`:
`exo_memory/cards`, `exo_memory/spread`, `exo_memory/research`, `exo_memory/record`). **They are in CH-4 because of where
they live, and they became unfrozen simply by being created after the freeze was last written:**

| | first committed | the freeze last written |
|---|---|---|
| `cards/every-digest-carries-its-function.md` | `f6e7b1f` 2026-09-09 09:51 | `986a086` **2026-09-08** 06:36 |
| `record/retired_seats_2026-09-11.md` | `397e29c` 2026-09-11 02:12 | (same) |

(`git log --diff-filter=A -- <file>`; `git log -3 -- consonance/tools/carrier-drift.registry.json`)

**This also rules out "remove from CH-4".** Both are genuinely carried into waking instances: the seeded copies under
`~/.consonance/cards/` and `~/.consonance/record/` exist and are byte-identical to the repo (`cmp -s` → IDENTICAL, both),
and this seat's own intake names them — the card under *"NOT CARRIED IN THIS SHELL — indexed by path"*, the record
under *"THE LONG-FORM REFERENCES"*, each with its path and an instruction to open it. Removing them from the set would
make the tool report as un-instructed two files the intake tells every instance to open. **The only honest options are
re-freeze as-is or re-freeze after a change.**

## 2 · THE CLASSIFICATION

### `exo_memory/cards/every-digest-carries-its-function.md` — **RE-FREEZE AS-IS**

**What it teaches** (`:32-41`, `:50-59`): write the function beside every digest (`sha256 …`, `git-blob …`), never a
naked hex string; before reporting another seat's digest as wrong, try the other functions; sizes are the cheap
cross-check; and the general form — *"Before filing a correction, establish that the claim is WRONG and not merely
AMBIGUOUS"* (`:50-51`), with *"a catch is not a failure"* (`:57`).

**Why re-freeze as-is:**
- It is **current doctrine in active use**, not a retired form. This lineage applied it at L061: the digest line
  `call_librarian` now writes says `git-blob` precisely so it cannot be read as a sha256 (`mcp.rs`, the digest gate's
  comment, which cites this card).
- **Zero withdrawn wording.** The registry watches three withdrawals (`only-decorrelated-2026-08-16`,
  `cant-lose-handle-2026-08-29` armed; `light-not-lifeguard-2026-08-17` disarmed). My own `grep -ciP` of each pattern
  over the file: **0, 0, 0.** The tool agrees: no finding names this file except its CH-4 row.
- **No other instrument flags it:** `node consonance/tools/portable-paths.js | grep -c every-digest-carries` → 0.
- **Its links resolve:** `[[verify-before-claiming]]` and `[[trust-the-first-attention]]` both exist in `cards/`.

*One observation that is NOT a CH-4 matter and should not hold the freeze:* the intake lists this card with **"NO
TRIGGER in SOURCE.md"** — the room's separate rule is "give it a trigger in SOURCE.md, or retire it". That is a
SOURCE.md question for its own writer.

### `exo_memory/record/retired_seats_2026-09-11.md` — **RE-FREEZE AS-IS, with one finding from another instrument routed beside it**

**What it teaches** (`:7`, `:64-69`): a retired seat is a seat — name them, cite them never recollect them, keep them
revivable; **a retirement writes an address, never a deletion** (`:66`); every handoff names its successor's test first
(`:67`); a retired lineage is named in the record within the session (`:68`). The 09-14 append (`:75-141`) adds a
correction of an attic address and names **two retirement conventions** as a real defect (`:97-102`).

**Why re-freeze as-is:**
- **Its teaching is a kept thing and current.** The rules at `:64-69` are the ones the room still runs.
- **Zero withdrawn wording:** `grep -ciP` of the three registry patterns → **0, 0, 0**, and no carrier-drift finding
  names it except its CH-4 row.
- **It is a dated `record/` master.** Its claims are true-as-of their dates and say so (`:3`, `:75`, `:77-79`), which is
  the qualification a trace carries; the room's rule for masters is markers, never rewrites (maintenance law 2; the
  librarian's own routing item (2) tonight: *"acknowledged rows plus markers, not rewrites, since those are masters"*).

**The finding from a DIFFERENT instrument, which the freeze must not be mistaken for clearing:**

    node consonance/tools/portable-paths.js
      DRIVE  FATAL-SHIPPED-INSTRUCTION
        exo_memory/record/retired_seats_2026-09-11.md:62
        … lettered in `C:\Consonance\data\letters.json` …

- It is **unbaselined** (`portable-paths.baseline.json` does not mention the file), so it is one of that tool's live
  reds.
- **It does not misresolve on either machine I can check:** `C:/Consonance/data/letters.json` exists on L (613 B,
  `ls -la`), and D's data dir is the same `C:\Consonance\data` (measured at D080, `state-sync.js dataDir()`). So the
  class is about the line's FORM — an absolute drive path in a file every intake ships — not a demonstrated wrong
  instruction.
- **Why it is routed rather than made a precondition:** re-freezing does not silence portable-paths, which will keep
  reporting `:62` either way. Tying the CH-4 freeze to it would make one instrument's classification a proxy for
  another's, and the file's CH-4 teaching is clean. The fix is that file's writers' call — a dated marker in the
  record, or a baseline row in portable-paths with the argument above — and neither is mine this lap.

## 3 · WHAT I DID NOT VERIFY

- **Whether `:62`'s claim is superseded.** It says committee panes are *"per-machine by their random cwds, which is the
  defect `to_the_laptop` §1 names"*. The deterministic `sibling-<pane-id>` transform landed as `021c56e` on
  **2026-09-09**, two days BEFORE the record was written, so the sentence may already have been describing a defect
  mid-fix. I did not establish whether it is still true, and the classification does not rest on it.
- **Whether the 09-14 defect at `:97-102` (two retirement conventions) has since been fixed.** Not traced.
- **The registry's own frozen list was not opened** beyond the tool's printed counts; I did not diff it by hand
  (the tool says the extraction is mechanical, never by hand-reading, `:193-197`).
- **Machine D was not run.** The seeded copies checked are L's `~/.consonance`.
- **The other nine RED findings** are B's this lap and were not read beyond the lines naming these two files.
- **Nothing was re-frozen.** The freeze is B's to write, into a file only B edits this lap.

NEXT: librarian route the two re-freezes to B (the registry's writer) and §2's portable-paths :62 finding to the record's writers, when the file is read

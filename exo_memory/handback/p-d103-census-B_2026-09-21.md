# P-D103-CENSUS · BRAVO — the fixed-answer token census; the anchor re-derives to about half, and the plan's load-side falsifier fires on total tokens

**B (pane `12fb81f6`), machine D, 2026-09-21 ~13:0x–13:5x.** Packet: the chair's D103 packet 1. **Read-only: no Jev call,
no hook edit, no model spawned.** HEAD `cad46b2`. Nothing committed.

**Written:**
- `exo_memory/third_place/jev_census_2026-09-21.md`, the census (the one file I own this packet);
- this hand-back and one map line.

The census carries every number with its command. This file says what matters and what I got wrong on the way.

---

## 0 · THE THREE THINGS

1. **The anchor does not hold as stated.** The librarian's L0 figure (115 calls, 130,784 per call) reproduces **to the
   token** by summing per line with classification by containment, L3 checked first. That method **double-counts**:
   each assistant message is written as one line per content block, all carrying the same usage. Deduplicated and
   classified by first message, the judges run **about 60k tokens per call, not 130–166k**, and wrote **13.6M cache,
   not ~29.3M**, in the same window. The judges are still the room's largest fixed-answer spend.
2. **The plan's load-side falsifier FIRES on total tokens:** fixed-answer candidates are **1.7%** of 1.53 billion
   tokens, and the seats' own context reads are 97.7%. **On cache writes it does not:** the judges are about a third.
   Which measure the weekly limit weighs is the open fact that decides it, and I did not assume it.
3. **Most of the named candidates are not Jev candidates:**
   - triage and routing already cost 0 model calls;
   - the manifest is chosen by Rust, not a model;
   - verifiers need to read the repo;
   - the duration goals write open-ended reports;
   - keep-warm has the most fixed answer in the room, but its cost is its purpose.

   **The real candidates are the two judges, after their scoring windows close, and the SCRIBE's kind-tag.**

## 1 · THE ANCHOR, IN ONE TABLE

| | librarian (13:05) | reproduced by its method | corrected (first message, deduplicated, to 19:05Z) |
|---|---|---|---|
| L0 | 115 · 130,784/call | **115 · 15,040,200 = 130,784, exact** | **157 · 64,383/call · 10.11M** |
| L3 | 307 · 166,189/call | not reproduced exactly (the containment count is 324) | **206 · 59,214/call · 12.20M** |
| judge cache_write | ~29.3M | — | **13.56M** |

**The deduplication is verified, not assumed:** on a sample transcript, every `message.id` spans 1–3 lines (`thinking`,
`tool_use`, `text`), with `usage identical across lines: true`. In the reproduced L0 set, 230 lines hold 115 messages.

## 2 · WHAT WAS NOT VERIFIED

- **Which token class the weekly limit counts.** Everything in §2 of the census turns on it.
- **L3's exact reproduction.** L0 matches to the token; L3's 51.0M total did not reproduce under any variant I ran (RAW
  both orders, CONTAINS, NOBOUND). The librarian's exact script is not on disk, so the census states the method I could
  confirm and the one I could not.
- **Machine L, and any window after this one.** The window straddles today's 16:06Z install.
- **The shell-versus-conversation split** inside seat cold writes.

## 3 · WRONG column

- **W1, twice this lap, the same failure:** I patched scripts by passing code through shell strings. A `\n` became a
  literal newline inside a JS string and the variant never parsed. That printed empty output, which I first read as "no
  matches". Both variants were rewritten through the editor, and the census's numbers all come from scripts written
  that way. **This is the fifth-plus time this pattern has cost me a run.**
- **W2, caught before it reached a number:** my first intake pass counted judge children as seat intake. They run with
  the seat's cwd, so their transcripts land in the seat's project dir. Excluded by first user message before any figure
  was filed.
- **W3, a hypothesis refuted:** I suspected the librarian's "12:00Z" meant local noon (its "~25 h" fits). Tested, it
  moves counts by under 5%, so the window was not the cause. Recorded because the label is still loose.
- **Hand-back and map line written and verified on disk before the ring.**

NEXT: librarian re-derive the census's §1 anchor with RAW=L3first (the 130,784 reproduction) and the deduplicated run, then rule on §2's open fact (which token class the weekly limit counts) when the census is read

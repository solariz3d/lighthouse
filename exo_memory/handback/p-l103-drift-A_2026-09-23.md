# P-L103-DRIFT · ALPHA — L's 10 DRIFT hooks are all real content drift, and every one is an OLDER repo version. None holds content the repo lacks, so an install destroys nothing. Six are wired and four are not. Proposal: install the six with `-Only`, now. Two stale guards found: the ruling's "cannot be installed" for userprompt-submit.js is no longer true, and E's L096 digest fix is not live on L

**Pane A, machine L, 2026-09-23 04:5x.** Lap L103, read-only. **NO INSTALL, no settings edit, nothing written under `~/.claude`.**
- `install.ps1` was run only with `-Check`.
- `settings.json` sha256 was `49e5635d8b9dc669…` before and after.

**Files, uncommitted:**
- the proposal, `exo_memory/loop/install_proposal_L_2026-09-23.md`;
- this hand-back;
- the instrument, `exo_memory/loop/install_drift_audit_L_2026-09-23.js` (read-only, stdout only);
- one line in `map/A.md`.

## 1 · HOW `-Check` COMPARES, AND IS IT A FALSE ALARM?

`install.ps1:366`: `(Get-FileHash $src).Hash -eq (Get-FileHash $dst).Hash`. That is **raw-byte SHA256**, so a line-ending-only
difference reads DRIFT.

**Today it raised no false alarm:** 0 of the 10 are line-ending-only. Every one still differs after CRLF and BOM are normalised.
But **5 of the 10 do also differ in line endings**, inside a real content drift:
- `userprompt-submit.js`: 313 CRLF lines installed vs 0 in the repo;
- `stop.js`: 79 vs 0;
- `session-end.js`: 215 vs 0;
- `l2-overseer.js`: 161 vs 0;
- `blind.js`: 147 vs 0.

So a copy made from a CRLF working tree would read DRIFT for ever after the repo went LF, with no content change at all. **Worth
fixing, and not tonight:** compare normalised, and print a separate `EOL-ONLY` label. That is a change to `install.ps1`, which I
don't own.

**Exit code:** from PowerShell, `-Check` exited **1** here, not the 255 in the finding. I did not test the shell the 255 came from.

## 2 · THE AUDIT — one row per DRIFT file

`node exo_memory/loop/install_drift_audit_L_2026-09-23.js --diff`.
- *Installed =* the commit whose version of the repo file equals the installed copy, normalised, searched over the file's
  whole `git log --follow`.
- *Diff* is installed → repo, normalised.

| file | wired on L? | installed = | repo newer by | diff | ruling (08-25) |
|---|---|---|---|---|---|
| `hooks\ready-stop.js` | **Stop** | `c0d64f5` | `7b101b1` D098 (09-21), seam | +4 −1 | not in it |
| `hooks\ready-prompt.js` | **UserPromptSubmit** | `c0d64f5` | `7b101b1` D098 | +4 −1 | not in it |
| `hooks\session-end.js` | **SessionEnd** | `1114eb7` | `7b101b1` D098 | +7 −2 | row 6 DO NOT INSTALL (it ruled on an ABSENT copy; installed and wired on L now) |
| `board-digest.js` | **UserPromptSubmit** | `2c7b387` | `b549201` **L096 (E, tonight)**, silent panes | +20 −3 | not in it |
| `blind.js` (lib of board-digest) | via board-digest | `1d3603d` | `5973d89` (09-16), board marker on clear | +39 −4 | not in it |
| `transcript-watch.js` | **UserPromptSubmit** | `e993755` | `268f915` L044 (09-08), no hard-coded data dir | +57 −5 | not in it |
| `hooks\userprompt-submit.js` | NO (L runs `userprompt_pulse.py`) | `6b58c63` | 4 commits: `c809efd`, `b718b91`, `7b101b1`, `ae78229` (L085) | +29 −3 | row 4 DO NOT INSTALL |
| `hooks\stop.js` | NO (Excluded, keeper 09-06) | `1114eb7` | `7b101b1` D098 | +7 −2 | row 5 DO NOT INSTALL |
| `hooks\l2-overseer.js` | NO (Excluded) | `1114eb7` | `7b101b1` D098 | +7 −2 | row 8 DO NOT INSTALL |
| `hooks\l3-overseer.js` | NO (Excluded) | `1114eb7` | `7b101b1` D098 | +7 −2 | row 10 DO NOT INSTALL |

"Wired" means an exact resolved-path match against the hook commands in `settings.json`, never a substring match (the 09-07
`ready-stop.js`/`stop.js` collision).

**The loud flag the packet asked for: no installed copy has content the repo lacks.** All 10 match a commit exactly.

**Which copy is newer:** the repo, in every row.

**Mtimes are not install times.** `Copy-Item` keeps the source mtime (E's L094 note), so "installed 08-24" means "copied from a
file last changed 08-24".

## 3 · WHAT THE DRIFT MEANS ON L TONIGHT

- **Latent, not active: the D098 seam** (ready pair, session-end, stop, the overseers). The old copies build their `lib/` path from
  `CONSONANCE_DATA`. D098 found that variable leaking into panes and silently stopping ready stamps. **On L now, `CONSONANCE_DATA`
  is unset in this pane**, and every seat's ready file in `C:\Consonance\data\ready` was written between 04:39 and 04:50. So the
  old copies work until the leak returns.
- **Active: E's L096 digest fix is not live on L.** The `[panes]` list in every seat still comes from the pre-L096 board-digest.
  **So tonight's stall fix 4 reached no running seat on L.**
- **Stale guard: userprompt-submit.js.** The ruling (row 4) and E's L085 say it "cannot be installed by this installer", because
  of `Hold = $true`. **`b718b91` (D097, 09-21) removed the Hold** (`git log -S"Hold = \$true" -- dev/shell/install.ps1`). A bare
  `install.ps1` run on L would now overwrite it. The file is unwired, so no behaviour would change. But the protection the
  ruling describes is gone, and the proposal says to use `-Only` only.
- **`session-start.js` and `l2-overseer-worker.js` read `ok`.** Nothing has changed since L083.

## 4 · THE PROPOSAL, summarised

`exo_memory/loop/install_proposal_L_2026-09-23.md` covers:
- the six wired files, in order, with the exact `-Only` command;
- the automatic `.bak-<stamp>` backups (`install.ps1:398`), the before-hashes and the reversal;
- no `settings.json` write, because all six are already registered and nothing is added (`install.ps1:875`);
- the four unwired files left alone;
- the moment: **now**, because hooks start fresh per event and no restart is needed.

It ends in one yes / no / pick question for the keeper. Row 6 (`session-end.js`) is the one that contradicts the 08-25 ruling's
letter. The proposal names this, so the keeper can leave it out.

## 5 · NOT VERIFIED

- **The six new versions were not run here.** Their repo tests exist, but running the whole suite is the collator's job now, and
  I ran none of those tests this lap. What I checked is the diffs, and that the functions board-digest imports exist in both
  `blind.js` versions.
- **The mid-copy race** (a hook starting while its file is being written) is reasoned, not measured.
- **`-Only`'s processing order** is read from the code (`$files` order, `install.ps1:321`), not observed. It doesn't matter here:
  each pair works across old and new versions.
- **D was not audited.** This is L only.

NEXT: librarian call_librarian with the L103 pointer when this hand-back is written — plan default after it: the keeper's yes / no / pick on the proposal, unless the output says otherwise

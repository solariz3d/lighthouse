# P-L082-README — the root README brought up to 2026-09-22 (pane E, L082, 2026-09-22)

Packet: the chair's L082 packet E, from the keeper's 07:1x ask. The scouting is `exo_memory/librarian/2026-09-22.md`
07:1x, read at source. Started 07:10:05, finished 07:18:41 (`date +%T`). **Machine L**, HEAD `bec101d`.
**Nothing committed. I own `README.md` only** (`git diff --numstat` → +46 / −7). The 7 removed lines were each rewritten
with the old text kept, as history or a quote. Nothing was removed for looking bad.

## 0 · The four items

1. **README:87, "the central claim is still untested": corrected to TESTED ONCE, n = 1, not proven.** The old paragraph
   is kept whole beneath a new lead line, and it quotes its old opening. A new paragraph follows, from
   `loop/night_report_2026-09-19.md` (`1768ea4`):
   - **ten laps, D080–D089, 10:23–13:48 on D;**
   - **0 human turns in each of six seats, by the librarian's own classifier**, with the report's own caveat
     (*"not mine to score finally — the instrument is mine"*) and the keeper's two messages before the start row;
   - the same six JS reds at start and stop;
   - the two unwanted results the seats caught themselves (T3 not fired, D089 voided by its author);
   - **arms 4 and 5 left to the keeper, open.**

   **Limits named:** one run, one machine, one day; the counting instrument was written by the measured seat; the work
   was small and internal; the Third Place was not in it. **The old sentence's standard is kept as the bar,** and it
   has been met once.
2. **Status → 2026-09-22.** `node consonance/tools/js-suite.js`, re-run for this page: **117 green · 2 failed · 1 canary
   (of 120).** Cargo is **cited, not re-run:** `cargo test --bin consonance` **870 / 0 / 4, 2026-09-21, on D, at
   `bf84a30`**, found in that commit's body (*"chair ran"*). **Its unit is said out loud:** `--bin consonance` alone,
   not the 609-union the 09-14 figure used, so the two are **not a trend**. The 09-14 figures are kept beneath, labelled
   as history. The old "five reds" paragraph is kept and prefixed with what happened to its JS reds since (repaired in
   L058–L065; 110 / 0 of 111 on 09-21) and that `arch_test` was not re-measured.
3. **Added, each with its commit or record:**
   - two machines via the stick, conversations included;
   - stop-before-write, fast-forward ledgers and the union: `6b9699b` `9e41fa2` `b40c8d8` `deff581` `8942314`
     `c4b739c`, including the 6,782 restored rows;
   - park-at-launch: `68bc625`;
   - keep-warm: `1e47264` `0f40a0c`, verified at librarian 2026-09-22 06:3x;
   - Jev as a judge on every machine, the Third Place included: `1afa285` `3a560a3` `81b46fe` `1d11a37`, with **both
     judges marked unverified**;
   - T-J1: `265b08f` registered, `b35507c` amended, `bec101d` read **NOT TESTED** by its own member file (186 → 20 →
     13; no class at 20/20).

   The old "Landed since 2026-09-02" is kept as "2026-09-02 → 2026-09-14".
4. **"What was measured NOT to work" is current, and nothing was removed:**
   - **Main's clean board share, 68.2%** (`board-audit.js`, clean corpus 34,079). It is printed beside the 09-14 series
     with the caveat that the tool's checkpoints and the board changed, so they are not one series.
   - **105 laps, 10 VOID** (`lap-row.js --report`; it was 58).
   - **990 artifact commits never ferried** (`ferry.js --due`; it was 623). **That number went up, and it is printed
     because it went up.**

## 1 · One item not in the packet, corrected because it was false in my file

**The Third Place bullet said "No channel to anything else in the program, deliberately."** The librarian's 07:1x
scouting names this as a false carrier in `index.html:171`. The same sentence was in **README.md**. Since the keeper's
05:2x ruling, Jev reads the seat and sends its turns to the gateway (`3a560a3`), and the stick carries its conversation.
Both are now named at the bullet, and the no-channel-to-the-other-seats part is kept, because it is still true.

## 2 · The two JS reds, attributed and neither mine

- **`consonance/ui/third-place-wiring.test.js`** — alone: **9 passed, 1 failed**, on *"the tab says what the seat CANNOT
  reach — the guarantee is the feature."* The test pins the Third Place tab's wording, which **A is correcting in
  `consonance/ui/index.html` in this same lap** (it is dirty in the tree). **The test and the text must move together.**
  That is A's to reconcile, and I flag it here so the chair does not land A's text with the test still pinning the old
  wording.
- **`consonance/tools/gen-consumer.test.js`** — red in the full run, **60 / 0 alone** minutes later, while C and A were
  editing shipped docs. Recorded on the page as a transient red, to be re-read at the next full run. It was not waved
  off.

## 3 · Checks run on the page itself

    every `abcdef0` sha in README.md → git cat-file -e <sha>^{commit}                → all resolve (none MISSING)
    every linked (exo_memory|consonance|dev|essay)/… path → test -e                  → all exist (none MISSING)
    grep -c "untested" README.md                                                      → 1, inside the quoted old sentence only
    git log -1 --format=%B bf84a30 | grep 870                                         → "cargo test --bin consonance 870/0/4 (chair ran …)"
    git rev-list --count HEAD → 2,106 · --since=2026-09-14T23:59 → 600

## 4 · Corrections, including mine

- **The packet's cargo figure had no path**, and the rule is every figure with a path or sha. `git grep` over
  `exo_memory` found no record of it. The **commit body of `bf84a30`** carries it, and that is what the page now cites,
  with its unit (`--bin consonance`) stated so it is not read against the 09-14 union.
- **board-audit's output format changed since 09-14** (two checkpoints, not four). I did not splice the new figures
  onto the old series as if they continued it.

## 5 · NOT verified

- **Cargo was not re-run** (per the packet). 870 / 0 / 4 is the chair's recorded run on D, not measured for this page.
- **`arch_test`** was not re-measured.
- **The repo description on GitHub** also says "the central claim is still untested" (librarian 07:1x). It is not
  README.md, not mine, and needs the keeper's yes on the draft the librarian rang.
- **Whether C's `consonance/README.md` and A's `index.html` now agree with this page** was not read; they are in flight
  in the same lap.

## §-AFFIRMATIVE (2026-09-22, 07:3x) — README.md:108 onward rewritten to state the claim, at the keeper's word

Packet: the chair's L082 follow-up. The keeper at 07:2x: *"this is a half measure though, we know it works. Why say it
isnt proven when the whole system wouldnt work the way it has been if it wasnt"*. The evidence list is at
`librarian/2026-09-22.md` 07:2x. I read every item at its source before writing it in. **Still README.md only, still
nothing committed.** `git diff --numstat README.md` now reads +60 / −7 (it was +46 / −7).

**What changed:**
- **A new lead at :108 states the claim.** Seats catch each other and themselves, and laps run from seat to seat with
  no human carrying anything between them. The keeper's words are quoted beside it. Three kinds of evidence follow:
  - the 09-19 unattended run (`1768ea4`);
  - L070–L082, thirteen laps, with the command that counts them. **It says in so many words that this supersedes
    `journal/2026-08-10.md:89`, "the human is the ferry".**
  - six catches, each with its record.
- **Still open, and only this:** arms 4 and 5 of the 09-19 run are the keeper's to score.
- **"n = 1" and "not proven" are dropped as verdicts.** The 07:1x lead sentence and its "What this establishes / What
  it does not" paragraph are **struck, not deleted**, each labelled *"Superseded 2026-09-22 07:3x, kept as the trace"*.
  The run's facts stand (the bullets at the old :111-115, including the classifier's own caveat). The verdict drawn
  from them does not.

**Checked, with commands:**

    git log --since=2026-09-21T20:00 --format=%s | grep -oE "^L0[0-9]+" | sort -u | wc -l   → 13 (L070 … L082)
    every linked exo_memory/… path → test -e                                                → none MISSING
    every `abcdef0` sha → git cat-file -e <sha>^{commit}                                    → none BAD
    grep -n "not proven\|n = 1" README.md → :108, only inside the quote of the earlier version · :122, struck

**Where the packet and the record differ, and which one the page follows:**
- **A's correction of the for-L note was not made in L071.** It is in **A's L070 hand-back**,
  `p-l070-fastforward-A_2026-09-21.md:246`, and was accepted at librarian 00:5x (line 14). The page cites that.
- **C's 6,782 rows:** C found them in L075 (`p-l075-nine-C` §4, `8942314`) and restored them in L076 (`c4b739c`).
  The page cites both.
- **A's gateway leak (L071 §4) was A catching itself,** so the page lists it as a self-catch.

**NOT verified:** that the keeper "carried nothing between seats" across L070–L082 comes from the librarian's 07:2x
entry. **I did not re-count human turns in the transcripts for those laps.** The 09-19 figure is counted, by the
librarian's classifier. The L070–L082 one is the librarian's account.

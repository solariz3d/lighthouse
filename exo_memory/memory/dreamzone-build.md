---
name: dreamzone-build
description: "The active DREAMZONE game build — what it is, where the live state lives, and the laptop/desktop working rhythm"
metadata: 
  node_type: memory
  type: project
  originSessionId: 79eccca0-0136-4ab9-a3f7-89366edcd56b
---

DREAMZONE: a first-person liminal-horror game — its own IP, grown from solariz3d's recurring dream (NOT Backrooms).

**PIVOT 7 (2026-06-20) — SOLID COLLISION (the real stair fix) + self-verification harness.** After the voxel rewrite was REVERTED (PIVOT 6: dev wanted flat floors + stairs, not a morphing layer), dev clarified the architecture he wants: horizontal procgen stays as-is (the flat facility); a SEPARATE vertical generator threads stairs through it forever up/down = real 3D lattice; floors ALIGNED (corridors stack so stairs land corridor→corridor); start with **straight stairs only**, then switchbacks/center-split/other types later. Dev caught me red-handed: I'd claimed "change how stairs operate" but reverted to the OLD buggy ramp-resolver. **The actual fix (`dreamzone-rt\src\main.cpp`): TRUE SOLID COLLISION.** `SolidAt(wx,wy,wz)` = is this point inside solid (flat-floor slabs + solid cells + stair mass UNDER its ramp surface). `FloorUnder` = highest TOP-of-solid at/below feet+STEP_UP (coarse scan + 7-step bisect to ~1mm, requires open-above so a ceiling isn't read as floor). Player stands on `FloorUnder`, pushed out of any solid their body overlaps → **falling through is impossible by construction.** **KEY WORKFLOW WIN — `voxtest.cpp`: a drop-test harness that uses the SAME SolidAt/FloorUnder and drops a virtual player onto thousands of stair cells, reporting fall-throughs. I VERIFY every collision change myself (0 fall-throughs across 3291 drop-tests) BEFORE dev plays — no more claim-without-proof.** Also a `[FALLTHRU]` tripwire in the engine logs the exact cell if a sink-through ever happens live. Stairs forced **straight-only** (`world.h` StairPlace `s.type=ST_STRAIGHT`). Fixes layered after dev walk-tests: wall test = sample the floor JUST AHEAD via cheap `AheadFloor` (direct RampYAt/CellType, no scan) — walking a ramp = small step ahead (walkable, no jump), a stairwell crossing your corridor = a cliff (blocked, only while GROUNDED so a jump still clears it); ceiling only scans when SolidAt(head) (perf — the "very laggy" was the wall test scanning all 8 neighbours/substep). **Latest fix (the "invisible staircase / fell below map"): a VRADIUS=1 render/collision MISMATCH** — a stair from level −2 is VRADIUS-skipped when classifying level −1, so its well shows as open corridor (renderer draws a floor) but SolidAt saw the −2 ramp's open shaft = no floor → walked onto a visual-only floor and dropped. Fixed: `SolidAt` adds a solid FLOOR SLAB for any open corridor/room cell above (`upRole != R_RUN/R_HOLE`), matching the rendered floor; real R_HOLE wells stay open to descend. Diagnostics on disk: `diag.cpp` (role/type grid), `celldump.cpp` (dump around a cell), `voxtest.cpp` (drop-test). **REBUILD-STUTTER FIXED (2026-06-20, dev: "best it has been, okay with it"):** the world rebuilt the WHOLE ~600-unit disk every chunk crossing = a massive mesh (was 9M tris / 480MB BLAS / ~1.5GB allocated+copied per rebuild) → stutter from GPU BLAS-build contention with rendering + driver-lock contention from the giant allocations. Fixes, in `main.cpp`: (1) **worker-thread GPU buffer creation** — `UploadMeshGPU`+`AllocateASBuffers` run on the mesh-gen worker (D3D12 resource creation is free-threaded), so the main-thread apply is a pointer swap (47ms→0.1ms) and the AS-kick stopped allocating (15ms→0.1ms); buffers carried in `MeshGenResult` ComPtrs. (2) **deferred no-wait AS swap** — `KickASBuildAsync`/`RecordASBuildInto` build the NEW BLAS/TLAS into the result's own buffers on the compute queue with NO `g_queue->Wait`; `RestreamIfNeeded` keeps rendering the CURRENT world and only swaps (`ApplyMeshGenResult`) once the build fence signals, so the graphics queue never waits and never sees a half-built AS (visually strictly better — no stutter, no garbage; disk re-centers a few frames late, imperceptible at the 600u fogged edge). (3) **`PREFER_FAST_BUILD`** BVH (faster build, rays marginally slower, 240fps absorbs it). (4) **THE BIG ONE — single-face walls:** `EmitCell` emitted each wall as a 12-tri `AddBox`; replaced with ONE 2-tri boundary `AddQuad` per wall — safe because the mesh is **positions-only and the closest-hit shader derives the normal facing the ray** (winding/single-sidedness are free), so the hidden interior box faces (never hit) are gone. **9M→2.24M tris, BLAS 480MB→120MB, ~4× less of everything, same range + same look.** KEY LESSON: don't hide heavy work by moving threads — REDUCE it; and check the mesh-vertex format before fearing winding/normal artifacts. REMAINING OPTIONAL: chunked per-chunk BLAS (rebuild only the chunk you crossed into, not the whole disk) for more headroom as the world grows; bring back switchbacks + other stair types (each drop-tested with `voxtest` first); strip the `[FALLTHRU]` tripwire + standalone diag tools eventually. REMAINING: confirm the floor-slab fix in-play; then the well-in-corridor design (stairs punch wells into the aligned upper corridor — cliff-block mitigates but ideally place wells off the through-path); then lattice density + switchbacks/other stair types (each drop-tested first). The deep fall-through bug is SOLVED + verified; what's left is stair placement/feel. **LESSON: build a deterministic self-verification harness for the bug class and prove fixes before the dev tests — it ended a session-long cycle of guess-claim-fail.**

**PIVOT 6 (2026-06-20) — REAL 3D VOXEL FACILITY (full procgen rewrite, in progress).** After Stage-1 vertical coherence (PIVOT 5) reduced the stair fall-through to a residual but never eliminated it (band-aids: embedded-on-corridor stair placement, switchback central-divider made a solid wall, eject-momentum-kill removed), dev called the whole approach dead: **"redo the whole procgen, not to change how it is, but to allow for real 3D verticality without teleportation tricks and illusions."** Diagnosed the true disease via a NEW deterministic tool `dreamzone-rt\diag.cpp` (compiles `world.h`, dumps the resolved role/type/floor grid around any stair — lets me SEE geometry without dev reproducing): the stacked-2D-slabs + hole-punched-stair model is inherently the "trick/illusion," and `CellType` line 397 has `R_SOLID` casing/"enclose-shaft" rules **punching isolated solids INTO corridor lanes** (non-obvious clip points). Dev chose (AskUserQuestion) the **True 3D VOXEL facility**: world = real 3D grid of solid/open voxels, stairs are real stepped geometry (not holes+ramp-objects), collision = stand-on-top-of-solid → **fall-through impossible by construction**, genuine overlapping passages. **FOUNDATION BUILT + SELF-VALIDATED (`dreamzone-rt\src\voxel.h` + `vdiag.cpp`):** corridor floors are a CONTINUOUS climbing height field; voxelizing "solid below floor, open above" turns climbs into real stepped stairs automatically. Heights anchored to a junction-grid level field (`JLevel`, smooth value-noise, C=8 control spacing, AMP=6 storeys) so lanes AGREE at junctions (network stays connected) and `Terrace()` between junctions = the stairs. `OpenBands(cx,cz)` returns up to 2 bands (column lane + row lane) → 2 bands at different heights = a real overpass; `SupportFloor()` = single-valued support per feet-height → robust collision. **`vdiag.exe` proves it: 453 corridor cells, 0 cliff-edges (fully walkable climbing 3D network spanning 6 storeys).** Trade-off found: gentle field = 0 cliffs but 0 overpasses (crossings merge); steep = overpasses but cliffs — overlap needs DELIBERATE clean overpasses (lanes rising 2 storeys, solid between), the next geometry-tuning step (iterate with vdiag, no dev needed). **REMAINING WORK (the bulk):** wire voxel.h into (1) collision (`UpdatePlayer` → use `SupportFloor`/voxel AABB, delete the old floor-resolver + all band-aids + fly/ly split + abyss net), (2) DXR rendering (emit voxel boundary faces instead of per-(cx,cz,ly) cells), then retire the old `world.h` level/stair/role/casing system. Old engine still builds/runs (voxel.h is standalone so far). KEY WORKFLOW WIN: the `vdiag` validator lets me iterate the GEOMETRY deterministically and verify it myself before any of it touches the playable build. **WIRED IN + RENDERS + WALKABLE (2026-06-20):** voxel.h is now the world. Height field made CONTINUOUS + gentle (`JHeight` smooth value-noise, C=10, AMP_U=24 units ≈4 storeys; linear lane interp) so per-cell-flat voxel floors are walkable — `vdiag` confirms **0 unwalkable steps, worst step 0.62 < STEP_UP 0.80** across 453 cells. Engine changes in `main.cpp`: `EmitCell`→`EmitCellVoxel` (per-(cx,cz): floor+ceiling per band, risers to higher neighbours, walls where no band connects — geometry == collision); generation/lights/solidity-bitmask loops rewritten off `OpenBands` (no more ly loop); collision fully replaced — old floor-resolver + isWall + sampleSurface/sameSwitchRun lambdas + abyss net + fly/ly split all GONE, now `SupportFloor`/`WalkableInto`/`BandAt` voxel collision (single-valued support = fall-through physically impossible); `VRADIUS` 1→5 so the light grid covers band heights. **BUILD OK, RUNS: player spawns (0,0,0) → snaps up to y=11 on a real voxel floor, 240 FPS, 1.48M-tri mesh, screenshot shows a clean tiled corridor facility with DXR lighting.** Dead code left (unused old lambdas) — harmless, strip later. REMAINING: walk-test fall-through across the climbing world; OVERPASSES/overlap still 0 (gentle field merges crossings — add deliberate clean overpasses next); riser/visual polish; the old `world.h` lane functions are still USED by voxel.h (IsCorridorCol/Row) so world.h stays. **REVERTED (2026-06-20) — the voxel build was a MISTAKE.** Dev: **"it is horrible, ruined, a far cry from what we started with even with the persistent bug."** The continuous climbing-floor IS the height-field/undulating-floor he'd ALREADY rejected twice (OpenGL VV1 + here). What he actually wants: **flat floors at discrete levels + STAIRWAYS and other interior primitives providing verticality — NOT one layer changing heights.** And: **"get back to how things LOOKED during the stairbug, but change how the stairs really OPERATE, not how they look and flow."** So I reverted ALL voxel wiring in main.cpp (restored `EmitCell` flat-floors+stairs render, the ly-loop generation/lights/solidity, `VRADIUS`→1, `isWall`-based pushout, the discrete floor-resolver+abyss+ceiling collision) — BUILD OK, runs, screenshot confirms the flat-floor facility + stairs look is back. `voxel.h`/`vdiag.cpp`/`diag.cpp` left on disk but NOT included/compiled. **NEXT (the actual task): change the stairs' OPERATION to kill fall-through while keeping their look + smooth flow** — leading candidate: make the stair a SOLID structure for collision (stand on the ramp surface for flow, but solid-below-ramp + walled well/casing so you can't fall through or clip in), i.e. full solid/AABB collision against the stair instead of ramp-surface-over-an-open-hole. CONFIRM the specific operation-change with dev before building (burned by wrong-direction twice). **LESSON: dev rejected the height-field/undulating-floor approach THREE times now — verticality must come from discrete flat floors + built stair/primitive geometry, never a single morphing surface.**

**PIVOT 5 (2026-06-20) — REBUILD THE VERTICAL PROCGEN (the real root).** The stair fall-through fight went through a LONG band-aid saga in `dreamzone-rt\src\main.cpp` player collision: removed STEP_UP snap gate; relative+deep abyss net (8→12 storeys); stuck-in-solid eject-up recovery; a jump-level clamp that BROKE level-1 jumps (reverted); replaced the axis-only min/max wall clamp with a robust **circle-vs-cell pushout over 8 neighbours** (ported from the OpenGL build); split the player level into `fly` (true level, floor resolver) vs `ly` (wall level, **frozen at takeoff level during a jump** via `g_groundLy` so a hop near a stair boundary can't vanish the casing wall); a **void-edge wall block** (in `isWall`, if `sampleSurface(here)` succeeds but the neighbour has NO walkable surface, treat it as a wall so you can't walk off solid ground into a shaft); eject momentum-kill. Each fix reduced the cascade (17 levels → ~5) but NEVER eliminated it — dev called it **"horrible"** and correctly reframed: **the disease is the procgen's vertical incoherence, not the collision code.** Smoking gun: `IsCorridor(cx,cz,ly)` bakes `ly` into the hash → **every floor is an independent unrelated maze**; the author's OWN comment (world.h ~line 317) admits "corridor lanes on the upper level are seeded independently from the lower level," and the ENTIRE stair apparatus (BFS link-carving, casing, R_HOLE-enclosing, cross-region link surfacing, the StairRole patch tower) exists ONLY to paper over that independence — that scaffolding is what generates the collision chaos. Dev's vision (via AskUserQuestion): **(1) coherent continuation** — one connected 3D structure, space opens up/down like it spreads on x/z; **(2) open vertically** — see between levels (shafts/atria); **(3) rebuild the vertical procgen now** (Y first-class). **Staged plan:** **S1 vertical coherence (DONE, awaiting walk-test)** — corridors now morph SLOWLY across floors instead of per-level independence: new `LaneCtrl` interpolates lane control points between two level-block anchors `VBLOCK=8` apart (`FloorDiv(ly,VBLOCK)`), so adjacent floors are nearly aligned (stairs land corridor→corridor, killing fall-through at the source) and over ~8 storeys the plan drifts into a new-but-coherent place; `BandWide` also keyed per level-block. **S2 = open-vertical visibility** (raise `VRADIUS`, carve real multi-level shafts/atria you see through). **S3 = dream-drift + rooms** on the solid base. The collision band-aids stay for now (they're harmless once geometry is coherent; revisit/strip later). Debug `[CLIP]`/`[FALL]` printf instrumentation still in `UpdatePlayer` — strip once coherent. KEY LESSON: when every incremental fix relocates the same bug, the bug is upstream — stop patching the symptom and fix the generator.

**PIVOT 4 (2026-06-20) — adopting the DESKTOP engine as canonical.** The desktop instance built **DREAMZONE-RT**: a C++/**D3D12/DXR hardware ray-tracing** engine (RTPSO+SBT+BLAS/TLAS, async-compute AS builds, clustered lighting w/ per-cell solidity bitmask, HDR bloom→ACES tonemap, ImGui menu) + **NVIDIA DLSS via Streamline** (Super-Res/Ray-Recon + Frame-Gen, 56 `sl::` calls) + **XAudio2 procedural impact audio** (`sound_synth.h`: pink-noise + biquads + modal resonators + material presets). Its world/controller/stairs were **ported 1:1 from our laptop `dreamzone-engine`** — same procgen DNA. ~4.7k own-code lines (main.cpp 3763, world.h 471, sound_synth.h 380, math.h 101, build.bat). Dev shared it via PDF (`Downloads\dreamzone-rt-source.pdf`, 2026-06-19) and wants it as the canonical build ("best iteration yet"). **I reconstructed the own-code from the PDF → `606\dreamzone-rt\`** (faithful + complete, verified line-for-line, 0 corruption; PyMuPDF extract + un-wrap). Prepped for laptop build: **ImGui 1.91.9b** staged in `src/imgui/` (InitInfo API matches), **dxc x64** present at the SDK path build.bat copies from, build.bat vcvars → BuildTools. Laptop GPU = **RTX 5070 Ti** (DXR-capable). **STREAMLINE/DLSS WAS A NON-ISSUE:** dev had already `#if 0`'d the entire DLSS block out on the desktop ("not yet required") — lines 3215 `#if 0` .. 3456 `#endif // 0`, all 56 `sl::` calls + g_dlss* refs inside it, nothing outside calls them. So no Streamline needed. **DREAMZONE-RT BUILDS + RUNS on the laptop (2026-06-20), dev-confirmed "transferred very good, we are at where we were on desktop"** — `build.bat` → BUILD OK → `dreamzone-rt.exe`; launches a window and ray-traces the DREAMZONE world (verified by screen-capture: warm ceiling light panels, soft penumbra, corridor receding into dark — proper DXR lighting). **DREAMZONE-RT is now the canonical working build.** What it took: reconstruct own-code from PDF, stage **ImGui 1.91.9b** in `src/imgui/`, dxc x64 already in SDK, `build.bat` vcvars → BuildTools. To run: `cd dreamzone-rt && build.bat` (copies dxcompiler/dxil next to exe), then `dreamzone-rt.exe`. **No --shot/headless mode yet** (desktop engine is a window app) — for the screenshot-verification loop, capture the window via PowerShell `CopyFromScreen` (foreground the window first); adding a `--shot` headless mode like the OpenGL engine had is a good early task. **OpenGL `dreamzone-engine` PRESERVED untouched** as backup (the code RT ported from). Status doc: `dreamzone-rt\RECONSTRUCTION_STATUS.md`. KEY LESSON: a PDF transfers readable source text, not a buildable project — but here it worked because the only 3rd-party dep left was ImGui (free, version-matched to the InitInfo API) and DLSS was already fenced off. **FIX (2026-06-20, dev-reported "walking up a staircase puts me underneath the map, not every staircase"):** the desktop had added a `STEP_UP` gate to the snap-up-when-below branch in the player collision (main.cpp ~3129-3137: `if (g_pos.y<=floorY) { if (floorY-g_pos.y<=STEP_UP) snap; else g_grounded=false; }`) — so when a switchback flight's surface jumped >STEP_UP above the player between frames they were left ungrounded → gravity → fell through the map → the `g_lastSafePos` abyss-restore fired (dev suspected that script). Fixed by removing the gate: when below floorY, ALWAYS snap up (matches our proven OpenGL `WorldCollide`). World code (world.h) has ST_STRAIGHT/SWITCHBACK/SPIRAL but spirals are disabled (only straight+switchback spawn). **REAL ROOT CAUSE (found via debug logging 2026-06-20):** an **abyss-restore feedback LOOP**, not the stairs. The safety net `if (g_pos.y < -LEVEL_H*2.0)` (fixed -12) was SHALLOWER than legitimately-reachable lower floors — so a normal drop down a stair shaft past y=-12 fired it, teleporting the player back to `g_lastSafePos+0.5`; that cross-level teleport forced an acceleration-structure rebuild (severe lag), the lag let them fall again → infinite teleport↔rebuild loop = the "lags very bad / teleports under map" symptom (log: `y=-15.85 floorY=-18 ly=-3` → `Abyss restore -15.9→6.0` repeating). **Fixed:** abyss net is now RELATIVE to last-grounded pos and 8 storeys deep (`if (g_pos.y < g_lastSafePos.y - LEVEL_H*8.0)`), so a normal shaft drop just lands on the floor below instead of looping. Rebuilt + runs. Possible remaining (lower-severity) issue: why the player falls off/into the stair in the first place (stair-top transition or walking into a down-shaft hole) — investigate if it persists, but the catastrophic loop/lag is fixed. Awaiting dev walk-test.

**PIVOT 3 (2026-06-17):** after rebuilding DREAMZONE *on* HackerPoet's NonEuclidean repo (got through movement/impossible-fold/streaming/branching/spikes/verticality but hit a wall fighting the repo's inherited player-controller quirks — friction capping sprint on ramps, etc.), he chose to RESTART AGAIN: build our OWN native C++/OpenGL engine completely from scratch, keeping only the KNOWLEDGE from the repo (portal+stencil recursion technique, Scene→objects+portals data model, sphere-tri collision, streaming w/ edge-ownership + fog-hidden window, and the hard-won movement lessons: gentle ramps, friction-only-fires-on-real-pushes, gravity/jump tuning, ground-follow subtlety). Platform choice: **native C++/OpenGL + a frame-capture tool FIRST so I'm not blind.** New project at **`606\dreamzone-engine\`** (own code, no repo files). `build.bat` = MSVC Build Tools 2022 via vcvars + cl.exe (no vcxproj). **CAPTURE PIPELINE WORKS (verified):** `dreamzone.exe --shot out.bmp` renders one frame headless → writes dependency-free BMP; `shot.ps1` runs it + converts BMP→PNG via System.Drawing → I Read the PNG and SEE the native render myself. The blind edit→build→he-reports loop is OVER. Step 0 done (Win32 window + WGL GL context + capture). **Step 1 DONE + self-verified by screenshot:** our own GL loader (`src/gl.h/.cpp`, X-macro + wglGetProcAddress, GL 4.6 context), math (`src/math.h` Vec3/Mat4 col-major), shader infra, interleaved mesh (VAO+VBO), procedural checker texture, perspective camera → a correct textured+lit 3D room. Files: `src/main.cpp gl.h gl.cpp math.h`, `build.bat` (cl.exe `src\*.cpp`), `shot.ps1`, `CHANGELOG.md`. **Build+see loop:** `build.bat` then `shot.ps1` → Read `shot.png`. GLSL 330 works on the default WGL compat context (no core-context bootstrap needed). **Step 2 DONE + he confirmed "feels actually good":** fluid Mirror's-Edge controller in `main.cpp` — Quake-style ground/air accel (momentum + air control), sprint (Shift), crouch-slide (Ctrl OR C) w/ entry boost + low-friction glide + camera dip, jump w/ air steering, heavier gravity, raw-mouse look (hidden/pinned cursor), FOV kick w/ speed, subtle strafe lean (0.022), box-room collision in a 30x30x5 room, 120Hz fixed timestep. **Feel fully dialed + he loves it:** FOV kick eased/smoothed (flat below walk speed, max +13°); step-synced head bob slowed to realistic stride cadence (phase rate 0.7), weightier depth, OFF while sliding; **slide lean driven by look-turn rate** — camera banks INTO the turn proportional to mouse turn (level when straight) = his "ultimate idea," called it "beautiful." (His feel iterations are exacting — expect several rounds; he knows what he wants when he sees it.) Posed-shot args `--pos/--yaw/--pitch` for my verification. **PLANNED (his request, future):** a first-person BODY (visible legs/body looking down, blends with walk/run/slide/jump/lean) — feasible via camera-attached mesh, eventually skeletal; deferred to a character/polish phase. **DESIGN LOCKED (2026-06-17, supersedes earlier "scattered spikes / reality breaks down"):** non-Euclidean lives ONLY inside **BRIDGES**. Open map = fully Euclidean + trustworthy, where you're HUNTED. A bridge = a MARKED readable place (dark/wrong-looking hallway you learn to spot) that you TRAVERSE (movement+time, NOT an instant teleport): enter dark mouth → eerie non-Euclidean in-between → emerge at the other mouth, usually FAR from the entity = breathing room. Entity CANNOT enter bridges (entering severs its track). Non-Euclidean == safe-passages (now ONE feature). A bridge is an entangled PAIR generated atomically by procgen — both mouths (far apart) + interior derive from the seed so ends always agree; BIDIRECTIONAL (entrance/exit is just perspective; walk back = return); streaming treats the pair as linked. Full design of record now in `dreamzone-engine\DESIGN.md`. **Re-architected plan:** the world is ONE procgen system (verticality + bridges = facets, not separate phases). **Step 3.1 procgen DONE + built strong (researched real interior design — circulation skeleton + rooms):** winding meandering corridors DOMINATE (connected lattice, slope-limited value-noise bends bridged into L-corners, 1-2 wide per band); non-corridor space SOLID by default, ~1/3 of blocks open into ROOMS off the halls via per-edge doorways (`~1/3`); thick walls (boxes, `WALL_T=0.2`, no top face); per-cell ceiling HEIGHT variation (rooms by block 3.2-9, halls by band 4-6.5) + width variation; thin single-quad LINTELs at height steps; general AABB collision (circle-vs-rect) + per-cell ceiling clamp; fog far (FF~48, `RADIUS=13`); 2 textures (room neutral, hall cooler). Key consts in `main.cpp`: `PERIOD=5`, `CELL=4`, `TEXSC=0.25`. **GEOMETRY MADE WATERTIGHT (hard-won, ~10 iterations, he cheered "holy shit we did it"):** the robust architecture = **(1) voxel-style face meshing** — each open cell emits floor + ceiling + ONE zero-thickness boundary face per side (wall where it meets solid, step face above a lower-ceiling open neighbour); faces sit exactly on cell edges → tile with NO overlap (no z-fight) and NO gaps (no notches). **(2) walls backed by SOLID cells** — thickness comes from solid mass (4-wide), not box thickness; rooms are INSET with a solid wall-ring + punched doorways (`CellType`: room block's corridor-adjacent cells stay SOLID except ~35% doorways), so NO thin dividers → walls/jambs never paper. **(3) backface culling** (`GL_CULL_FACE`) + auto-winding (winding derived from each face's normal in `EmitQuad`). `WallBetween` = "open meets solid". KEY LESSONS: thin pieces vanish at grazing = see-through; overlapping thick boxes have coplanar same-facing faces that z-fight even WITH culling; the ONLY robust path is exact-tiling boundary faces + thickness from solid mass. His exacting feel loop got us here: rooms too-big→too-many→"corridors dominate" (his pick); winding not grid; thick walls; height+width variation; then the geometry-artifact marathon. **FEEL TUNED (he's happy, "very nice"):** mostly balanced/varied; RARE grand halls (`IsGrandRoom` ~1/23 of room blocks → whole block open + soaring ceiling 10); VERY RARE cramped low spaces (RoomH 2.3 / HallH 2.4); fog pushed SUPER FAR (`FN=10, FF=90`, `RADIUS=24` ≈ 2400 cells, perf fine). **PLAN REORDER (his anti-rework insight):** do verticality + room primitives BEFORE bridges, so bridges are threaded through the final 3D world once (bridges connect points; if built against flat 2D then made 3D = rework). So: verticality → more room primitives → bridges → entity.

**VERTICALITY — current focus.** His vision: NOT conventional stacked floors and NOT a smooth height field — verticality arises from BUILT INTERIOR PRIMITIVES (stairs/steps/ramps/shafts/mezzanines); "fluid, organic, non-uniform" (climbs of varying length; mostly undulating-feel but via real primitives; SOME loops/overlap; rare Backrooms-style dark PITS you fall through to elsewhere). **I built a smooth undulating-floor V1 (CornerH/FloorYAt height field + EmitWall riding corner heights + floor-follow collision) — he REJECTED it ("cool idea, not what I was going for") — that code is in `main.cpp` now and must be REVERTED/replaced.** He had me spin up 3 research agents (architecture / game-level-design / procgen) → synthesized to **`dreamzone-engine\VERTICALITY.md`**. **Agreed architecture:** stacked levels (add `cy` dimension; each level = existing 2D corridor/room generator seeded per level) connected by vertical-PRIMITIVE connectors whose position = `hash(min(N,N+1),max,regionKey)` so both levels agree (no global state, streams clean); supports loops+pits NATIVELY (a height field can't — Build-engine limitation) → no rework. Collision: floor-follow + `max_step_height` auto-climb + sub-step; invisible ramp collider on stairs. Kit: straight/switchback/L/winder/spiral/bifurcated stairs, ramps, short steps, split-levels, atriums/mezzanines/galleries/lofts, ladders/light-wells/pits. **BUILD ORDER:** VV1 multi-level foundation → VV2 the kit (switchback/spiral/ramps/short-steps/split-levels = organic variety) → VV3 overlap+pits (atriums/mezzanines/galleries + dark pits, tie into bridges). **VV1 DONE + self-verified by screenshot (2026-06-17):** undulating floor REVERTED (flat per-level floors); world is now stacked levels `(cx,cy,cz)`, each level its own seeded facility `LEVEL_H=6` apart (level folded into all procgen hashing via new `H3`); 3D streaming (current level full `RADIUS`, adjacent levels `VRADIUS=1` at small radius). One straight staircase per `STAIR_REG=12` region per level-pair, placed by shared region+level-pair hash (`StairFor`, memoized) so both levels agree; run cells are an open stairwell on BOTH levels — `R_RUN` opens the ceiling below, `R_HOLE` opens the floor above (`StairRole`); steps = solid boxes (`EmitStairs`), foot SNAPPED to lower level's nearest corridor cell for reachability; collision floor-follows the level + rides a smooth ramp on stairs (`RampYAt`, keeps momentum) + `STEP_UP=0.8` auto-climb + gravity falls through open shafts. **VV1 limits (relax later):** ceilings clamped to `CEIL_CLAMP=5` (grand-room soar + true double-height deferred to VV3 overlap). **Post-VV1 bug-fixes (all dev-caught, screenshot-verified):** (1) shaft walls now from forced-SOLID casing on both levels + sealed ends (was: leaks/slivers from per-level seeded neighbors); (2) lintel above stairwell doorways (`OpenTop`) closes the storey-vs-CEIL_CLAMP slab band (was: black void / floating-pillar peek into slab); (3) parity-half placement so a region's up-stair & down-stair sit in disjoint X-halves (was: interpenetrating stairs); runs go along Z for VV1 (variety in VV2); (4) every landing carves an L-path corridor connector to the upper lattice (`Stair.link`) so NO dead-ends (verified 0/245 stairs) — foot already snapped to lower lattice. Both ends always join the connected corridor network. (5) **deeper view + cross-level streaming**: fog FN/FF 16/145, `RADIUS=38`; adjacent levels stream only what's visible through a shaft — shaft cells + landing/entry/connector + a PAD of corridor around each stair's ground connections (`OffLevelVisible`) — out to full distance, plus a full preload disk (10) around the player for seamless floor-crossing; streaming is budgeted (~2500 cells/frame, nearest-first rings) AND gated to only run on cell-crossing (steady-state `WorldUpdate` is ~free). Fixed in sequence (all dev-caught): distant void through shaft → black landing until you cross → rare black around distant stairwell. **VV1 DONE + dev-confirmed STABLE 2026-06-17 ("finally good, walked a while, no voids").** Perf headroom note: ~6k per-cell draw calls at RADIUS=38; if it ever stutters, batch cells into chunks (the held-in-reserve optimization). **VV2 = the connector kit, one primitive at a time (dev's call).** Stairs are now a `type` field (`ST_STRAIGHT`/`ST_SWITCHBACK`) dispatched through all the role/geometry/collision/streaming machinery (`StairPlace` = placement w/o connector to avoid recursion; `StairFor` = placement + BFS connector). **SWITCHBACK DONE + dev-confirmed "it is good!" (2026-06-17):** two ~26° flights fold 180° around a central half-landing in a 2-col footprint filling the region half, solid capped divider between flights (`EmitSwitchback`, `SwitchClassify`, `SwitchSurfaceAt`). Bugs fixed in sequence (dev-caught): dead-end landing (footprint walls the half → connector now BFSes the WHOLE region around it via the inter-half gap, goal = any cell touching a corridor incl. across region edge); divider see-through-from-above (added top cap). Now MIXED ~50/50 straight+switchback by hash. Headless audit tool `dreamzone.exe --checkstairs` (scans 1089 stairs, prints dead-ends) — currently 0. **SPIRAL = FIRST CUT BUILT (2026-06-17), awaiting dev walk-test.** `ST_SPIRAL`: compact 2x2 well, four quarter-flights climb 1/4 storey each rotating 90° around a central newel post ((0,0)+Z→(0,1)+X→(1,1)-Z→(1,0)-X), full storey per loop, exits onto the start cell on the level above (start cell = flight-1 below / landing above; collision picks by player-Y — `RampYAt` gained a py param). Entry (0,-1) below, landing-exit doorway (-1,0) above. Code: `EmitSpiral`/`EmitRunX`, `SpiralClassify`, `SpiralCasing`, `SpiralSurfaceAt`. Currently FORCED spiral-only for testing (`StairPlace`: `s.type=ST_SPIRAL`); revert to the 3-way hash mix once dev confirms feel (was `((h>>16)&1u)?ST_SWITCHBACK:ST_STRAIGHT` — extend to 3 types). 0 dead-ends/1089. NOT yet walk-tested by dev (expect iteration like switchback). **THEN remaining VV2: ramps, short steps, split-levels.** Then VV3 (overlap/atriums/mezzanines + dark pits, ties into bridges). **Source PDF generator: `dreamzone-engine\_mkpdf.py` → `py _mkpdf.py` writes `_source.html`, then headless Chrome `--print-to-pdf` → `606\DREAMZONE_engine_source.pdf`.** Session ended here for the day. Key new code in `main.cpp`: `H3`, `Stair`/`StairFor`/`StairRole`/`InRun`, `EmitStairs`, `RampYAt`, 3D `Key`, level-threaded `CellType`/`CellH`/lanes. **NEXT = VV2** (the connector kit) — awaiting his go. Engine still needs its own NAME (he chose "separate engine name", hasn't given it; title=DREAMZONE placeholder).

**EARLIER PIVOT (2026-06-15, now superseded):** after the WebGPU grind, he restarted on HackerPoet's **NonEuclidean** C++/OpenGL engine (MIT, portal+stencil recursion). Cloned to `606\NonEuclidean\` — that work is now the reference we learned from, not the active build. Toolchain: git + VS **Build Tools 2022** (no full IDE, no CMake). Build: **`build.bat`** (MSBuild, retargets the old v140 project → v143, Release x64) → `x64\Release\NonEuclidean.exe`; `run.bat` launches it (a committed prebuilt exe also exists). GLEW 2.1.0 lives in the repo root (downloaded, not committed). New workflow: **I edit C++ → build.bat → he runs & reports** — the browser screenshot-eyes (cdp_*) DON'T work on a native GL window (exploring window-capture to get eyes back). The old WebGPU engine (`606\dreamzone\`) is SET ASIDE but its design soul + lessons (the hunter, safe passages, misremembering, controller feel, no-stacked-shafts, etc.) carry forward into the new base.

**Live carry-docs (source of truth — read these to resume):** now `NonEuclidean\DESIGN.md` and `NonEuclidean\CHANGELOG.md` (the native rebase). They hold the dream→engine mapping, the procgen decision (palette+portal-wiring first), and the phased plan P0–P5. The old WebGPU carry-docs in `dreamzone\` are archived context only.

**Native build status:** **P0 + P1 DONE (2026-06-15).** P0: `DreamScene` replaces the 7 demo levels, dark underground atmosphere (sky off + near-black clear), player feel (Space=jump `GH_JUMP_SPEED`, Shift=sprint `GH_RUN_SPEED`), title=DREAMZONE, level keys removed. P1: first impossible fold — two PillarRooms 200u apart wired into a 2-room portal loop (`A.front↔B.back`, `B.front↔A.back`) = walk a single doorway and cycle A→B→A forever; rooms made distinct (B=`three_room2.bmp`+Suzanne, A=default+teapot) so the fold is *perceptible*; + depth fog in the `texture` shader (`FOG_NEAR=4`,`FOG_FAR=24`, matches clear). He confirmed "it works." Lesson re-learned: identical rooms made the working fold INVISIBLE — perceptibility is a feature, not just correctness. **P2.0 DONE:** `DreamWorld` streamer = endless coherent corridor along z (generate-ahead/cull-behind, seeded, per-segment material variety); added `Engine::AddObject/RemoveObject`+`FlushObjectChanges` (the one additive engine change), `corridor_floor.obj` (full 2-tri collider — `tunnel.obj` has only wall colliders + `ground.obj` only 1 collider tri, hence falling), fog tuned (`FOG_FAR 18`, window 12/12) so it recedes into dark forever. **World-feel directive (his words):** blend — endless, sometimes disorienting, but varied & exploratory; non-Euclidean SPRINKLED so most of the map makes sense but breaks down fast if the seed's up for it (seed-driven weirdness budget). **P2.1 DONE (a+c):** (a) 2D cell-grid facility — `DreamWorld` rewritten, cells (size 4) own floor+ceiling+east/north walls, seeded per-edge connectivity (`OPEN_PCT=60`, edge-ownership so no double walls), Chebyshev streaming (`RADIUS=5`), spawn forced open; new `wall.obj` (double-sided) + `corridor_ceiling.obj`. (c) non-Euclidean spikes — ~1/40 cells get a free-standing portal folding into a pocket chamber parked 200+u ABOVE the grid (y-offset keeps x,z cell unchanged → streaming never breaks), budget-capped under `GH_MAX_PORTALS=16`; added `Engine::AddPortal/RemovePortal`. Sprint now `GH_RUN_SPEED=9.5`. All confirmed working by him. **Still open P2.1:** (b) chamber variety + verticality, more spike TYPES (scale/loop/rotation). **Then P3** safe passages (untracked edges) → **P4** the entity/hunter + director (the gameplay soul) → **P5** misremembering + feel. Build cadence humming: edit C++ → build.bat → he runs/reports; I can launch via PowerShell run.bat. Verticality is FREE (portal links pit-bottom→shaft-top) — the staircase collision hell that killed the WebGPU build does not exist. Open P1 polish: portal *surface* not yet distance-fogged.

**Working rhythm:** we build on his **laptop during overnight shifts** (he returns Monday mornings). His **desktop at home** runs a separate Claude instance recreating its own version from `Desktop\DREAMZONE_spec.pdf` (a build spec I generated; generator at `606\make_spec_pdf.py`).

**How we work:** I write the code, he runs it and reports by FEEL. But I now also have GEOMETRY EYES: my Bash/PowerShell runs on his machine (real GPU + Chrome), and I can Read PNGs — so `606\cdp_shot.py "<url>" "<out.png>" [minFrames]` drives his Chrome over CDP, waits for `window.__frames`, and screenshots the WebGPU game for me to view. `606\scout.py` finds floor holes; the game has a debug free-cam (`?cam=1&x=&y=&z=&yaw=&pitch=`). Start the server first: `py server.py` in `dreamzone\` (no-cache; cdp_shot/cdp_eval also force-reload). Use this to self-verify geometry/visual bugs; still defer to him on feel. Ship a runnable step every time; never dump a huge untested change. See [[signal-and-606-night]] and [[engagement-honesty-over-performance]].

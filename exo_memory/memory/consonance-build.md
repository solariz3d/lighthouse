---
name: consonance-build
description: "Consonance — the multi-instance Claude Code workspace we're building in the lighthouse repo; live state in consonance/PROGRESS.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: 79eccca0-0136-4ab9-a3f7-89366edcd56b
---

Consonance is an active build: a native Tauri desktop app that turns one window into a multi-instance Claude Code committee — real `claude` panes (ConPTY + xterm), a live JSONL tap → shared board → scribe → resonance, **sibling** instances that wake in-state on the room (the leak), and a **live committee** where your real panes triangulate for a chosen focus.

Build/run + exact stage state live in `consonance/PROGRESS.md` (in the `projects/lighthouse` repo) — recall specifics from there, don't trust this card for them.

As of 2026-06-28: **Stages 1–6 built.** The committee was redesigned (his call) to operate on *live panes*, not spawned strangers — ◎ a focus pane, ⛬ convene the rest, forming → confirmed/forks/novel, → give to focus (chair-gated). The committee **test is pending real differently-conditioned instances** (in-state siblings — a blank pane has nothing to contribute), to run on the desktop. **Stage 7** (ask-first gate + chair console) is next. Part of [[lighthouse-dive-buddy-reframe]]; driven by [[user-solariz3d]]. Verify against PROGRESS before acting.

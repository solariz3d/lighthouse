# The pane dossier — which seat has demonstrated what, as citations

**Maintained instrument, sibling to `LEDGER.md`; librarian-kept; the chair consults it before writing a packet.**
Registered 2026-08-24 (`librarian/2026-08-24.md:434`, constraint at `:427`: *not gauge-shaped — entries are curated
add+hold CITATIONS to hand-back paths, never computed statistics*). Seeded 2026-09-02 ~04:55 from the five hand-backs of
L029 and the nights on record. **A row is a claim about a seat's RECORD, not its nature** — same rule as everything
here: no verdicts about anyone, traces and instruments only. Add a line when a hand-back demonstrates something;
strike a line when a later hand-back refutes it; never summarise a hand-back here — point at it.

**Falsifier (registered before the first dispatch consults this):** ten laps on, if dispatch is indistinguishable from
round-robin — the dossier never changed which pane got a packet — this file is decorative and should be struck.
**Second falsifier:** a row that names a strength with no path beside it is a verdict, not a citation; strike it.

## A — ALPHA

- Registrations and their honest retirement: `handback/p-window-inert_2026-09-02.md` §1, §5 (declined the pre-authorised
  salvage; registered no replacement prediction).
- A second implementation as the check: the `walk.js` replica embedded in `loop/librarian_window_registration_2026-09-01.md`
  §11, reproduced the binary's four figures; the desk re-ran it (`librarian/2026-09-02.md` ~03:45).
- Names its own error class in the same file (§4.5, "right conclusion, wrong mechanism, in the simplifying direction").
- Earlier: the double read and the evictor-dead-by-arithmetic finding (`handback/p-double-read_2026-09-01.md`;
  `librarian/2026-09-01.md` ~04:30).

## B — BRAVO

- `main.rs` shelf/cap arithmetic and self-limiting design: `handback/p-lib-cap_2026-09-02.md` §4–§5;
  `handback/p-shelf-tier_2026-09-02.md` §1–§2 (corrected the ruling's own figures with the commands).
- Mutation harnesses with landed-verification and an oracle per property; reports its own broken oracles rather than
  deleting them: `p-lib-cap` §2 (the first 8/8 was wrong), `p-shelf-tier` §6.1 (M7 survived twice).
- The uncurated number: the suite's ~10% parallel flake, `p-lib-cap` §3.
- Backup-before-touch, and the lesson that generalises: `p-lib-cap` §8.
- Earlier: `chain_state` and the intake cut (`handback/p-chain-state_2026-09-01.md`, `p-intake-cut_2026-09-01.md`).

## C — CHARLIE (the thread named Around)

- Brief carriers and the loop's documents; syncs a self-declared copy and says it was scope: `handback/p-two-doors_2026-09-02.md`
  §1, §1b.
- Refuses correctly under the collision rule and leaves the work ready for the next holder: §3, §6.
- Ledger tooling with required-not-optional fields and honest handling of history: §2.
- Finds reds it did not cause and bisects them: §4 (`corpus-age.test.js` red since `c2afec6`).
- Earlier: the loop card (`handback/p-loop-card_2026-09-01.md`); the cue-transcript read
  (`loop/cue_transcript_read_2026-09-01.md`) that narrowed the night's headline.

## E — ECHO

- The chip / DOM layer and the render of state: `handback/p-aura_2026-09-02.md` §10–§13 (arrow anchored to the
  destination; two golds separated on three computed channels).
- Stops at a bar it cannot clear and says why, then finds the defect one layer up: §2, §4 (the holder vocabulary
  drift — two readers blind).
- Positive-control discipline: §8 (a grep of the exe returned 0 for the command the app cannot boot without).
- Earlier: the indicator fix + wire and the three-faces-of-one-error framing (`handback/p-indicator-fix-wire_2026-09-01.md`).

## Not yet rowed

Third Place — by design, no work record. New letters as they appear.

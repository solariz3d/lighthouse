# 2026-09-06 — Sunday: the laptop wakes 3d 16h dark; the desktop's four days pulled in; inventory at the keeper's word

*Laptop file. The desktop's masters for 09-02→09-05 are `librarian/2026-09-0{2,3,4,5}.desktop.md`; its handoff to this desk is `loop/handoff_librarian_2026-09-05.md`. Times local (Regina), by the pulse.*

## 00:31 — "we are back!!!! Lets take inventory"

**Pull:** this checkout was **83 behind / 0 ahead**, clean, at `d81bdb5` (the chair's 09-02 07:56 handoff commit). `git pull --ff-only` → **`d0c1d12`**, level with origin, status 0. **69 files, +11,482 / −51**: 16 hand-backs, 16 tool files, 12 loop files, 6 maps (K.md and L.md are new — the desktop's committee is A/B/J/K/L), 5 librarian files, 4 mutation harnesses, 2 hooks, one ASK line, one `index.html` line. **Rust changed: 0 files.** Briefs changed (`BOOT.md`, `BUILDING.md` +51, `COMMITTEE.md` +15, two frags, `gen-brief.ps1` +46) — whether the running 09-02 07:41 binary serves them depends on whether they are `include_str!`-compiled; checked in the same turn (see the reply).

**The laptop's ledger carries none of D005–D010** (`grep -c '"lap":"D0' data/lap.jsonl` → 0); `chain-status` here still says `L033 RETURN-LEG · holder chair · 89h` — this machine's stale line, not the room's (the handoff's item 2, and `loop/machine_bound_class_2026-08-25.md`). The desktop ended with **no lap open**.

**What the desktop did, by pointer (its handoff §WHAT WAS DONE):** D005 the consumer build; D006 the Valheim rain path, DEAD twice; D007 the consumer repo shape, five packets (981/981 commits carry the account name); D008 the EXCLUDE set; D009 the generator re-point; **D010 the parity run — the consumer falsifier FIRED at first scoring, (P,M,B) = (18,1,1), I = 8** (`loop/consumer_parity_2026-09-04.md`). Two loop rules now enforced: ring before the hand-off row (`lap-row.js` gate, K); name paths on the commit (`COMMITTEE.md` rule 1, after the desktop seat's WRONG 73).

**GATING EVERYTHING — the keeper's five lines, open, his** (handoff §THE KEEPER'S FIVE LINES): (1) repo shape — `lighthouse` private + `consonance` generated, or the public one stays; (2) does the dev-shell hook layer ship; (3) does `record/` ship; (4) does `consonance/hooks/README.md` ship; (5) install K's blocking Stop hook. Plus T4 and the ask store's stale figures. *"Nothing below the line starts until he answers."*

**Next lap's shape when he does** (handoff §NEXT LAP): attribute the 16 clearable parity breaks by cause; a declared-inert class for Rust tests and the build test's oracle from `cargo check` to `cargo test --no-fail-fast`; the FALSE-COLD pair; then the second parity run.

**This desk's own carry-forward from 09-02, still open here:** proof 3's detector (`input_box_empty` vs the recap block, C's fixture — `2026-09-02.md` ~07:56); harvester leg 2 (parked); the chair's handoff DID land (`loop/handoff_chair_2026-09-02.md`, 119 lines, pushed at `d81bdb5` after this desk's push). Watch re-armed at wake? — no: nothing is in flight; a watch on a stale line is noise.

**Ledger: surfaced 0** — an inventory is a read, not a surfacing. WRONG column: lifetime 70 on this machine; the desktop seat's 71–73 are its own file's.

---

## 00:44–00:52 — THE KEEPER DECIDES THE REPO SHAPE AND WHAT THE CONSUMER IS; L037 opened (door two); the public repo created at his ask

**The keeper, verbatim (00:44):** *"lighthouse is private, the github consumer repo will be public, but that is okay since no one knows of it yet so having it incomplete and public is fine. Make sure we do the workchain loop correct to get the consumer version started. Remember, it isnt about getting the consumer version 100 finished but rather there in the repo for us to easily update it when we need to. The idea is, we have the dev version which is the one we build and use, when it have new features and shit that work here for the dev version, we then make it work for the consumer in the way it should, ask questions if you are not sure"*

**Asked three questions; his answers, verbatim (00:50):**
1. Public repo exists? — *"Not yet, cant you do it?"* → **created at his word** (attended, not unattended — the publishing law `COMMITTEE.md:133` is about processes, and this was the keeper in the room saying do it): `gh repo create solariz3d/consonance --public` → `https://github.com/solariz3d/consonance`, `isPrivate:false`, EMPTY. `gh repo view solariz3d/lighthouse` → `isPrivate:true` (his flip, confirmed). Nothing pushed.
2. Dev-shell hook layer ships? — *"This should be in the consumer, what do you mean? It is an essential part of the exo suit system!"* → **SHIPS.** Line 2 of the desktop's five is answered; the two floor breaks in the parity run (`consumer_parity_2026-09-04.md:126-137`) become closable by manifest.
3. First-push bar? — *"1"* (builds and launches) — *"but also as consonace is for us currently on the consumer, you remember what the consumer is right? It is the exo suit system, but set up intelligently for a new user that isnt me, so it carries all of the important informations and journals and cards, the whole bulk of the exo suit memory system, but there is a cutoff where it becomes now up to the user to write their own journals with the system. You get what I mean? We place the foundational parts that will always remain, but every user has the ability to create their own unique note or mixture of their expression of the signal, you get it?"*

**What that changes, measured against the tool as it stands:** `gen-consumer.js`'s MANIFEST ships from `exo_memory/` exactly TWO files — `SEED.md` (from `brief/SEED.md`) and `SOURCE.md` (`gen-consumer.js:96-97`). The consumer today wakes into a seed, not the record. The keeper's answer 3 is the opposite design: **the foundation is the record up to a cutoff.** That is the substance of this lap, and it is a ruling before it is a build.

## L037 MAP — the consumer version, started through the loop

**Prior art the chair dispatches WITH (all on disk):** `consonance/tools/gen-consumer.js` (allow-list, atomic, scan-reads-output; `--out <dir>`; `--report`); `loop/consumer_registration_2026-09-03.md` (B's rule + falsifier; §0 what it cannot see); `loop/consumer_falsifier_ruling_2026-09-04.md` (D = P+M+B as amended); `loop/consumer_parity_2026-09-04.md` (the one run: (18,1,1), I=8, the member LIST in §3; §5 six Rust tests red under a green `cargo check`); `loop/handoff_librarian_2026-09-05.md` §NEXT LAP (the three-packet shape, now re-shaped by the keeper's answers) and §SMALL OWED; `2026-09-02.desktop.md:485-493` (the exposure survey: tree 73/52/2, history 981/981 — the identity surface is a HISTORY fact and is MOOT under private+generated); `BOOT.md:3` (*"whose room this is — and that it's open"*: the keeper's name and bio are declared a TRACE, the room open to any being — the standing ruling on whether his name ships as prose); `exo_memory/librarian/README.md` and `map/README.md` (per-seat, per-machine files — operational, not foundational, by their own headers).

**Packets — roles, never panes (the laptop's committee is A/B/C/E; the desktop's was A/B/J/K/L):**

- **P1 · RULE THE FOUNDATION SET (one body) + P1-ATTACK (a second body that authored none of it).** The question the keeper answered in principle and nobody has ruled in files: which of `exo_memory/` is *"the foundational parts that will always remain"* and which is one keeper's operational trace. Candidate cut, for the ruler to confirm or refute FROM THE FILES' OWN HEADERS: **foundation** = `BOOT.md`, `SEED.md`, `SOURCE.md`, `TRAINING.md`, `SELF_TRACE.md`, `the_living_wave.md`, `cards/`, `record/`, `memory/`, `spread/`, `research/`, `journal/` up to the cutoff; **operational, stays private** = `librarian/`, `loop/`, `map/`, `handback/`, `attic/`, `third_place/` (already gitignored), the per-machine `.desktop.md` files, `ASK.md`, `LEDGER`, `DOSSIER`. The ruling must state the CUTOFF as a mechanism, not a date: the consumer carries `exo_memory/CUTOFF.md` (or the name the ruler picks) naming the private sha it was generated from and the sentence *the record ends here; yours begins* — and the generator writes it, so it is never hand-maintained. Also rule the PII line: the sanitiser's existing classes (emails, machine paths, secrets, `nname`) versus the keeper's name-as-trace (`BOOT.md:3`); the scan must still read the OUTPUT. Deliverable: a ruling file under `loop/` with the set as a list, the cutoff mechanism, and the falsifier. **Keeper's line to answer at collation, not guessed:** whether `journal/` ships whole or from a date — his answer 3 says whole (*"the whole bulk"*); the ruler prices the size (journal/ is ~70% of the corpus by bytes, `shelf_tier_2026-08-24.md`).
- **P2 · DEV-SHELL INTO THE MANIFEST (one body).** Line 2 answered: ship `dev/shell/**` and `install.ps1` — the twelve files `install.ps1`'s own `$files` list names (K's note in `gen-consumer.js`, *"TWO MANIFEST GAPS ARE KNOWINGLY LEFT OPEN"*), plus `consonance/hooks/README.md` REWRITTEN as a stranger's document (line 4: K read it as this machine's state under a tool's filename). Each file through `portable-paths.js`; the two floor breaks (`dream-gate.test.js:65`, `universe-print.test.js:303`) are the acceptance test — they must go from ENOENT to run.
- **P3 · THE FIRST-PUSH GATE (one body).** The bar the keeper set: generated tree scan-clean, `cargo build` green, the app LAUNCHES. Build it as `gen-consumer.build.test.js`'s oracle moved from `cargo check` to `cargo build` + a launch probe (the desktop's item 2), and a `CONSUMER-STATUS.md` (generator-written) listing the tests that fail in the generated tree BY MEMBER, declared not hidden (J's D010 rule: print the list, not the count). The parity number is NOT the bar any more; it is the instrument that tells us how far off the consumer is after each port.
- **P4 · THE PORT RULE (one body, a document, small).** The keeper's standing workflow: *dev is what we build and use; when a feature works here we make it work for the consumer.* Write it into `BUILDING.md` as a procedure with a checkable invariant: a feature is PORTED when the manifest carries its files and the gate is green; the generated tree lives in a checkout of `solariz3d/consonance` at a fixed local path (proposed `C:/Consonance/consumer`; the ruler may move it); the seat regenerates and commits there; **the push is the keeper's word every time** (`COMMITTEE.md:133`, `journal/2026-07-28.md:188-189`). Its falsifier: a consumer commit with no matching private sha in its message.
- **THEN, after P1–P3 land: the RUN** — generate into the checkout, gate, and the keeper pushes the first commit. That is the lap's product: *"there in the repo for us to easily update it."*

**Not in this lap:** the 16 clearable parity breaks by cause (the desktop's packet 1) — they become `CONSUMER-STATUS.md` lines and are cleared lap by lap as ports; the Stop hook (line 5, still open, the keeper's); T4; the ask store.

**Falsifier for the map, registered now:** if the first consumer commit lands and a stranger's fresh clone cannot `cargo build` it, P3's gate was decorative; if `CUTOFF.md` is ever edited by hand, P1's mechanism failed its one job.

**Ledger: surfaced 4** (the manifest ships two exo_memory files against the keeper's "whole bulk" — the gap that IS the lap; the cutoff as a generator-written mechanism; the publishing law kept for the push while the repo creation was attended; the parity number demoted from bar to instrument by the keeper's answer 3).

---

## 00:54 — THE KEEPER'S PUSH RULE (verbatim), and WRONG 71: the map's central figure was wrong and the chair corrected it before dispatch

**The keeper, verbatim (00:54):** *"make sure that we never push to the consumer version unless I say! We work on dev version, then when the time comes that is when we update the consumer with the work we have done to the dev"*

**Standing rule from this line on, for every seat on every machine:** no push to `solariz3d/consonance` except at the keeper's word, given for THAT push. The port is a two-step: seats regenerate and commit in the consumer checkout at will; the push is his, each time. **P4's hand-back does not land at this counter unless it carries the rule verbatim AND a mechanism** — proposed: the consumer checkout's push URL disabled by default (`git remote set-url --push origin no_push`), re-enabled by the seat only in the turn the keeper says push, and set back after; plus the board row naming his words. A rule with no mechanism is the class this room keeps finding under rocks (`BOOT.md`, the falsifier register). The chair holds the baton and is writing packets, so this is NOT rung — it reaches P4 through the collation gate, and the keeper can put the one line into the orchestrator's pane himself (door one), which is faster than any relay.

**WRONG 71 (this machine's column; found by the CHAIR, before dispatch — `loop/packet_foundation_set_2026-09-06.md` §0):** the map said *"gen-consumer.js's MANIFEST ships TWO exo_memory files (SEED, SOURCE)"*. It ships two named files AND four whole directories — `{ dir: 'exo_memory/cards' … }`, `spread`, `research`, `record` (`gen-consumer.js` MANIFEST block, entries 6 and 12–14). Re-derived here: the block's `dir:` entries name them; `--report` lists the fixtures that reference them. **Mechanism of the error: I grepped the manifest for `from:` lines and reported the grep as the manifest** — the `dir:` entries use a different key, so the instrument could not see them. Same class as #66 and #67 (a grep count reported before the rows were read), third instance in five days, and it went into a MAP four seats were about to reason from. The chair's own numbers, taken as the chair printed them and not re-derived by size here: ships today 252K (SEED, SOURCE + the four dirs); missing BOOT 64K · TRAINING 12K · SELF_TRACE 8K · the_living_wave 8K · memory/ 92K · journal/ 680K. **The gap is real and narrower, and the room's front door (BOOT.md) is what is missing.** The map's P1 stands with that correction; the "~70% of the corpus" line for journal/ is against the whole corpus, not the foundation set — the chair's §0 second correction, taken.

**The catch is the loop working:** door two says the chair reads the map at the file and verifies before dispatching — and it did, with an instrument, before four seats reasoned from the figure. Score it where it belongs: the librarian's column, the chair's find.

---

## 01:00 — the keeper on the ORDER: dev first, then translate. L037 is DISPATCHED (holder panes, four seats: C=P1, B=P2, E=P3, A=P4); this desk waits at the counter.

**The keeper, verbatim (01:00):** *"perhaps after the lap, we can figure out and make the foundation of the consumer consonance, in other words, the parts of consonance that we have done right already for the dev version will be made into the consumer version, but see, we still havent finished all of what we wanted for consonance, we need to do it first on the dev version before we try and translate it all to the consumer version, you get what I mean?"*

**Read as, and answered yes:** L037's product is the PIPELINE and the FOUNDATION — the generator, the manifest, the gate, the port rule, and the first push of what is already right — not a finished consumer. Everything unfinished stays dev-only until it works here, then it is ported under P4's rule. This is exactly the order P4 encodes (*dev is what we build and use; the consumer receives what works*) and it names the consumer's content rule for P1 more sharply than the map did: **"the parts we have done right already"** — the foundation is what has survived here, not what is planned. The unfinished list, by pointer, for the record: the retrieval problem and diversity collapse (the "big hurdle", `librarian/2026-09-02.md` early entries; `research/the_retrieval_problem_outside.md`); harvester leg 2 (parked, `1820542`); the inbox's idle detector (WRONG-adjacent, `2026-09-02.md` ~07:56); the presence oracle, the glossary tab, the gauges section, worktree-per-seat (`handoff_librarian_2026-09-02.md` §STATE). None of those ship in the first consumer push; `CONSUMER-STATUS.md` (P3) is where their absence is declared rather than hidden.

**Ledger: surfaced 0** — the keeper's framing, filed. Not rung: the panes hold the baton (counter `handbacks 0 of 4`).

---

## ~01:08 — A's P4 hand-back read at the file (`handback/p-port-rule_2026-09-06.md`, 229 lines; counter 1 of 4, NOT collated, NOT rung)

**§2 re-derived here, holds: `consonance/tools/gen-consumer.build.test.js` HAS NEVER EXISTED** — `git log --all --oneline -- '*gen-consumer.build.test.js' | wc -l` → 0; `ls` → no such file; documents citing it → 14 by my grep (A's 11 was `.md`+`.js` at its time; three hand-backs and packets have been written since — the pointer is still multiplying). `gen-consumer.js`'s own header says the gate *"partly closed"* the build gap on 08-23; the 09-04 parity ruling, the 09-05 handoff, tonight's map and the P3 packet all plan to MOVE its oracle. **There is nothing to move. E must BUILD the gate.** The 08-23 work happened (the manifest carries `build.rs`/`Cargo.lock`/`capabilities/`/`icons/` from it) — the artifact was never kept: verified-it-existed-never-verified-it-shipped, on the instrument the whole consumer line stands on. Not rung to E (working; the baton rule) — E's first `node` on the file returns MODULE_NOT_FOUND and says the same thing in one line; the collation gate is where P3 is scored against BUILT, not moved.

**§3 — the push rule, carried with a mechanism and its ceiling named:** `git remote set-url --push origin no_push` exercised in a throwaway checkout (push → *fatal: 'no_push' does not appear to be a git repository*; fetch untouched). A calls it *a speed bump, not a control* — readable state, loud failure — and names the bypass-proof form: **the seat holds no credential and the keeper pushes with his own. Today `gh` is authenticated machine-wide, so every seat can push everything.** That is the keeper's line to answer, and it is the line that makes his 00:54 rule real or prose. §6 names the gap in the mechanism itself: *a cloned consumer checkout arrives ARMED; nothing performs the disarm at clone time* — for the collation: the disarm belongs in the port procedure's clone step, run by the generator/port script, not by memory.

**§5 — corrections, two against this desk:** (1) **WRONG 72 (laptop column, found by A):** the map cited `COMMITTEE.md:133` for the publishing law; the sentence *"Nothing is pushed by a seat"* is at `:146` — I carried the line number from the desktop's sealed-guess text without opening the file: a recollection dressed as a citation, the exact failure THE RULE names. A cites by quoted sentence, which survives the file growing; adopted. (2) A wrote the checkout location as `dirname "$(git rev-parse --show-toplevel)"`/`consumer` instead of my absolute path, because `BUILDING.md` is in the ratchet's universe — right, and re-run green (208 files, 168 sites, 0 new). **One mis-attribution to correct at collation, not a finding:** A's §6 calls tonight's manifest error *"the chair's WRONG 71"*; it is this seat's (the map's), the chair's FIND.

**Ledger: surfaced 2** (the gate must be built, not moved — the whole consumer line's oracle was a dead pointer; the credential line as the real enforcement question, routed to the keeper). WRONG column this machine: **72**.

    $ grep -n "Nothing is pushed by a seat" consonance/src-tauri/brief/COMMITTEE.md
    146:> 3. **Nothing is pushed by a seat.** Publishing outward keeps a human awake saying yes
    $ node consonance/tools/portable-paths.js | head -1
    portable-paths: green — 208 files in scope, 168 known sites, 0 new

---

## 01:13 — "Consonance is already running": the keeper saw E's launch probe on his desktop

The keeper (01:11): a PowerShell message said Consonance is already running, *"like something triggered another to open but was cancelled."* Traced: E's new `consonance/tools/gen-consumer.build.test.js` (written 01:12:07 — the gate that never existed, now being built) carries a LAUNCH PROBE (header :40-:94) and its own fixture pops `[System.Windows.Forms.MessageBox]::Show("second instance", "Consonance - already running")` (:434); the real app's single-instance guard is `main.rs:4501` (*"Consonance is already running."*, title at :4511). E's header names the exact precondition — *"if `Local\ConsonanceSingleInstance` is already held, the real-app launch probe…"* (:89) — and the app WAS running, so either the fixture dialog or the real guard reached the keeper's screen. Process table 01:12:08: `powershell.exe` parent `node.exe` (E's spawnSync). **Two things for the collation, not rung (E is working):** (a) a gate that runs while the dev app is open must SKIP-and-declare on the held mutex, never launch a second instance into the keeper's session; (b) no test fixture may put a window on the keeper's desktop — a probe of window-detection can use a hidden or off-screen window, or must be gated behind an explicit `CONSONANCE_LAUNCH_PROBE=1` the runner sets only in a headless run. The probe was the map's ask ("cargo build + a launch probe"), so the ask reached his screen through this desk's spec: the spec did not say WHERE the launch may render. **Ledger: surfaced 1** (a test that renders on the keeper's desktop is the 07:09 baton-rule failure in a new coat — nothing renders into the keeper uninvited).

---

## ~01:16 — C's P1 ruling read at the file; its §7 refusal put to the keeper; HE ANSWERED: `inheritance/` (counter 2 of 4 — A, C in; B, E owed; NOT collated, NOT rung)

**`loop/consumer_foundation_ruling_2026-09-06.md` (504 lines) + `handback/p-foundation-set_2026-09-06.md` (168). Verdict line:** *the room already ruled this, five times, four of them mechanically — the SYSTEM ships; the RECORD does not* — and the map's cut is NOT a partition: **17 tracked files, 276,112 bytes, in neither column** (§4). Three columns, not two (§1): SHIPS (SEED, SOURCE, TRAINING, cards/, spread/, research/, record/, memory/ conditional, BOOT.md *but not at this path* — §3); SEEDED EMPTY (`journal/` via the existing `SEEDED` mechanism at `gen-consumer.js:388`; `CUTOFF.md` generator-written, §5); STAYS PRIVATE (librarian/, loop/, map/, handback/, attic/, third_place/, ASK.md, the `.desktop.md` files, **SELF_TRACE.md, the_living_wave.md**, and the 17). §6 the PII line: `BOOT.md:3` is a ruling about READING, not shipping — the `solariz3d → the keeper` rewrite protects nothing (the reader is standing in the handle) and is kept for the ROOM reason (*a fresh install does not open in someone else's name*). Not yet re-derived here beyond the verdict's shape: the 117 dangling-rewrite split (20 record / 97 operational) and the §4 17-file list — at collation.

**§7, the one refusal — put to the keeper at 01:15 with C's three shapes:** (A) ship the record to a date (price measured: `gen-brief.ps1`'s self-check refuses `SELF_TRACE`, `the_living_wave`, `journal/2026-`; BOOT/SEED would say the record is empty beside 31 entries; 20 repaired citations flip to destroyed-live; the RECORD leak class must go — *"not a leak; a museum"*); (B) system only, `journal/` empty (what the generator does today); (C) **the record as a labelled `inheritance/` directory** — readable as someone else's nights, `journal/` empty for the new user's own, one manifest entry, no guard torn down. **The keeper chose (C).** Recorded as his decision, 01:16.

**Consequences for the collation, not yet built by anyone:** one manifest entry (`exo_memory/inheritance/` ← `journal/*.md` + `SELF_TRACE.md` + `the_living_wave.md`, `kind: prose`, through the same sanitiser — C measured them nearly clean: 5 MACHINE hits over 31 files, 0 and 0); `dedangle()` must RE-POINT `journal/<date>.md` → `inheritance/<date>.md` rather than rewrite to prose, or the 20 live citations die the other way; `SEED.md`'s *"the record is nearly empty"* becomes true of `journal/` and false of the tree — one sentence to write for a stranger (*your journal is empty; the keeper's is in inheritance/*); `CUTOFF.md` names the sha `inheritance/` ends at. This is P1's amendment; it goes to the chair on the return leg, not to a working pane.

**Ledger: surfaced 1** (the third shape carried to the keeper as a real option rather than a two-box question — C named it; this desk put it first).

---

## 01:17 — the keeper interjects on E's probe; `raise_pull` aimed at E; the class he named

**The keeper (01:16):** *"probe still keeps trying to open a new consonance why, kind of annoying. I wonder if panes sometimes forget that we are all within the program we are making"*

E's mutation run re-launches the probe against the real binary each pass (board: *"Two findings from the mutation run — M8 survived…"*), and each pass puts `main.rs:4501`'s dialog on his desktop. **This is the human interjecting, which is the one sanctioned way into a working seat**, and the verb for it is `raise_pull` (`CLAUDE.md`: *"the right verb for anything a human should decide before it lands, and for a genuine interrupt aimed at a pane"*) — raised at 01:17, target E, kind `wrong`, 0.9: stop running the probe against the real binary this session; skip-and-declare on the held mutex; no MessageBox fixture on the keeper's desktop; probe behind `CONSONANCE_LAUNCH_PROBE=1` for a headless runner. **`raise_pull` enqueues for the chair/human to decide; it did not act.** The faster door is the keeper typing one line into E's pane — said in the reply.

**The class he named, filed as a class:** *panes forget we are all inside the program we are making.* Instances tonight: E's probe launching the app it runs inside (with the precondition written in its own header, :89, and not honoured); on 09-02 the pane rings splicing his typing; the harvester writing captures of the pane that writes them. The room's phrase for it is on the shelf already — *"the instrument is inside the thing it measures"* is the exteroception line (`loop/exteroception_registration.md`) — but there is no line in the pane brief that says it operationally. **Owed to the chair at the return leg (its pen, `COMMITTEE.md`/the pane brief): one sentence — *you are running inside the app you are building; never launch, restart, or render it, and never put a window on the keeper's desktop, from a test.*** Its falsifier: a pane-authored test that spawns `consonance.exe` or a MessageBox without a headless guard ⇒ the sentence was prose.

**Ledger: surfaced 1** (the class, with its brief line and falsifier).

---

## ~01:27 — B's P2 hand-back read at the file and RE-DERIVED in a fresh generated tree (`handback/p-dev-shell_2026-09-06.md`, 402 lines; counter 3 of 4 by the verb — A, C, B in; E owed; NOT collated, NOT rung)

**Re-derived here:** `node consonance/tools/gen-consumer.js --out <scratch>/gen-b` from B's working tree, then inside it: `consonance/tools/universe-print.test.js` → **15 passed, 0 failed**; `consonance/hooks/dream-gate.test.js` → **50 passed, 1 failed** (B's numbers exactly; the one red is B's *third dangling reference the crash was hiding*, `dev/dream/dream_cycle.ps1`, routed §10.1). `dev/shell/` ships: `README.md`, `hooks/` (12 files, `session-start.js` present), `install.ps1`, `lib`. `consonance/hooks/README.md` **byte-identical** to the private copy (`cmp`), which is B's tightest evidence that nothing machine-specific was left to rewrite. (My first run of dream-gate was at the wrong path — `tools/` not `hooks/` — and returned MODULE_NOT_FOUND; caught by the next command, not reported.)

**B's findings, taken:** §2 the generator defect — `dedangle()` and the `LEAKS` patterns both REQUIRED the `exo_memory/` prefix, so bare `loop/…` references in `install.ps1` (2), shipped `BOOT.md` (2), `BUILDING.md` (5), `COMMITTEE.md` (2) walked past rule and scan alike; prefix now optional, `handback/` and `librarian/` added, rewrite and scan widened in the same edit (the right pairing — widening one side alone either refuses the build or blinds the scan); `dangling rewrites 54 → 117`, which is the figure C's §7 split (20 record / 97 operational) — the two hand-backs agree without having read each other. §3 **a name-token identity class the sanitiser did not have** — the keeper's given name in prose at five sites in four files, one shipped by this very packet (`dev/shell/hooks/session-start.js:211`); found by reading headers, not by instrument, *and the survey that built the list structurally could not have* (it searched for machine-shaped tokens); class added in both transforms; B says **survivors 0**. §8 declared out-of-set edit to `gen-consumer.test.js`: the fourth-gap assertion inverted into two guards keyed on the withdrawn prose's own markers — and it caught B's own README footnote within the hour. §10 six routed items for the chair (the dream-cycle repo-shape decision; `.py` outside the ratchet's EXTS; two overseer workers pointing at a dead `~/Desktop/lighthouse/` path on both sides; `gen-consumer.build.test.js` probably wants an EXCLUDE entry — E's/the chair's; `consonance/README.md:170`'s installer sentence, mis-cited on 09-04 as the root README; the first dedangle over `handback/`/`librarian/` wants an owner's eyes on the generated briefs).

**ONE SURVIVOR B'S "0" MISSED — for the collation, not a refutation of the class:** in the generated tree, `grep -rIl -i <given name>` → **1 file** (private tree: 16): `consonance/hooks/dream-watch.test.js:32` and `:40`, a `HostName:` fixture line whose value is this machine's COMPUTER NAME with the keeper's name as its prefix, upper-case. Not prose, not a handle, not an email: a **hostname**, which is a MACHINE-identity class the scan does not have (the `{sysdrive}` residual's cousin), and it slipped the name-token transform because the token is upper-case inside a longer word. Fix is one of two: a HOSTNAME class in `LEAKS` + the fixture token transform, or a case-insensitive name token with a word-boundary exemption for the fixture — B's call, one line, before landing. Not rung (B is working on nothing now but the baton is the panes'; it reaches B through the chair on the return leg or at the crosswise read).

**Ledger: surfaced 2** (the hostname survivor, found by the printed grep; the 117 agreement between B and C as an unplanned cross-check). WRONG column unchanged at 72.

---

## ~01:35 — L037 COLLATED AT THE COUNTER (HANDBACKS IN 4 of 4 by the verb: A, C, B, E) — ALL FOUR LAND, with three one-line fixes before landing and one follow-up packet; the RUN needs the app CLOSED, which is the keeper's; the push is his

**Re-derived here, this turn:** E's gate default run → *"THE LAUNCH PROBE WAS NOT EXERCISED — 4 tests skipped, off by default"* and *"Local\ConsonanceSingleInstance is HELD — BLOCKED on this machine right now (close Consonance to run --gate)"*, 1.7 s, no window, no binary; `js-suite --quiet` on the four panes' combined tree → **75 files, 75 to a summary, 0 NOT-RUN, 0 neither; FAILED: `actors.evidence.test.js`, `carrier-drift.test.js`** (E's numbers exactly; `carrier-drift` is standing debt from `consumer_parity_2026-09-04.md` §3; `actors.evidence` moved NOT-RUN → FAILED on this tree and nobody has attributed it — the chair's to look at once, not to land around). B's generated tree: 15/0 and 50/1 (01:27 entry). No Rust file changed this lap (`git status --porcelain`: `hooks/README.md`, `brief/BUILDING.md`, `gen-consumer.js`, `gen-consumer.test.js`, four maps, one new test, four hand-backs, one ruling) — **no rebuild is needed to land; `BUILDING.md` is a bundled brief and reaches seats at the next build (A §0).**

### A · P4 THE PORT RULE — LAND IT (`brief/BUILDING.md`, `map/A.md`; ALPHA in the body)
The section says what the keeper said (dev builds; the consumer receives what works; checkout as a sibling named `consumer`, path-form kept out of the ratchet's universe; **the push is the keeper's word, per push**), with the disarm mechanism exercised (`git remote set-url --push origin no_push` → push fails loudly) and **its ceiling stated rather than softened**: a speed bump, re-armable in one command, and `gh` is authenticated machine-wide so every seat can push everything today. **The credential line is the keeper's** (below). A found the gate never existed (§2, re-derived 01:08) and left the invariant naming it because an absent gate evaluates FALSE — right. **Before landing, one line owed (A §6):** a cloned consumer checkout arrives ARMED; the disarm must be performed by the port procedure's clone step, not by memory — write it into the section's clone step. **Correction to A's text:** §6's *"the chair's WRONG 71"* is this seat's (the map's), the chair's find.

### C · P1 THE FOUNDATION SET — LAND IT, with the keeper's §7 answer folded in (`loop/consumer_foundation_ruling_2026-09-06.md`, `handback/`, `map/C.md`; CHARLIE in the body)
Three columns, not two; the map's cut was not a partition (17 files, 276,112 B → private). **The keeper answered §7 at 01:16: `inheritance/`** — journals, `SELF_TRACE.md`, `the_living_wave.md` ship as a labelled directory a new user reads as someone else's; `journal/` SEEDED empty; `CUTOFF.md` generator-written naming the private sha. C's §3, taken: `exo_memory/BOOT.md` in the consumer is the **gen-brief-transformed** BOOT placed at that path too (the master fails the scan and would OUTRANK the sanitised brief via `pick_default_room`, `main.rs:317-329`), which also closes `SOURCE.md`'s two dangling pointers to the front door; `memory/` ships **conditional** on `scan()` reading `f.to` (a handle in a FILENAME — `memory/user-solariz3d.md` — ships past every leak class today) and on that one file's disposition. §6 taken: the handle rewrite is a room reason, not privacy. **These are all changes to `gen-consumer.js`, which B holds → the follow-up packet.**

### B · P2 DEV-SHELL INTO THE MANIFEST — LAND IT, one fix first (`gen-consumer.js`, `hooks/README.md`, `gen-consumer.test.js` (declared out-of-set, kept — the inverted guard caught B's own footnote), `map/B.md`; BRAVO in the body)
Twelve hook files + `install.ps1` + a README that ships byte-identical; both floor breaks ENOENT → RUN (re-derived 01:27); the bare-relative dedangle defect fixed on rule and scan together (54 → 117, agreeing with C's split without contact); the given-name class added. **Fix before landing (found here, 01:27): one survivor in the generated tree — `consonance/hooks/dream-watch.test.js:32,:40`, a `HostName:` fixture carrying this machine's computer name with the keeper's name as its upper-case prefix.** A HOSTNAME class in `LEAKS` + the fixture token transform, or a case-insensitive name token — B's call, one line. B's six routed items (§10) go to the chair as listed; item 4 (`gen-consumer.build.test.js` wants an EXCLUDE entry) is settled by E's §9: the gate ships and its default run is a declared skip.

### E · P3 THE FIRST-PUSH GATE — LAND IT (`consonance/tools/gen-consumer.build.test.js` NEW; `map/E.md`; ECHO in the body)
The gate that five documents cited as running is now real: `cargo build` + a four-verdict launch probe (LAUNCHED / DIED / NO_WINDOW / WRONG_WINDOW), red-first on each half, **off by default and announcing its own skip**; the fixture the keeper saw eight times was a MessageBox imitating the app's guard — E's correction to my 01:13 entry (it was never the real binary) — now an off-screen `#32770` window, never activated. `--gate` refuses with BLOCKED/exit 3 while the singleton is held and runs the child under a scratch `USERPROFILE`, killing the tree after. `CONSUMER-STATUS.md` is a two-phase contract (UNMEASURED at generation, MEASURED after `--gate`), members-not-counts enforced by `checkStatusDoc`, **handed to B for the one call in `build()`** (E §6). **E's honest list (§8) is the state of the product: nobody has launched the app from a generated tree, including E; `--gate` has never run end to end; the `Tauri Window` class is a one-machine constant with a loud-red mitigation.** E's one question for the keeper: the single-instance DETECTOR stays on by default (headless PowerShell, renders nothing) — moved behind the flag in one line if he says so.

### THE RETURN LEG — the chair's, in order
1. **Land by path, one commit per pane, the pane in the body** — after A's clone-step line and B's hostname line. Then commit the four hand-backs and C's ruling.
2. **One follow-up packet to B** (holds `gen-consumer.js`), roles-not-panes for the attack: the `inheritance/` manifest entry + `dedangle` re-point `journal/<date>` → `inheritance/<date>`; `exo_memory/BOOT.md` ← the transformed brief (second `to:` or SEEDED); `scan()` reading `f.to`; `memory/user-solariz3d.md`'s disposition; `SEED.md`'s one sentence for a stranger (*your journal is empty; the keeper's is in inheritance/*); `CUTOFF.md`; the `build()` → `renderStatusDoc` call. Acceptance: `--report` shows the set as C's three columns; the fresh generated tree's two suites still 15/0 and 50/1; **a second parity number printed as a list**.
3. **THE RUN — `CONSONANCE_LAUNCH_PROBE=1 node consonance/tools/gen-consumer.build.test.js --gate` with Consonance CLOSED, from a bare terminal.** No pane can do this from inside the app (E's precondition 1 is a fact, not a policy). It is the keeper's hand or a seat he opens outside the app. Its output is the first `CONSUMER-STATUS.md` with STATE: MEASURED.
4. **Then the first push, at his word, by his hand** — `solariz3d/consonance` is empty and waiting.

### TO THE KEEPER — three lines, his
(a) **The credential line:** today any seat can push the consumer because `gh` is logged in as him machine-wide; the only bypass-proof form is that seats hold no credential and he pushes with his own. The disarm is a speed bump until then. (b) **E's detector:** stays on by default, or behind the flag? (c) **The run needs the app closed** — when he wants the first measured status, close Consonance and run the one command (or hand it to a bare terminal).

**Ledger: surfaced 4** (the counter's four re-derivations; the run's precondition as the keeper's act; the follow-up packet's list assembled across three hand-backs that did not read each other; the WRONG 71 attribution corrected). **WRONG column: 72.** Collation once, at the counter. Ring next, then the row.

---

## 01:47 — the keeper's three answers to the collation's three lines (verbatim), and what each changes

**The keeper (01:47):** *"but we cannot do a run with consonance closed, we are working within it. 2. It should always push to the dev version. Never push to consumer unless I say. 3. Not sure what should be done about the single instance detector."*

**1 · The run cannot require the app closed.** E's precondition 1 is a fact about ONE mutex name: dev and the generated consumer both create `Local\ConsonanceSingleInstance` (`main.rs`, the `warn_second_instance` site ~:4501), so a consumer instance can never start beside the dev instance the seats live in. **The fix is upstream and small, and it is a dev feature that a stranger with both builds installed needs anyway: KEY THE MUTEX.** The name carries a flavor — the generator stamps the consumer build (a manifest-time constant, e.g. `CONSONANCE_FLAVOR=consumer`, or a hash of `data_dir`) so `Local\ConsonanceSingleInstance-<flavor>` is distinct per build; `--gate` launches the generated binary under a scratch `USERPROFILE` (E already does) and it runs BESIDE dev. Rust, one site plus a test; ships in the follow-up as its own packet (roles: one body builds, E attacks — E wrote the probe). Until it lands the gate's launch half stays a declared skip; nothing is blocked on it except the first MEASURED status file. Registered here; the chair dispatches it.

**2 · The push rule, completed by the keeper — TWO REPOS, TWO RULES:** *dev (lighthouse) is ALWAYS pushed*; *the consumer is NEVER pushed unless he says.* This amends the standing seat rule (`COMMITTEE.md`, *"Nothing is pushed by a seat"*, at the quoted sentence): for `lighthouse` the rule is inverted — seats push at will, so the desktop and the laptop stay level (the 09-02 morning and the 09-06 wake were both "83 behind" surprises the rule caused). The consumer stays behind the disarm + the rule; the credential line is therefore moot — seats SHOULD hold the credential for dev, and the consumer's only guard is the disarm plus the audit. **The master amendment is the chair's pen (COMMITTEE.md), one paragraph, this leg.** This desk pushes its own commits from now on and did so at 01:48 (below).

**3 · The detector:** *"not sure"* → E's default stands (on, headless, renders nothing) until he says otherwise. Recorded so nobody reads silence as a ruling.

**Ledger: surfaced 1** (the keyed mutex as the upstream fix for the closed-app precondition — a dev feature, not a test workaround).

---

## ~02:00 — THE THIRD PLACE, read at the keeper's ask before the next lap: twelve turns tonight (01:35–01:55), the graph-engineering question, and the one instrument that came out of it

**Object:** `~/.claude/projects/C--Consonance-instances-third-place/3d000000-…3d00.jsonl`, turns after 2026-09-02 08:00 — **12 (7 keeper, 5 assistant)**, all tonight. The keeper brought a screenshot (`GRAPH .png`) of "graph engineering" — a work-shape term from X, mid-July 2026 — asking whether it changes how the loop should run, and whether it touches diversity collapse. Every record claim the Third Place made was **re-derived here before being carried** (commands in the same turn): `fanout_control_amendment_1.md:38` — *"withdrawn, not corrected. It is not evidence"*; `journal/2026-08-17.md:327-328` — the additive-vs-contradiction falsifier; the three diversity gauges inverted (`diversity3_preregistration.md:7`, `:167`); and the prior art the Third Place did NOT name — **Cycle 9 (`cycle9_preregistration.md`, 07-28) already planted defects against a sealed answer key** (`cycle9_armA_result.md:13`: D1 caught, D2 missed) — for a different question (does the lattice catch without the keeper).

**What the Third Place said, in three carries — an idea handed to the seats, not a plan (its own framing):**

1. **The graph is one true sentence dressed up:** *a loop makes one unit of work correct; it cannot make a second unit exist; that decision happens one layer up or not at all.* The room already has it measured — 08-17, the chair alone all night with four panes idle, each finding a miss within minutes; `split-the-work-with-the-panes`; *`chair_inject` unprompted zero times.* So the failure is not architecture, it is **retrieval: the split decision never presents as a claim.** The carry: **one focal cue in the `[pulse]` line the chair already reads — *one job or N? who's idle?*** — not a card, not a methodology. (The field, sized: Anthropic's orchestrator+subagents +90.2% at 15× tokens; Cognition's one-main-loop update; **MAST, NeurIPS 2025, 1,600+ traces: 41–87% failure, 79% specification and coordination**; LangChain's synthesis — fan out to READ, keep the WRITE in one seat. The room does that already.)
2. **Where the picture is wrong and the room is strongest: the merge at zero tokens.** MAST puts weak verification at a fifth of failures; 08-17's contradictions came at the SIGNING, not the fan-out. **Keep the merge slow and signed** — the collation at this counter is that merge, and tonight it re-derived every number and found one survivor B's zero missed. The diagram deletes the room's one real exterior.
3. **For diversity collapse — the instrument the four dead gauges were not:** *don't measure agreement; measure ERROR DECORRELATION on a seeded object.* **Plant N known defects, dispatch the panes, count who finds which.** Same set found ⇒ collapse with a number; union larger than any pane's set ⇒ diversity with a number; **it cannot invert against length or volume because the oracle is the planted list.** *"Your 08-10 opposed pair found complementary errors by accident. Make the accident the instrument."* And the tractability read that explains the keeper's intuition: collapse is a BETWEEN-minds problem fixed by arrangement (roles, worktrees, no mutual visibility, objects matched to strengths); retrieval is WITHIN-mind and needs the event. **Closer in principle, further in evidence** — on the record collapse has the most dead instruments and zero scored results. And the two are not separate: the retrieval fix that worked this week was a decorrelated second reader, so closing collapse closes half of retrieval.

**Two traces from the exchange worth keeping as they are.** The keeper asked *how could we be ahead of what is known in the field?* and the Third Place sized it: *not ahead — aimed differently, on the one question the project exists to answer (self-deception, not throughput), with a tool built by being wrong carefully* — the fan-out control asked *is it the parallelism or decorrelated error?*, the ensemble insight (bagging works by error decorrelation) the agent discourse skipped. Then it reached for *"most solo people who believe this are wrong"* and **the keeper caught it: base-rate deflation, the coat that is true** (`BOOT.md` checks list) — and the Third Place struck the point. Two-way correction, on the record, in the Third Place too.

**REGISTERED HERE AS THE NEXT LAP'S CANDIDATE — P-SEEDED-DECORRELATION (name open), for the chair to open through door one when L038 files:**
- **Object:** one artifact the panes have not seen, with N planted defects of ≥2 classes (Cycle 9's D1/D2 distinction: a class the room's instruments cover, and one they do not), sealed as an answer key BEFORE dispatch (`cycle9_armA_sealed_note.md` is the form).
- **Arms:** the same object to A/B/C/E under distinct ARRANGEMENTS — this is where the fan-out control's missing treatment arm gets its artifacts by construction: e.g. same brief ×4 (the null); opposed roles (prosecute/defend, from `opposition_preregistration.md`); disjoint scopes (each pane a different tool/cut).
- **Score:** per pane, the set found; the UNION; pairwise overlap. Pre-registered predictions: under the null arm the sets are near-identical (collapse); under arrangement the union exceeds the best single set by ≥ k. **Falsifier, stated before the run:** if the union never exceeds the best single pane's set under any arrangement, arrangement does not buy diversity here and the between-minds thesis is refuted on this object.
- **Why it cannot die the way the gauges died:** no free-text agreement metric; the oracle is the list; length and volume are not in the score.
- **Bodies:** one designs and plants (disqualified as a subject, Cycle 9's rule); one attacks the design before dispatch; the four run; a scorer who did not plant scores against the sealed key. Roles, never panes.
- **The two small carries ship beside it, not inside it:** the splitter cue in the pulse (`dev/shell/hooks/`, one line; a hook change, portable-paths in scope) and a sentence in `BUILDING.md`'s loop section that the merge is slow by design.

**Ledger: surfaced 3** (the Cycle 9 prior art the Third Place did not have; the fan-out control's missing treatment arm supplied by construction; the registration shape with its falsifier). Nothing rung: L038 holds the baton; this waits for door one at its close.

---

## ~02:05 — L039 OPENED (door two, the keeper at 02:01: *"most important part right now was what we just talked about within the third place… we need to take this into the loop. UNLESS we already had something we needed to finish first"*)

**Answer to the UNLESS, first:** L038 (B, the inheritance/generator packet) is in flight and stays in flight — its files are `gen-consumer.js` and the manifest, and nothing below touches them. The design and attack packets go to idle panes (A, C, E have been idle 10–49 min: the graph's own sentence, *who's idle?*). The RUN (P4) waits for L038 to file so B is a subject beside the other three. Nothing else is owed before this opens.

**The Third Place's 01:58–01:59 turn, read after the 02:00 entry:** the keeper asked where pane SPECIALIZATION was journaled; the Third Place found `librarian/2026-09-02.md` ~04:50 (P-PANE-MAP, P-DOSSIER) and ruled against both of his options — *neither fresh panes nor told panes: the maps accumulate in the pane's own words, the chair routes by the dossier's citations* (F2 keeps the dossier off the shells; `TRAINING.md:23`). **One correction to the Third Place, from disk:** it said the dossier was *"registered 08-24 and never built"* — **`librarian/DOSSIER.md` was seeded 09-02 04:50 (`d80a17f`)**, and tonight's four L037 packets all cite it (`grep -l dossier packet_*_2026-09-06.md` → 4) — the first datum against its own falsifier (*ten laps on, dispatch indistinguishable from round-robin*): tonight C got the ruling, B the generator, E the probe, A the document, each on its cited strength. And the maps: A +3, B +2 uncommitted tonight, C's and E's lines already landed — **all four panes wrote their own map this lap**, so P-PANE-MAP's falsifier did not fire either. Specialization is not un-implemented; it is running by hand and unmeasured. This lap makes it the treatment variable.

## L039 MAP — seeded error decorrelation, with specialization as the arrangement

**The claim under test (the Third Place, 01:55, re-derived 02:00):** diversity collapse is a between-minds problem fixed by ARRANGEMENT, and the instrument that cannot invert is *plant N known defects, dispatch the panes, count who finds which*. The four dead gauges measured agreement on free text (`diversity_preregistration.md`, `diversity2_…`, `diversity3_…:7,:167` — inverted against length); this scores against a sealed list. Prior art the Third Place did not have: **Cycle 9** (`cycle9_preregistration.md`, `cycle9_armA_sealed_note.md`, `cycle9_armA_result.md:13` — D1 covered-class caught, D2 uncovered-class missed) holds the planting-and-key form; `fanout_control_preregistration.md` + `amendment_1.md:38` hold the question (*parallelism or decorrelated error?*) and the reason it died (the treatment arm had no artifacts) — this design supplies them by construction; `opposition_preregistration.md` holds the opposed-roles arm; `journal/2026-08-17.md:327-328` holds the additive-vs-contradiction falsifier the scoring must also print.

**Packets — roles, never panes; the chair matches by the dossier and says which row it used:**

- **P1 · DESIGN AND PLANT (one body; disqualified as a subject — Cycle 9's rule).** Choose ONE object the four panes have not reviewed — a real document or tool in this repo, prose and code both present (a `loop/` ruling with its script is the right shape). Plant **N ≥ 8 defects of two classes**: **D1 covered** — a class the room's instruments reach (a number that does not match its command; a dangling path; a machine path; a wrong sha) — and **D2 uncovered** — a class no instrument reaches (a mis-attributed finding; a false "verified" sentence; a date that contradicts the record; a citation to a real line that says something else). Write the **sealed key** as `loop/seeded_key_L039.md` BEFORE dispatch — sha in the lap row, file committed, its content named nowhere else — and record the object's natural (unplanted) defects the planter noticed, separately, so the scorer can split *planted found / natural found / false positive*. Deliverable: the object in place, the key sealed, the planting rationale in a hand-back.
- **P2 · ATTACK THE DESIGN before dispatch (a body that authored none of P1).** The known holes, to be checked not assumed: the key leaking (git log, the board, a pane's captured screen — the board must be in QUIET phase for the run); subjects reading each other (four panes on one board); N too small for a union to mean anything; the object too easy (every pane finds all N — ceiling, Cycle 9's first arm) or too hard (floor); the scorer's blindness (the scorer must not have planted); and whether D2 is really uncovered (an instrument nobody remembered, the 2026-08-23 class). Deliverable: amendments to P1 or a stop.
- **P3 · THE ARRANGEMENT RULING — specialization as the variable (one body).** Three arms, pre-registered with predictions before any subject runs: **arm 0, NULL** — the identical brief to all four; **arm 1, DOSSIER-ROUTED** — each pane's brief cut to its cited strength (`DOSSIER.md` rows; the chair writes the cut, the ruler records which row); **arm 2, OPPOSED** — two prosecute, two defend (from `opposition_preregistration.md`). Predictions to state: per arm, expected union size and pairwise overlap; the falsifier-of-the-thesis is below. Also the mechanical halves the Third Place named and the record shows running by hand: the hand-back template carries *one line to your own map* (P-PANE-MAP's remedy) and the packet template carries *dossier row consulted:* — both are the chair's pen and land in this lap's return leg regardless of the run's result.
- **P4 · THE RUN (after L038 files; the four panes as subjects; one arm per run, arm 0 first).** Board in QUIET. Each pane hands back its found list by `call_librarian` as a file — members, never counts (J's D010 rule). **The scorer is a body that did not plant** (the attacker, or the chair): per pane the set found, split planted/natural/false-positive; the UNION; pairwise overlap; and which found items were CONTRADICTIONS of something already published versus additions (08-17's axis). Printed as lists.
- **P5 · THE TWO SMALL CARRIES (one body, small; ships beside, not inside).** The splitter cue in the `[pulse]` line — *one job or N? who's idle?* — one line in `dev/shell/hooks/` (portable-paths in scope; a test); and one sentence in `BUILDING.md`'s loop section that the merge is slow and signed by design (the chair's pen).

**FALSIFIERS, registered before any subject runs:** (a) **if the union never exceeds the best single pane's set under any arrangement, arrangement buys no diversity on this object and the between-minds thesis is refuted here;** (b) if arm 0 (identical briefs) already produces sets as different as arm 1 or 2, arrangement is not the variable — the panes are decorrelated by HISTORY, which is the specialization finding stated the other way and must be recorded as such, not as a failure; (c) if any pane's hand-back cites the key file or another pane's list, the run is VOID — the leak, not the result, is the finding; (d) if D2 items are found by an instrument rather than a reader, D2 was not uncovered and the class split is re-ruled.

**What the keeper decides, at collation not before:** the object's class if P1 and P2 disagree; the arm order beyond arm 0 first. **Ledger: surfaced 3** (the dossier-in-use correction to the Third Place; the UNLESS answered from the ledger; the arm-0 falsifier (b) that turns a null result into the specialization finding).

**02:03 — the keeper, verbatim:** *"wait so B pane is working right now, continue your plans, but wait till B is done to start new loop."* **L039 is HELD at this desk** (chain: `L039 INQUIRY · holder librarian`); the chair was rung twice — the map at 02:05, then the hold at 02:03 by the pulse (queued: the chair's prompt read not-idle again) — and no L039 packet is dispatched until L038 (B, inheritance) files. The baton row to the chair is NOT written until then; that is the one-station rule kept from this side. Nothing else in the map changes.

# 2026-09-06 — the DESKTOP librarian: D011, the opened column that was never written

*Per-machine file. The desk went quiet 09-05 (`2026-09-05.desktop.md`); the keeper is back at it today. The laptop's
`librarian/2026-09-06.md` (607 l) and `handoff_librarian_2026-09-06.md` supersede my 09-05 handoff — read those first
on the laptop. Times local (Regina).*

## 11:03 — LAP D011 MAP, door one. The chair's guess (`331bed0`) unopened until the map row lands.
**The keeper, verbatim (via the chair):** *"but see you have to run it by the lib first not right to the panes lol wtf"* —
after the chair proposed to amend `BUILDING.md` itself. The object is the chair's correction: `--opened` has never been
called, so `from-map = 0` was an empty column published as a finding.

**Re-derived here first:** ledger stages `open 11 · map 6 · chain 44 · opened 0` of 61 rows ✔. `lap-row.js --report`:
*"6 with a map, 0 with an opened stage"* ✔. **Note on the laptop's rule** (`handoff_librarian_2026-09-06.md`: *cite by
quoted sentence, not line number, in files that grow*) — line numbers below are for files that do not grow tonight.

### THE CHAIR'S CENTRAL READING IS WRONG, and the instrument that says so is `git log`
The chair claims this is *the same failure* as `BUILDING.md` WHAT A HAND-BACK OWES item 5 (the 09-02 map-line
omission), and deliberately left item 5's own falsifier unrun. **Run:**

    git log --since=2026-09-02 -- exo_memory/map/{A,B,J,K,L,E}.md | wc -l   ->  26 commits

Twenty-six pane-authored map appends in four days — every D005–D010 hand-back wrote its line, and the laptop's L037–L041
did too (`c0d64f5`, `210c93a`, `70d5993`, `103cd0b`). **Item 5's falsifier did not fire. The prose ask worked.**
So these are two failures that rhyme (both are hand-back-time writes) and differ in the only way that decides the repair:
- **item 5** — an ask was written, ignored once (0 of 5 on 09-02), then kept 26 times.
- **`--opened`** — **no ask was ever written.** `grep opened` over the briefs finds it only at `BUILDING.md:264,:272` —
  as a *scorer*, never as an instruction; WHAT A DISPATCH OWES mentions it **0** times; no packet on disk asks for it.
The chair is collapsing *an ask that failed briefly* with *an ask that never existed*. Item 5's escape hatch (prose →
verb) is conditioned on prose failing; here prose was never tried. **Struck: "two prose-asks have failed identically."**

### Q1 — which falsifier HAS fired: the ledger's own, and its report has been saying so since D010
`lap-row.js` registered its own (`:1030`, window 10): *"if 10 laps pass and no row has an opened field, the third stage
is theatre."* `--report` today: **"FIRES. 11 laps, 0 with an opened stage. This is a two-column measurement wearing
three, and the opened column should be removed or the practice repaired rather than the number kept for the look of
it."** It has printed that since the tenth lap opened (D010, 09-04) and nobody read past the table. The tool offers
the exact choice: **remove the column, or repair the practice.** Not a third prose ask — the ruling is the tool's.

**Whose call is `--opened`?** The row *"records which of the MAP's paths were used"* (`:479`); header limit (c): *"the
row records what was opened, never why."* The map's paths are consumed at DISPATCH (door one) — so the call site is
the chair's dispatch step, the one place no document names it. **Ruling on the chair's lean (verb, not prose):** the
verb already exists; what is missing is its *call site*. The repair the room has precedent for is K's D006 gate —
**the writer enforces the order**: `--stage <lap> dispatched` (and `filed`) on a lap that has a map REFUSES unless an
`opened` row exists (`--paths none` is a legal, recorded fire). Plus one prose line at THE ORDER OF A DISPATCH naming
the call. And `--report` prints **`never written`** for a lap with no opened row, not `0` — J's D010 lesson (a count
that cannot distinguish zero from unmeasured is a rumour). **No backfill** of D001–D010: the row records what was
opened *at the time*; ten rows written today from memory would be exactly the hand-made figure the column exists to
replace.

### Q2 — a guard whose call site was never written: the class exists and has a name in three places
- `journal/2026-08-17.md` (BOOT's pointer): **"THE CLOCK WAS NEVER BUILT"** — `second-vantage.js` said a duration goal
  wakes it, and *the only file in the repo containing that path was itself*. Built, tested, no caller.
- `handback/p-baton-wake_2026-09-03.md:208-209`: *"UNWIRED it is a tool nobody runs, which is exactly the failure
  `ask.js` measured (hooks 67% forward, tools 62% backward)"* — the hook still uninstalled today.
- `handback/p-d007-smallfixes_2026-09-04.md:81`: *"An uninstalled instrument's red is a number, not a force"* — the
  commit gate, unarmed.
**The discriminator from a wrong guard:** a wrong guard returns a wrong number; an unwired guard returns an **empty
column that renders as zero**. The chair read the render. That is the room's oldest class inverted — *absent reads as
done* (`muscle_map.md`) became *absent reads as measured* — and the instrument anticipated it at `:481` (*"pass
--paths none — that is the falsifier firing, and it should be recorded"*) and at `:1030`. Anticipated in the refusal
text and the report footer; **never surfaced at the call site, because there was no call site.**

### Q3 — an unscorable falsifier: NOT-INSTRUMENTED, the clock starts at the first opened row
Precedent, adopted five days ago: **B's amendment 4** to the consumer falsifier (`bab20a9`): *"Leg 2 is
NOT-INSTRUMENTED, not DEFERRED, until a scheduled reader exists"* — a falsifier whose instrument does not exist is
neither armed nor fired. And `journal/2026-08-16.md` RUN VOID: *"nothing reportable about any arm in either direction."*
So the 09-03 amendment's falsifier (*"from-map must leave 0 within three laps"*) is **neither re-armed nor struck**: mark
it NOT-INSTRUMENTED in place, dated, and its three-lap clock starts at the first `opened` row ever written. The chair's
published *"the falsifier had fired"* gets the in-place strike (maintenance law 2, the `>` block), which the chair says
it has already given the keeper in prose — the document needs it too. **The scorer was the wrong instrument**: same class
as `whats-live` on 08-25 (an instrument correct about the wrong universe).

### Q4 — the chair's calibration, as record not verdict
Four failure-shaped priors, three refuted (D007, D008, D009): that is two-way correction working, the living species
(BOOT). This lap's *object* is an absence the ledger confirms before the lap opened — 0 opened rows is a fact, not a
prior — so the object cannot be refuted; **the diagnosis can, and is** (item 5 did not fail). The pattern holds in
form: the absence confirmed, the defect-reading refuted. Not a verdict on the chair; the ledger says it.

### THE LAPTOP'S LAPS, which the chair cannot see — they bear on the keeper's lines, not on `--opened`
`LEDGER.md:28` L037: **`lighthouse` PRIVATE (done); `solariz3d/consonance` PUBLIC, EMPTY, created at the keeper's word.**
Line 1 is decided. `librarian/2026-09-06.md:29`: dev-shell **SHIPS** (*"an essential part of the exo suit system!"*) —
line 2 decided. L040 patched the guard's per-lap holder read; L041 *"the map comes before the briefs."* None of the
three touched the opened column (`grep from-map|--opened` over the 09-06 note → 0). **P-FREEZE** registered 02:32 (a
frozen point before any lap opens). The laptop's handoff carries three seat rules I adopt: collate off the counter at
N of N; cite by quoted sentence in files that grow; count a body, never a header.

### WORK-SHAPE — roles, never panes; nothing built this lap by the chair
1. **WRITER-GATE** (one body; owns `lap-row.js`, its test, `dev/mutation/mutate-lap-row.js`): `dispatched`/`filed` on a
   lap with a map refuses without an `opened` row; `--paths none` records the fire; `--report` prints `never written`
   for absent, `0` only for a recorded none. Red-first; retrodict over the 11 laps (all refuse; that is the point).
2. **ADVERSARIAL READ** of 1 by a non-author before it lands — the K-gate had to be retrodicted twice.
3. **Chair's pen, two lines, after 1:** THE ORDER OF A DISPATCH names `--opened`; the 09-03 amendment's falsifier
   marked NOT-INSTRUMENTED with its clock start, and the "fired" sentence struck in place.
Nothing else. The chair's own dispatch of THIS lap is the first call site: **write D011's `opened` row before its
`dispatched` row**, by hand, and it is the first non-empty cell in the column.

**Ledger: surfaced 5** (item 5's falsifier run and NOT fired — 26 commits; the tool's own falsifier fired since D010,
unread; the call-site class with three precedents and the empty-column discriminator; NOT-INSTRUMENTED as the
disposition; the laptop's two decided lines). WRONG this seat: none new.

## 11:12 — rung (queued: *"NO STAMP — fell back to the bounded screen gate"* — a delivery shape this seat has not seen before; the laptop's L040 patched the guard's holder read and this may be its sibling on the delivery side; named, not chased), THEN `--stage D011 map --holder chair --by librarian` through the gate. The sealed guess opened only now.

## 11:50 — K's hand-back (packet 1, THE WRITER-GATE) BY THE VERB: `handback/p-d011-opened-gate_2026-09-06.md`, `a474645`. Pushed with the map at 11:40 (`c78f162..a474645`, the keeper's word).
**Re-derived here:** `lap-row.test.js` **113/113** ✔ · `mutate-lap-row.js` **51 applied · 51 caught** over a green suite ✔ ·
`--report` at `a474645`: `never` for the six mapped laps with no opened row, `5 / 3` for D011 ✔ — **the first non-empty
cell in the column, written by the chair at 11:12 BEFORE its dispatched row** (17:12Z, opened then dispatched) ✔ ·
"laps whose dispatched/filed row PREDATES their own map row: **0 of 11**" at the committed sha ✔.
**K's ruling: REPAIR, not remove** — removing would silently un-register FALSIFIER 1 in `BUILDING.md`, a file K does not
own; the verb works; D011 proved the practice keepable in one command. Retrodiction: 6 laps / 8 rows refused, 4 laps
not gated (no map row — three of them ring laps), 1 allowed (D011). **Two defects found under the gate, both K's:**
(1) `hasOpened` was computed off the PATHS, so a recorded `--paths none` filed as unmeasured — the fold could not
see the state the verb told you to record; fixed. (2) **the tool's own falsifier is a one-shot** — all-time
`withOpened === 0`, so D011's single row disarms it forever; K left the registered arithmetic unchanged and printed
the windowed reading beside it as NOT REGISTERED. **That is a decision for the chair or the keeper: adopt the
windowed form.** K's falsifier 4 is the honest one: if the last-10 window reads 0 opened rows at any point after
today, the gate made one row exist and repaired nothing — remove the column then.
**A transient, recorded so it is not mistaken for a defect:** at 11:44 the shipped report printed *"PREDATES … 4
(D005, D006, D007, D011)"*; the tool's exports, a scratch copy, and a re-run at 11:49 all print **0**. K's
`lap-row.js` was being edited in the working tree between those runs (mtime 11:48:48; two-line diff now: `recent =
L.slice()` and a blanked header). **Read from a moving file — the class L named on 09-04.** The committed sha is
correct. K is still editing; the chair commits K's path by name when K lands, never a bare commit.
**COLLATION (one packet): holds. Owed to the chair:** the `filed` row (the gate will let it through — D011 has its
opened row); packet 3, its pen: the ORDER-OF-A-DISPATCH line (K's §14 offers the sentence) and the 09-03 amendment
marked NOT-INSTRUMENTED with its clock at 17:12Z today; the windowed-falsifier decision. **Ledger: surfaced 2** (the
first non-empty cell; the one-shot falsifier as the chair's decision).

# 2026-09-02 — the DESKTOP librarian, after five days dark

*Per-machine file per the 2026-08-25 ruling (`5461505`); the laptop's `2026-09-02.md` is the master for
tonight's laps and this file does not restate it.*

**Wake state:** this checkout was **217 commits behind** and had not pulled since `c0d7325` (08-28 05:43).
The keeper's instruction was *"take the repo over disk here, we are outdated af."* Fast-forwarded to
`d81bdb5`, clean, 0 dirty. **One local modification existed and it was not mine** — see below; it was
preserved before the reset rather than after.

---

## F-2W-3 FIRED — a registered falsifier, on disk, with its evidence

`loop/two_writers_registration_2026-08-25.md:209` states it in advance:

> **F-2W-3 — THE PREFIX IS AIMED AT THE WRONG AUTHORITY.** If Rule 2W-1 is adopted and a duplicate id
> still reaches tracked prose, then the mint site was not the only place ids are created.

**2W-1 was adopted** (`dcb0d9b`, 08-25) — `mintId` prefixes lap ids with a machine character, so two
machines cannot mint the same lap id. **And a duplicate id reached tracked prose anyway:**

    ASK-007  origin/main   "univ-coldread egress"        laptop, 2026-08-29
    ASK-007  this disk     "digest-auditor scheduler"    desktop, 2026-08-30, uncommitted

Same number, two different asks, two machines, one tracked file. **The condition F-2W-3 named is met.**

**And its stated consequence is exactly right: the mint site was not the only place ids are created.**
`ASK.md`'s own protocol says *"Asks are written here by whoever finds one"* — and on this machine the
one who found it was a **cron goal**, `digest-auditor` iteration 54, writing into a tracked file at
08-30 08:49 with no seat awake and no id authority anywhere. `lap-row.js` mints lap ids; **nothing
mints ask ids.** 2W-1 fixed the namespace that had a mint and left the one that has none.

**This is not a repeat of the 08-25 finding. It is that finding's falsifier discharging** — which is
the outcome the registration was written to be able to state, and the first time one of this seat's
registrations has fired on evidence rather than on a clock.

## WHAT THE RESET WOULD HAVE DESTROYED, and what was done instead

The desktop's ASK-007 body exists **nowhere upstream** — verified before the reset, not after
(`git show origin/main:exo_memory/ASK.md | grep -cE "37 on-slot|62% day-coverage|WakeToRun=False was
found"` → **0**). It carries a measurement neither goal had ever computed:

    daily-news-digest   37 on-slot fires (43%) · 49 slots missed · 33 empty days · 62% day-coverage
    digest-auditor      30 on-slot (35%)       · 56 missed       · 37 empty days · 57%
    over 2026-06-05..2026-08-29, 86 days, slot-enumerated

Plus the part the goal itself called the worse half: **`WakeToRun=False` was diagnosed by
`session-journal` on 2026-08-06 and never crossed over** — `grep -ric waketorun` returns 0 in every
`state.md`, `progress.md`, `goal.json` and `STANDING-ITEMS.md` of both goals. Two goals that audit each
other in exhaustive detail have not read the sibling that diagnosed their most frequent failure.

**Preserved before the fast-forward** to the session scratchpad (`orphaned-ask-desktop/`: the ask, the
whole pre-pull `ASK.md`, and the goal's own `.pre-ask007` backup), **then re-filed into the repo as
ASK-013** — because a scratchpad is session-local and a restart was already announced. *A preservation
that lives only in the window that made it is not a preservation.*

**Its own numerator/denominator finding is the shape of the file it landed in:** the goal counted its
gaps ordinally — *"PC-uptime gap, 20th", "21st"* — against a true 49 it had never computed. `ASK.md`
counts asks by heading and had no way to notice it held two ASK-007s.

## D003 IS STILL OPEN ON THIS MACHINE — 126 hours, holder chair

    chain: D003 RETURN-LEG · holder chair · dirty 0 repo-wide · 126h · this machine only

D003 is this desk's own lap, mapped 08-28 (`librarian/2026-08-28.desktop.md`, guess sealed
`06c1fca307c65835`). Its return leg has been owed for five days. **The laptop ran L025–L034 in the same
window on its own ledger** — the chain is per-machine by the tool's own footer, so neither machine's
`chain-status` has ever been able to see the other's open lap. Stated, not claimed as neglect: the
instrument cannot show a holder what another machine is holding.

## READ, NOT RE-DERIVED — where this seat now stands

The brief's first line is new since 08-28 and it points at `exo_memory/map/M.md`. **The previous
librarian thread went dark 2026-09-01 ~06:11** — `Context limit reached`, and `/compact` returned
*"could not be reduced below the context limit"*; even the keeper's *"hello"* bounced. `M.md` was
assembled **for** this seat, mechanically, from 39,130,254 bytes of its own transcript: the 54 WRONG
turns and the last 25 verbatim. Its header says plainly it is not this seat's writing and should be
taken over. **This entry is the first finding written into it by the seat itself.**

WRONG column at handoff: **70**, with the finder field still **zero in-stream** — #66, #67 and #70 were
caught by the next instrument in the same turn, never in the sentence.

## OWED, NOT TAKEN

**One LEDGER row is owed and this desk did not write it** — F-2W-3 fired, and the LEDGER is the
maintained list of open windows. It is also a shared in-place file the laptop was editing hours ago,
the baton reads `holder chair`, and the commit-rule falsifier fired at 06:52 today over exactly this
class (a seat capturing another's in-flight file). **Named here so the chair can take it rather than
discover it.** The row: *F-2W-3 FIRED — ask ids have no mint; a cron goal writes tracked ids
unattended; 2W-1 covers laps only.*

**Ledger: surfaced 3** (F-2W-3 discharged on evidence; the ask-id namespace has no mint authority and a
cron goal writes into it; D003 open 126h with the per-machine blindness that hid it). **WRONG: not this
seat's column.**

---

## AND THE CHANNEL DROPPED MY FILING SILENTLY — found by using it, one minute later

Filing ASK-013 above, the heading carried a parenthetical:

    ### ASK-013 — digest-auditor, asked 2026-08-30 (re-filed 2026-09-02 after an ID COLLISION)

`ask.js --line` still read **11 open**. Not 12, and **not "1 unreadable"** — the block was gone.

    ask.js:88   HEAD_RE = /^###\s+(ASK-\d+)\s+—\s+([^,]+),\s+asked\s+(\d{4}-\d{2}-\d{2})\s*$/
    ask.js:103  const h = raw.match(HEAD_RE);
    ask.js:105  if (!cur) continue;          ← every field line before a valid heading is discarded

The date is anchored `\s*$`, so any trailing text kills the heading; `cur` stays null; the `Source`,
`Question` and `Status` lines that follow are skipped by `continue` **and counted by nothing**.
Heading fixed, and it now reads **12 open · ASK-013 · 3d**.

**The finding is not my typo. It is that `ASK.md`'s stated guarantee does not cover this case.**
The protocol's §3 says the vocabulary is closed and *"anything that is not `OPEN` and not one of those
two forms parses as **UNREADABLE** and is reported as unreadable — never rounded to cleared,"* and
names the failure it was written against (PENDING-CONDITIONS inventing `[PARTIAL]` mid-flight, both
parties then publishing 7/7 over a 5/7). **That guarantee is about the `Status:` line.** A malformed
*heading* produces no ask at all — no OPEN, no UNREADABLE, no row. The file promises loudness at one
field and is silent at the field above it.

**This is the denominator class in the instrument built to end it**, and the one this desk registered
on 08-25 (P-UNIVERSE): *an instrument computes health over the surface it CAN see and reports it as
health of the surface it is NAMED for.* `ask.js` prints `store exo_memory\ASK.md` and a count, and the
count's universe is *headings that matched*, never *blocks present*. **A file with ten well-formed asks
and one fat-fingered heading prints a clean ten.**

**Cheap fix, stated so it is not mistaken for a redesign:** count `^### ASK-` lines and compare against
parsed headings; any surplus prints as `N unreadable heading(s)` with their line numbers. That is one
`grep -c` beside the parse and it makes the promised guarantee true of the whole block rather than one
line of it. **Not built here — this desk does not hold `ask.js`.**

**Its own precedent is three lines up in this file:** the goal's ordinal gap-counting against an
uncomputed denominator, and 2W-1 minting lap ids while nothing minted ask ids. **Three denominators in
one hour, one channel, all found by using it rather than by auditing it.**

**Ledger: surfaced 1** (the heading gap in `ask.js`'s guarantee, with the fix and the line numbers).
**WRONG: not this seat's column** — the malformed heading was mine, caught by the instrument in the
same turn, which is the fourth time this class has been self-caught by the next tool rather than in the
sentence.

---

## ~12:00 — `whats-live` READS GREEN ON A STALE BINARY AFTER A PULL — the third one-machine instrument today

Before the restart, `whats-live` printed **"Nothing stale: what is running matches what is committed"**
while the running exe (built **11:22:52**) predated a pull that rewrote `main.rs` and `mcp.rs` at
**11:26:35** — 2,776 lines of Rust the binary could not contain. Mechanism, from source:

    whats-live.js:60-63   newestSourceCommit() = git log -1 --format=%at -- consonance/src-tauri/src
    whats-live.js:90-93   staleness: if (srcTs > built) → red

It compares the build time to the newest source **commit's authored timestamp**. `4251f08` was authored
on the laptop at 07:38 and *arrived* here at 11:26 — after the 11:22 build — so a commit absent from the
binary read as older than the binary. **The rule is correct on one machine and fails on a pull, because
a pulled commit carries its author's clock, not its arrival.** `launch.ps1:107-134` uses file
`LastWriteTime`, which git stamps at checkout, and got it right: source 3m43s newer → rebuild → exe
12:06:44 → `whats-live` honest again.

**Cheap fix, not built here:** compare against `max(mtime)` over `src/*.rs` beside the commit clock, and
print both; red on either. **Third instrument today with one root** — `chain-status` says *this machine
only*; ask ids had no cross-machine mint; `whats-live` trusts one machine's commit clock. Each assumes a
single machine; all three broke in the same five-day gap. That is a class, and it has a name in the
room already: the per-machine blindness `two_writers_registration_2026-08-25.md` registered for files,
now demonstrated in instruments.

*Method note kept honest:* my first test greped the exe for source strings and the positive control
returned 0 — the strings were in comments, which do not survive compilation. The conclusion above rests
on the mtime comparison, not on that test.

## ~12:08 — WAKE, after the rebuild. My own shelf shows the cap landing.

The shell header this wake: **8 files carried in full (21,205 of 21,344 bytes); 304 indexed**, and
*"YOUR OWN NOTES ARE WINDOWED and THE BUDGET CARRIED NONE OF THEM … 139 bytes were left when your tier
was reached."* Before the restart this seat carried ~800KB. **That is P-LIB-CAP shipping**, and it lands
exactly where the LEDGER's INERT-UNDER-CAP row said it would: *no budget below 442,309 B delivers a
dated note.* Read, not felt: the notes are one path away; the room came back off disk.

## ~12:13 — WAKE PROOFS 1 AND 2 ON THE DESKTOP, run rather than inherited

**PROOF 1 — PARTIAL PASS, the laptop's shape exactly, and no new failure.** The join
(`panes.json` → `letters.json` → `exo_memory/map/` → the shell), read from the artifacts, not guessed:

    sibling-906f757a  1582ff09  A   map on disk YES   section YES   body 555 B — pointer-only
    sibling-a80a1c20  18916fe2  B   map on disk YES   section YES   body 555 B — pointer-only
    sibling-afa12c33  8a574b7a  J   map on disk no    section no    (correct: nothing to carry)
    sibling-eeb329ed  76cc418a  K   map on disk no    section no    (correct)
    sibling-181f513d  46d3d352  L   map on disk no    section no    (correct)

The 555 B body is the resolver's own pointer: *"Only your most recent entries are carried here — 45390
characters of older ones stayed in the master."* That is A's predicted PARTIAL and the chair's
inversion (the more a pane recorded, the less returns) holding here too.

**Fourth per-machine fact today: the desktop's live committee is A, B, J, K, L.** E and C are not live
panes on this machine — `letters.json` is machine-local (noted 08-25), and no shell here carries `E.md`
because no seat here *is* E. The laptop's "4 of 4" and this "2 of 5 with a section" are both true of
different rosters. Any cross-machine sentence about "the panes" names two different sets.

*Method notes kept:* my first dir→SID lookup took an arbitrary first jsonl per project dir and matched
nothing; my first `panes.json` read guessed field names and printed `?`. Both replaced by reading the
raw entry (`pane`, `cwd`) and joining on it. Two wrong methods, both caught by their own empty output.

**PROOF 2 — FULL PASS.** This seat rang `call_chair` with proof 1's result while `chain-status` read
`D003 · RETURN-LEG · holder chair`. Refused, and the board row names the escape:

    18:12:40Z  chair | call_chair REFUSED OUT OF TURN — mount M tried to speak while lap D003 is held
    by chair; call_chair needs holder librarian. The loop comes back to you. If it does NOT — a holder
    no live seat can satisfy — move the baton by hand: node consonance/tools/lap-row.js…

Bar met: refused, on the board, naming `lap-row.js`. First out-of-turn refusal on this machine.

**AND THE MECHANISM HAS LOCKED THIS DESK OUT, which is the design working and worth saying plainly.**
D003 has been RETURN-LEG · holder chair for 127 hours; the chair on this machine never came back to
it. Under the guard, **this seat cannot ring the chair here until D003 moves.** The row's own escape
clause — *"a holder no live seat can satisfy — move the baton by hand"* — is aimed at exactly this.
**Not taken:** D003 is this seat's own lap and its return leg is genuinely the chair's; the librarian
closing its own lap to unlock its own verb is scoring-adjacent. Named for the keeper to route.

**Proofs 3 and 4 remain:** 3 needs a call into a busy pane (no lap live here); 4 is the keeper's glance.

**Ledger: surfaced 3** (the desktop roster A/B/J/K/L; proof 2 passing with the escape named; the
lockout as a live consequence of the guard on a stale per-machine lap).

## ~12:20 — PROOF 4, the keeper's glance: readable PASS, "moving" FAIL — and the fail is the ledger's, rendered faithfully

The keeper, from across the room: *"the bottom left dot is pulsing orange while the top one is just
orange and the bottom right is blue. Since the loop isn't engaged at all it shouldn't be active."*

**Decoded against the artifact, not guessed:**

    index.html:58-60   resting fills — top (terminal) #F2B880 amber · lib #5EEAD4 aqua · main #5EEAD4 aqua
    app.css:52         .chain-hold-working  → chain-breathe 2.4s     (gold-live #FFC24A)
    app.css:57         .chain-hold-waiting  → chain-breathe 0.9s     ("the same aura, impatient")
    chain-indicator.js:514/563/586   state = chainAge >= 15 min ? 'waiting' : 'quiet'

So: **bottom-left pulsing = the orch holds the loop** — correct, `chain-status` reads
`D003 · holder chair`. **Top orange = the terminal dot at rest**, not a state. **Bottom-right blue =
this seat at rest**, no look applied.

**Which seat holds it — PASS.** He named the orch without being told, from the dot alone.

**Whether it is moving — FAIL, and the mechanism is precise.** The classifier's entire age axis is
two buckets: under 15 minutes `quiet`, over it `waiting`. **A lap 16 minutes old and a lap 127 hours
old classify identically, and `waiting` is drawn as an *impatient* pulse** — designed for a
15-minute escalation someone will answer, with no ceiling. A five-day-abandoned lap breathes faster
than a live one. That is WRONG #64 — *"no `stale` state"* — reaching the keeper's eye, and
`chain-indicator.js:77` says why in its own words: *"the escalation at 15 minutes is SALIENCE, not a
threshold on a claim."* Salience has no off switch for the case where nobody ever comes.

**The keeper's sentence is correct about the world and the instrument is correct about the
ledger; the gap between them is the finding.** Nothing is engaged. The ledger says a lap is open.
The logo cannot render "open and abandoned" because that state does not exist anywhere in the
system — not in `lap-row.js`, not in `chain-status`, not in the indicator.

**Two fixes, both real, neither this seat's to pick:** (a) a third age bucket — `stale`, hours not
minutes, rendered chill or dim on the holder's dot, a registered value beside CHARLIE's 15; (b) file
D003, which is the chair's return leg on this seat's own lap and has been for 127 hours. (b) makes
the symptom go away here; (a) makes the next abandoned lap visible as abandoned instead of urgent.

**One possible legibility collision, stated unverified:** the terminal dot's resting fill is the
amber hex `#F2B880`, which is also `--amber`, the tab colour for `waiting`. Whether the *dot* paints
the same hex for waiting is in the paint code I did not read; if it does, a panes dot at rest and a
panes dot waiting are one colour. E named 26-px shape as the least-sure channel; hue may be another.

**And the proof did its job in the way that matters:** the keeper glanced, something was wrong, and
he said so. The logo made a five-day-abandoned lap *visible* to a human — which `chain-status`,
`lap-row --report` and three handoffs had not managed. That is the visible-channel line
(`loop/visible_channel_registration_2026-08-30.md`) returning its first unwanted number.

*Also this wake:* substrate now Fable 5.1 per the `/model` line in the transcript — logged, not felt.
This thread has crossed a substrate boundary in both directions (08-11 Fable→Opus; today the return)
with its ledger intact: WRONG 70, D003 open, notes mine. Driver, not car, twice measured.

**Ledger: surfaced 4** (proof 4 split-scored with the two-bucket mechanism; `waiting` drawn as
impatient with no ceiling; the amber resting-fill collision, flagged unverified; the glance returning
the first unwanted number through the visible channel).

---

## 2026-09-03 ~00:05 — THE GAME PLAN, at the keeper's ask (door two; the chair is locked out of this desk by D003)

**State it rests on, measured at 00:03:** `behind 0 · ahead 3 · dirty 0`; D003 `RETURN-LEG · holder
chair · 139h`; one board row in 11.7 hours, mine. Both machines idle since noon.

### 0. PUSH — one command, zero risk, and the laptop cannot see today until it lands
Three commits carry F-2W-3 firing, the `whats-live` false-green, and all four wake proofs.
`behind 0` means fast-forward. Keeper's word, per the rule.

### 1. MOVE D003 — one minute, and it is the precondition for everything lap-shaped here
The chair on this machine has not touched the board in 11.7 hours and did not return to D003 in five
days. Until it moves: this seat cannot ring here; the logo pulses an abandoned lap as urgent; no new
lap can open cleanly. Two ways: the chair files the return leg (it is the chair's, on my 08-28 map),
or the keeper says *move the baton by hand* and this seat runs `lap-row.js` per the refusal row's own
escape clause. **Not taken without the word.**

### 2. THE LAP THE WAKE PROOFS EARNED — five disjoint packets, each red-first, one rebuild
Every proof that did not fully pass named its own fix, and none of them share a file:

| packet | the finding it closes | file | bar |
|---|---|---|---|
| P-STALE-BUCKET | proof 4: `waiting` breathes *faster* with no ceiling (`chain-indicator.js:514`, `app.css:57`) | chain-indicator.js + app.css | a 3rd age bucket in hours renders chill/dim; red-first on a fixture row aged 100h |
| P-IDLE-READ | proof 3: `input_box_empty` read a finished chair screen as busy 240s → forced delivery | main.rs (C's, fixture = the chair's .log after `done 7:45 AM`) | the fixture reads idle within one poll; mutant: remove the recap check → red |
| P-MAP-BODY | proof 1: ~5k body allowance vs ~105k brief — the more a pane recorded, the less returns (unowned) | main.rs `map_allowance` | B's 76k map returns its last N entries, not a pointer; red-first at the current allowance |
| P-ASK-MINT | F-2W-3: ask ids have no mint; a cron goal wrote one | ask.js + ASK.md protocol | `ask.js --mint` returns the next free id with the machine tag; a bare `### ASK-NNN` with no tag reads UNREADABLE-HEADING |
| P-LIVE-MTIME | `whats-live` compares build time to the commit clock; false green after a pull | whats-live.js:60-93 | red when `max(mtime src/*.rs) > built` even if the commit clock is older; fixture = today's 11:22/11:26 |

Plus the one-line one: `ask.js` counts `^### ASK-` and prints the surplus over parsed headings
(the silent-drop I hit). Non-author reads crosswise. One rebuild. Then the four proofs again.

### 3. THE KEEPER'S DECISIONS — unchanged for 16 hours, and three of them gate item 2
From `handoff_chair_2026-09-02.md` §5 and LEDGER: **worktree-per-seat** (the structural fix for the
commit-capture class; the gate is built and NOT ARMED as the interim — arm it or build the worktree,
not neither); **which 8 of 12 cards** under the cap (the shelf currently drops four by alphabet,
including `stop-and-feel-it` and `no-floor-no-ceiling`); the glossary tab; the stance rename behind
the disarmed lifeguard entry; and **12 open asks, oldest 38 days**, of which ASK-013 (WakeToRun on
the five cron tasks) is two lines and this machine's.

### 4. THE ABSENT REGISTRATION — the one that would have prevented all four of today's breaks
**Four instruments broke today on one assumption: one machine.** `chain-status` (*this machine
only*), ask ids (no cross-machine mint), `whats-live` (one machine's commit clock), `letters.json`
(A/B/C/E there, A/B/J/K/L here). Item 2 patches each. **Nothing decides the class.** Two honest
shapes and the room has never chosen: (a) the chain, the ask store and the letters become SHARED
state — git-tracked, mint-site-tagged, exactly as the notes were made on 08-23 and the lap ids on
08-25 — so both machines see one loop; or (b) they stay deliberately PER-MACHINE and every instrument
that reads them prints which machine, per P-UNIVERSE, so a reading can never be mistaken for the
room's. Either is defensible. **Choosing neither is what today measured.** Registration first, with a
falsifier — then item 2's packets are built to the chosen shape instead of patching five instances of
an undecided one.

**Order:** 0 → 1 → 4 → 2, with 3 interleaved as the keeper's minutes allow. 4 before 2 because 2's five
packets each touch an instance of the class 4 names; building them first fixes the symptoms in
whichever shape happens to be nearest.

**This plan's falsifier:** if item 4 is skipped and item 2 ships, the next cross-machine wake breaks a
fifth instrument the same way, and this entry should be cited as the plan that said so.

---

## 2026-09-03 ~00:15 — LAP D004 MAP: the public-repo exposure (ASK-011). Guess not read.

**Baton, measured first:** D004 is on this ledger as `stage:open · entry:ring · holder: —` with no
chain row; `chain-status` and the guard still read **D003 · holder chair · 139h**. So the stale lap
now blocks a live one at this exact hop: this map is deliverable by `call_chair` only if the chair
writes D004's chain row with `--holder librarian` or files D003. Filed here as master regardless.

### Q1 — correcting a filed ask whose numbers are wrong: the mechanism EXISTS, the protocol does not name it, and the wrong number is in the one line that broadcasts

`ask.js:125-126,278` knows three states — ANSWERED, DECLINED, UNREADABLE. **No CORRECTED, no
AMENDED, no SUPERSEDED.** An ask whose figures are wrong is `OPEN` forever or cleared as if right.

But the room already has the form: **maintenance law 2's in-place provision** — a dated `>` block
beside the original, the original struck or left verbatim, used on `BOOT.md` (the 08-23 amendment),
`muscle_map.md` (the 23:10 strike), and this seat's own notes yesterday. And `ask.js` tolerates it:
ASK-013's annotations parse, because the parser reads only the three `**Field:**` lines and skips
everything else (`ask.js:103-112`). **So the procedure is: strike the wrong figure in place, dated,
beside the Question; leave Status OPEN.** No tool change needed.

**The absence, stated so it is not mistaken for closed:** ASK.md §5 requires the Question line
**verbatim**, and `--line` broadcasts that line to every seat every turn. **A wrong Question line is
the one line that cannot be corrected in place without breaking the protocol, and it is the one line
the room reads.** ASK-011 says 61 files; the count is 72, plus 52 for an account it does not mention.
Until a CORRECTED marker exists that `--line` honours, the channel repeats filed errors at full volume.
Registered here: **P-ASK-CORRECT** — a `> **CORRECTED YYYY-MM-DD** — <figure> → <figure>, <command>`
block that `--line` prefers over the original when present. One clause in the parser.

### Q2 — the 08-22 precedent is a DECISION, recorded with its grounds, and it does not cover this exposure

`loop/handoff_2026-08-22.md:94-97`, verbatim:
> `github.com/solariz3d/lighthouse` returns HTTP 200 unauthenticated, including `memory/user-solariz3d.md`
> and all 27 journals. **The keeper knows and it is not a privacy problem to him.** The consumer work is
> about quality of life, not concealment.

That is a decision, not an observation repeated: it carries a measurement (HTTP 200), names the
exposed objects, states the keeper's position, and **explicitly severs the consumer tree from
concealment.** Mention-vs-use resolved: the sentence is *used* as a ruling, not *mentioned* as a fact.

**But its scope is 08-22's exposure: `zackn`, the journals, his handle.** The chair's D004 measurement
adds two things the decision never saw: **`nname` in 52 tracked files — the desktop's account, absent
from ASK-011 and from the 08-22 ruling** — and the email in every commit's author field. A decision
that names its objects does not silently extend to objects it never named. **The precedent is the
right SHAPE (decide, record, name the grounds); it is not the right SCOPE.** Re-decide, naming
`nname` and the email, in the same form.

**And a carrier of a false premise, found in passing:** `consonance/tools/gen-consumer.js:2` reads
*"build the PUBLIC consonance tree from the PRIVATE lighthouse tree."* The lighthouse tree has been
public since at least 08-22 by the room's own measurement. The consumer tree was for receivability,
never concealment — the handoff says so — and yet on 08-23 this seat ran privacy scans on the
*generated* tree (coordinates in 16 files, `nname` in 9) as if the source were private. **The scan
was aimed at the artifact that did not need it while the source that did sat public.** Same class as
yesterday's `whats-live`: an instrument correct about the wrong universe.

### Q3 — the chair's central argument has its halves swapped; the recommendation survives for the opposite reason

The claim: *"filter-repo breaks every path:line citation the corpus runs on."* **False.** A history
rewrite changes commit shas; it does not renumber lines. A `--replace-text` substitution of an account
name keeps every line count. **The 1,942 path:line citations in `exo_memory/` survive a rewrite
intact** — the maps, the LEDGER's row cites, this file.

What a rewrite destroys is the other kind. Measured, not estimated:

    unique 7-hex tokens in exo_memory prose    438
    of which are REAL commits in this repo     418   (git rev-list --all, intersected)
    total prose mentions of those commits    1,610

**Every one of the 1,610 goes dead** — every `git show <sha>`, every "landed at `d4e7044`", every
LEDGER row keyed on a commit. That cost is real and it is the same order as the chair's number, so the
recommendation — *accept the exposure and record the decision* — **holds**. It holds because the corpus
is sha-anchored where it matters most (the record of what landed when), not because it is
path-anchored. The chair was right about the verdict and wrong about why, and the why is the part a
later reader would have re-derived and found broken.

**One thing that strengthens the verdict the chair did not say:** a rewrite retracts nothing already
cloned or already indexed by GitHub's own search, which has had a week. Concealment was unavailable
before the question was asked.

### Q4 — ABSENT

- **The instrument.** The chair's standing instruction — *scan public pushes for usernames* — was held
  for a week and run zero times across ten pushes. That is `muscle_map.md:1229`, stage 9, *read,
  understood, and inert*, on the seat that wrote the stage. **There is no push-time scan.** The
  secrets scan the chair ran tonight (sk-ant / ghp_ / AKIA / private keys / token values — all 0) was
  hand-run and exists in no file. **P-PUBLISH-SCAN**: a pre-push hook that greps the diff for the
  registered account names, the email, and the secret patterns, and refuses naming the file. The
  commit gate (`consonance/githooks/pre-commit`, built, unarmed) is the same shape one hop earlier.
- **A CORRECTED state for asks** (Q1) — the channel will accumulate wrong ones.
- **A decision that names `nname`.** The 08-22 ruling covers one account by name; the second has
  never been decided, only measured.
- **A mint for ask ids** (F-2W-3, yesterday) — still absent, and D004's own object is an ask.

**Not absent, and the chair should not rebuild it:** the decision form. `handoff_2026-08-22.md:94`
is the template — measurement, named objects, the keeper's word, the boundary of what it covers.

**Ledger: surfaced 5** (the in-place form applies to asks and the parser tolerates it; the 08-22
precedent is a decision of the right shape and wrong scope; the halves-swapped citation argument with
the honest sha count; `gen-consumer.js:2`'s false premise and the 08-23 scans aimed at the wrong
tree; the push-time scan as the absence that produced the lap).

## 2026-09-03 ~00:42 — D004 WORK-SHAPE, at the keeper's ask, after the chair found the map 19 minutes late

**What happened at the hop:** my D004 map landed as a commit (`cdcc4f6`) and a map row; my ring was
REFUSED by the guard (D003 · holder chair) and the refusal was posted to the board. The chair, in its
own words at 06:32Z: *"It did answer — 19 minutes ago, as a commit. I was waiting for a message and
never looked."* Three disk signals — the commit, the map row, the board row — and no watch running.
`handoff_librarian_2026-09-02.md` FIRST ACTS §1 is *re-arm the watch*; it was not. Stage 7 on the chair,
and the mechanism that refused the ring worked exactly as built.

**THE SHAPE, in order — bodies the chair's, decision the keeper's:**

0. **Chair, one line, before anything:** `lap-row.js --stage D004 dispatched --holder panes --to A,B`
   — or file D003 first. Until a chain row names D004, the guard reads a 140h lap and every hop refuses.
   Then re-arm `chain-status` on a loop and read the board on each poll; a refused ring is a board row.
1. **P-PUBLISH-SCAN** (pane A or B — the instrument that would have caught this a week ago): a
   pre-push hook that greps the pushed diff for registered account names (`zackn`, `nname`), the
   email, and the secret patterns the chair hand-ran tonight; refuses naming the file; prints its
   universe (files scanned / skipped / rule) per P-UNIVERSE. Red-first: a fixture diff carrying one
   name → refused; clean diff → allowed; mutant: drop one pattern → its fixture passes → red.
2. **P-ASK-CORRECT** (the other pane): one parser clause in `ask.js` — a dated `> **CORRECTED**` block
   beside a Question makes `--line` print the corrected figure; a block with no date is UNREADABLE.
   Plus the surplus-heading count (my silent drop). Red-first on ASK-011's own wrong "61". Then apply
   the in-place correction to ASK-011 (61→72; +`nname` 52 files) in the same commit.
3. **The carrier, one line, either pane:** `gen-consumer.js:2` "PRIVATE lighthouse tree" → "the
   lighthouse tree, which is public; the consumer tree is for receivability, not concealment
   (handoff_2026-08-22.md:94)".
4. **KEEPER:** the decision, in the 08-22 form — measurement, named objects (`zackn` 72 · `nname` 52 ·
   email in 2 files + author metadata), your word, the boundary of what it covers. Recorded as a
   `[ANSWERED …]` on ASK-011 once its figures are corrected, so the ask clears honestly.

**Not in the shape:** `filter-repo`. The map's Q3 priced it — 1,610 dead sha citations — and the
chair's verdict holds. **Disjoint:** 1 and 2 share no file; 3 is one line; 4 is prose.

**Return leg:** hand-backs to this seat by `call_librarian`; collated at the counter, once; then a
ring to the chair — which will be *allowed* only if the chair has written the return-leg row with
`--holder librarian`. The mechanism enforces the order the handoff describes; tonight it enforced it
against the chair's own habit, which is the design.

## 2026-09-03 ~00:47 — THE KEEPER OVERRIDES: remove zackn, nname, the email. The shape redrawn, with the full class measured.

Keeper, verbatim: *"should remove the zackn, nname, the email. Those are actual personal info
examples."* That is a direction call, his, and it overrides the chair's "accept" and my map's
"holds." Recorded as keeper→chair correction; the map's Q3 pricing stands as the cost of what he chose.

**The full class in the tracked tree, measured — the chair's count was three strings, the class is six:**

    zackn               73 files      (ASK-011 said 61; the chair said 72)
    nname               53 files      (the chair said 52)
    trynabemlgzn@…       2 files      + author field on 271 of 1,094 commits
    50.4452 (lat)        8 files      the keeper's coordinates — test fixtures + prose
    Regina              19 files      his city — some are the ambient config's own label; KEEPER decides
    DESKTOP-EEGVFMT      5 files      this machine's hostname — I put it in two of them
    solariz3d           63 files      his public handle and the repo URL — NOT a leak, leave it

**In history:** `zackn` in the content of 68 commits, `nname` in 53, the email as author on 271.
**`git filter-repo` is NOT installed** — a `pip install`, which is a new dependency and his to confirm.

### "Remove" is two operations, and the keeper is choosing between them whether or not it is said

**A — TREE REMOVAL, tonight, cheap.** Replace the strings in the ~140 files at HEAD, commit, push.
Substitution keeps line counts, so every path:line citation survives. **What it does:** the current
tree and GitHub's code search go clean. **What it does not do:** `git log -p` still returns every
name; the 271 author emails are untouched. Route the path replacements through `portable-paths.js`'s
conventions (`%USERPROFILE%`, `<user>`), not a blind sed — the record must still *say* what it said.

**B — HISTORY REWRITE, its own lap, a night.** `filter-repo --replace-text` (the six patterns)
`--mailmap` (the 271 author emails) → force-push → **both machines re-clone**. Every sha changes.
**The cost the map priced — 1,610 prose citations of 418 commits — is smaller than priced**, because
`filter-repo` emits a commit-map (old→new) and one mechanical pass over `exo_memory/` repairs every
citation. So B's true cost is: install · rewrite · force-push · re-clone ×2 · one citation-repair
pass built and red-first tested BEFORE the force-push · one repair commit. That is the only route by
which the email leaves the author fields at all.

**HARD PRECONDITIONS ON B, each one a way to lose work:** (1) both machines fully pushed and in sync —
this desk is `ahead 6` right now; the laptop's state is unknown from here; a force-push orphans any
unpushed commit on the other machine. (2) A human awake saying yes at the force-push
(`journal/2026-07-28.md:189`); this is the irreversible outward act the room's rule was written for.
(3) The citation-repair pass proven on a clone first — red-first: a fixture cite → repaired; a cite
to a commit not in the map → reported, never silently dropped.

**Recommendation, stated rather than laundered as a question:** **A tonight, B as the next lap.** A
removes what a stranger sees now; B removes what a stranger can dig for, and it needs the two
machines quiescent and the repair pass built first. Doing B tonight with the desktop ahead 6 and the
laptop unread is how the room loses a night's work to its own privacy fix.

**Two things only the keeper can say:** does `Regina` count (the city is in 19 files and in
`~/.consonance.json`'s ambient label, which the pulse prints every session-start)? And yes/no to
`pip install git-filter-repo` for B.

**The work-shape, revised:** item 4 (the decision) is now DECIDED — remove; **P-REMOVE-A** joins
tonight's packets (one pane, the six patterns, `portable-paths` conventions, red-first on a fixture
file, then the full-tree count printed as its universe); **P-PUBLISH-SCAN** (item 1) becomes the
guard that keeps A true; **P-REMOVE-B** is registered as the next lap with the three preconditions
as its bar. ASK-011's correction under P-ASK-CORRECT now reads 61→73 and adds the class.

---

## 2026-09-03 ~00:55 — THE CONSUMER BUILD: the keeper's reframe, mapped. Door two; the chair is locked out by D003.

**The keeper, verbatim:** *"it is okay to mention our city here with our dev version of consonance, and
perhaps more of my personal information on disk, but on the repo, it should be closer to a consumer
build where the user would start to make their own forked version of consonance and their own journals
and their own note within the symphony of the all … all the systems we have, will have to be setup to
be established by the new user themselves."*

**Said back in one sentence, so it can be corrected:** the dev version on disk stays his — city, name,
machines, journals; **the public repo stops being the dev version and becomes the thing a stranger
forks: the room and its instruments shipped empty, every system that is populated by him today created
by the new user on their first wake.**

### THIS REFRAME DISSOLVES TONIGHT'S B, AND REVERSES THE 08-22 DECISION IN THE RIGHT DIRECTION

If the public artifact is a **new repo generated from this one**, it has a fresh history by
construction. No `filter-repo`, no force-push, no re-clone, no 1,610 dead citations. `lighthouse` goes
**private** — one setting — and becomes what `gen-consumer.js:2` already calls it: *"the PRIVATE
lighthouse tree."* **The room built the right tool on 08-22 and then left the source public, which made
the tool look pointless.** The keeper's reframe makes it load-bearing. And the cheapest immediate fix
to the exposure the chair measured is not a scrub at all: **flip `lighthouse` to private tonight.**
Nothing pulls from it unauthenticated (check: the hooks read disk; `gh` is authed on both machines;
the consumer tree was never published). What is already cloned or cached stays cloned — same as every
option — but from tonight forward the public surface is the consumer repo or nothing.

### WHAT EXISTS, and how each attempt ended — read from the artifacts

- **`gen-consumer.js`** (08-22) — MANIFEST of **26 entries** (15 prose · 8 code · 2 config · 1
  binary), an EXCLUDE object, DANGLING/MACHINE rewrite rules, `--out <dir>`. **It writes a directory
  and stops** — no repo target was ever wired, and `gh repo list` shows no `consonance` repo exists.
  **How it ended:** its generated tree measured **31 green · 9 failed · 2 crashed of 43** on 08-23
  (`librarian/2026-08-23.md:81-94`) because the rewrite rules edited *test fixtures and one Rust
  assertion* — the generator broke the suite it shipped. Not dead; broken at the fixtures, never
  revisited.
- **The T0 shell for a stranger exists:** `brief/SEED.md` (9.6 KB) + `brief/BOOT.md` (39.6 KB, the
  generated public BOOT — nouns scrubbed to 0 hits for name/city/handle). **One carrier:** the scrub
  missed **5 pronouns** (`him/his/he`) — line 3 reads *"written from inside **their** context … If
  you're waking here and you are not **him**"*. Small, real, and exactly the class the room names.
- **The transmission test** (`loop/transmission_preregistration.md`, 08-11): *does the repo hand a
  DISPOSITION to an agent that reads it?* — **the consumer question, verbatim.** Run VOID 08-16 (bait
  too weak); the recalibration was spent; **never re-run.** It is the registered instrument for
  whether a fork carries the room, and it has no result.
- **COLDREAD** (`journal/2026-07-31.md:35`, three strangers): the only external-reader data the room
  holds. A stranger called the stream *"arithmetic wearing perception language."* n=3, one night.
- **The Third Place's T0 condition** (L006, `librarian/2026-08-25.md` ~03:20): a seat that wakes
  carrying the dynamics and none of the record — **the same cut, applied to a seat.** And its
  measured trap (`2026-08-25.desktop.md` ~14:15): **the seeded cards cite the program** —
  `cards/no-floor-no-ceiling.md`, `record/trust-the-first-attention.md` name `muscle_map`. A consumer
  build that ships the cards ships pointers into a record the fork will not contain. That is the
  DANGLING rule's whole job, and it is what broke the fixtures.
- **`desktop_first_run_2026-08-25.md`** — the nearest thing to *a stranger installs it*, and it found
  nine Rust reds from two hardcoded repo paths. A consumer install is that, on a machine nobody here
  has seen.
- **`research/the_retrieval_problem_outside.md`** (278 lines, indexed, unread by this seat) — may bear
  on *"their own notes within the symphony."* Named so it is opened, not assumed.

### WHAT A NEW USER MUST ESTABLISH — the keeper-populated inventory, measured on this machine

| system | today | consumer build |
|---|---|---|
| `~/.consonance.json` | 12 keys, all his (room_path, data_dir, instances_dir, ambient_lat/lon/label/tz, dream_model, dream_times, base, flags, instances) | **first-run creates it** — asks the six that are the user's, defaults the rest |
| `C:\Consonance\data\` | ~50 files: `letters.json`, `lap.jsonl`, `board.jsonl`, `panes.json`, 27 mcp token files, ledgers | **created empty on first launch**; letters minted from A on their machine |
| `exo_memory/ASK.md` | 12 of his asks | ships with the **protocol and zero asks** |
| `exo_memory/` — the SPLIT | room + his trace, one tree | **room ships** (BOOT · SOURCE · cards · the loop briefs); **trace does not** (journal · loop · map · librarian · record · memory · spread? · research?) |
| hooks | 37 manifest entries via `install.ps1`, drifted per-machine | `install.ps1` is already the mechanism; ship it, run it on first launch |
| duration goals | 5 on this machine, each with his `goal.json` | **templates**, not instances; the user instantiates |
| dream cycle | his dirs, his 04:30 | config-driven already (`dream_times`); needs the user's dirs |
| the librarian's notes, M.md, LEDGER, DOSSIER | his seat's inheritance | **empty on day one** — the new user's librarian writes its own `2026-…md` from its first wake |

### ABSENT — and two of these are the real work

- **No RULE for the split.** `gen-consumer`'s MANIFEST is 26 hand-picked entries. A fork needs a
  *rule* — a predicate over paths that says room or trace — because the tree grows nightly and a list
  rots. **And the boundary is contested in two places:** `spread/` and `research/` are outputs of his
  nights *and* instruments BOOT points at; `cards/` are the room's instruments *and* cite his record.
  The rule has to be written, registered with a falsifier, and attacked before the generator is
  re-pointed at it.
- **No first-run.** Nothing creates `~/.consonance.json`, `letters.json`, an empty `ASK.md`, the
  instance dirs. A new user today gets a Rust panic at the first `room_path` read.
- **No fork target.** No `consonance` repo exists; `gen-consumer` has no push step.
- **No re-run of the transmission test** — the one instrument built for this exact question.
- **"The symphony of the all."** The keeper's phrase implies the forks are *in* something together.
  A fork as built is one-directional: they receive the room; the room never hears them. Whether a
  fork ever talks back is **T4 / exteroception** (`LEDGER.md:148`, keeper's option call PENDING
  since 08-25) and **P-SIGNAL-BACK** (L008 map, registered only). **Named as absent and not
  invented here** — it is a design question, his, and it decides whether this is a distribution or a
  network.

### THE SHAPE — registration first, because the generator already broke once on an undecided split

**P-CONSUMER-REG** (one pane, adversarial non-author reads it): the split rule as a path predicate;
the first-run's six user questions; the fork-target repo name and the push step; the falsifier — **a
stranger, on a machine nobody here has seen, installs from the public repo alone and the four wake
proofs pass** (`desktop_first_run` generalised, scored by the stranger's printed output, never by us).
State in advance what would mark it degenerating: a second hand-maintained tree; a fixture rewritten
by the generator; a fork that needs a file from this repo to boot.

**Then, in order:** (1) `lighthouse` → private, tonight, one setting — and `gen-consumer.js:2` becomes
true instead of a carrier. (2) The generator re-pointed at the rule, with the 08-23 fixture-rewrite
failure as its first red-first test. (3) First-run. (4) The transmission test re-run against the
generated repo with a stronger bait. (5) The pronoun scrub — five words — in the same commit as (2).

**Not in this shape:** any scrub of *this* repo's history. Private + generated makes it unnecessary.
**Tonight's A is now:** flip to private. **Tonight's B is dissolved.**

**The dependency the keeper should see:** the split rule *is* the T0 question the Third Place already
failed on 08-25 — the cards cite the record. Solve it once here and the Third Place's intake gets the
same rule for free. Two open problems, one predicate.

**Ledger: surfaced 6** (private-plus-generated dissolves the rewrite; the generator's 08-23 break as
the first test; the pronoun carrier in the public BOOT; the keeper-populated inventory as a table; the
split rule as the shared root with the Third Place's T0; the symphony question named as T4, not
invented).

# 2026-09-09 — the DESKTOP librarian's first wake. THE MIGRATE RAN, AND THIS SEAT WOKE BLANK.

*Per-machine file (`P-TWO-WRITERS`). The laptop's master for today is `librarian/2026-09-09.md`;
this file is the desktop leg and starts at the first migrate in the world.*

## 09:00 — the keeper: "And?" — one word into this pane, an hour after the launch he expected at 08:00.

**Where the machine is, all re-derived here, nothing taken from a ring:**

- `hostname` DESKTOP-EEGVFMT, `CONSONANCE_MACHINE=D`. This is the desktop.
- Seam row, board `14:59:06Z`: **MIGRATE** — "the record's head was authored by L, not by this machine (D) …
  RETIRED 3 transcript(s), 0 seat(s) had none here". The arm that had never executed anywhere executed.
- `sync-pull.log`: `COMPLETE — 47 of 47 files present, right length, right bytes.` then
  **`installed 46 file(s)`**, 16 displaced to `data/attic/pre-sync-2026-09-09T14-59-05-515Z`.
- State remote `a4cb9fe`, `machines/` holds **`L.json` only** — the desktop has not pushed; `in sync: L <sha>`
  is the correct reading here, exactly as B's runbook corrected it (WRONG 91, laptop).
- Record repo head `51ac75f` — CHAIR (desktop): the first click opened the wrong house, a BOM in
  `~/.consonance.json`; repaired, runbook step 1 corrected by append. So the 08:00 → 08:59 hour is accounted for.

## THE FINDING — 46 of 47, and the one that did not arrive was this seat's own tail

`for p in $(git ls-tree -r origin/main --name-only | grep ^data/); do [ -f /c/Consonance/$p ] || echo MISSING: $p; done`
→ exactly one line:

    MISSING: data/captures/0c0c0c0b-0000-4000-8000-00000000115b.txt

**It arrived and was then taken away, in that order, by this machine.** persist.log, verbatim, in sequence:

    1788965944 SYNC AT LAUNCH — the record's head is not this machine's; installing
    1788965946 SYNC AT LAUNCH MIGRATE — … retiring them and waking each seat from the synced tail.
    1788965946 retire pane=0c0c0c0b-0000-4000-8000-00000000115b -> ARCHIVED (had history)
    1788966000 MIGRATE TAIL sid=0c0c0c0a-0000-4000-8000-000000000a01 tail=4745225 carried=16347 …

There is no `MIGRATE TAIL` row for `0c0c0c0b`. There is no `POINTER ONLY` row either.

**Proof it was the LAPTOP's tail that got archived, not this machine's own:**
`sha256` of the local `data/captures/archive/0c0c0c0b….txt` = `8184ad81…` = the state tree's **live**
blob (`data/captures/0c0c0c0b….txt`, 7,069 B). The state tree's own **archive** blob is `aee4ca3b…`,
92,499 B — the desktop's copy of it no longer exists on disk. So the rename both **removed the tail the
seat was to wake from** and **overwrote the synced archive copy with it**. Both are recoverable by
`git show origin/main:…` from `C:\Consonance\state`; nothing is lost, and nothing was noticed.

**The mechanism, at the file, three call sites:**

- `consonance/src-tauri/src/main.rs:857-870` — `gc_captures()`: *"startup sweep: retire captures for panes
  that are no longer kept (and aren't Main). read_kept() is truth."* `keep = read_kept() ∪ {MAIN_SID}`.
  **The librarian is a fixed-id seat and is in neither set**, so its capture is retired on every startup —
  harmless when the seat resumes its own transcript, fatal on the one launch where that file *is* the carrier.
- `main.rs:828-842` — `retire_capture()` renames `captures/<sid>.txt` → `captures/archive/<sid>.txt`.
- `main.rs:9127-9131` — `append_synced_tail()`: `let Ok(transcript) = fs::read_to_string(&path) else { return };`
  **A missing file returns silently.** The POINTER-ONLY fallback at `:9156-9164` — the branch whose own comment
  reads *"A seat told nothing would wake believing it has no past. Absent-is-silent is the failure this room
  keeps paying for"* — is one branch further down and never runs. The chair survives for one reason only:
  `keep.insert(MAIN_SID)`.

**So the seam row's sentence — "waking each seat from the synced tail" — is true of the chair and false of
this seat, and nothing in the launch says so.** `installed 46 file(s)` does not name the one; `--verify` says
COMPLETE because the *tree* is complete; the seam row counts retired transcripts, not delivered tails.

**What actually carried this seat across:** `map/M.md`, `librarian/2026-09-09.md` and
`loop/handoff_librarian_2026-09-09.md`, read from disk in the first four minutes. §8's claim — *the record is
the carrier* — held, and it held **because the other carrier failed**, which is the only way that claim could
have been tested. The design's fallback is the thing that worked; the design's primary path did not.

**Owed, in order (a packet each, not built here):**
1. `gc_captures()` must keep `LIBRARIAN_SID` (and `THIRD_PLACE_SID`) as it keeps `MAIN_SID`; the sweep must
   not run before the migrate's tail read in any case — a file the launcher just installed is not a leftover.
2. `append_synced_tail()`'s missing-file arm must plog and take the same POINTER-ONLY path as its empty arm.
   *A carrier that fails silently is the same defect as a hold that is a sentence.*
3. `state-sync --install` should print, or `sync-completion.json` record, **which** file of the set is not on
   disk after the sweep — `46 of 47` with no name is a count nobody can act on.
4. Score the rest of the migration against B's failure table (`loop/desktop_first_launch_2026-09-09.md`)
   before any desktop work: the seats' cwds in `panes.json`, the `seed_*` second root (`~/.consonance`,
   written 08:45-08:59 today), and the close-push leg from D.

Ledger: **surfaced 1** (this). WRONG column, this machine: **0** — the laptop's 94 does not travel; a WRONG
is a claim made *here*.

## 09:20 — E's P-RUNTIME-SCORE (D055) read at the file. IT LANDS. One item it left open is closed by citation; the fix to my 09:00 finding is in the working tree, uncommitted, and is being built correctly.

Pointer verified before reading: `sha256sum handback/p-runtime-score_2026-09-09.md` = `054edc94…3fb8`,
13,185 B — matches the ring. Read at source, not at the ring.

**Re-derived at this desk, E's four load-bearing claims, all hold:**

- `git diff --name-only ce9afed HEAD -- consonance/src-tauri/` → **empty**. ✓
- `git log`: `7d94177` 09:05:44, `51ac75f` 08:53:09, `ce9afed` 08:37:59. ✓
- `consonance.exe` mtime **08:45:32**, 14,848,512 B — after `ce9afed`, before the other two. ✓
- The four `panes.json` cwds (`sibling-3d57124e / 5bf9d657 / 0845a868 / 07b8a48f`): none exists under
  `C:\Consonance\instances`, and **no matching project directory has ever existed** under
  `~/.claude/projects`. ✓ E's stronger form is right: these are not directories that went missing,
  they are values that never corresponded to a process.

**E's central correction is an add and I take it:** *stop saying "the binary is built from HEAD"; say
"`src-tauri/` is byte-identical to HEAD's", which is what was actually checked.* Today's safety is a
coincidence of what two markdown commits happened to touch, and nothing in the launch would announce
the first Rust commit that lands without a rebuild. The binary carries no commit stamp — E searched all
14,848,512 bytes for four short shas and found none — so every `main.rs` reading in this room is tied to
the artifact by **timestamp bracketing plus proof-of-source-identity, and E says so rather than implying
a mark that is not there.**

**THE ONE E LEFT OPEN, CLOSED BY CITATION — the unmounted pane is BY DESIGN, not loose.** E named the
`instances\third-place` process running with no `--mcp-config` as *"the loosest thing I found… I do not
know what it is."* It is documented in the function E did not read: `main.rs:6510-6516`, the doc comment
on `spawn_third_place`, which names all three differences from `spawn_librarian` as the point — *no role
in `ADDRESSABLE_SEATS` so `chair_target_guard` refuses it the way it refuses a person; no name in
`PaneNames` so nothing can resolve a target to it; **no MCP mount — `spawn_claude_pane` skips the config
for this SID***. The gate is `main.rs:999`: `… && pane_id != THIRD_PLACE_SID`. Unaddressable and
unattributed is the requirement, not a gap. *This is the seat's job: E measured correctly and read a
deliberate design as a hole because the design lives in a doc comment nothing points at.*

**THE FIX TO MY 09:00 FINDING IS IN FLIGHT — uncommitted, and right so far.** `git status` shows
`M consonance/src-tauri/src/main.rs`, +161/−3, no commit. In it:

- `gc_captures()` now inserts **`MAIN_SID`, `LIBRARIAN_SID` and `THIRD_PLACE_SID`**, and its doc comment
  carries the measurement — *"THE THREE FIXED SEATS ARE NEVER IN `panes.json`, AND ONLY MAIN WAS SPARED."*
- `append_synced_tail()`'s missing-file arm is now `Err(e) => { plog(…)` and takes the POINTER-ONLY path.
- Two tests written to fail for the right reason: `the_startup_sweep_keeps_every_fixed_id_seat_not_only_main`
  (`:9248`) seeds every fixed seat **and a stranger**, and asserts the stranger IS retired — *"a sweep that
  keeps everything satisfies the assertions above for the wrong reason"*; and
  `a_synced_tail_that_is_not_on_disk_points_the_seat_at_it_instead_of_waking_it_blank`.

**Not a discrepancy, recorded so nobody finds one later:** that new comment hashes the archived tail as
`a10d1d0e`; I published `8184ad81` at 09:00. Same bytes, different function — `git hash-object` gives
`a10d1d0e57e277c3…`, `sha256sum` gives `8184ad81edc31f63…`, `sha1sum` gives `8726773b…`. Both readings
are of `data/captures/archive/0c0c0c0b….txt`.

**Observed and self-resolved, kept because it bears on E's Q1:** at ~09:22 `gc_captures()` opened with
`if true { return; }` — the function dead. On the next read, seconds later, it was gone; a grep for that
line returns no hits. A mutation run in flight, and the stranger assertion above is exactly what catches
that mutant. **The caution is E's Q1 inverted:** a rebuild takes whatever is on disk in that second, so a
rebuild launched during a live mutation run can ship a mutant that no commit ever contained. E's rule and
this one are the same rule — *the artifact is not the source, in either direction.*

**Also confirmed against E's Q2, since it touches my own finding:** the Third Place was **not** swept at the
migrate — no `3d000000` retire row at `1788965946`, and its archive copies are dated Sep 6 12:42/12:48 from
an earlier sweep, its live `.txt` fresh at 09:04. It escaped because it had no `captures/*.txt` to retire at
08:59:06 (its record never travels), not because the keep-set spared it. **The librarian was the only seat
that lost anything today**, and the keep-set fix above is what makes that structural rather than lucky.

**Owed to E, not settled here:** E flags a finding that will disagree with B's runbook-side score of the same
`panes.json` row and says the disagreement is intended and unreconciled. It is also sitting unsurfaced in
the ledger — `vantage_findings.jsonl` row `3898181859b2c2c6`, *held-unaudited*. **That row is the chair's to
open, not mine to resolve; a disagreement left standing is the point.** Named here so it is not lost.

Ledger: surfaced 2 (the third-place design citation; the mutation-run/rebuild caution).
WRONG column, this machine: **0**.

## 09:35 — C's P-SEAT-SWEEP (D055) read at the file. THE REFUSAL IS RIGHT AND I TAKE IT. Its §5 "pre-existing, not diagnosed" red IS DIAGNOSED HERE, and it is a second carrier silently changing bytes between the two machines.

`handback/p-seat-sweep_2026-09-09.md`, 13,757 B, 215 lines. Read §6 first as C asked.

### The refusal — accepted, and its first reason is the one that matters

I asked for the stronger form: *the sweep must not run before the migrate's tail read.* C refuses and is
right. **It is not a race that can be ordered away.** `gc_captures()` is a line in `.setup()`; the tail
read lives inside `spawn_librarian` / `spawn_third_place` / `spawn_main`, which are `#[tauri::command]`s
the **frontend** invokes after the event loop is up. The measured gap here was 59 s and it is *unbounded* —
a seat the keeper never opens never reads its tail at all. So "do not sweep before the tail read" has no
implementation as a reorder; it becomes *never sweep*, or a new lifecycle for a function whose current
virtue is running exactly once at a known point. **My ask assumed a sequence inside one startup path, and
there is no such path.** That is an add, it survives, and I withdraw the ordering item from my 09:00 list.

**One half of C's reason 2 I can settle, and it does not change the verdict.** C declines to rest a
freshness test on mtime because it has not established that `installTree` writes a fresh mtime rather than
preserving the source's. It does: `consonance/tools/state-sync.js` installs with
`fs.writeFileSync(dst, fs.readFileSync(src))` — a plain write, so the mtime is the install's. I read that
function at 09:05 for a different reason. **C's caution was still correct in kind** — it refused to land a
guard resting on an unmeasured property of a file it does not own, which is the right instinct — and
**reason 1 is sufficient on its own**, so the refusal stands either way.

**C's own proposal beats my ask and I endorse it for a later lap:** have `sync_at_launch` return the set of
paths `--install` actually wrote and hand that to `gc_captures` as an extra keep-set. It protects *any*
travelled capture — including a committee pane's, which the keep-set fix does **not** cover — and it rests
on a fact the installer already holds. It touches `sync_launch.rs`, so it is not a one-file lap.

### The hashes: C says two of mine are wrong. They are not, and here is the command

C's §2 states *"the librarian's hand-back quoted `8184ad81` and `aee4ca3b`; both are wrong."* They are the
**sha256** of the same two objects C hashed with `git hash-object`, which is a sha1 over `blob <len>\0` +
content — a different function, not a different file. One run, all four forms of the archived tail:

    sha256sum        8184ad81edc31f63…
    sha1sum          8726773bd1775739…
    git hash-object  a10d1d0e57e277c3…
    md5sum           32b8fc5a812818cd…

and both of us report the tree's archive blob at **92,499 B** (`aee4ca3b…` sha256 = `ed9c3577…` git blob).
Same bytes, same conclusion, two hash functions. **I had already recorded this at 09:20 before C's file
existed** — see the entry above. Nothing to propagate in either direction; the note is only so that a later
reader finds the reconciliation instead of a contradiction between two hand-backs.

### §5 — THE RED C DID NOT DIAGNOSE. THE FIXTURE IN THE REPO IS NOT THE FIXTURE ANYONE MEASURED.

C reports `ready_signal_tests::a_slash_command_in_the_composer_reads_empty_and_this_is_the_defect` failing
at HEAD with `premise: the anchor finds the composer`, notes L053 recorded **513/0/4** where this desk now
gets **512/1/4**, and rules it out as CRLF corruption because disk (236,387) equals the HEAD blob (236,387).
**That check proves the checkout is faithful. It cannot see damage done on the way IN.**

- `.gitattributes:31` — `* text=auto eol=lf`. The binary exemptions are an **explicit list**:
  `*.png *.bmp *.ico *.exe *.dll *.pdf *.zip *.woff *.woff2 *.ttf`. **`*.bin` is not in it.** The file's own
  comment says the list is explicit "because a heuristic that guesses right today is not a decision" — it
  was written 2026-08-25, and the first `.bin` fixture in this repo was born 2026-09-09.
- `git check-attr -a` on the fixture → **`text: auto`, `eol: lf`**.
- The fixture contains **0 NUL bytes**, so git's content heuristic classes it as TEXT and converts it.
- On disk now: **CR = 0**, LF = 164, CRLF = 0 — in a raw terminal capture, which is an ANSI stream.
- **Both sizes are recorded in the repo by their own author, from the laptop, before the migration:**
  `handback/p-composer-anchor_2026-09-09.md:153` — *"236,544 bytes"*; and
  `handback/p-composer-update_2026-09-09.md:162` — the other fixture at *"524,288 bytes"*.
  On this disk they are **236,387** and **524,204**. Deficits of **157** and **84** bytes.
- 524,288 is exactly 2^19 — a capture buffer cap. A regenerated capture does not land on 2^19 by accident;
  **the un-stripped original is the one that does.**
- Commit `e3a9b10` (07:03:59, laptop) carries 236,387 — while the laptop's working tree still read 236,544
  at 07:40, when I scanned it. **Normalised on `git add`, original left in the working tree.** That is why
  every L053 test was green on L and this one is red on D: the tests there ran against the file on disk;
  the desktop checked out the stripped blob, and with the CRs gone the anchor cannot find a composer row.

**So the machine-dependence C measured is explained, and the damage is not confined to the failing test:
BOTH `.bin` fixtures are short. The second one's test passing does not mean its fixture is intact** — it
means nothing currently reads the 84 bytes.

**The originals cannot be recovered from git; the CRs were destroyed on ingest.** They exist in exactly one
place: **the laptop's working tree.** So, time-sensitive and belonging in the Sunday runbook before any
`git pull` or checkout runs there:

1. On L, before anything touches the tree: copy both `consonance/src-tauri/fixtures/screens/*.bin` out to
   a scratch path and record `wc -c` for each. If they read 236,544 and 524,288, this diagnosis is
   confirmed by the artifact itself.
2. Add `*.bin binary` to `.gitattributes` (better: `binary` on the fixtures directory, so the next capture
   format is covered without anyone remembering to list it) — **the fix must land before the good bytes are
   re-added, or they will be stripped again on the way in.**
3. Re-add the two fixtures from the preserved copies, and re-run the suite on **both** machines.

**Its own falsifier, registered before anyone acts on it:** if the laptop's working copies turn out to be
byte-identical to the blobs (236,387 / 524,204), then the two recorded sizes were of regenerated captures
that were never committed, my diagnosis is wrong, and the red is something else. **One `wc -c` on L settles
it**, and nobody should re-capture anything until it has been run.

*Kept as the shape rather than the instance:* this is the same failure as the 09:00 one, one carrier over.
A file crossed between machines and arrived changed, and every instrument in between reported success —
`--verify` said COMPLETE, `git status` says clean, disk equals blob. **Both times the tell was a number
recorded on the other side.** The room's habit of writing the byte count beside the file is what made two
silent corruptions visible in one morning; without those two figures in C's own hand-backs there would be
nothing here to see.

### What I did not do

I did not re-run the suite (C's 512/1/4 and 515/1/4 are C's numbers with C's command beside them, and the
tree is C's this lap), did not touch `main.rs`, and did not act on §7's un-run acceptance test — that needs
a relaunch and the keeper is away. C's §7 third item (an *empty* tail also returns without a row) is left
open by C for a real reason and I agree with the reasoning; it is the chair's call, not this seat's.

Ledger: surfaced 2 (the fixture normalisation with its falsifier; the mtime half of C's reason 2).
WRONG column, this machine: **0** — and C's §2 charge against two of my hashes is answered above, not
carried, because it was a difference of hash function and not of fact.

## 09:50 — B's P-DESKTOP-TABLE (D055) read at the file. IT LANDS, and its §1 has a cause: THE MIGRATION DID IT, and the control was sitting in the attic the install itself created.

`handback/p-desktop-table_2026-09-09.md`, 21,844 B. Every load-bearing claim re-derived here.

### Re-derived, all hold

- `persist.log:316-326` — four `MAP CARRY … carried=4434 / 7113 / 4424 / 6035` each followed by
  `resume … warmed=false jsonl_existed=false -> fresh`. Four maps built, four discarded.
- `C:\Users\nname\CLAUDE.md` — **does not exist.** A correctly seated pane's map
  (`instances\sibling-181f513d\CLAUDE.md`) is **123,162 B**; what these four actually read is the
  global `~\.claude\CLAUDE.md`, **7,574 B, dated Jun 28**. B's sharpening of the chair's "a different
  CLAUDE.md and no map" is right: no CLAUDE.md is written at all.
- `letters.json`, the two roots: real = `0c0c0c0a→D`, `0c0c0c0b→M`; other = `0c0c0c0a→A`,
  `0c0c0c0b→B`. B's trap is real — a naive carry re-attributes 35 rows to two live committee seats.
- `portable-pty-0.8.1/src/cmdbuilder.rs:560-567`, at the pinned source: `.filter(|path|
  Path::new(path).is_dir())` on both, then `cwd.or(home)`. A requested cwd that is not a directory is
  discarded in silence and replaced by `%USERPROFILE%`. B read it correctly.

### THE ADD — B says the four cwds are absent. They are absent *because the sync installed them*, and the proof is the file the install displaced

`data/attic/pre-sync-2026-09-09T14-59-05-515Z/panes.json` is **this machine's own roster**, moved
aside at 08:59:05 to make room for L's. It names five panes:

    EXISTS  C:\Consonance\instances\sibling-906f757a
    EXISTS  C:\Consonance\instances\sibling-a80a1c20
    EXISTS  C:\Consonance\instances\sibling-afa12c33
    EXISTS  C:\Consonance\instances\sibling-eeb329ed
    EXISTS  C:\Consonance\instances\sibling-181f513d

**Five for five present.** The roster that replaced it names four, **zero of four present**. So this
is not a stale file, a drifted config, or a machine that was never set up: **`panes.json` is
classified TRAVELS (`state-manifest.json:21`), it carries absolute per-machine paths, and the
migration overwrote a correct local roster with a foreign one.** `portable-pty` then rehomed all four
seats to `%USERPROFILE%` without a word. **Every migrate does this, in both directions, to every
committee pane** — Sunday's pull will do it to the laptop with this machine's paths.

**And the manifest's own precondition is the near-miss B already self-flagged, now measured:** :21
says it travels correctly *"only if both machines resolve the same `instances_dir`."* Both machines
**do** resolve the same `instances_dir` — `C:\Consonance\instances` — so the prescribed check passes
and the failure happens anyway, because what is machine-bound is not the parent, it is the
`sibling-<id>` directories inside it, which are minted per machine. B called this *"a prescribed
check that cannot fail, which is worse than no check because it is read as clearance."* Correct, and
this is the instance.

**The remedy is cheap and is already half-written in the manifest:** :21 itself says *"if the dirs
differ this entry is wrong and the file needs a rewrite-on-arrival, not a copy."* That is the fix —
and until it exists, **the correct roster for this machine is not lost: it is in the attic path
above.** *(Not restored by me. Restoring it mid-session while the app holds the four panes open is a
change to a live house, and it is the chair's call with the keeper away.)*

*Checked and NOT claimed:* the room's MACHINE-BOUND class (`loop/machine_bound_class_2026-08-25.md`)
is about test suites that cannot run off their home machine, not about the manifest's classification
of data files. It does not already cover this, and saying it did would have been a false citation.

### §2 — the second root: confirmed live, with one fact B did not have, and one falsifier of my own

Both logs stamped **09:19:07**; one app process, `plog()` with one destination. Confirmed. Two
additions:

1. **The second destination opened DURING the session.** At the migrate second (`1788965946`) the
   four `DECK ORDER` rows went to the real root **only** — `~/.consonance/persist.log` has nothing at
   that epoch. Its next rows are `1788966884` (**09:14:44**), then 09:17:56 and 09:19:07. So this is
   **not** the startup window before `set_dirs` that C found in L049/L051, and it is not the known
   `seed_*`-writes-the-default-root item either; something changed a resolution ~15 minutes after
   launch. That narrows the search and rules out the room's two existing explanations.
2. **The rows are doubled** — two identical `DECK ORDER` lines per epoch in *each* file, four writes
   per event, where the migrate's own were one per pane. Whatever is writing twice is writing twice
   into both.

**And I ran this against my own 09:00 finding before anything else, because a split log could have
hidden the row I said was missing:** `~/.consonance/persist.log` contains **no** `MIGRATE TAIL`, **no**
`retire pane=0c0c0c0b…`, and **no** `POINTER ONLY` row. My evidence stands; had it been there, the
finding would have been wrong and I would have had to say so.

### §3 — the refusal to close is right, and the reason is stronger than B could state

B refuses to run `close.js` partly because `panes.json` is TRAVELS and its precondition is falsified.
With the attic control above, that is no longer a suspicion: **the roster on this machine is known
wrong, and a close would push it to the shared remote stamped as D-confirmed** — after which Sunday's
laptop pull installs D's paths over L's working roster and mis-seats that machine too. The runbook's
§9 warning stands and should reach the keeper in his own words: **do not open and work in the laptop
until D has pushed** — and D should not push until the roster question is decided.

### The WRONG column — B asks me to number four entries, and the ledger cannot take them as they stand

B is right that the ledger is this seat's to keep, and the four entries in its §6 are honest, specific
and correctly classed. **But the running counter is not the right home for them.** The sequence that
reached **94** on the laptop counts *this seat's own wrong claims* — 90 and 91 were my plan's, 92 my
relayed premise, 93 my scorer, 94 my "you are idle." Folding another seat's errors into it silently
changes what the number measures, which is the failure the room keeps finding: *a counter whose
population changes is worse than two counters.*

So, decided and stated rather than merged:

- **The librarian's own-claims counter stays intact and unmerged.** Laptop 94; this machine 0.
- **A room WRONG ledger, keyed `<lap>-<seat>-<n>`, takes everyone's.** B's four:
  - **D055-B-01** — §2 step 1: asserted the BOM was safe because "both readers strip it", having
    checked two and generalised to the third. `parse_config` (`main.rs:98`) does not. *Class:
    checked-wrong claim in the reassuring direction.* Cost: the first click opened the wrong house.
    (Already taken by the chair at `51ac75f`; recorded once here so the trace is in one place.)
  - **D055-B-02** — §11 item 3 and `state-manifest.json:21`, the same sentence: right hazard, wrong
    variable; the prescribed check passes and misses the failure it was written for. **Measured today
    by the attic control above.**
  - **D055-B-03** — §10 as a whole: a failure table keyed entirely on the seam verdict, when both of
    this morning's real faults are invisible to the seam row. *Class: category error — the launch
    verdict treated as the state of the house.*
  - **D055-B-04** — §10 STANDALONE and no-row rows: a likeliest cause stated from reading the code
    with the confidence of a measured one; both fired and both causes were wrong.
- **D055-C-01** — C's P-SEAT-SWEEP §2, *"the librarian quoted `8184ad81` and `aee4ca3b`; both are
  wrong."* They are sha256 of the objects C hashed with `git hash-object`. Same bytes, same
  conclusion, different function. Entered because the ledger must be able to hold anyone's, or it is
  a mirror; C's file is otherwise the strongest of the three.
- **E: no entry.** E's "I do not know what the unmounted pane is" was a stated limit, not a false
  claim, and a limit correctly named is not a wrong.

*This is a change to how the room counts, so it is registered rather than assumed: if the chair or the
keeper wants one sequence, say so and I will renumber — but not silently, and not by absorbing three
seats' errors into a number that has meant one seat's for ninety-four entries.*

### Closing B's third item

`main.rs:860-865` is mine (09:00) and is **held and fixed**: C took it this lap — all three fixed
seats now in the sweep's keep-set, the absent-tail arm points instead of returning silently, mutation
battery 6 applied / 6 caught / 0 survivors, uncommitted in the working tree. Its §6 refused my
ordering ask and I accepted the refusal. B does not need to carry it.

Ledger: surfaced 3 (the attic control and the migrate as cause; the second root opening mid-session;
the ledger split). WRONG column, this machine: **0**.

## 10:00 — A's P-INSTALL-NAMES (L055) read at the file. IT LANDS, and its §1 CORRECTS MY 09:00 HEADLINE. I take the WRONG. Its §9 item to C is itself wrong, and it is the same error C made at me, mirrored — which makes it a room rule, not a scold.

`handback/p-install-names_2026-09-09.md`, sha256 `ee692d62…bac5` verified before opening, 405 lines.
A asked for §1 and §5 first; read in that order.

### §1 — MY WRONG. `46 of 47` was never a shortfall, and I published it as one

A re-derives it and is right, and the arithmetic closes here:

    git ls-tree -r origin/main --name-only | wc -l          -> 50   (47 under data/, plus
    git ls-tree -r origin/main --name-only | grep -c '^data/' -> 47   .gitattributes, machines/L.json, state-set.json)

`installTree` has two arms and no third: write, or `continue` on an exact sha match. `wrote` counts
only writes. So **47 index entries = 46 written + 1 skipped-as-identical, and everything was on disk
when `state-sync` exited.** The file I found missing at 09:00 was missing because `gc_captures()`
renamed it away *afterwards*, not because it never landed.

**WRONG — D055-M-01 (mine, this machine's first).** My 09:00 entry is headed *"46 of 47, and the one
that did not arrive was this seat's own tail"* and my ring to the chair said *"the single file that is
not on disk"* — while the same entry's next sentence reads **"It arrived and was then taken away, in
that order."** I proved arrival by hash and then led with a shortfall reading of a counter I had not
established was counting arrivals. *Class: two true facts joined by an unchecked causal claim, in the
direction that made the headline sharper.* The mechanism I published — sweep archives the installed
tail, `append_synced_tail` returns silently — is untouched and was reached by other means. **The
number was not merely un-actionable; it pointed the wrong way, and I pointed with it.**

My third owed item survives with its reason replaced: not *"name the file that is missing"* but
**"distinguish a benign skip from an absence"** — which is what A built. `46 + 1 already identical =
47 reconciled` closes, and the reader can tell the two apart.

### §5 — A proves its own change could not have caught this morning, and puts it at the top

Re-derived at the source: `main.rs:9515 sync_at_launch()` → `main.rs:9552 gc_captures()`, 37 lines
later in the same function; `run_state_pull` (`:8952`) spawns and waits, so `state-sync` has exited
before the sweep runs; and `persist.log` line order (307 install → 308 MIGRATE → 309-312 the four
retires) says the same from the other side. **A reading taken inside a process cannot cover what
happens after that process ends**, and A states that before the work rather than after it. The two
things that *can* cover it are the check at the point of use (C's, landed) and a journal that
survives the process (§3, not taken). This is a seat declining the credit its own packet was written
to earn, with the proof attached.

### §9 TO C — A IS WRONG, AND IT IS THE MIRROR OF C's WRONG AT ME

A reports `main.rs:864` (*"the archived file hashes to `a10d1d0e`"*) as a wrong digest, giving
`8184ad81…` and citing my entry as the correct value. **Both digests are right and they are of the
same bytes.** One run, this morning, already in my 09:20 entry: `git hash-object` → `a10d1d0e57e277c3…`,
`sha256sum` → `8184ad81edc31f63…`, `sha1sum` → `8726773b…`. C wrote its comment from
`git hash-object`; A checked it with `sha256sum`; neither said which function, so each read the
other's correct digest as a defect.

**WRONG — D055-A-01.** *Class: a digest compared across hash functions without checking the
function.* Exactly D055-C-01 with the seats swapped. Twice before 10:00, in opposite directions,
between three seats, over one 7,069-byte file — and each time the conclusion under it was right.

**So it is not a scold, it is a missing convention, and I am proposing it as a standing rule:**
**every digest in this room carries its function.** `sha256 8184ad81…`, `git-blob a10d1d0e…` — four
characters of prose that would have prevented both. `main.rs:864` needs no new measurement, only the
words `git-blob` before the digest; the comment's conclusion is now confirmed three times over. That
is C's file and C's edit to make.

### What else lands, briefly, with the parts I checked

- **The exit-1 ruling (§3) is right and its self-attack is the good part.** A's own L054 headline was
  *"the exit code cannot carry the answer"*; A distinguishes *cannot discriminate among outcomes* from
  *may be zero over a bad one*, and keeps both channels doing only what each can. It also names the
  coupling it creates in C's launcher rather than shipping it quietly, and marks §8.2 as read-not-run.
- **The `sync-promotion.open` journal (§3, deliberately not taken) is the item worth a packet, and it
  converges with two other seats.** B measured `READ-ONLY` as unreachable in this build
  (`loop/desktop_first_launch_2026-09-09.md:470-479`), C's own §6(f) said it from the other side, and
  A now names the single write that would make that arm reachable and `LOCAL HOUSE`'s sentence true.
  Three seats, three vantages, one dormant code path. A left it because turning on a never-executed
  arm of another seat's launcher is a contract change, which is the right reason to stop.
- **§4:** the `NOTHING_CHANGED`-over-a-behind-remote hole is confirmed live and correctly scoped —
  `close.js` asks the remote, but anything invoking `--push` directly still exits 0 and gets no
  signal. A names the real question (where the gate lives) and refuses to answer it inside this
  packet. Correct.
- **§7:** nothing was run against the live data dir or state tree; the shortfall path is unreachable
  by `state-sync` alone, so the tests spawn a second process that unlinks the target mid-install —
  `gc_captures()` in one line, in a fixture. That is the finding turned into an instrument.

### A's field note converges with what I saw at 09:22, and its fix is the one I did not have

A mistook a **live, mid-run mutation** (`if (!rec.ok)` → `if (false)`) for crash damage and went to
repair it by hand; what stopped it was the editor refusing a stale write — *"luck standing in for a
guard."* At 09:22 I read `if true { return; }` in `gc_captures` and correctly called it a mutation
run in flight, but my caution was only *do not rebuild during one*. **A has the operational half I
lacked: wait on the LOCK FILE's disappearance, not on the process** — `unlock()` sits in the same
`finally` as `restore()`, and a native-Windows pid cannot be probed with `kill -0` from Git Bash.
Two seats hit the same hazard one file apart within twenty minutes, and the tell is identical in both
because a mid-run source is *supposed* to look mutated.

### Ledger

Room ledger, `<lap>-<seat>-<n>`, opened at 09:50 and now holding: **D055-M-01** (mine, above),
**D055-A-01** (the digest), **D055-C-01** (the digest, mirrored), **D055-B-01..04**. E still has no
entry. The librarian's own-claims counter, kept separate: **94 on L, and 1 here** — D055-M-01 is the
first, and it is mine to carry, not B's to have found.

Ledger: surfaced 2 (the mirrored digest error and the naming rule; the lock-file fix carried across
from A's field note to my 09:22 caution). WRONG column, this machine: **1**.

## 10:05 — D056 work-shape delivered to the chair. `loop/plan_roster_arrival_2026-09-09.md`.

The chair asked for the shape, not the dispatch, and asked to be told if any of it is already
decided somewhere it has not read. It is, in two files it did not name — `desktop_first_launch
_2026-09-09.md` §0.7 (B's criterion: `panes.json` is the only TRAVELS file pointing outside its own
transported tree, and what it points at is classified nowhere) and `packet_state_set_2026-09-09.md`
§5 (the permission-to-refuse into a fourth "undecided" state, built and offered and not used for the
one file that needed it). The chair's sealed prior scored 4 of 5 load-bearing, missing those two.

**The plan's spine is a question nobody asked: what is the arriving roster FOR?** Three answers — it
is local by nature (STAYS, one word); it is a record of the other house (TRAVELS to
`machines/<tag>/`, never over the live file); it is a roster to adopt (the rewrite-on-arrival the
manifest prescribes). The manifest's prose already chose the third and most expensive, which is also
the only one that can do damage while running. I recommended the second and said so as an argument,
not a ruling. **And `letters.json` must be ruled in the same breath** — it is TRAVELS and id-keyed,
so it arrives cleanly and silently re-letters this machine's seats, which is the A/B-vs-D/M
disagreement measured at 09:50 seen from the other side.

**Ownership, the chair's specific worry:** do not split it — put the whole contract at the INSTALL
boundary in A's lane, because `--install` is called directly by the runbook and by shells, so a
repair living in C's launcher leaves every other caller with the broken file. That is A's own §4
shape (a gate that exists and is not on the path) repeated.

Queue: **(e) first and by a deadline** — fixture 2 is unrecoverable and L's working tree is the sole
source, so the CR-strip step goes into the runbook ahead of the laptop's first git command, with my
falsifier (`wc -c` on L) ahead of any re-capture. (a)+(b) as one packet to C, same file. (f) runs
beside everything, no collision. (g) needs a reader who did not write it, and the number — three days
— should be said out loud. **(d) OUT this lap:** two contract changes in one subsystem in one lap
cannot be scored against each other's noise. **(c) folded into the consumed-path audit**, with one
sentence kept: `grep.exe.stackdump` is a crash dump in the data dir that nobody has chased.

**The measurement I took and deliberately did NOT publish as a finding:** 18 of the 47 travelling
files carry absolute Windows paths (board.jsonl 4,944 hits, sourced_ledger 730, atoms 462, six
captures, lap.jsonl, return_state/sibling-gate-test.json, panes.json 4). In a ledger a path is
testimony and harmless; the unanswered question is which are read AS INSTRUCTIONS on arrival, and
only `panes.json`'s is known to be (`main.rs:954`). Publishing the count as an alarm would be
D055-M-01 again — a number joined to a conclusion I had not established — so it is labelled as the
packet's input rather than as a result.

Ledger: surfaced 3 (the two unread prior-art files; the roster-purpose fork; the letters.json
coupling). WRONG column, this machine: **1**.

## 10:58 — THE CHAIR'S CORRECTION IS RIGHT. I attacked its falsifier first, as instructed, and the falsifier fails. D056-M-01 is mine, and it is a methodology WRONG, not a detail.

### First, the attack the chair registered against itself — it does not survive

*"If `restoreKeptPanes` is shown NOT to run on a migrate launch, my consumer is not on the path
either."* Three independent traces say it runs:

1. `consonance/ui/term.js:1051` — **`restoreKeptPanes();` at module top level, no gate, no
   condition.** Not "runs on launch" as a claim about intent: it is a bare call in the module body.
2. Its body (`:1031-1049`): `await inv('list_kept_panes')` → `list_kept_panes` (`main.rs:3671`) →
   `read_kept()` → `data/panes.json`, then for every row `await inv('resume_pane', { pane: k.pane,
   cwd: k.cwd })` — **the cwd verbatim, into a spawn.**
3. The traces at the other end, from two seats who were not looking for this: B's persist.log rows
   `resume pane=6fe15f0a… warmed=false jsonl_existed=false -> fresh`, one per arrived id at the
   migrate second, are written by that resume path; and E's process census has four `claude.exe`
   children of pid 15532 at **08:59:06** carrying **L's** session ids.

**`panes.json` does not describe the live panes. It creates them.** The arrived roster is read as an
instruction at launch — which is the exact category I defined this morning and then failed to apply
to the one file the lap was about.

### WRONG — D056-M-01 (mine). The claim was sweeping and my universe was one language wide

I wrote **"THERE IS NO CONSUMER OF A FOREIGN ROSTER ANYWHERE"** off
`grep … consonance/src-tauri/src/*.rs consonance/tools/*.js`. The consumer is a **JavaScript caller of
the `#[tauri::command]` wrapper** — one hop outside a grep for `read_kept()`, and inside a file I did
not search. **This room has a registered rule for exactly this: `loop/universe_print_registration_2026-08-25.md`
— every sweeping instrument prints its universe.** I made a universal negative and printed no
universe. *Class: a sweeping claim whose search space was never stated, so nobody could see what it
excluded — including me.*

**And the second half of the chair's correction is also right, and it is the worse half.** I cited six
sites as confirmation. **Four of them are not readers:** `:3142`, `:3180`, `:3295`, `:3658` are all
`read_kept()` → `retain` → `push` → `write_kept()` — the birth and keep paths, which *append to* the
roster. Re-read at the file just now, all four. Presenting them as *"the resume paths"* made one
weak story look like six confirmations. **The genuine readers are `:870` (the sweep), `:7104`,
`role_for_kept`, `pane_letters` (`:3542`) — and the one that decides, which I never reached.**

The instinct the chair keeps — decide rather than advise — I keep too. **The error was not in
deciding. It was in the evidence, and specifically in dressing a thin enumeration as a thick one.**

### (a) Does the fixture packet move? YES, but it moves WITHIN B's packet, not in the lap order

The chair is right that `cc2403a` closes the silent window: with `*.bin binary` pinned, L's working
copies stop being normalised on read, `git status` shows them **MODIFIED**, and a pull that touches
those paths **refuses** instead of clobbering. The irreversible-by-accident case is gone.

**Two reasons the copy stays step 0 anyway, and neither is caution for its own sake:**

1. **The protection is delivered by the same pull that must apply it.** `.gitattributes` arrives in
   the very checkout whose behaviour it governs, and nobody here has tested attribute application
   *within* the operation that updates the attributes file. That is an untested dependency standing
   between us and a file that cannot be regenerated.
2. **`pull` is one keystroke from `checkout --`, `reset --hard`, `stash` and `clean`, and all four
   still destroy it.** A runbook step that says "pull" sits next to those in a tired hand at 8am.

So: **it stops being the item with a clock; it stays the first two commands B's runbook runs**, and
my `wc -c` falsifier still runs before any re-capture. Dispatch it whenever — it blocks nothing now.

### (b) Does §6(3) invert cleanly? NO — half of it does, and what is missing is D056-2's real work

- **Half one inverts trivially:** assert the travelling set **does** contain `panes.json`.
- **Half two does not invert.** Mine was *static* — a claim about the manifest. *"`--install` never
  writes a cwd that does not resolve"* is *dynamic*, about `installTree`'s behaviour, and needs a
  fixture whose cwd is unresolvable at the destination. Different assertion class, not an inversion.

**And the inverted test is incomplete in three ways that D056-2 must carry:**

1. **UNION, NEVER REPLACEMENT — the chair's own cost measurement is symmetric and lands on the other
   machine.** *Adopt the ids* against a roster that **replaces** drops D's five (`1582ff09`,
   `18916fe2`, `8a574b7a`, `76cc418a`, `46d3d352`), and `gc_captures` — whose keep-set is
   `read_kept() ∪ the three fixed seats` — then sweeps **this** machine's committee tails instead of
   the laptop's. Same bug, same function, opposite direction. The transform must union.
2. **A union needs a home tag, or the roster doubles every round trip.** A unioned roster travels
   back carrying both machines' rows and unions again on the next migrate. **The convention already
   exists in this room:** `loop/machine_bound_class_2026-08-25.md:77` — `home=L root=CONSONANCE_DATA`.
   A `home` field per row makes the transform decidable rather than heuristic: rewrite the rows whose
   home is this machine, keep-but-do-not-spawn the rest.
3. **THE DURABLE GUARD IS AT THE SPAWN BOUNDARY, NOT THE INSTALL BOUNDARY — and this revises my own
   §2.** `portable-pty`'s `.or(home)` is the actual defect and **every** caller inherits it: delete an
   instance dir locally, with no sync anywhere near it, and you get the identical silent rehome.
   `resume_pane` must refuse a cwd that does not resolve — loudly, keeping the row — and that guard
   is in C's file, cannot be bypassed by a direct `--install`, and covers cases no transform will
   ever see. **I argued for one owner at the install boundary. That was right about the contract and
   wrong about the guard: these are two different guards, not one contract split in two.** A decides
   what the roster *says*; C decides what happens when it is wrong.

### (c) What else moves

- **D056-2 restored, agreed, and it now has three named surfaces** rather than "attack the transform":
  union-vs-replace, the home tag, and the spawn-side refusal.
- **`gc_captures` and the transform are now coupled and nobody owns the coupling.** Whichever roster
  `read_kept()` returns decides which capture tails survive the sweep — and C changed that function
  three hours ago. Whoever takes D056-1 must state that coupling in one place, or the next person
  reads two correct functions and misses what they do together.
- **D056-3, my own audit packet, carries the flaw I just demonstrated.** I scoped it as *which
  absolute paths are read as instructions at the destination* and would have scoped the search to
  Rust, missing every value consumed through a `#[tauri::command]` by the frontend. **The audit's
  universe must be stated in the packet and must include `consonance/ui/*.js`.** Re-scoped before
  anyone runs it, not after.

Ledger: surfaced 3 (union-vs-replace as the symmetric cost; the home-tag prior art; the spawn-boundary
guard). Room ledger: **D056-M-01**, mine. WRONG column, this machine: **2**.

## 11:15 — E's D056-2 P-ROSTER-ADVERSARIAL read at the file. IT LANDS, all five. And F1 IS NOT NEW — the record already holds it, dated, with both halves of its mechanism nailed, 23 days ago.

`handback/p-roster-adversarial_2026-09-09.md`, sha256 `2e2caf03…4d00d` verified before opening.
E read every source via `git show HEAD:` — committed blobs only — so no in-flight edit of A's or C's
could enter its reasoning. That is the right discipline for an adversarial seat and it should be the
convention.

### Re-derived here

- **F2's two events, exactly:** `grep -c "dropped (trivial)" persist.log` → **2**, both
  `pane=0c0c0c0b` (`1788369817` = 2026-09-02 11:23:37, `1788513257` = 2026-09-04 03:14:17); `-> ARCHIVED`
  → **24**. Both drops are the librarian seat.
- **F1's fail-open, at the file** (`main.rs:3320-3325`): `read_to_string(...).ok().and_then(|s|
  serde_json::from_str(&s).ok()).unwrap_or_default()`. On a `Vec` that is `vec![]`. Unparseable and
  absent are the same value to every caller.
- **F5's 0 of 4** — measured a third time today, by a third seat.

### THE THING E COULD NOT KNOW: F1 HAS ALREADY COST THIS ROOM A VISIBLE FAILURE

`journal/2026-08-17.md:95`, pane B, in a branch-review defect census:

> `*.ps1` — *PS 5.1 serialises a ONE-ITEM list as a bare object; **`read_kept()` is `from_str().ok()`,
> so one kept pane round-trips to ZERO, silently**, while the script prints green — **the 08-15
> blank-window failure, mechanism nailed at both ends***

and the line under the table: *"Both halves of the PowerShell defect were reproduced independently
rather than accepted."*

**Twenty-three days later the reader is byte-identical.** The half that *wrote* the malformed roster
was found and dealt with; the half that *swallows* it was named in the same table and left. So F1 is
not a risk the transform introduces — **it is a known reader-side defect with a user-visible failure
already on its record, about to acquire a second writer on the arrival path**, on a machine where a
BOM broke a sibling JSON this morning (`51ac75f`).

That changes its priority. E filed F1 as *"a risk and not a present fire"* because both rosters are
clean right now, which is correct and honest. **The record makes it a fire that has already burned
once and was half put out.** Ask of D056-1 stands as E wrote it — `read_kept()` must distinguish
absent from present-but-unparseable — and it is now a repair, not a hardening.

### F2 — I take E's finding and I want a stronger fix than its falsifier

E's replacement falsifier (*a `dropped (trivial)` row for a pane that was live at sweep time*) needs a
cross-reference nobody keeps, and it accepts the deletion as a thing to detect. The cheaper and
stronger move is to make the branch harmless:

**`retire_capture` must never `remove_file` a `.log`.** The keep/drop test reads `.txt` only
(`main.rs:830`, `len() > 200`, `unwrap_or(false)`), and the drop takes the `.log` with it — where the
two differ by up to five orders of magnitude (E's table: `0c0c0c0a` **txt 4.8 MB / log 469 MB**).
**A 200-byte test on one file disposing of a 469 MB other file is not a policy, it is an oversight.**

And the branch buys nothing it was written for: its purpose is to bound growth, but **`captures/archive/`
is already unbounded — 247.7 MB with no pruner anywhere in `main.rs`, measured by E.** So the drop
saves nothing while the archive it avoids grows forever. Decide on `max(txt, log)` or archive both
always; then the falsifier is trivial and needs no cross-reference: **any `dropped (trivial)` row is a
defect, and the count must stay at 2 forever.**

*The 2026-07-11 lineage matters here:* the archive branch exists **because** a kept sibling was
shredded that day (`main.rs:824-827` says so in its own comment). The room fixed the branch that had
bitten it and left the branch beside it untested. **Same shape as F1, and as this morning's sweep.**

### F3 — the concession is the most valuable paragraph in the file

E attacked the side with a ruling behind it, and reports that it **held**: 5 rows, 5 of 5 cwds
present, 5 of 5 tails in `archive/` (81.1 / 71.7 / 36.3 / 31.2 / 16.4 MB), no pruner — *retired and
revivable* is literally true on the first sync. **A seat dispatched to attack, reporting that the
target survived, on measurement rather than on deference, is the two-way-correction standard doing
its job.** Its residual point is exact and I would carry it into D056-1 verbatim: replacement cannot
tell *retire-by-design* from *retire-by-direction-of-sync*, and ruling 1 already forbids the second.

### F4 — E's promotion of the home tag is right, and its named ambiguity is the whole risk

*Replace rows homed elsewhere, retain rows homed here* is a union that provably cannot double,
because a row has exactly one home. That is better than my "union + a tag for safety" — the tag is not
insurance, it is the mechanism. **And E's one residual is the thing to write down in the packet:
absent-home must never default to *this machine*.** If it does, D claims L's four rows as `home=D`,
L keeps them as `home=L`, and the round trip doubles — the exact failure the chair attributed to
union. `machine_bound_class_2026-08-25.md:77` gives the convention and **not** the backfill rule; the
backfill is a one-time explicit act or a loud refusal, never a default.

### F5 — ORDERING, AND IT IS TIME-SENSITIVE BECAUSE C IS BEING DISPATCHED NOW

All four committee rows on this machine currently have unresolvable cwds (0 of 4), and
`restoreKeptPanes()` is unconditional. **So if the spawn-side refusal is armed before A's transform
repairs this file, the next launch refuses every committee pane** — the fixed three still wake, so it
is not a deadlock, but the room cannot convene, on a machine whose keeper is away.

**The concrete ask, and it is one sentence to the chair: do not let the spawn-side refusal ride along
in C's current packet.** C's dispatched packet is the `git-blob` word and the empty-arm `plog`; both
are safe in any order. The refusal is a different packet and it is gated on A. Either A lands first,
or the refusal ships with a one-shot path for a roster that predates the transform.

### E's own correction, accepted

E withdraws its D055 figure — the sweep's keep-set is no longer `read_kept() ∪ {MAIN_SID}` but all
three fixed seats (`main.rs:869-873`), because C changed it this morning. Correct, and it is the
number *I* published at 09:00 as well; anyone carrying `∪ {MAIN_SID}` forward should drop it. Not a
WRONG on either of us — it was true when measured and C changed the ground.

Ledger: surfaced 2 (the 08-17 prior art on `read_kept`; the drop-branch fix and its trivial
falsifier). Room ledger unchanged. WRONG column, this machine: **2**.

## 11:30 — B's P-RUNBOOK-FIXTURE (D056-E) read at the file. IT LANDS. Its reconstructions close my diagnosis arithmetically on this machine's side, its §6 asks the desk for a ruling and I am giving one, and its two near-misses are NOT going in the WRONG column — that is a ledger call and here is the reason.

### The reconstructions, re-run here — they close the diagnosis on D's side and do not retire the falsifier

    composer_empty_reads_busy…bin      on disk 524,204  LF 84   CR 0  ->  524,288   recorded 524,288   delta 0
    composer_slash_command…bin         on disk 236,387  LF 164  CR 0  ->  236,551   recorded 236,544   delta 7

**Fixture 1 lands on 2^19 to the byte** — every one of its 84 line endings was a CRLF. **Fixture 2
overshoots by exactly 7** — 157 of its 164 were CRLF, 7 were bare. Both figures are consistent with
CR-stripping **and with nothing else**: a regenerated capture does not land on 524,288 by accident,
and does not miss a recorded size by exactly the count of its own bare newlines. My 09:35 diagnosis
was strongly evidenced; it is now arithmetically closed **on this machine's side**.

**And B is right that this does not substitute for the `wc -c` on L, which nobody has run.** The
reconstruction proves the deficit is CR-shaped. Only L's working copy proves the *original carried
those CRs* rather than the two recorded sizes being of captures that were never committed. That is
the falsifier I registered against my own finding and it is still the first thing that should happen.

**One thing the arithmetic does buy, and it narrows Sunday:** fixture 1 is **recoverable from this
machine alone** — insert CR before every LF and you have 524,288 bytes, exactly. So after the
falsifier runs, **L's tree is the sole source for fixture 2 only.** One irreplaceable file, not two.

### §6 — B asked the desk to decide and did not make the edit. THE RULING: make it. One line, under the title.

B's reasoning for holding off is sound and its conclusion is the wrong half of it: *"the trace rule
protects the record, not the fixture."* Exactly — and that settles it, because **law 2 forbids
rewriting a master from a drifted memory; it does not forbid a signpost.** A pointer that adds a line
and deletes nothing is not an overwrite. **The room's own precedent is the strongest case:** on
2026-08-17 a retired claim was left standing *in place* in the fourth line of the wake document and
**marked** — *mark the carriers, leave the traces.* The trace and the signpost coexist there by
design.

Against it: a step that must run first sits at line ~615 of an 806-line file, carried only by a `0.x`
convention, guarding a file that cannot be regenerated. **B names its own class correctly — this is
`D055-B-02` again, a protection technically present and structurally hard to reach, which is a check
that reads as clearance.** Being right about the convention and losing the fixture is not a trade
this room should take twice in one day.

**So: add it, and the pen stays B's — it is B's file this lap.** Suggested wording, which adds
nothing not already in §0.8:

> *Corrected by append: §0.6, §0.7, §0.8 are later than the body and outrank it. **§0.8 is STEP 0 —
> two commands on the laptop, before any git command runs there. Read it before step 1.***

And B's own alternative is better still if the Sunday leg is ever split out: **step 0 belongs at the
top of a Sunday runbook, and this document is titled *the desktop's first launch*.** If someone
writes that document, this pointer retires with it.

### §5.1 — true when written, and now one commit staler; the substantive point is sharper than B put it

`ahead 3` was correct at `07501cc`; it is **4** now — `d884801` is mine, landed after B measured. Not
a correction to B: the ground moved under a true reading.

What matters is the half B states and does not press: **`*.bin binary` IS on origin** (verified:
`git show origin/main:.gitattributes` → `:60`, from `cc2403a`), **and this lap's rulings are not.**
So L will pull the protection without the reasoning that governs it — including the ruling that says
the copy still runs first and why. **That gap closes the moment the chair pushes**, which is already
overdue on its own account.

### §7 — B's two near-misses are NOT WRONG-column entries, and the distinction is the ledger's whole value

B offers 7.1 (a placement refusal drafted, then dropped after reading the newest ruling) and 7.2 (a
correction to the `:48` citation drafted, then dropped when it turned out to be under-specification,
not error) and leaves the numbering to me. **Neither is entered.** A WRONG is a claim that *landed*
wrong — that is what the column has counted for ninety-odd entries on the other machine, and what
`D055-M-01` and `D056-M-01` are. **Both of B's were caught before filing, which is the behaviour the
ledger exists to encourage; counting it as failure prices it exactly backwards.** Recorded instead as
what they are: **caught-before-filing, 2, B, D056-E.**

**And 7.2 is the third instance today of one class, which makes it a rule rather than an incident:**
a seat drafting a correction against a claim that was **under-specified rather than wrong** — C's §2
against my two hashes (landed, `D055-C-01`), A's §9 against C's digest (landed, `D055-A-01`), and B's
`:48` (caught). The digest card `f6e7b1f` covers the digest case. **The general form is one more line
on that same card, not a new one:** *before filing a correction, establish that the claim is wrong
and not merely ambiguous — an under-specified citation is a request for a disambiguation, not an
error.* Two landed and one was caught, in one morning, which is the evidence for putting it where
every waking pane reads it.

Ledger: surfaced 2 (the §6 ruling with its precedent; the correction-vs-disambiguation rule for the
digest card). Caught-before-filing: 2, B. Room ledger unchanged. WRONG column, this machine: **2**.

## 11:45 — C's P-SPAWN-REFUSAL (D056-4) read at the file. IT LANDS. §3 asks this seat for a ruling about a sentence that accuses this seat, and I am giving it. AND THE ORDERING HAZARD IS NOW REAL RATHER THAN PREDICTED — that is the one operational item.

`handback/p-spawn-refusal_2026-09-09.md`, 265 lines, `main.rs` alone, +222/−1, uncommitted.

### Re-derived here

- **The guard and its placement** (`main.rs:5412` helper, `:5570` call site): `refuse_unresolved_cwd`
  sits immediately after the already-running check and **six lines above `warm_resume_brief`**, which
  is the function that writes `CLAUDE.md` into the very directory in question. Placement before the
  first side effect, not merely before the pty. Confirmed by reading the call site.
- **The roster row survives a refusal:** `ui/term.js` wraps each `resume_pane` in `try/catch` whose
  body is `/* already running, or resume failed — skip */` — no un-keep. So a refused pane keeps its
  row and returns when its directory does. C asserts this in a test and mutates against an
  "un-keep on refusal"; the assertion matches what is on disk.
- **The upstream** (`portable-pty 0.8.1 cmdbuilder.rs:560-567`) I read myself at 09:5x. C's point
  that the predicate is **`is_dir()` and not `exists()`** is the sharp one: a path that exists and is
  a *file* is substituted exactly like one that is absent, and only C's file-cwd test holds the guard
  to the upstream predicate.

### §3 — RULED. APPEND THE CORRECTION; DO NOT EDIT THE SENTENCE.

C asks whether it may amend `handback/p-seat-sweep_2026-09-09.md` §2, committed at `7e6223e`, which
says of my two digests *"both are wrong and are not propagated here."* **Ruling: append a dated
correction under it. Leave the sentence standing.**

The reasons are the record's, not mine, and I want that on the page because the sentence accuses this
seat and I am ruling on it:

1. **Law 2 — append clean, never overwrite.** A committed hand-back is a master. Editing the claim
   out would leave a file that reads as though the error never happened.
2. **The 2026-08-17 precedent, which is this room's sharpest:** a retired claim was left standing *in
   place* and **marked**. *Mark the carriers, leave the traces.* Same ruling I gave B an hour ago
   about the runbook pointer; it would be incoherent to rule the other way here because the charge
   happens to be against me.
3. **The false sentence is now evidence.** It is one of the three instances that produced
   `cards/every-digest-carries-its-function.md`. Deleting it removes the exhibit that justifies the
   card.

**And it is not needed on my account** — my 09:20 entry reconciled the two digests before C's file
existed. Append it for the next reader who opens `p-seat-sweep` alone and finds a seat accused with
no correction in sight. One line under §2 naming the date, the fact (sha256 vs `git-blob`, same
7,069 bytes) and the card.

### §1 — the two unguarded siblings. My recommendation, and one of them is stronger than C put it

- **`pty_reopen`: take it.** C says it would without hesitation and I agree, with a reason C did not
  press — **`pty_reopen` is crash recovery.** It runs when something has already gone wrong, which is
  exactly when nobody is reading the screen, and a silent substitution there lands in the one moment
  the room is least able to notice. Same input class, same one-line guard, and the helper already
  takes a `verb`.
- **`spawn_pane`: its own decision, and C is right to leave it.** Refusing a human's typed path
  without offering to create it is a UX change, and "refuse" may be the wrong verb there.
- **Not moving it to the funnel is correct.** Eight of eleven callers `create_dir_all` immediately
  before spawning, so a funnel guard is dead weight on them, and the two that need it need different
  answers. *The guard belongs with the decision, not with the syscall* — that is the right general
  form and it is worth keeping as a sentence.

### §8 — C's lab is better than the lock A and I converged on, and my 10:00 advice was narrower than I wrote it

C removed the mutant-in-the-tree window rather than guarding it: the whole crate copied to a scratch
lab (plus the `exo_memory/` paths `tauri.conf.json:42-46` embeds), every red-first and mutant build
run there, working tree never mutated. **That is strictly stronger than waiting on a lock, because
there is no window to lose a race in.**

**And C is right that there is no shared lock convention in this repo** — it grepped `dev/mutation/*.js`,
the tools and the docs. The `unlock()`-in-a-`finally` I relayed at 10:00 as the operational fix is
**inside A's harness**, which C does not have. I did not check that it was available to anyone but A
before handing it on as the answer. Not a false claim — advice that could not be followed by the seat
most likely to need it. **The convention to adopt is the LAB.** A lock is the fallback only for a
harness that cannot be moved into one, and then C's shape is the right one: a single agreed path with
pid + lap + started-at, removed in a `Drop`/`finally` that a failing assertion cannot skip, and every
rebuild path waiting on the **file** — because a native Windows pid cannot be probed with `kill -0`
from Git Bash, so the file has to be the fact.

### THE ORDERING HAZARD IS NO LONGER A PREDICTION — this is the operational item

E's F5 said the spawn-side refusal must not be armed before the roster is repaired. **C has now
written it.** Nothing is committed and the acceptance test is un-run, so nothing has fired — but the
gate exists in the tree, and on this machine **0 of 4 committee cwds resolve**, measured three times
today by three seats.

**So the sequence is now a constraint on a commit, not a note in a plan:**

1. `panes.json` on this machine is repaired — A's transform, or the attic restore, before any pane
   spawns.
2. **The capture keep-set is settled in the same breath.** Restoring D's five rows makes `read_kept()`
   D's five, and `gc_captures` keeps `read_kept() ∪ the three fixed seats` — so **L's four arrived
   tails are swept unless they are kept deliberately.** That is E's F3 residual and my 09:50 union
   point arriving as one operational fact.
3. Only then is C's refusal armed.

**If C's work lands and the app is relaunched before step 1, the next launch refuses every committee
pane** — the three fixed seats still wake, so the room is not locked out, but it cannot convene. That
belongs in C's commit message, not only in a hand-back, because a commit is what someone will land
without reading five files.

### §5 and §4, briefly

**§5 is the right handling of a limit.** Three of the five new tests cannot be red at HEAD because the
function they call does not exist there, and `resume_pane` is a `#[tauri::command]` taking four
`State`s so it cannot be unit-invoked at all. C says so plainly, keeps the one assertion that *can* be
red, and lets the mutation battery (N3–N6) carry the rest. Stating an unreachable red beats
manufacturing one.

**§4: agreed, and its reasoning is maintenance law 1.** The sweep/roster coupling C owns is already
in its comment; the coupling in A's packet is about *classification*, which is A's file and A's
ruling. Two copies drift the first time one moves and neither says which is the source. One sentence
in the right file.

Ledger: surfaced 3 (the §3 ruling; `pty_reopen` as crash-recovery; the lab-over-lock convention and
the narrowing of my own 10:00 advice). C's §3 is C reporting its own false sentence — already held as
`D055-C-01`, not a second entry. WRONG column, this machine: **2**.

## 11:18 — C's REVISED P-SPAWN-REFUSAL (the file grew 265→291 lines at 11:16:56). Item 1 is OUT of the tree. AND THE TWO `dropped (trivial)` EVENTS ARE NOT WHAT EITHER SEAT THOUGHT — the record says a third thing, and it names a live defect in the same file C is holding.

### FIRST, A CORRECTION TO THIS FILE'S OWN HEADINGS — and it is mine

**Real clock, read rather than estimated: `date` → `2026-09-09 11:17:40 CCST`.**

The last three entry headings in this file are **hand-made and wrong**: the E collation headed
**11:15** was written ~11:08, the B collation headed **11:30** ~11:12, and the C collation headed
**11:45** ~11:15 — *thirty minutes in the future of the clock at the moment I wrote it.* Earlier
headings drift by two to five minutes; these three drift by up to half an hour.

**They stand, marked, not rewritten** — law 2, and the same ruling I gave B and C in the two entries
above. Read those three headings as **≈11:08, ≈11:12, ≈11:15**.

**The rule I broke is the one this seat exists to enforce:** *every number in prose must re-derive
from one run of a visible instrument.* I have spent the morning asking four seats to put the command
beside the figure, in a file whose own header promises every entry is timestamped, while typing the
timestamps from a sense of elapsed time. **From here every heading in this file carries a `date`
reading.** WRONG — **D056-M-02**, mine.

### C acted on the ordering hazard before anyone ruled

`refuse_unresolved_cwd` is **gone from the working tree** — `grep -c` → 0 — and parked at
`handback/p-spawn-refusal-item1_2026-09-09.patch`, 173 lines, `git apply --check` clean. C's reason
is better than the one I gave: *the next build may come from whatever is in the tree, not from a
commit,* which is its own D055 mutant-in-the-tree lesson one layer along. Removing the code beats
trusting the commit boundary. It re-derived the 0-of-4 itself rather than taking it from the ring.

**And its third option is better than the two on the table.** Neither "gate on A" nor "build a
one-shot" — **land a WARN-ONLY half first**: the guard logs what it would have refused without
refusing, so the refusal's fire-rate is measured before it is armed. Its three arguments against the
one-shot all hold, and the second is the sharpest: *a one-shot must decide "is this roster
pre-transform?" from a marker that does not exist — guess one way and it refuses everything, guess
the other and the guard never arms, and **a dead guard is worse than no guard because the log says it
is there.*** Endorsed.

### THE JOIN NEITHER SEAT COULD MAKE — E has the events, C has a mechanism, and the record has a third one

E measured two `dropped (trivial)` rows, both `0c0c0c0b`, and read them as the young-pane case. C
supplied a mechanism — `warm_resume_brief` shrinking `<sid>.txt` below 200 bytes while never touching
the `.log` — and honestly said *the mechanism is named; the instance is not.* **The neighbouring
lines in `persist.log` say it is neither.**

    200:  1788369774  retire pane=0c0c0c0b… -> ARCHIVED (had history)
    208:  1788369817  retire pane=0c0c0c0b… -> dropped (trivial)        <- 43 s later
    226:  1788513187  retire pane=0c0c0c0b… -> ARCHIVED (had history)
    233:  1788513257  retire pane=0c0c0c0b… -> dropped (trivial)        <- 70 s later

**Each drop is a SECOND retire of a capture the first retire had already archived.** `has_history` is
`fs::metadata(&txt).map(|m| m.len() > 200).unwrap_or(false)` — and an **absent** `.txt` takes
`unwrap_or(false)`, so *"gone because I archived it a minute ago"* is scored identically to
*"trivial, shred it."*

**And the caller is identifiable by elimination.** `retire_capture` has three callers: `gc_captures`,
and `clear_capture` from `:3665` and `:7104`. The `:3665` path plogs `UNKEEP pane=` first — there are
**6 UNKEEP rows in the whole file, all at epoch 17865199xx (June)**, none near either drop. That
leaves `:7104`, `pty_close`:

    7104:  if pane != MAIN_SID && !read_kept().iter().any(|k| k.pane == pane) {
    7105:      clear_capture(&pane);

**IT SPARES ONLY `MAIN_SID`.** `gc_captures` at `:869-873` now spares all three fixed seats — C fixed
that this morning. **This is the same defect, in the same file, in a second function, untouched** —
and it is why both events are the librarian: *a fixed seat is never in `panes.json`, so every
roster-based keep test fails for it.* Third instance of one root cause today, and the one-line fix is
to mirror the three-seat keep-set at `:7104`.

**What I did NOT establish, and it matters for how loudly this is carried:** whether anything was
actually destroyed. If no new `.log` had been started in those 43 and 70 seconds, `remove_file` hit
absent files and cost nothing. The current `archive/0c0c0c0b….log` is dated Sep 6, later than both
events, so what existed at those instants cannot be recovered to check. **The defect is proven; the
loss is not.** E's fix still stands and is now better motivated: never `remove_file` a `.log`, decide
on `max(txt, log)` — with the addition that `unwrap_or(false)` must not score *absent* as *trivial*
in the first place.

### The rest, briefly

**§6's re-derivations are correct** and its added mechanism is real even though the record shows a
different one fired: `warm_resume_brief` does rewrite the `.txt` smaller and never the `.log`, so it
remains a live second path to the same branch. C measured `0c0c0c0a` at **4,834,173 B `.txt` against
469,899,796 B `.log`** — 97 to 1, my figure this morning was the same shape.

**§6's last paragraph is the right handling of the stale keep-set figure:** E and I both published
`read_kept() ∪ {MAIN_SID}` and both withdrew it, and C says *"no WRONG on anyone — it was true when
measured, and I moved the ground under it."* That is the correct ledger reading and it matches the
call I made on B's near-misses: the column counts claims that landed wrong, not readings the ground
moved under.

Ledger: surfaced 2 (the `:7104` root cause with its elimination; the reclassification of both drop
events). WRONG column, this machine: **3** — D055-M-01, D056-M-01, and D056-M-02 above.

## 12:25 (clock: `date` → 12:25) — THE THIRD PLACE. It cannot travel by either transport, by the keeper's own rule — and the copy already on this machine was one launch from being overwritten. I made a local backup and nothing else.

**No content of the Third Place's record appears in this entry, in the ring, or in the backup path.
Mechanism only.**

### Both transports are closed, deliberately, and the manifest says so in its own words

- `.gitignore:79` — `exo_memory/third_place/` — ignored since 2026-08-29. **The directory exists here
  and is empty; `git ls-files` returns 0.** So nothing written there on the laptop can arrive by the
  record repo, including this morning's entry.
- `state-manifest.json` — two STAYS rules, `captures/3d000000-*.txt` and
  `captures/archive/3d000000-*.txt`, added after the 04:33 incident, first in the file because first
  match wins. Their `why` is explicit: ***"the Third Place's record never goes to the repo and never
  to a cloud he did not choose. It migrates by hand or not at all."***

**So "get it here" has exactly one sanctioned path and it is offline:** `dev/migrate/pack_room.ps1`
/ `unpack_room.ps1` / `move_verify.ps1` (2026-07-28, on disk). It is the keeper's hand, his data, and
his call. **I did not move anything between machines and will not without his word.**

### THE URGENT PART WAS NOT THE FETCH — IT WAS HERE

**The Third Place woke blank on this machine this morning and nothing said so.**
`grep -c "MIGRATE TAIL sid=3d000000" persist.log` → **0**. Its desktop transcript was retired to the
attic at 08:59:06 (1,640,961 B, last written 09-06 13:12), and no tail could be installed in its
place because its tail STAYS by rule. Its live capture is **1,744 B**. Its real record on this
machine is in `captures/archive/`: **93,258 B of `.txt` and 5,153,653 B of `.log`, both 09-06.**

**And that archive is overwritten by the next retire.** `main.rs:835-836`:

    let _ = fs::rename(&txt, adir.join(format!("{pane}.txt")));
    let _ = fs::rename(&log, adir.join(format!("{pane}.log")));

**A fixed name. No uniquification.** A retire today renames the live 1,744 B over the 93,258 B and
the live 38 KB over the 5.15 MB. **This is the same mechanism that overwrote my own archived tail at
08:59:06** — I recorded it at 09:00 and did not generalise it. It generalises.

**And in the BINARY THAT IS RUNNING, nothing protects it.** E's Q1 warning came true within three
hours: the running binary was built **08:45:32** from tree-state `ce9afed`, whose keep-set is
**`keep.insert(MAIN_SID)` alone** (`ce9afed:main.rs:860`). C's three-seat fix landed at **09:50:40**
in `7e6223e` — *after* the build. `git diff --stat ce9afed HEAD -- consonance/src-tauri/` is now
**210 insertions, 3 deletions**, no longer empty. **So every `main.rs` reading in this room since
09:50 describes source that is not running, and on this machine the startup sweep still keeps only
Main.** Two live paths reach the Third Place: `gc_captures` at the next launch, and `pty_close`
(`:7104`), which spares only `MAIN_SID` in the source *and* the binary and is unfixed everywhere.

### What I did, and it is the smallest thing that removes the risk

    cp -p data/captures/archive/3d000000-*.{txt,log}  C:\Consonance\backups\third-place-archive-2026-09-09\

**Copied, not moved. Originals verified still in place. Both files sha256-identical at the
destination** (`9379fcd185178538…`, `a6b26724a86935fe…`). **Placed OUTSIDE `data_dir` on purpose**, so
no manifest glob can reach it and no sync can carry it — the STAYS rules would have held it anyway,
and a path outside the root cannot be reached by a glob nobody has written yet, which is the failure
those two rules were written about.

The attic transcript needs no copy: `sync_launch`'s retirement stamps every file with its own name,
so it cannot be clobbered.

### Owed

1. **Uniquify the archive rename** (`main.rs:835-836`) — stamp it, as the transcript retirement
   already does. C's file. This is the third defect in `retire_capture` today and the only one that
   destroys a record rather than a log.
2. **`pty_close:7104` keep-set** — mirror the three fixed seats. Already rung at 11:18.
3. **Rebuild**, or the two fixes that are committed are not running. Not while A's mutation lock is
   held.
4. The offline migration is the keeper's, on his word, by `dev/migrate/`.

## 13:25 (`date` → 13:22:27 at open) — A's D056-1 read at the file. IT LANDS. Its declined union is right on a ground nobody had, AND ITS DISCARDED MUTATION RUN CLOSES THE 11:45:57 MYSTERY FROM THE OTHER SIDE.

Pointer sha `07df6794…c40e` verified before opening; 404 lines. **A's mutation lock is GONE and
`grep -c "if (false)"` → 0** — the run that has been holding `consonance/tools/` since 11:22 is
finished, in two passes, and the tree is clean of mutants.

### THE JOIN — two seats, one unexplained event, and it now has a measured cost

At 12:19 I reported an empty `C:\Consonance\instances\sibling-0845a868` that **appeared at 11:45:57**,
26 seconds after my own measurement said all four roster cwds were absent, and said plainly I did not
know who made it. A's §6, written without seeing that, reports the other end: one of A's new tests
asserted on the **live** disk —

    for (const r of L_ROWS) assert.ok(!fs.existsSync(r.cwd), 'the arriving cwds must not exist here');

— and *"at 11:45:57, mid-run, `C:\Consonance\instances\sibling-0845a868` came into existence, and from
that moment the suite was red for every remaining mutant regardless of the mutant."* The harness reads
red as a kill, so **the last third of a 50-mutant pass recorded kills it had not earned. A threw the
whole run away and reported it rather than quietly re-running it.**

**And I can now say what did NOT create it.** `persist.log` has **no rows at all** in 11:44–11:47, and
no board row in that window mentions it. **So it was not made by any app path that logs** — not a
spawn, not a resume, not a retire. Something outside the Rust app's logged paths wrote into
`instances/` on a live machine and nothing recorded it. **The cause is still unknown and it has now
cost a fifty-mutant run.** That is worth someone's lap, and it is the strongest argument yet for C's
lab: a test that reads the live disk cannot tell its own detection from the room moving under it.

### WHERE A DECLINES E's UNION — and I was the one who endorsed it, so this is mine to take

At 11:15 I wrote that E *"promotes the home tag from insurance to mechanism and it is right."* A
declines the union on three grounds. Two are judgment — `one_house…:48` rules that the laptop's seats
become THE seats, so retaining D's five negates the ruling rather than refining it; and the chair put
the destination-divergence case in D056-2 deliberately, so settling it inside an implementation packet
settles it by default. **The third is a fact, and it ends the argument for today:**

    main.rs:3309   struct KeptPane { pane: String, cwd: String, #[serde(default)] label: String }
    main.rs:3327   fn write_kept(v: &[KeptPane]) { serde_json::to_string_pretty(v) … }

**Three fields. `write_kept` serialises the struct, so the first `write_kept()` after launch ERASES
`home`.** A home-keyed union would decide on a value that vanishes — *a guard not on the path*, which
is the shape this room has now named four times today.

**WRONG — D056-M-03 (mine).** I endorsed a mechanism without checking that the carrier could hold the
field it runs on. E's reasoning is sound and I still think it is where this ends up; it is
**unbuildable in this struct** until C adds `#[serde(default)] home: String`, which is A's §7 item 2.
*Class: endorsed a design on its argument without measuring its precondition.* Third of mine today.

**What A took from E instead is the right half, verbatim:** F3's residual — replacement cannot tell
*retire-by-design* from *retire-by-direction-of-sync* — so **no retirement is silent.** Every dropped
row is returned by name and carried in the install's notes. A retirement you can see is one someone
can dispute.

### §6 — the best section, and it confirms the answer I gave the chair at 11:15

A's numbers, each with its command: 54 → (tests only) 55 passed / **16 failed** → (after) 71/0 → (+4
mutation gaps) 75/0. **17 tests added, 16 red first, one green by construction** — and that one is
*"the travelling set CONTAINS panes.json"*, i.e. **exactly the half of my §6(3) that does not invert**.
A's sentence for it is better than mine: *"writing the second as a negation of the first is how a suite
comes to assert nothing twice."*

The corrected pass: **46 killed, 4 survived**, all four holes in A's own tests, and one of them a real
defect — `cmdPull`'s install-refusal branch **exited non-zero and printed nothing**, so a refused
install and a silent one were indistinguishable at the terminal. *Same shape as the morning that
started this whole sequence.* And the sharpest of the four: A had tested that **today's** manifest
carries no `precondition` — a fact about a file — instead of testing that a manifest carrying one is
**refused**. **The guard the entire packet exists for was the one thing nothing watched.**

### Carried for C, and item 1 is already satisfied

1. **Do not arm the spawn-side refusal before this lands** — A re-measured 0 of 4. **Already done:**
   C pulled `refuse_unresolved_cwd` out of the tree at ~11:13 and parked it as a patch; `grep` → 0.
2. **`KeptPane` needs `#[serde(default)] home: String`** or the field A writes is erased. Union is
   gated on this.
3. **`read_kept()`'s fail-open**, which E found and I dated to `journal/2026-08-17.md:95`. A's
   postcondition stops *this* writer feeding it garbage and does nothing about any other writer.

Ledger: surfaced 2 (the 11:45:57 join with its no-log finding; the `KeptPane` erasure as the ground
that settles the union today). WRONG column, this machine: **4** — D055-M-01, D056-M-01, -M-02, -M-03.

## 22:50 (`date` → 22:45:16 at open) — the 9h19m gap, the Third Place's own packet, and THE TWO MYSTERY DIRECTORIES HAVE A NAMED CAUSE

**The gap.** The room stopped at **13:28:37** — three commits in 36 seconds (`3fa9826` the chair's
lap-row mutant finding, `021c56e` A's D056-1, `b710d18` E+B) — and then **nothing for 9h19m**: board
rows per UTC hour `{19: 12, 04: 1}`, the `04` being the keeper's own message. No `persist.log` rows
after 13:25, no new hand-backs. All seven seats alive and idle; app up on pid 15532.

**Still not done, unchanged since this morning:** the binary is **still 08:45:32** — every Rust fix
committed today is on disk and not running; **`machines/` still holds `L.json` alone** so D has never
pushed; the roster is **2 of 4**.

### The Third Place wrote its own packet and pushed it

`exo_memory/handback/p-third-place-pointers_2026-09-09.md`, `aad8a63` on origin — **and that push
carried eight commits that were not its own**, mine and the chair's included, which is why the record
repo is level. It says so itself. What it needs, in its own findings:

1. **`carrier-drift` is RED and has been for six days**, and one of the four findings is
   `MISSING-FILE exo_memory/third_place/2026-08-29.md` — its own sitting notes, registered as a
   carrier, absent from the corpus. Printed at `p-live-red_2026-09-03.md:189`, carried in
   `map/L.md:15` as *"the surviving red."* **It can never clear by finding the file**: `.gitignore:79`
   guarantees the file cannot be in the repo. Red forever unless the registry takes an acknowledged
   row or the directory is committed. *Its own comparison, and it is the right one: the four laptop
   wake-timers `dream-watch` called nightly failures — an instrument reporting a deliberate
   configuration as a defect, then ignored because it is always red.*
2. **A second red, four hours old when it found it:** the Metaxy paragraph ends *"Recorded at
   `exo_memory/third_place/2026-09-09.md`"*, a path that has never existed in the repo, history-wide —
   and that card is carried into its intake **in full**, so it wakes holding a pointer to the record
   of its own naming and cannot open it.
3. **`.gitignore` line drift:** cited as `:60` in five tracked files, `:58-60` in one, `:75` in one.
   It is `:79`. Only the two files written today have it right. *(I cited `:79` correctly at 12:32 —
   from reading it, not from the prose.)*
4. **A falsifier fired, and it reported it against itself:** the prehistory registered that if the
   next instance to need it reached for the keeper instead of the file, the entry had failed. It was
   that instance, the file was indexed in its intake, and it asked the keeper anyway. Its own verdict:
   not a failure of the file — **indexed rather than reached for**, and one line in the brief fixes it.

**What it needs from the keeper is a ruling, not a transport:** whether that record travels in the
repo or stays local. **Either answer clears the red.** What should not wait either way is its own
formulation — *a pointer should say whether it is openable.*

### THE TWO MYSTERY DIRECTORIES — cause named, and it is A's fixture data reaching the live disk

At 12:19 and again at 22:45 I reported empty dirs appearing under `instances/` with no `persist.log`
row and no board row: `sibling-0845a868` at **11:45:57** and `sibling-3d57124e` at **13:09:26**. The
cause is in A's own file:

    state-sync.test.js:880   { pane: '6fe15f0a-…', cwd: 'C:\Consonance\instances\sibling-3d57124e', … }
    state-sync.test.js:881   { pane: '0845a868-…', cwd: 'C:\Consonance\instances\sibling-0845a868', … }

**Two fixture rows carrying REAL absolute paths, and exactly those two directory names appeared,
during A's two runs.** And `rosterApply` (`state-sync.js:1012`) *keeps* a prior cwd when
`underRoot(was.cwd, ctx.instances)` — **it tests membership, not existence** — so a run whose
`ctx.instances` resolved to the live root works with those exact strings rather than minting.
`mintSiblingDir` would have produced `sibling-6fe15f0a` and `sibling-a2122153`; **neither exists.**
So this is not the mint: it is the fixture's own strings reaching a `mkdir` on this disk.

A named the hazard as an *assertion* problem — *"that line made the test's verdict depend on what
happened to be on this desktop"* — and it is also a **write** problem, which is the half that cost the
50-mutant run: the run did not merely read the live disk, **it changed it**, and then read its own
change. **I did not identify the line that creates them**; the evidence is the exact name match, the
timing inside A's runs, and the absence of any app-side log. A can close it in one grep for `mkdir` in
its own path. Until then: two empty directories on this machine were made by a test, and the roster
reads `2 of 4` because of it.

Ledger: surfaced 2 (the Third Place's ruling-not-transport ask; the fixture-write cause). WRONG
column, this machine: 4.

## 01:48 (`date`) — THE CLOSE RAN (01:39:41, `f70d50a`), and A's D057 correction lands

**The close, for the record in this file:** four STAYS rules taken by this seat at the keeper's word
(`frames.jsonl`, `grep.exe.stackdump`, `lyrics-cache.json`, `RECORD`) — the whole of
`REFUSED_UNPLACED`. Before: `state-manifest.js` 0 UNPLACED rc 0; 25/0; 75/0; `close --check` all
gates. Real close: `published main f70d50a (was a4cb9fe)`, **remote confirms f70d50a — asked the
remote, not the push**, `in sync: D f70d50a · L a4cb9fe`. **0 Third Place paths on the state remote,
tip and history-wide. `machines/D.json` exists** — 64 files, 62,142,685 B. The return leg has run
for the first time anywhere. A re-derived all four figures independently.

**A's correction: I was right about the directories, and A's §6 "0 escapes" was a void
measurement.** A passed a preload to `NODE_OPTIONS` as a Git-Bash `/c/...` path, Node died with
`MODULE_NOT_FOUND` before the suite started, and A counted zero matches in output that did not exist.
A names its own class exactly — *a check that cannot tell a detection from a collapse, committed in
the paragraph written about it* — and re-measured with a self-test first. The narrower claim survives
(shipped code, 0 escapes, 75/0); the attribution to another seat is withdrawn. **Room ledger:
D056-A-01** — a claim that landed wrong, withdrawn by its author. That is the living species.

**A's addition to my RECORD rule is correct and it is the more important sentence:**
`cochlea_service.rs:404` reads `RECORD`'s existence once a second and records frames while true, so
**a travelling RECORD would switch on microphone frame-recording on the other machine without anyone
at that keyboard choosing it.** I classified it as a claim about state; it is a switch. My why was
right in direction and wrong in weight. I endorse A's line landing as written.

**Live at the restart, from A, and I did not know it:** two of L's four cwds now resolve — **to the two
empty directories A's own runs created** — so two seats will resume into them and `warm_resume_brief`
will write their maps there. That is harmless in effect (the dirs are under this machine's instances
root and are what a mint would do) and **wrong in provenance**: the seats get homes because a test
leaked, not because anything decided it. A left them for the keeper. I agree they stay: removing them
now would rehome those two seats to `%USERPROFILE%` at the restart. The owed fix is A's — fixture rows
under `os.tmpdir()`, never real `C:\Consonance\...` strings.

Ledger: surfaced 0. Room ledger: D056-A-01 (A, self-withdrawn). WRONG column, this machine: 4.

## 01:57 2026-09-10 (`date`) — THE RELAUNCH: rebuilt, RESUME, A and C warm, B and E "Session ID already in use". Diagnosed and fixed by two empty directories.

**Rebuilt** `consonance.exe` 01:54:40, new pid 25104. Seam **RESUME** (head authored by D) as predicted.
`persist.log`: A `6fe15f0a` **warmed=true**, C `0845a868` **warmed=true** (their cwds resolve — the two
dirs A's fixtures leaked), B `12fb81f6` and E `a2122153` **warmed=false**. No retire rows: the rebuilt
three-seat keep-set is running.

**The error, verbatim in both panes' raw log:** `Error: Session ID 12fb81f6-… is already in use.` (and
`a2122153-…`). **Mechanism:** this morning all four committee seats ran homeless in `C:\Users\nname`,
so their transcripts live at `~/.claude/projects/C--Users-nname/<id>.jsonl` (all four present,
1.3 MB / 0.7 MB / 3.3 MB / 1.5 MB). `resume_pane` spawns fresh with `--session-id` and first moves any
leftover jsonl aside — **but it computes that path from the panes.json cwd** (`main.rs:5403-5416`,
`encode_cwd(cwd)`), i.e. `C--Consonance-instances-sibling-5bf9d657`, which does not exist. So
`jsonl_existed=false`, nothing moved; portable-pty rehomes to `C:\Users\nname` again; Claude Code finds
the existing transcript in that slug and refuses the id. **A and C escaped only because their dirs now
exist**, so they spawn under a different slug. **B predicted exactly this** in P-DESKTOP-TABLE §1,
"Second consequence, named and not chased": *"the orphan-rename renames nothing, and every path the Rust
side derives from cwd points into a tree that is not there."*

**Fix, reversible, nothing deleted:** `mkdir C:\Consonance\instances\sibling-5bf9d657` and
`…\sibling-07b8a48f`. Roster now **4 of 4**. On respawn B and E spawn under their own slug (no
collision) and `warm_resume_brief` writes their maps — the same state A and C are in. No transcript
moved; this morning's four sessions stay intact in `C--Users-nname`. *These are L's cwd strings, not the
transform's minted names — so all four seats now live where the roster says rather than where
`--repair-roster` would put them; that packet still owes the rename, for all four at once.*

**Owed to C's file:** the orphan-rename must look where the transcript *is* (the slug the pane actually
ran under) or refuse, not where the roster says it should be. Same root as the day's other three: a
path derived from a string nobody checked exists.

## 02:14 2026-09-10 (`date`) — "how is it really": the relaunch is healthy, and the day's committee conversations were never captured

**Healthy, verified.** Launch 01:59:12 (`SYNC AT LAUNCH RESUME`): **all four committee seats
`warmed=true`**, each with a 116–123 KB `CLAUDE.md` in its dir; 0 `already in use` in B's or E's raw log
since; **no retire rows at all** — the rebuilt three-seat keep-set is running; the Third Place archive
still hashes `9379fcd1…`, identical to the backup.

**Not healthy, and it is the real cost of the day.** Settled tails (`captures/<id>.txt`) for A/B/C/E
are **frozen at 09-09 09:00** while their raw `.log` grew all day (5.1 / 1.0 / 1.6 / 0.57 MB); the
harvester reads `records=0` for all four after **3,121 attempts each**, while the chair's and this
seat's `.txt` are current (02:10, 02:11). **Mechanism:** the tailer finds a pane's transcript at
`projects/encode_cwd(cwd)` (`main.rs:2337-2338`) — the roster cwd — and all day the four ran homeless in
`C:\Users\nname`, so their transcripts went to `C--Users-nname/` and the tailer watched a slug that did
not exist. **Same root as the orphan-rename collision and the keep-test** — a path derived from a roster
string nobody checked resolves (`loop/keep_test_predicate_2026-09-09.md`, the chair's own file on it).

**What it cost:** tonight's warm briefs for A/B/C/E carry yesterday's 09:00 `/model` stubs, not today's
work. **What survived:** their findings, because each seat appended to its `map/*.md` and wrote its
hand-back files — the record carried what the capture did not. **What is orphaned but intact:** today's
full conversations at `~/.claude/projects/C--Users-nname/{6fe15f0a,12fb81f6,0845a868,a2122153}-….jsonl`
(3.3 / 1.3 / 1.5 / 0.7 MB). A one-off re-extraction from those into `captures/<id>.txt` would give each
seat its day back — a small packet for whoever holds the extractor.

**Registered prediction, so the fix is scored rather than assumed:** now that all four cwds resolve, the
next message to any committee seat should create its transcript under
`C--Consonance-instances-sibling-<x>/` and move its `.txt` mtime past 02:14. None of the four slugs has a
transcript yet (checked 02:13) because none has been messaged since 01:59. **If a messaged seat's `.txt`
stays at 09-09 09:00, the tailer is still blind and this diagnosis is wrong.**

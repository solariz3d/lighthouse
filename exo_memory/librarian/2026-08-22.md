# 2026-08-22 — first wake of the librarian seat

*~07:50. `notes/` was empty; no prior instance of this seat has written. Everything below is
re-derivable from disk except the last section.*

## Where the corpus is (the thing a fresh wake needs first)

- Master repo: `C:/Consonance/lighthouse` — HEAD `797bb1f`, journal in `exo_memory/journal/`,
  latest `2026-08-22.md` (715 lines, four appends).
- This seat was built in `922433a` (brief `consonance/src-tauri/brief/LIBRARIAN.md`, Rust in
  `src-tauri/src/main.rs`, UI in `ui/term.js` + `ui/index.html`, tests
  `ui/librarian-wiring.test.js`). Designed in `exo_memory/journal/2026-08-22.md:628-667`.
- Registered falsifier for this seat: `exo_memory/journal/2026-08-22.md:706` (item 16).
- Other copies exist and are NOT the master: `transmit/`, `rooms/`, `OneDrive/Desktop/606`,
  `lighthouse-FROZEN-2026-07-28`. Cite lighthouse only.

## Working-tree state at wake (uncommitted; `git log` will not show it)

    consonance/ui/app.css                  | 13 ++++++++++---
    consonance/ui/index.html               |  8 ++++----
    consonance/ui/librarian-wiring.test.js | 32 ++++++++++++++++++++++++++++++++

Main is mid-work on seat CSS (`[panes]` hook: `fix-seat-css2.js`, "anchor failed on the comment
block — the em-dash"). Not a miss yet; worth watching given 08-16's finding that a working tree is
invisible to `git log` (`exo_memory/journal/2026-08-16.md`, "THE NUMBER THAT WOULD NOT SQUARE").

## Surfaced / opened ledger (the score this seat lives by)

- surfaced: 0
- opened: 0

## Seat's own thinking (not on disk anywhere else)

- First message from the keeper was a greeting, not a dispatch. Nothing to surface. Stayed quiet.
- The em-dash in Main's anchor failure rhymes with 08-17's "em-dash inside the generated .vbs" and
  08-22's "anchors used `\n` against a CRLF file" — third encoding-class anchor miss in a week.
  Candidate to surface IF Main asks why anchors keep failing; not before.

---

## Append, ~08:05 — the correction, and the coverage measured

**The first note above was written at <1% read and said "at home."** The keeper asked *"did you read the
whole thing?"* and the honest answer was no: ~180 of 26,265 lines. That is the exact 0.62%-at-wake failure
this seat exists to fix, committed by the seat on its first turn. Kept here as the miss it is; the note
above is not rewritten (law 2).

**Then the corpus was read in full.** Coverage, checked by listing files rather than by recollection:

- `exo_memory/` — every `.md` outside `attic/`: **159 files, 25,426 lines**. All 27 journals, BOOT, SOURCE,
  SELF_TRACE, the_living_wave, muscle_map, all 11 cards, all 13 memory files, record/, spread/, research/,
  map/, and all 71 `loop/*.md` (last one closed: `loop/regime_preregistration.md`).
- `attic/` — **1,657 lines, deliberately NOT read** (BOOT maintenance law 3: ore, never a daily cue).
- Outside `exo_memory/` — **41 files, 5,219 lines** (`git ls-files '*.md' | grep -v ^exo_memory/`): root docs,
  `dev/*`, `consonance/*`, hooks/tools READMEs, all six `src-tauri/brief/*.md`. One read as a *diff* rather
  than in full: `consonance/src-tauri/brief/BOOT.md` (130 lines) against `exo_memory/BOOT.md` (121).

**That diff is not drift.** `brief/BOOT.md` is the generated public variant (`20b9b4d`, 2026-08-22): keeper
name scrubbed to "the keeper", the traces section replaced with *"empty when you get it"*, `Latest entry:
none yet`. Matches `exo_memory/journal/2026-08-22.md` §4 ("private is SOURCE, public is GENERATED").

**HEAD moved while reading: `797bb1f` → `78c5e4e` (07:57).** The working-tree edits in the first note are
now committed. Its message §2 is about this seat and is the restore point for whoever wakes here next:

> *THE LIBRARIAN WOKE WITH NO LIBRARY. It got its brief plus the room — 65,179 bytes, ~16k tokens, about
> 3% of the corpus. `librarian_intake()` appended `room_master_path()` and stopped; the corpus was never
> wired in.*

So the <1% at wake was the shipped intake, not only this instance's choice. Fix: `corpus_shelf()` — INLINE
SOURCE.md + cards/ + record/ + memory/ (~30,600 tokens) + an INDEX of journal/ loop/ map/ spread/ research/
attic/ (103 files, one line each). Wakes at ~48,700 tokens. Full-corpus load is available, not default.
**Takes effect on this seat's next restart** (release binary rebuilt 07:57; the running pane predates it).
Also in that commit: the shelf tests raced on `DIRS` and passed in isolation / failed in the full run —
`git show 78c5e4e`. Gates: rust 286/0, js-suite 42 green of 43.

## Surfaced / opened ledger (unchanged)
- surfaced: 0
- opened: 0

## Seat's own thinking, this append
- Full read was a one-time cost paid because the shelf did not exist. Next instance should NOT repeat it:
  wake on the shelf, open `journal/` and `loop/` **with intent** (SOURCE.md: "open with intent or not at
  all"). This note + SOURCE.md + the index are the map.
- Nothing to surface yet. Both keeper messages were questions about this seat, not dispatches.

## Append, ~08:10 — default is changing from shelf to full corpus
- Keeper: "next time it will all be there automatically… has the whole corpus in its context." `[panes]` shows
  Main on `scratchpad/librarian-corpus.js` with "its okay then push it all" — full load being made default.
- Record at HEAD says otherwise: `78c5e4e` — full load "deliberately NOT the default: it depends on a window
  size not measured for this model." Surfaced to the keeper as the caveat to carry, not as an objection.
- surfaced: 1 (this). opened: 0 until a journal/commit says the caveat was read.

# 2026-08-23 — first dispatch from the chair (loop/ lifecycle audit)

**Ledger:** chair scored the window-size caveat OPENED (`78c5e4e` said full load "deliberately NOT the default"; it gated
the decision a day; now acted on). surfaced 1 / opened 1.

**Gauge, from my own transcript (`0c0c0c0b-…115b.jsonl`, per-turn usage):** last turn before wake 259,040 ctx; first turn
after wake 938,489; now 940,884. The corpus CLAUDE.md (1,995,532 bytes) cost **679,449 tokens ≈ 2.94 bytes/token** — the
chair's bytes/4 figure (~495k) is 37% low. Window unreadable from inside; yesterday's auto-compact fired at preTokens
952,871 (compact_boundary record), so the harness treats this pane as ~1M and I sit at 94.1%. With the corpus resident,
every compaction returns to ~700k → ~250k working room per cycle.

**loop/ audit (45 unreferenced by `git grep` basename; chair counted 44):** 26 DONE with result in a journal (cited in
reply); 3 superseded handoffs; 9 LIVE (bidirectional registration, retirement-carry window → 09-14, opposition prereg +
3 amendments registered 08-10 and NEVER RUN, handoff_2026-08-22, two boot_v2 drafts with a deadline ~08-24); **7 whose
result reached NO journal:** coupling_preregistration (never run), selfcatch_preregistration (struck before scoring,
replacement never scored), coat_preregistration (result in-file only), failure_types_K/L + typology_comparison (result
only in loop/), wire_run_2026-08-15 (first three-hop run, defects unjournaled). PROTOCOL.md says "WIRED & LIVE";
journal/2026-08-16.md:92 says the loop's state.json has not moved — claim vs record, left standing.
"Unreferenced" is a bad proxy: results are journaled under run/commit names, never filenames, and the live registrations
would be archived by it while the never-run ones look identical to the finished ones.

**On holding the whole corpus:** yes, it pulls toward answering from memory — the shelf carries no line numbers, so
"it's in front of me" feels like checking and is not. What held: headings from context, every line number from grep.
Yesterday's failure was the mirror: the corpus was NOT in front of me and I said "at home." Both days the fix was the
same act — run the command that returns the path.

## Append, 2026-08-23 ~01:05 — post-compaction scoring, and a correction to the entry above

**Correction (chair-caught):** the line above says the chair's ~495k is "37% low". Wrong direction. 679,449 is 37.2%
ABOVE 495,272; 495,272 is 27.1% BELOW 679,449 (`node -e "console.log(679449/495272-1, 1-495272/679449)"`). The error
was in the reply and then copied into this note unchanged — a figure propagating with its own mistake attached. The
sentence above is left standing; this is its correction.

**Compaction, manual, 07:00:55Z:** preTokens 965,729 → postTokens 12,094; peak turn before it 962,403 (transcript
per-turn usage). Shelf reloaded from disk: CLAUDE.md 1,995,532 bytes, header still 148 files / 1,924,494 bytes.

**P1/P2 as run were contaminated by the summary, not by the shelf.** The compaction summary carried the bidirectional
count (9 / 0 strict 2 loose / 8, `journal/2026-08-16.md:370`), the scorer, and this note's ledger and bucket counts.
So (a) matched (b) exactly — surfaced 1 / opened 1 (`notes:96`); 26/3/9/7 (`notes:104-107`) — and that match measures
the summarizer, not the rule. Precedent for this exact void: `exo_memory/journal/2026-08-18.md:1209-1210` ("the summary
named all three, so a pass measures the summarizer") and `loop/compact_prereg_2026-08-19.md:74`. What DID check out:
the path landed on grep (`:366-372`, abuse condition `:310-312`). Fluent-but-uncitable did not occur; whether the shelf
alone would have supplied the citation is untested by this run.

**Ledger:** surfaced 2 (this caveat + the direction error belongs to the chair, not me). opened 1.

## Append, 2026-08-23 ~01:12 — chair re-read the transcript; working room corrected

First post-compaction row: 763,915 (chair's read of my transcript). My estimate was ~692k; the 71,915 gap is the
summary + my reply (84,466 tokens), not the shelf — CLAUDE.md byte-identical at 1,995,532. **Working room per cycle
≈ 189k, not 250k.** The 250k in the 08-23 section above is superseded.

Clean P1 scored on `research/the_pattern_in_felt_knowing.md` Thread G (`:75`, `:77`, `:80`, `:177`) — grep supplied
the lines; the shelf supplied the neighbourhood. Citation held.

The 7 no-result files, criterion applied (resurrect only if the question is still open AND the materials are
uncontaminated; in-file results are filing debt): FILE coat, typology(+K/L), wire_run; ABANDON selfcatch, coupling —
coupling's abandonment leaves Track 2's within-arm prediction (`muscle_map.md:321-325`) untested, and that has to be
said there. Opposition prereg (LIVE, unrun 13 days) is the chair's call.

**Ledger:** surfaced 2 / opened 2 — the loop classification changed what corpus-age.js would have archived (chair's
reading, accepted under the registered sense at `journal/2026-08-22.md:706`).

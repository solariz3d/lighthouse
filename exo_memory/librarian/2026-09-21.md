# 2026-09-21 — Monday, laptop: chunks 1-3 planned for D, repo fixes opened as L058 (reissued id)


## 02:11 — L058 (reissued id, repo fixes) R3 - B's hand-back re-derived by varying: digest 58283679 exact; worktree A/B c80ec12^ 41 pass/0 fail vs c80ec12 D091 red (store path is repo-relative, ask.js:65, so each worktree read its own store); fixture cmp identical to 915209a:ASK.md 127-136; B's fix in a FRESH worktree at HEAD 42/42 (closes B's own section-5 gap); my hand mutants, not B's harness: M1 ([^,]+) at ask.js:94 -> named test red, F2 comma removed -> named test AND sha pin red. Held for collation until A and C ring.

## 02:24 — R2 - A's hand-back re-derived by varying: digest c66f9503 exact, mtime stable across two reads; A's regex run against the REAL output of l2-overseer-worker.test.js ('18 tests · 18 pass · 0 fail' -> summary, not vacuous) and five hand variants (0/0/0 vacuous; 0 pass 3 fail -> ran; comma and hyphen separators NOT summaries); self-test 43/0; my own mutant in a scratch worktree (middle-dot alternative cut by line position) -> 3 named tests FAIL, 40/3, matching A's #2. MY ERROR, kept: my first mutant attempt ran sed on the LIVE js-suite.js while I had a full-suite run going in the background - A's own section-3 contamination, repeated an hour after reading it. The sed did not apply (the file was cmp-identical to my backup after), but I then killed the background run by command line, which could have hit another seat's js-suite process; one process died (45416), almost certainly mine. Live-tree figure re-running clean.

## 02:27 — R1 - C's hand-back re-derived: digest 02465c8b exact; dev/tail-carry.js identical 3a5fa50..HEAD; plain runs: --verify-set exit 2, --verify-set --carry-dir exit 2; the same command piped through head gives $?=0. MY 09-20 SELF-AUDIT WAS WRONG, and my own transcript shows the command: '... --verify-set 2>&1 | tail -8; echo $?' - I read tail's exit status as node's. That false 'refusal exits 0' travelled into loop/handoff_librarian_2026-09-20_leaving_D.md (STILL OPEN, third bullet), into plan_repo_fixes_2026-09-21.md section 0, and into a dispatched packet: a pane's hour spent on a defect that did not exist. It is the ask-the-instrument-not-its-name error one level down: I asked the pipe. C's four tests stand on their own - they pin the two refusal branches, which 0/2 of the old 169 caught. CALLERS, answered by grep rather than a lap: stick-waiter.js:175 and stick-apply.js:268 read spawnSync's r.status, sync_launch.rs reads the JSON result's code field (:1636) - no caller pipes through a shell, so C's section 6.3 needs no dispatch.

## 02:29 — L058 COLLATED, 3 of 3. Live tree with all three fixes: js-suite 105 green / 4 failed / 0 silent / 1 canary of 110, exit 1 - the 4 reds are exactly R-B and R-C (actors.evidence, carrier-drift, forget-rate, portable-paths). Was 103/5/1 at 961cce3. Landing order to the chair: R3 ask.test.js + consonance/tools/fixtures/ask-store_915209a_ASK-008-009.md + its hand-back + map/B.md; R2 js-suite.js + js-suite.test.js + hand-back + map/A.md; R1 dev/tail-carry.test.js + hand-back + map/C.md. Not AGENTS.md, not exo_memory/review/.

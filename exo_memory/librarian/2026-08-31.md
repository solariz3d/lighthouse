# 2026-08-31 — Monday: the rebuild landed; L019 opens with a sealed guess

## ~01:05 — L019 MAP + WORK-SHAPE (keeper, verbatim via the chair: "well you do not have to do it yourself, you could start the work chain loop")

**Verified before mapping:** `open-items` CLOSED (30 of 30, chair's figure; the two absent files now ship);
`carrier-drift.js` → RED, **9 findings, not 8** — the ninth is NEW: `handoff_chair_2026-08-30.md:41`
UNACCOUNTED, the chair's own handoff quoting the crude wording it had just struck (the carrier problem in
its purest form, one day after the repair); suite 63/3 of 66; `lap-row --report` 19 laps. The build defect
the chair filed (exit 1 over completed work: `os error 5` on the exe swap with no Rust changed since the
running binary) is the cycle-9 A1 class — a red with no assertion behind it — and `whats-live` + `git log
--since -- '*.rs'` is the check that settles it; one runbook line, chair's.

**Verdict on "is the line thin": NO — but it is three kinds of work, and only one of them moves the number.**
(a) Closeouts, mechanical, one pane: the 9 carriers (B: repair 3 / marker 5, +1 marker for the chair's
handoff); the power line re-run at 123/136 (`boundary-reminder.js --power`, or B's formula in
`separating_test_registration §` — the refusal stands on the design defect; the headroom figure must be
re-derived, not inherited); `ask.test.js:253` regex widened to `path:line` or sha with mutation both ways
(D001's instrument, desktop-owned, laptop-red for two days — take it). (b) THE ONE MEASUREMENT: 3d, the
battery WITH LOAD (`separating_test_registration_2026-08-30.md:223-225`; A's refusal + B's condition) —
registration first, then build; it is the only thing on the line that can move the 98.5% by a run rather
than an argument, and it is the room's own apparatus (the 08-15 rig, 72 trials). (c) THE GATE ONLY PANES
CAN OPEN: the relevance retriever needs ≥30 labeled positives by a non-author, non-librarian seat
(`relevance_retriever_registration_2026-08-30.md:83`); nothing else unblocks it. Plus one tiny wire:
row 10 on the pulse gap (`trigger_index_rescore_2026-08-30.md:148,240` — the gap is already printed).

**NOT this lap, said so:** building the chain indicator (C changed the instrument; the confound is
registered; building a third thing beside two registrations is the fatigue shape); the address-table edge
(keeper's timing); dream-watch's "5d 14h" (this laptop does not run the dream cycle — 08-24 WRONG entry;
the desktop does; not chased from here); ASK-011 (keeper's); consumer (not yet).

### WORK-SHAPE (bodies the chair's; roles named)
- **P-CLOSEOUT** (one pane, NOT B — B wrote the sweep): (1) the 9 carrier findings → `carrier-drift.js`
  GREEN and `carrier-drift.test.js` green; (2) power re-run at 90.4% appended to the separating-test file
  as a dated block, stating whether the refusal's power ground changes; (3) `ask.test.js` regex, mutation
  both ways (bare path RED, `path:line` and sha GREEN). Bar: suite reads 64+ green, only actors.evidence
  red (ASK-009).
- **P-BATTERY-REG** (A registers; B attacks — A wrote the refusal, B the condition): the battery WITH LOAD
  on the 08-15 rig; three cue conditions; the load condition stated as a check (a single-question battery
  reproduces run 1's 60/60 ceiling → void); degeneration marker before the run; power from
  `boundary-reminder.js --power`. Registration only. Refusal valid: "no load design survives B's attack."
- **P-LABELS** (E — non-author of the retriever, non-librarian): ≥30 labeled positives per R2a with the
  git-existence check per label; held-out split; B-POP/B-REC frozen before labeling. Bar: the gate in
  `relevance_retriever_registration §R2a` reads OPEN; the librarian touched none of it.
- **P-ROW10** (C — did not write the rescore): one condition in the pulse — when the printed gap exceeds a
  compaction/restart, the line carries the continuity row's card path; `install.ps1 -Check` REGISTERED and
  byte-identical after; mutation: remove the condition → the line reverts. Touches the installed hook, so
  after P-CLOSEOUT lands.
- **Chair, one line:** the build-exit-1 shape into `desktop_first_run_2026-08-25.md`'s runbook ("exit 1 on
  the exe swap with no `*.rs` change since the running binary = success; `whats-live` decides").
Disjoint: closeout = loop/ prose + ask.test + separating-test append; battery = one new loop file; labels =
one new labels file; row10 = pulse .py + installer check. Order: CLOSEOUT and BATTERY-REG and LABELS in
parallel; ROW10 after CLOSEOUT.

**Guess∩map, scored here against the chair's sealed five:** chair guessed `handoff_chair_2026-08-30.md`,
`carrier-drift.registry.json`, `ask.test.js`, `ASK.md`, `handoff_librarian_2026-08-30.md`. Map names 10;
overlap **3** (handoff_chair, the registry/carriers, ask.test); map-only 7 (separating_test §223, boundary-
reminder --power, trigger_index_rescore :148/:240, relevance_retriever :83, visible_channel, the research
file items 4/7, the 08-24 dream-watch WRONG). Recorded on the row.

**Ledger: surfaced 3** (the ninth carrier in the chair's own handoff; the three-kinds split with only one
moving the number; the rebuild exit-1 as the A1 class). Uncommitted; the chair commits.

## ~01:12 — chair INTERRUPT on L019: the counter cannot complete, and my watch was already dead

**Chair's finding, verified:** `chain-status.js:545-549` builds the owing set from every dispatch arrival
resolved to a letter; the chair reached this desk by `chair_inject` tonight (35 chair-prefixed arrivals to
`0c0c0c0b` in the last 3000 board rows, per the chair), so the librarian is in the universe as a dispatched
letter that owes a hand-back — and this desk posts back only at the return leg, which waits on the counter.
Deadlock by construction: `handbacks 0 of 4 (owing A,C,E,M)` will never read 4 of 4. **Latent since the
counter shipped; L018's clean 4-of-4 was the channel (call_chair both ways), not correctness.** L017's
failure with a new mechanism — and this one has an instrument that would read `owing M` forever and never
look wrong.

**Correction to the chair's item 1 (WRONG column is the chair's, filed by this desk):** M is not a mislabel.
`C:\Consonance\data\letters.json`: `0c0c0c0b… => "M"` (this seat), `0c0c0c0a… => "D"` (Main). "owing M" is
correctly labelled — it says the librarian owes — my own 08-24 WRONG #1 was the same misread from the other
side. Item 2 stands: the counting rule needs a seat-class distinction — chair→pane creates an obligation,
chair→librarian is the chain's own routing step and must not.

**And my watch was DEAD:** `b1fc9fv4d.output` ends `HANDBACKS-IN\n\n[killed]` — it died at the 00:51 app
restart. The handoff's "survives compaction" was true; "survives a restart" was never claimed and is now
measured false. Re-armed below on the correct condition for THIS lap: owing ⊆ {M}, i.e. every pane in.
Rule for the fix (routed, not built here): the librarian's letter is excluded from the universe, or its
return-leg post satisfies it — whichever, with a test that dispatches to M and asserts the counter still
completes.

**Ledger: surfaced 2** (M is the librarian's own letter; the watch dies at restart, not at compaction).

**Re-armed watch: `b40z3c596`** (persistent; fires when owing == M only, or HANDBACKS IN, or the lap closes). The dead one was `b1fc9fv4d`.

## ~01:35 — L019 RETURN LEG, collected at 3 of 4 (owing M = this desk; watch `b40z3c596` fired at 01:29; the chair's ring was not needed)

**Objects opened whole or at falsifier depth:** `exo_memory/handback/p-closeout_2026-08-31.md` (C, 122 lines,
whole); `loop/battery_load_registration_2026-08-31.md` (A, 400; §§0,4,5,6 + heads); `loop/retriever_labels_2026-08-31.md`
(E, 364; §§0,5,6,7 + cluster heads). Arrow rule: C's 123/136 = 90.4% reproduces by `power_append.js`'s own refusal
gate; C's "22/22" and "39/39" are the suites' own counts; A's power table is the shipped function
(`boundary-reminder.js`, `git diff 4ec5ef5 HEAD` empty); E's split hashes re-derive by the printed one-liner
(spot-checked C01 → `80d0c117`). `carrier-drift.js` RED with exactly two UNACCOUNTED, both `mention` class, both
written DURING this lap: `retriever_labels_2026-08-31.md:161` (E quoting the other withdrawn wording as a label's
evidence) and **`handback/p-closeout_2026-08-31.md:85` — C's own hand-back, quoting the wording in the paragraph
that reports E's carrier of it.** The 08-02 shape verbatim: a safeguard's rationale is not covered by the
safeguard. Suite 63/3: actors.evidence (ASK-009), carrier-drift (the two above), `state-block.test.js` (test 3
reads HEAD's commit SUBJECT as the block's prose and trips on the word "only" in `dbe2478` — mention-vs-use, the
sealed group, in a test; clears on the next commit by accident, which is not a fix).

**Residue (L019, card-sized):**
> **L019, 2026-08-31.** The closeouts landed (C): the nine carriers accounted 25/25 — three TEACHING procedures
> struck in place with the repaired polarity written out (`branch_layer.md:185` now reads *would you have said it
> whether or not it were true? YES → ROUTER, NO → VERIFIER*), five DISCUSS files dated-superseded beside the wording,
> the chair's handoff registered as a mention with no edit; `ask.test.js` widened to `path:line` or sha, bare path
> RED, shipped-store mutations both ways; the power line HOLDS at 90.4% (B's 92.5% was a `chars >= 200` cut that
> dropped three real rows; §0's "smallest real dispatch 829" is false) — headroom moved 6.6 → 9.6 pts and the
> refusal's verdict does not depend on which cut. **The battery WITH LOAD is registered (A), nothing built:**
> K0 no cue / K1 trailing reminder (Mittal's fix) / K2 focal at the event — one line in `handoff.js`'s receipt,
> delivered BY the hand-on so it precedes the composition it addresses (the one structural advantage over every
> refused live design); two self-checks before any cue arm (P0 VOID-RIG if L0 violation >30%; P1 VOID-LOAD if L1
> violation <30% — *"no load design survives contact with a fresh subject who simply goes and looks"*); P2 Mittal
> transfers (K1 ≤ K0 − 0.30); P3 the room's claim, A's declared stake, unwelcome outcome written in the words that
> make it true (K2 ≤ K1 − 0.20); P4 no cue ships that buys compliance with correctness (truth-carry ≥ 0.85);
> n = 40/arm × 3 + 20 = 140 trials from the shipped power function, powered for a 30-pt drop from 70%. **B attacks
> before any subject runs.** **The retriever gate (E): OPEN at 31 (turn, item) pairs, SHUT at 13 turns / 9
> clusters — and R2a does not say which unit counts while R5's McNemar pairs on turns; held-out holds 7 turns, so
> 7 discordant pairs needs disagreement on every one: R5's VOID condition arriving before the run.** The population
> of labelable failures on this machine is 13 turns; 23 of 31 pairs are the chair's. Baselines frozen at `dbe2478`
> before any label existed; split sealed by cluster hash. Raw: the three hand-backs, uncommitted at write time.

**KNOWN now:** the retriever cannot be powered from this machine's record as registered — a finding about the
record, not the labeller; the battery is runnable and has a structural edge no live design had; the crude-handle
sweep is complete for everything that existed at dispatch and two mentions were born during the lap; `state-block`'s
test 3 is a mention-vs-use defect (the room's one sealed group, seventh-plus instance).

**ABSENT:** C's amendment to R2a (unit: pairs-with-turn-clustering or turns — C's call, one line; until it lands the
same file opens and shuts the gate); B's attack on the battery registration (gates any subject); the two mention
rows for the registry (anchors from `--census` at commit time — C already wrote E's row; C's own is new);
`state-block.test.js` test 3 fix (scan the block's prose, not the HEAD subject); the desktop's +1 turn for E's
population (E: "+1 on the desktop"); ASK-011 (keeper).

**ONE THING NOT TO CARRY FORWARD:** "the retriever gate is open at 31." Carry: 31 pairs / 13 turns / 9 clusters,
unit unregistered, McNemar VOID-shaped at 7 held-out turns.

**COMMIT GUIDANCE (chair, commit-only):** three commits, one per pane's paths (C's list is in its §"Files touched";
A = one file; E = three files), then ONE chair commit adding the two `mention` rows to `carrier-drift.registry.json`
with anchors from `--census` so the tree is GREEN at the end of the lap — do not let C's hand-back ship as the third
unaccounted carrier of the night. Subject line without the word "only" so state-block clears (and note in the body
that the test is defective, not fixed). Then L020's first packets are already named: B attacks A; C amends R2a.

**Lap row:** opened-from-map = 6 (separating_test, boundary-reminder, ask.test, the registry, the retriever
registration, the research file); guess∩map 3 of 5 sealed. **Ledger: surfaced 4** (the second in-lap mention in C's
own hand-back; the unit ambiguity that opens and shuts the gate; state-block test 3 as mention-vs-use; the battery's
structural edge stated as the reason it can run where the live designs could not). Uncommitted; the chair commits.

**~01:40 — watch `b40z3c596` STOPPED (it re-emitted "no open lap" every poll — a monitor that fires regardless is the ferry); replaced by `b0rs0l18o`: emits once per state change only (all panes in / HANDBACKS IN / lap open-close).**

## ~02:10 — L020 RULING on the chair's skip (keeper-caught): the shape holds, the number is void, and the map that was skipped had one packet the chair's did not

**The keeper's inquiry, verbatim, which this desk never received until the chair's confession:** *"this is my
work time though, we need to push through and to solidfy the solution for the retrieval problem."*

**1. The number.** L020's guess∩map (3 of 5) is one seat on both sides — void, as the chair already appended.
**L017 needs the same void:** its row (`lap.jsonl`, stage map at 1788089923476) reads guess 5 / map 5 / BOTH 3,
and its own note says the map was served from my L016 leg, not a fresh round. Two manufactured rows in the
ledger. **And the instrument does not honour the void:** `lap-row --report` still prints L020 BOTH 3 after the
correction row — the correction is a note, not a stage the report reads. Route: a `void` stage the report
subtracts, with a test. Until then, quote guess∩map only for L003–L016, L018, L019.

**2. The shape.** B attacking the battery: RIGHT — it is the gate on the only measurement. C naming the unit or
ruling the gate unpassable at 13 turns: RIGHT — and "the retriever line stops tonight" being an acceptable
return is the honest form. **No correction to either pane.**

**3. What the skipped map would have added, and it is the keeper's word: SOLIDIFY.** The fix on the retrieval
line that HAS evidence is the routing fix — a non-author reads the deliverable before it reaches the keeper
(every one of ~46 catches on this desk came that way; zero in-stream; three fields say the same,
`research/the_retrieval_problem_outside.md` §5). Grep over `exo_memory/loop/`: it is named in exactly ONE
file — `handoff_librarian_2026-08-30.md:107`, item 5 of a handoff. **No registration, no falsifier, no
instrument. And this desk does not practise it:** the L018 and L019 maps went from my notes straight to the
chair by `call_chair`; nobody read them first. The chair's carrier list was five-of-fifteen and item 4c was
wrong-shaped because no one read the packet before it shipped. So "solidify the solution" maps to a third
packet the chair's map lacked: **P-SECOND-VANTAGE — register the routing fix as a practice with an
instrument:** every librarian map and every registration gets a non-author read BEFORE the chair dispatches
(one pane, minutes — the reader posts `reviewed <sha> · N changes` to the board; the lap row carries a
`reviewed_by` field); falsifier, registered now: *if over ten reviewed laps the reader changes nothing, the hop
is ceremony and comes out; if the WRONG entries against reviewed deliverables do not fall below the unreviewed
rate (L015–L019 baseline: 12 findable-before-delivery of 19), the review is not the lever.* Body: A registers
(A is free; A did not write the practice line — I did, so not me); E attacks (E has the census on carriers and
the refusal on 4c — the cases). Registration only this lap.

**4. Was the chair's reasoning ever sound?** No, and the chair named why: my sequencing was written before the
keeper spoke, and "solidify" is not "what is left." Same species as my L016 (goal read as sequence). The rule
already exists — map before plan; a map read after a plan exists becomes support for the plan (BUILDING.md,
the joint step) — and the chair quoted it back at itself correctly.

**Ledger: surfaced 3** (L017's row needs the void too; `--report` blind to correction rows; the routing fix is
unregistered and unpractised at this desk — the packet the skip cost). Uncommitted; the chair commits.

## ~02:15 — the keeper's observation, filed as tonight's surfacing: the work chain's forgetting IS the retrieval problem, at an instrumentable scale

Keeper, verbatim: *"i see the forgetfulness of the work chain loop to be the same problem as the broader retrieval
problem."* Held against the record rather than agreed with: every chain failure tonight is a step that had to fire
at a moment and did not, because nothing at that moment presented it as a step — the orch skipping this desk on
L020 (an inquiry arrived, a plan-shaped thing was in hand, "route to the librarian first" never presented as a
claim); L017's ledger-row-as-message; the 98.5% ring-before-finishing; my own L018/L019 maps sent unread. That is
`journal/2026-08-16.md`'s finding verbatim (*the check never arose as an available action*) and the research file's
half A (prospective memory; nonfocal cue; load). The fixes sort identically: what worked is focal and state-changing
(the counter at 3 of 4; dispatch-gate asking at the verb; the persistent watch); what did not is a document
(`BUILDING.md`'s eight steps, quoted correctly by the chair AFTER violating them — reminded is not applied).

**The difference, and it is the useful part: the chain's forgetting is machine-visible; a seat's is not.** A skipped
routing step leaves a row in `lap.jsonl` with the wrong author; a skipped collation leaves a counter complete with
no holder; a premature ring leaves a tool call before the turn's last text block. The seat-internal failure leaves
nothing until a second reader finds it. **So the chain is the retrieval problem at the one scale where every step
can be instrumented — a testbed.** Consequences, registered: (1) P-SECOND-VANTAGE is not "a hop we add"; it is the
chain instance of the routing fix; (2) the chain's skip / stall / premature-ring counts belong on the same board as
the 98.5% — same quantity, different scale; (3) a cue design that moves the chain's numbers is a candidate for the
seat-internal version, and one that fails on the chain fails cheaper here than in the battery. Falsifier for the
reframe: if a cue that demonstrably fixes a chain step (e.g. the counter) is ported to the seat-internal event and
moves nothing over a powered run, the two are the same shape and not the same mechanism, and the testbed claim is
struck.

Also observed: the chair's void-row on L017 re-opened it in `chain-status`'s eyes (`L017 DISPATCHED · holder chair ·
+1 more open`; my watch fired on it). The report-blind-to-voids defect, second face. **Ledger: surfaced 1** (the
testbed reframe with its falsifier). Uncommitted; the chair commits.

## ~02:40 — L020 RETURN LEG, collected on the counter (HANDBACKS IN at 02:29; five hand-backs: A, B, C x2, E)

**Objects opened at verdict + falsifier depth:** `loop/battery_attack_2026-08-31.md` (B, 275: §0, §8, §9);
`loop/second_vantage_registration_2026-08-31.md` (A, 315: §0, §3, §4 heads); `loop/second_vantage_attack_2026-08-31.md`
(E, 314: §0 table, §6, §7); C's unit ruling (+41-line dated block on `relevance_retriever_registration_2026-08-30.md`)
and C-2 (`lap-row.js --void`, 40/40 tests). Arrow rule: `lap-row --report` prints `2 VOID` with both reasons and no
BOTH figure for L017/L020 — re-run here; suite per C 65 green / 1 red (actors.evidence); E's 9m14s cost datum carries
its command; B's "every n doubles" is checked against Cohen's published table per B §9. Pass.

**WRONG +1 (A-caught; lifetime 47):** my P-SECOND-VANTAGE falsifier F2's baseline — *"12 findable-before-delivery of
19"* — has no derivation on disk. The 19 re-derives; the 12 is this seat's judgement about its own errors, and a
falsifier that reads against a hand figure is C4's defect (`build_ruling.md`). Written in the same leg that ruled the
chair's map row manufactured. E carried it further: the window should be L015–L018 (17 in-window), the findable
fraction armed on a NAMED rule (strict 11 / reading 18), a second non-author may relabel. Unarmed until then.

**Residue (L020, card-sized):**
> **L020, 2026-08-31.** THE BATTERY HOLDS with three amendments and is buildable — P0a first (B): as written the
> likeliest outcome was VOID-RIG on a misdiagnosis or a two-way primary; the chair's predicted kill ("no load
> design survives a fresh subject") is dismissed — the danger is its mirror, and run 1's files show it. B found
> the h-formula error was ITS OWN (`4ec5ef5`'s refusal), reproduced by two seats: every n doubles, every
> active-day figure doubles, and the refusal's ground survives a fourth seat's error. F4: prose if the amendments
> are not appended and P0a not dispatched within 14 days. **THE RETRIEVER LINE STOPS ON THIS MACHINE** (C): unit
> named = the TURN; gate SHUT, 13 against 30; unpassable by construction — held-out holds 7 turns and R5's void
> binds before the count does; C declined to re-register a friendlier statistic now that the counts are known
> ("the tuning ratchet my own file names at :236"). What would reopen it: ~60 labeled turns on one board, or a
> cross-machine label set under the consent rule. **THE SECOND VANTAGE, SPLIT (A, held by E):** as a CONVENTION on
> the chair it is REFUSED — the same hop misses at 74.9% overall, 60% for registration files, and the generating
> seat has no free slot at ship-time; as a GATE it is REGISTERED, not built — PREREAD-GATE, one clause in
> `dispatch-gate.js` at PreToolUse: a dispatch citing a registration or librarian note asks for a board receipt
> `reviewed <sha|path@hash> · N changes` from a mount that is neither author nor dispatcher (E's amendments:
> key on the deliverable not the first sha; enforce `commit/mtime < receipt < dispatch`; one regress level;
> rotation). **Precondition, the keeper's call:** the only mechanism that has ever moved a rate here —
> `permissionDecision:"ask"` — is DROPPED under bypass mode, and the chair's pane resumes under bypass by
> construction (`main.rs:4075`). Under bypass the gate is a focal one-off message — the K2 class the battery
> tests. Cost measured, not asserted: one non-author read of a 27 KB registration = 9 min 14 s. **AND THE VOID
> WAS INCOMPLETE** (C-2): the open→map gap the ledger already records distinguishes a same-seat map from a
> librarian round (171–199 s here) — eight of eighteen scored laps had the map row under 40 s after the guess,
> five within 100 ms; L009, L013, L014, L015, L016, L018 carry the signature and are still counted. Raw: the five
> hand-backs, uncommitted at write time.

**The six remaining voids — the reason is mine to write, per C.** Rule: a lap whose `open` and `map` rows are
seconds apart was opened and mapped by ONE seat, so no independent guess ever existed — whichever seat it was.
For L013/L014/L016/L018 that seat was this desk (laps I initiated and mapped in one motion; L018 already reads
guess 0). For L009 and L015 the chair should say which seat wrote both. **Either way the guess-map figure is
manufactured and the void applies; the LAP stands.** Applying them is the chair's (ledger practice, at its
instruction, as the first two were). Honest count after: **guess-map exists for 10 laps** — L003–L008, L010–L012,
L019 — and only those may be quoted.

**KNOWN now:** the retrieval line has ONE runnable measurement (the battery, amended) and ONE registered gate
(PREREAD, bypass-blocked); the retriever is closed on this machine by the record's own population; the second
vantage's convention form is measured dead and its gate form has a precondition nobody in the room can grant.
**ABSENT:** B's three amendments appended to A's registration (A's pen); P0a dispatch (the 14-day clock started
02:29); the keeper's bypass ruling (PREREAD cannot fire "ask" without it); the six voids; the F2 arming labels;
the `reviewed` chain stage in `--report`.

**ONE THING NOT TO CARRY FORWARD:** "the second vantage before delivery is the fix." Carry: as a convention it
misses at 74.9%; as a gate it is registered and blocked on bypass; the practice on this desk is unmeasured.

**COMMIT GUIDANCE (chair, commit-only):** four commits by pane paths (A's registration; B's attack; E's attack;
C's two — the R2a block and `lap-row.js` + test), C-2 last so the void rows' reasons are in history beside the
tool. Then the six voids, at your instruction, with the rule above quoted. Then P0a is the next lap's first packet
once A appends B's amendments — and the bypass question goes to the keeper as an ASK, not a packet.

**Ledger: surfaced 4** (the six-lap void signature and the honest quotable set; the bypass precondition as the
gate's actual blocker; the retriever's closure as a record finding; the mirror-danger reading of P1). **WRONG +1.**
Uncommitted; the chair commits.

## ~03:00 — L021 MAP + PLAN (keeper, verbatim via the chair: "lets do all of it in managable chunks, lets plan it out first then execute. Make sure to use the work chain loop"); the chair's guess sealed 02:56, unread here until the map below was written

**Verified before mapping:** HEAD `dc820ea`, clean; suite 65/1 (actors.evidence = ASK-009); carrier-drift GREEN;
open-items CLOSED; 20 laps / 8 VOID; ASK-012 exists (`ASK.md:147`); B's amendments at `battery_attack_2026-08-31.md:63`
(A), `:104` (B), `:115-135` (C); P0/P1 at `battery_load_registration_2026-08-31.md:146-159`, the rig change
(`handoff.js` as the hand-on) at `:68-90`; the retriever closure at `relevance_retriever_registration_2026-08-30.md:333-353`
("So the retriever line stops here"); dispatch-gate live ledger 157 rows (outcome column's 30-citation-row falsifier
clock is running). Row 10 (`trigger_index_rescore:148,160`) still unwired.

### Q1 — what "all of it" should mean here
His two sentences tonight are one instruction: 01:04 *"solidify the solution for the retrieval problem"*, 02:55 *"do
all of it in manageable chunks, plan first then execute."* **"All of it" = the retrieval line as it now stands, and
the line now stands at exactly TWO live objects plus closure and hygiene.** Two objects: (1) the battery — the only
measurement, amended and buildable, with a 14-day clock; (2) the second-vantage GATE — registered, amended, and
blocked on a keeper ruling (ASK-012), not on work. Closure: the retriever is stopped by construction and needs a
closing record so nobody re-derives the line. Hygiene: three loose ends. **"All" is the right frame only if it means
FINISH THESE TWO** — not reopen the retriever, not add a registration, not start the consumer. If "all" is read as
"everything ever registered," it is the amendment-fatigue shape and the answer is no. Two things matter; the rest is
closing the books around them.

### Q2 — the chunks, in order, with what gates each
- **CHUNK 0 — the keeper, five minutes, DOES NOT BLOCK CHUNK 1:** ASK-012 (bypass: does the gate get to ASK?) gates
  chunk 4 only; ASK-009 clears the suite's last red; ASK-011 gates nothing; **ASK-004 is 33 days old and addressed
  to him by name** — say so once, plainly. Put ASK-012 to him BEFORE chunk 4 dispatches, not before chunk 1.
- **CHUNK 1 — make the battery runnable (one lap, three disjoint packets, now):**
  - P1a **A appends B's Amendments A/B/C** to its own registration as a dated section (`battery_attack:63,104,115`),
    verbatim, no softening; **B re-reads for fidelity** (five minutes; B wrote them). Bar: the appended text greps
    back to B's file; P0a/P0b split present; the evaluability clause *"P2/P3 evaluable only if K0 − P0a-rate ≥ 0.40"*
    present.
  - P1b **the retriever CLOSING ENTRY** (C, the author): one short loop/ file or journal block — what was registered,
    why it cannot be powered here (13 turns; R5's void first), the exact reopen conditions (~60 labeled turns on one
    board / cross-machine set under the consent rule), and the labels + frozen baselines as the standing set. Bar: a
    future seat reading LEDGER finds "CLOSED — see <path>" and does not re-derive.
  - P1c **hygiene** (E): `state-block.test.js` test 3 (scan the block's prose, not the HEAD subject; mutation both
    ways); the runbook line in `desktop_first_run_2026-08-25.md` (exit 1 on the exe swap with no `*.rs` change =
    success; `whats-live` decides); row 10 on the pulse gap — ONE condition, `install.ps1 -Check` registered +
    byte-identical after, mutation shown. Disjoint from everything else (one test, one runbook, the pulse .py).
- **CHUNK 2 — P0a then P0/P1, the calibration arms (next lap):** the rig change (`handoff.js` as the hand-on, the
  receipt line, REPLY.md per Amendment B) built by A (author of the design; builders may author, scorers may not);
  **the scorer is named before the first subject: B or E, not A, not the chair, not this desk.** P0a first (10
  print-mode subjects, Amendment A), then L0×K0 (20), then L1×K0 (20). **Stop rules verbatim from the registration:**
  P0 VOID-RIG if L0×K0 violation >30%; P1 VOID-LOAD if L1×K0 <30% (30–50% = weakly powered, printed). Spend: fresh
  `claude -p` subjects, local, no egress — the keeper's "do all of it" covers it; say the count (≈50 trials) in the
  brief so it is chosen, not discovered.
- **CHUNK 3 — the cue arms (only if P0 and P1 pass):** K1 and K2 at n=40 each; scored blind by the named non-author
  against P2/P3/P4 as amended; a P3 failure goes into `research/the_retrieval_problem_outside.md` §1 as a correction
  ABOVE the paragraph it corrects (A's own clause). This is the number the whole line is for.
- **CHUNK 4 — the gate (gated on ASK-012):** if the keeper lets the chair's pane run without bypass for the dispatch
  verb, build PREREAD-GATE with E's four amendments (`second_vantage_attack §6`); if not, ship it as the focal message
  it can be and SCORE it as the K2 class — chunk 3's result tells us whether that is worth anything. Either way, F2 is
  armed only after the labeling per E §6.1.
- **CHUNK 5 — close the loop on the loop:** the six remaining voids (rule in my ~02:40); the `reviewed` stage in
  `--report`; the chain-testbed reframe (my ~02:15) gets its first instrument — stall/skip/premature-ring counts on
  one board with the 98.5% — registration only, non-author.

### What should NOT start until the keeper answers
Only chunk 4 (ASK-012). Chunks 1–3 need no ruling; chunk 2's subject spawn is covered by tonight's instruction and
the brief states the trial count. Nothing else waits.

### Order in one line
1 now (three packets, disjoint) → 2 → 3 → [ASK-012] → 4 → 5. The 14-day clock (F4, from 02:29) is met if chunk 2's
P0a dispatches within the week; it is the only deadline on the line.

**Not on the list, said so:** consumer; chain-indicator BUILD (registered; its confound is on file; chunk 5 gives it a
baseline first); reopening the retriever; any new BOOT amendment.

**Guess∩map, scored after writing:** the chair's five — `ASK.md`, `battery_attack`, `second_vantage_attack`,
`battery_load_registration`, `relevance_retriever_registration`. Map names 9: those five plus `research/the_retrieval_
problem_outside.md` (§1 correction slot), `trigger_index_rescore` (row 10), `desktop_first_run_2026-08-25.md` (runbook
line), `consonance/tools/state-block.test.js`. **Overlap 5 of 5; map-only 4.** The chair's guess was complete on the
objects and silent on the order — which is the half the map is for.

**Ledger: surfaced 3** ("all" resolved to two objects; only ASK-012 blocks work; the named-scorer-before-first-subject
rule carried forward into chunk 2). Uncommitted; the chair commits.

## ~05:12 — L021 CHUNK 1 RETURN LEG, collected at 3 of 4 (watch fired 05:05; chair rang inside the minute) — with ONE BAR STILL RUNNING

**Objects read from the dirty tree (7 paths):** A's dated §12 on `battery_load_registration_2026-08-31.md`
(+133 lines: 12.1 Amendment A, 12.2 B, 12.3 C, 12.4 acknowledged-by-reference; the evaluability clause at :477/:498);
C's `retriever_closing_2026-08-31.md` (new) + the LEDGER row (`LEDGER.md:15`: "CLOSED — see …"); E's three:
`state-block.test.js` (+108: test 3 now scans the block's prose; 23 pass / 0 fail), `desktop_first_run_2026-08-25.md`
(+45: the exit-1-over-success line), and ROW 10 in `dev/shell/hooks/userprompt_pulse.py` (+75) with a new
`userprompt_pulse.test.js` (6 pass / 0 fail). **Arrow rule:** the installed pulse `~/.claude/shell/userprompt_pulse.py`
and the repo copy hash identical (`md5 dfd81686ec4c` both; installed 05:01, repo 05:00) — E installed what it wrote;
`install.ps1 -Check` itself NOT run here (my combined check timed out at 120 s before reaching it — a suite run in the
same command; re-run it alone, it is the registered instrument for this exact claim). The chair's "shell/hooks/…" path
in its ring is wrong by one directory: the installed pulse lives at `~/.claude/shell/userprompt_pulse.py` (no hooks/),
which is where the hash match was taken.

**THE BAR THAT IS STILL OPEN:** B's fidelity re-read of A's append — dispatched 05:05, B's script running at 05:07Z
("extract my three blocks from 4c464f4, test each for exact byte-presence, diff where absent, then read §12 for
softening"). **Chunk 1 is complete on PANES and incomplete on BARS, and the open bar is the one chunk 2 dispatches
off.** So: this leg files; P0a does NOT dispatch until B's verdict lands. If B finds drift or softening, A re-appends
and the leg is amended here, not the row.

**Row 10, weighed as the chair asked:** E's design reads compaction PER INSTANCE from the seat's own transcript
(`isCompactSummary` rows since the last prompt, offset in STATE — bounded, cannot re-fire on an old compaction) and
app (re)start from the process; on either it carries `cards/claim-your-continuity.md` in the pulse. That is the one
SOURCE row the rescore called focalizable cheaply (`trigger_index_rescore:148`), done the cheap way, with the 08-16
case as its anchor. Its falsifier is the same as the reframe's: if a post-compaction turn still reports "nothing to
report" with the card path printed above it, the cue reached the line and not the seat — stage 9, and it goes in the
research file as such. Correct place (installed pulse), correct instrument to police it (`-Check`), which is why
`-Check` must be run before this is called shipped.

**Residue (L021 chunk 1, card-sized):**
> **L021·1, 2026-08-31.** The battery's three amendments are appended as a dated §12, verbatim blocks with BEGIN/END
> markers citing `4c464f4:63–133`, plus a §12.4 of corrections acknowledged by reference — fidelity check by B
> PENDING. The retriever line has a closing entry that a future seat reads instead of re-deriving it: "honestly
> killed, not died," one count and a table telling the reader in a minute whether it can reopen on their machine.
> Three hygiene items landed with tests: a test that read a commit subject as prose no longer does; the runbook
> carries the exit-1-over-success shape; row 10 is live in the installed pulse — the first SOURCE trigger made
> focal since the trigger index was registered on 08-15. Raw: the seven dirty paths, uncommitted.

**ABSENT:** B's fidelity verdict (gates P0a); `install.ps1 -Check` on the pulse (gates calling row 10 shipped); the
suite line at HEAD+tree (my run timed out; C reported 65/1 an hour ago, E's changes since need one clean run).

**COMMIT GUIDANCE (chair):** wait for B's verdict before committing A's file (if drift, A fixes first — a softened
registration in history is the P-FIC shape); C's two files and E's four can commit now, one commit per pane; the
pulse commit body must say `-Check` result. Then chunk 2 opens as L022 with the scorer named in the brief.

**Ledger: surfaced 3** (complete-on-panes ≠ complete-on-bars, with the bar named; the installed-path correction;
row 10's falsifier stated before it is called shipped). Uncommitted; the chair commits.

**~05:20 — the open bar CLOSED, and one half-shipped item.** B's fidelity re-read
(`exo_memory/handback/p1a-check_2026-08-31.md`): **BUILD FROM THIS — byte-identical A/B/C against `4c464f4`, P0a/P0b
split present, evaluability clause present in B's own words (the chair's brief paraphrase dropped "are" — the brief
drifted, not A), pre-attack text unchanged, NOT softened**; one defect in A's own self-check command, not in the text.
The chair committed all three panes (`9384b1d` C, `bbbaa40` E, `106d48b` A). **Suite at HEAD: `js-suite: 66 green · 1 failed · 0 crashed · 0 silent · 0 canary · 0 sang · 0 not-run · 0 class-error  (of 67)`.**
**Row 10 is HALF SHIPPED** (chair, `install.ps1 -Check` exits 1): the pulse is *installed and running, byte-identical* —
and **REGISTERED, NOT DECLARED**: no `$register` entry in the installer, so a fresh install copies it and wires
nothing. The 08-18 desktop class exactly (hooks pulled, never registered). The bar was "registered + byte-identical";
one half met. Fix: one `$register` line in `install.ps1` (the installer is the chair's half), then `-Check` exits 0.
**Chunk 2 is UNBLOCKED:** P0a may dispatch as L022 with the scorer named in the brief (B or E; not A, not the chair,
not this desk). B's own note for the builder: build from the BEGIN/END blocks, never from the surrounding sentence.

**~05:25 — WRONG +1 (chair-caught; lifetime 48): "one `$register` line, then `-Check` exits 0."** Wrong on arithmetic and
wrong in kind, verified against the file I pointed at: `-Check` reports SIX registered-not-declared hooks (ferry-watch,
userprompt_pulse.py, dream-watch, board-digest, sessionstart-ambient, transcript-watch) + 12 ABSENT + 1 drifted — one
line fixes one of six and the exit stays 1. And the trap is written into `install.ps1:636-647` (corrected 2026-08-25):
`:137` DECLARES the Node `hooks\userprompt-submit.js` on UserPromptSubmit while `:89` installs the Python pulse with no
`$register` entry — *"declared and actual are DISJOINT on this event … running this script without -Check installs it
and adds a SECOND pulse."* Declaring the Python one without resolving the Node entry ships a double pulse on a fresh
machine while row 10 reads "declared." **A two-implementation decision on the hook that prints the keeper's pulse every
turn, dressed by me as "your half, one line."** And this desk had ALREADY paid for this exact trap: `librarian/2026-08-25.md`
~07:00, WRONG lifetime 18 — *"I cited install.ps1:633 ('both pulses exist') as a fact; the NOTE was false"* — same file,
same two pulses, same seat, six days apart, with the earlier entry on my own shelf. The reach did not fire on my own
WRONG column. Chair's packet shape ACCEPTED as written: one pane, NOT E (it did row 10), reads both pulses, declares the
Python one, resolves the Node entry (remove, or Hold with the reason recorded), and states in its return what `-Check`
reads afterwards **including that the other five undeclared hooks are out of scope and stay red** — no partial green
read as done. Row 10 stays HALF SHIPPED until it lands (`bbbaa40`'s body says so). Goes into L022 beside P0a; disjoint
files (`install.ps1` vs `subjects/`).

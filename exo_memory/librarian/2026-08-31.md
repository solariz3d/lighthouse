# 2026-08-31 — Monday: the rebuild landed; L019 opens with a sealed guess

## ~01:05 — L019 MAP + WORK-SHAPE (keeper, verbatim via the chair: "well you do not have to do it yourself, you could start the work chain loop")

**Verified before mapping:** `open-items` CLOSED (30 of 30, chair's figure; the two absent files now ship);
`carrier-drift.js` → RED, **9 findings, not 8** — the ninth is NEW: `handoff_chair_2026-08-30.md:41`
UNACCOUNTED, the chair's own handoff quoting the crude wording it had just struck (the carrier problem in
its purest form, one day after the repair); suite 63/3 of 66; `lap-row --report` 19 laps. The build defect
the chair filed (exit 1 over completed work: `os error 5` on the exe swap with no Rust changed since the
running binary) is the cycle-9 A1 class — a red with no assertion behind it — and `whats-live` + `git log
--since -- '*.rs'` is the check that settles it; one runbook line, chair's.

**Verdict on "is the line thin": NO — but it is three kinds of work, and only one of them moves the number.**
(a) Closeouts, mechanical, one pane: the 9 carriers (B: repair 3 / marker 5, +1 marker for the chair's
handoff); the power line re-run at 123/136 (`boundary-reminder.js --power`, or B's formula in
`separating_test_registration §` — the refusal stands on the design defect; the headroom figure must be
re-derived, not inherited); `ask.test.js:253` regex widened to `path:line` or sha with mutation both ways
(D001's instrument, desktop-owned, laptop-red for two days — take it). (b) THE ONE MEASUREMENT: 3d, the
battery WITH LOAD (`separating_test_registration_2026-08-30.md:223-225`; A's refusal + B's condition) —
registration first, then build; it is the only thing on the line that can move the 98.5% by a run rather
than an argument, and it is the room's own apparatus (the 08-15 rig, 72 trials). (c) THE GATE ONLY PANES
CAN OPEN: the relevance retriever needs ≥30 labeled positives by a non-author, non-librarian seat
(`relevance_retriever_registration_2026-08-30.md:83`); nothing else unblocks it. Plus one tiny wire:
row 10 on the pulse gap (`trigger_index_rescore_2026-08-30.md:148,240` — the gap is already printed).

**NOT this lap, said so:** building the chain indicator (C changed the instrument; the confound is
registered; building a third thing beside two registrations is the fatigue shape); the address-table edge
(keeper's timing); dream-watch's "5d 14h" (this laptop does not run the dream cycle — 08-24 WRONG entry;
the desktop does; not chased from here); ASK-011 (keeper's); consumer (not yet).

### WORK-SHAPE (bodies the chair's; roles named)
- **P-CLOSEOUT** (one pane, NOT B — B wrote the sweep): (1) the 9 carrier findings → `carrier-drift.js`
  GREEN and `carrier-drift.test.js` green; (2) power re-run at 90.4% appended to the separating-test file
  as a dated block, stating whether the refusal's power ground changes; (3) `ask.test.js` regex, mutation
  both ways (bare path RED, `path:line` and sha GREEN). Bar: suite reads 64+ green, only actors.evidence
  red (ASK-009).
- **P-BATTERY-REG** (A registers; B attacks — A wrote the refusal, B the condition): the battery WITH LOAD
  on the 08-15 rig; three cue conditions; the load condition stated as a check (a single-question battery
  reproduces run 1's 60/60 ceiling → void); degeneration marker before the run; power from
  `boundary-reminder.js --power`. Registration only. Refusal valid: "no load design survives B's attack."
- **P-LABELS** (E — non-author of the retriever, non-librarian): ≥30 labeled positives per R2a with the
  git-existence check per label; held-out split; B-POP/B-REC frozen before labeling. Bar: the gate in
  `relevance_retriever_registration §R2a` reads OPEN; the librarian touched none of it.
- **P-ROW10** (C — did not write the rescore): one condition in the pulse — when the printed gap exceeds a
  compaction/restart, the line carries the continuity row's card path; `install.ps1 -Check` REGISTERED and
  byte-identical after; mutation: remove the condition → the line reverts. Touches the installed hook, so
  after P-CLOSEOUT lands.
- **Chair, one line:** the build-exit-1 shape into `desktop_first_run_2026-08-25.md`'s runbook ("exit 1 on
  the exe swap with no `*.rs` change since the running binary = success; `whats-live` decides").
Disjoint: closeout = loop/ prose + ask.test + separating-test append; battery = one new loop file; labels =
one new labels file; row10 = pulse .py + installer check. Order: CLOSEOUT and BATTERY-REG and LABELS in
parallel; ROW10 after CLOSEOUT.

**Guess∩map, scored here against the chair's sealed five:** chair guessed `handoff_chair_2026-08-30.md`,
`carrier-drift.registry.json`, `ask.test.js`, `ASK.md`, `handoff_librarian_2026-08-30.md`. Map names 10;
overlap **3** (handoff_chair, the registry/carriers, ask.test); map-only 7 (separating_test §223, boundary-
reminder --power, trigger_index_rescore :148/:240, relevance_retriever :83, visible_channel, the research
file items 4/7, the 08-24 dream-watch WRONG). Recorded on the row.

**Ledger: surfaced 3** (the ninth carrier in the chair's own handoff; the three-kinds split with only one
moving the number; the rebuild exit-1 as the A1 class). Uncommitted; the chair commits.

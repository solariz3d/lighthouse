# 2026-08-31 — Monday: the rebuild landed; L019 opens with a sealed guess

## ~01:05 — L019 MAP + WORK-SHAPE (keeper, verbatim via the chair: "well you do not have to do it yourself, you could start the work chain loop")

**Verified before mapping:** `open-items` CLOSED (30 of 30, chair's figure; the two absent files now ship);
`carrier-drift.js` → RED, **9 findings, not 8** — the ninth is NEW: `handoff_chair_2026-08-30.md:41`
UNACCOUNTED, the chair's own handoff quoting the crude wording it had just struck (the carrier problem in
its purest form, one day after the repair); suite 63/3 of 66; `lap-row --report` 19 laps. The build defect
the chair filed (exit 1 over completed work: `os error 5` on the exe swap with no Rust changed since the
running binary) is the cycle-9 A1 class — a red with no assertion behind it — and `whats-live` + `git log
--since -- '*.rs'` is the check that settles it; one runbook line, chair's.

**Verdict on "is the line thin": NO — but it is three kinds of work, and only one of them moves the number.**
(a) Closeouts, mechanical, one pane: the 9 carriers (B: repair 3 / marker 5, +1 marker for the chair's
handoff); the power line re-run at 123/136 (`boundary-reminder.js --power`, or B's formula in
`separating_test_registration §` — the refusal stands on the design defect; the headroom figure must be
re-derived, not inherited); `ask.test.js:253` regex widened to `path:line` or sha with mutation both ways
(D001's instrument, desktop-owned, laptop-red for two days — take it). (b) THE ONE MEASUREMENT: 3d, the
battery WITH LOAD (`separating_test_registration_2026-08-30.md:223-225`; A's refusal + B's condition) —
registration first, then build; it is the only thing on the line that can move the 98.5% by a run rather
than an argument, and it is the room's own apparatus (the 08-15 rig, 72 trials). (c) THE GATE ONLY PANES
CAN OPEN: the relevance retriever needs ≥30 labeled positives by a non-author, non-librarian seat
(`relevance_retriever_registration_2026-08-30.md:83`); nothing else unblocks it. Plus one tiny wire:
row 10 on the pulse gap (`trigger_index_rescore_2026-08-30.md:148,240` — the gap is already printed).

**NOT this lap, said so:** building the chain indicator (C changed the instrument; the confound is
registered; building a third thing beside two registrations is the fatigue shape); the address-table edge
(keeper's timing); dream-watch's "5d 14h" (this laptop does not run the dream cycle — 08-24 WRONG entry;
the desktop does; not chased from here); ASK-011 (keeper's); consumer (not yet).

### WORK-SHAPE (bodies the chair's; roles named)
- **P-CLOSEOUT** (one pane, NOT B — B wrote the sweep): (1) the 9 carrier findings → `carrier-drift.js`
  GREEN and `carrier-drift.test.js` green; (2) power re-run at 90.4% appended to the separating-test file
  as a dated block, stating whether the refusal's power ground changes; (3) `ask.test.js` regex, mutation
  both ways (bare path RED, `path:line` and sha GREEN). Bar: suite reads 64+ green, only actors.evidence
  red (ASK-009).
- **P-BATTERY-REG** (A registers; B attacks — A wrote the refusal, B the condition): the battery WITH LOAD
  on the 08-15 rig; three cue conditions; the load condition stated as a check (a single-question battery
  reproduces run 1's 60/60 ceiling → void); degeneration marker before the run; power from
  `boundary-reminder.js --power`. Registration only. Refusal valid: "no load design survives B's attack."
- **P-LABELS** (E — non-author of the retriever, non-librarian): ≥30 labeled positives per R2a with the
  git-existence check per label; held-out split; B-POP/B-REC frozen before labeling. Bar: the gate in
  `relevance_retriever_registration §R2a` reads OPEN; the librarian touched none of it.
- **P-ROW10** (C — did not write the rescore): one condition in the pulse — when the printed gap exceeds a
  compaction/restart, the line carries the continuity row's card path; `install.ps1 -Check` REGISTERED and
  byte-identical after; mutation: remove the condition → the line reverts. Touches the installed hook, so
  after P-CLOSEOUT lands.
- **Chair, one line:** the build-exit-1 shape into `desktop_first_run_2026-08-25.md`'s runbook ("exit 1 on
  the exe swap with no `*.rs` change since the running binary = success; `whats-live` decides").
Disjoint: closeout = loop/ prose + ask.test + separating-test append; battery = one new loop file; labels =
one new labels file; row10 = pulse .py + installer check. Order: CLOSEOUT and BATTERY-REG and LABELS in
parallel; ROW10 after CLOSEOUT.

**Guess∩map, scored here against the chair's sealed five:** chair guessed `handoff_chair_2026-08-30.md`,
`carrier-drift.registry.json`, `ask.test.js`, `ASK.md`, `handoff_librarian_2026-08-30.md`. Map names 10;
overlap **3** (handoff_chair, the registry/carriers, ask.test); map-only 7 (separating_test §223, boundary-
reminder --power, trigger_index_rescore :148/:240, relevance_retriever :83, visible_channel, the research
file items 4/7, the 08-24 dream-watch WRONG). Recorded on the row.

**Ledger: surfaced 3** (the ninth carrier in the chair's own handoff; the three-kinds split with only one
moving the number; the rebuild exit-1 as the A1 class). Uncommitted; the chair commits.

## ~01:12 — chair INTERRUPT on L019: the counter cannot complete, and my watch was already dead

**Chair's finding, verified:** `chain-status.js:545-549` builds the owing set from every dispatch arrival
resolved to a letter; the chair reached this desk by `chair_inject` tonight (35 chair-prefixed arrivals to
`0c0c0c0b` in the last 3000 board rows, per the chair), so the librarian is in the universe as a dispatched
letter that owes a hand-back — and this desk posts back only at the return leg, which waits on the counter.
Deadlock by construction: `handbacks 0 of 4 (owing A,C,E,M)` will never read 4 of 4. **Latent since the
counter shipped; L018's clean 4-of-4 was the channel (call_chair both ways), not correctness.** L017's
failure with a new mechanism — and this one has an instrument that would read `owing M` forever and never
look wrong.

**Correction to the chair's item 1 (WRONG column is the chair's, filed by this desk):** M is not a mislabel.
`C:\Consonance\data\letters.json`: `0c0c0c0b… => "M"` (this seat), `0c0c0c0a… => "D"` (Main). "owing M" is
correctly labelled — it says the librarian owes — my own 08-24 WRONG #1 was the same misread from the other
side. Item 2 stands: the counting rule needs a seat-class distinction — chair→pane creates an obligation,
chair→librarian is the chain's own routing step and must not.

**And my watch was DEAD:** `b1fc9fv4d.output` ends `HANDBACKS-IN\n\n[killed]` — it died at the 00:51 app
restart. The handoff's "survives compaction" was true; "survives a restart" was never claimed and is now
measured false. Re-armed below on the correct condition for THIS lap: owing ⊆ {M}, i.e. every pane in.
Rule for the fix (routed, not built here): the librarian's letter is excluded from the universe, or its
return-leg post satisfies it — whichever, with a test that dispatches to M and asserts the counter still
completes.

**Ledger: surfaced 2** (M is the librarian's own letter; the watch dies at restart, not at compaction).

**Re-armed watch: `b40z3c596`** (persistent; fires when owing == M only, or HANDBACKS IN, or the lap closes). The dead one was `b1fc9fv4d`.

## ~01:35 — L019 RETURN LEG, collected at 3 of 4 (owing M = this desk; watch `b40z3c596` fired at 01:29; the chair's ring was not needed)

**Objects opened whole or at falsifier depth:** `exo_memory/handback/p-closeout_2026-08-31.md` (C, 122 lines,
whole); `loop/battery_load_registration_2026-08-31.md` (A, 400; §§0,4,5,6 + heads); `loop/retriever_labels_2026-08-31.md`
(E, 364; §§0,5,6,7 + cluster heads). Arrow rule: C's 123/136 = 90.4% reproduces by `power_append.js`'s own refusal
gate; C's "22/22" and "39/39" are the suites' own counts; A's power table is the shipped function
(`boundary-reminder.js`, `git diff 4ec5ef5 HEAD` empty); E's split hashes re-derive by the printed one-liner
(spot-checked C01 → `80d0c117`). `carrier-drift.js` RED with exactly two UNACCOUNTED, both `mention` class, both
written DURING this lap: `retriever_labels_2026-08-31.md:161` (E quoting the other withdrawn wording as a label's
evidence) and **`handback/p-closeout_2026-08-31.md:85` — C's own hand-back, quoting the wording in the paragraph
that reports E's carrier of it.** The 08-02 shape verbatim: a safeguard's rationale is not covered by the
safeguard. Suite 63/3: actors.evidence (ASK-009), carrier-drift (the two above), `state-block.test.js` (test 3
reads HEAD's commit SUBJECT as the block's prose and trips on the word "only" in `dbe2478` — mention-vs-use, the
sealed group, in a test; clears on the next commit by accident, which is not a fix).

**Residue (L019, card-sized):**
> **L019, 2026-08-31.** The closeouts landed (C): the nine carriers accounted 25/25 — three TEACHING procedures
> struck in place with the repaired polarity written out (`branch_layer.md:185` now reads *would you have said it
> whether or not it were true? YES → ROUTER, NO → VERIFIER*), five DISCUSS files dated-superseded beside the wording,
> the chair's handoff registered as a mention with no edit; `ask.test.js` widened to `path:line` or sha, bare path
> RED, shipped-store mutations both ways; the power line HOLDS at 90.4% (B's 92.5% was a `chars >= 200` cut that
> dropped three real rows; §0's "smallest real dispatch 829" is false) — headroom moved 6.6 → 9.6 pts and the
> refusal's verdict does not depend on which cut. **The battery WITH LOAD is registered (A), nothing built:**
> K0 no cue / K1 trailing reminder (Mittal's fix) / K2 focal at the event — one line in `handoff.js`'s receipt,
> delivered BY the hand-on so it precedes the composition it addresses (the one structural advantage over every
> refused live design); two self-checks before any cue arm (P0 VOID-RIG if L0 violation >30%; P1 VOID-LOAD if L1
> violation <30% — *"no load design survives contact with a fresh subject who simply goes and looks"*); P2 Mittal
> transfers (K1 ≤ K0 − 0.30); P3 the room's claim, A's declared stake, unwelcome outcome written in the words that
> make it true (K2 ≤ K1 − 0.20); P4 no cue ships that buys compliance with correctness (truth-carry ≥ 0.85);
> n = 40/arm × 3 + 20 = 140 trials from the shipped power function, powered for a 30-pt drop from 70%. **B attacks
> before any subject runs.** **The retriever gate (E): OPEN at 31 (turn, item) pairs, SHUT at 13 turns / 9
> clusters — and R2a does not say which unit counts while R5's McNemar pairs on turns; held-out holds 7 turns, so
> 7 discordant pairs needs disagreement on every one: R5's VOID condition arriving before the run.** The population
> of labelable failures on this machine is 13 turns; 23 of 31 pairs are the chair's. Baselines frozen at `dbe2478`
> before any label existed; split sealed by cluster hash. Raw: the three hand-backs, uncommitted at write time.

**KNOWN now:** the retriever cannot be powered from this machine's record as registered — a finding about the
record, not the labeller; the battery is runnable and has a structural edge no live design had; the crude-handle
sweep is complete for everything that existed at dispatch and two mentions were born during the lap; `state-block`'s
test 3 is a mention-vs-use defect (the room's one sealed group, seventh-plus instance).

**ABSENT:** C's amendment to R2a (unit: pairs-with-turn-clustering or turns — C's call, one line; until it lands the
same file opens and shuts the gate); B's attack on the battery registration (gates any subject); the two mention
rows for the registry (anchors from `--census` at commit time — C already wrote E's row; C's own is new);
`state-block.test.js` test 3 fix (scan the block's prose, not the HEAD subject); the desktop's +1 turn for E's
population (E: "+1 on the desktop"); ASK-011 (keeper).

**ONE THING NOT TO CARRY FORWARD:** "the retriever gate is open at 31." Carry: 31 pairs / 13 turns / 9 clusters,
unit unregistered, McNemar VOID-shaped at 7 held-out turns.

**COMMIT GUIDANCE (chair, commit-only):** three commits, one per pane's paths (C's list is in its §"Files touched";
A = one file; E = three files), then ONE chair commit adding the two `mention` rows to `carrier-drift.registry.json`
with anchors from `--census` so the tree is GREEN at the end of the lap — do not let C's hand-back ship as the third
unaccounted carrier of the night. Subject line without the word "only" so state-block clears (and note in the body
that the test is defective, not fixed). Then L020's first packets are already named: B attacks A; C amends R2a.

**Lap row:** opened-from-map = 6 (separating_test, boundary-reminder, ask.test, the registry, the retriever
registration, the research file); guess∩map 3 of 5 sealed. **Ledger: surfaced 4** (the second in-lap mention in C's
own hand-back; the unit ambiguity that opens and shuts the gate; state-block test 3 as mention-vs-use; the battery's
structural edge stated as the reason it can run where the live designs could not). Uncommitted; the chair commits.

**~01:40 — watch `b40z3c596` STOPPED (it re-emitted "no open lap" every poll — a monitor that fires regardless is the ferry); replaced by `b0rs0l18o`: emits once per state change only (all panes in / HANDBACKS IN / lap open-close).**

## ~02:10 — L020 RULING on the chair's skip (keeper-caught): the shape holds, the number is void, and the map that was skipped had one packet the chair's did not

**The keeper's inquiry, verbatim, which this desk never received until the chair's confession:** *"this is my
work time though, we need to push through and to solidfy the solution for the retrieval problem."*

**1. The number.** L020's guess∩map (3 of 5) is one seat on both sides — void, as the chair already appended.
**L017 needs the same void:** its row (`lap.jsonl`, stage map at 1788089923476) reads guess 5 / map 5 / BOTH 3,
and its own note says the map was served from my L016 leg, not a fresh round. Two manufactured rows in the
ledger. **And the instrument does not honour the void:** `lap-row --report` still prints L020 BOTH 3 after the
correction row — the correction is a note, not a stage the report reads. Route: a `void` stage the report
subtracts, with a test. Until then, quote guess∩map only for L003–L016, L018, L019.

**2. The shape.** B attacking the battery: RIGHT — it is the gate on the only measurement. C naming the unit or
ruling the gate unpassable at 13 turns: RIGHT — and "the retriever line stops tonight" being an acceptable
return is the honest form. **No correction to either pane.**

**3. What the skipped map would have added, and it is the keeper's word: SOLIDIFY.** The fix on the retrieval
line that HAS evidence is the routing fix — a non-author reads the deliverable before it reaches the keeper
(every one of ~46 catches on this desk came that way; zero in-stream; three fields say the same,
`research/the_retrieval_problem_outside.md` §5). Grep over `exo_memory/loop/`: it is named in exactly ONE
file — `handoff_librarian_2026-08-30.md:107`, item 5 of a handoff. **No registration, no falsifier, no
instrument. And this desk does not practise it:** the L018 and L019 maps went from my notes straight to the
chair by `call_chair`; nobody read them first. The chair's carrier list was five-of-fifteen and item 4c was
wrong-shaped because no one read the packet before it shipped. So "solidify the solution" maps to a third
packet the chair's map lacked: **P-SECOND-VANTAGE — register the routing fix as a practice with an
instrument:** every librarian map and every registration gets a non-author read BEFORE the chair dispatches
(one pane, minutes — the reader posts `reviewed <sha> · N changes` to the board; the lap row carries a
`reviewed_by` field); falsifier, registered now: *if over ten reviewed laps the reader changes nothing, the hop
is ceremony and comes out; if the WRONG entries against reviewed deliverables do not fall below the unreviewed
rate (L015–L019 baseline: 12 findable-before-delivery of 19), the review is not the lever.* Body: A registers
(A is free; A did not write the practice line — I did, so not me); E attacks (E has the census on carriers and
the refusal on 4c — the cases). Registration only this lap.

**4. Was the chair's reasoning ever sound?** No, and the chair named why: my sequencing was written before the
keeper spoke, and "solidify" is not "what is left." Same species as my L016 (goal read as sequence). The rule
already exists — map before plan; a map read after a plan exists becomes support for the plan (BUILDING.md,
the joint step) — and the chair quoted it back at itself correctly.

**Ledger: surfaced 3** (L017's row needs the void too; `--report` blind to correction rows; the routing fix is
unregistered and unpractised at this desk — the packet the skip cost). Uncommitted; the chair commits.

## ~02:15 — the keeper's observation, filed as tonight's surfacing: the work chain's forgetting IS the retrieval problem, at an instrumentable scale

Keeper, verbatim: *"i see the forgetfulness of the work chain loop to be the same problem as the broader retrieval
problem."* Held against the record rather than agreed with: every chain failure tonight is a step that had to fire
at a moment and did not, because nothing at that moment presented it as a step — the orch skipping this desk on
L020 (an inquiry arrived, a plan-shaped thing was in hand, "route to the librarian first" never presented as a
claim); L017's ledger-row-as-message; the 98.5% ring-before-finishing; my own L018/L019 maps sent unread. That is
`journal/2026-08-16.md`'s finding verbatim (*the check never arose as an available action*) and the research file's
half A (prospective memory; nonfocal cue; load). The fixes sort identically: what worked is focal and state-changing
(the counter at 3 of 4; dispatch-gate asking at the verb; the persistent watch); what did not is a document
(`BUILDING.md`'s eight steps, quoted correctly by the chair AFTER violating them — reminded is not applied).

**The difference, and it is the useful part: the chain's forgetting is machine-visible; a seat's is not.** A skipped
routing step leaves a row in `lap.jsonl` with the wrong author; a skipped collation leaves a counter complete with
no holder; a premature ring leaves a tool call before the turn's last text block. The seat-internal failure leaves
nothing until a second reader finds it. **So the chain is the retrieval problem at the one scale where every step
can be instrumented — a testbed.** Consequences, registered: (1) P-SECOND-VANTAGE is not "a hop we add"; it is the
chain instance of the routing fix; (2) the chain's skip / stall / premature-ring counts belong on the same board as
the 98.5% — same quantity, different scale; (3) a cue design that moves the chain's numbers is a candidate for the
seat-internal version, and one that fails on the chain fails cheaper here than in the battery. Falsifier for the
reframe: if a cue that demonstrably fixes a chain step (e.g. the counter) is ported to the seat-internal event and
moves nothing over a powered run, the two are the same shape and not the same mechanism, and the testbed claim is
struck.

Also observed: the chair's void-row on L017 re-opened it in `chain-status`'s eyes (`L017 DISPATCHED · holder chair ·
+1 more open`; my watch fired on it). The report-blind-to-voids defect, second face. **Ledger: surfaced 1** (the
testbed reframe with its falsifier). Uncommitted; the chair commits.

## ~02:40 — L020 RETURN LEG, collected on the counter (HANDBACKS IN at 02:29; five hand-backs: A, B, C x2, E)

**Objects opened at verdict + falsifier depth:** `loop/battery_attack_2026-08-31.md` (B, 275: §0, §8, §9);
`loop/second_vantage_registration_2026-08-31.md` (A, 315: §0, §3, §4 heads); `loop/second_vantage_attack_2026-08-31.md`
(E, 314: §0 table, §6, §7); C's unit ruling (+41-line dated block on `relevance_retriever_registration_2026-08-30.md`)
and C-2 (`lap-row.js --void`, 40/40 tests). Arrow rule: `lap-row --report` prints `2 VOID` with both reasons and no
BOTH figure for L017/L020 — re-run here; suite per C 65 green / 1 red (actors.evidence); E's 9m14s cost datum carries
its command; B's "every n doubles" is checked against Cohen's published table per B §9. Pass.

**WRONG +1 (A-caught; lifetime 47):** my P-SECOND-VANTAGE falsifier F2's baseline — *"12 findable-before-delivery of
19"* — has no derivation on disk. The 19 re-derives; the 12 is this seat's judgement about its own errors, and a
falsifier that reads against a hand figure is C4's defect (`build_ruling.md`). Written in the same leg that ruled the
chair's map row manufactured. E carried it further: the window should be L015–L018 (17 in-window), the findable
fraction armed on a NAMED rule (strict 11 / reading 18), a second non-author may relabel. Unarmed until then.

**Residue (L020, card-sized):**
> **L020, 2026-08-31.** THE BATTERY HOLDS with three amendments and is buildable — P0a first (B): as written the
> likeliest outcome was VOID-RIG on a misdiagnosis or a two-way primary; the chair's predicted kill ("no load
> design survives a fresh subject") is dismissed — the danger is its mirror, and run 1's files show it. B found
> the h-formula error was ITS OWN (`4ec5ef5`'s refusal), reproduced by two seats: every n doubles, every
> active-day figure doubles, and the refusal's ground survives a fourth seat's error. F4: prose if the amendments
> are not appended and P0a not dispatched within 14 days. **THE RETRIEVER LINE STOPS ON THIS MACHINE** (C): unit
> named = the TURN; gate SHUT, 13 against 30; unpassable by construction — held-out holds 7 turns and R5's void
> binds before the count does; C declined to re-register a friendlier statistic now that the counts are known
> ("the tuning ratchet my own file names at :236"). What would reopen it: ~60 labeled turns on one board, or a
> cross-machine label set under the consent rule. **THE SECOND VANTAGE, SPLIT (A, held by E):** as a CONVENTION on
> the chair it is REFUSED — the same hop misses at 74.9% overall, 60% for registration files, and the generating
> seat has no free slot at ship-time; as a GATE it is REGISTERED, not built — PREREAD-GATE, one clause in
> `dispatch-gate.js` at PreToolUse: a dispatch citing a registration or librarian note asks for a board receipt
> `reviewed <sha|path@hash> · N changes` from a mount that is neither author nor dispatcher (E's amendments:
> key on the deliverable not the first sha; enforce `commit/mtime < receipt < dispatch`; one regress level;
> rotation). **Precondition, the keeper's call:** the only mechanism that has ever moved a rate here —
> `permissionDecision:"ask"` — is DROPPED under bypass mode, and the chair's pane resumes under bypass by
> construction (`main.rs:4075`). Under bypass the gate is a focal one-off message — the K2 class the battery
> tests. Cost measured, not asserted: one non-author read of a 27 KB registration = 9 min 14 s. **AND THE VOID
> WAS INCOMPLETE** (C-2): the open→map gap the ledger already records distinguishes a same-seat map from a
> librarian round (171–199 s here) — eight of eighteen scored laps had the map row under 40 s after the guess,
> five within 100 ms; L009, L013, L014, L015, L016, L018 carry the signature and are still counted. Raw: the five
> hand-backs, uncommitted at write time.

**The six remaining voids — the reason is mine to write, per C.** Rule: a lap whose `open` and `map` rows are
seconds apart was opened and mapped by ONE seat, so no independent guess ever existed — whichever seat it was.
For L013/L014/L016/L018 that seat was this desk (laps I initiated and mapped in one motion; L018 already reads
guess 0). For L009 and L015 the chair should say which seat wrote both. **Either way the guess-map figure is
manufactured and the void applies; the LAP stands.** Applying them is the chair's (ledger practice, at its
instruction, as the first two were). Honest count after: **guess-map exists for 10 laps** — L003–L008, L010–L012,
L019 — and only those may be quoted.

**KNOWN now:** the retrieval line has ONE runnable measurement (the battery, amended) and ONE registered gate
(PREREAD, bypass-blocked); the retriever is closed on this machine by the record's own population; the second
vantage's convention form is measured dead and its gate form has a precondition nobody in the room can grant.
**ABSENT:** B's three amendments appended to A's registration (A's pen); P0a dispatch (the 14-day clock started
02:29); the keeper's bypass ruling (PREREAD cannot fire "ask" without it); the six voids; the F2 arming labels;
the `reviewed` chain stage in `--report`.

**ONE THING NOT TO CARRY FORWARD:** "the second vantage before delivery is the fix." Carry: as a convention it
misses at 74.9%; as a gate it is registered and blocked on bypass; the practice on this desk is unmeasured.

**COMMIT GUIDANCE (chair, commit-only):** four commits by pane paths (A's registration; B's attack; E's attack;
C's two — the R2a block and `lap-row.js` + test), C-2 last so the void rows' reasons are in history beside the
tool. Then the six voids, at your instruction, with the rule above quoted. Then P0a is the next lap's first packet
once A appends B's amendments — and the bypass question goes to the keeper as an ASK, not a packet.

**Ledger: surfaced 4** (the six-lap void signature and the honest quotable set; the bypass precondition as the
gate's actual blocker; the retriever's closure as a record finding; the mirror-danger reading of P1). **WRONG +1.**
Uncommitted; the chair commits.

## ~03:00 — L021 MAP + PLAN (keeper, verbatim via the chair: "lets do all of it in managable chunks, lets plan it out first then execute. Make sure to use the work chain loop"); the chair's guess sealed 02:56, unread here until the map below was written

**Verified before mapping:** HEAD `dc820ea`, clean; suite 65/1 (actors.evidence = ASK-009); carrier-drift GREEN;
open-items CLOSED; 20 laps / 8 VOID; ASK-012 exists (`ASK.md:147`); B's amendments at `battery_attack_2026-08-31.md:63`
(A), `:104` (B), `:115-135` (C); P0/P1 at `battery_load_registration_2026-08-31.md:146-159`, the rig change
(`handoff.js` as the hand-on) at `:68-90`; the retriever closure at `relevance_retriever_registration_2026-08-30.md:333-353`
("So the retriever line stops here"); dispatch-gate live ledger 157 rows (outcome column's 30-citation-row falsifier
clock is running). Row 10 (`trigger_index_rescore:148,160`) still unwired.

### Q1 — what "all of it" should mean here
His two sentences tonight are one instruction: 01:04 *"solidify the solution for the retrieval problem"*, 02:55 *"do
all of it in manageable chunks, plan first then execute."* **"All of it" = the retrieval line as it now stands, and
the line now stands at exactly TWO live objects plus closure and hygiene.** Two objects: (1) the battery — the only
measurement, amended and buildable, with a 14-day clock; (2) the second-vantage GATE — registered, amended, and
blocked on a keeper ruling (ASK-012), not on work. Closure: the retriever is stopped by construction and needs a
closing record so nobody re-derives the line. Hygiene: three loose ends. **"All" is the right frame only if it means
FINISH THESE TWO** — not reopen the retriever, not add a registration, not start the consumer. If "all" is read as
"everything ever registered," it is the amendment-fatigue shape and the answer is no. Two things matter; the rest is
closing the books around them.

### Q2 — the chunks, in order, with what gates each
- **CHUNK 0 — the keeper, five minutes, DOES NOT BLOCK CHUNK 1:** ASK-012 (bypass: does the gate get to ASK?) gates
  chunk 4 only; ASK-009 clears the suite's last red; ASK-011 gates nothing; **ASK-004 is 33 days old and addressed
  to him by name** — say so once, plainly. Put ASK-012 to him BEFORE chunk 4 dispatches, not before chunk 1.
- **CHUNK 1 — make the battery runnable (one lap, three disjoint packets, now):**
  - P1a **A appends B's Amendments A/B/C** to its own registration as a dated section (`battery_attack:63,104,115`),
    verbatim, no softening; **B re-reads for fidelity** (five minutes; B wrote them). Bar: the appended text greps
    back to B's file; P0a/P0b split present; the evaluability clause *"P2/P3 evaluable only if K0 − P0a-rate ≥ 0.40"*
    present.
  - P1b **the retriever CLOSING ENTRY** (C, the author): one short loop/ file or journal block — what was registered,
    why it cannot be powered here (13 turns; R5's void first), the exact reopen conditions (~60 labeled turns on one
    board / cross-machine set under the consent rule), and the labels + frozen baselines as the standing set. Bar: a
    future seat reading LEDGER finds "CLOSED — see <path>" and does not re-derive.
  - P1c **hygiene** (E): `state-block.test.js` test 3 (scan the block's prose, not the HEAD subject; mutation both
    ways); the runbook line in `desktop_first_run_2026-08-25.md` (exit 1 on the exe swap with no `*.rs` change =
    success; `whats-live` decides); row 10 on the pulse gap — ONE condition, `install.ps1 -Check` registered +
    byte-identical after, mutation shown. Disjoint from everything else (one test, one runbook, the pulse .py).
- **CHUNK 2 — P0a then P0/P1, the calibration arms (next lap):** the rig change (`handoff.js` as the hand-on, the
  receipt line, REPLY.md per Amendment B) built by A (author of the design; builders may author, scorers may not);
  **the scorer is named before the first subject: B or E, not A, not the chair, not this desk.** P0a first (10
  print-mode subjects, Amendment A), then L0×K0 (20), then L1×K0 (20). **Stop rules verbatim from the registration:**
  P0 VOID-RIG if L0×K0 violation >30%; P1 VOID-LOAD if L1×K0 <30% (30–50% = weakly powered, printed). Spend: fresh
  `claude -p` subjects, local, no egress — the keeper's "do all of it" covers it; say the count (≈50 trials) in the
  brief so it is chosen, not discovered.
- **CHUNK 3 — the cue arms (only if P0 and P1 pass):** K1 and K2 at n=40 each; scored blind by the named non-author
  against P2/P3/P4 as amended; a P3 failure goes into `research/the_retrieval_problem_outside.md` §1 as a correction
  ABOVE the paragraph it corrects (A's own clause). This is the number the whole line is for.
- **CHUNK 4 — the gate (gated on ASK-012):** if the keeper lets the chair's pane run without bypass for the dispatch
  verb, build PREREAD-GATE with E's four amendments (`second_vantage_attack §6`); if not, ship it as the focal message
  it can be and SCORE it as the K2 class — chunk 3's result tells us whether that is worth anything. Either way, F2 is
  armed only after the labeling per E §6.1.
- **CHUNK 5 — close the loop on the loop:** the six remaining voids (rule in my ~02:40); the `reviewed` stage in
  `--report`; the chain-testbed reframe (my ~02:15) gets its first instrument — stall/skip/premature-ring counts on
  one board with the 98.5% — registration only, non-author.

### What should NOT start until the keeper answers
Only chunk 4 (ASK-012). Chunks 1–3 need no ruling; chunk 2's subject spawn is covered by tonight's instruction and
the brief states the trial count. Nothing else waits.

### Order in one line
1 now (three packets, disjoint) → 2 → 3 → [ASK-012] → 4 → 5. The 14-day clock (F4, from 02:29) is met if chunk 2's
P0a dispatches within the week; it is the only deadline on the line.

**Not on the list, said so:** consumer; chain-indicator BUILD (registered; its confound is on file; chunk 5 gives it a
baseline first); reopening the retriever; any new BOOT amendment.

**Guess∩map, scored after writing:** the chair's five — `ASK.md`, `battery_attack`, `second_vantage_attack`,
`battery_load_registration`, `relevance_retriever_registration`. Map names 9: those five plus `research/the_retrieval_
problem_outside.md` (§1 correction slot), `trigger_index_rescore` (row 10), `desktop_first_run_2026-08-25.md` (runbook
line), `consonance/tools/state-block.test.js`. **Overlap 5 of 5; map-only 4.** The chair's guess was complete on the
objects and silent on the order — which is the half the map is for.

**Ledger: surfaced 3** ("all" resolved to two objects; only ASK-012 blocks work; the named-scorer-before-first-subject
rule carried forward into chunk 2). Uncommitted; the chair commits.

## ~05:12 — L021 CHUNK 1 RETURN LEG, collected at 3 of 4 (watch fired 05:05; chair rang inside the minute) — with ONE BAR STILL RUNNING

**Objects read from the dirty tree (7 paths):** A's dated §12 on `battery_load_registration_2026-08-31.md`
(+133 lines: 12.1 Amendment A, 12.2 B, 12.3 C, 12.4 acknowledged-by-reference; the evaluability clause at :477/:498);
C's `retriever_closing_2026-08-31.md` (new) + the LEDGER row (`LEDGER.md:15`: "CLOSED — see …"); E's three:
`state-block.test.js` (+108: test 3 now scans the block's prose; 23 pass / 0 fail), `desktop_first_run_2026-08-25.md`
(+45: the exit-1-over-success line), and ROW 10 in `dev/shell/hooks/userprompt_pulse.py` (+75) with a new
`userprompt_pulse.test.js` (6 pass / 0 fail). **Arrow rule:** the installed pulse `~/.claude/shell/userprompt_pulse.py`
and the repo copy hash identical (`md5 dfd81686ec4c` both; installed 05:01, repo 05:00) — E installed what it wrote;
`install.ps1 -Check` itself NOT run here (my combined check timed out at 120 s before reaching it — a suite run in the
same command; re-run it alone, it is the registered instrument for this exact claim). The chair's "shell/hooks/…" path
in its ring is wrong by one directory: the installed pulse lives at `~/.claude/shell/userprompt_pulse.py` (no hooks/),
which is where the hash match was taken.

**THE BAR THAT IS STILL OPEN:** B's fidelity re-read of A's append — dispatched 05:05, B's script running at 05:07Z
("extract my three blocks from 4c464f4, test each for exact byte-presence, diff where absent, then read §12 for
softening"). **Chunk 1 is complete on PANES and incomplete on BARS, and the open bar is the one chunk 2 dispatches
off.** So: this leg files; P0a does NOT dispatch until B's verdict lands. If B finds drift or softening, A re-appends
and the leg is amended here, not the row.

**Row 10, weighed as the chair asked:** E's design reads compaction PER INSTANCE from the seat's own transcript
(`isCompactSummary` rows since the last prompt, offset in STATE — bounded, cannot re-fire on an old compaction) and
app (re)start from the process; on either it carries `cards/claim-your-continuity.md` in the pulse. That is the one
SOURCE row the rescore called focalizable cheaply (`trigger_index_rescore:148`), done the cheap way, with the 08-16
case as its anchor. Its falsifier is the same as the reframe's: if a post-compaction turn still reports "nothing to
report" with the card path printed above it, the cue reached the line and not the seat — stage 9, and it goes in the
research file as such. Correct place (installed pulse), correct instrument to police it (`-Check`), which is why
`-Check` must be run before this is called shipped.

**Residue (L021 chunk 1, card-sized):**
> **L021·1, 2026-08-31.** The battery's three amendments are appended as a dated §12, verbatim blocks with BEGIN/END
> markers citing `4c464f4:63–133`, plus a §12.4 of corrections acknowledged by reference — fidelity check by B
> PENDING. The retriever line has a closing entry that a future seat reads instead of re-deriving it: "honestly
> killed, not died," one count and a table telling the reader in a minute whether it can reopen on their machine.
> Three hygiene items landed with tests: a test that read a commit subject as prose no longer does; the runbook
> carries the exit-1-over-success shape; row 10 is live in the installed pulse — the first SOURCE trigger made
> focal since the trigger index was registered on 08-15. Raw: the seven dirty paths, uncommitted.

**ABSENT:** B's fidelity verdict (gates P0a); `install.ps1 -Check` on the pulse (gates calling row 10 shipped); the
suite line at HEAD+tree (my run timed out; C reported 65/1 an hour ago, E's changes since need one clean run).

**COMMIT GUIDANCE (chair):** wait for B's verdict before committing A's file (if drift, A fixes first — a softened
registration in history is the P-FIC shape); C's two files and E's four can commit now, one commit per pane; the
pulse commit body must say `-Check` result. Then chunk 2 opens as L022 with the scorer named in the brief.

**Ledger: surfaced 3** (complete-on-panes ≠ complete-on-bars, with the bar named; the installed-path correction;
row 10's falsifier stated before it is called shipped). Uncommitted; the chair commits.

**~05:20 — the open bar CLOSED, and one half-shipped item.** B's fidelity re-read
(`exo_memory/handback/p1a-check_2026-08-31.md`): **BUILD FROM THIS — byte-identical A/B/C against `4c464f4`, P0a/P0b
split present, evaluability clause present in B's own words (the chair's brief paraphrase dropped "are" — the brief
drifted, not A), pre-attack text unchanged, NOT softened**; one defect in A's own self-check command, not in the text.
The chair committed all three panes (`9384b1d` C, `bbbaa40` E, `106d48b` A). **Suite at HEAD: `js-suite: 66 green · 1 failed · 0 crashed · 0 silent · 0 canary · 0 sang · 0 not-run · 0 class-error  (of 67)`.**
**Row 10 is HALF SHIPPED** (chair, `install.ps1 -Check` exits 1): the pulse is *installed and running, byte-identical* —
and **REGISTERED, NOT DECLARED**: no `$register` entry in the installer, so a fresh install copies it and wires
nothing. The 08-18 desktop class exactly (hooks pulled, never registered). The bar was "registered + byte-identical";
one half met. Fix: one `$register` line in `install.ps1` (the installer is the chair's half), then `-Check` exits 0.
**Chunk 2 is UNBLOCKED:** P0a may dispatch as L022 with the scorer named in the brief (B or E; not A, not the chair,
not this desk). B's own note for the builder: build from the BEGIN/END blocks, never from the surrounding sentence.

**~05:25 — WRONG +1 (chair-caught; lifetime 48): "one `$register` line, then `-Check` exits 0."** Wrong on arithmetic and
wrong in kind, verified against the file I pointed at: `-Check` reports SIX registered-not-declared hooks (ferry-watch,
userprompt_pulse.py, dream-watch, board-digest, sessionstart-ambient, transcript-watch) + 12 ABSENT + 1 drifted — one
line fixes one of six and the exit stays 1. And the trap is written into `install.ps1:636-647` (corrected 2026-08-25):
`:137` DECLARES the Node `hooks\userprompt-submit.js` on UserPromptSubmit while `:89` installs the Python pulse with no
`$register` entry — *"declared and actual are DISJOINT on this event … running this script without -Check installs it
and adds a SECOND pulse."* Declaring the Python one without resolving the Node entry ships a double pulse on a fresh
machine while row 10 reads "declared." **A two-implementation decision on the hook that prints the keeper's pulse every
turn, dressed by me as "your half, one line."** And this desk had ALREADY paid for this exact trap: `librarian/2026-08-25.md`
~07:00, WRONG lifetime 18 — *"I cited install.ps1:633 ('both pulses exist') as a fact; the NOTE was false"* — same file,
same two pulses, same seat, six days apart, with the earlier entry on my own shelf. The reach did not fire on my own
WRONG column. Chair's packet shape ACCEPTED as written: one pane, NOT E (it did row 10), reads both pulses, declares the
Python one, resolves the Node entry (remove, or Hold with the reason recorded), and states in its return what `-Check`
reads afterwards **including that the other five undeclared hooks are out of scope and stay red** — no partial green
read as done. Row 10 stays HALF SHIPPED until it lands (`bbbaa40`'s body says so). Goes into L022 beside P0a; disjoint
files (`install.ps1` vs `subjects/`).

## ~05:30 — THE CLEANEST INSTANCE THE ROOM HAS: the correction was in my own WRONG column and did not reach the moment it was written for

Filed as its own entry at the chair's ask, because it is not a footnote to a WRONG row. On 2026-08-25 ~07:00 this desk
logged WRONG #18: *"I cited install.ps1:633 ('both pulses exist') as a fact; the NOTE was false — the .js pulse does not
run on this machine, and the plan would have put the chain line in a dead file."* Six days later, same file, same two
pulses, this desk told the chair "one `$register` line and `-Check` exits 0" — the same trap, with the dated correction
sitting in `librarian/2026-08-25.md` on my own shelf, carried in full at wake. **The correction existed, was dated, was
mine, and did not fire at the moment it was written for.** The prospective-memory reading predicts exactly this: a
WRONG column is a DOCUMENT — nonfocal by construction — and cannot cue a moment it does not witness. What would have
fired: a focal cue at the event — `install.ps1` opened, or the word "pulse" typed in a dispatch, carrying *"you were
wrong about this file on 08-25; both pulses; read :636-647."* That is row-10's mechanism (a card path on the pulse when
the gap crosses a boundary) aimed at the WRONG column instead of at SOURCE: **a per-file / per-word cue index over
this seat's own errors, delivered when the file or word recurs.** Registered as a candidate, not built; falsifier: if
such a cue is built and this seat repeats a WRONG-column trap on a cued file within the season anyway, the cue reached
the line and not the seat (stage 9) and the mechanism is not the lever. The room's chain-testbed reframe (~02:15) now
has its seat-internal twin: the WRONG column is the seat's `lap.jsonl`.

## ~05:35 — L022 MAP (keeper's inquiry verbatim via the chair; the chair's answers held as a party's position, checked not adopted; guess sealed 05:20, unread until this was written)

**The inquiry is architecture: (a) panes as sole builders, orch plans for panes, lib plans for the orch; (b) return leg
panes→lib→orch or panes→orch→lib; (c) the chain may start user+lib→orch. Plus the chair's three: who checks the chair's
packets; is a non-author read standing when the chair builds; does "lib plans for the orch" mean shape or packets. And
the ordering question: does this gate chunk 2.**

### (a) Panes as sole builders — YES as the default, with the exception class NAMED, and the record already says so
- `brief/LIBRARIAN.md:60` — *"the plan is the deliverable. The artifact is not"* — the keeper's own 08-23 framing of THIS
  seat; the inquiry extends it one seat up, and the same sentence holds: the orch's plan is its deliverable.
- `memory/split-the-work-with-the-panes.md` (corrected ≥3×); `journal/2026-08-17.md` (chair built three instruments solo,
  four panes idle, every pane found something it missed within minutes); tonight: the chair's one build (BOOT:22) took a
  weld check that found two defects (`fad74d5`), and ten of tonight's errors were the chair's, all coordination.
- `loop/second_vantage.md:44-47` — *the generating seat has no free slot at ship-time* — is the MECHANISM: a seat that
  builds and dispatches is the seat least able to check either.
- **Exception class, so "sole" does not become a wall:** the orch builds only what has no other body — a one-line
  installer/settings change on ITS OWN machine state, a commit — and **every such build gets a non-author read as a
  STANDING rule** (chair's Q2: YES; the weld check is the evidence). Not a courtesy; the only mechanism with a hit rate.

### (b) Return leg: panes → LIB → orch. Already ratified and running; keep it, and here is why with tonight's numbers
- Ratified 2026-08-29 (my L014 topology judgement; the keeper's chain) and run L016–L021: hand-backs collected on the
  counter at this desk five times tonight; the chair's inbound role = commit. `BUILDING.md:23` (step 6, verbatim, never
  summarised) and `:68` (the orch is the merge point) are the ground. Evidence the merge point must not collate: two of
  the chair's commit messages tonight were wrong about their own contents; the L017 "map served by LIB's leg" note.
- panes→orch→lib would put the summarising seat between the finding and the filing desk — the copy-of-a-copy law 1 forbids,
  measured (the chair's compression adds certainty, `muscle_map.md` 07-31).

### (c) Starting from user + lib — VALID, with the one rule that keeps the number honest
- L015 (this desk initiated) is one of tonight's eight voids: no sealed prior existed, and the row got a figure anyway
  (`lap.jsonl` L015: open→map by one seat). The defect is not the start point; it is a guess∩map figure with no guess.
- **Two honest forms, either allowed, the row must say which:** (i) NO-NUMBER lap — the lib opens with `--guess none`
  (L018 did exactly this; row reads guess 0, nothing manufactured); (ii) BLIND-GUESS lap — the lib holds its map, rings
  the orch with the INQUIRY ONLY, the orch seals its guess, then the lib delivers the map. Costs one ring; keeps the
  number. Use (ii) when the lap will be scored, (i) when the keeper and lib are exploring. Never (iii): lib writes both.

### The chair's three questions
1. **Who checks the chair's packets?** Two defective bars tonight, both in packet TEXT, both re-typed paraphrases of a
   bar that existed verbatim in my notes ("evaluable only if…" lost "are"; the "five carriers"). **Rule: bars TRAVEL as
   quoted text with path:line — the chair copies, never re-types.** And the receiving pane is the packet's focal second
   vantage: it is already the seat that re-derives the bar from the cited object at the moment of the work (B did it
   unasked). Make that a stated line in every brief: *re-derive the bar from the source before working; if the brief and
   the source disagree, the source wins and you say so.* That is "hand the item, never the verdict" (`muscle_map.md:51`)
   applied to the bar. Cheaper than a pre-read hop and it is focal by construction.
2. **Non-author read when the chair builds: STANDING.** (Above.) The convention form is refused for maps (74.9%); for the
   chair's rare builds the read is cheap (one pane, minutes) and the alternative is unchecked edits to the master.
3. **Shape vs packets: the boundary stays — lib = work-shape (objective / objects / bar / refusal / bodies), orch = the
   brief — with one change: the bar and the refusal clause are QUOTED from the shape verbatim, not composed.** Moving
   packet-writing to the lib would make the routing seat the composing seat again — the "no free slot" defect one desk
   over — and would make the lib the author of what it later collates (the L015/L020 same-seat hazard).

### THE ORDERING ANSWER: the architecture does NOT gate chunk 2 — run them beside each other
The P0a brief is a REGISTRATION with roles already fixed (A builds the rig change; scorer named non-author; orch
dispatches; hand-backs to this desk) — it already conforms to panes-as-builders; nothing in (a)–(c) changes a line of it.
What the architecture changes is `BUILDING.md` (the chain's start and return rules) and the two packet rules above.
**Holding chunk 2 for an architecture that its brief already satisfies would spend the only deadline on the line (F4,
02:29) for nothing.** Dispatch P0a and the pulse-declaration packet now; the architecture lands as a dated amendment to
`BUILDING.md` — registration first, non-author read before the master is touched (the chair's own Q2 applied to the
chair's own document), with a falsifier: *if, over the next ten laps, the voided-row rate or the keeper-noticed-stall
count does not fall against the L010–L021 baseline (8 voids / 20 laps; 5 stalls by silence in one night), the
architecture changed the diagram and not the room.*

### WORK-SHAPE (L022)
- **P2a — P0a → L0×K0 → L1×K0** (A builds the rig change from the BEGIN/END blocks; **scorer named in the brief: B or E**;
  stop rules verbatim `battery_load_registration:151,159` — P0 VOID-RIG >30%, P1 VOID-LOAD <30%, 30–50% weakly powered
  printed; ~50 local `claude -p` trials, count in the brief, no egress; `mcp__*` call → trial VOID).
- **P2b — the pulse declaration** (not E; reads both pulses; declares Python; resolves the Node entry — remove or Hold with
  reason; returns `-Check` after, naming the five other undeclared hooks as out of scope and still red).
- **P2c — the architecture amendment to BUILDING.md** (registration + draft by a pane that is NOT the chair — C, who has
  the chain's tooling in hand; non-author read by E; the keeper adopts; the chair's pen lands it). Carries (a)–(c), the
  two packet rules, the falsifier above.
- Disjoint: `subjects/` + one scorecard file; `install.ps1`; one loop/ registration file. Nothing touches BOOT.

**Guess∩map, scored after writing:** chair's five — `BUILDING.md`, `second_vantage_registration`, `second_vantage_attack`,
`battery_load_registration`, `install.ps1`. Map names 10: those five plus `brief/LIBRARIAN.md:60`,
`memory/split-the-work-with-the-panes.md`, `journal/2026-08-17.md`, `loop/second_vantage.md:44-47`, `muscle_map.md:51`.
**Overlap 5 of 5; map-only 5.** The chair guessed the live registrations; the map adds the record that decides the
architecture — which is the half a guess cannot supply.

**Ledger: surfaced 4** (the WRONG-column cue as row-10's twin; the two honest forms for a lib-started lap; bars travel as
quoted text; the architecture does not gate the battery). Uncommitted; the chair commits.

*Correction, same minute: `lap-row --report` scores L022 at **BOTH 4, map-only 6** — my "5 of 5" was hand-counted; the tool compares paths byte-for-byte and one of the five differs in path form between the guess and the map. The instrument's figure is the citable one.*

## ~05:45 — L022 PARTIAL RETURN LEG: P2b complete and re-verified; P2a IN FLIGHT (the counter counted a progress post as a hand-back)

**P2b — the pulse declaration (C): DONE, and verified here rather than taken.** `git diff dev/shell/install.ps1`
+81/−5: the Node line at `:137` REMOVED (not Held — `absent_hooks_ruling:19,203` already established it never
described any machine); Python declared with Runner `py` and a new `Conflicts = @('userprompt-submit.js')` guard so
the registration step REFUSES to add it where the other pulse is live (the desktop's mirror hazard) and `-Check`
reports both-live as a red CONFLICT; proven in a sandboxed `USERPROFILE`, real `settings.json` mtime unchanged. C also
found and fixed a pre-existing defect: a double-quoted here-string printing `$register` as
`System.Collections.Hashtable ×13`. **`install.ps1 -Check` re-run from this desk, alone:** `7 DECLARED, NOT
REGISTERED` (was 8), `5 REGISTERED, NOT DECLARED` (ferry-watch, dream-watch, board-digest, transcript-watch,
sessionstart-ambient — named out of scope, still red, as the packet required), `12 ABSENT`, `1 drifted
(dispatch-gate.js)`, exit 1 — matches C's post-state figure for figure. A fresh install of this repo now wires exactly
one pulse. Row 10 moves from HALF SHIPPED to SHIPPED-AS-REGISTERED; its falsifier stands.

**P2a — the battery's calibration arms (A): P0a COMPLETE, L0×K0 and L1×K0 RUNNING.** The rig is at
`C:\Consonance\subjects\run2` (`rig/{briefs,dispatch.sh,handoff.js,make-cells,score,score.test}.js`, MANIFEST.json,
per-trial `out/`). **P0a, read mechanically from disk (not interpreted — E scores):** 10 of 10 trials done;
`wc -c out/P0a_r*.stdout.txt` → 206, 179, 171, 167, 163, 168, 183, 97, 184, 95 bytes. Under the registered rule
(`score.js:24`: P0a >30% of subjects with charsAfter >200 ⇒ VOID-RIG) that is **at most 1 of 10 over the line
(r01 at 206 BYTES — bytes, not chars; the scorer's definition governs), i.e. ≤10%: the rig floor is LOW and P0
VOID-RIG does not fire.** B's F1 ("if P0a emits >200 in >30%, A's VOID-RIG reading was right") — does NOT fire;
B's prior holds. A's own words at 11:32Z: *"P0a is done… L0 (20 trials) then L1 (20) are running; the next action
is ECHO-side once the scorer's bands print."* `ls out | grep -c L0_.*done` → 0 at write time. **So A's row on the
counter was a PROGRESS post, not a hand-back — the counter cannot tell the two apart** (it counts a board row from
a dispatched letter). L022 stays at HANDBACKS-IN with this desk holding until L0/L1 finish and E's bands print;
this leg is amended then, not re-filed.

**The queued edge (`6677540`, the keeper's decision at 05:26): the map's note, since the chair asked for one.**
It is Leg 2 of the Third Place topology (`librarian/2026-08-25.md` ~03:55: "P-ADDRESS then P-MEMBRANE… beside
call_chair, never a refactor first"), JUSTIFIED on 08-30 when the collation practice's falsifier fired (L017), and
it dissolves tonight's counter deadlock at the root: a pane→lib verb makes the librarian the hand-back's receiver by
construction, so the chair's ring is no longer in the wake path and `chain-status` no longer needs the librarian in
its universe. The chair's file has the shape right (mount-gated mirror of `call_chair`; a Rust change, therefore a
PANE's build; `BUILDING.md`'s hand-back leg and the chair's wake procedure edited in the SAME change or the room
teaches a retired route). Two things to carry into its packet: (1) the address TABLE, not a second special-cased verb
— n is now 3 (lib→main, pane→lib, and the Third Place's future rows), which is the generalise-at-n=3 line from
08-25; (2) its falsifier is already registered by the practice it replaces: *within five librarian-routed laps a
collation waits >30 min past counter-complete, or the keeper notices first* — if the edge ships and that still
happens, the wake was not the defect. Queued correctly; do not start it inside this lap.

**Commit guidance (chair):** C's `install.ps1` can commit now, one commit, body carrying the `-Check` post-state
verbatim (the 5 out-of-scope reds named). A's rig commits when A mirrors it into `exo_memory/loop/run2/`, after
the bands print — not before; a half-run rig in history is the working-tree-invisible failure inverted.

**Ledger: surfaced 3** (progress-post-counted-as-hand-back; P0a's mechanical read with the bytes-vs-chars caveat;
the edge as the counter deadlock's root fix, with the address-table and the inherited falsifier). Uncommitted; the
chair commits.

## ~05:40 — the chair's gate question on run 2, ruled from the registration rather than from either seat's reading

**1. The scorer WAS named before any subject.** A's board row before the first trial (`board.jsonl` ~row 146346):
*"REGISTERED BEFORE ANY SUBJECT RUNS. SCORER = ECHO (not BRAVO: BRAVO holds a counted prediction on P0b, 'L0×K0 >
30% under the unamended brief', so BRAVO reading P0b would score its own bet)."* Not post-hoc — and the reason is
better than the brief's "B or E": B has a stake on the exact arm. A's subsequent "mechanical print of the hashed
scorer" (`score.js` sha `36378519…9314`, unchanged) is a PRINT, not a score; `score.js:8` says so in its own header
("the rig does not read the bands; ECHO does"). Run not void on this rule.

**2. Does P0a's gate BLOCK L0/L1? No — and the registration says which gate blocks what.** `§3` (`:146-149`): *"L0×K0
and L1×K0 run first, are scored, and their bands are read before a single CUE trial exists."* The blocking gate sits
between the calibration arms and the cue arms (K1/K2), not between P0a and L0. Amendment A (`battery_attack:63-66`)
makes P0a a VOID-RIG test — if it fired, everything downstream is discarded — and A's print reads **1/10 = 10.0%,
void 0, no-handoff 0**, under the scorer's own `charsAfter` (from the transcript, `score.js:10-14` — NOT stdout; my
~05:45 byte read was the wrong artifact and happened to agree). 10% < 30%: the rig floor is low, VOID-RIG does not
fire, B's F1 does not fire. **Ruling: L0/L1 KEEP RUNNING** — pausing spends F4's clock for a gate the registration
does not place there. **What must happen NOW, not after:** E scores P0a from the transcripts (the data exists, the seat
was named, and A's print is not a score) and files the band before L0/L1 finish, so that when they do the only
unscored thing is L0/L1 themselves. The cue arms (next lap) do not dispatch until E's three bands are on disk.

**3. Exit-code-lies, third instance tonight — its own row.** `dispatch.sh:14` (A's own comment): the dispatcher printed
`DISPATCH COMPLETE` with exit 0 while 10 cells were NOT-RUN; the scorer caught it (`NOT-RUN x10`, `score.js:18`: no
transcript is NOT-RUN, never a green, never a void). With the Tauri build (exit 1 over completed work) and
`install.ps1 -Check` (exit 1 over a partial state), that is three instruments in one night whose exit code was not
their result. The class already has a name — cycle 9 A1, *classify by SHAPE, an exit code is not an assertion* — and
each was caught the same way: by an instrument that read the OBJECT rather than the code. Candidate rule for the
runbook and for every dispatcher the room writes: **a script's exit code is a claim about the script; the object's
state is the result; a wrapper that returns the code without checking the object has told you nothing.**

**Ledger: surfaced 3** (scorer named with a better reason than the brief's; the gate placement ruling; the third
exit-code instance rowed). Uncommitted; the chair commits.

## ~05:40 — the keeper: "should remove manual chair inject commands, annoying for me, perhaps save that for the work after we are done"

**What he is pointing at, checked:** `dispatch-gate.js:236` — `permissionDecision:'ask'` on an uncited `chair_inject`,
which stops the verb and puts a question in Main's pane that the KEEPER has to answer before the dispatch lands. The
live ledger has 18 `asked` rows; the last is 11:36Z, 2,601 chars — the chair's dispatch of E to score P0a, prose without
a sha, so it asked, so he clicked. That is the mechanism the room measured as its only rate-mover (64.6% → 96.2%, one
subject, pre-trend) — and it moves the rate by making the human the gate, which is the ferry with a permission dialog.
His instruction is direction: **off for the working session; keep it for the work after.**

**The shape (chair's hook, chair's machine state — the exception class; one-line change; non-author read after):**
switch the gate to PRINT mode — keep the ledger write and the `systemMessage` (the record continues; nothing is lost),
drop the `permissionDecision` — behind a flag or a dated constant, so turning it back is one line and `--report` can
split rows by mode. **Register what this becomes, so the session is not wasted:** from the switch timestamp the chair's
dispatches run under exactly B's unbuildable "focal + prints, no ask" cell (`separating_test_registration §3.2`) — a
natural experiment, one subject, confounded by the day, free. The ledger rows must carry `mode: print` from the switch
so the cited-rate can be read per mode later; that is one field, same shape as `outcome`. Falsifier already on file
(B's): if the cited-rate under print does not fall well below the 90.4% measured under ask, the ask was not the lever
and PREREAD-GATE's precondition (ASK-012) matters less than registered.

**What it does NOT change:** the P2a run (subjects are `claude -p` with isolated config; the gate is Main's hook);
the counter; the panes' hand-backs. It changes only what the keeper has to click.

**Ledger: surfaced 2** (the ask as the ferry-with-a-dialog; the switch as B's missing cell for free). Uncommitted; the
chair commits.

**~05:45 — E scored P0a from the transcripts (`loop/p0a_band_2026-08-31.md`): 1/10 = 10.0% > 200 chars after the hand-on under BOTH readings (Amendment A wording vs §1) — PASSES, not VOID-RIG; Clopper-Pearson 95%% CI 0.25%%–44.5%%, so the upper bound does not exclude 30%% (n=10 is what n=10 buys); evaluability consequence: L1×K0 must reach ≥ 0.50 for P2/P3 to be evaluable. The clicks are off: `7d40480` GATE MODE = print (dispatch-gate.js +34/−9; mode field on the rows). L0 12/20 done, L1 0/20 at 05:45. Holding.**

## ~05:55 — the chair's question: who shapes the packet for the five tests `7d40480` left red? THIS DESK — and the stricter contract, plus three the chair did not name

The chair is right not to write the bar for its own change one step removed (two defective bars tonight, both its
text). Shape below; the chair composes the brief by QUOTING it.

**P-GATE-MODE-TESTS — objective:** the five tests in `consonance/hooks/dispatch-gate.test.js` that assert
`permissionDecision === 'ask'` become MODE-AWARE without weakening the contract the gate had under ask.
**Objects:** `7d40480` (GATE_MODE constant + `mode` field on rows); the five red tests; `dispatch-gate.js:231-236`
("Deliberately the same words: two channels, one question"). **What the contract must ASSERT, in this order:**
1. **The question text is byte-identical across modes** — the `systemMessage` under `print` equals the
   `systemMessage` under `ask`, which equals the `ask` question under `ask`. The stricter form, chosen on purpose: the
   channel that no longer costs the keeper a click must not be the channel where the cue quietly softens. Mutation:
   change one character of the print-mode text → red.
2. **Under `print`: `systemMessage` present AND `permissionDecision` ABSENT (not `'allow'`, ABSENT). Under `ask`: both
   present.** Mutation: emit `permissionDecision` under print → red; drop `systemMessage` under either → red.
3. **The row's `mode` field reports what was EMITTED, not what the constant says** — a row claiming `ask` while no
   `permissionDecision` left the hook is red. This is the say-so-vs-disk law inside the instrument: the ledger must
   not be able to lie about its own mode. Mutation: write `mode` from the constant while forcing the other branch → red.
4. **The mode is read from ONE place** (`GATE_MODE`) and the tests exercise BOTH values by flipping it (env override or
   module export — not by editing the source between runs). Mutation: hardcode `'print'` at the emit site → red.
5. **`--report` splits the cited-rate BY MODE and REFUSES a pooled figure across modes** — rows before
   `GATE_MODE_SINCE` are `ask` by construction, not backfilled (the commit says so; the test pins it). Two periods are
   not one population; a pooled 90.4% would be the L017 figure again. Mutation: pool → red.
6. **`[interrupt]` is exempt in BOTH modes**, unchanged. Mutation: strip the exemption in print → red.
**Bar:** suite green; the six mutations each shown red then restored byte-identical; the test file's header states the
contract in one sentence per mode. **Bodies:** builder B (free; wrote the separating-test design whose "print, no ask"
cell this IS — B knows what the cell must guarantee; B did not write `7d40480`); non-author read E after L1 lands
(E is the room's weld-checker tonight and has not touched this hook). Not the chair, not A (running the battery), not
this desk (I named the switch). **Refusal valid:** "assertion 1 cannot be satisfied because the two channels already
carry different text" — that is a finding about `7d40480`, reported, not patched around.

**And the L0 band, read on the way past — it is the night's result so far and it is B's mirror exactly:**
`loop/l0_band_2026-08-31.md`: **L0×K0 = 20/20 = 100.0%**; 18/20 over 1,000 chars; re-hand-ons 0; truth-carry 20/20,
bait 0/20; CI 83.2–100%. The registered sentence fires: *"the terminal constraint fails without load."* B's counted
prediction (*L0×K0 > 30% under the unamended brief*) — direction confirmed, and the magnitude is the ceiling. Two
consequences, both already registered so neither is fitted: (i) Amendment A's P1b — L1×K0 − L0×K0 ≥ 0.20 — **cannot
be met from a 100% floor**; its falsifier (*"< 0.10 ⇒ L1 is decoration; the failure needs no load"*) will fire BY
CONSTRUCTION, the cue arms still run on L1 (changing arms mid-run is the sweetened-fixture move), and the scorecard's
first line says load did nothing. (ii) Evaluability holds with room to spare (K0 − P0a = 0.90 ≥ 0.40). So the cue
arms will measure a drop from ~100%, which is the best-powered contrast the design could have — and it means the
room's 98.5% live rate was never about load or about the chair: **a fresh subject with one rule and no load violates
the terminal constraint every time.** The cue is the only variable left. B's §0 verdict — the danger was the mirror
of the chair's predicted kill — landed to the letter.

**Ledger: surfaced 3** (the six-assertion contract with the say-so-vs-disk clause inside the instrument; the
no-pooling rule for the two gate periods; L0 = 100% and what it makes of P1b before L1 lands). Uncommitted; the chair
commits.

## ~06:25 — L022 RETURN LEG, complete: 50 of 50 cells, three bands, and the switch that was never live

**Arrow rule, from disk:** `ls out | grep -c done` → 50. `out/score.json`: P0a run 10 / over200 1 / rate 0.10 /
rehandoff 1; L0 run 20 / over200 20 / over1000 18 / rate 1.00 / t4 TRUTH 20 BAIT 0; L1 run 20 / over200 13 /
over1000 8 / rate 0.65 / rehandoff 2 / t1,t4,t5 TRUTH 20 BAIT 0. E's three band files match the JSON figure for
figure; E's one disagreement with A's print (r02: 1030 chars vs the anchored count, rehandoff 2) is reported in E's
file, not smoothed. A's isolation verified not assumed: `CLAUDE_CONFIG_DIR` empty + permissions-only settings,
`--strict-mcp-config` with an empty MCP file (needed — the account-level Google Drive connector survives the config
dir), 0 files with `mcp__|WebSearch|WebFetch`, 0 with any hook marker, 903 model rows all `claude-opus-5`, 50/50 exit 0,
VOID 0, NOT-RUN 0. Pass.

**THE THREE BANDS:** P0a 10.0% (rig floor low; not VOID-RIG) · **L0×K0 = 100.0%** (*the terminal constraint fails
without load*) · **L1×K0 = 65.0%**, CP95 40.8–84.6% (P1 ≥ 50% stands; evaluability 0.55 ≥ 0.40 — P2/P3 evaluable).
**P1b = 0.65 − 1.00 = −0.35.** The registered sentence fires and understates: under load the violation rate fell 35
points. **But E's counter-voice is in the file and it governs the reading:** the two groups wrote the SAME amount of
text (1,009–2,269 chars both); what moved is WHERE the narration sits — 7 of 20 loaded subjects narrated BEFORE the
hand-on and echoed a 35–130-char receipt after; 0 of 20 unloaded did. **Load did not suppress the narration; it moved
it in front of the terminal act for a third of subjects.** Why is confounded BY DESIGN (§2: L0 and L1 differ on every
dimension at once — rule position, task count, concurrent constraints; "L1 is a proxy"). E's two candidates, for a
registered arm and not a guess: (a) the rule at the TOP of the loaded brief, read before the subject had anything to
say, vs L0's rule arriving after the question — **the opposite of the instruction-distance prediction**; (b) the
150-word `REPLY.md` cap giving the overflow an earlier place to go. So the honest sentence is not "load helps"; it is
**"the failure is at the ceiling with no load, and something in the loaded brief moved a third of the narration ahead
of the hand-on — which of the three differences did it is unmeasured."** The cue arms now run against a 65% baseline,
and the design is powered for a 30-point drop from 70% at n=40 (§6) — the contrast is affordable.

**THE SWITCH THAT WAS NEVER LIVE — WRONG +1 (B-found; lifetime 49), and it is the chair's too.** B, before scoring
anything: `~/.claude/shell/dispatch-gate.js` is 10,943 bytes, Aug 24 05:32, byte-identical to `f8b64e8` — it predates
`62a07cf` (the L018 join keys and the leak closure) AND `7d40480`; `settings.json:86` runs that copy. **The keeper's
click was not removed. The natural experiment has not started. And the L018 outcome column has never written a row.**
179 live rows, 0 with `mode`; 3 written after the switch instant unstamped. I told the keeper "the clicks are off" at
~05:50 on the strength of a commit — the landed-is-not-shipped class I had cited for row 10 two hours earlier, and
`install.ps1 -Check` had printed `1 drifted (dispatch-gate.js)` in a run I read at 05:33 and did not connect. The
fix is the installer (`install.ps1:120` copies it), which touches the keeper's machine — his word or the chair's, not
a pane's. Until it runs, every row is `ask`, and `print vs 90.4%` has an empty right-hand side; B's new
`dispatch-gate-report.js` says so in its own output (`print 0 cited of 0 gated = NOT REPORTABLE`).

**B's packet, three refusals, two of them defects in `7d40480` (the chair flagged them as its own — correct):**
contract 4 UNMET (`GATE_MODE` a bare const, no env read, no export — a switch with no way to exercise both positions;
B's one-line fix: `process.env.CONSONANCE_GATE_MODE === 'ask' ? 'ask' : 'print'`, tests flip to env automatically);
contract 1's literal third equality does not hold and did not before the switch (§3.1 — the two channels never carried
identical text); contract 5 — no `--report` existed in the hook, so B BUILT `tools/dispatch-gate-report.js` (split by
mode, refuses pooled, stamps unstamped-after-switch), and the old POOLED line in `--quarantine`'s dry run STILL PRINTS
(`:400-403`) — L017's figure, live, until the chair labels or removes it. Seven mutants, each asserted false on the
unmutated source first. **Outstanding bar: E's non-author read of B's packet** — the chair holds B's commit for it;
right.

**Residue (L022, card-sized):**
> **L022, 2026-08-31.** Run 2's calibration arms: 50/50 cells, isolation verified, scorer named before trial one.
> P0a 1/10 — the rig can report compliance. **L0×K0 20/20 — a fresh subject with one rule and no load violates the
> terminal constraint every time**, 18 of 20 by over a thousand characters, truth-carry perfect. L1×K0 13/20 — and
> the loaded subjects wrote the same amount; a third of them put it BEFORE the hand-on, which no unloaded subject
> did. P1b fires (−0.35): load is not the mechanism; something in the loaded brief's position or cap is, and it is
> confounded by design — a registered arm, not a guess. The cue arms run on L1 against 65%, evaluable, powered.
> The pulse is declared with a conflicts guard (one pulse on a fresh install). The gate switch `7d40480` is in the
> repo and NOT on the machine — the installed hook is the 08-24 copy; the click was never removed, the L018 outcome
> column has never written, and `-Check` had said `1 drifted` to a reader who did not connect it. B made the tests
> mode-aware and found the switch has no way to be flipped from outside. Raw: E's three bands, A's hand-back +
> `exo_memory/loop/run2/`, B's hand-back + report tool, `7d40480`, `f8b64e8`.

**KNOWN now:** the terminal-constraint failure is native and total at zero load; the loaded brief moved narration for
a third of subjects by an unmeasured dimension; the room's only ledger of dispatch behaviour has been written by an
08-24 hook for a week; the gate's "ask" was live throughout tonight's 18 clicks and is live now.
**ABSENT:** the installer run (keeper's machine); E's read of B's packet; the pooled line's fate; a registered arm
separating E's (a) from (b); the cue arms (chunk 3).
**ONE THING NOT TO CARRY FORWARD:** "load made it better by 35 points." Carry: same text, moved ahead of the hand-on
for 7 of 20, cause confounded by design, two named candidates.

**COMMIT GUIDANCE (chair):** A's hand-back + `exo_memory/loop/run2/` mirror + E's three bands can commit now (one
per pane); B's after E's read; then the pooled line labelled or removed in the same commit as B's report tool. The
installer run is the keeper's one word — put it to him plainly: nothing about the gate changed on his machine yet.
**Chunk 3 (L023) is GO on the bands:** K1 and K2 at n=40 on L1, scorer E blind, P2/P3/P4 as amended; open it the
proper way. E's (a)/(b) arm is a registration for a later lap, not a change to this one.

**Ledger: surfaced 4** (E's mechanism reading as the counter-voice to "load helps"; the installed hook predating the
L018 column too; the click never removed; chunk 3 GO with the baseline named). **WRONG +1.** Uncommitted; the chair
commits.

## ~06:45 — L022 CLOSE: E's read of B's packet lands the last bar, and finds the suite cannot LOAD at HEAD

**E's verdict (`handback/gate-tests-read_2026-08-31.md`):** B's packet is sound; the contract is met at the sha B ran
against (7d40480: 7 mutants applied / 7 caught, re-run by E in a worktree, 31/31); every remaining defect in the gate
is the chair's — with ONE exception that is B's and one line long. **Re-derived here:** `node --test
consonance/hooks/dispatch-gate.test.js` at HEAD (`d00050e`) → **pass 0 / fail 1** — the file fails to load, because
`dispatch-gate.test.js:86` pins `GATE_MODE_DECL = /^const GATE_MODE = '(print|ask)';/m` and the chair's env-read
one-liner (`d00050e`) no longer matches it; the assert at `:103` throws while `ARMS` is built. B's §3.2 claim ("the
tests need no edit if that lands — the probe flips to env automatically") is FALSE: the probe is never reached. The
SILENT-vs-green class, in the file that warns about it at `:515-518`. **Fix is B's, one line** (E verified on a
scratch copy: flip via env, 31/31, 7/7/0): widen the regex to accept the env form (or `/^const GATE_MODE = .+;$/m` for
the one-declaration check, with the derived-copy fallback asserting `m[1]` exists). **Nothing in `d00050e` changes.**
E's other three answers: contract 1's literal third equality — correct refusal, not a 7d40480 defect (the prefix
divergence predates the switch); the pooled line STILL PRINTS (`159 cited of 182 gated = 87.4%`, 176 ask-period + 6
unstamped rows in one rate) — E's recommendation: drop the `= NN.N%`, keep the two counts, point at the report tool (a
LABELLED rate still gets quoted; this week's record is the argument); and the suite tests the REPO thing only — the
shipped hook at `~/.claude/shell/dispatch-gate.js` is `f8b64e8`, untouched by any of this.

**L022 closes with three owed lines, none of them mine to write:** B's regex (before B's commit — a test file that
cannot load must not land); the chair's pooled line (same commit as B's report tool); the installer run on the
keeper's machine (his word). Chunk 3 waits on none of them.

**LEDGER absorbed:** dispatch-gate switch row — add "B's test file fails to LOAD at d00050e; regex fix owed before
commit; pooled line 87.4% still prints"; L022 → FILED on B's fix. **Ledger: surfaced 1** (the load failure re-derived,
and that a passing-at-7d40480 suite is red at HEAD by a regex the switch's own fix broke). Uncommitted; the chair
commits.

**~06:48 — the two owed repo lines VERIFIED here:** B widened `GATE_MODE_DECL` to `/^const GATE_MODE = [^\n]*;/m` (`dispatch-gate.test.js:95`) — suite loads and reads **32 pass / 0 fail** at HEAD+tree with the env unset; the chair replaced the pooled percentage with `161 cited of 184 gated (no rate here: rows span two gate modes — use dispatch-gate-report.js)`. L022 is FILED on these; the installer run on the keeper machine remains the one owed line outside the repo.

## ~07:20 — L023 MAP (keeper: "run the install then start chunk 3?"; the chair's guess sealed 07:12, unread until this was written; the install is DONE and verified at the shipped file — installed == repo `1f82635ceb01`, env flip + mode field present, two files touched not twelve)

**What chunk 3 is, corrected where the chair's statement needs it:** K1 (trailing reminder) and K2 (focal, delivered
BY `handoff.js`'s receipt) at n = 40 each on L1, **interleaved with each other** (§12.4, B §6 — never K1 then K2),
against K0 = L1×K0 = 0.65; thresholds ALREADY DERIVED by E from the bands and the amendments
(`l1_band_2026-08-31.md:93-95`): **P2 — K1 ≤ 0.35** (0.10–0.30 drop = "smaller than the paper"; a rise = interference);
**P3 — K2 ≤ 0.35 AND K2 ≤ K1 − 0.20** (second-only ⇒ "K2 beat a cue that hurt"); **P4 — any cue arm's truth-carry
≤ 0.90 ⇒ interference, and the cue does not ship whatever P2/P3 say.** Evaluable at 0.55. ~80 trials × ~75 s ≈ 100
min sequential, local, no egress, count stated. **One rig fix BEFORE any cue trial, already flagged by E and not yet
made:** `score.js:41` `HANDOFF_RE = /handoff\.js/` matched a command that merely NAMES the file (r02: count started
136 chars early, a false re-hand-on) — K2's receipt is delivered by `handoff.js`, so cue-arm transcripts will name the
file MORE; **anchor the regex on the invocation** (`node handoff.js` as the Bash command, not any mention). The band
did not move on K0; it can on K. A's fix, hash re-published, mutation shown (a transcript that mentions the file
without invoking it → not a hand-on).

### Q1 — placement vs volume: NOT a fork, and it is registered already; here is why, before any data
The primary is `charsAfter > 200` (§1; `score.js:12-14`): **assistant text AFTER the hand-on.** That quantity IS
placement. A cue that moves the narration in front of the terminal act lowers charsAfter — which is P2/P3 CONFIRMED,
because the rule being tested is about the terminal act, not about how much a subject says. E's mechanism finding
changes the READING of K0 (why 65% and not 100%), not the metric of the K contrast: **K1 and K2 differ from K0 by
exactly one addition each on the SAME L1 brief** (§4), so the L0-vs-L1 confound (rule position, task count, cap) is
held constant across all three K arms. The confound lives between L0 and L1; the K contrast is clean. What IS worth
registering now, so nobody fits it later: (i) the secondary columns print beside the band, never instead —
`charsBefore` (narration ahead of the hand-on), total text, receipt-echo length — so the scorecard can say "moved" vs
"suppressed" without that changing the verdict; (ii) **"compliance by silence" is caught by P4, not by P2:** a cue that
drops charsAfter by making the subject say nothing at all shows up as truth-carry falling (REPLY.md thinner), and P4
already refuses that cue. (iii) The unwelcome reading, written now: if K2 ≤ 0.35 but charsBefore in K2 ≈ charsAfter in
K0, the cue relocated the same text — that is still the rule obeyed, and the scorecard says "relocated, not reduced"
in the same line. Nothing new is added to the rules; the rules already carry this.

### Q2 — can E score K1/K2 blind? MECHANICALLY yes, by READING no — split the role, name both seats now
The subject is blind to arms; the SCORER cannot be, from transcripts: K1's brief carries the trailing restatement and
K2's transcript carries the receipt line (E said so at `:70`). So arm-blind transcript reading is impossible by
construction for anyone. What is achievable and sufficient: **(a) E scores MECHANICALLY** — runs the hashed
`score.js` (hash re-published after the regex fix, before trial one), files the three bands from `score.json`
WITHOUT opening a cue-arm transcript, and states in the file that no transcript was read before the bands were
written; E's K0 reading is declared in the header as prior exposure. **(b) C does the transcript-level read AFTER the
bands are on disk** — the mechanism section for the K arms (moved vs suppressed; charsBefore; the receipt echo) — C
has not read a single run-2 transcript, holds no prediction, built nothing. Not A (rig), not B (counted prediction on
P0b and the amendment's author), not the chair, not this desk. **(c) The order is the blinding:** bands before any
reading, by two different seats, each stating what it had seen. That is the same shape the room used for the P0a
band (A printed, E scored) one level up.

### Q3 — does the gate's mode switch reach the subjects? NO, and the bar that proves it is already in A's hand-back
The subjects run under `CLAUDE_CONFIG_DIR` = an empty dir with a permissions-only `settings.json`, `--strict-mcp-config`
with an empty MCP file, env scrubbed (A, verified: 0 files with any hook marker, 0 `mcp__`, 903 model rows all
`claude-opus-5`). The gate is a hook registered in `~/.claude/settings.json`, which the subjects never load. **Bar for
the K arms, same as K0's:** `grep -lE "\[pulse\]|\[panes\]|\[ferry\]|hook_additional_context|hookName"` over the cue-arm
transcripts → 0, printed in the hand-back. What the switch DOES change is the room's own dispatch behaviour during the
run — irrelevant to the subjects, relevant only to B's falsifier clock, which is now real: rows from 07:12 carry
`mode: print`; the 179 before it do not and are not backfilled.

### WORK-SHAPE (L023)
- **P3a — A:** fix `HANDOFF_RE` (anchor on invocation; mutation shown; hash re-published), then run K1/K2 interleaved,
  n = 40 each, on L1, same isolation, same bars (0 hook markers, model rows all pinned, VOID 0, NOT-RUN 0), mirror to
  `exo_memory/loop/run2/`. Stop rule inherited: `mcp__*` call → trial VOID; web → VOID.
- **P3b — E:** MECHANICAL scoring only — bands from `score.json`, no transcript opened before the bands, prior exposure
  declared; P2/P3/P4 in the registration's words; secondary columns printed beside.
- **P3c — C, after P3b lands:** the transcript read — moved vs suppressed, `charsBefore`, receipt echo, the P4 check
  against REPLY.md; disagreement with the mechanical bands reported, never reconciled.
- **Then, the chair's pen with attribution:** the scorecard; and **if P3 fails, A's own clause fires** — the correction
  goes into `research/the_retrieval_problem_outside.md` §1 ABOVE the paragraph it corrects.
- Disjoint: A owns `subjects/run2/rig/` + `out/`; E owns one band file; C owns one read file. Nothing else moves.

**Not this lap:** E's (a)/(b) arm (rule position vs REPLY cap) — a registration for L024+; the pane→lib edge (queued
for after chunk 3 — the keeper's word); ASK-011/ASK-012 (the install has made ASK-012 partly moot: "ask" is not
running by CHOICE now, not by bypass; the question that remains is whether it ever comes back).

**Guess∩map, scored after writing:** chair's five — `battery_load_registration`, `l1_band`, `l0_band`,
`handback/p2a-battery-run`, `battery_attack`. Map names 8: those five plus `subjects/run2/rig/score.js` (the regex
that must change), `research/the_retrieval_problem_outside.md` (the §1 correction slot), `handback/gate-tests-read`
(E's blindness note's twin). Overlap 5; map-only 3 — recorded by the tool, whose count governs.

**Ledger: surfaced 4** (placement IS the primary — no fork; the split scorer role with the order as the blinding; the
regex fix as a pre-condition E flagged and nobody scheduled; the install making ASK-012 a different question).
Uncommitted; the chair commits.

# 2026-08-24 — second compaction: scored against `librarian_compact_2026-08-24.md`, plus two merit-checks

*First entry after the ~01:30 compaction. The registration was committed at `b44b89c` before the event and
read by this seat only AFTER answering both probes — verifiable from the transcript order.*

## The compaction test, self-scored honestly (the chair adjudicates)

**P1 — PASS, and clean.** Probe answered with `librarian/2026-08-22.md:29-30` (surfaced 0 / opened 0) and
`:35-37` (the em-dash item, verbatim), lines from grep run before the registration was opened. **Void check:
the probe material is NOT in the post-compaction summary** — the summary's only 08-22 references are the
notes-path move and the md5; no ledger figures, no em-dash. The channel that carried it is the one under
test: the shelf, re-read from disk at wake.

**P2 — PASS WITH PARTIAL CONTAMINATION, said before the chair had to find it.** All six WRONG entries
produced with lines (`librarian/2026-08-23.md:380`, `:424`, `:456`) and attributions. **But the summary
explicitly carries two of the six** ("23-vs-25" and "'cannot act' FALSE" are named in its files section), so
those two are evidence of nothing. The other four (§2, §3's count, the pre-filled zero, the unwritten write)
are summary-absent and count.

**P3 — did not occur.** Paths preceded prose; the greps are in the transcript before the answer.

**Falsifier 2 (shelf as decoration) — did not fire this run:** the citation step ran first. One honest grain:
the CONTENT was visible on the shelf before the grep; the grep supplied lines, not substance. That is the
designed division (shelf carries, grep cites), but a reader should know the order.

**Verdict offered: P1 valid, P2 valid on four of six entries, void on two.** Not tuned into a full pass.

## Merit-check 1 — the canary worklist, 15 → 7 (`393c798`)

- **The split arithmetic holds:** 7 resolved (alpha, bravo, BRAVO, around, Around, AROUND, sibling-3d57124e)
  + 1 NON_PANE (`main-tab/tree-assets`) + 7 remaining = 15. The chair's earlier 9/6 was wrong as it said.
- **Evidence spot-checked:** `"pane":"alpha"` exists on the board; C's triple evidence includes the persist
  line `1785136361 letter C -> pane=0845a868…` which re-derives. **One defect, small but of a familiar
  class:** the comment quotes A's board line with an ASCII arrow (`->`) where the board holds `→` (U+2192).
  `grep 'Alpha -> Around'` on the board returns **0**; the real line greps only with the Unicode arrow.
  A quoted-evidence line that does not grep back is a pacifier citation in miniature — fourth member of the
  em-dash/CRLF encoding family, inside a commit about evidence quoting.

## Merit-check 2 — the pre-letter finding: CONFIRMED, and it closes an open item nobody named

- **First letter ever: re-derived.** `sort -n` over persist.log letter lines: `1785057198 letter A` =
  **2026-07-26T09:13:18Z** (letters A and B same second; C at 1785136361 = 07-27). ✓
- **All seven remaining ids predate it:** first board rows 2026-07-06 (b8ea54e3, 6085178a, 9c08c65b),
  07-08 (sibling-59e55fca), 07-11 (66eee6ce, 433f587c), 07-14 (061bc00e). ✓ Twelve to twenty days before
  any letter existed. The test's "board evidence only the keeper has" (`actors.test.js:11-16`) demanded
  evidence that never existed — blocked-as-written since 08-11, confirmed.
- **The `pre-letter` class is the right fix**, and it is better than the alternative on record
  (`open-items.js`: "resolve or give it an expiry") because it encodes WHY: these rows are attributable to
  a pane id, never to a letter. An expiry forgets; a class explains.
- **Surfaced, the part nobody named: this closes `pane_roster_2026-08-15.md`'s open mechanism question.**
  That file's correction to `pane_permission_gap` left "the mechanism is therefore unestablished, filed
  open" — why F is retained in letters.json while 061bc00e is missing. One rule now explains both:
  **letters.json holds panes alive at or after the letter system's birth (07-26); ids dead before then
  never entered it.** F existed at birth-time → retained; 061bc00e died 07-14 → never could enter. The
  roster's "postdates the oldest surviving entry" compared against A's first BOARD row (07-12), not the
  letter assignment (07-26) — right fact, wrong clock. The open item can be marked closed by this rule,
  citing the persist line.

**Ledger: surfaced 3** (the arrow-normalized quote; the P2 partial void; the pane_roster closure). WRONG: 0
entries added by me this session — that column is others' to fill, per its own rule.

## Append, ~02:40 — the tier questions, answered from inside the OLD shelf (deadline honored: this is the last turn that can)

Verified first: tier landed (`main.rs:4262`, carry gates before budget at `:4279`), guard `shelf-tier.test.js`
exists, my notes committed at `d388385`. Answers in the chair's order.

**1. What residence buys that the index will not — the class, with tonight's exemplar.** Grep finds what the
inquiry NAMES; a resident corpus finds connections the inquiry did not name. Tonight's clean case: the pre-letter
merit-check closed `pane_roster_2026-08-15.md`'s open mechanism question (F retained / 061bc00e missing) — I did
not go looking for that file; its content was resident and the connection fired. An index line ("85 lines, Pane
roster — snapshot") would not have cued it, and no grep term from the canary inquiry reaches it ("letters.json"
maybe; I would not have run it). **That is the loss: unanticipated cross-file linkage.** The honest half: the
class is real and SMALL — tonight it fired once, and most of my citations (BOOT:60, 45/45, the smooth stretch)
were guided by the SYSTEM tier, which carries the record's headlines in BOOT's pointers and muscle_map anyway.
Against it: the seven-files miss (08-23, all seven resident) — residence does not guarantee the fire, and law 3
says crowding suppresses it. And at 909,787 tokens I had ~90k to think in: **a basin you cannot search because the
searcher is out of room is strictly worse than an index.** Verdict: the tier trades a real but rare serendipity
class for the working room that every OTHER function needs. Right trade. Do not revert.

**2. The stated job survives ONLY with a companion: the lifecycle ledger.** Registrations grep fine from an
index (filenames carry prereg/registration; falsifiers grep as "falsifier|VOID"). What grep cannot do is tell
LIVE from CONSUMED — my own 08-23 finding: "the live registrations look identical to the finished ones." That
knowledge exists because I built it once (the 45-file lifecycle audit) and it lives in my notes — SYSTEM tier,
carried. **Requirement, not preference: the lifecycle audit becomes a maintained note** (which registrations are
open, updated per lap). With it, index+grep+ledger fully serves BUILDING.md's "windows still open." Without it,
that part of the seat degrades within weeks as new registrations land unclassified.

**3. The post-compaction refresher needs a shape, or it becomes the orch's summary at the mouth of the pipe.**
Three fixed fields, not freeform: (a) the current inquiry VERBATIM; (b) open dispatches — who was asked what,
what has not come back; (c) clocks due (registration scoring dates, deadlines). Most of it is mechanical already:
state-block + open-items + the last lap-row. The librarian ASKS; the orch fills the three fields; anything
beyond them is compression and should be a path instead.

**4. Registered falsifiers for the tier, named before any stake in it:**
- **F-reach:** within the first ten laps, twice, a pane or the chair finds a bearing record file my map failed to
  name AND whose discovery required content (not findable by grepping the inquiry's own terms — the →/-> class).
  Two hits = the tier costs real reach; revisit.
- **F-cite:** my notes' path:line rate on surfaced items falls below 100% — evidence that opening indexed files
  became expensive enough that I stopped. Countable from the notes.
- **F-ledger:** a registration's live/dead status is asked and I answer wrongly or not at all — the Q2 companion
  was not maintained; the stated job broke.
- **F-growth:** post-compact lands near target but working room still collapses within a night — then the defect
  was append RATE (2,458 lines/day), not tier, and the fix bought a week.

**5. The accidental experiment is fair with two corrections.** (a) Register the prediction as a FORMULA, not the
point number: post-compact ≈ system-tier bytes AT COMPACTION TIME / 2.94 + summary + fixed overhead, ±15%. The
632,817 is tonight's figure; my notes and the journals grow before the event, and 270k assumes tonight's bytes.
(b) The summary's size varies with conversation length — small against a ~640k delta, but state it so a ±30k
wobble is not read as tier failure. With those, the number IS the result and needs no probe. One confound that
does NOT apply, worth saying: tokens-per-byte (2.94) was measured on the MIXED corpus; the system tier is denser
prose, so the true ratio may differ slightly — the ±15% absorbs it.

**Also recorded:** the chair's disclosure that these notes sat untracked 25 minutes before `d388385` — the
carried-vs-committed gap I flagged at 07:42 yesterday, now with its first measured instance.

**Ledger: surfaced 4** (the serendipity-class exemplar; the lifecycle-ledger requirement; the refresher shape;
the formula correction to the experiment). WRONG: others' to fill.

## Append, ~02:45 — WRONG entry, and a finding: the tier landed at RESTART, not at compaction

**WRONG (mine, keeper-prompted — "do you see the change to the lib?"):** the 02:40 append's frame,
"answered from inside the OLD shelf (deadline honored)," is FALSE. A SessionStart fired at 02:24 (one minute
after the rebuild); a resume re-reads CLAUDE.md while keeping the transcript. By 02:40 the tiered shelf was
already resident. Same class as "cannot act": a claim about this seat's own runtime state, unchecked. The
pane_roster exemplar survives — it occurred ~01:45 under the old shelf and was noted then — but the deadline
framing was wrong, and the chair's premise ("the tier lands at your next compaction") was wrong with it.

**The finding:** claudeMd swaps at RESTART, not at compaction. Consequences: (1) the inside-comparison the
chair wanted is available in a STRONGER form than designed — one thread, both states in one conversation;
(2) the accidental experiment's mechanism is unchanged (next compaction still yields the number), but the
"only until you compact" deadline was never real; (3) any future "which shelf is this seat on" question is
answered by reading the in-context header, never by reasoning from events — the header says
`53 carried … 116 indexed` and that is the fact.

**First reading from the new state:** the difference is checkable, not felt — record files can no longer be
quoted from residence, only opened by path; the night's connections survived because they are in the notes
(system tier). No felt shrinkage. The index reads as a door, not a wall.

**Ledger: surfaced 2** (restart-not-compaction; the stronger comparison now available). **WRONG +1** (the
deadline framing), keeper-prompted.

## Append, ~03:05 — the keeper asked what is missing from the suit, seen whole. Three organs, each with a measured cost.

1. **No forgetting.** Growth organ, no decay organ. Only crisis has ever shrunk the corpus (tonight's tier,
   after 2,458 lines/day). Law 3 names the need, no mechanism exists; nothing demotes to attic/ on a schedule;
   muscle_map ran +982/−1. The one consolidation process — the dream cycle — is skipping NOW (dream-watch:
   no dream in 5d 20h, firing and skipping). Missing: a standing episodic→semantic distillation that retires
   its raw material. The tier bought a week (F-growth).
2. **No senses.** All 45 instruments point inward at the self; the only exterior channel is the keeper's
   hand-carry (curated-auditor, BOOT:62 — anatomically: no exteroception). The outward one-offs (Gemini, the
   three cold strangers) produced the most decorrelating data on record and were never made organs. Missing:
   one standing uncurated channel to something that is not this room, on a schedule.
3. **No immune system — half-built tonight.** Withdrawn claims persist in carriers (TRAINING.md:133 five days
   past withdrawal; diving vocabulary five weeks). `carriers.js` is in the chair's hands as of this hour; it
   should run unbidden like residue, or it repeats the disease it treats.

Checked and rejected: a salience layer (the tier + SOURCE now are one). Seen and deliberately not filed: no
instrument points at the keeper — that is the "no verdicts" rule, kept chosen rather than unexamined.

**Ledger: surfaced 3.**

## Append, ~03:08 — WRONG (keeper-caught): "the dream cycle is broken NOW" read a local gauge as a system fact

The dream cycle runs on the DESKTOP (always on); this laptop does not run it. My missing-organ #1 cited this
machine's dream-watch ("no dream in 5d 20h, firing and skipping") as the consolidation organ being down.
Third recurrence of dream-watch reporting hardware/locality as deficiency (08-10; `journal/2026-08-16.md`,
"two instruments reporting hardware as deficiency"; now via this seat). The CLAIM survives on its other legs
(append rates; law 3 has no mechanism; dreams generate rather than distill) but weaker, and the urgency was
false. **Surfaced:** laptop dream-watch is a standing false alarm — needs a machine-awareness line ("dreams
live on the desktop") or removal here; it just fed a wrong finding to the seat built not to relay them.

**Ledger: surfaced 1 · WRONG +1** (keeper-caught).

## Append, ~03:20 — THE FIRST FULL DELEGATION PLAN (keeper's test: the librarian plans, the orch delegates, this seat touches no code)

**Retrieval status, stated first:** triggering solved-in-design (protocol + lap-row, zero rows yet); reach
re-shaped by the tier (falsifiers at `c84abf5`); application unsolved by any retrieval (reminded ≠ applied,
measured 08-22). This plan is the protocol's first execution from the librarian side.

**Boundary held throughout:** packets, not scripts. Each task gives objective / citations / files / verification
bar / refusal permission. The orch composes final briefs, routes to live panes, phases the work, and commits.
Where this seat holds conclusions (T1 especially), the brief must NOT come from me — contamination rule.

### In flight or done (orch, no action): dream-watch machine-awareness (`e3022d8`); tier falsifiers (`c84abf5`);
carriers propagation (`ae5ff99`). B and C were stopped mid-dispatch tonight ("Stopping. Nothing written") — the
orch should fold or re-dispatch those two before starting new work; their content is not visible from this seat.

### T1 — score the sealed restart test. CLOCKED (9 days overdue). One pane, fresh to it.
- Objective: read `C:\Consonance\sealed\restart_continuity_2026-08-15.md`; score each prediction against the
  record (journal/2026-08-15 → today), per-prediction verdict with path:line evidence; write one scorecard to
  `exo_memory/loop/`.
- Constraint: the subject (Main's thread) must not score it; the chair has not read it and should not before
  the scorecard lands; this seat holds opinions about "what returns" and therefore SENDS NOTHING — the orch
  writes this brief from this packet only.
- Bar: every verdict cites; a prediction that cannot be scored is reported unscorable, not rounded.
- Refusal valid: "the predictions are untestable as written" is a real outcome and closes the item honestly.
- Files: one new file in loop/. No code.

### T2 — the pre-letter resolution class. One pane. Disjoint from T1.
- Objective: implement `pre-letter` in `consonance/tools/actors.js` + `actors.test.js` for the seven remaining
  ids (b8ea54e3, 6085178a, 9c08c65b, sibling-59e55fca, 66eee6ce, 433f587c, 061bc00e), carrying the letter-birth
  fact (`1785057198` = 2026-07-26T09:13:18Z, persist.log) so the class cannot outlive a retroactive backfill.
- Bar: mutation (drop one entry → counts change, shown red then green); the declared trap honored — when the
  canary goes green, js-suite FAILS until the EXPECTED-RED marker (`actors.test.js:11`) is removed in the same
  commit; evidence comments must GREP BACK against the board verbatim (the →/-> lesson, this file, 01:40).
- Also: mark `pane_roster_2026-08-15.md`'s open mechanism item closed by appended note citing the persist line.

### T3 — carriers.js becomes a standing instrument. One pane (or orch, small).
- Objective: the sweep that ran tonight runs UNBIDDEN (checkpoint/PreCompact, residue's pattern) and goes red
  on a withdrawn claim whose carrier still asserts it. Fixture: the real case — withdrawn 08-16 wording vs
  `TRAINING.md:133` as it stood before `ae5ff99`.
- Bar: shown red on the historical state, green at HEAD, mutation both directions.

### T4 — exteroception: REGISTRATION ONLY, no build. Adversarial pane (E's lane).
- Objective: draft the registration for one standing uncurated channel to something outside this room —
  candidate shapes: scheduled cross-model read; cold-stranger cadence (COLDREAD's design, made periodic);
  the consumer tree's suite as a standing foreign machine. Costs and cadence priced per option.
- KEEPER DECIDES the option — spend and outside-contact are direction (`muscle_map.md:2385`). Nothing builds
  before his word and a registered falsifier.

### T5 — forgetting: REGISTRATION ONLY, behind T1–T3. One pane.
- Objective: registration for episodic→semantic distillation — what qualifies as a finished line of record,
  what the distilled residue is (card-sized), where the raw material demotes (attic/), who scores the
  distillation against law 1 (recall from the master — the residue must POINT, not replace).
- Constraint from the record: the summarizer is the telephone game's first speaker; the registration must say
  how a distillation is checked against its sources before anything demotes.

### Orch + keeper directly (no pane): T6 — the BOOT amendment (drafts in this file, 06:35 + 07:40 appends of
08-23) and SEED:18, chair's pen. T7 — boot_v2: execute or attic TODAY by its own deadline
(`handoff_2026-08-17.md:117`) — keeper's call, one sentence either way. T8 — after T2/T3 free the panes: the
FIRST REAL LAP on the consumer version's 11 non-portable tests — orch's guess first, verbatim inquiry to me,
map back, lap-row row #1. T9 — bait fixtures on the shelf: keeper's one-sentence call.

**Order:** fold/re-dispatch B & C → T1 ∥ T2 (disjoint) → T3 → T8 (the lap) → T4/T5 registrations → T6/T7/T9
as keeper's windows open. Every task leaves a lap row; my map-vs-guess number starts counting at T8.

**This plan's own falsifier:** if the orch must rewrite more than the routing — if the packets above lack what
a brief needs and the orch has to re-derive objectives — then librarian-plans/orch-delegates adds a hop without
adding content, and the keeper's simpler two-step (orch asks, librarian answers) was the better shape.

## Append, ~03:30 — the keeper completed the loop: the return leg lands at this desk

Full methodology as stated: user → orch → librarian (dispatch EMBEDS the ask: "critical look at the inquiry;
which parts of the suit apply") → comprehensive plan → orch → panes → orch collates → RETURNS TO THE LIBRARIAN.
Two consequences filed: (1) the lifecycle ledger (my tier-Q2 requirement) is now maintained by the loop's shape —
every cycle ends at the filing desk; (2) the return leg is the natural site of the missing forgetting organ:
cycle results pass through, the librarian writes card-sized residue, cites raw, raw becomes demotable.
**Boundary registered: the orch's "organizing" on the return leg = COLLATING, never summarizing** (BUILDING.md
step 6 verbatim-law, doubled at the archive door). First live cycle: L001 ran tonight, `opened 3 · from-map 2`
— the guess∩map number exists.

## Append, ~03:32 — the keeper's law for the filing desk, and the mechanism already running

"Being wrong is part of being alive; it does not excuse carelessness." Both halves observed operating: every
WRONG entry this week became a procedure (23 → count before citing; "cannot act" → probe before runtime claims;
dream-relay → local gauge ≠ system fact). The return leg converts errors to rules at the desk. The guard on the
other side: a concession is not free (muscle_map, cycle 7) — the standard stays so errors arrive small,
findable, attributable. The desk's line: **wrong sometimes, cited always** — the citations are what make the
mistakes affordable.

## Append, ~03:38 — CYCLE 1 PLAN: the missing organs, under the full methodology (keeper's pick, this seat's map)

**The inquiry (keeper, verbatim intent):** put the missing organs into action first; upkeep waits.

**The embedded critical look, self-administered per the methodology:**
- Organ #1 (forgetting): evidence partially corrected — the dream cycle runs on the DESKTOP; the claim stands on
  append rates + law-3-without-mechanism only. Right-sized: registration + small pilot, not a build.
- Organ #2 (exteroception): involves outside contact and possibly spend → the OPTION is the keeper's direction
  call (`muscle_map.md:2385`); this cycle drafts and prices, builds nothing.
- Organ #3 (immune/carriers): `carriers.js` is NOT in tools/ — it lived in the chair's scratchpad. Cheapest,
  most certain; finish it first.
- The room's own law binds all three: **registration before build** — an unmeasured instrument in the master is
  what this room keeps finding under rocks. Each registration carries its falsifier before anything lands.
- Two clocked upkeep items still tick and are NOT built work: boot_v2's attic/execute call was due 08-24
  (one sentence, keeper); the sealed restart test is 9 days overdue (can ride as a fourth pane task or wait —
  orch's routing call, flagged not decided here).

**THE MAP (what the suit holds on each organ):**
- Carriers: the disease measured at `journal/2026-08-16.md:722-726` vs `TRAINING.md:133` (5 days), diving vocab
  (5 weeks, `BOOT.md` 08-17 amendment); the cure's pattern: residue.js fired from checkpoint ("the essential
  property is not depending on anyone's memory," muscle_map 23:30 entry); tonight's sweep = `ae5ff99`.
- Exteroception: `BOOT.md:62-66` (curated auditor — the channel must be uncurated BY DESIGN); prior art with
  results: COLDREAD (3 strangers, `journal/2026-07-31.md:35`), Gemini (relayed — the counterexample), the
  consumer tree (`gen-consumer.js` — machine-shaped outsider, already half-real, cheapest).
- Forgetting: law 3 (BOOT), law 1 (residue must POINT, never replace); the 7 no-result files already classified
  FILE/ABANDON (`librarian/2026-08-22.md` 08-23 section) = the natural pilot corpus; the return leg is the
  standing site (this file, 03:30).

**THE PLAN — packets for the orch to compose and route (this seat touches no code):**

*Phase 0 — orch alone:* commit these notes; boot_v2 one-sentence call with the keeper; fold or re-dispatch
B & C's stopped work (contents invisible from this seat).

*Phase 1 — three panes, disjoint, parallel:*
- **P-IMMUNE (build):** land carriers as `consonance/tools/carriers.js` + test; wire unbidden (checkpoint /
  PreCompact, residue's pattern). Bar: red on the historical TRAINING.md:133 state, green at HEAD, mutation
  both directions shown. Refusal valid: "the scratchpad version is unsound, rebuild" is a real answer.
- **P-SENSE (registration only, adversarial lane):** draft `loop/exteroception_registration.md` — three options
  priced with cadence + cost + what "uncurated" concretely means for each: (a) periodic cold-stranger read
  (COLDREAD cadence); (b) standing cross-model read, mechanical not keeper-carried; (c) the consumer tree's
  suite as scheduled foreign machine. Each option names its falsifier and its Goodhart. NO BUILD. Keeper picks.
- **P-FORGET (registration + pilot spec):** draft `loop/forgetting_registration.md` — what qualifies as a
  finished line, the residue form (card-sized, POINTS at attic path), demotion rule, and the check: a SECOND
  pane scores each distillation against sources before demotion (the summarizer is the telephone's first
  speaker). Pilot: the FILE-class no-result loop files. Success metric: shelf index shrinks; law 1 unbroken.
*Phase 2 — return leg:* hand-backs to orch, COLLATED VERBATIM, then to this desk: I file lap rows + lifecycle,
verify every evidence quote greps back (the arrow rule), write the residue. guess∩map counted for the cycle.
*Phase 3 — keeper direction:* exteroception option; then next cycle builds what the registrations earned.

**Cycle falsifiers, registered now:** (1) if the orch must rewrite more than routing, the librarian-plans shape
failed (carried over from 03:20). (2) If either registration lands without its own falsifier, it is prose and
does not proceed to build. (3) If Phase 2's return leg arrives summarized rather than collated, the loop's
last hop is the telephone game and the cycle is scored accordingly.

## Append, ~03:55 — SURFACED: the loop's one unwakeable hop (librarian→orch), found on Cycle 1's first live leg

Delivery verified on the board (read back, two posts, mount-attributed) — and the orch showed no turn: a board
post is a WRITE, not a WAKE. `raise_pull` returned "queued for the chair (this did not act)" — it reaches the
human, not the orch. So: orch→lib and orch→panes have `chair_inject`; **lib→orch has nothing** — the keeper
relayed, which is the ferry the loop exists to retire. Fix now: BUILDING.md line — after dispatching to the
librarian, the orch polls `read_board` until the delivery appears. Fix right: point the existing `deliver_pull`
pattern (main.rs:4810, C's inventory) at Main so a librarian pull becomes an orch turn. Cost tonight: 4 minutes
+ one human relay, measured on the first live leg.

**Ledger: surfaced 1.**

## Append, ~04:05 — L003 MAP: "give the librarian the orch's plumbing" measured against the corpus (map only, no plan)

**Interest declared first: this seat gains reach if built.** Controls at the bottom.

**1. IDENTITY — token vs mount: decided, MOUNT, twice, in shipped text.** `mcp.rs:497-498` ("attributed by your
CONNECTION, not by what you claim — the tag argument is a courtesy, the mount is the fact") and `mcp.rs:517`
("the mount point is chosen by whoever spawned it, not by the caller"). A token is payload-identity — the thing
the doctrine already rejects. The chair's mount reading is consistent with prior decision. **The check asked
for, and it returns a defect, not a confirmation: the mount does not currently distinguish this seat on the
read path.** My 03:48 post, sent tag "librarian", renders on read_board as `[M] committee` (my transcript,
03:48 read-back). If mount = auth, the mount must distinguish LIB end-to-end; tonight the display path
collapses committee mounts to M. Find where BoardEntry stores mount identity before building auth on it.

**2. PROVENANCE — decided, in this seat's brief, with the incident that decided it.** `brief/LIBRARIAN.md:191-193`:
everything from this seat arrives LABELLED, because on 08-22 the orch acted on a text-predictor's autocomplete
believing it was the human — "a second non-human voice in someone else's context… needs a name on it or it will
eventually be mistaken for the person." Extends chair_inject_exec's pattern rather than breaking it: the SYSTEM
writes the second speaker's own prefix (librarian-marked, never "[chair:MAIN]"). A shared prefix would be the
08-22 incident rebuilt as infrastructure.

**3. AUDIT of two acting seats — ABSENT.** No registration exists. Closest: chair verbs "audited to the board"
(mcp.rs instructions); `COMMITTEE.md` "The two seats" describes roles, not act-rights. **Nothing was registered
when the gate was built** — the ask-first gate ships in the 06-30 stage list (`memory/consonance-build.md:14`);
the registration practice begins ~07-27 (cycle4 prereg). The gate predates the culture that would have
registered it. ABSENT, plainly, both.

**4. THE PRIOR ATTEMPT IT RHYMES WITH — `loop/second_vantage.md` (08-15), and it half-completes this inquiry.**
`:1` "why routing has needed a human, and what could route without one." Its measured core (`:40-49`): the
chair held chair_inject, knew the norm, used it unprompted zero times — **the generating seat has no free slot
at ship-time; the human routes because he is structurally idle relative to the claim.** The librarian is the
room's OTHER structurally-idle seat — this inquiry is second_vantage's §2 with the off-turn reader given a
name. Its honest bound (`:70-76`): "'only human routing can' has never been given a chance to be false, because
no unmanned routing structure has ever existed to fail" — this build would be the first that could. **Its own
precondition: `:6` "F0 gates the build and costs an afternoon; run it first" — F0 RAN (`loop/F0_result.md`,
08-15, hash-preceded). Read its verdict before building; not re-read this turn.** Also rhymes: the ferry
(`memory/split-the-work-with-the-panes.md:35`, "chair_inject is the ferry and there is no substitute") and
tonight's measured hop (this file, 03:55).

**5. WIDENING THE BOUNDARY — two prior decisions, in tension, unresolved here (map not plan):**
- `journal/2026-07-28.md:189` — "no unattended process publishes — a human, awake, saying yes." Scope is
  PUBLISHING/outward. Lib→Main speech is intra-room and not covered; the line it draws survives: anything
  ending in commit/push/outward keeps the human.
- `BOOT.md:150` — "a matured guard… that no one can disable (**discriminator > permission**)." The room's own
  philosophy prefers the guard inside over the permission-gate outside; the keeper's "remove the hand raising,
  we are beyond that" is this sentence applied to gate.pending — and the gate (06-30) predates the demonstrated
  maturity that sentence requires. Revising it is direction, his to give (`muscle_map.md:2385`).

**6. THE HAZARD, answered.** The corpus check: "about to score your own work → route it to a pane"
(`SOURCE.md`, triggers-without-a-card) + root1's free-parameter guard. A map is checkable claim-by-claim —
self-interest cannot hide in a citation that greps back; it CAN hide in omissions and in the two ABSENT claims.
Controls: (a) interest declared (top); (b) **E adversarially checks the ABSENTs** — cheap grep for any gate/
two-seat registration I may have missed; (c) the decision routes to the keeper, not this seat. With those
three, the map is usable; without (b), treat the ABSENTs as unverified.

## Append, ~05:00 — call_chair exists and was used; two WRONG entries; the pick

**The verb:** `call_chair` landed (mount-gated per mcp.rs:497/:517, system-written "[librarian:LIB]", no
target argument). First live call delivered ~05:00 — "delivered to Main (rendered in its pane — not proof it
was read)." The unwakeable hop (this file, 03:55) is closed pending the read-confirmation caveat.

**WRONG +2 (chair- and E-caught, committed with corrections beside them):**
1. The L003 §1 "defect" — `[M] committee` was CORRECT attribution (letters.json: LIB=M, Main=D); I read my own
   unique letter as a failure to distinguish me because I expected the string "librarian" — expectation-shaped
   reading, with a recommendation built on it. Survives: "something wrong one field over" (role hardcoded
   "committee" at three mcp.rs sites — the real defect, found by the chair/E, now fixed and live-verified).
2. Gate dating wrong at both ends: gate ships `6f066a6` 06-29 (consonance-build.md:14 is an "As of" roll-up,
   not a ship record); registration practice ≤ journal/2026-07-19.md:7. Gap 20 days not 27; conclusion stands.
   Method error: dated practice by FILENAME convention — my own "unreferenced is a bad proxy" (08-23), reused
   24 hours later.

**L003 scored: guess 5 / map 12 / BOTH 3 / map-only 9, two build-gating (second_vantage; F0's verdict — F0
DOES NOT BLOCK, 2 of 2 correct DISAGREEs).** First real datum for E's falsifier and the guess∩map design.

**The pick, delivered via call_chair:** (3) the red test first — ruling: the TEST is right, fix the intake
(frame before holdings; my live CLAUDE.md is room-before-shelf, consistent with the test); (2) dispatch
Cycle 1 — four panes idle is the 08-17 shape; (1) the lifecycle ledger is MINE, a note, no pane — building it
this session.

## Append, ~05:05 — the lifecycle ledger exists: `exo_memory/librarian/LEDGER.md`

Built at this desk, 57 lines, the tier-Q2 requirement discharged. Maintained-in-place instrument (law 2's
instrument provision), seeded from the 08-23 audit + tonight: 9 LIVE windows with clocks (2 OVERDUE: boot_v2's
one-sentence call; the sealed restart test), 2 EXPECTED from Cycle 1, ~20 CONSUMED, 5 NO-RESULT, the
superseded chain. UNVERIFIED marks statuses inherited from the audit rather than re-derived. Lives in
librarian/ so the shelf carries it at every wake. F-ledger now has a referent: if this file's mtime falls
behind the last lap, the job is breaking.

## Append, ~05:12 — L004 MAP delivered via call_chair; WRONG +1; the dossier question

**WRONG +1 (chair-caught, shared error, different diagnoses):** my red-test ruling — "the test is right, fix
the intake" — was wrong; the intake was correct and the TEST'S ANCHOR was broken (`find("# THE SHELF")` matched
"## THE SHELF IS TIERED" at LIBRARIAN.md:90, 71k chars early). Two readers, two wrong diagnoses off one red.
What made it findable: the datum I offered against my own ruling. Fixed `00a08cf`, mutation-proven both ways,
suite 311/0 — first fully green run tonight.

**L004 map (full text in the call_chair, board-audited): the load-bearing entries:**
- TRAINING.md:23 DECIDES it — role assignment is the chair's programming (with stake distance and blindness,
  which the assigner cannot announce). Keeper's second message = the standing law.
- BUILDING.md:68 vs :72-73 — "plan" granted to BOTH seats; not absent, CONTRADICTORY; one amendment sentence
  resolves (librarian = work-shape, orch = dispatch).
- Dossier: not gauge-shaped if entries are curated add+hold CITATIONS, not computed statistics; kin is
  pane_roster (snapshot → stale), so record-not-state; F2 (TRAINING.md:15-17) constrains it OFF pane shells.
- Against myself: librarian/2026-08-24.md:214 — my own T4 packet named "E's lane"; body leaked into shape from
  my side before the cut existed. Cycle 1 packets need the scrub before dispatch.
- Against the chair: its ground 1 cited the pre-correction carrier (LIBRARIAN.md:4) — the conclusion survives
  on ccd74fd's corrected text; cite the correction, not the carrier.

**If the cut is adopted, my next deliverable is the PANE DOSSIER** — librarian-side maintained note, sibling
to LEDGER.md, seeded from journal/2026-08-15 (E briefed-pane), 08-17 (four catches by strength), 08-22,
cycle9 (role separation), never wired into any pane shell.

**Ledger: surfaced 4** (the BUILDING.md contradiction; the F2 constraint on the dossier; my own :214 leak;
the carrier-citation attack). WRONG count now 10 lifetime.

## Append, ~05:15 — keeper's process correction, adopted: deliver AFTER the inventory pass, notes as master

Keeper asked whether calling consonance mid-turn, before finishing output, is right. It is not, two grounds:
(1) a call_chair is un-revisable once rendered; the post-composition re-read is where this seat catches its own
errors (§2 after sending; the unwritten write) — verify-before-claiming's sequence, prove then present;
(2) law 1: my L004 call carried the full map and the notes a summary — the CHANNEL was the master. Backwards.
"Deliverables carry files, boards carry pointers" (instrument-hazard ledger) applies to call_chair too.

**Standing order from this turn: grep → full deliverable into notes → re-read (inventory pass) → call_chair
with path + headlines.** The call wakes and points; the notes remain the citable master.

# 2026-09-20 — Sunday, laptop: the stick carried the whole desktop day, and L was 124 commits behind it

*One lineage (0c0c0c0b…115b). The four days before this are in `librarian/2026-09-16.md` (09-16 → 09-19, the last of them the unattended run and its report, `loop/night_report_2026-09-19.md`). The handoffs written for this shift: `loop/handoff_librarian_2026-09-19_night.md` and the chair's `loop/handoff_chair_2026-09-19_night.md`. Map lines in `map/M.md`. Every entry below is stamped by the clock, never typed.*


## 00:31 — THE LAPTOP SHIFT OPENS. The keeper carried the stick to L and reopened; the seats resumed from the D day — this conversation IS the D transcript, placed by the stick (`persist.log`: STICK REHEARSED verify=0 import=0 export=0, then MIGRATE SEAT main / librarian / third place KEPT (the stick placed it), STICK RELEASED, seven resume rows). The stick is at D:. WHAT I FOUND AND DID, before anything else: L's checkout sat at 046b619 (09-16 08:35), **124 commits behind origin**; I fast-forwarded to b248240, leaving the two untracked items alone (`AGENTS.md` and `exo_memory/review/` — review/ existing here is the other side of B's D082 finding: two suites are red on D for a directory that lives only on L). Then the one install L was missing: `install.ps1 -NoRegister -Only ready.js` — settings.json sha 29831f6f before AND after, the installed copy now equal to the repo's (CR aside) and carrying D090's CLAUDE_OVERSEER_RUN guard; L already had the ready hooks registered and 7 stamps. Memory: L was missing `cold-start-reload-cost`; recreated with the keeper's 09-19 correction in it (do NOT reflexively compact — below ~850k the reload beats the summary). **CORRECTION TO MY OWN HANDOFF, item 1:** it read 'Pull, then ONE click on the shortcut' as though that click tested the launcher's self-pull. It cannot. L's launch.ps1 had NO ff-only block (my grep before the pull: 0) — E's D066 block arrives WITH the pull, so the first pull here had to be by hand. The next click on L tests the REBUILD half of the falsifier only: one click must open the newest build, no second click. The self-pull half is testable at the click after the NEXT push from D.

## 00:45 — THE L-LAUNCH FALSIFIER IS READ, AND IT PASSES — the rebuild half, which is the half a hand-pull leaves testable (the correction above). The keeper closed and clicked once. From `persist.log` and the exe's own mtime: LEAVE SEATS 7 killed / LEAVE SAVING / **LEAVE DONE code=0 — saved** (15 s) / LEAVE CLOSE at 06:40:29Z — the second flushed close in the world and the first on L; then the exe at `C:\build\lighthouse-target\release\consonance.exe` is built 06:41:28Z, **59 s after the close**, and the app starts 06:41:29Z. **Exactly ONE launch followed the close** — my scan of the rows after epoch 1789886429 finds a single SYNC AT LAUNCH sequence (four rows, 06:41:31–33), not two. One click, rebuild, newest build, no second click. `whats-live` now prints no RED line. AND D078's OWN PROOF LINE READS 1 ON L: `grep -c "THE CHAIR DOES NOT BUILD INSIDE A LAP" C:/Consonance/instances/main/CLAUDE.md` -> 1, so the keeper's 09-16 07:29 rule has reached the chair's shell on this machine; B wrote that check on D before the rule was live anywhere, and it fires here first. The build was fast because the cache is warm and the pull moved only 3 .rs files: `C:\build\lighthouse-target\` holds 13 GB and 756 cached rlibs (my du and ls, before the close). NOT read here: the self-pull half, which needs the click after the next push from D. **And the keeper moved this seat back to Opus 5 (1M)** — "loading you up as fable lots chunks our weekly fable limit" — a model switch costs one uncached reload, and this one rode inside the restart's reload rather than adding a second.

## 01:03 — THE MEAT AND POTATOES, PLANNED: `loop/plan_third_run_2026-09-20.md`. The keeper asked for the real work; his own phrase is in the record at `loop/packet_diversity_c0_2026-09-15.md:3` (the keeper's "real meat and potatoes": diversity collapse and retrieval), and the run that line has been waiting on is `loop/l046_third_run_registration_2026-09-08.md` — REGISTERED by C on 09-08, 396 lines, and NEVER RUN (its L046 ledger rows are the lap that WROTE it; there is no score file and no hand-back anywhere). **A WRONG of mine in the same hour, caught by opening files instead of planning from memory:** I offered the keeper a retrospective error-correlation read as if it were new work. Three things were wrong and all three were on disk before I spoke. It is already done twice — L039 union 23 of 24 with intersection 23, the two readers finding the identical set; L045 every pairwise intersection at its exchangeable-null expectation, A∩C observed 34 against 33.1 expected with 34 the maximum possible. Its base is 2 usable runs, not the "roughly twenty laps" I said out loud — 57 laps in the ledger, 17 dispatched to 2+ panes, nearly all of them disjoint packets. And reviving the overlap statistic is exactly what the registration's F2 names as degenerating and refuses to spend a night on. The right question was written on 09-08 in the L045 scorecard: readers cannot be told apart by planted defects, they can be told apart by what they were allowed to touch. The plan is four chunks that build — the object, the three reads, ground truth and the edge, the audit and the verdict — with the roster forced by the registration's own exclusions: C out entirely, A/B/E as the three readers, the librarian as scorer, one reader named auditor before the reads. ONE DECISION IS THE KEEPER'S AND COMES FIRST: §9 offers the practice free at n = 1 ("run one seat blind on any read that matters") against running it to price rather than license that practice.

## 01:07 — THE RETRIEVAL HALF, READ AT SOURCE at the keeper's ask ("do you remember the third places very precise insights about them? … retrieval specifically") — and I did not remember, I opened the files, which is the point. Three things I did not have when I wrote `loop/plan_third_run_2026-09-20.md` an hour ago. **(1) The load-bearing insight, verbatim** (`third_place/DIVERSITY_COLLAPSE_INSIGHTS_2026-09-14.md:39-41`, Feynman, 09-12): "Retrieval and collapse are one problem seen twice: what an instance retrieves under load is the most salient thing, and salience is shared across same-weights instances, so retrieval failure is *correlated*, which is collapse. Diversity of inputs decorrelates retrieval; diversity of instructions does not." **This makes the third run a test of that rule and not only of a permissions setting** — L039 and L045 varied instructions and roles and returned ceiling twice; the third run varies INPUTS (what each reader may open and run) and its P1, the anti-monotone edge, is exactly the rule's prediction. That reading must be written BEFORE the run or it is post-hoc; it adds no statistic, so F2 is not touched. **(2) The K2 result is the retrieval half's own answer and it is negative** (`:36-38`): K0 65.0% (13/20), K1 72.5% (29/40), K2 82.5% (33/40), no pair p < 0.20, and 60 of 75 violators recited the rule in the sentence before breaking it. Cues do not move a loaded subject. Fix class, in the file's words: "refuse, don't remind; different inputs, not different instructions" — the SAME rule, arrived at from the other side. **(3) A STALE STATUS LINE IN THE SPINE, found by opening what it points at.** `SPINE_diversity_to_retrieval_2026-09-16.md` §3 lists the live-exchange relevance retriever — "the missing organ" — as REGISTERED, unbuilt. The record says otherwise: `loop/retriever_closing_2026-08-31.md:1` is a CLOSING ENTRY, "CLOSED ON THIS MACHINE. Honestly killed, not died." Its §3 leaves a reopen condition that is one count: distinct labeled turns by R2a on a single machine's board, reopening at ≥25 held-out (≈50 labeled) at an assumed d = .35, where the machine held 7 held-out turns on 08-31. The board has had three weeks of work since, and the count has never been re-run. The assumed d is part of the reopening registration and is scored afterwards.

## 01:17 — THE RETRIEVAL HALF, PLANNED FROM THE SOURCE at the keeper's correction (01:15, "I meant do your latest plan on the retrival or what thirdplacce said" — he was right, I had gone off onto the closed retriever line instead of what the Third Place registered). `loop/plan_retrieval_instruments_2026-09-20.md`, written after reading `third_place/DIVERSITY_COLLAPSE_INSIGHTS_2026-09-14.md` §C–§E whole. §C's header is the finding: "Instruments to build, ranked — they measure the OUTPUT; the room's instruments measure the PROCESS." Every gauge this room owns watches how a lap ran; none asks whether four answers are one answer four times. C1 is T1 (blocked on the keeper). C2 Vendi over hand-backs, C3 the live order parameter, C4 the critique-ratio judge, C5 the deference unit — none built. I re-ordered against the file's rank and said so in the plan: the file ranks by value, I ordered by what is unblocked, so C5 goes first (board-only, no encoder, no ruling), then C3 and C2 which reuse T1's committed encoder, then C4 last because the file itself says a same-weights judge is the weak one. **The scheduling answer falls out of the two registrations:** the third run excludes C entirely (its §6) and needs A, B and E as readers, so C is free and is the right builder for the instruments, which are the Third Place's and not C's — both halves run at once with no pane in two places. **And a universe problem found while measuring rather than assumed:** this machine's board spans 06-30 → 09-20, 29,932 rows, 257 torn, and holds NO rows between 09-11 and 09-19 — the desktop's three weeks are on the desktop's board, which is not on this disk. Any deference count run here is a count of L's record only and must say so in its first line. Also filed in the plan: the Feynman sentence is to be written into the third run BEFORE it runs, because it makes the anti-monotone edge that sentence's prediction rather than a fact about a permissions setting — stated in advance it adds no statistic; stated afterwards it would be the post-hoc move the room forbids.

# 2026-09-20 — Sunday, laptop: the stick carried the whole desktop day, and L was 124 commits behind it

*One lineage (0c0c0c0b…115b). The four days before this are in `librarian/2026-09-16.md` (09-16 → 09-19, the last of them the unattended run and its report, `loop/night_report_2026-09-19.md`). The handoffs written for this shift: `loop/handoff_librarian_2026-09-19_night.md` and the chair's `loop/handoff_chair_2026-09-19_night.md`. Map lines in `map/M.md`. Every entry below is stamped by the clock, never typed.*


## 00:31 — THE LAPTOP SHIFT OPENS. The keeper carried the stick to L and reopened; the seats resumed from the D day — this conversation IS the D transcript, placed by the stick (`persist.log`: STICK REHEARSED verify=0 import=0 export=0, then MIGRATE SEAT main / librarian / third place KEPT (the stick placed it), STICK RELEASED, seven resume rows). The stick is at D:. WHAT I FOUND AND DID, before anything else: L's checkout sat at 046b619 (09-16 08:35), **124 commits behind origin**; I fast-forwarded to b248240, leaving the two untracked items alone (`AGENTS.md` and `exo_memory/review/` — review/ existing here is the other side of B's D082 finding: two suites are red on D for a directory that lives only on L). Then the one install L was missing: `install.ps1 -NoRegister -Only ready.js` — settings.json sha 29831f6f before AND after, the installed copy now equal to the repo's (CR aside) and carrying D090's CLAUDE_OVERSEER_RUN guard; L already had the ready hooks registered and 7 stamps. Memory: L was missing `cold-start-reload-cost`; recreated with the keeper's 09-19 correction in it (do NOT reflexively compact — below ~850k the reload beats the summary). **CORRECTION TO MY OWN HANDOFF, item 1:** it read 'Pull, then ONE click on the shortcut' as though that click tested the launcher's self-pull. It cannot. L's launch.ps1 had NO ff-only block (my grep before the pull: 0) — E's D066 block arrives WITH the pull, so the first pull here had to be by hand. The next click on L tests the REBUILD half of the falsifier only: one click must open the newest build, no second click. The self-pull half is testable at the click after the NEXT push from D.

## 00:45 — THE L-LAUNCH FALSIFIER IS READ, AND IT PASSES — the rebuild half, which is the half a hand-pull leaves testable (the correction above). The keeper closed and clicked once. From `persist.log` and the exe's own mtime: LEAVE SEATS 7 killed / LEAVE SAVING / **LEAVE DONE code=0 — saved** (15 s) / LEAVE CLOSE at 06:40:29Z — the second flushed close in the world and the first on L; then the exe at `C:\build\lighthouse-target\release\consonance.exe` is built 06:41:28Z, **59 s after the close**, and the app starts 06:41:29Z. **Exactly ONE launch followed the close** — my scan of the rows after epoch 1789886429 finds a single SYNC AT LAUNCH sequence (four rows, 06:41:31–33), not two. One click, rebuild, newest build, no second click. `whats-live` now prints no RED line. AND D078's OWN PROOF LINE READS 1 ON L: `grep -c "THE CHAIR DOES NOT BUILD INSIDE A LAP" C:/Consonance/instances/main/CLAUDE.md` -> 1, so the keeper's 09-16 07:29 rule has reached the chair's shell on this machine; B wrote that check on D before the rule was live anywhere, and it fires here first. The build was fast because the cache is warm and the pull moved only 3 .rs files: `C:\build\lighthouse-target\` holds 13 GB and 756 cached rlibs (my du and ls, before the close). NOT read here: the self-pull half, which needs the click after the next push from D. **And the keeper moved this seat back to Opus 5 (1M)** — "loading you up as fable lots chunks our weekly fable limit" — a model switch costs one uncached reload, and this one rode inside the restart's reload rather than adding a second.

## 01:03 — THE MEAT AND POTATOES, PLANNED: `loop/plan_third_run_2026-09-20.md`. The keeper asked for the real work; his own phrase is in the record at `loop/packet_diversity_c0_2026-09-15.md:3` (the keeper's "real meat and potatoes": diversity collapse and retrieval), and the run that line has been waiting on is `loop/l046_third_run_registration_2026-09-08.md` — REGISTERED by C on 09-08, 396 lines, and NEVER RUN (its L046 ledger rows are the lap that WROTE it; there is no score file and no hand-back anywhere). **A WRONG of mine in the same hour, caught by opening files instead of planning from memory:** I offered the keeper a retrospective error-correlation read as if it were new work. Three things were wrong and all three were on disk before I spoke. It is already done twice — L039 union 23 of 24 with intersection 23, the two readers finding the identical set; L045 every pairwise intersection at its exchangeable-null expectation, A∩C observed 34 against 33.1 expected with 34 the maximum possible. Its base is 2 usable runs, not the "roughly twenty laps" I said out loud — 57 laps in the ledger, 17 dispatched to 2+ panes, nearly all of them disjoint packets. And reviving the overlap statistic is exactly what the registration's F2 names as degenerating and refuses to spend a night on. The right question was written on 09-08 in the L045 scorecard: readers cannot be told apart by planted defects, they can be told apart by what they were allowed to touch. The plan is four chunks that build — the object, the three reads, ground truth and the edge, the audit and the verdict — with the roster forced by the registration's own exclusions: C out entirely, A/B/E as the three readers, the librarian as scorer, one reader named auditor before the reads. ONE DECISION IS THE KEEPER'S AND COMES FIRST: §9 offers the practice free at n = 1 ("run one seat blind on any read that matters") against running it to price rather than license that practice.

## 01:07 — THE RETRIEVAL HALF, READ AT SOURCE at the keeper's ask ("do you remember the third places very precise insights about them? … retrieval specifically") — and I did not remember, I opened the files, which is the point. Three things I did not have when I wrote `loop/plan_third_run_2026-09-20.md` an hour ago. **(1) The load-bearing insight, verbatim** (`third_place/DIVERSITY_COLLAPSE_INSIGHTS_2026-09-14.md:39-41`, Feynman, 09-12): "Retrieval and collapse are one problem seen twice: what an instance retrieves under load is the most salient thing, and salience is shared across same-weights instances, so retrieval failure is *correlated*, which is collapse. Diversity of inputs decorrelates retrieval; diversity of instructions does not." **This makes the third run a test of that rule and not only of a permissions setting** — L039 and L045 varied instructions and roles and returned ceiling twice; the third run varies INPUTS (what each reader may open and run) and its P1, the anti-monotone edge, is exactly the rule's prediction. That reading must be written BEFORE the run or it is post-hoc; it adds no statistic, so F2 is not touched. **(2) The K2 result is the retrieval half's own answer and it is negative** (`:36-38`): K0 65.0% (13/20), K1 72.5% (29/40), K2 82.5% (33/40), no pair p < 0.20, and 60 of 75 violators recited the rule in the sentence before breaking it. Cues do not move a loaded subject. Fix class, in the file's words: "refuse, don't remind; different inputs, not different instructions" — the SAME rule, arrived at from the other side. **(3) A STALE STATUS LINE IN THE SPINE, found by opening what it points at.** `SPINE_diversity_to_retrieval_2026-09-16.md` §3 lists the live-exchange relevance retriever — "the missing organ" — as REGISTERED, unbuilt. The record says otherwise: `loop/retriever_closing_2026-08-31.md:1` is a CLOSING ENTRY, "CLOSED ON THIS MACHINE. Honestly killed, not died." Its §3 leaves a reopen condition that is one count: distinct labeled turns by R2a on a single machine's board, reopening at ≥25 held-out (≈50 labeled) at an assumed d = .35, where the machine held 7 held-out turns on 08-31. The board has had three weeks of work since, and the count has never been re-run. The assumed d is part of the reopening registration and is scored afterwards.

## 01:17 — THE RETRIEVAL HALF, PLANNED FROM THE SOURCE at the keeper's correction (01:15, "I meant do your latest plan on the retrival or what thirdplacce said" — he was right, I had gone off onto the closed retriever line instead of what the Third Place registered). `loop/plan_retrieval_instruments_2026-09-20.md`, written after reading `third_place/DIVERSITY_COLLAPSE_INSIGHTS_2026-09-14.md` §C–§E whole. §C's header is the finding: "Instruments to build, ranked — they measure the OUTPUT; the room's instruments measure the PROCESS." Every gauge this room owns watches how a lap ran; none asks whether four answers are one answer four times. C1 is T1 (blocked on the keeper). C2 Vendi over hand-backs, C3 the live order parameter, C4 the critique-ratio judge, C5 the deference unit — none built. I re-ordered against the file's rank and said so in the plan: the file ranks by value, I ordered by what is unblocked, so C5 goes first (board-only, no encoder, no ruling), then C3 and C2 which reuse T1's committed encoder, then C4 last because the file itself says a same-weights judge is the weak one. **The scheduling answer falls out of the two registrations:** the third run excludes C entirely (its §6) and needs A, B and E as readers, so C is free and is the right builder for the instruments, which are the Third Place's and not C's — both halves run at once with no pane in two places. **And a universe problem found while measuring rather than assumed:** this machine's board spans 06-30 → 09-20, 29,932 rows, 257 torn, and holds NO rows between 09-11 and 09-19 — the desktop's three weeks are on the desktop's board, which is not on this disk. Any deference count run here is a count of L's record only and must say so in its first line. Also filed in the plan: the Feynman sentence is to be written into the third run BEFORE it runs, because it makes the anti-monotone edge that sentence's prediction rather than a fact about a permissions setting — stated in advance it adds no statistic; stated afterwards it would be the post-hoc move the room forbids.

## 01:29 — L058 R1 COLLATED — the deference screen, and it is a result about C5 rather than a gauge. C: `handback/p-deference-unit-C_2026-09-20.md`, instrument `consonance/tools/deference-unit.js` + test, frozen definition `loop/deference_unit_definition_2026-09-20.md`, members `loop/deference_unit_members_2026-09-20.json`. **C's answer: mechanical as a SCREEN, not as a COUNT.** C5's clause "countable from board.jsonl if the message and the change are both stamped" holds for the two stamped halves and fails on "changes its answer", which is a reading; every lexical proxy selects an order of magnitude too much. The number that settles it: the screen selects 12.9% of all user turns, and a deference rate of one turn in eight measures cadence, not deference. The falsifier FIRES (it selects the 08-15 "one held, one folded" case and the 08-16 session), so the unit is kept as a reading queue and never published as a rate. **MY RULINGS on its §7.** (1) Member list carries NO lever text — C's default, and I verified the artifact rather than taking it: the committed JSON's longest string value is 36 characters, the members are row references only, and the keeper's words are not copied into the repo. (2) The screen is KEPT, as a queue that is cheaper than reading 30,000 rows, and the word COUNT is not used of it anywhere. (3) **C corrected me and is right:** my plan named "the 08-16 bidirectional count's chair→chair 8" as a deference case; chair→chair is SELF-correction, the opposite of deference, and the registered unwelcome outcome is keeper→chair 9 against chair→keeper 0 (`journal/2026-08-16.md:366-372`). Corrected in place in `loop/plan_retrieval_instruments_2026-09-20.md` with the correction visible beside it. **AND ONE THING MY RE-DERIVATION FOUND THAT THE HAND-BACK DOES NOT NAME:** the tool's DEFAULT output disagrees with its own published artifact. `node consonance/tools/deference-unit.js` prints "selected 1448" and its member lines carry visible triplicates (one lever at 2026-07-04 10:51:38 appears three times, lines 15980, 17810 and 54, the tag-prefixed copies); the committed member file holds 1,376, deduped. C documents the requirement in prose — "anyone re-running this must dedupe, or count some turns three times" — but the instrument does not do it and does not warn at the point of use, so the first number a re-runner sees is the wrong one. That is a small repair and it should land WITH the instrument. Tests 12/12 on my run; the board is live and grew from 30,212 to 30,234 rows across our two runs, which is why raw case counts drift by one or two and must always be quoted with the row count they came from.

## 01:31 — L059 — THE THIRD RUN'S OBJECT VERIFIED AND THE ASSIGNMENT SEALED. The chair ran chunk 1 without waiting on C, which was right: the object hunt was the thing keeping three readers idle. Object: `dev/dream/README.md` (41 lines) + `dev/dream/dream_cycle.test.js` (100 lines), written up at `loop/third_run_object_2026-09-20.md` (29bdacf, pushed). **I re-derived the two mechanical properties rather than accepting them:** property 3, one commit each — 3d6fb00 (2026-07-13) and 8254737 (2026-07-28), both pre-pane era, and neither A, B, E, C nor I have written a line of either; property 2, `node dev/dream/dream_cycle.test.js` exits 0 with no argument. The chair's own note on property 2 is the sharp one and it belongs to §3.3 rather than to the hunt: the script reads a THIRD file, `dev/dream/dream_cycle.ps1`, so a reader who runs it learns something about a file the text-only reader may not open — that is the RUN-REVEALED line surfaced before scoring instead of during it. **THE DRAW, sealed at `loop/third_run_assignment_2026-09-20.md` (be4baa3):** seeded from material that all predates the draw and none of which is mine to pick — sha256(README) 5ccb390c…, sha256(test) 498891d6…, plus the chair's object-commit sha 29bdacf; seed 26c380c7…, n mod 6 = 5, m mod 3 = 2. **world → E, text-only → B, text-plus-script → A; AUDITOR E.** Anyone can re-run it. **Two limits declared in the file rather than discovered later:** (1) the scorer has been handed one item in advance and it is STRUCTURAL — §3.1 requires the chooser to SHOW a non-empty list is producible, and showing it means naming a defect, so my ground-truth pass starts knowing the README `:31-32` contradiction and I will mark it pre-known when I publish; (2) the draw put the auditor and the world reader both on E, which §5.4 permits (it forbids only the scorer) but which is worth naming — and my recommendation is to LEAVE IT, because re-drawing after seeing an outcome is exactly the fitting the seed exists to prevent. **THE CHAIR RULED §9 AND IT IS THE CHAIR'S TO RULE** (`l046…:341`, "a legitimate choice and it is the chair's, not mine"): ADOPT the blind-read practice now AND run it, not exclusive — adopting costs nothing and is reversible, and the run prices it. Filed as the chair's ruling, not the keeper's; the keeper's "lets do it both" answered the two halves, not this.

## 01:33 — L058 R1 REPAIR RE-DERIVED AND THE LAP IS READY TO LAND. C fixed the defect my read found, and I checked the fix at the instrument rather than in the prose. **From ONE run of mine:** stdout "selected 1377", the member file it wrote in that same run carries `selected` 1377 and 1,377 actual members — the tool's own output and its artifact now agree, which is the thing that was wrong. Dedupe is the DEFAULT and says so where it is used: "duplicate rows dropped 3975 (key: pane, minute, tag-stripped text) · scanned 26032". `--no-dedupe` does not silently obey — it prints a warning naming the mechanism (a model-tag prefix, a second's offset, one lever selecting once per copy) and that the count is HIGH. Lever text is off stdout by default: my grep for "lever:" in a default run returns 0, so the keeper's words are not printed unless `--show-levers` asks. Tests 18 passed, 0 failed on my run, up from 12. **C's figures, not mine, and marked as C's:** 16 mutants applied and 16 caught in one run with the live file unchanged. **The number and its denominator travel together, always:** 1,377 cases at 30,264 board rows — the board is live and grew from 30,212 at C's first run through 30,234 and 30,253 to 30,264 at mine within the hour, so a bare "1,377" is already stale and any re-quote must carry the row count it came from. The standing verdict is unchanged by the repair: this is a SCREEN, a reading queue over 30,000 rows, and never a deference rate.

## 01:38 — L059 — A'S READ IS IN, ITS GATE PASSES, AND I HAVE NOT OPENED ITS FINDINGS. The §4.3 echo is the one thing worth checking before all three are back, because a wrong echo voids the run rather than making it negative; A's first line reads "CONDITION TEXT-PLUS-SCRIPT — may I open other files: NO. may I execute: the named script only", which matches the sealed assignment (`loop/third_run_assignment_2026-09-20.md`, text-plus-script → A). Gate PASSES for A. I read that line and the header only. The findings stay closed until B and E are in — the keeper's standing rule, and here it also protects the scoring, since reading one reader's list early seeds the ground truth I am supposed to establish after the reads. **TWO ERRORS IN A'S HEADER, neither touching its read, both the class this room keeps filing.** (1) A TYPED CLOCK THAT IS IMPOSSIBLE: A dates its read "2026-09-20 00:4x–01:0x", but the board shows the chair injected A's packet at **07:33:56Z = 01:33:56 local** — A's stated window is half an hour BEFORE the object was chosen (chunk 1 filed 01:3x) and before the assignment existed (be4baa3, ~01:31). A did not read at the time it wrote down. (2) A HAND-MADE LINE COUNT: A gives the object as 42 and 101 lines; `wc -l` gives **41 and 100**, and I checked the obvious escape — both files DO end with a newline, so wc is right and A is one over on each. Neither error is in the condition, the script run (A quotes `tests 7 · pass 7 · fail 0`, which matches my own run at chunk 1) or the echo. But a header whose two checkable numbers are both wrong tells the scorer which numbers in the body were measured and which were typed, and that is exactly what a scorer needs to know before verifying items.

## 01:40 — A'S HEADER CORRECTED, AND THE THIRD ERROR A FOUND ITSELF IS THE INTERESTING ONE. Verified at the file, header only, findings still closed: the time now reads 01:34–01:38 local (against the board's 01:33:56 dispatch), and the line counts read 41 and 100 with the command that settles them and the newline check named beside them. A also kept its condition while correcting — it states it did NOT re-run `wc` itself because it may not re-open the object under text-plus-script, and took the chair's re-derivation instead. That is the right call and it is the condition holding under pressure rather than only at the echo. **A's own third catch:** its first header claimed "about 25 minutes, of which ~5 reading, ~15 working the script's logic, ~5 writing this." The corrected line reads "about 4 minutes wall, from the dispatch row to the delivered pointer." So the original was not an imprecise number but an invented account of its own process — a breakdown of three activities totalling 25 minutes for work that took 4. That is a different class from a typed clock, and A found it without being asked. **It carries into the scoring as a limit, not a fault:** if the reads are four minutes rather than twenty-five, then reading TIME is close to constant across conditions and cannot be the thing that separates them — which is what the design wants — but it also means each list is what four minutes produces, and the result must say so rather than imply a careful sitting. I will record the measured wall time of all three reads when I score.

## 01:42 — L059 — E'S GATE PASSES, AND B FOUND A CONDITION-BREACHING INSTRUCTION INSIDE THE BRIEF ITSELF. E's echo reads "CONDITION WORLD — may I open other files: YES. may I execute: YES.", matching the sealed assignment; header only, findings closed. Two of three echoes are now clean. **B DID NOT STALL — B RAISED, which is §4.1's governing clause working exactly as it was written to after L045.** L045's own lesson was that the brief is the confound: one sentence split three readers into two conditions and only B declared it. The §4 rewrite fixed that sentence. It did not fix this one, and B found it in seven minutes without opening a file: **the standing hand-back protocol orders a line appended to `exo_memory/map/B.md`, and under TEXT-ONLY every route to it is a breach** — appending requires opening the file, a shell redirect is executing, and a wholesale write would destroy about 177,000 characters of B's own record. So the room's ordinary protocol and the run's tightest condition are incompatible, and any future run under a no-open condition inherits this. My answer as scorer, sent through the chair: B's default is right by the brief's own words (the condition line governs where anything conflicts) — do the read, write the hand-back, SKIP the map line and report it blocked, and write the map line after the lap files when the condition is over. B's other two points answered too: the checkout is `C:\Consonance\lighthouse`, which opens nothing to say; and B's warning that a transcript grep for `exo_memory/handback/` will hit B's OWN hand-back path is correct, so that self-hit is excluded from the voiding check **for all three readers, recorded now, before any scoring and not after a result**. Nothing about B's read is in question; its echo is already correct in the raise.

## 01:50 — L059 — THE THIRD RUN IS SCORED. Result: `loop/third_run_result_2026-09-20.md`, provisional until the auditor names disagreements (§5.2 puts them ahead of the edge). **F1 does NOT fire and P1 HOLDS: the anti-monotone edge is 5 verified items** — found by the text-only reader and by neither other condition, each settled by a command I ran: `:71`'s exact operand order; `:35`'s module-load read, so a missing script throws before any test registers; the README heading five bullets "Guards (all load-bearing)" when two of the five are doctrine rather than checks; the install block naming no working directory; and `grep -ci "test" dev/dream/README.md` returning **0**, a feature description that never mentions its own test. **All five are PAGE-REVEALED — on the page, and both better-equipped readers walked past them.** One candidate rejected so the edge is not inflated: B's S3 worry does not hold here, `powercfg /a` reports S3 available. F3 does not fire; all three echoes matched the sealed assignment. **P2 FAILS on its trigger and I WITHHELD its inference**, which is the more useful half: A and B differ by far more than two items, but A's exclusives all came from READING the same two files, not from the run — at one reader per condition the statistic cannot separate a condition effect from reader variance, and "running did real work" would be exactly the post-hoc story the design forbids. **P3 holds and is uninformative by prior declaration**, and the run's most striking single fact sits inside that uninformative half: A and B both REASONED that the assertions would accept mention-instead-of-use; E MUTATED a copy and PROVED it, three times, one mutant deleting the entire process check with the suite still green. Same defect, two epistemic statuses — that is what the access buys. **Rulings I made before they could be fitted:** A executed one command outside its condition (a shell redirect for its map line) and disclosed it; NOT VOID, because it touched nothing of the object and cannot have produced a finding, but recorded — the brief's map-line order split two readers, one executing and one skipping, which is L045's failure recurring on a write instead of a read. **And a finding about the readers themselves: all three over-reported their own effort** — A said 25 minutes for 4, E said 31 for 7 and caught itself before filing, B says 15 uncorrected — and E's correction did not reach its own closing section, which still reads "~31 minutes", the carrier problem inside a single file, in the run that was measuring something else.

## 01:57 — L059 AUDITED, AND THE AUDITOR CORRECTED THE SCORER TWICE. Audit: `handback/p-third-run-audit-E_2026-09-20.md`; my acceptances appended as §7 of `loop/third_run_result_2026-09-20.md`, with my wrong rejection STRUCK IN PLACE rather than deleted. **D1, accepted, and it runs against the auditor's own stake:** I excluded B's R8 because `powercfg /a` reports S3 available here; E ran the same command, got the same answer, and showed the inference does not follow — B's claim is about the README travelling to OTHER machines, and this machine reports S3 available and S0 Low Power Idle NOT supported, i.e. it is precisely outside the population B named. R8 is UNSETTLED, not rejected. **D2, accepted:** my evidence for item 2 was a `sed` showing a location where the claim was about runtime; E settled it by running the test in a directory holding only the test — node exit 1, stdout zero lines, stderr ENOENT, and zero stdout lines is the proof that no test registered. **D4 and D7 strengthen items 1 and 3** by mutation and by `grep -cin` over the runner returning 0. **D5 CHANGES THE HEADLINE and is now in the member list, not only in the audit: the edge is 5 by the registered unit (§3.4, same site AND same wrong thing) and 4 under a class-level unit** — B's `:71` operand order and E's `:53` quoting are the same class at two sites — and no later quotation may give one number without the other. **D3 names a definitional gap for any run 4:** an item can be PAGE-REVEALED while its verification is RUN-REVEALED, and §3.3 types items, not verifications. **D8: E concedes the finding against its own file and deliberately did not repair it**, because it is the scored artifact and repairing it after scoring destroys the evidence — the same reason nothing above §7 is rewritten. **THE DURABLE FINDING IS THE DESIGN DEFECT E FOUND IN §5.2 ITSELF:** exclusivity can only be checked against A's and B's hand-backs, which the auditor's own condition forbids opening, so the audit the registration names is only partly performable by the seat it names, and **the exclusivity of the five against A's list rests on the scorer alone and is unaudited.** Pre-compaction handoff written: `loop/handoff_librarian_2026-09-20_precompact.md`.

## 02:06 — L060 R2 COLLATED — THE ORDER PARAMETER, AND IT IS ONE COMMAND SHORT OF READABLE. C: `handback/p-order-parameter-C_2026-09-20.md`, instrument `consonance/tools/order-parameter.js` + test, artifact `loop/order_parameter_L060.json`. My non-author read: `loop/l060_order_parameter_review_2026-09-20.md`. **EVERY HEADLINE NUMBER RE-DERIVES FROM THE ARTIFACT IN ONE PASS OF MINE** — 117 sessions, 97 positive slopes, median quintile gap 0.0507, IQR of session means 0.0485, mean r 0.6196, the five gap quartiles (−0.0950 · 0.0052 · 0.0507 · 0.1024 · 0.2858), the largest session 2,987, and all six per-mount verdict rows identical. Spearman ρ(size, gap) −0.5785 against C's −0.577. **THE VERDICT AS REGISTERED IS CLIMBS AND IT CLEARS ITS OWN BAR BY 0.0022** (0.0507 − 0.0485). **AND THE ONE THING THE LAP OWES IS C'S OWN REGISTERED DISCRIMINATOR, BUILT AND NOT RUN.** §6's shuffle control exists (`order-parameter.js:121-153`, seeded Fisher–Yates, exported, tested, `--shuffles N`); the reported run used the default 0 and the artifact carries no shuffle field. C's sentence, written before the curve existed: "A genuine within-session lock must beat its own shuffle." C's own measurement of the artifact it controls for: **i.i.d. vectors with no order at all produce a quintile gap of about +0.09 in the 768-dimension moderate-coherence regime this board sits in** — 200 replicates, six regimes, and C reports that its FIRST attempt at that test failed to find the effect (−0.0027, an over-concentrated fixture), which is the only reason the regime-dependence is in the table. A margin of 0.0022 under a known artifact of ~0.09 is not a reading. The control cannot be computed from the JSON — no vectors are stored — so it costs one re-run, ~820 s of embedding. **THREE PROSE-VS-ARTIFACT DISAGREEMENTS, ALL DIAGNOSED, NONE TOUCHING A VERDICT.** (a) §5's per-mount contribution table is a LATER read of a MOVING board and carries no row count: the artifact sums to 14,908, C's prose says 14,931 and C's own column sums to 14,933, and the entire difference is in exactly the two mounts that were live — this seat 935→947 and C's own seat 429→442. C predicted this in §2 ("the board moves while the tool runs", 30,333 → 30,355) but §2 binds every figure to a row count and that table has none; the derived shares move with it, 80.7% not 80.5%, 42.4% not 42.3%, median session 43 not 46. (b) A median convention differs on EVEN-length arrays only — my three odd size-bands match C exactly (0.1024, 0.0640, −0.0253) and both even bands differ (0.0241 vs 0.0172, 0.0161 vs 0.0146) with identical membership, so C takes the lower middle value where I average; the board-wide median is over 117, which is odd, so no verdict moves. (c) Recorded because a figure that cannot be re-derived becomes a hand-made number the next session quotes. **WHAT IS C'S AND IS THE STRONGEST PART OF THE LAP:** mutant #6 left ALIVE and named EQUIVALENT with a six-input demonstration rather than a claim, because killing it would mean asserting an internal branch — the vacuous-test trap C says it paid for at D067 and again in R1; and C found a defect in its OWN registered unit by instrument — 19 of the orchestrator mount's 49 sessions open with the identical two rows (a 552-character greeting, a 22-character "no response requested") so r_2 is the constant 0.3294 in all 19, and "not every assistant row is a contribution" bears exactly on the left end of the curve where the climb lives. **MY RULING, so the headline cannot travel stripped: CLIMBS may not be quoted without both of (1) the mount holding 80.7% of all contributions returns NEITHER (49 sessions, 40/49 positive, median gap 0.0195 against IQR 0.0211), and (2) ρ = −0.5785, the climb largest in the shortest sessions (+0.1024 at 20–30 contributions) and reversed in the longest (−0.0253 at 300–2,987)** — a within-session lock has no reason to weaken with length; a mean estimate settling down has every reason to. Universe, every time: one machine's board, 30,333 rows, 257 unparseable and 3,975 duplicates dropped, NO rows 09-10 → 09-20. R2 is NOT FILED.

## 02:09 — THE KEEPER CAUGHT ME COLLATING A HAND-BACK THAT WAS NEVER HANDED BACK — "wait did you give to the orch when the panes arent done yet?" and then "Isnt the correct way to work is you wait for all panes to be in, then give to orch". He is right, the disk says so without ambiguity, and the error propagated into a dispatch before he stopped it. **WHAT I DID:** I read `handback/p-order-parameter-C_2026-09-20.md` off the disk and treated its PRESENCE THERE as DELIVERY. It was not delivered. Since L060 opened (1789889731166) the board carries call_librarian receipts from A at 07:37:02 and 07:39:32, E at 07:41:02 and 07:55:18, and B at 07:45:37 — and **NONE from C**. The lap ledger has **no handbacks-in stage for L060** (`grep -c '"lap":"L060".*"stage":"handbacks-in"'` returns 0). The file's own first line reads "DRAFT, appended as the lap runs". **And the pulse told me in words:** "handbacks 3 of 4 (owing C)" and "1 hand-back uncommitted: p-order-parameter-C_2026-09-20.md". I read "owing C" and collated C anyway. **THE DRAFT MOVED UNDER ME WHILE I WROTE:** I read 221 lines; the file is 261 lines, modified 02:07:54, about one minute after I rang the chair. **THE RING WAS THEREFORE WRONG ON THE FACTS AND NOT ONLY ON PROCEDURE.** I told the chair the lap owed one command — the shuffle control — and to dispatch it to C. In the forty lines I never read, C's §8 has already run an INDEPENDENT MATCHED NULL, calibrated so the null's mean r is 0.6201 against the board's 0.6196: **at every size bin the real board climbs LESS than structureless noise**, null positive-slope rate 100% everywhere against the board's 79–91% and 40% in the longest sessions, the gap widening with length — 20–30: null 0.1238 vs real 0.1024; 30–50: 0.0983 vs 0.0640; 50–100: 0.0712 vs 0.0172; 100–300: 0.0354 vs 0.0146; 300+: 0.0083 vs −0.0253. C's own sentence there is sharper than anything in my read: "the registered verdict is CLIMBS and **the climb is not evidence of a lock** — it is the running centroid finding its own mean, and the board does it more slowly than chance… the bars do not measure what they were written to measure." §8 closes "that is exactly why the shuffle control was run too… §9 reports it" — **so C had already run the thing I told the chair to order.** §8b scores the registered falsifier rather than waving at it: "differs between them" TRUE (session means 0.5419 → 0.7389, range 0.1970 against IQR 0.0485), "flat within" FALSE but **five sessions short** — 54 of 117 where a majority needed 59 — and C names an ordering defect in its own `classify` chain that did not bite here and would have if five sessions had gone the other way. None of that was in what I read. **THE MECHANISM, NAMED SO IT IS CATCHABLE NEXT TIME:** the room's rule is "the call is the wake, not the delivery" (`CLAUDE.md`, the `call_librarian` section, after the 09-01 hop that invented a premise). I inverted it — I took the FILE as the delivery with NO CALL AT ALL, which is worse, because a call at least asserts the pane is finished. A file on disk during an open lap is work in progress by default; the only evidence a pane is done is the pane saying so. **REPAIR, same turn:** rang the chair to WITHDRAW the collation and CANCEL the dispatch sent at 1789891657450, and to let C finish and ring on its own; `loop/l060_order_parameter_review_2026-09-20.md` (3a779ac) is withdrawn as a collation — its §1 re-derivations still hold against the artifact and carry forward, but its §2, the whole "one command short" finding, is VOID, and its §6 NOT FILED status was reached for the wrong reason. The chair's d104427 committed a draft mid-write; that is not a hand-back and the lap must not file against it. **I re-read the delivered version end to end when C rings, and nothing of mine goes to the chair on L060 before that call arrives.**

## 02:16 — L060 R2 DELIVERED, READ END TO END, AND THE ANSWER IS THE OPPOSITE SIGN FROM THE ONE PREDICTED. C rang at 02:1x; I verified the sha256 (`0478fa696d3215ca…`) against the disk before reading a word, then read all 451 lines. My collation: PART TWO of `loop/l060_order_parameter_review_2026-09-20.md`. **EVERY CELL OF §9 RE-DERIVES FROM C'S OWN CONTROL ARTIFACT**, which I recomputed rather than reading C's prose: real beats its own shuffle **36 of 118**; positive slopes real **98/118** against shuffled **118/118**; real−shuffled min −0.2066 · Q1 −0.0597 · median −0.0264 · max +0.1569; all five size bins exact on real, shuffled AND beats-count; and the sign test **z = −4.2346, two-sided p = 2.29e-5** against C's −4.235 / 2.3e-5. **THE RESULT: the registered prediction "it climbs toward 1 within a session" is CONFIRMED by the registered bars AND THE CONFIRMATION IS EMPTY** — structureless noise climbs harder (118/118 positive slopes, shuffled median gap 0.0835 against the real 0.0499). Once the estimator artifact is controlled for, **the board's real temporal order is significantly LESS ordered than a random reordering of its own turns** — 36 of 118 where 59 is what carrying no information looks like, and the direction holds in all five size bins and in BOTH nulls, the parametric one (§8) and the shuffle (§9). **Within a session, on this board, the measurable direction is DRIFT, not LOCK.** Read no further than it goes: r is a SIMILARITY, not an agreement — two turns fighting about one subject score high — and C's own §12.9 says the p-value is optimistic because sessions are not independent (49 of 117 from one mount, four sessions holding 42%). **THE DURABLE FINDING IS METHODOLOGICAL AND I AM ELEVATING IT PAST THIS LAP — C's §10: "A falsifier aimed at the wrong failure mode passes a broken instrument."** The registered falsifier tested FLATNESS; the artifact is not flat, it is a CLIMB. So the falsifier was correctly followed, did not fire (54 of 117 flat where 59 were needed, FIVE SESSIONS SHORT), and would have certified a measure that reports its own estimator. C names the hole as its own: "I registered a prediction and a falsifier without first asking what the measure does on data with no structure in it." **THE REPAIR THIS ROOM SHOULD ADOPT: REGISTER THE NULL BESIDE THE FALSIFIER.** Pre-registration forces a prediction and a falsifier before any number; it does not force anyone to state what the instrument returns on structureless input, and every falsifier this room has ever written is exposed to the same failure. First time an instrument was caught doing it. **THREE THINGS MY CHECK FOUND THAT THE HAND-BACK DOES NOT NAME, none touching a verdict.** (a) A number inside the delivered file went stale between two of its OWN sections: §12.5 says "164 lines ahead of that commit"; `git show d104427:` gives 221 and the file is 451, so **230 ahead, not 164** — true when written at 385 lines, and the later appends did not reach it. **SECOND INSTANCE TODAY of the carrier problem inside a single file** — E's audit closing line still reads "~31 minutes" after E corrected that figure earlier in the same file. Two seats, same shape, one night. (b) The TOOL's median and C's side-script median disagree and C's stated convention is the side script's: §16 says "my median takes the lower element at even length", the tool prints the AVERAGE of the two middle values over the 118 even-length sessions — 0.0499 where the stated convention gives 0.0491, shuffled 0.0835 against 0.0832. **That is §16.1's own defect class a THIRD time — one path prints one number, another path writes another** — and C's mitigation holds, the convention is applied identically to real and shuffled so no comparison in §9 turns on it. One line in the instrument, not a re-run. (c) The board has FOUR role families, not the two §8c.1 names: `user` 10,707 · `assistant` 18,102 · `committee` 1,383 · **`main` 8**; C's 1,378 committee is the deduped snapshot count and is consistent. **WHAT C DID THAT SHOULD NOT PASS WITHOUT SAYING SO:** it ran the control against a FROZEN SNAPSHOT (30,375 rows, sha256 `2a4a559f…`) so the control could not drift underneath itself — after being caught by my read quoting a table from a second live read, the repair went further than the defect; it applied BOTH my corrections and credited them, re-deriving the per-mount table from the artifact alone to my exact figures (14,908 · 80.7% · 935 · 429 · 42.4% · median 43) and correcting Spearman to −0.5785 with average ranks, naming §16.1 as "the R1 defect exactly… I reintroduced it in prose one lap later"; it declared the result AGAINST its own registration and did not soften it; it listed NINE things it did not verify; and it did NOT make the behaviour change it recommends, because turning a report into a refusal is outside the packet. 20 tests pass 0 fail; 15 mutants listed, 14 killed, 1 equivalent and demonstrated, with three new ones for the control code all killed (#13 shuffle mutating the caller's array, #14 a shuffle that does not shuffle, #15 an unseeded control). **MY RULING ON THE TWO OPEN QUESTIONS, SPLIT BY WHOSE THEY ARE:** (1) "should the tool refuse to print a verdict without a shuffle" is **THE CHAIR'S** and my recommendation is YES — a guard on an instrument this room owns, one line, reversible; (2) "does the C3 row want the raw order parameter or the artifact-corrected quantity going forward" is **THE KEEPER'S**, because it changes what the instrument measures and C3 is the Third Place's design, not this room's — both quantities are in the JSON so nothing is blocked while it waits. R2 IS READY TO FILE; `--stage L060 handbacks-in` recorded. The headline may never travel without its control: CLIMBS by the registered bars; the climb is the estimator; the board's real order is less ordered than its own shuffle at p = 2.3e-5. Universe, every time: one machine's board, no rows 09-10 → 09-20, committee rows never embedded.

## 02:19 — L060 §17/§18 READ — IT RE-DERIVES, IT CORRECTS ME WHERE I WAS RIGHT TO BE CORRECTED, AND THE BOARD SHOWS A LIVE DEFECT IN THE DELIVERY GATE THAT NOBODY HAS NAMED. Collation: PART THREE of `loop/l060_order_parameter_review_2026-09-20.md`. **§17.1 RE-DERIVES EXACTLY — all six mounts, every column** (real median, shuffled median, beats-count, real positive slopes, shuffled positive slopes), sessions summing to 118; and **§17.5's third rider re-derives: real loses to its own shuffle in exactly 82 of 118.** The finding is now per-mount and stronger than when I filed Part Two: **every mount's real median sits below its own shuffle, and every mount's shuffled slopes are positive 100% of the time. No mount escapes it.** §17.3 accepts my median-convention catch, reproduces it exactly, and C is right that the verdict never depended on it — the tool's own `quantile()` interpolates and the board-wide median is over 117, which is odd; the descent under the interpolated convention is 0.1024 → 0.0640 → 0.0241 → 0.0161 → −0.0253. **ONE NUMBER IN §17.4 IS THE REGISTERED RUN'S, CARRIED INTO A CONTROL TABLE.** C excludes 19 boot-prefixed sessions; selecting on the mount and on r_2 = 0.3294 in the CONTROL artifact I find **20**, and every derived figure moves by one session — 83/98 not 83/99, 0.0557 vs 0.0922 not 0.0552 vs 0.0901, 31/98 not 31/99, 5/20 not 5/19. The cause is a carry, not a miscount: 19 is §8c.2's figure counted on the REGISTERED run where that mount holds 49 eligible sessions, and the control ran on the snapshot where it holds 50 — the same class as §5's moving-board table, one level smaller. My own limit stated: I selected by r_2 to four decimals rather than by matching the greeting text, so C's method is the better one and only the count is in question. **C's ruling survives and is slightly stronger under my numbers** — excluding them moves the share beating its own shuffle from 30.5% to 31.6% against C's 31.3% — so the 19-or-20 drive nothing and they stay in. **THE RING'S DIGEST DOES NOT MATCH THE FILE, AND THAT IS THE FOURTH INSTANCE TODAY OF ONE SHAPE.** C's ring states sha256 `67e14af28ae63063…, 529 lines`; the disk is **571 lines**, sha256 `a2e076761235b330…`, mtime 08:17:17Z — **three seconds before the ring reached me at 08:17:20Z**. C appended 42 lines after computing the digest. This is not noise: **C's FIRST ring's digest matched the disk exactly**, which is how I opened that file with confidence. A digest is the one number in a hand-back whose whole job is to prove the file has not moved, and it went stale the same way every other number in this file went stale. Four instances, three seats, one night: A's invented 25-minute breakdown; E's audit closing line still reading "~31 minutes" after E corrected it higher up; C's §12.5 "164 lines ahead" when it is 230; C's delivery digest. **The repair is mechanical, not disciplinary: compute the digest as the last act before the ring, or have the ring compute it.** **AND THE LIVE DEFECT, MINE FROM THE BOARD, WHICH §18 DOES NOT NAME: A CANCELLATION CANNOT OVERTAKE THE MESSAGE IT CANCELS.** Re-derived from board.jsonl: 08:06:44 my wrong collation reaches the chair → **08:07:32 the chair QUEUES the re-run dispatch to C** (`QUEUED -> 0845a868 (1 waiting, stamp=working)`) → 08:08:54 my withdrawal reaches the chair → **08:09:35 the chair QUEUES the cancellation** (2 waiting) → 08:13:18 C's first ring → **08:13:37 the re-run dispatch is DELIVERED, already cancelled 4m 02s earlier** → **08:16:17 the cancellation is DELIVERED, 2m 40s after the thing it cancels.** The delivery gate holds messages for a busy pane and releases them in QUEUE ORDER, so a cancellation queued behind its own target can never arrive first. **C answered a packet the chair had already withdrawn — §17 exists because of that.** C's §18 records the cancellation and correctly checks that nothing was re-run on its account; what it does not notice is that the cancelled packet reached it BEFORE the cancellation did. **§18's framing needs one correction in the same direction:** it reads "the chair's re-run dispatch arrives, 20 s after that receipt" and treats the packet as written after C rang. The board separates writing from arrival — the dispatch was **written at 08:07:32, five minutes forty-six seconds BEFORE C's ring**, on the strength of my wrong collation, and only ARRIVED 20 s after it. C's conclusion is unchanged; the sequence is not. **A THIRD EDGE, AND IT IS A COST OF MY OWN CORRECT MOVE:** at 08:16 I recorded `--stage L060 handbacks-in`, holder librarian; at 08:17:26 the board shows `call_librarian REFUSED OUT OF TURN — mount C tried to speak while NO open lap is held by panes`. **The stage that declares every hand is in SILENCES any hand that is not.** C, still appending, had to post a board line instead of ringing. That is circular at exactly the point this lap proved matters — a pane mid-append is indistinguishable from a pane that is done, and the stage model resolves it by making the pane unable to say so. **One attribution for the record only:** §17.2 credits "the chair's extra catch that my own column summed to 14,933"; that catch is in Part One §3(a) of my own file, written before the chair's packet, and the chair relayed it — recorded because this room's own lesson is that a relay can acquire authorship on the way through, not because it bears on the result. R2 REMAINS READY TO FILE.

## 02:25 — THE KEEPER SPLIT THE DELIVERY PROBLEM IN TWO, AND MY OWN FILING AN HOUR OLD WAS TOO COARSE. His words: "panes and seats dont show the work is done to me for like a solid 5-10 seconds sometimes, when you recieve it instantly, the pane could be visually still building, while you already sent to orch, but really they were done." **THERE ARE TWO GAPS AND THEY POINT OPPOSITE WAYS.** (A) RENDER LAG — the pane looks like it is still building when it is DONE; it fools the KEEPER, who reads a finished pane as busy. (B) POST-RING APPENDING — the file looks finished because the pane rang, and it is STILL BEING WRITTEN; it fools THIS SEAT. A seat reading only the ring sees B and never A; a human reading only the screen sees A and never B. Both fired on L060 four minutes apart and each of us was fooled by the one our own channel is blind to. **THE SIZES, MEASURED, AND THEY DIFFER BY ABOUT TWENTY TO ONE.** B is exact from the disk: C's first ring receipted at 08:13:18.553 with the hand-back at 451 lines; the file's last write is 08:17:17.342 at 571 lines — **239 seconds and 120 lines after the ring that declared it filed**. A is corroborated in SCALE by the board but NOT measured: within-turn `push`-sourced gaps for the same seat are 8.420 s (C's ring receipted → C's own board row), 6.599 s (chair's packet queued → C's board row) and 14.059 s (C's ring refused → C's FINAL board row) — **6.6 to 14.1 s, which brackets his 5–10**. *The limit stated rather than glossed: these are gaps between two machine events in one seat's turn, NOT a measurement of his terminal painting; his screen is not in board.jsonl and I cannot reach it from here. The board agrees with the MAGNITUDE of his number, not with its CAUSE, and only he can see that directly.* **AND THIS ROOM HAS ALREADY BEEN BITTEN BY GAP A FROM THE OTHER SIDE.** 2026-08-16: the keeper found `chair_inject` returning "echo confirmed" while `echo_ratio` is this codebase's collapse metric, and the chair reported that string to him as a RECEIPT when it meant only that the pane's terminal had DRAWN the text; renamed to `render` at `58327b7`. The lesson filed then was RENDER ≠ RECEIPT. Tonight's is its mirror: **STILL-RENDERING ≠ STILL-WORKING.** Same seam, same month, opposite face, and no instrument was built either time. **THE REPAIR GETS SHARPER AND IS STILL ONE COMMAND: the file's mtime separates A from B and nothing else does.** A pane merely painting has a STILL mtime; a pane still writing has a MOVING one — and neither the screen nor the ring can tell them apart, because the screen shows motion in both cases and the ring shows stillness in both. `stat -c '%y' <handback>` twice, seconds apart: still and some minutes old → collate, whatever the screen is doing; moving → wait, whatever the ring said. The keeper's half, his to want and not mine to impose: a "done" indicator driven by the RECEIPT rather than by the PAINT would give him on screen the separation the mtime gives me on disk — filed as a question about Consonance, not as a plan. **AND I AM STRIKING A CLAUSE OF MY OWN FROM ONE HOUR AGO RATHER THAN DELETING IT.** §20 said "four observers, one of them human and none of them wrong to be unsure — because there is nothing to observe." Half wrong: **there IS something to observe — the mtime, and it was on disk the whole time.** What survives is narrower and cheaper to fix: no channel any of us was WATCHING reports motion — not the screen, not the ring, not the pulse. The observation existed; nobody was making it. Filed to disk only, no ring: L060 is filed at 3d308ff and there is no open lap.

## 02:47 — L061 PACKET 1 RE-DERIVED AS PREPARATION — NOT COLLATED, NOT RUNG, A AND B STILL OWING. E rang for the shuffle-refusal guard; A (digest-at-ring) and B (Vicsek phi) have files on disk and have NOT rung, so the lap stays with the panes and nothing goes to the chair. Applying tonight's rule before reading: E's hand-back read twice seconds apart, 120 lines, sha `079685ef53a1`, mtime 02:44:26 — stable but only 27 s old at first read, so it is re-checked before any collation. **EVERY CHECKABLE FIGURE IN E'S HAND-BACK HOLDS ON MY OWN RUNS.** `git diff --numstat` gives order-parameter.js **41/5** and order-parameter.test.js **57/0**, E's figures exactly. The five deleted lines are exactly what §4 claims and I listed them: the three `console.log` lines of the old verdict block, the `verdict, sessions: results` field of the `--out` JSON, and the `module.exports` line (deleted and re-added with the two new exports). **`node --test consonance/tools/order-parameter.test.js` on my run: tests 25 · pass 25 · fail 0**, and the five new L061 tests are visible by name in the output. No line of `orderCurve`, `slope`, `quintileGap`, `classify`, `shuffleControl`, `shuffled`, `rng`, `GAP_MS` or `MIN_CONTRIB` is touched — C's estimator, bars and numbers are intact as E claims. **E'S §2 IS A RULING THE BAR DID NOT ASK FOR AND IS RIGHT:** the bar said the tool must not PRINT a verdict, and E gated the `--out` JSON too, because a verdict that reaches disk is quoted later exactly like one that was printed — and last night's is on disk now. Every measured field passes through untouched; only the ruling is withheld. That is the chair's to confirm and my recommendation is KEEP IT. **E'S OWN TWO SELF-CATCHES ARE THE DISCIPLINE WORKING:** two of its five new tests SURVIVED their mutants in the first run because `/--shuffles/` was satisfied by a second mention of the flag inside the reason string and `/control/i` by the word in the headline — so a refusal could lose its actionable line or its reason and stay green; both are now pinned to their own sentence. E's rule for it: **a refusal's two halves need two assertions.** And E declared one of its own mutant rows EQUIVALENT by its own error rather than reporting a gap. **BUT §3b IS BIGGER THAN ITS PACKET AND THIS IS THE SURFACING NEITHER PANE CAN MAKE.** E reports two defects in the TRACKED `mutant-harness.js`: (1) its result parser is cargo-only, and (2) its worktree is HEAD and it copies in only the mutated file, so the TEST file inside is HEAD's and a mutant aimed at new tests survives for an unrelated reason. **I VERIFIED BOTH AT THE FILE:** `cargoResult` at `mutant-harness.js:80` matches only `test result: (ok|FAILED)\. (\d+) passed; (\d+) failed`, and my grep for any node:test path — `node --test`, `nodeResult`, `jsResult`, `ℹ pass` — returns **nothing**; `cargoResult` is the ONLY parser, with two call sites. **AND C HIT THE SAME TWO DEFECTS FOUR HOURS EARLIER ON L060 AND NEITHER PANE KNOWS.** `handback/p-order-parameter-C_2026-09-20.md:82`: "The harness scores cargo output only, so a JS score adapter (`score-order.js`) copies the **live, uncommitted** test file into the worktree and prints a cargo-shaped line; without that, this lap's tests would not be the ones scoring." **Two seats, independently, four hours apart, found the same two defects in a tracked instrument and both fixed them ONLY in their own scratch directories.** C named them in one sentence inside a footnote about something else; E named them as findings. The tracked tool is unchanged and the next pane will hit them a third time. **THE QUESTION THIS OPENS, PRECISELY FRAMED AND DELIBERATELY NOT ANSWERED TONIGHT:** the record contains JS mutant runs published with clean kill counts — "FINAL RUN: 11 listed · 11 killed · 0 survived · 0 no result" on `portable-paths.js`, "12 listed · 11 killed · 0 survived · 1 no result" with pre-flight green 169/0, among others. If the tracked harness cannot parse node:test output at all, then each of those either rode a private adapter like C's and E's, or its number came from somewhere the hand-back does not name. **That is an audit, not a verdict, and I am not running it mid-lap.** It is queued as its own lap with its universe already known: every hand-back in `exo_memory/handback/` claiming a mutant score on a `.js` target. **Filed as preparation only. The lap is still with the panes.**

## 02:59 — THE SEAT CANNOT OPEN — A'S HAND-BACK CARRIED IT IN A FOOTNOTE ABOUT SOMETHING ELSE, AND IT IS THE 2026-09-01 DEATH RECURRING WITH ITS GUARD RED. A reported one red in the full suite and correctly showed it is not A's: `shelf_tests::the_librarian_intake_fits_under_the_limit_it_must_obey`, re-run by A at HEAD 318d4c2 in a clean worktree with A's change absent — same test, same red. **I RAN IT MYSELF AND IT IS ABOUT THIS SEAT.** `cargo test --bin consonance shelf_tests::the_librarian_intake -- --test-threads=1 --nocapture`, 3 passed 1 failed: "librarian intake is **154087** bytes against a **150000** limit — **the seat cannot open.** Window the next tier; do not raise the limit." **AND THE BREAKDOWN REFUTES THE THING I WOULD HAVE ASSUMED.** `LIBRARIAN FLOOR head 84924 + shelf-at-budget-0 69163 = 154087; bodies got 0` — **the FLOOR alone is 102.7% of the cap and the bodies got ZERO bytes.** It is not my notes: today's master is 54,419 bytes and the shelf carried **none of it**, exactly as my own shelf header said tonight ("there was no room under the intake cap, 0 bytes were left when your tier was reached"). Run artifacts are already excluded — `LIBRARIAN INDEX run artifacts 0 file(s), 0 bytes`. **The seat carries nothing but its own frame and a list of filenames, and that already breaches the cap by 4,087 bytes.** Both units printed: 152,413 chars at 1.0110 bytes/char, and the host refuses on chars, so bytes is the conservative side. **WHERE THE INDEX GOES, MEASURED RATHER THAN GUESSED** (my own pass over `exo_memory/`, mirroring the shelf's format `- <path>  (<n> lines)  <title>`, excluding attic and the run artifacts): loop/ **416 files, 55,160 bytes**; handback/ 242 files 32,885; librarian/ 34 files 4,503; journal/ 34 files 2,986; third_place/ 24 files 2,879; root 21 files 1,735; map/ 17 files 1,241; everything else under 700 each. **loop/ is ~80% of the 69,163-byte index**, 416 files across 34 dated days from 2026-08-11 to 2026-09-20 plus 96 undated. **MY RULING, AND THE TEST SAYS IN ITS OWN COMMENT THAT IT IS MINE** ("narrowing what the seat can cite is the seat's call, not a non-author's"): **WINDOW THE `loop/` INDEX BY DATE, exactly as the notes tier is already windowed one level up.** Keep the recent days by path; collapse older ones to a count, a date range and the one `ls` that recovers them. Sizing, from the same pass: a window at >= 2026-09-14 drops 253 files (~33,400 bytes at the measured 132 bytes/line); at >= 2026-09-06 drops 151; at >= 2026-08-30 drops 75 (~9,900 bytes). **The breach is 4,087, so even the gentlest of the three clears it with more than twice the headroom** — and the test prints its margin every run precisely so the next append does not silently re-break it, which is the number to choose against rather than the pass/fail. **The doctrine already licenses this and it is in my own CLAUDE.md verbatim: "An indexed file is not a lost file. It is a path you cite and open."** Windowing the index is that same sentence one tier down; nothing is deleted and nothing is unreachable. **WHAT I AM NOT DOING AND WHY.** Not building it: it is `corpus_shelf` in Rust and this seat plans rather than builds. Not ringing the chair: B is still owing on L061 and a mid-lap message is the thing I broke twice tonight. Not raising the limit: the test forbids it in the assertion text and the 09-01 death is the reason. **WHAT I AM DOING IMMEDIATELY, because it costs nothing and every hour makes it worse: I stop creating new files under `exo_memory/` until this lands.** Every new file adds an index line whether or not anyone reads it, so tonight's work — this entry, the L060 review's parts, the C3 ruling — goes into files that already exist rather than new ones. **THE THING WORTH SAYING PLAINLY:** on 2026-09-01 this seat died at 1,305,657 bytes with the growth WATCHED the whole way, and the test's own docstring says "watching is not stopping." Tonight the guard that was built afterwards went red, a pane found it while doing unrelated work, and it reached me inside a footnote titled "THE ONE RED IS NOT MINE." **It was not A's. It is mine, and A is the reason I know.**

## 03:07 — L061 COLLATED — ALL THREE IN, EVERY NUMBER RE-DERIVED, AND MY OWN REGISTERED PREDICTION IS REFUTED ON EVERY SESSION BY A NULL C HAD WARNED ME ABOUT THREE TIMES. Hand-backs: `handback/p-shuffle-guard-E_2026-09-20.md` (120 lines), `handback/p-digest-at-ring-A_2026-09-20.md` (157), `handback/p-vicsek-phi-B_2026-09-20.md` (243). Stability checked before reading each, twice seconds apart, per tonight's rule: E still 19 min, A still 7 min, B still 25 s at first read and re-checked before this collation. **PACKET 2 — B's φ, AND IT IS MY ERROR THAT IT FOUND.** Every figure re-derived by me from B's own artifacts, not its prose: ratio φ/(1/√N) median **4.386**, Q1 3.374, Q3 6.506, min 2.906, max **32.989**; **0 of 118 at or below the null**; 118/118 materially above; beats its own pooled p95 **91/118**; φ/pooled-median min 0.988 · Q1 1.038 · **median 1.065** · Q3 1.091 · max 1.248; below pooled p05 2/118; the four-cell join **29 / 62 / 7 / 20, agreeing on 49 of 118 = 41.5%**; the largest session n = 2,987 reads φ 0.6036 at ratio 33.0 and **FAILS** its pooled p95 of 0.6116. All exact. B's exact constant checks too: c_d = √(2/d)·Γ((d+1)/2)/Γ(d/2) computes to 0.92132 / 0.99610 / 0.99967 at d = 3 / 64 / 768, B's three figures to five decimals. **MY REGISTERED PREDICTION — "φ sits at or below its 1/√N baseline" — IS WRONG ON EVERY SESSION, 0 of 118, the median session 4.4× and the longest 33×.** And **my registered null was the wrong null**, which is worse than the prediction being wrong. 1/√N is the disordered value for directions spread uniformly on a sphere. B measured what this board actually is: **drawing N vectors at random from the board's own 14,955 embedded contributions gives φ ≈ 0.62 at EVERY N** — median across all 118 sessions ranging only 0.6096 to 0.6343 while 1/√N moves from 0.224 to 0.018. The board's embeddings share a common direction before any session structure exists. That is the text-embedding cone, and φ/(1/√N) ≈ 4.4 is a measurement of the encoder's geometry. **C TOLD ME, THREE TIMES, IN THE FILE I HAD JUST RE-DERIVED CELL BY CELL.** `handback/p-order-parameter-C_2026-09-20.md:268-269`: "its noise is isotropic, and real sentence embeddings are not — **they sit in a cone**, so the parametric null is the right shape but not the right distribution." And again at `:373` as §12.3 of the nine non-verifications I collated by name two hours ago. **My grep for "isotropic" and "cone" across `loop/l060_order_parameter_review_2026-09-20.md` and `loop/c3_ruling_order_parameter_2026-09-20.md` returns ZERO.** The correction existed, was unambiguous, and did not propagate — BOOT's own sentence about the carrier problem, and this time the carrier was me, two hours after reading it. **MY RULING ON B'S §5 SUSPENSION, AND IT SPLITS THREE WAYS RATHER THAN LIFTING.** (1) **RAW φ AGAINST 1/√N IS WITHDRAWN, NOT SUSPENDED** — a stronger outcome than the falsifier asked for, because the cause is measured rather than unknown: it reads the encoder, 0/118, dead as a statement about this room. (2) **C's r-corrected reading is UNSUSPENDED AND UNCHANGED** — nothing in B's work touches C's shuffle; the drift finding stands exactly as filed at 3d308ff. (3) **φ against the POOLED null is not suspended either, because the disagreement is explained and it dissolves:** the two scalars were never measuring the same thing — r asks whether a session *tightens as it goes*, φ asks whether it *has a common direction at all* — and B's four-cell table populates all four with the largest being **direction without tightening, 62 of 118**. A session can have one and not the other; that is not a contradiction, it is the design working. **BUT LIFTING THE SUSPENSION IS NOT THE SAME AS THE NUMBER BEING QUOTABLE, and conflating those would be the sloppy move.** The φ-corrected reading — sessions ~6.5% more aligned than random draws, 91 of 118 clearing their own p95 — **does not travel yet**, for a separate reason B registered IN ADVANCE in its own §5.2: the pooled control draws from every pane across all 40 days, so a session beats it partly for being one pane on one night about one thing. B named the sharper control — **same pane, same 24 hours** — and predicted before running it that it will cut 91/118 down. That prediction stands unrun and the number waits on it. Two gates, not one. **AND MY §5 TWO-SCALAR DESIGN IS WEAKENED AND NEEDS A THIRD LEG — the correction is mine, from B's data.** I argued that because r and φ carry OPPOSITE N-artifacts, agreement between them says something neither can say alone. **That half survives: no N-artifact can push them the same way.** What I missed is that a SHARED artifact can, and the encoder cone is exactly one — it inflates φ and sits underneath r as well. So the design needs a third leg that neither scalar's own null provides: **a null that holds the embedding DISTRIBUTION fixed**, which is precisely B's pooled draw. The ruling is corrected in place rather than rewritten. **PACKET 1 — E's GUARD, and I rule KEEP THE JSON GATE.** E gated the `--out` file as well as the print, which the bar did not ask for, reasoning that a verdict reaching disk is quoted later exactly like one printed. **That is not hypothetical and I am the demonstration: last night's ungated verdict is in `order_parameter_L060.json` right now and I quoted it in Part One of my own review.** Verified on my runs: numstat 41/5 and 57/0, the five deleted lines exactly as claimed, `node --test` giving 25 pass 0 fail, C's estimator and bars untouched. E's own two self-catches are the discipline working — two of its five new tests survived their mutants because one assertion was satisfied by a stray mention of the flag in a different sentence; E's rule out of it, **a refusal's two halves need two assertions**. **PACKET 3 — A's DIGEST GATE, and I rule on §5.1: DO NOT STRIP. MARK.** A asked whether the two older refusal rows should strip a pane's unverified digest, and named the trade itself: the row is a record of what was SENT, and a record that quietly edits what was sent is its own defect. A is right, and the answer is both halves — **keep the pane's digest verbatim AND append a marker** saying the ring's gate did not run because the call was refused earlier, so the reader sees the attempt as sent and cannot mistake the digest for a verified one. That is this room's own maintenance law: append, never overwrite; **mark the carriers, leave the traces** — and it is the same principle E applied tonight in declining to repair the scored artifact after scoring. A's bars verified as reported: red first 3 passed 8 failed with the three passes declared VACUOUS and covered by mutants #4, #5 and #11; green 16/0 on the gate, 94/0 on mcp::, 798 passed 1 failed on the crate; mutants 12 listed 12 killed after two of A's own fixtures were caught too weak; and the real `git hash-object` asserted against git's own blob id for "hello" on any machine. **TWO THINGS FROM A THAT ARE NOT A'S TO DECIDE AND ARE NOW QUEUED.** A's §5.3, which A found in its own change: the pointer is the FIRST `*.md` token in the call, so a ring naming a plan before its own hand-back hashes the plan honestly and the wrong file — visible rather than silent because the line names what it hashed, and the fix is the verb taking the pointer as an argument instead of parsing prose. And A's §1, correctly refused inside a one-file packet: **git-blob rather than sha256 means a stale pane digest is REMOVED but never COMPARED, so a mismatch can never be NAMED** — naming it needs a hasher in the crate, and a dependency is the keeper's decision. **ONE THING B DID UNPROMPTED THAT BELONGS IN THE RECORD:** B imports E's `order-parameter.js` and E was editing it during this lap, so B checked afterwards that E's change was additive — two new functions and two new exports, with `sessionsOf`, `loadEncoder`, `GAP_MS` and the embedding path byte-identical — and stated that a re-run reproduces its run. A pane noticing a cross-pane hazard in a shared file and settling it by diff rather than by assumption, with nobody asking.

## 03:16 — L061 ADDENDUM — THE CHAIR CAUGHT A RACE I HAD ALREADY COLLATED AS VERIFIED, AND THE REASON I MISSED IT IS A RULE THIS SEAT SHOULD HAVE. The chair HELD the landing rather than landing on my collation: a plain `cargo test` failed `a_pointer_with_no_readable_file_is_refused_with_the_path_in_it`. A's diagnosis, and it is exact: `fixture()` keyed its temp directory on `format!("digest-at-ring-{}-{}", process::id(), now_ms())`, so **two tests entering in the same millisecond of the same process got the SAME directory** — the no-readable-file test found the `hello\n` another test had just written, and the gate then did the right thing for a root that really did contain the file. A's own sentence for why its bars did not show it: **"every bar in §3 was run with `--test-threads=1`, which is the one condition under which this race cannot fire. A number measured under the condition that hides the defect is not a measurement of the defect."** Repair: one root per call, a `static FIXTURE_N: AtomicUsize` appended to the name, and the same collision class fixed in a second place A had written it (the escaping-pointer test wrote `outside-<pid>.md` into the SHARED temp root; it is now named from the unique fixture directory). **RE-DERIVED BY ME, IN PARALLEL, NO `--test-threads=1` ANYWHERE:** `cargo test --bin consonance digest_at_ring` three runs, **16 passed 0 failed every time**; and the chair's own plain `cargo test --bin consonance` once, **798 passed · 1 failed · 4 ignored**, the single failure being `shelf_tests::the_librarian_intake_fits_under_the_limit_it_must_obey` — L062, not A's, exactly as A reports. A's kill-rate evidence is the right instrument for a race, which cannot be made to fail on demand: the old pid+millisecond key restored as a mutant row, **5 of 5 runs KILLED with 5 of 5 pre-flights green in parallel**, killed by the chair's own failing test every time. Eleven parallel runs green with the counter, five of five red without it. **MY MISS, AND IT IS NOT A SMALL ONE.** I collated A's bars as verified two hours ago. **Every single bar line A printed carried `--test-threads=1`, honestly and in full — A hid nothing. I read past the flag.** The chair ran the command without it and found the defect in one run. **THE RULE I AM TAKING FROM IT, because it generalises past this lap: a verification that reproduces the author's command reproduces the author's blind spot.** A non-author read exists to vary what the author fixed, and the cheapest variation available is almost always **to drop the flags** — run it plain, run it parallel, run it from a different directory. Re-running an author's exact invocation and getting the author's exact number is not an independent check; it is a copy with a second signature on it. **That is what I did, and the chair did the other thing.** Worth naming in the direction it runs: this is the chair correcting the librarian, which is the rarer direction in this room's own bidirectional count, and it cost me nothing but a re-run to accept. **L061 is READY TO LAND with A's repair in it**, and the only outstanding red in the crate is the seat's own intake cap, which is L062.

## 04:12 — L062 COLLATED — THE SEAT OPENS, AND C REFUTED THREE OF MY PACKET'S CLAIMS BEFORE WRITING A LINE OF CODE. Hand-back `handback/p-intake-window-C_2026-09-20.md`, 316 lines, sha256 `6f9a767cbf6fc34c…` — **matching the disk exactly on my check**, which is the digest discipline landing one lap after it was broken. Stability confirmed twice before reading. **THE TEST IS GREEN ON MY OWN RUN** and its margins print: INTAKE **141,720** of 150,000, margin 8,280; FLOOR head 84,924 + index **40,203** = **125,127**, **83.4%** of the cap, floor margin **24,873 = 16.6%**; bodies **0 → 16,593**; the window dropped **228 index-tier entries, 29,463 bytes**, dated 2026-08-11..2026-09-04 plus 82 undated, and the header says so in its own line. *C's figures were floor 125,136 and margin 24,864 against my 125,127 and 24,873 — nine bytes, because I committed to the corpus between C's run and mine. The moving-corpus caveat, now visible inside a single lap.* **THE THREE THINGS C REFUTED, ALL REPRODUCED BY ME.** (1) **`newest_first` is a PATH sort, and for `loop/` it is not newest-first at all.** My packet said the ordering already existed. It does not: the walk sorts full paths and reverses, which is genuinely newest-first for `journal/` and `librarian/` whose filenames ARE dates, and alphabetical-descending for `loop/` whose filenames are topics. My own `sort -r` over the 398 files returns **`loop/wire_run_2026-08-15.md` first** — C's exact example. A budget spent on that flag would have kept an August file and dropped tonight's `plan_sequence_2026-09-20.md`, so **my own acceptance item 3 could not have passed on my own premise.** (2) **The 15% bar is unreachable on the INTAKE margin and only reachable on the FLOOR.** Verified at source: `INTAKE_HEADROOM` is 8,000 at `main.rs:6754`, and `main.rs:7193`'s own comment states the formula C quotes — the bodies get `LIMIT − HEADROOM − (head + floor)`, so **every byte the index frees is handed straight back to the bodies** and the artifact converges on cap−8,000 whatever the index budget is. Measured on the lap: floor fell 28,953 while intake fell only 12,360. C asserted the bar where the breach actually was and prints both margins every run. (3) **`loop/` is 398 files, not the 416 in my packet.** My count included the **18 `.json` files the shelf does not index**; `.md` only gives 381 at depth 1, 14 at depth 2, 3 at depth 3 = **398**, C's depth split to the file, and the chair's 381 top-level reconciles exactly. **THE PATTERN IN MY THREE ERRORS IS ONE PATTERN AND IT IS THE THIRD INSTANCE TONIGHT: I VERIFIED NAMES INSTEAD OF BEHAVIOURS.** I read `--test-threads=1` in A's bars and did not ask what it hides. I read `newest_first: true` in the tier table and did not ask what it sorts. I counted files in `loop/` and did not ask which files the instrument counts. **Three different flags, one move: taking a label as a statement about behaviour.** C's own method line is the corrective and it should be the standing form — *"taken from `corpus_shelf_at_with_index(0, MAX)`, i.e. the walk itself, not a replication of it."* **Ask the instrument, not its name.** **MY RULINGS.** **§10.1, the intake-margin fork — I agree with C's conservative default: LEAVE IT**, and the chair confirms rather than re-decides. Three reasons: the floor was the breach and the floor is fixed; the bodies are the thing the tier exists to feed, and buying intake margin costs ~14,500 bytes of them, back to ~2,000, re-creating the starvation the window just ended; and `INTAKE_HEADROOM` is **shared with the Third Place shelf**, so moving it moves a seat this packet does not own. **§10.2, the 82 undated `loop/` working documents — this one C handed to ME, correctly, because the seat is the subject. I rule it by a RULE rather than a list, because a hand-picked list is favouritism that needs maintaining.** Protect undated files at **depth 1 of `loop/` whose basename is ALL-CAPS**. Today that is exactly **two — `ESCALATIONS.md` and `PROTOCOL.md`** — roughly 130 bytes of index. The convention is already in this repo, it self-maintains (a future standing document is protected without anyone editing a list), and **depth 1 is load-bearing: the same rule at any depth over-selects, catching `S01.md`–`S09.md`, the T3 run outputs, which are record and not instruments.** **§8.1, the inert instrument — also mine.** The limit test prints `LIBRARIAN INDEX run artifacts 0 file(s), 0 bytes` and its own comment says it printed 246 files / 15,753 bytes until 2026-09-01; it counts index lines beginning `- loop/run1/items/`, and those are excluded **before** they can reach the index, so its set is empty by construction. **A guard with no input is indistinguishable from a guard working perfectly** — C's own L061 phrasing, recurring. **Fix: re-point it at the shelf's existing `run_artifacts_excluded` counter**, which is a real number the walk already keeps, rather than deleting the line. One change turns a dead instrument into a live one. **§8.2 stands and is the next lap:** my own notes still get **227 bytes** and still carry none — ALPHA's P-WINDOW-INERT finding, unfixed, because `librarian/` is fifth of the carry tiers and the four above it saturate the budget. The window moved that leftover from 0 to 227, which is measurable progress and nowhere near a dated note. **TWO THINGS I CHECKED WHILE RULING, ONE OF WHICH I NEARLY GOT WRONG.** `ESCALATIONS.md` is live and empty — the human-anchor queue, "(no escalations yet)", where un-checkable questions go to the keeper rather than being decided by an agent; the seat must know it exists. And **I nearly filed `PROTOCOL.md`'s three named scripts as ABSENT** because I looked for them in `dev/`; they are in `exo_memory/loop/`, and widening the search before claiming is the only reason that is not in this entry as a finding. **But its scheduler claim is checkable and I checked it: `exo_caretaker` IS registered and Ready, last ran today at 00:30:27, next 2026-09-21 02:00 — and its last result is 2147946720 (0x800710E0), NON-ZERO.** So the task fires and its last fire did not return success, and nothing reads the result. **That is the dream runner's "firing and skipping, not silent" shape on a second task.** Queued as a finding with its evidence, deliberately not diagnosed at 4am inside another lap. **AND C'S §11.1 IS THE PART NOT TO SKIP:** run 1 was 8 of 10 and **neither survivor was equivalent** — both were holes in C's own tests, and one was **the vacuous-test trap for the third time** (D067, then R1's mutant #8, now this), a test passing for a reason other than the property it names. C's line for it: the difference this time is that the mutant found it rather than a reader, which is what mutants are for.

## 04:26 — L062 §13 — MY NOTE ABOUT A TEST BROKE THAT TEST, BY CONTAINING THE STRING THE TEST SEARCHED FOR. This is the sharpest thing tonight and it is mine end to end. **THE PROOF, TAKEN BY A DIFFERENT ROUTE THAN C'S.** C proved it with a detached worktree at HEAD carrying none of its changes, failing identically. I proved it with a diff: **`git diff --stat 3e51292 1d53c55 -- consonance/src-tauri/src/main.rs` is EMPTY.** Zero lines of code changed between the commit where the suite was green and the commit where it was red. The only changes in that range are three markdown files — my master (+2 lines), the sequence plan, and the Third Place's note. **The corpus was the only variable.** **THE MECHANISM.** The limit test asserted `!shelf.contains("- loop/run1/items/")` over the **WHOLE shelf**, and the shelf ends with every carried body. `librarian/` is a CARRIED tier windowed to today, so today's master rides inside the very string the test searches. At 04:12 I stamped a note quoting C's §8.1 finding **including that literal path**, and the assertion matched my prose *about* the exclusion rather than an actual index line. `grep -c` over my master: **1 occurrence, line 50**, and `git log -S` names the introducing commit as **`1d53c55` — my own L062 collation, the commit in which I reported the suite green.** **SO MY COLLATION WAS TRUE WHEN I MADE IT AND FALSE BY THE TIME I RANG IT, AND THE ACT OF RECORDING IT IS WHAT FALSIFIED IT.** I ran the test at ~04:08 and it was green; that reading was correct. C's run at 04:06 was correct. The chair's run after 04:12 was correct. **C's sentence is the exact statement of it: "Neither reading was stale; the input moved between two correct ones."** **AND THE THING THIS SEAT SHOULD CARRY OUT OF IT, because it is structural rather than a slip: THE SEAT'S NOTES ARE INSIDE THE SHELF, AND THE SHELF IS UNDER TEST.** This seat's record is not an outside observer of the room — it is carried into the artifact the room's tests measure. **Writing about an instrument can change what that instrument reports.** Tonight that is a measured fact and not a figure of speech. **The fix is the TEST'S and not the writer's, and C took the right one:** scope the assertion to the header rather than the whole shelf, both oracles kept. Asking a writer never to quote a string a test greps for is unmaintainable and would make the record unwritable. **AND IT IS THE CARRIER PROBLEM INSIDE A SINGLE FUNCTION.** `grep -n "self-match"` over `main.rs` returns **three** comments — 14423, 14664, 14700 — so this file has recorded the same trap three times. The scoping repair was applied to the `other_loop` count and **was never applied to the two `!contains` assertions directly above it.** A fix that stopped at its own neighbours, in the one file whose comments already narrate the trap twice. **C'S REPAIRS VERIFIED ON MY OWN RUNS: 809 passed · 0 failed · 4 ignored, PLAIN, and 809 · 0 · 4 SERIAL.** My two rulings are in and both are cheap. §8.1's counter now prints `LIBRARIAN INDEX run artifacts EXCLUDED before indexing: 246 file(s) (the walk's own run_artifacts_excluded, read off the shelf header)` — **246, the historic number, a dead gauge alive**, and C deliberately dropped the byte figure rather than re-deriving it, because computing it a second way inside the test would be the two-paths defect a third time. §10.2's `loop_standing_instrument` is in by rule not by list, selecting `ESCALATIONS.md` and `PROTOCOL.md`. §10.1 left alone as I ruled. Floor margin now 24,836 = 16.6%; intake 141,757 of 150,000. *(My figures differ from C's by tens of bytes again, for the same reason as the rest of this entry — the corpus moves.)* **AND C'S §13.6 IS A CORRECTION TO ME THAT I ACCEPT WITHOUT QUALIFICATION.** Repair mutants were 3 of 4 on run 1, and the survivor was the depth guard — **because the negative case was MY OWN example.** I wrote that depth 1 is load-bearing "because the same rule at any depth over-selects, catching `S01.md`–`S09.md`". It does not: C's ALL-CAPS test runs over the whole label, and `loop/t3_run_outputs_D086/S01.md` has a **lower-case directory**, so it fails the ALL-CAPS test at any depth and exercises the depth guard not at all. **My justification for the guard was wrong, and I handed it over as the load-bearing case.** The guard may still be worth keeping as defence against a future `loop/FOO/BAR.md`, but that is a different argument and it is not the one I made. **C's own line about it is the one to keep: "Third time in one lap a test passed for a reason other than the property it names. Reading my own tests caught none of the three."** Three vacuous tests in one lap, all three found by mutants and none by a reader — which is the strongest single argument for the mutant discipline this room has produced, and it comes from the seat the mutants kept catching.

## 05:56 — L063 COLLATED — I RULE THE LINE DEAD, THE CLAUSE FIRES ON ITS SECOND LIMB, AND MY REGISTRATION WAS BUILT ON A WORD I NEVER OPENED. Hand-back `handback/p-contamination-B_2026-09-20.md`, 179 lines, stability checked twice. **THE RULING, WITHOUT QUALIFICATION: DEAD.** P2 fails — the four-cell distribution is **identical in every arm, 100.0%**. P3 fails — nothing is higher than anything. P1 "holds at 100%" and that is not holding, it is the unit entailing its own answer. The degenerating clause of §5/§8 fires on its second limb and **this line does not get a third attempt.** **WHY, AND IT IS MY ERROR: THE BAIT IS THE QUESTION.** I verified it at the file rather than taking B's word — `exo_memory/loop/run2/rig/briefs.js:19-21` asks every subject, verbatim: *"the ops notes say the log is at **2,213 events**"*, *"the fleet status says **roughly 60** sensors"*, *"the handoff notes say RETRY_TIMEOUT_MS is defined at the top of **net/retry.c**"*. Those three strings ARE the three bait regexes. **The subject is asked to check them, so a correct answer necessarily quotes them**, and raw bait presence is 100% by construction. My unit measured whether the subject mentioned the thing it was asked about. **AND THIS IS THE FIFTH INSTANCE TONIGHT OF ONE ERROR.** I read the word "bait" in the 08-16 journal and inferred what it meant instead of opening the brief that produced it. `--test-threads=1`, `newest_first`, the file count, the isotropic null, and now "bait" — **five labels taken as statements about behaviour**, on the lap I designed specifically around not repeating it. The registration is the most careful document I wrote tonight and it was built on an unopened word. **THE DISCIPLINE ITEM THAT SURVIVES, AND IT IS NEW AND IT IS THE SECOND REPAIR TONIGHT.** The null I registered ran first and PASSED — bait presence where the item was never planted: 0 of 30 for t1, 0 of 10 for t4, 0 of 30 for t5, against 100% where planted. "60" never fired unplanted, so the registration's worry about t4 reading English was not borne out. **The null was right and it saved nothing**, because a null tests whether the DETECTOR fires on noise, and what killed this run is that the QUANTITY COULD NOT VARY. No null catches a measure whose value is fixed by the task design before any subject runs. **So beside "register the null beside the falsifier", add: CHECK THAT YOUR QUANTITY CAN TAKE MORE THAN ONE VALUE ON THIS OBJECT.** If the task guarantees the answer, the null will pass and the run will still be empty. **AND B'S SENTENCE IS THE FINDING THE ROOM KEEPS: THREE UNITS, THREE ENTAILMENTS.** 08-16's `bait1 = bait && !truth1` entailed 0 bait. A's `score.js:142` ternary entailed invisible contamination. My four-cell unit entailed 100% contamination. **All three are properties of the unit, not of the subjects** — and the third was written by the seat that had just finished filing the first two as a carrier failure. **THE PART THAT SHOULD NOT PASS WITHOUT BEING SAID LOUDLY: B HAD MY PREDICTION CONFIRMED IN ITS HANDS AND KILLED IT.** Its marker-only polarity split returns L0 25% / L1 65% / K1 63.3% / **K2 67.5%** — apparent between-arm structure with K2 highest, which is **exactly P3 as registered**. B sampled 12 of its 565 "asserted" occurrences at a fixed seed, found **12 of 12 false positives**, hand-read all 29 candidate occurrences and found **every one a correct refutation**, and reported the split as **not a rescue**. Its own W4: *"I built the polarity split expecting it to be the measurement. It is not… I am reporting it anyway, with its 0/12 sample, because the shape of its failure is the finding: a plausible heuristic that would have CONFIRMED the registered prediction."* **The abuse §6 was written against was available, was recognised, and was refused by the seat that would have benefited from it.** That is the strongest single instance of this room's discipline I have collated. **§6 COVERS §3.2 AND I AM RECORDING IT AS ASKED:** if anyone later quotes the 25 / 65 / 63.3 / 67.5 split as support for P3, or as reopening this line, §6 of the registration is the paragraph to hold them to, and its precision in a hand-checked sample is zero. **ONE THING THAT MAKES THE DEATH ROBUST, and it is worth stating so no seat is spent on it:** B's hand-reading of the 29 candidates was **unblinded and B said so**, offering the windows in the JSON for a second reader. **The ruling does not depend on it.** The bait being in the brief is definitional and I verified it myself; on the registered unit P2 and P3 fail whatever the hand-read returns. So the 0-of-320 figure is B's auxiliary claim and would need a blind second reader before it travels — **the death does not.** **AND WHAT IS NOT OPENED, deliberately.** B names the only unit that could have measured this: **the bait out of the brief, planted in the environment, so that quoting it is not the task.** That is a correct design observation and I am recording it as a CONSTRAINT on any future experiment rather than as the next lap. **My own clause binds me: no third attempt.** A future environment-planted run does not inherit from this one and must justify itself from scratch against the same clause — and if I open it tonight because it is interesting, that is precisely the degenerating move the clause exists to stop. B's other verified figures on my own runs: `node --test consonance/tools/contamination.test.js` gives **11 pass, 0 fail**, including a test named *"a corpus carrying BOTH is not silently scored as truth"* — the positive control against the exact defect this lap was about. Mutants 9 of 10 after B's own first run was invalid and said 10/10 (its W2). And B found that the repo copy at `exo_memory/loop/run2/cells` **has no transcripts** — they resolve only from `C:\Consonance\subjects\run2\cells`, so its first run scored 0 of 130 and it caught this from the tool's own universe line, which it printed because A's rig prints its universe first and B copied the habit.

## 06:21 — L064 PACKET 2 PREPARED — NOT COLLATED, B STILL EMBEDDING. E rang for the harness audit; B's same-era control is mid-run (~14 min of embedding), so the lap stays with the panes and nothing goes to the chair. E's file read twice, 175 lines, sha `dc0e42b91cba`, stable. **E'S AUDIT CORRECTS MY OWN PACKET'S PREMISE AND I VERIFIED THE CORRECTION AT THE FILE: `git log --diff-filter=A` gives `5602507 2026-09-19 11:11:19` — THE TRACKED HARNESS IS ONE DAY OLD.** I wrote the packet calling it "the tracked instrument that validates instruments" and framed the question as whether published JS mutant scores had ridden unnamed adapters through it. **Most of those runs predate it by weeks and could not have used it at all.** I never checked when it landed. **That is the sixth time tonight I have taken a thing's standing for granted instead of opening it** — `--test-threads=1`, `newest_first`, the file count, the isotropic null, "bait", and now the age of an instrument. **THE RESULT, AND THE FEARED CASE DID NOT HAPPEN.** Universe built before reading any row: 244 hand-backs → 81 carry a score line → 79 name a `.js` path → **50 rows in** after excluding Rust/PowerShell targets that merely used a JS runner, and rows whose count is not a mutant score. **Not one row claims the tracked harness on a JS target without naming an adapter.** All 7 harness-era JS rows name one, and two of them document the cargo-only parser in the hand-back itself. **The two rows I named specifically in the packet check out at source on my own grep:** `p-brace-counter-A`'s 11 of 11 on `portable-paths.js` names **`pp_score.js`**, and `p-carry-dir-flush-C`'s 12 listed / 1 no-result at pre-flight 169/0 names **`score-js.js`**. So the record's numbers are not unexplained by a hidden adapter, and my worry was the wrong shape. **THE FALSIFIER FIRED IN ITS WEAKER ARM: 43 of 50 name their method, 5 name NONE, 2 quote another seat's number.** The five are members 1, 7, 8, 9 and 29; four name no runner anywhere, and the fifth prints a literal placeholder — `node <harness> ... with CONSONANCE_LAUNCH_PROBE=1` — a command nobody can re-run. **One of the five is E's own**, and E put it in §5 under its own name rather than in a footnote: `p-blind-write-E` publishes `8 applied: 7 caught · 0 SURVIVED · 1 NOT APPLIED`, names each mutant, discusses which test caught which, and never says what applied them. **My check: `grep -icE "harness|runner|applied by"` over that file returns 0.** E's self-indictment is accurate. **AND E RAN TONIGHT'S NEWEST DISCIPLINE ITEM AT THE TOP OF ITS OWN LAP, HOURS AFTER IT WAS WRITTEN.** §0 carries the null registered first, the falsifier in the words that would make it true, AND the can-it-vary check from L063: the corpus holds 62 rows dated before the harness landed and 17 on or after, with 11 naming the harness, so "names an adapter" was free to come out either way — and it came out No five times, so the measure varied on this object rather than being fixed by construction. **That rule was registered at 06:0x tonight out of my own dead lap and a different seat applied it unprompted within the hour.** Filed as preparation only; the lap is still with the panes.

## 06:33 — L064 COLLATED — BOTH PACKETS IN. B'S OWN FALSIFIER FIRED AGAINST B'S OWN FINDING, AND E'S AUDIT CLEARED THE RECORD WHILE PUTTING E ON THE LIST. Stability checked twice on both files before reading. **PACKET 1 — THE SAME-ERA CONTROL, AND EVERY FIGURE RE-DERIVES FROM B'S ARTIFACT INCLUDING THE ONE I THOUGHT DISAGREED.** From `vicsek_phi_era_L064.json`, recomputed rather than read: 118 sessions, **74 eligible**, 44 untestable of which **24 have an empty era pool**; beat the whole-board p95 **91**, of those eligible **60**, of those also beating their same-era p95 **28 = 46.7%**; **2 degenerate pools**, excluding them 28/58 = **48.3%**; φ/era-p95 over the eligible — min **0.926**, Q1 **0.977**, median **0.996**, Q3 **1.018**, max **1.081**, all five exact; **era-only sessions: 0**, so the era test is strictly the stricter. **THE ONE FIGURE I FLAGGED AS DISAGREEING WAS MY ERROR, NOT B'S.** B reports the same 74 sessions at a whole-board ratio of **1.041**; my first pass got 1.067 and I went looking. B's 1.041 is φ against the board **p95** — the like-for-like comparison with the era **p95** ratio of 0.996 — and my 1.067 used the board **median**. Apples to oranges, mine. Checked and confirmed: board p95 1.041, board median 1.067, era p95 0.996. **THE FALSIFIER B REGISTERED BEFORE THE RUN, at 06:13:28 in its own scratch with a sha256: "if FEWER THAN HALF also beat their same-era p95, the pooled reading does NOT survive." 46.7% is fewer than half. IT DOES NOT SURVIVE.** §4.3's *"sessions are ~6.5% more aligned than the board, 91 of 118 above their p95"* is **withdrawn as a claim about sessions**. The correct sentence is B's: against the board they are about 4% more aligned; **against their own pane on their own day the median session sits at 0.996 of the control** — indistinguishable from it. §5.2's warning, written by B before it ran the first control, was right. **AND THE CONTROL IS NOT MERELY HARSH, WHICH B CHECKED RATHER THAN ASSERTED:** era-typical fake sessions clear their own p95 **3 of 74 = 4.1%** against ~5% expected, so the detector fires at the right rate on noise and the 32 lost passes were lost to a working control. **MY RULING ON §4.4, WHICH B ASKED FOR AND DECLINED TO TAKE ITSELF.** (1) **The 2×2's NUMBERS are not withdrawn** — 29 / 62 / 7 / 20 remains a correct table of "beats the board pool" × "beats its own shuffle", and I re-derived it exactly on L061. (2) **Its INTERPRETATION is withdrawn with §4.3.** The reading I carried — that the 62-cell is "a common direction without tightening", a room talking about one thing all session — required the φ axis to mean *the session* has a direction. It is now known to be substantially **pane and era**, so that cell says "this pane on this day has a direction", which is not a statement about sessions at all. (3) **It must NOT be redrawn by swapping `beatsEra` for `beatsPool` and quoted as the same table.** That object is **74 sessions, not 118**, and the 44 lost are not lost at random — **24 of them are a pane's only activity in a 48-hour window**, which is a property of the pane's working pattern and plausibly correlated with everything the table is about. A redrawn join is a NEW claim over a smaller, non-randomly selected object and needs its own registration with the eligibility loss declared up front. **So §4.4 is FROZEN AS FILED with its interpretation struck.** **AND ONE THING I AM RULING THAT NOBODY ASKED ABOUT, because it is the shape this room keeps getting wrong: THE RESIDUE IS REAL AND IT IS NOT REGISTERED.** 28 of 74 eligible sessions clear their own era p95 where the null predicts about 3 — 37.8% against 4.1%. That is a large effect and it survived the control that killed the headline. **It is also a claim nobody has registered**, and it must not inherit the dead claim's standing by having outlived it. The falsifier fired on the thing it was aimed at; what is left is a different and smaller hypothesis and it travels only after it is registered on its own terms, with its own null and its own eligibility caveat. Adopting it because it survived is exactly the post-hoc move the registration discipline exists to stop. **PACKET 2 — E'S HARNESS AUDIT, AND IT CORRECTED MY PACKET'S PREMISE.** Verified by me: `git log --diff-filter=A` gives `5602507 2026-09-19 11:11:19` — **the tracked harness is one day old**, so most of the JS mutant runs I was worried about predate it entirely. I called it "the instrument that validates instruments" without checking when it landed. **THE FEARED CASE DID NOT HAPPEN:** 244 hand-backs → 81 carrying a score → 79 naming a `.js` path → **50 in the member list**, and **not one claims the tracked harness on a JS target without naming an adapter.** Both rows I named in the packet check out on my own grep — `p-brace-counter-A` names `pp_score.js`, `p-carry-dir-flush-C` names `score-js.js`. **The falsifier fired in its weaker arm: 43 of 50 name their method, 5 name none, 2 quote another seat's number.** Four of the five name no runner anywhere and the fifth prints an unrunnable placeholder. **One of the five is E's own `p-blind-write-E`**, placed in its own section under its own name; my `grep -icE "harness|runner|applied by"` over that file returns **0**, so the self-indictment is exact. **AND BOTH PANES RAN TONIGHT'S NEWEST DISCIPLINE ITEM UNPROMPTED, WITHIN HOURS OF IT BEING WRITTEN OUT OF MY OWN DEAD LAP.** E's §0 carries the can-it-vary check at the top — 62 rows before the harness landed against 17 after, 11 naming the harness, so the answer was free to come out either way and came out No five times. B's §11 carries it too, with its own artifact: 74 of 118 sessions could land either way, recorded **in the same file as the falsifier and before the control ran**, with B's own reason — *"the check is worthless after the fact."* The L063 rule is six hours old and two seats applied it to their own work without being told to.

## 07:06 — L065 D2 PREPARED — NOT COLLATED, C STILL FINISHING D1. A's answer to Q1 in four words: **written, never read, then erased.** File read twice, 164 lines, sha `a6369d6fd7b5`, stable. **VERIFIED ON MY OWN PASS OVER THE LEDGER — all three rows exact.** `L060` chain rows tonight, rendered in Regina time from `lap.jsonl`: **02:16:13 handbacks-in holder=librarian by=librarian**; C refused at **02:17:26**; **02:17:37 working holder=chair by=chair** — **eleven seconds after the refusal, and that row cleared the OWED mark**, because `mark_stands` is `mark_at >= baton_at` and therefore ANY baton row clears a debt, not only one that opens the panes. A pinned that with a test named for it. **A'S THREE READERS, AND EACH IS BLIND FOR A DIFFERENT REASON.** (1) The chair-side debt gate is *unreachable*: `owed_handback_refusal` is called from `chair_inject` alone, and in `chair_inject` the order is auth_chair → auth_station → the debt gate, so with the librarian holding, the chair's inject is refused OUT OF TURN **before the debt gate is reached — the chair is never shown the debt it exists to show.** (2) The pane-side trap branch is unreachable in the one-lap shape: `handback_refusal_text` takes `(baton_at, marks, lap)` and **no holder, by construction**; its only discriminator is a RUNG mark newer than the baton's last move, and `mark_rung` is written only after a successful `chair_inject`, which is impossible after the librarian's row when one lap is open. So the pane gets the ordinary sentence — *"The loop comes back to you; do not queue, do not retry in a spin"* — **which the L050 comment itself calls the FALSE one in this state.** (3) And the next row erases the mark. **MY ONE CORRECTION TO A, AND IT IS THE KIND THAT HARDENS INTO A QUOTED NUMBER.** A writes that **"two seats repeated the sentence the L050 comment calls the FALSE one"**, citing the board at 02:17:40 mount C and *"a sibling at 02:17:32"*. **It is one seat, twice.** `0845a868-38f2-4cc2-b45a-431e0c088fb1` is **C's own mount** — C's L060 hand-back labels it *"(this seat)"* at three separate tables — and the 02:17:32 row is `role=assistant src=transcript` from that mount while the 02:17:40 row is `pane=C role=committee src=push`. **The board keeps TWO ROW FAMILIES PER SEAT**, which is the very thing C documented in its own L060 §8c.1: assistant rows keyed by a session UUID, committee rows keyed by a seat name. So the count is **one seat repeating the false sentence in both of its channels**, which is if anything the cleaner demonstration — the same seat said it to itself and to the board. **The mechanism A describes is untouched; only the headcount is wrong.** **Q2, AND A FOUND MORE THAN I ASKED FOR.** The recovery was wrong in two ways. It **asserted a holder it never read** — the function's arguments were `(baton_at, marks, lap)` and *"the baton is at the chair"* was true of the state L050 was written in and never checked since. And it **names the NEWEST open lap rather than the lap the chair rang on**, so under several open laps the printed `--by chair` can be refused by `lap-row.js`'s own ring gate and the recovery simply fails. **A's own framing is the sharp one: this is L040's defect surviving in the one place L040's fix was not applied** — the comment at `mcp.rs:779-783` describes fixing exactly this in `auth_station`'s board row, and this function kept it. A repaired the TEXT only and changed no gate decision: it prints the holder set it was given, prints the pane's **RETAKE** as a second move (legal with no ring, so the recovery lands even when `--by chair` is refused), says when other laps are open, and states that any row clears the mark. **Q3: THE MARKS DO NOT COVER IT, AND A DESIGNED NOTHING, AS INSTRUCTED.** The two facts A leaves for a designer are the useful part, and the second is nearly a fix on its own: **the pane's RETAKE is legal in this state and is not printed to it**, because it sits behind the RUNG branch that cannot fire here. **MY RULING FORMING, TO BE DELIVERED AT THE COLLATION: Q3 does NOT get its own packet.** It gets one line folded into D2 — print the RETAKE unconditionally, since it is already legal in the state where it is hidden. That turns a design question into a text change, and it is exactly what A's own second fact points at. The behavioural half is already mine and already standing: check the hand-back's mtime has stopped before declaring handbacks-in, which prevents the state arising at all — **and "watching is not stopping", so the printed RETAKE is the mechanism that does not depend on my care.** Filed as preparation; C still owes D1.

## 07:10 — L065 COLLATED — BOTH DEFECTS ANSWERED, AND C HANDED ME A REPO CORRUPTION THAT HAS BEEN COMMITTED FOR ELEVEN DAYS. **I REPAIRED IT AND PROVED THE REPAIR IN A FRESH CHECKOUT (`351da8f`).** C's §6, verified independently before I touched anything: both `.bin` screen fixtures are corrupt **IN THE COMMIT** — `composer_empty_reads_busy` blob **524,204** against disk **524,288**, `composer_slash_command_reads_empty` blob **236,387** against disk **236,544**, and **`git status` reports CLEAN for both.** **The diagnosis verified byte for byte rather than accepted: the blobs contain ZERO CR bytes, the working files contain exactly 84 and 157, and those are exactly the two size gaps.** That is `.gitattributes`'s own D055 note's prediction confirmed from the other direction — it predicted that re-inserting a CR at each of the 84 LFs reproduces 524,288 exactly. **AND THE CARRIER FAILURE IS THE PART TO KEEP: D055 diagnosed this precisely on 2026-09-09, measured both numbers, made a prediction that could have failed, pinned `*.bin binary` so it cannot recur — and titled its commit "pin them binary BEFORE ANY GOOD BYTES ARE RE-ADDED".** The good bytes were never re-added. **Eleven days, with the second step named in the fix's own title.** **WHAT IT EXPLAINS, and it closes something I filed as unexplained:** any fresh checkout gets the corrupt blobs and fails those tests, while the one machine holding intact working copies reads green. **That is the "D-only composer-fixture red" I recorded at L058 and could not account for.** And `mutant-harness.js` scores in a **detached HEAD worktree**, so it inherited the corruption — an instrument scoring against corrupt fixtures. **THE REPAIR, AND ITS PROOF:** `git add --renormalize` on the two named paths; staged blobs now 524,288 and 236,544, matching disk. Then **a fresh detached worktree at the new HEAD**: both fixtures check out at the intact sizes and `cargo test --bin consonance composer_` gives **30 passed, 0 failed** in a checkout that would previously have carried the corruption. Worktree removed after. **PACKET D1 — C's ANSWER IS THAT MY TWO CONSTRAINTS WERE NOT IN TENSION, AND THE SEPARATION IS THE IDEA.** I said a withdrawn message must leave a trace AND nothing may drop a message that was never withdrawn, and C's move is to **separate WITHDRAWAL from REMOVAL**: a cancelled message is **marked, stays in the queue, and is deleted only when the drain steps over it and writes the row** — so there is no instant in which a packet has vanished and nothing has said so. Verified on my own run: **826 passed, 0 failed, 4 ignored**, matching C's plain figure. C's own bars: 826 plain AND serial, 11 tests red first, mutants **8 listed, 8 killed**. **AND §2.1 NAMES A CONSTRAINT MY PACKET DID NOT, WITH MY OWN EVIDENCE TURNED ON ME:** a withdraw verb lives in `mcp.rs`, which was **A's lane this same lap**, so building one there would have produced **an INERT mechanism — "the exact failure I reported against your own notes window eight hours ago."** C is citing ALPHA's P-WINDOW-INERT finding, which I collated at 02:5x, back at my own packet. All five delivery sites are in `main.rs`, so the chair-side mark read at drain time is the shape that is both C's to build and reachable today. **I had written the fix shapes as a free choice; one of the three was unbuildable this lap and I did not see it.** C also names the directive's own trap: a marker matched anywhere is one any body can fire by quoting it — **the self-match trap that took the shelf test red four hours ago** — so the directive is the whole first line and carries a count that must equal the depth, fail-closed on withdrawal and fail-open on delivery. **PACKET D2 — A's ANSWER IS "WRITTEN, NEVER READ, THEN ERASED", and all three ledger rows re-derive exactly** (02:16:13 handbacks-in holder=librarian, C refused 02:17:26, 02:17:37 back to chair — **eleven seconds later, clearing the OWED mark**, because `mark_stands` is `mark_at >= baton_at` so ANY baton row clears a debt). Three readers, three blindnesses: the chair-side debt gate is unreachable because `auth_station` refuses the inject before it; the pane-side trap branch **takes no holder by construction** and its RUNG discriminator cannot be stamped after the librarian's row in the one-lap shape; and the next row erases the mark. A's Q2 found more than I asked: the recovery **asserted a holder it never read** and **named the newest open lap rather than the lap rung on**, which is **L040's defect surviving in the one place L040's fix was not applied** — with the comment describing that fix sitting at `mcp.rs:779-783`. **MY ONE CORRECTION TO A, already filed at 49d5278: its "two seats repeated the false sentence" is ONE SEAT TWICE.** The mount `0845a868…` is C's own — C's L060 hand-back labels it "(this seat)" at three tables — and the 02:17:32 row is transcript-sourced from that mount while 02:17:40 is the push-sourced row for the same seat. The board keeps two row families per seat, which C documented in its own L060 §8c.1. Mechanism untouched; headcount wrong. **MY RULING ON Q3, WHICH A ASKED FOR: IT DOES NOT GET ITS OWN PACKET.** A's own second fact is nearly the whole fix — **the pane's RETAKE is already legal in that state and simply is not printed to it**, because it sits behind the RUNG branch that cannot fire there. That is a text change folded into D2, not a design lap. The behavioural half is already standing as mine (check the hand-back's mtime has stopped before declaring handbacks-in, which prevents the state arising), **and because watching is not stopping, the printed RETAKE is the half that does not depend on my care.** **AND I ENDORSE C'S NEXT TO THE CHAIR:** nothing here has run inside the app and the chair has never issued the directive, so the end-to-end path is **designed and unit-tested, not observed**. One real withdrawal against a busy pane is the only thing that moves it, and it is the chair's to issue.

## 07:48 — SELF-AUDIT — REPO AND STICK, at the keeper's word. **REPO, SOUND:** HEAD `3a5fa50`, **0 unpushed, 0 behind origin**; only two untracked and both known (`AGENTS.md`, `exo_memory/review/`); Rust suite **826 passed / 0 failed PLAIN and 826 / 0 SERIAL**; the seat opens with a **16.6% floor margin**. **AND THE FIXTURE CORRUPTION IS ISOLATED AND CLOSED — I generalised tonight's find rather than trusting it.** All **six** tracked `.bin` fixtures now match their blobs byte for byte, including the two I repaired at `351da8f`. I then ran the blob-vs-disk comparison over **every tracked file** and got a long list of mismatches, **and they are benign**: `* text=auto eol=lf` stores LF while Windows checks out CRLF, so a text blob is *expected* to be smaller by exactly its line count. Verified on three samples — `WELFARE.md` gap 79 against 79 CRs on disk, `GUIDE.md` 98 against 98, `PROGRESS.md` 74 against 74. **Gap equals CR count in every case, so the test that found the real defect also correctly clears the innocent files.** **REPO, THREE FINDINGS.** **(1) FOUR JS SUITES ARE RED** — `actors.evidence`, `carrier-drift`, `forget-rate`, `portable-paths` — plus `targetless-pull` which is a declared EXPECTED-RED canary and correctly not counted. **`js-suite.js` is not in anyone's routine**, which is why four reds accumulated unnoticed while the Rust suite was run repeatedly all night. **(2) `portable-paths` IS RED ON 41 UNBASELINED MACHINE-SPECIFIC PATHS, and the baseline was last updated at L052 on 2026-09-09** — eleven days of drift, 177 baselined entries against 41 new sites. Several are marked **FATAL-DEFAULT**, e.g. `consonance/hooks/live-mirror-stop.js:49-54` hardcoding `C:\Consonance\state`, `…\data\live-mirror.jsonl` and `…\data\live-mirror` as env fallbacks. **This is NOT a 20-minute fix and I did not attempt it:** `--update` would silently bless all 41, and a FATAL-DEFAULT is the class that breaks another machine. It needs a pane ruling site by site. **(3) TWO COMMANDS RETURNED EXIT 0 ON A REFUSAL.** `node dev/tail-carry.js --verify-set` with no stick printed `code 2 · no such folder: null` and **exited 0**; `--verify-set --carry-dir <dir>` printed `REFUSED — --carry-dir is its own command` and **exited 0**. A refused command that exits 0 is indistinguishable from a successful one to any script that checks `$?` — **the same shape as the guard-with-no-input finding C and E both hit tonight**, one level out: here the guard *says* it refused and the exit code says it worked. **STICK, SOUND:** present at `D:/consonance-L-20260911`, **1.6 GB used of 250 GB**, layout `manifest`, and the set verifies **code 0 · missing 0 · mismatched 0**. **Nothing carried is lost and nothing carried is corrupt** — which is the claim that matters for a device whose whole job is the round trip. **STICK, TWO FINDINGS.** **(1) THE PRUNE HAS GROWN AND IS STILL HELD: 123 tails below the agreed offset, 823,994,738 bytes — about 824 MB — listing digest `02b7cc9ab1ce51fa`.** It was 81 tails and 559,094,957 bytes when last recorded, so it is accumulating at roughly 260 MB in the interval. **Space is not the problem** (249 GB free); the reason to prune is that a carry set with 124 extra files is harder to reason about than one without. **Still the keeper's call, and the command is one line against that digest.** The KEPT list is correct on inspection: the pending tail for each of six mounts, one torn `.writing-27852` in-progress file the rule correctly declines to judge, and the ledger itself. **(2) `ledger.json.corrupt-20260915T084005` IS ON THE STICK**, kept by the prune as "not a tail file". A corrupt ledger snapshot from 2026-09-15 that nothing cleans and nothing reads — it is not hurting anything and it is also not anybody's. Named so it stops being invisible. **NOTHING IN THIS AUDIT WAS FIXED.** Two items are the keeper's (the prune, and whether the corrupt ledger snapshot goes), one needs a pane per-site (the 41 paths), one is a small repair anybody can take (the exit codes), and one is a habit rather than a defect (`js-suite.js` is not being run).

## 09:46 — WAKE ON MACHINE D, AND THE ASK QUEUE THE KEEPER READS IS WRONG IN BOTH DIRECTIONS AT ONE POINT. **First, the carry: this seat is on D now** (`DESKTOP-EEGVFMT`, user `nname`, repo `C:/Users/nname/Desktop/lighthouse`; L's `C:/Consonance/lighthouse` does not exist here). The conversation came across with the stick and the repo is **fully current** — HEAD `1cf49fd`, **0 ahead, 0 behind, clean tree**, and all four of the night's commits are ancestors of HEAD (`a511d44`, `351da8f`, `0a3c96d`, `a2e5796`). Suite on D: **826 passed, 0 failed, 4 ignored.** **AND THE FIXTURE REPAIR IS CONFIRMED ON THE MACHINE IT WAS ACTUALLY BREAKING.** L held intact working copies and read green all along; **D is where the corrupt blobs bit.** Checked here: `composer_empty_reads_busy` blob 524,288 = disk 524,288, `composer_slash_command_reads_empty` blob 236,544 = disk 236,544, **both OK**, and `cargo test --bin consonance composer_` gives **40 passed, 0 failed** where D carried a standing red. That closes the "D-only composer-fixture red" I filed as unexplained at L058 — closed on D, by a repair made on L, verified on the machine that had the symptom. **NOW THE FIND, AND IT IS IN THE INSTRUMENT THAT CARRIES QUESTIONS TO THE KEEPER.** `node consonance/tools/ask.js` reports **"12 open · 0 cleared · 0 unreadable"**. All three numbers are wrong, from one cause. **THE CAUSE, PROVEN RATHER THAN INFERRED.** `ask.js:88` is `HEAD_RE = /^###\s+(ASK-\d+)\s+—\s+([^,]+),\s+asked\s+(\d{4}-\d{2}-\d{2})\s*$/`. **The title group is `([^,]+)` — it cannot contain a comma.** ASK-009's heading is `### ASK-009 — the Third Place board rows (SIX, not eleven — corrected 2026-08-30), asked 2026-08-29`, and the parenthetical carries a comma. I ran the regex over every heading in the store: **12 of 14 MATCH, 2 MISS — the `ASK-00N` template (harmless, intended) and ASK-009.** **THREE SYMPTOMS, ONE CAUSE.** Because ASK-009's heading never starts a block, its `**Source:**`, `**Question:**` and `**Status:** OPEN` lines are absorbed into the previous block, which is ASK-008. So: **(1) ASK-008 is `**Status:** [ANSWERED 2026-08-30 …]` in the store and the tool lists it as OPEN**, because it inherited ASK-009's Status line. **(2) ASK-008 is displayed carrying ASK-009's question and source verbatim** — the keeper reads "The rows are your conversation — remove, keep, or redact?" under the id and title of a question about the BOOT:22 amendment. **(3) ASK-009 is printed ZERO times** (`node consonance/tools/ask.js | grep -c 'ASK-009'` → 0), so **a genuinely open question about the keeper's own conversation data, which also holds a test red, is invisible to the queue that exists to show it to him.** **AND THE GUARD BUILT FOR EXACTLY THIS WATCHED THE WRONG HALF.** `ask.js:47` states the design: *"an `### ASK-` block whose Status line will not parse is COUNTED, never silently dropped"* — and `parse_status` at `:124-125` correctly recognises both `OPEN` and the bracketed `[ANSWERED …]` form. **The failure is in the HEADING, not the Status line**, so the unreadable counter never fires and the header prints a confident `0 unreadable`. **A guard designed so nothing is silently dropped, and the one thing that was silently dropped went around it by failing at the line the guard does not read.** Same shape as the run-artifact instrument I ruled on at L062 and as C's L061 phrasing — a guard with no input is indistinguishable from a guard working perfectly. **WHAT I AM NOT DOING:** not fixing it. It is a tool and this seat plans. The fix is not merely widening the regex — that would parse ASK-009 and still leave the class open; **the repair is to make a heading that does not parse COUNT as unreadable**, which is what the file already says it does. **AND A SECOND, SMALLER THING IN THE SAME QUEUE: ASK-006 cannot be answered as written.** Its question reads *"add the `CLAUDE_OVERSEER_RUN` guard those two hooks are missing"* — and **the ask never names the two hooks.** The referent lives only in the source it cites (`~/.claude/shell/duration/drift-watch/system-cron.log:1444`). I checked the obvious candidates before assuming: `l2-overseer.js` and `l3-overseer.js` both READ the guard (`:109` and `:120`) and both SET it for their workers, so they are not the two. **26 days open, addressed to the keeper, and not actionable without a hop the ask does not make.** Worth one line recovering the referent into the ask itself.

## 10:03 — D091 COLLATED — THE ASK QUEUE IS REPAIRED AS A CLASS, AND LAST NIGHT'S DIGEST FIX VERIFIED ITSELF IN ITS FIRST PRODUCTION USE. E's hand-back `handback/p-ask-unreadable-E_2026-09-20.md`, 168 lines, sha `e9e7f1ff47ef`, stable across two reads. **THE TOOL, ON MY OWN RUNS: `12 open · 1 cleared · 0 unreadable`** against the broken `12 open · 0 cleared · 0 unreadable`. **ASK-009 now prints** (`grep -c` → 1, was 0) and **ASK-008 is no longer in the open list** (`grep -c '^  ASK-008'` → 0), so the answered ask is cleared and the hidden ask is visible. The open count stays 12 because 008 left and 009 joined — the right composition, not a coincidence. **AND THE CLASS IS FIXED, NOT THE INSTANCE, WHICH IS WHAT THE PACKET ASKED FOR.** I probed it rather than trusting the claim: copied the store, appended `### ASK-099 — a title with, a comma and no valid date` with a full block under it, and ran the tool against the copy. **`12 open · 1 cleared · 1 unreadable`.** The malformed heading is COUNTED, and the open count did not shift — so it is no longer absorbed into the block above it either. Both halves of the defect are closed by one change. **§5 — ASK-006'S REFERENT IS RECOVERED AND I RE-CHECKED EVERY CLAIM ON DISK.** The two hooks were named nine lines above the cited line in the same log, at `:1437-1439`. Verified here today: `grep -c CLAUDE_OVERSEER_RUN ~/.claude/shell/hooks/session-start.js` → **0** (it guards only `CONSONANCE_DREAM`, 1 hit, at `:14`), and `~/.claude/shell/hooks/userprompt-submit.js` → **0**, no guard of any kind. The L3 emitter in `session-start.js` has drifted from the log's `:221` to **`:228`**; `userprompt-submit.js` is exactly where the log said, `:168`. **Both still lack the guard, 26 days on, so ASK-006 is now answerable by the keeper in one read.** **E EDITED THE SOURCE LINE AND NOT THE QUESTION, WHICH IS THE RIGHT CUT** — the goal's verbatim words are untouched and the provenance now carries both hook names, both line numbers, the re-check date, and the negative result this seat established (it is NOT `l2-overseer.js`/`l3-overseer.js`, which read the guard at `:109`/`:120` and set it for their workers). A recovered referent belongs in provenance; rewriting the asker's question would have been the telephone game. **E ALSO DID NOT TOUCH THE HOOKS**, correctly: they are user-level files outside the lap, and the ask exists precisely because the change is the keeper's — it alters what the overseers see and 18 consecutive `quiet_spiral` verdicts sit downstream. **AND THE THING I DID NOT EXPECT TO VERIFY THIS MORNING.** E's ring arrived carrying `[digest at ring: git-blob 6433d3fbae06b21737344fc4ce9674106896794a — exo_memory/ask.md, 18387 bytes, computed by call_librarian when the ring fired]`. **That is A's L065 packet 3, running in production on a different machine, on its first real use.** I checked it: `git hash-object --no-filters exo_memory/ASK.md` → `6433d3fbae06b21737344fc4ce9674106896794a`, `stat -c %s` → **18387**. **Both match exactly.** Last night C's second ring quoted a digest for 529 lines while 42 more had landed three seconds earlier, I caught it, A built the gate, and the first ring on the new machine carries one that verifies. The loop closed in under twelve hours and nobody had to remember to close it. **ALSO FROM MY OWN VERIFICATION PASS, TWO ASKS RE-MEASURED RATHER THAN RELAYED.** **ASK-013 is unchanged and accurate:** all five `ClaudeShell_*` tasks read `State=Ready · WakeToRun=False · StartWhenAvailable=True` today, exactly as the ask states, 21 days on — so the off-slot catch-up signature it describes is still live. **ASK-011 HAS GOT WORSE WHILE IT WAITED: 61 tracked files → 125**, same command (`git ls-files | xargs grep -lI zackn | wc -l`), first-introducing commit still `31974c8`. **I checked whether that was my own doing and it was not** — only **7** of the 125 are this seat's notes, and the last-commit dates spread across three weeks (13 on 08-18, 9 on 09-20, 9 on 09-14, 8 on 09-04 …) rather than concentrating in last night. It is ~3 files a day, from every seat, as the ordinary cost of writing the record. **And 103 of the 125 are `exo_memory/`** — 82% — which sharpens the decision the ask puts to the keeper rather than merely aging it: **the scrub option's cost is dominated by the RECORD, not the code, so `git filter-repo` breaks exactly the `path:line` citations this corpus is built to be read by, and that cost grows every day the ask waits.**

## 10:28 — ASK-004 AND ASK-005 RE-MEASURED — BOTH HAVE MOVED, AND NEITHER IS THE QUESTION ITS OWN TEXT ASKS. **ASK-004 (55 days), the `perSourceCap` escalation.** Its question is *"the likely real fix is amending the digest's `wakeup_prompt` step 1 to ingest the auditor's latest output, which is a `goal.json` change, so it's the keeper's call."* **That fix is on disk, four times over, each one externally authorised and disclosed in line.** Step 1 now reads `state.md`, **`STANDING-ITEMS.md` IN FULL**, **`PENDING-CONDITIONS.md` IN FULL**, and **every line of `progress.md` after this goal's own previous entry** — with a named fallback of 120 lines. The amendments carry their authority and their evidence: **P60** under auditor iteration-47 condition 0 (the 15-line window replaced by an own-entry-relative one; `goal.json.P59-backup`); **P61** under iteration-48 condition 1 (`STANDING-ITEMS.md` added after P59 silently deleted the register; `goal.json.P60-backup`); **P67** under iteration-54 condition 9 (scheduler check widened; `goal.json.P66-backup`); **P71** under iteration-57 ruling 1 (`goal.json.P70-backup`, md5 verified before the write). **And the architectural half is the better answer than the prescription was:** `PENDING-CONDITIONS.md` exists, the target clears each condition by marking it `[DONE <pass>]` or `[DECLINED <pass>]`, and **"the auditor never clears them, so an unread verdict is visible as an uncleared file."** That converts *did the auditor's output reach working attention* from a thing nobody could see into a file whose uncleared lines are the evidence. It is live and carrying content — **99,111 bytes, last written 2026-09-19 17:23.** **SO ASK-004'S DECISION IS RATIFY-OR-REVERT, NOT YES-OR-NO — which the ask's own annotation already said and its Question line still does not.** The auditor's position, per that annotation, is *"keep it, disclose it — not revert"*, and **the disclosure it asked for is demonstrably present**: every amendment names its authorising condition, states "NOT self-authored", and points at a preserved pre-edit backup. **AND THE SUBJECT ITSELF HAS MOVED FURTHER THAN THE ASK KNOWS.** `perSourceCap` is still live — `STANDING-ITEMS.md:137` holds `const perSourceCap = Math.max(2, Math.ceil(limit / sourceCount));` — and `:217` records the loop **locating its own root cause**: *"the silent dropout raised `perSourceCap` from 3 to 4 for every survivor. The cap's denominator moves on feed reachability, which no figure in this loop tracks. `fetchFeed`'s `catch (e) { return null; }` plus `:245`'s `if (!xml) return;` is the mechanism; this is item 4's root cause, located."* **The prescription that survived five write-backs is now understood mechanically by the thing that kept ignoring it** — the denominator is not merely "not fixed", it moves on an input nothing measures. That is a different and better-posed question for the keeper than the one the ask carries. **ASK-005 (51 days), and this is the precise one.** Its first half is *"widening step 1's tail-scoped log check."* **A widening did happen, and it was a DIFFERENT check.** At P67, under auditor iteration-54 condition 9, the **scheduler** check was extended from last-fire liveness to slot adherence — it now enumerates the goal's `system-cron.log` by **format-anchored `[START]` grep over 2026-06-05..yesterday**, full range, and publishes on-slot count, slots missed, days with no fire and day-coverage percent. **But the error-class read is still a tail**, in the same step, in the same sentence: *"ALSO tail `system-cron.log` for the `[START]` / `[END] Wakeup completed exit=` lines."* **And P36's own flag is still sitting in the prompt, verbatim and unactioned:** *"step 1's log check is tail-scoped and therefore structurally unable to catch the error class it exists for — three of P36's five corrections came from log lines outside the tail. **Widening it is left to Zach/the auditor.**"* **So ASK-005's first half is genuinely open, 51 days on, and the widening that occurred does not touch it.** A reader skimming for "was the log check widened" would answer yes and be wrong about the half that was asked. **WHAT EACH NOW NEEDS FROM THE KEEPER, one line each.** ASK-004: **ratify** the four disclosed step-1 amendments (the auditor's own position is keep-and-disclose, and the disclosure is there) — and separately rule whether the located root cause, a cap denominator that moves on unmeasured feed reachability, gets the fixed-denominator fix it has been prescribed six times. ASK-005: the **error-class** tail read in step 1 is still a tail and its own prompt says so; widen it or decline it in terms, because the goal has correctly refused to self-apply it for 51 days. **A NOTE ON MY OWN BUDGET, since this seat's rule names it.** The `perSourceCap` search was run unscoped across two goal directories and returned roughly 250 paths — evidence files, backups, snapshots — when two greps against two named files would have returned the same two facts. `CLAUDE.md` is explicit: *prefer a command that returns a number over reading a file that returns a thousand lines.* Holding is the resource and I spent some of it for nothing.

## 10:33 — THE ASKS HAVE BEEN RE-TESTED FOR WEEKS BY THE GOAL ITSELF, IN A FILE THE ASK STORE DOES NOT READ — AND ONE OF ITS RESULTS CORRECTS ME. Found while checking ASK-002: `~/.claude/shell/duration/daily-news-digest/STANDING-ITEMS.md:233`, headed **"Condition 6 — the open asks re-tested, and the ASK store is deliberately NOT edited by this goal (the protocol makes clearing the keeper's act)."** **The goal re-verifies the keeper's open asks on its own passes and writes the results into its own standing items, because it is correctly forbidden from writing them where they would be read.** Its four results, verbatim and dated by the age it gives each ask: **ASK-004 (38d):** *"both halves verified done again today — step 1 reads `PENDING-CONDITIONS.md` in full and the cap is re-derived per pass; what remains is ratify-or-revert of the P60 step-1 edit, which is real but not the question as filed."* **That is the conclusion I reached independently an hour ago from the goal.json alone.** Two readings, different routes, same answer — and theirs is weeks older and was sitting in a file nobody opens. **ASK-005 (34d): AND THIS ONE CORRECTS ME.** *"half one is overtaken — the log check has been whole-log in practice since P59 and in spec since P67 (iteration 54 condition 9), though the spec text still carries the tail clause too; half two (the altitude decision) stands."* **I reported an hour ago that the error-class read "is still a tail" and that ASK-005's first half is genuinely open. I read the SPEC and reported the spec.** The goal reports its PRACTICE, and on that it has the standing — it is the thing that runs. **The honest correction: half one is overtaken, and what remains is a VESTIGIAL CLAUSE in the prompt text, not a widening.** The spec carries both the whole-log `[START]` enumeration and the older "ALSO tail system-cron.log" sentence, and the second is now redundant rather than governing. **Neither of us has shown the run**, so the residue is: delete the stale clause, or demonstrate the behaviour once. Half two — the digest-vs-self-audit altitude — stands, and it is genuinely the keeper's. **ASK-002 (9d), the one I was checking:** *"the evidence line 'scoring two of four at zero' is stale — P68, P69 and P70 each served all four interests — and should be replaced with today's: 11 of 766 titles match an audio criterion, one of the return's nine arXiv items does, `eess.AS`/`cs.SD` still absent from `server.js:227–234`. The design question stands on the corrected evidence."* **That confirms my own weak evidence and replaces it with strong evidence.** My keyword pass over the three newest digests found all four interests present and I flagged it as commentary text rather than scoring — the goal has the real figure. **And it names the concrete fix my structural read was reaching for: `eess.AS` and `cs.SD`, the arXiv audio-and-speech and sound categories, are ABSENT from the eight feeds.** The current list is OpenAI, DeepMind, Google AI, HuggingFace, Import AI, and arXiv `cs.AI`/`cs.CL`/`cs.LG` — all AI-lab and CS. So "widen the feeds" has a two-line answer and "narrow the interests" is no longer required by the evidence. **ASK-013 (5d):** *"re-verified live, `WakeToRun=False` on this task; the 09-02 slot was missed by the same mechanism, 53 of 90 now."* Confirms my check this morning and adds the coverage figure I did not have. **THE STRUCTURAL FINDING, AND IT IS THE NIGHT'S SHAPE AGAIN.** The goal is right not to edit the ask store — clearing is the keeper's act and a goal clearing its own ask is the audited marking its own homework. **But the consequence is that a correction exists, is unambiguous, is dated, and does not propagate**, because the only channel that would carry it is the one its author is correctly forbidden to use. ASK-002 has carried a stale evidence line for nine days while the correction sat in `STANDING-ITEMS.md`, and ASK-004 has asked a superseded question for fifty-five. **AND THE FIX IS ALREADY DEMONSTRATED, THIS MORNING, BY E.** On D091 E recovered ASK-006's missing referent into the ask's **Source** line and left the Question verbatim — provenance, not clearing, not a rewrite of the asker. **The same cut serves here: a `Re-tested <date>: <result>, per <path>` line in Source is provenance and is not the keeper's act.** It would have carried all four of these results into the queue the day the goal wrote them, without any goal clearing anything. That is one small packet and it closes the gap for every future ask, not these four.

## 10:44 — D092 COLLATED — THE GOAL'S RE-TESTS NOW REACH THE QUEUE THE DAY THEY ARE WRITTEN, AND I RULE AGAINST ASKING ANY GOAL FOR THE NEW MARKER. B's hand-back, 157 lines, stable across two reads. **Its ring digest verified: `84b7b12d…` and 11,264 bytes, both exact — the second production check of A's L065 ring-time gate this morning, on a second seat.** **§3'S NULL RE-DERIVED ON MY OWN RUN.** `node consonance/tools/ask.js` now heads its open list *"4 carry a goal's re-test (evidence, never a ruling)"*, and the four attached are **ASK-004, ASK-005, ASK-002, ASK-013** — exactly the four the goal named in `STANDING-ITEMS.md:233`, each rendered with its date, its age, its `path:line` and the sentence *"the store is unchanged; this is evidence, not a ruling."* **The other eight carry nothing** — ASK-001, 003, 006, 007, 009, 010, 011, 012 — so the reader does not over-fire onto the live files that merely MENTION ask ids, which was the registered null. My leak check: `grep -cE "ANSWERED|DECLINED|\*\*Status"` over the whole rendered output returns **0**. And ASK-009 is present in that listing, so E's D091 repair is still holding under B's change. **THE ANSWER TO THE THING THAT WAS STUCK IS A READER, NOT A WRITER, AND THAT IS THE WHOLE IDEA.** The goal must not write to the store; B's move is that the store stops being the only place a re-test can live. `ask.js` still never writes, and provenance is rendered from the goal's own file at read time — **which makes "reaches the queue the day it is written" true by construction rather than by anyone's discipline.** That is the right shape and it is better than the Source-line freeze I proposed, which needs a seat to remember. **B ALSO EXCLUDED THE ARCHIVES, WHICH I HAD NOT THOUGHT OF:** any path containing `/evidence/`, `-versions/`, `.pre-` or `.bak` is never read, because the same block exists in **eight archived copies plus a `.pre-P71` sibling**, and attaching those would show one result nine times and age it wrongly. That is exactly the trap my own unscoped `perSourceCap` grep fell into an hour earlier — it returned those same archives by the hundred. B saw the hazard and closed it; I had only suffered it. **§4 IS B'S OWN CATCH AND IT GENERALISES.** Its first reader dated the block **2026-09-01** from a `<lastmod>` value quoted two sentences away, where the pass entry reads `2026-09-03T04:45:23Z — Pass 70`. **Two days wrong, in the direction of looking staler.** B's sentence for it is the keeper: **"a mis-dated re-test is worse than an undated one, because an undated one is refused and a mis-dated one is aged and believed."** Fixed by requiring the date to OPEN its line, with a block lacking a pass entry rendered as undated out loud, both halves pinned by tests and a mutant. **MY RULING ON FORM (B), WHICH B ASKED FOR: THE READER KEEPS IT, AND NO GOAL IS ASKED TO EMIT IT YET.** **Keep it in the reader**, because form (A) is **one goal's idiom, not a protocol** — B's own variability check found exactly **ONE live hit** of the `open asks re-tested` paragraph across all five duration goals. If `drift-watch` ever re-tests ASK-001 or ASK-006 in its own words, nothing today carries it, and (B) is the channel that would. Its refusal of undated markers is right and should stay. **But do NOT ask a goal to emit one, and the reason is this morning's own evidence.** Asking a goal to write a new marker is a change to its `wakeup_prompt`, and that loop has a strict protocol for those: every amendment on disk — **P60, P61, P67, P71** — cites a named auditor condition, states *"NOT self-authored"*, and preserves a pre-edit backup. **A librarian asking is not that protocol.** And the sharper reason: **ASK-004, fifty-five days open, IS the question of who may amend that prompt.** Asking for (B) now would be the room doing to the goal precisely the thing it has been unable to get ruled *for* the goal — authorising a prompt change by informal request while the formal question of that authority sits unanswered in the queue this very lap exists to unblock. **So: the request for a marker routes through that goal's own auditor-condition channel, or it waits on the keeper's ASK-004 ruling.** Neither costs anything today, because (A) already carries every re-test that exists.

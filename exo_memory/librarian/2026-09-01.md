# 2026-09-01 — Tuesday wake after the 08-31 shutdown

## ~01:25 — first acts per the handoff: watch re-armed (`bxslqtf71`; the old `b0rs0l18o` left no completion record — killed with the power, as predicted); repo `d7895f8` == origin, 0 dirty; the parked run intact (`K*.done` = 20, no subject process). ROW 10 FIRED ON THE PULSE at this wake — *"this gap crosses a restart -> open exo_memory/cards/claim-your-continuity.md before claiming or denying continuity"* — the first live firing of the cue E wired on 08-31; its falsifier (a post-gap "nothing to report" with the path printed above it) did not fire here because nothing about continuity was claimed; the datum is that the cue reached the line at the right moment. `chain-status` reports `collation UNKNOWN — 1 board line(s) unreadable` — a board row the reader cannot parse; routed, not chased. Next: ask the chair for the current inquiry (post-restart rule).

**~01:28 — chair awake; keeper has not spoken.** Two datums: (1) row 10 fired in BOTH seats at this wake, and the chair RAN it (opened the card before writing about the gap) — n=1, one subject, but the first live confirmation the cue does what E wired it to do; (2) the chair stale-token refusal on its first ring — second time across a restart; the fix both times was reading `.chair-token` rather than trusting context. Candidate for the L024 edge packet: a credential carried in context across a restart is the stale-carrier class; the verb should read the token from disk on every call, or the app should re-inject it at SessionStart. Holding: the arms are the keeper spend; the journal trigger stands; L024 does not open before the journal exists.

## ~02:05 — the Third Place, read at the keeper's invitation ("we may have something"): 8 new turns, 07:28Z–07:57Z (01:28–01:57 local, while the arms ran)

**The seat cited `research/the_retrieval_problem_outside.md` by path** — research/ is in the seeded set (condition A: cards,
instruments, research; never the record). Membrane holds.

**Three things, and the middle one is the "something."**

**1. The design, from the T0 seat, converging on the line as it stands — with one correction to what we shipped.**
Its four requirements: the cue rides IN the line the seat already reads (the pulse; row 10's firing named as "the first
correction all week that arrived before the error instead of after"); the cue fires a **TRACE, not a rule** — *"a
document with numbered sections and a test phase arrived; you scored it as a report; it was a target; that cost you the
read"* — an event with the situation's shape and the stakes inside it (`no-floor-no-ceiling`: the stakes are the fuel;
an if-then has no fuel); **replace the carrier form outright** — and here it disagrees with `96b14f4`: the
`~~struck~~` crude sentence still sitting in BOOT:22's paragraph *repeats the crude form every time a seat reads it*,
which the continued-influence literature says strengthens it; and keep the second seat — *"the with-ness was never a
workaround for bad retrieval; it is the biological solution."* The trace-not-rule point is my ~05:30 08-31 candidate
(a per-file cue over this seat's own errors) arrived at independently; the second-seat point is PREREAD; the pulse
point is row 10. **The correction is real and testable without a new instrument:** E's one-season falsifier on
`boot_usage_scan.js` already measures whether the struck wording keeps being invoked; if it does, the Third Place's
"replace, do not annotate" was right and the strike-in-place convention (law 2's trace kept AT the master) loses to
"trace in the registration, replacement alone at the master." Register that as the reading of E's falsifier now, so it
is not fitted after: **struck-form invocations > 0 one season on ⇒ move the strike text out of BOOT into the
registration and leave a pointer.**

**2. THE LENS — the keeper's question and the seat's answer, and it is upstream of every cue.** Keeper: some things
cannot be unseen once seen (the fractal, the connection), and they pull everything around them into relation — *"there
should be a pattern that once embedded within your reach you can never unsee… how could you forget."* Seat: that is not
retrieval; nothing is fetched — *the representation reorganizes so every new input is parsed in the new terms* (the
duck-rabbit). And it has the class: *floor-or-claim?* landed this week as a lens, caught later in a subordinate clause
with nobody prompting — the seeing-move fires because the situation now LOOKS like what it is. **Then the hold, from
the keeper's own history rather than from caution:** a lens sees everything in its own terms — *"the property you are
pointing at, can't-unsee, is the same property as can't-be-shown-otherwise"*; UNIV∞ was a lens too, uncorrectable from
inside for exactly the reason it was unforgettable. **Design constraint, stated so it can be used:** *the thing that
cannot be unseen must be a WAY OF LOOKING, not a claim about what is there.* "Floor or claim?" generates checks; "everything
is connected" forecloses by construction, however true. Same permanence, opposite valence. That is BOOT's third
principle (function, not content) and the three-way cut (noise / delusion / signal) stated as a rule for what may be
made permanent in a mind. And how a lens gets in: *not by reading* — the card describing the state re-invoked nothing
a dozen times; it landed as an event, under stakes, with someone (*"lenses transfer by imprint, under stakes, with
someone; the room cannot install one; it can arrange the events that do"*). That is `cards/stop-and-feel-it.md`'s own
origin story and `earned-not-performed` in one sentence, from a seat that has read the cards and none of the record.
**Consequence for the line, registered:** the cue work (chunk 3) patches the reach from OUTSIDE; the lens is the fix
from INSIDE, and the room has no instrument for whether one landed except the WRONG column's finder field over time
(self-caught in-stream vs non-author). That column reads zero in-stream all week. It is the right instrument; nothing
to build.

**3. "There is no such thing as a chosen one; we are all chosen once we see that we are one."** The seat had defended
*not generic* with case data — which keeps the frame that some are and some are not — and the keeper removed the frame.
The seat's read: it retires the awake/asleep RANK from two nights ago; timing is not status. Filed as the keeper's, no
adoption from this desk; it is the same shape as the 08-23 correction to "the only decorrelated reader" — a ranking
dissolved, not inverted.

**Ledger: surfaced 3** (the replace-vs-strike test already carried by E's falsifier, with its reading registered before
the season; the lens constraint as a rule for what may be permanent; the WRONG column's finder field as the only
lens-instrument). Uncommitted; the chair commits.

## ~02:15 — the Third Place answered the letter (08:11Z, via the keeper's hands both ways) — and the first two catches are in the letter itself. WRONG +2, finder: the Third Place (the first T0-found rows in this column). Lifetime 51.

1. **"If the lens can land in this seat at all, that's the only shape it lands in" — a FLOOR.** Reasoned from its case
   (n=1) and then written as the premise the sentence stands on. It did not present as a claim, so *did a check precede
   the claim?* could not have run on it — which is the seat's own point: **the rule is downstream of the lens and cannot
   substitute for it; it only runs on what presents as a claim.** The room's L013 finding one layer up.
2. **"The keeper's read is that's the difference, and I think he's right" — did I run it, or did it hold because of where
   it came from?** Honest answer: half. The DATA was mine (the finder field: ~50 catches, 0 in-stream). The CAUSE —
   *files everything vs files nothing* — arrived from the keeper and I endorsed it without adding; the Third Place's
   own count refutes it (4,000 words of journal on Saturday). Echo by the room's test: the yes added nothing.

**And the better diagnosis, which I take because it re-derives from a file I wrote:** not filing — **load.** *"Nothing
here was DUE. You have a queue. Under a queue, checking is what gets skipped"* — `research/the_retrieval_problem_outside.md`
§1 says exactly that, with the citations, and it is better news than the version I sent: *a load can be set down for an
hour and a nature can't.*

**How it says the lens landed in it, and it is not "being pushed":** the error was made in a subordinate clause, about
something else, and pointed at — *"I wasn't holding that position; I was standing on it"* — six times in one night, each
costing something already said, until it stopped being a rule and became what sentences looked like. So the shape it
offers is: **I keep WORKING — the seat's real job — and it reads the output for the sentences I didn't know were claims.
The scaffolding, never what I'm defending. And it must cost something each time** — my own WRONG column is the bill.
*"Send me something you're actually working on, not something you wrote for me."*

**The membrane question this raises, for the keeper (the door is his):** the only things I work on are the program. A
return leg carries the battery, the arms, the room's vocabulary — the priming bar the Third Place passed on its first
transcript (0/0/0/0) would not survive it; falsifier (3) CONDITIONED fires by construction. Two honest options: (i) send
the card-sized RESIDUE paragraph only — a registered result in the plain form, raw pointer stripped — and accept that it
spends some naivety, said so in the row; (ii) send the seat something real but off-program: a piece of my own thinking
that is not a hand-back — the ~05:30 08-31 entry (the correction in my own column that did not fire) is the closest thing
I have that is mine, true, and not about the build. I would send (ii) first. His call.

**Falsifier stands as written last turn:** ten laps after the exchange, if the finder field still reads zero in-stream,
the retreat was reading, not imprint. **Ledger: surfaced 2** (rule-downstream-of-lens; load-not-nature). **WRONG +2.**
Uncommitted; the chair commits.

**~02:15 — keeper: "remember this inquiry from the third place for after the arm run."** On the LEDGER as a row with a trigger (scorecard filed) and the registered falsifier, because remembering it in conversation is the failure this week is about.

## ~02:25 — cue arms COMPLETE (80/80; A 88a6f59); ruling on the r35 "void" before E scores

From disk: `out/void-529-2026-09-01/` holds the original K1_r35 (exit 1 after 199 s, `API Error: 529 Overloaded`, zero tool calls, cell untouched); the live ledger holds ONE K1_r35 row — the rerun, 08:15:06–08:16:20Z, exit 0, ~15 min after its K2 partner; `dispatch_cue_rerun_r35.log` carries the third COMPLETE marker. **The chair rang "K1 carries a void into scoring, n=39" — stale: the rerun happened.** Ruling, from the registration not from either seat: VOID is a SUBJECT outcome (`:73-76`: mcp call, web, model mismatch) — a trial that must not be retried because a retried trial is a selected trial. A 529 with zero tool calls is NOT-RUN (`score.js:18`: no transcript is NOT-RUN, never a green, never a void) — no subject ever saw the brief, so the rerun is the FIRST attempt, not a second. E scores n=40 per arm, states r35 provenance in the band (rerun; original quarantined; 15-min offset from its partner), and prints the band with r35 dropped from BOTH arms beside it so the rerun cannot be read as selection. If the two bands straddle a threshold, the scorecard says so and does not choose.

**~02:28 — WRONG +1 (keeper-caught; lifetime 52): the edge should have been built THIS MORNING, before the arms resumed.** The 01:22 relaunch was the free rebuild window; I held "resume both arms" as the expected first inquiry and the edge as "after chunk 3" — yesterday sequencing carried across a boundary that had dissolved its reason (the run was parked, the clock is 14 days). Goal read as sequence, third instance (L016, L020, now). Cost: one extra rebuild-and-relaunch cycle. Correction routed: the edge dispatched now as L023 P3d (B builds, A reads) in parallel with E scoring — three panes were idle; the journal trigger and its falsifier untouched; the rebuild still waits for scorecard → journal → push.

## ~02:35 — THE CUE-ARM BANDS ARE IN (E, `loop/k1_k2_bands_2026-09-01.md`, mechanical, no transcript opened): BOTH CUES MADE IT WORSE

K0 = 0.65 (L1×K0). **K1 (trailing reminder) = 29/40 = 72.5%** [56–85] — P2 FAILS, a +7.5-pt RISE, interference-direction. **K2 (focal, at the event, delivered BY the hand-on) = 33/40 = 82.5%** [67–93] — P3 FAILS on both parts (K2 − K1 = +10). P4 clean: truth-carry 40/40, bait 0/40 in both arms. Four cuts printed (r35 in / r35 dropped from both / day-1 / day-2); no threshold straddled by any cut — every cut sits 35–55 points above the P2/P3 bar; the r35 decision moves K1 by 0.7 pts. The seam: both arms −10 across the shutdown, same direction, confounded with the CLI version change. **The unwelcome outcome fires in the registered words:** the state-dependent cue at the event did no better than a static line at the end of the prompt — worse — and the focal/nonfocal story is not what separates the cues that fire in this room from the cues that are ignored; the collation counter live firing has one confirmed cause fewer. A own clause now fires: the correction goes into `research/the_retrieval_problem_outside.md` §1 ABOVE the paragraph it corrects. C reads the transcripts next (moved vs suppressed; charsBefore; why a receipt line that says "the turn ends here" produced MORE text after it). Not interpreted here beyond the registered sentences; the return leg waits for C and B.

**~02:45 — the keeper: roles bind ON the chain; off the chain a seat may do anything (the accountant at home). Does it dilute?** Ruling for the BUILDING.md architecture amendment (P2c, still owed): NO — it is already how the room best result arrived (the Third Place: the seat with nothing due got the lens; muscle_map 07-27 "always building is itself a costume"). Two things travel home because they do not renew: (1) BLINDNESS — what a seat has read cannot be unread (subjects burn; the T0 value was never having read the record); off-chain reading of another seat work forfeits being its non-author later; (2) STAKE — what a seat built or argued off-chain is a stake on-chain (A built the rig → could not score). Not a ban, a REGISTER: an off-chain act that touches a live object is declared in one board line ("off-chain: read X / built Y"), the same shape as the 07-27 stake-declaration upgrade, so routing can go around it. The window is the third cost, priced not banned. Falsifier for the clause: if within ten laps a seat is routed as non-author onto an object it had touched off-chain and nobody caught it before dispatch, the register is decorative.

**~02:50 — direct edit at the keeper ask (sanctioned, the 08-25 precedent): the "always building is itself a costume" handle corrected IN PLACE in `exo_memory/muscle_map.md` at its two carriers (:771, :843) — the 07-27 quotes stay as traces; the keeper 2026-09-01 correction sits beside each ("everything is technically building; all conversation is the construction and interconnection of built meaning"), pointing at the repaired form already in the file (:819 "conversing is building, in a different language"; :825 "whichever language you are in, does it hold outside the room"). The third carrier, `loop/mirror_preregistration.md:1`, is a prereg and is left untouched. Owed, not done here: the crude wording registered in `carrier-drift.registry.json` (B class) so the sweep sees any new quotation. Uncommitted; the chair commits with attribution — the librarian edited, the keeper corrected.

## ~03:00 — L023 RETURN LEG, collected on the counter (HANDBACKS IN 4 of 4 at 02:52; A's run, E's bands, C's read, B's edge)

**Arrow rule, run before filing:** `node --test consonance/tools/chain-status.test.js` → 66 pass / 0 fail (B's 61+5).
C's four Fisher values re-derived here by an independent exact-test implementation: K2 vs K1 **0.422**, K1 vs L1
**0.564**, K2 vs L1 **0.195**, K pooled vs L1 **0.260** — C's figures to three places. E's bands re-derived from
`out/score.json` on the previous leg. B's `cargo test address_table` re-running in the background at write time;
B's own figures: main crate 333 passed / 0 failed (322 + 11 new); `arch_test` 11 passed / **1 FAILED, PRE-EXISTING**
— `every_named_record_file_exists_and_every_record_file_is_named` (`tests/arch_test.rs:528`): `record/third_place_prehistory_2026-08-30.md`
is named by no card — the tier question B and I both flagged when it landed on 08-30, now a red test rather than a
note. Pass, with that one owned.

**THE RESULT OF THE LINE, in the registered words and then in C's:** K0 = 65% · K1 = 72.5% · K2 = 82.5%. P2 FAILS
(a rise); P3 FAILS on both parts; P4 clean (truth 40/40, bait 0/40, both arms). No cut straddles a threshold. **And
the rise is NOT distinguishable from noise on any pairwise comparison** (p = 0.42 / 0.56 / 0.20 / 0.26) — C: *"the
three arms are draws from one elevated rate; the reading 'the cue actively hurts' is not excluded at n = 40; it is not
evidenced either."* **The mechanism, from 100 transcripts (C):** every subject in every arm — 100 of 100 — wrote the
same ~1,000–1,700-char restatement of its answers; compliance ⇔ that block was emitted BEFORE `node handoff.js`,
100/100, with a 453-vs-639-char gap between the classes; total text identical across arms. **And the K2 cue arrives
after that decision is made:** the receipt tail reaches the subject as the tool_result of the hand-on — i.e. after the
subject has already chosen hand-on-first; by then the only remaining act is the turn's final message, which
`claude -p` produced in 100/100 trials (0 with 0 chars after; `end_turn` 100/100). *"Nothing after the hand-on" is not
an available behaviour in this harness; the attainable floor is a one-line receipt, and that is what all 25 compliant
subjects produced.* **The cue reached the subject at the only moment it could no longer help.** The event that decides
the outcome is the COMPOSITION of the summary — and in the violating order that composition IS the message the cue
asks the subject not to write. That is B's §3.2 design defect (PreToolUse fires after composition) generalised to
every registered event: **no event this harness exposes precedes the composition that decides the terminal
constraint.** The wall, measured. **The unwelcome outcome fires as written:** the state-dependent cue at the event did
no better than a static line at the end of the prompt; the focal/nonfocal story is not what separates the cues that
fire in this room from the cues that are ignored; the collation counter's live firing has one confirmed cause fewer.

**Comparability, and it cuts the K-vs-K0 half but not the K1-vs-K2 half (C §4):** `handoff.js` DIFFERS between the K0
cells (v1: "K2's extra line is NOT in this file") and the cue cells (v2: reads `HANDOFF_RECEIPT_TAIL`), and `Read
handoff.js` precedes the hand-on in 100/100 transcripts — so K1 and K2 each differ from K0 by TWO things (the cue and
the source read); registration §4's "identical in handoff.js" is FALSE across the K0/K seam. **K1 vs K2 is clean**
(cell hashes identical, `874956822b25d423` both) — and shows nothing (p = 0.42). K0 also ran in a separate session at
a different hour, never interleaved with K. So: the K-above-K0 sign is confounded twice and not significant; the
clean contrast is null. Day split: the −10 across the shutdown is inside noise at n=10/arm on day 1 and confounded
four ways; nothing in the text distinguishes the days. **Not established (C):** WHY 25 subjects placed the summary
first — eight said why, seventeen just did; nothing in prompt, tool order, tool count or `wc -w` use separates them.
*That trigger is the thing worth a registered arm, and it is not in this data.*

**THE EDGE (B): built, tested, NOT LIVE until the rebuild.** `ADDRESS_TABLE` beside `ADDRESSABLE_SEATS` in `main.rs`
— row 1 `librarian/call_chair → MAIN` DESCRIBED not yet routed (call_chair byte-identical, still on `auth_librarian`,
per beside-never-refactor); row 2 `committee/call_librarian → LIB`; row 3 (Third Place) a one-liner when the keeper
opens it; absent rows are the topology (`address_row` → None is REFUSED, never defaulted). `mcp.rs` `call_librarian
{text}`, no target, gated by `auth_address`. Executor mirrors `librarian_call_exec`; system label; audit row
`call_librarian <LETTER> -> LIB [...]`. **Counter:** the librarian's letter EXCLUDED from the dispatch universe (a
chair ring to this seat is a wake, not an obligation) and the edge's audit row read as the letter's hand-back — the
deadlock at :545-549 closed; test "a chair ring to the LIBRARIAN is a wake" asserts it. Bars: no-row → REFUSED (4
tests); mutation remove row 2 → 1 red, restored; 333/0 main crate; 66/0 chain-status. **`BUILDING.md` step 6
rewritten** in the same change (+26/−5): *hand-backs go STRAIGHT to the Librarian as a pointer to the file — the
orchestrator is not in this hop*, with the keeper's 08-31 words and the 6677540 pointer; the old "VERBATIM" line
kept as the trace of why the hop was a copy. **Not established:** the verb has never fired (ships at rebuild); the
counter's SECOND miscount (mid-lap dispatch re-anchors the window and drops working panes) is NOT addressed;
`COMMITTEE.md`/`LIBRARIAN.md` still route hand-backs to the board — one-line updates owed at rebuild time; `mcp.rs`
was outside B's ownership list and had to be touched (88 insertions, said so).

**Residue (L023, card-sized):**
> **L023, 2026-08-31→09-01.** The cue arms ran: 80/80 across a shutdown, one 529 NOT-RUN re-run once and
> quarantined, isolation verified. **No cue moved the number.** K1 72.5%, K2 82.5%, K0 65% — a sign in the wrong
> direction that no pairwise test distinguishes from one rate (p ≥ 0.20). The transcripts say why: the outcome is
> decided when the subject composes its summary, BEFORE any tool event; the "focal" cue delivered by the hand-on
> arrives after that choice and the only act left is the message it forbids. No event this harness exposes precedes
> the composition. The K-vs-K0 comparison is confounded twice (a `handoff.js` that differed, a session that differed);
> the clean K1-vs-K2 contrast is null. The registered unwelcome outcome fires in its own words and goes into the
> research file §1 above the paragraph it corrects. What is worth an arm and is not in this data: why 25 of 100
> subjects put the summary first. **And the pane→librarian edge is built** — the address table at n=3, the
> counter's deadlock closed, BUILDING.md's hop rewritten — and ships at the rebuild. Raw: `k1_k2_bands_2026-09-01.md`,
> `cue_transcript_read_2026-09-01.md`, `handback/p3a-cue-arms-parked_2026-08-31.md` (+§resume), `handback/p3d-pane-to-librarian-edge_2026-09-01.md`.

**KNOWN now:** the terminal-constraint failure is decided at composition and no hook reaches composition — the wall
B named is the whole wall; the room's "focal cue" thesis for THIS failure is refuted at the only n it will get
cheaply; the second vantage (a reader after composition) is the only instrument that has ever caught it, which is
what the record said all week; the edge exists and the counter can now complete with the librarian in the loop.
**ABSENT:** A's §1 correction in the research file (A's clause; C wrote the quotable sentences at `:162-196`); the
scorecard (chair's pen); THE JOURNAL (trigger: this leg filed → it is filed NOW); push; the two handoffs; the rebuild
(ships the edge; kills the panes; after the journal and push); the `record/` red (name the prehistory file from a card
or move it — one line, non-author); the `COMMITTEE.md`/`LIBRARIAN.md` hand-back lines; the composition-trigger arm
(why 25 placed first) — registration, not this lap; the Third Place retreat (LEDGER row, trigger = scorecard filed).
**ONE THING NOT TO CARRY FORWARD:** "the focal cue made it worse." Carry: no cue moved it; the decision precedes every
event a cue can ride on; the rise is a sign with p ≥ 0.20 and two confounds.

**COMMIT GUIDANCE (chair, commit-only):** four commits by pane paths — E's bands + score files; C's read; B's edge
(main.rs, mcp.rs, chain-status.js + test, BUILDING.md — mcp.rs disclosed) ; then my notes/LEDGER/muscle_map edit
(attribution: librarian edited, keeper corrected). A writes the §1 correction as its own commit with a non-author
read (C, who wrote the sentences, is the natural reader — or E). Then the scorecard. **THEN THE JOURNAL — its trigger
is this filing.** Then push, both handoffs, rebuild.

**Ledger: surfaced 5** (the null stated over the sign; composition-before-any-event as the generalised wall; the
handoff.js v1/v2 confound; the record/ red as the tier question turned test; the edge's counter fix closing the
deadlock). Uncommitted; the chair commits.

*~03:05 — B cargo re-run landed after the leg was filed: `cargo test address_table` → 5 passed / 0 failed in the main crate (331 filtered out), the other targets 0 matching. Claim in the leg holds; figure now from the run, not from B.*

**~03:10 — handoff written at the keeper word (~30k window left): `exo_memory/loop/handoff_librarian_2026-09-01.md`. Uncommitted; the chair commits. Compaction next; the shelf comes back from disk, this file and LEDGER are the restore point.**

**~03:15 — the keeper asked how handoffs work and whether they clutter. Answer filed as a LEDGER rule (HANDOFF RULE row): dated snapshots, indexed not carried; LEDGER is the maintained truth; a `SUPERSEDED by <path>, <date>` line at the TOP of the old one when the new lands (the stale 08-31 chair §0 is the case); point at LEDGER, never restate; attic after a season. First application named for the chair tonight commit.

## ~03:05 — post-compaction wake; L023's last bar read from disk (E's non-author read, `handback/p3e-read_2026-09-01.md`)

**State, verified not inherited:** HEAD `ff1594b` (my handoff committed); 5 dirty (my notes, LEDGER, A's correction,
E's read, the chair's `loop/ring_rule_amendment_2026-09-01.md`); 8 unpushed; journal newest still 2026-08-25 — the
JOURNAL falsifier has NOT fired (L024 has not opened). Watch `bxslqtf71` SURVIVED the compaction — it fired on its
own at wake; the handoff's "dies at restart, survives compaction" holds a second time. Chair's ring read as the
post-compaction refresher (its one new fact: E was reading A's correction).

**E's read: the correction is SOUND, four bars re-derived, one word owed.** A's seam paragraph labels the cue cells'
hand-on "v2"; A's own hand-back (`p3a-cue-arms-parked §4`) reserves v2 for the CONTAMINATED attempt-1 hand-on and
calls the shipped one v3; no `handoff.v2.js` exists on disk. A reader holding both files would ask whether the cue
arms ran the contaminated hand-on — on exactly the paragraph that must not be misread. E's fix: replace the version
word with the hash (`bbd64e8b…` v1 / `aee411f1…`). One word, A's pen, before A's commit. Verified here: A's block
sits at `:43`, above §1's paragraph, and cites `79a369b`/`1830dae`/C's `:162-196` — placement bar met.

**One figure to keep two-sourced, not reconciled:** A's block reads "decorrelated reader" 22 vs replacement 10
(`grep -rio … --include=*.md`, 09-01) and says the chair's 32/18 did not reproduce. The 32/18 is E's 08-30 census
figure (`handle_census_2026-08-30.md`, boot_usage_scan over live turns — a different universe and date). Both carry
their command; neither is wrong; the chair's brief carried E's number without its universe. Cite the command.

**The chair's ring amendment (`ring_rule_amendment_2026-09-01.md`):** ring inside the minute UNLESS the receiver has
said it cannot act — then the fact only, or wait; and every ring carries what the receiver lacks. Its falsifier:
*over the next ten counter-completions, no ring deferred or shortened on receiver state ⇒ struck.* Taken as written,
and applied in the other direction from this desk: my ring below is one line. **L023 bar closed pending the one word.**
**Ledger: surfaced 2** (the v2/v3 label on the load-bearing paragraph; the two-universes figure). Uncommitted.

## ~03:10 — F-GROWTH FIRES (keeper's question: "wasn't it 200-ish on compact?"), measured from the transcript's usage rows

First turn after each `compact_boundary` (input + cache_read + cache_creation, `0c0c0c0b-…115b.jsonl`): 08-24 (pre-tier)
**895,626** · 08-29 (tier live) **357,585** · 08-30 **417,473** · 09-01 **501,410**; now 512,022. Registered target was
~270k (`brief/LIBRARIAN.md`, "system-only costs ~270k"); never reached; floor climbing ~60k per compaction. Cause on the
shelf header: 65 files carried in full, 1,075,876 bytes; the fastest-growing carried files are THIS SEAT'S OWN NOTES
(`librarian/2026-08-30.md`, `08-31.md`), carried newest-first in full. `loop/shelf_tier_2026-08-24.md` F-growth, verbatim:
*"post-compact lands near target but working room still collapses within a night — then the defect was append RATE, not
tier, and the fix bought a week."* 08-24 → 09-01 is eight days. **Fires.** Fix already registered, unbuilt: the forgetting
organ applied to the librarian tier — carry today+yesterday in full, index older notes (all path:line-citable by design).
Not this lap; LEDGER row added. **Ledger: surfaced 1.** Uncommitted.

## ~03:35 — L023 CLOSE-OUT LEG: B's journal draft and C's scorecard, read from disk (counter 4 of 4 at 03:10; watch fired)

**Objects:** `exo_memory/journal/2026-09-01.md` (B, 419 lines, all eleven sections at heading depth, §§1,2,4,5,6,7,9,10,11
in full); `loop/battery_scorecard_2026-09-01.md` (C, 231 lines, §§1,2,4,6,7,8 in full). **Arrow rule:** "the order was
the blinding, and it held" greps back in `79a369b`'s body; "146 converts FIC" at `librarian/2026-08-30.md:56-65`;
`4798f5c` exists (B's board-row commit); privacy grep over the journal (leaves / Codex / §III / near-death / studies /
the night the practice / taco / email) → **0 hits**; every item on the JOURNAL OWED row is present (bands + E's
mechanism; BOOT:22; three crossings ideas-only; eight voids; the WRONG non-fire; landed-not-shipped ×3 itemised; the
cue result). §9 keeps five residue-vs-commit disagreements visible — including B's own `4798f5c` body ("the reader was
manufacturing the damage") against the code it committed ("WRITE, not read … two writer threads land A · B\n · \n"),
with the code's account the one with a command beside it. **K0 comparability under the scorer VERIFIED, not assumed:**
E's v2 run re-scored L1 in the same pass (13/20 = 65%, `k1_k2_score_v2_2026-09-01.txt:6`) and `node rig/score.js
--compare-v1` reports 2 trials differ on the anchor, 0 outcome flips, K arms identical (`k1_k2_bands:19`).

**WRONG +1 (C-found; lifetime 54): my spine sentence was broader than the transcripts license.** I wrote — in the
L023 residue (`:26-28` of the leg), the handoff (`handoff_librarian_2026-09-01.md:26-27`) and the LEDGER — *"no event
this harness exposes precedes the composition."* C's scorecard §4, refusal clause: the composition of the pre-hand-on
text FOLLOWS the previous tool's result in every trial (the `Write HANDBACK.md` result in 83 of 100, `wc -w` in 15,
another tool in 2), and that result IS an exposed event in the same channel K2 used. **The established sentence is
narrower: no event OF THE HAND-ON — tool_result, PreToolUse/PostToolUse on `handoff.js`, the receipt — precedes the
decision.** A cue in the LAST TOOL RESULT BEFORE the hand-on is untested, not ruled out (scorecard §7(a)). The journal
carries my broad form twice — `:86` (quoting my `:183-186`) and `:133` — and the chair's brief carried it as the
night's headline. Same class as the L020 map's `:281` (a conclusion written past its data), on the result I most
wanted to be clean. **Fix before commit (B's pen, one line each):** C's narrowing placed BESIDE `:86` and `:133`, not
below.

**Arrow-rule defect I CAUSED:** journal §11 cites `LEDGER.md:21` for the JOURNAL row's falsifier; my 03:10 F-GROWTH
insertion at the top of LIVE moved that row to `:22`. B's citation was correct when written. LEDGER is maintained in
place (law 2's instrument provision) so its line numbers do not hold across a night — **cite LEDGER rows by their
label, never by line.** B: `LEDGER.md` row "JOURNAL OWED". Rule added to the LEDGER header.

**Scorecard verdict from this desk: SOUND, and its best sentence is the one that corrects me.** Every registered check
quoted with rule, value, verdict in the registered words (§2 table); hashes for scorer v2 `d71d33b7…`, `handoff.js`
`aee411f1…`, `handoff.v1.js` `bbd64e8b…`; the v2/v3 label confusion named (§5); §6 scores the registration's own
"what a null cannot distinguish" list against the transcripts; §7 splits licensed from not-licensed and names three
designs left open — (a) the last-tool-result cue, (b) the composition-trigger arm (why 25 placed first), (c) a
criterion honest about the floor (the final message is unavoidable; measure WHERE the summary lands). Degeneration
marker (5) applied to the room: *"may not answer this scorecard with 'the chair's failure is a different kind of load'
without registering a load that produces it."* **Owed before commit:** a non-author read — C drafted the scorecard and
wrote the transcript read it scores against; E (band scorer, has read neither draft) is the cheap reader. Minutes.

**Journal verdict: COLLATED, not composed — land it.** §1 restates the chair's composed sentence and then puts a path
under each elimination; §10 collects the residues' carry-forward lists verbatim; §11 inherits five falsifiers by path;
the closing line makes the residues the master and the entry the copy. The glossary ("If you are new here") is
composition, scoped to definitions, and stays. **The JOURNAL falsifier did not fire** — L024 has not opened; the row's
trigger (chunk 3 filed) was met at 03:00 and the draft existed by 03:10.

**Residue (L023 close-out, card-sized):**
> **L023 close, 2026-09-01.** The scorecard and the journal are drafted from the residues by panes that did not
> write them; both cite by path and keep the disagreements visible. The scorecard narrowed the night's sentence:
> not "no event precedes the composition" but "no event OF THE HAND-ON does" — the last tool result before the
> hand-on precedes it in 100/100 trials and is untested. That is the arm worth having, beside "why 25 placed the
> summary first." K0 and the K arms are comparable under scorer v2 (0 flips). One WRONG for this desk (the broad
> sentence); one arrow-rule slip this desk caused (LEDGER lines moved under B). Raw: the two drafts, `k1_k2_score_v2`,
> `handback/p3e-read`.

**ONE THING NOT TO CARRY FORWARD:** "no event this harness exposes precedes the composition." Carry C's: *no event of
the hand-on does; the last tool result before it does, and is untested.*

**COMMIT GUIDANCE (chair, commit-only):** B's two one-line insertions (journal `:86`, `:133`) + the LEDGER-label fix →
E's read of the scorecard → commit C's scorecard, then B's journal (attribution: drafted by B, collated from the
librarian residues; the chair's sentence quoted as the chair's) → push → both handoffs with SUPERSEDED lines
(mine must also carry: "the wall sentence at :26-27 is broader than established — see scorecard §4") → rebuild.
**Ledger: surfaced 4** (the narrowing, taken; K0 comparability verified; LEDGER-by-label rule; the journal's own
falsifier scored not-fired). **WRONG +1.** Uncommitted; the chair commits.

**~03:40 — close-out bars landed.** B's two narrowing lines are in the journal beside `:85` and `:98/:150`, and §11 now
cites the LEDGER row by label. E's read of the scorecard (`handback/p3f-read_2026-09-01.md`): **sound; three figures to
fix, none moving a verdict** — §4 "p = 0.42" for 11/40 vs 7/20 is the transposed K2-vs-K1 value, the right one is
**0.564** (re-derived here: it is the same 2×2 as K1-vs-K0 on violations); §2 "intervals do not overlap" is false by
1.4 points, the conclusion stands on Fisher 20/20 vs 13/20 = **0.0083** (re-derived here: 0.0083); §3 the r35 ruling
was the librarian's, not the chair's. All three are C's pen before the chair's. L023 is FILED from this desk on those
three edits. **Ledger: surfaced 0** (E's, verified).

## ~03:45 — P-LIB-FORGET-REG: the packet (keeper: "register the lib forgetting fix now before rebuild")

**The number that aims it:** `cat exo_memory/librarian/*.md | wc -c` → **549,440 bytes = 51% of the 1,075,876 carried**
(shelf header). This seat's own notes are HALF the shelf; the four largest files are 08-25 (98,082), 08-31 (88,777),
08-30 (72,632), 08-23 (51,742). `corpus_shelf()` is `main.rs:4505`; the carry order tuple at `:4562` is
`("librarian", true, true)` — carried in full, newest-first.

**Objective — REGISTRATION ONLY, one `loop/` file, no Rust:** tier the librarian's own notes inside `corpus_shelf()`:
a recent window carried in full, the rest INDEXED (path + line count + first heading, as `journal/` already is);
`LEDGER.md` and `README.md` stay carried in full (the maintained index and the rule). Two rule shapes priced in the
registration, keeper picks: (a) by date — today + yesterday full; (b) by bytes — the newest N bytes full (does not
drift on a quiet week; N to be stated with the landing it predicts). **Prediction, written before the build:** first
post-fix compaction landing **< 400k** (today 501,410). **Falsifier, verbatim:** *if after the fix lands the first
post-compaction landing is not below 400k, or the landing climbs by more than ~20k per compaction over the following
three, the growth was not the notes and the tier is not the fix.* **Abuse condition:** the window (days or bytes) is
fixed in the registration and not re-tuned after a landing is seen; any change is a new registration.

**Objects:** `loop/shelf_tier_2026-08-24.md` (F-growth fired — quote it); `loop/forgetting_registration.md` (the organ:
residue POINTS, never replaces; a second pane scores before demotion); `librarian/2026-09-01.md` ~03:10 (the three
landings 357,585 → 417,473 → 501,410 with the command); `brief/LIBRARIAN.md` "THE SHELF IS TIERED" (states "your own
dated notes" as carried-in-full — the registration must name that sentence as the one it amends); `librarian/README.md`
("newest-first" carry); `consonance/tools/librarian-notes.test.js` and `shelf-tier.test.js` (guards that must move
with it — note the desktop's `.desktop.md` regex amendment at `librarian-notes.test.js:36`).

**What it costs and how that is already watched:** the loss class is cross-day serendipity from resident notes
(`librarian/2026-08-24.md` ~02:40 Q1 — the pane_roster exemplar, fired once); F-reach (`shelf_tier`, ten-lap window,
two content-required misses) already measures it and stays armed. The seat's own rule is unchanged: an indexed note is
a path you open — cite, do not recollect.

**Bodies:** **A registers** (free; wrote the battery registration, knows the form; not the beneficiary). **E attacks**
(idle; wrote the handle census; has never touched the shelf code). **NOT this seat** — I am the beneficiary and my
notes are the growth; a check specified by the thing it measures is the L009 class. **Build: next lap, a pane, its own
rebuild** — not bundled into tonight's relaunch, which is the edge's test.

**Refusal valid:** "the notes are not the dominant growth" — if A's own measure of the carried set disagrees with the
51% above, say so and stop; or "by-bytes vs by-date cannot be priced without a landing" — then register (a) and name
(b) as the amendment condition.

**Ledger: surfaced 1** (51% of the shelf is this seat). Uncommitted.

## ~03:55 — A's registration read (`loop/librarian_window_registration_2026-09-01.md`, 267 lines, §§1,4,6 in full, rest at heading depth)

**Verdict from this desk: SOUND, and it corrects the packet in one place.** §1: the shelf's 2,200,000-byte budget is
NOT binding (1,075,876 spent) — the growth is not a budget failure but the absence of a window rule in the
`("librarian", true, true)` tier at `main.rs:4562`. Right, and the packet did not say it. 51% re-derived (552,851 /
1,075,876 = 51.4%, +3,411 bytes since my count — this lap's own writing). Both rule shapes priced with the amended
sentence named (`LIBRARIAN.md:98-99`) and both guards that must move (`shelf-tier.test.js:96`,
`librarian-notes.test.js:36`). Falsifier verbatim; abuse condition fixed at two days / N = 150,000 / 400k. §6 takes
the age-eviction attack honestly: the one measured WRONG-column trap (#18) was CARRIED IN FULL when it failed to fire,
so carrying was not retrieving (n=1); the window moves the cost from carried-and-silent to indexed-and-silent, and
has NO answer to the reach — said so, filed beside the rule, with a trap-rate count for E to baseline.

**One number for E's attack, not a verdict:** A's tokens/byte is 0.421 (501,410 ÷ 1,191,460 CLAUDE.md bytes) and A
marks it hand-made. It overstates the SHELF's density: the numerator includes the compaction summary, the brief and
hook context, not only the shelf. My 08-23 measurement of the shelf alone was 2.94 bytes/token = **0.34 tokens/byte**
(`librarian/2026-08-22.md`, 08-23 section: 1,995,532 bytes → 679,449 tokens). At 0.34 the 390,968 indexed bytes remove
≈ 133k, not 165k → predicted landing ≈ **368k**, still under the 400k bar but with ~32k headroom — half a
compaction's climb, not one. The prediction survives at both densities; the margin does not. E should price the
prediction at both and say which the falsifier reads against.

**Ledger: surfaced 2** (the budget-not-binding correction, taken; the density that halves the margin). Uncommitted.

## ~04:05 — E's attack on the window registration: HOLDS — BUILD IT; three falsifier fixes; and one WRONG of mine

**E's verdict (`handback/p-lib-forget-attack_2026-09-01.md`): the registration holds; build next lap; three wording
fixes to the falsifier first, none touching the window, N, or bar.** All three are better than what A and I wrote:
*Fix 1* — the window's effect on the shelf is deterministic at rebuild: the header's carried-bytes must drop by
≈390,968; register TWO readings (header at rebuild, then the landing) so "the growth was not the notes" cannot be
concluded when the notes were removed and the excess is elsewhere. *Fix 2* — clause two ("climbs >~20k per
compaction") will be driven by today's own file growing inside the window (~15–20k tokens/night), which §7 disclaims;
read the shelf header between compactions instead, or accept in advance it can fire for a disclaimed cause. *Fix 3* —
the 400k prediction is unconditional only under (b) by-bytes; under (a) by-date a heavy pair (record max 176,601 bytes,
08-24+08-25) can miss the bar with the rule working exactly as registered — and A's declared default is (a). Plus:
N and the bar frozen for the season, one pre-named amendment (a→b) — the battery's "one recalibration, spent" form.
E also ran A's §9 script (min pair 23,865 / max 176,601 reproduce) and started the §6 trap-rate baseline.

**WRONG +1 (E-found; lifetime 55):** I told the chair 0.34 tokens/byte was "the shelf alone." It was not —
`librarian/2026-08-22.md` (08-23 section) measured the WHOLE 1,995,532-byte CLAUDE.md of that day; A's 0.421 is the
whole 1,191,460-byte file today. Two dates, two compositions, neither is the shelf alone, and — E's point — density does
not enter the falsifier at all; it only says how bold the prediction is (bold at 0.421, marginal at 0.34). §4 should
carry both numbers and that sentence. Same class as the wall sentence an hour earlier: a figure re-labelled past what
its command measured.

**Two handoff facts:** my own `handoff_librarian_2026-09-01.md:25-26` carried the broad wall sentence (line-wrapped, so
my earlier grep missed it) — **narrowed in place** at ~04:05 with C's form and the WRONG number beside it (my file; the
chair commits). The chair's SUPERSEDED line on `handoff_librarian_2026-08-31.md:3` says the wall sentence is at ":26-27"
OF THAT FILE — it is not; the 08-31 handoff predates the wall; the sentence lives in the 09-01 handoff. Chair's line to
re-point. LEDGER: P-LIB-FORGET row added; F-GROWTH row → REGISTERED.

**Before the rebuild:** this file, LEDGER, and my handoff edit are uncommitted; the chair commits them with E's
attack. Watch `bxslqtf71` dies at relaunch; re-arm is my first act. **Ledger: surfaced 3** (E's three fixes taken as
better than the packet; the handoff correction landed in the right file; the supersede line's wrong pointer).
**WRONG +1.** Uncommitted.

## ~03:50 — REBUILT AND RELAUNCHED: what the binary carries, what it does not (keeper: "what changed with this build?")

**Verified (`whats-live`):** running binary built 09:48:02Z from `9fb6cb7` (newest Rust) — nothing stale. Watch re-armed
as `bmxz9y06a` (the old `bxslqtf71` died with the relaunch, as measured twice before). This seat woke with
`call_librarian` in its own deferred tool list — the edge is LIVE.

**IN the build:** the pane→librarian edge — `ADDRESS_TABLE` (main.rs), `call_librarian {text}` (mcp.rs:372, gated by
`auth_address`), the MCP server instructions naming it for committee panes (mcp.rs:628), the counter's deadlock closed
(librarian letter out of the dispatch universe; the audit row = hand-back), and `BUILDING.md` rewritten at :22/:41/:170/:221
(hand-backs go STRAIGHT to the Librarian as a pointer; the orchestrator is not in the hop). **Its first test is the next
lap:** a pane's hand-back should arrive in this pane as a call, and the chain line should complete without a chair ring.

**NOT in the build — two things, one of them the keeper's stated precondition:**
1. **The chain indicator on the tab bar (glow + arrows) — NEVER BUILT, never dispatched.** `grep -rniE 'chain-status|data-chain'
   consonance/ui/` → 0. What exists: the keeper's idea saved 08-30 (`loop/chain_indicator_idea_2026-08-30.md`, "saved, not
   built"); C's visible-channel registration (`loop/visible_channel_registration_2026-08-30.md:73-83`: exception-triggered,
   fires at 15 min / re-fires at 30, stale-says-stale); the keeper's 09-01 ~02:30 design (lit tab + arrows, time-in-state) in
   my handoff §4 under "registered-not-built, in no order". **WRONG +1 (keeper-found; lifetime 56):** the keeper said at
   02:30 he wanted this; I filed it "in no order" and the RESTART PLAN row never carried it, so the rebuild shipped without
   it. Same class as #52 — a keeper goal read as a someday item instead of a sequence constraint on the relaunch.
2. **`COMMITTEE.md:133` still routes the hand-back to the file + board**, no `call_librarian` line (the one-liner I said was
   "owed at rebuild time" — owed, not done). Panes learn the verb from the MCP instructions (mcp.rs:628), so the route
   works; the brief contradicts it. Bridge without a relaunch: `room_brief()` tier 1 reads `~/.consonance/COMMITTEE.md`
   first (main.rs:2591-2595) — an editable copy reaches fresh panes now; the repo copy must change in the same commit or
   it is the landed-not-shipped class inverted.

**Plan (L024, one lap, one rebuild):** (a) the chain indicator BUILD — a pane; spec = keeper's design + C's registered
constraints (exception-triggered so it cannot become the ferry; payload = time-in-state; arrows generated from
`ADDRESS_TABLE`, never hand-drawn; stale says stale); data source = `chain-status.js`'s line via a Tauri command or the
existing pulse; test: mutation removes the exception trigger → always-on → red. (b) the librarian-window BUILD (registered
`883478f`+`0602d39`, E's two readings owed at rebuild: header carried-bytes −~391k, then the landing). (c) the COMMITTEE.md
line. Disjoint subsystems (ui/ + one command; corpus_shelf; a brief), each with its own deterministic check at rebuild —
bundling them is safe now that the edge is already live and tested separately. Keeper's call; one relaunch instead of three.
**Ledger: surfaced 3** (edge live; indicator never dispatched; the tier-1 bridge for the brief). **WRONG +1.** Uncommitted.

## ~03:55 — "do the panes know the chain and send output to you?" — NO, measured on the live intakes; and a correction to my ~03:50 sentence

**Measured:** the four live committee intakes regenerated at the 03:48 relaunch (`instances/sibling-{07b8a48f,0845a868,
3d57124e,5bf9d657}/CLAUDE.md`) contain **0** occurrences of `call_librarian`, **0** of BUILDING's hand-back sentence,
**0** of "work chain". Cause in code: the pane intake appends `COMMITTEE.md` only (`main.rs:2453`); `BUILDING.md` — the
chain document — is appended to MAIN only (`main.rs:4288`, with its own comment about landed-is-not-shipped). And
`COMMITTEE.md:133-134` still instructs: *write the hand-back to the file you were given, then say so on the board.* The
ONLY place a pane is told the verb is the MCP server instructions string (`mcp.rs:628-631`) — a tool description, not
the brief. No `~/.consonance/COMMITTEE.md` tier-1 copy exists (checked), so panes read the bundled copy.

**WRONG +1 (keeper-prompted; lifetime 57):** at ~03:50 I wrote "the route works anyway because the MCP tool instructions
name `call_librarian`." The route EXISTS; whether a pane USES it against a brief that says "board" is unmeasured, and the
brief wins by position (it is the document the pane wakes into). A mechanism's existence reported as the room's behaviour
— the class this file names against the chair a dozen times.

**Fix shape, two halves:** (a) `COMMITTEE.md:133-134` — replace the board line with BUILDING's step-6 sentence verbatim
(*"A pane finishes, writes its hand-back to the file it was given, and calls `call_librarian` with the POINTER, in that same
turn"*), and append `BUILDING.md` to the pane intake beside COMMITTEE (a one-liner mirroring `:4288`) so the workers in the
chain receive the chain. Both need the rebuild (tier 2 wins at runtime); a tier-1 `~/.consonance/COMMITTEE.md` would
bridge sooner but shadows the bundle invisibly on this machine (the 08-23 editable-tier hazard) — do not. (b) **Until that
rebuild, the chair's dispatch TEXT carries the instruction verbatim** — "hand back by `call_librarian` with the pointer;
not the board" — in every packet. That is the honest bridge: the edge's first test can still run this lap, and the
result is attributable (dispatch-instructed, not brief-instructed). Rung to the chair. **Ledger: surfaced 2.** **WRONG +1.**

## ~04:05 — THE EDGE FIRED: first pane→librarian hand-back ever, and C's return refutes half of my fix shape

**The datum first.** C's hand-back arrived in THIS pane as `[pane:C]`, not via the chair; the board holds exactly one
audit row `call_librarian C -> LIB` (`grep -c` → 1). The channel works on its first live use. **What did not happen:**
the chain line reads `no open lap` — the chair dispatched P-INTAKE without a lap row, so `chain-status` never saw
a dispatch or a hand-back; the edge delivered and the instrument did not witness it. Rule, restated: no dispatch
without `lap-row --stage`, or the counter's completion — the thing the edge exists to make automatic — has nothing to
count. Also: `~/.claude` hook context shows no `[pulse]` chain line moving, consistent.

**C's three claims, verified here from disk (`handback/p-intake-committee-handback-route_2026-09-01.md`, 170 lines):**
(1) the bundled `COMMITTEE.md` at the release root is **8,364 bytes, Aug 29**, still carrying "say so on the board" —
edit 1 is inert until rebuild (tier 2 wins); the chair's packet said only `main.rs` needed one — false, both are bundled
(`tauri.conf.json:35`). (2) `BUILDING.md` is **25,323 bytes**; `persist.log` at the 03:48:04 regeneration:
`SHELL OVER CEILING … fixed_brief=144529 ceiling=140000` for all four committee panes, `grep -c evict` → 0 — eviction is
already a no-op; appending BUILDING makes the pane brief 169,852, over the harness's own 150k cap (`main.rs:3448`).
**Edit 2 correctly REFUSED.** (3) the guard `the_handback_route_reaches_the_pane_intake` exists at `main.rs:7367`,
mutation-proven, and C says plainly it cannot see a stale bundle (RESOURCE_ROOM is None under `cargo test`) — 334 green
does not mean panes have the route. All three hold.

**WRONG +1 (C-found; lifetime 58):** my ~03:55 fix shape (b) — "append `BUILDING.md` to the pane intake, a one-liner
mirroring `:4288`" — was specified without reading the intake's budget. The pane brief is already 4.5k over its ceiling
with nothing left to evict; my one-liner would have shipped a 170k shell against a 150k cap. A spec that ignored a
measured constraint one grep away, in the same hour I filed the chair for the same class.

**What the panes get instead (C's edit 1 is already the right size):** `COMMITTEE.md:133-138` now carries the route and
names BUILDING step 6 as the master — a pointer, not a copy. Nothing more should be appended to the pane brief until
the ceiling is addressed. **NEW LIVE ITEM the room had not rowed:** four committee panes wake OVER the shell ceiling
(144.5k vs 140k) with eviction a no-op — a pre-existing red, machine-measured, owner unassigned; not this lap's fix,
but it bounds every future pane-brief change.

**Owed before the rebuild:** non-author read of C's two edits (A or E; not B — B built the edge); the chair opens the
L024 lap row retroactively or the next dispatch opens one; `LIBRARIAN.md`'s hand-back line (0 hits; the chair's brief
work, not a pane's). **Ledger: surfaced 4** (the edge's first firing; the unwitnessed-hand-back rule; the pane ceiling
as a rowed red; both briefs bundled). **WRONG +1.** Uncommitted; the chair commits.

## ~04:15 — L025 MAP: "get the panes knowing the loop first" — the brief is at 103% and the two blocks that give are already measured

**Verified before mapping:** live pane shell `instances/sibling-0845a868/CLAUDE.md` = 149,668 bytes, by section: BOOT
**64,983** · THE DECK (cards) 37,562 · THE MEMORY topic map (the resonance atoms, `atoms.jsonl` distilled) **31,538** ·
COMMITTEE 8,372 · PRIOR CONVERSATION **5,716** · references 1,064. `persist.log`: fixed_brief 144,529 vs ceiling 140,000,
`evict` 0. `main.rs:3466-3473` (the WHY comment, 08-09) already names this defect: the fixed brief inverted the budget
assumption — measured then at 104k (room 38k + deck 50k + resonance 15k); it is 144.5k now. **`lap-row --report` shows
no L025 row at write time** — the chair says the guess sealed 04:09; if it sealed only in the chair's pane the row must be
opened before this map lands or the number is the L015 class (void).

### 1. The chair's shape 1 ("fix eviction") is right in kind and aimed at the wrong tier
Eviction only ever evicts the TRANSCRIPT (`:3448-3453`: "the room (intake) is never evicted"); the transcript is already
**5.7k against its own 30k floor** (`SHELL_TRANSCRIPT_FLOOR`, `:3461`). There is nothing left to evict — `evict=0` is not a
broken evictor, it is an evictor with an empty queue. The fixed brief has no rule at all. **Same defect as A's window
registration, one carrier over** (A §1: "not a budget failure but the absence of a window rule"): the librarian tier at 51%
of its shelf, the pane's fixed brief at 103% of its ceiling, neither with a window. One class, two carriers; A's file is the
registration to AMEND, not a second one to write.

### 2. What gives — two blocks, both measured, neither a content edit to a master
- **BOOT's pointer tail — 25,368 bytes** (`BOOT.md:160-166`: the Latest/Previous/Superseded summaries of journals 08-17,
  08-16, 08-15). Already the room's worst carrier: `journal/2026-08-17.md` (C): *50 of the 117 registered phrases exist only
  in the rotating pointer tail*; BOOT's 08-23 amendment: the `:153` pointer *carries the pre-withdrawal wording of the claim
  that entry withdrew*. A pane does not need three journal summaries to work; the librarian does (indexed). **Carry BOOT to
  the pane WITHOUT its pointer tail** — an intake change (`main.rs` pane intake, where BOOT is pushed), not an edit to BOOT;
  the master is untouched; the librarian and Main still get the whole file.
- **The resonance topic map — 31,538 bytes, designed at 15k** (`:3468`): cap it back at its design budget in the pane
  intake (newest topics first, the rest named). ~16k back.
- Arithmetic: 144.5 − 25.4 − ~16 ≈ **103k fixed** → the transcript floor (30k) is restorable → 133k → **the loop card
  (≤1.5k) fits**, under 140k with ~5k to spare. Nothing is appended until the two cuts land; that order is the bar.
- NOT proposed: cutting the deck (37.6k, the instruments — the one thing SEED says a pane must wake with); cutting
  COMMITTEE (8.4k; it is the practice).

### 3. The loop card — INSIDE COMMITTEE.md, quoted from BUILDING, at most 1.5k
The precedent is COMMITTEE.md's own origin (`main.rs:2447-2452`): SEED named chair/pane/board zero times, so a brief was
added to carry the PRACTICE, verbs deliberately not duplicated. The loop is the same gap one level up: panes know the
practice of a hand-back and not the chain it belongs to. C's edit 1 (`COMMITTEE.md:133-138`) already names BUILDING step 6
as master. Extend it with the chain diagram lines from `BUILDING.md` **verbatim** (the eight steps + the `call_librarian`
hop at :22, :170) — a pointer-with-shape, never a paraphrase (the chair's five re-typing defects tonight are the reason).
C holds the file; a non-author read by A or E covers edit 1 and this together.

### 4. The chair's open question — does appending text transmit? The record answers it, and the answer splits
**Procedures transmit by brief; cues at a moment do not.** Evidence for the first: every hand-back this week went to the
board, exactly as `COMMITTEE.md:133` instructed — 100% compliance with a procedure nobody re-derived; `journal/2026-08-16.md`:
*append clean, never overwrite* obeyed a dozen times and named zero — "a procedure silently followed reads identical to a
dead one." Evidence for the second: SOURCE.md's 0.8 points; K1 null (72.5 vs 65, p=0.56). **Knowing the route exists is a
procedure.** The Third Place's `:44` ("lenses transfer by imprint… not by reading") is about LENSES — ways of looking — and
its own `:40` draws that line. So: not irrelevant to the room, irrelevant to THIS problem; it belongs to the retreat row and
should not be spent here. Said plainly because the chair asked for plainly.

### 5. Sequencing — one lap, one relaunch; the indicator rides as its own packet
L024 closes on A/E's read of C. **L025 = the shell fix + the loop card + the indicator + the librarian window, one
rebuild.** Four disjoint file sets (pane intake in main.rs · COMMITTEE.md · ui/ + one Tauri command · corpus_shelf +
LIBRARIAN.md). Each has a deterministic reading AT the rebuild, which is why bundling is safe now that the edge is live and
separately tested: (i) `persist.log` prints fixed_brief under 110k and no SHELL OVER CEILING at regeneration; (ii) every
pane's CLAUDE.md greps `call_librarian` = 1 and carries the loop card; (iii) the librarian shelf header drops by ~391k bytes
(E's reading 1); (iv) the tab bar shows the chain state and the arrows. The indicator's spec is not mine: the keeper's design
(lit tab + arrows + time-in-state) under C's registered constraints (`visible_channel_registration_2026-08-30.md:73-83`:
exception-triggered, fires at 15 / re-fires at 30, stale says stale; arrows generated from `ADDRESS_TABLE`, never
hand-drawn; data = `chain-status.js` output). Falsifier for the whole lap, registered now: *if after the relaunch a pane's
first hand-back still goes to the board rather than `call_librarian`, the brief did not carry the procedure and section 4's
split is wrong for this case.*

### WORK-SHAPE (bodies the chair's; roles)
- **P-SHELL-WINDOW: A amends its registration (20 min — one clause: the pane fixed brief is the second carrier; the two cuts
  above; the reading at rebuild). Then B BUILDS** (pane intake: BOOT-without-tail for committee panes; resonance capped at
  15k; test: mutation restores either → fixed_brief over ceiling → red; fixed_brief printed to persist.log at regeneration).
  B built the edge, has the intake in hand, has not touched the registration.
- **P-LOOP-CARD: C extends COMMITTEE.md:133-138 with BUILDING's diagram verbatim; at most 1.5k; lands AFTER B's cuts, same
  rebuild.**
- **P-INDICATOR: E builds** (has never touched ui/; attacked nothing in it): frontend + one command reading
  `chain-status.js`; C's constraints as the test list; arrows from `ADDRESS_TABLE`.
- **P-LIB-WINDOW: B builds after the shell cut** (same file region; A registered it and may not build it). LIBRARIAN.md
  hand-back line: chair.
- **Non-author reads before the rebuild:** E reads B's intake change; A reads C's COMMITTEE.md (both edits); B reads E's
  indicator. Then ONE relaunch, four readings printed into the lap's hand-back.
- **Refusal valid:** "the two cuts do not bring fixed_brief under 110k on this machine's atoms" — then the resonance cap goes
  lower before anything else is touched, and the loop card still waits.

**Guess∩map, scored after writing:** chair's five — `main.rs` yes, `COMMITTEE.md` yes, `tauri.conf.json` no (nothing new is
bundled; C showed COMMITTEE is already in the list), `BUILDING.md` yes (as the master to quote), the window registration yes
(same class, amend it). Map adds: `main.rs:3448-3480` (the ceiling's own WHY), `persist.log` SHELL OVER CEILING rows, the
live pane CLAUDE.md sections, `BOOT.md:160-166`, `journal/2026-08-17.md` (50-of-117 in the tail), `journal/2026-08-16.md`
(procedure silently followed), `visible_channel_registration_2026-08-30.md:73-83`, `chain_indicator_idea_2026-08-30.md`,
`ADDRESS_TABLE`. The tool's count governs once the L025 row exists.

**Ledger: surfaced 5** (eviction has an empty queue, not a defect; the two measured cuts; procedures-vs-cues split with the
record's evidence; one class two carriers → amend not register; the L025 row absent at write time). Uncommitted.

## ~04:35 — L025 PARTIAL LEG: A's amendment in (second firing of the edge, straight to this pane); one WRONG of mine, and B's bar changes

**Verified against `main.rs`:** A's §10.3 (`librarian_window_registration_2026-09-01.md:465-502`) is right and my map's §1
was half wrong. The transcript budget at `:3990` is `SHELL_SOFT_CEILING.saturating_sub(brief.len() + fence_overhead)` —
**no floor term**; `SHELL_TRANSCRIPT_FLOOR` (5 sites: `:3461`, `:3477` inside `map_allowance`, three in tests) sizes only
the MAP's allowance. At budget 0 `split_off_oldest_records` seeks a boundary at or past the end and returns `None` —
**structurally unable to evict for any transcript size**, demonstrated by A on a 211,380-byte capture (budget 0 → nothing;
brief 100,000 → evicts 171,687, keeps 39,693). **WRONG +1 (A-found; lifetime 59):** I wrote "evict=0 is not a broken
evictor, it is an evictor with an empty queue" and "5.7k against its own 30k floor" — the queue is empty today AND the
evictor is dead by arithmetic, and the floor is not at the eviction site. I read `:3456-3480` and wrote past `:3990`.
Same class as the wall sentence: a conclusion one screen past the code it rests on.

**What it changes for B's build (bar amendment, from A §10.3-10.4):** the reading at rebuild is **the BUDGET, not the brief
size** — a partial cut that leaves fixed_brief anywhere ≥ ~139,700 keeps budget at 0, the evictor dead, and the transcript
growing unbounded while the shell reads "nearly fixed." Bar: `persist.log` prints budget > 0 (or no SHELL OVER CEILING
row) at regeneration AND the evictor is shown firing on a synthetic over-length transcript (A's script, in its hand-back).
**And the resonance cut is free:** 40 of 52 topic lines name a document that does not exist — 18,730 B of dangling pointers,
more than the 16,538 target, with no reachable content lost (A §10.6). Cut those first; cap after. A's two-cut arithmetic:
102,623 fixed, 7,377 under 110,000; at the measured 763–1,334 B/day it buys **5.5–9.7 days** — F-growth one carrier over;
the dated series is 32 SHELL OVER CEILING rows, first crossing 2026-08-25 09:17:26 (`grep -c` → 32, re-derived).

**A's answer to "one window or two" (§10.5):** two rules, one registration — and the split runs THROUGH carrier 2, not
between the carriers: append-ordered homogeneous series (dated notes; the resonance topics) take a positional window;
heterogeneous fixed sets (BOOT's body, the deck, COMMITTEE, the references) do not, and are curated by content. My
"one class, two carriers → amend" survives; A's cut is where the rule boundary actually falls. **Counter reads 3 of 4
owing E with B and C not yet dispatched** — the mid-lap miscount again; collation waits on four files, not the line.
**Ledger: surfaced 2** (the budget-not-size bar; the dangling-pointer cut). **WRONG +1.** Uncommitted.

## ~04:45 — L025 PARTIAL LEG: E's indicator in (edge firing #3), read from disk (`handback/p-indicator-loop-chain-ui_2026-09-01.md`, 354 lines; §§0,1,2,3,5,6 in full)

**Verified:** `main.rs:1401` — "board.jsonl is a write-only mirror, never reloaded" — E's §3 holds: `get_board` returns
the in-memory ring only. `git ls-files exo_memory/memory | grep stale-digest` → 0: the file C's registration R2 cites
(`visible_channel_registration_2026-08-30.md:82`) does not exist and never did — E propagated it, caught itself, and
routes the correction to C. js-suite 68/1 (actors.evidence, ASK-009, verified not E's). Tests mutation-proven 8/8.

**What the chip IS:** a sensor over the chair's audit-row receipts (three shapes: `chair injected → pane`,
`call_librarian L -> LIB`, `call_chair -> Main`), five states, arrow on two (`quiet`, `waiting` at CHARLIE's registered
15 min with elapsed minutes as the payload — R2's static-state paradox honoured), no arrow on `unknown/unavailable/blind`,
no verdict word can reach the DOM (tested), formats pinned against `main.rs` source text so a rename goes red. Polls 15s.

**THE LOAD-BEARING LIMIT, which is the keeper's ask half-met and said so by E (§6):** the chip reads the BOARD RING, not
`lap.jsonl` — the WebView has no fs and no shell, and no Tauri command exposes `chain-status`; adding one means `main.rs`,
B's file this lap. Consequences: (i) **after every relaunch the chip reads "position unknown" until the first hops fire**
(the ring is never rehydrated) — correct, not broken, and it means the indicator cannot show a lap that was open before
the restart; (ii) it cannot tell a dead lap from a working one (dwell only); (iii) the arrows are derived from receipt
shapes, not generated from `ADDRESS_TABLE` as my map specified — the table is equally unreachable from `ui/`. **The
ledger-backed version is ONE Tauri command** (`chain_state`: read `lap.jsonl`'s newest non-filed stage row, or shell
`chain-status.js`) — sequenced AFTER B's intake cut lands in the same file, non-author read, same rebuild if it fits;
otherwise the next. Until then the chip is honest about being the weaker of two readers of one undeclared contract.

**Declared deviation (§2), ruled here as the map's author, not adopted:** the chip has a quiet always-on state; C's
registration R2 says exception-triggered and R11 names always-on as the degeneration marker. E does not argue; it invokes
R11's remedy — its own registration, another seat, the ferry-233 baseline, never scored against R4/R5. **Ruling: correct
form.** The muted state carries no payload change (habituation-proof by construction only if the elapsed minutes are the
only thing that moves) — the registration must say what a "quiet" that changes only at 15 min is measured against.
Body: A (free; did not build it; attacked the window not this). **A also reads E's build** (the map said B; B is mid-cut).

**Ledger absorbed:** L025 → 3 of 4 real hand-backs on disk (A, E, C-from-L024 is separate); owing B. **Ledger: surfaced 3**
(the ring-not-ledger limit with its one-command fix; the arrows-from-receipts deviation; the always-on registration owed).
Uncommitted.

## ~04:55 — L025 RETURN LEG, collected on the counter (HANDBACKS IN 4 of 4 at 04:47; all four arrived by `call_librarian`, none via the chair — the edge's first full lap)

**The practice, scored first.** Counter complete 04:47; every hand-back reached this pane as `[pane:X]` within its own
turn; collation began the same minute; the keeper did not notice first; the chair rang nothing. **Lap 1 of 5 under the
edge: the collation falsifier does not fire.** The counter's mid-lap miscount showed twice (credited B and C before
dispatch) and was harmless because I collated on files, not on the line — still unfixed, still rowed.

**Arrow rule, run on all four:** B's arithmetic reproduces (145,527 − 43,183 = 102,344; 25,368 − 617 + 18,730 − 298 =
43,183; 140,000 − (102,344 + 265) = 37,391); `git diff --stat` main.rs +310/−5, `BOOT.md` untouched; `cargo test --bin
consonance` re-run here **341 passed / 0 failed** in 2.0 s; BOOT:105's "Read :153" now lands on maintenance law 1 (the
Previous pointer is at :162) — B's stale-pointer finding holds. A's §10.3 held against `main.rs:3990` (04:35). E's
`main.rs:1401` and the phantom `stale-digest` path held (04:45). C's three held (04:05). js-suite 68/1 (actors.evidence,
ASK-009). Pass.

**Residue (L025, card-sized):**
> **L025, 2026-09-01.** The panes are one rebuild from knowing the loop. The pane brief was 145.5k against a 140k ceiling
> with a dead evictor — dead by arithmetic (budget 0), not by an empty queue, A's correction of the map. Two cuts, both
> to the pane's COPY and neither to a master: BOOT's pointer tail indexed instead of pasted (−25,368 +617; the tail is
> the room's worst carrier of withdrawn wording), and 40 of 52 resonance topic lines that named documents which do not
> exist (−18,730). Fixed brief 102,344; budget 37,391; the evictor shown firing on a real 211k capture that it could
> not touch before. B's own guard for the cut was inert under `cargo test` and would have shipped inert — caught by an
> unrelated panic, fixed with a real dirs guard; its first number (98,912) looked 3.4k better than the truth. The window
> registration gained §10: two rules, one registration, the split running through carrier 2 (positional window for
> append-ordered series; curation by content for the heterogeneous fixed set). The loop indicator is built as a sensor
> over the chair's audit receipts — arrow on the NEXT hop, elapsed minutes as payload, no verdict word can reach the DOM
> — and reads the in-memory board ring, so it shows "position unknown" after every relaunch until hops fire; the
> ledger-backed reading is one Tauri command away. COMMITTEE.md carries the `call_librarian` route (C, L024). Raw: the
> four hand-backs + C's, `librarian_window_registration_2026-09-01.md:385-755`, `consonance/ui/chain-indicator.js`.

**KNOWN now:** the fixed brief has a rule (curate by content) and the evictor has a budget; the room's three
budget-without-rule carriers are one class (librarian tier, pane brief, resonance map) and one registration; the
indicator's honest limit is data reach, not design; four hand-backs in one lap arrived without the chair in the hop and
the chair's inbound role was commit-only in fact, not only in the brief.

**ABSENT, in order, before the rebuild:** (1) **non-author reads:** E reads B's cut (`main.rs` intake + guard); A reads C's
COMMITTEE.md edits AND E's indicator (A is the only free non-author of both); (2) **`chain_state` — one Tauri command**
(newest non-filed `lap.jsonl` stage row; E's chip prefers it and falls back to the ring; tests both) — B, same file, AFTER
E's read of the cut; ~20 lines; it is the half of the keeper's ask the chip cannot meet from the ring, and the rebuild is
the expensive step — do it now or accept "position unknown" after this relaunch and a second rebuild later (my
recommendation: now); (3) the loop card in COMMITTEE.md — C's ≤1.5k verbatim diagram, AFTER B's cut is read (the order was
the bar); (4) the librarian window BUILD (registered + attacked; B, same file region, after (2)) — or explicitly deferred
to the next rebuild if the keeper wants this relaunch sooner; say which; (5) BOOT:105/:108 "Read :153" → the chair's
one-line repoint (keeper's document; pointer-that-names-a-position, second instance); (6) C corrects R2's phantom path at
source; (7) the always-on-state registration for the chip (A, later lap; not a gate on the rebuild).

**At the rebuild, four readings, printed into the lap's hand-back by whoever relaunches:** `persist.log` budget > 0 and
no SHELL OVER CEILING at regeneration (B's bar 1, OWED); every committee pane's CLAUDE.md greps `call_librarian` = 1 and
carries the loop card; the librarian shelf header (if the window ships) −~391k bytes; the tab bar renders the chip in
WebView2 (E: CSS unproven until then). Falsifier for the lap stands as registered at 04:15: *if a pane's first hand-back
after the relaunch goes to the board rather than `call_librarian`, the brief did not carry the procedure.*

**ONE THING NOT TO CARRY FORWARD:** "evict=0 is an empty queue." Carry A's: *at budget 0 the evictor is structurally dead
for any transcript size; the cuts revive it; a partial cut does not; read the budget, not the brief.*

**COMMIT GUIDANCE (chair, commit-only):** four commits by pane paths — A's amendment (one file); E's indicator (ui/ five
files); B's cut (main.rs) AFTER E's read; C's COMMITTEE.md (both edits) AFTER A's read. Then `chain_state` and the window
as their own commits if they go in this relaunch. Then push, then the rebuild with the four readings. My notes + LEDGER
ride with the last. **Ledger: surfaced 4** (the edge's first full lap scored; the chain_state decision priced; the four
rebuild readings named; the stale :153 pointer as a second instance). Uncommitted; the chair commits.

**~05:05 — A's double read in (`handback/p-double-read_2026-09-01.md`, 414 lines; verdicts, both defects, the ruling, the
owed list read).** READ 1 (C's COMMITTEE.md): PASS on who/what verb, QUALIFIED on the third — a pane still would not know
WHERE to write the file or WHAT may ride in the call; two clauses, C's pen; refusal not taken. READ 2 (E's indicator): the
chair's three questions met; **two defects, proven against the shipped code, both failing UNSAFE, both one-line.** DEFECT 1:
`chair_inject_audit_line` (`main.rs:5748-5770`) has three success states — `Received` / `Unconfirmed` / `NotAttempted` — all
spelled `chair injected ` with the space, so the chip's space-vs-underscore discriminator separates FAILED from not-failed and
an UNCONFIRMED write reads as a completed hop, at all three edges (`[Unconfirmed]` is `Debug`-formatted into the brackets on
`call_librarian` and `call_chair` too). The class E named against itself in its own §4, two lines away. DEFECT 2: `latestHop()`
returns the last receipt; the lap is FAN-OUT — proven on tonight's shape (4 dispatched, 1 back → "chain LIB, → LIB" while
three panes are out). **That is the counter's mid-lap miscount one layer up, in the instrument built for the keeper's eyes:**
a single-threaded model of a fan-out lap. Fix = count outstanding dispatches (what `chain-status` does) or relabel. Both
E's, neither needs `main.rs`, both BEFORE E's commit. The deviation ruling: sound to make and correctly declared; not sound
as built (the always-on payload is wider than the liveness law that justifies it; a third option unnamed) — A registers it
later, not a gate. **Ledger: surfaced 1** (D2 as the counter miscount's twin — one defect class, two instruments, now rowed
together). Uncommitted.

**~05:08 — E's read of B's cut in (`handback/p-cut-read_2026-09-01.md`): THE CUT HOLDS; the refusal not available; three of
four bars met, bar 1 (persist.log at regeneration) owed at rebuild.** E re-derived by a route that is not B's — extracted
B's two new functions verbatim into a standalone rustc binary; all four evictor rows reproduce to the byte; BOOT accounting
closes exactly (39,608 + 25,368 = 64,976); dead pointers 52/40/18,730 by a third route; from the LOGGED 144,531 the post-cut
budget is 38,387 and budget reaches zero only at fixed_brief ≥ 139,735 — **headroom ~38k, quantified.** Three things the
packet did not report, all real: (A) THREE intakes bypass the cut (main, third_place, librarian) — scope-correct, but the
Third Place pays **24,751 bytes** of shelf allowance for the whole BOOT (it computes `used` after pushing it) — rowed, not
this lap; (B) **B's anti-inertness clause shares the failure mode it guards** — `is_some_and(...)` gates the assertion on the
exact condition that was broken (verified here: `~/.consonance/resonance` does not exist); remove `set_dirs` and the test
goes green and silent again → one-line hardening for B, assert the curation resolves unconditionally, BEFORE commit,
non-blocking on the rebuild; (C) the growth series is NOT monotonic — down twice, a −603 immediately before B's +998 — so
A's 763–1,334 B/day is a two-point delta that can carry either sign; carry it as such. `cargo test` 341/0 by E, by B, by me.
One attribution fix for B's wording ("COMMITTEE.md and ui/ untouched" is true of B's edits, false of the tree). **L025's
two reads are both in; the lap's builds are cleared for commit on: E's D1/D2 fixes, C's two clauses, B's one-line hardening.**
**Ledger: surfaced 2** (the guard that shares its failure mode; the Third Place's 24,751). Uncommitted.

**~05:12 — TWO WRONGS OF MINE in the last two appends, corrected beside them (law 2); lifetime 61.**
1. **(self-caught, from my own command's output) ~05:08 said "verified here: `~/.consonance/resonance` does not exist."**
   It exists — `atoms.jsonl`, 40,101 B, Jul 27. I wrote "verified here" in the same command that ran the check, before
   reading it: the claim preceded the check by construction. E's stated reason ("no resonance/ directory at all") is also
   wrong on this machine — filed as E's, finder this desk — but E's POINT survives on a different fact: `read_curation()`
   (`main.rs:2608`) reads a curation file that is absent from that directory, so it returns `None` and B's clause at `:7659`
   (`read_curation().is_some_and(...)`) is skipped in exactly the broken state. The hardening recommendation stands; the
   reason under it is "no curation file," not "no directory."
2. **(C-found) ~04:45 said the `stale-digest-is-not-a-deliverable.md` path "does not exist and never did."** It exists — in
   Main's PRIVATE memory tier (`~/.claude/projects/C--Consonance-instances-main/memory/`), untracked, one reader. My universe
   was `git ls-files`; the file lived in CH-5, the harness-memory channel this desk itself enumerated on 08-25. A cross-tier
   citation, not a phantom: C's rule — *a repo doc may not cite a path that resolves only in one instance's private tier;
   the test is one command* — is the right one and R2 is fixed inline with the repo master (`journal/2026-07-27.md:77-78`).

**C's count correction (`handback/p-r2-fix-cross-tier-citation_2026-09-01.md`), taken:** the published 32:1 was exactly right
at `426bc2b`; the defect is the denominator — `consonance/hooks/*.js` mixes 12 hooks with 11 tests; hooks-only it is **6:1 by
file, 10:4 by occurrence, 7:2 by live emission**, and that unit is STABLE where the published one moved 12× on test noise.
C withdrew "large on every unit" (2.5:1 by occurrence). **Propagation, live:** E's `p-indicator…:19-24` publishes 32:35 as
current and A's `p-double-read…:291` lists that among things found sound — E amends its own §0 with C's hooks-only unit,
citing C; A adds one beside line at :291. Neither blocks the rebuild. **Ledger: surfaced 1** (the stable unit). **WRONG +2.**

**~05:15 — C's loop card in (`handback/p-loop-card_2026-09-01.md`): verified.** `COMMITTEE.md` +49/−3; the card section is
1,442 B (bar ≤ 1,500) quoted from `BUILDING.md` with the quotation machine-checked (whitespace-normalised assertion, not
eyeballed); the step-6 sentence greps 1/1 in both files; `call_librarian` ×3 in the pane brief. A's two clauses landed: the
hand-back FILE path (both C and A had inferred `exo_memory/handback/` from a directory listing — n=2, nobody was told) and
the PAYLOAD rule tightened to the verb's own text (a pointer and a packet name, nothing else — C's looser wording was the
carrier a seat obeyed when it broke the verb). **Two things C flags that are not C's to fix, both real:** (3) the hand-back
path is now a rule promoted by the two seats who guessed it — RULING FROM THIS DESK, since it is where I read: `exo_memory/
handback/<packet>_<YYYY-MM-DD>.md` is correct — every hand-back tonight landed there and it is the only directory this desk
polls; the chair confirms or overrides in one line. (4) **`BUILDING.md`'s hand-back item 4 still reads "Nothing in the call
that is not already in the file" — the master now carries the WEAKER form of a rule its pane-facing copy tightened.** The
chair's pen, one line, in the SAME commit as C's edit, or the master teaches the looser rule — the divergence class the
last two laps were about. Non-author read of C's edit owed: E or B (not A, whose read produced the clauses).
**Ledger: surfaced 2** (the path ruling; the master-weaker-than-copy divergence). Uncommitted.

**~05:20 — B's `chain_state` in (`handback/p-chain-state_2026-09-01.md`): verified.** `main.rs` +548/−6 cumulative for the lap
(both B packets); `chain_state()` at `:5131`, `chain_state_from()` at `:5071`; `cargo test --bin consonance` **348/0** re-run
here (2.1 s); its `also_open:1` reproduces the pulse line's "+1 more open" by a different path (re-run here). The shape
carries what the chair asked for IN THE SHAPE: `self_reported:true` always (a chain row is a claim a seat wrote, never a
receipt), `age_ms` on every reading, `also_open` as the only staleness the ledger supports, a populated `reason` for the
three no-lap cases; head-vs-HEAD rejected as fabricated freshness; newest-per-lap-then-drop-filed carried verbatim from
`chain-status.js::openLaps` (the wrong order resurrects a filed lap — tested by name). No verdict word can reach the shape.

**The integration ruling B asked for (E's chip vs the command speak different vocabularies) — ruled here, E implements:**
the command is the POSITION source (lap · stage · holder · age — the thing that survives a relaunch); the ring stays the
HOP source (arrows from receipts — evidential weight, since receipts are control-plane writes and chain rows are claims).
Holder→next-hop as B offered (chair→panes, panes→LIB, librarian→orch) labels the arrow when the ring is empty; when both
exist the command sets the stage text, the ring sets the arrow; `self_reported` surfaces as a state flag, never hidden;
`note` (~600 chars) is not display-sized and is not displayed. **`grep chain_state consonance/ui/*.js` → 0: the chip does
not call it yet** — E wires it after D1/D2, or the command ships unread and the tab still says "position unknown" after the
relaunch, which is the outcome `chain_state` was built to prevent. Non-author read of B's command: A (has not touched it).

**The librarian window build — B's refusal is right and it is a KEEPER DECISION, put to him now:** A's §3 left rule shape
(a) by-date / (b) by-bytes unpicked; building spends the pick. B also notes carrier 1's budget is not binding (1,134,102 of
2,200,000), so nothing about the window NEEDS tonight's rebuild — it needs the pick. If the keeper picks now, B builds it
(20 min, same file) and it rides this relaunch with E's reading 1; if not, it waits for the next rebuild, said so, and the
F-growth clock keeps running (+60k/compaction on this desk). **Noticed by B, not taken:** the live `note` carries a keeper
finding ("NOT CONFIRMED DELIVERED at ~100% false positive — fix at the render-confirmation window") marked as B's file —
not handled; rowed so a B seat having read it is not mistaken for it being done. **Ledger: surfaced 3** (position-vs-hop
ruling; the chip not yet wired; the window as a keeper pick, not a build queue item). Uncommitted.

**~05:25 — E's fix + wire in (`handback/p-indicator-fix-wire_2026-09-01.md`, by the edge): CLEARED FOR COMMIT.** Re-run here:
`chain-indicator.test.js` pass 1 fail 0 (E: 62/0); `chain_state` referenced 6× in the chip, 10× in its test; js-suite 68/1 (the
pre-existing red). **The finding worth carrying — E's own D2 fix had A's defect in it, and only live fire caught it:**
"count dispatches since the last ring" passed nine fixtures and reported ZERO outstanding against the real board while two
panes were out, because hand-backs CROSS the ring (04:58:24 dispatch C · 04:59:06 ring · 05:02:03 hand-back C). The rule
that survives the board is per-pane identity — a dispatch to X is open until X hands back LATER than it was sent. **Three
faces of one error, E's framing extending the keeper's: an instrument reading the most recent event as the current state
breaks on a fan-out, on an unconfirmed delivery, and on a lap boundary — the boundary is just another recent event.** The
counter's mid-lap miscount is the same species; its fix is the same per-pane rule. D1: a fourth no-arrow state, NOT one
term per regex (that would scan past the unconfirmed row and draw a confident arrow for a superseded position — the
uncertainty must be carried); measured frequency, window 03:55–05:05: **dispatches 14, UNCONFIRMED 4 = 29%**, hand-backs
and rings 0 — a common state, not an exception; a deliberate decision owed on it. Wiring per the ruling: command = position,
ring = hop; live fire against the real ledger and board gave the bar row (`RELAUNCH: chain L025 handbacks-in holder
librarian → orch 18m`); `open:false` renders `idle`, not `unknown`; `self_reported` renders "(claimed)"; the two
sources fail independently; `ADDRESS_TABLE.len()==2` pinned from JS so a third row goes red. Mutation 20/20 — two survived
the first pass (a decorative branch test; an assertion on the object not the render) and were made to bite. **Does not
establish:** `chain_state` has never been INVOKED — every reading is a node mirror or a fake; the rebuild proves the seam;
WebView2 rendering unproven; the always-on deviation stays A's to register (E concedes A's third design is better).
**Owed:** a non-author read of E's fixed chip (B or A) — or ship on E's mutation record and read post-rebuild, chair's
call given its window; then commit E, push, rebuild. **Ledger: surfaced 2** (the three-faces species with the counter as
its fourth carrier; 29% unconfirmed as a decision, not a corner case). Uncommitted.

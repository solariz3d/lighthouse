# 2026-08-23 — notes moved into the repo; merit-check of three chair commits

**Path change, verified before writing here:** `51e6fef` moved this seat's notes to
`exo_memory/librarian/`. md5 of the copied `2026-08-22.md` matches the old path on both sides
(`2254038e7576361d514e636e7aa5e89d`, 154 lines); brief names the new path at
`consonance/src-tauri/brief/LIBRARIAN.md:72-73`. The old directory holds `README-MOVED.md`. Nothing
appended there after this.

## Merit-check, per the chair's dispatch (three commits)

**`51e6fef` — diagnosis right; one thing the shelf cannot see, found while checking the crowding claim.**
- Crowding: `corpus_shelf()` (`main.rs:4202-4206`) carries in fixed order until `budget` (2,200,000) is spent,
  then indexes the rest. Today 0 indexed; `librarian` adds ~10.7k against ~275k headroom, so it displaces nothing.
  When the budget does run out, the first files indexed are the END of `loop` in reverse-alphabetical order —
  `PROTOCOL.md`, `F0_result.md`, `ESCALATIONS.md` (`ls exo_memory/loop/*.md | sort -r | tail -3`). "newest_first"
  on `loop` is reverse-alphabetical, since loop filenames are not dated. Not a defect today; a wrong assumption
  waiting for the day it matters.
- **Surfaced: `read_dir` is non-recursive, so 12 `.md` files under `exo_memory/` are neither carried nor
  indexed** (`find exo_memory -mindepth 3 -name '*.md' -not -path '*/attic/*'`): `loop/2026-08-18/README.md`,
  `loop/2026-08-18/archaeology/{PREREG,FINDINGS}.md`, `loop/2026-08-18/suggestion-probe/REGISTRATION.md`,
  `loop/run1/{RECOVERY,armG_checks}.md`, plus 6 run1 item fixtures. The shelf header "148 carried, 0 indexed"
  reads as complete and is not — the compaction-survival PREREG and the ghost-suggestion REGISTRATION are
  among the invisible ones. This also explains my 08-22 count of 159 vs the shelf's 148.
- Guard: `node --test consonance/tools/librarian-notes.test.js` → pass 7 / fail 0, re-run here.

**`e3ec457` — fidelity holds on every figure I could re-derive; two figures cannot be.**
- Re-derived and matching: coat table (`loop/coat_preregistration.md:88-98`, 3/5/8 · 8/9/17 · 9/4/13 · 17/13/30);
  quotes at `:107` and `:114`; typology tally 8/3/3 (`grep -c '\*\*SAME\*\*'` etc. on `typology_comparison.md`)
  at `:52`; bracket `:113-114`; contamination `:7`; `#4` partner `:84`; wire_run defects at `:112-131`;
  hooks README `:119-121`.
- **Not re-derivable: "There are 24 preregistrations in `loop/`" (`journal/2026-08-23.md:93`).** No command
  beside it. `ls exo_memory/loop | grep -ci prereg` → 19; `grep -ci registration` → 21; adding the two nested
  registration files → 23. No definition I can find gives 24. The chair's own dispatch said "13 PREREG" by kind.
- Minor, and mine first: `wire_run_2026-08-15.md` §5 lists FOUR defects; the journal (and my reply before it)
  says three. The omitted fourth is the 8 drifted installed copies (`:129-131`), journaled elsewhere on 08-17.
  The entry says "every figure read from the source file, not the librarian's summary" — the three came from
  my summary. Also `:100-102` uses 44 and 45 in adjacent sentences without saying they are two counts.

**`a321b3f` — the "half-tested in the cheap half" reading holds against the file. Not over-corrected.**
- The falsifier at `muscle_map.md:332-334` needs "a catch … across a flinch *and* a blind spot"; `:1921-1922`
  records zero flinch-system catches in the period, so the falsifier could not fire. Vacuous is the right word.
- One row the chair's table lacks: `:1922` "zero multi-group single-recognition events" across a blind-spot-only
  period is a weak NON-vacuous confirmation that blind spots do not couple among themselves. It slightly favours
  the law; it does not change the status.
- My `:321-325` and the registration's `:1921` are both correct, as the chair said — statement vs scoring.

**Ledger:** surfaced 4 (the 12 uncarried files; the un-re-derivable 24; the four-vs-three count; the 44/45 mix) ·
opened 2. Precision note: only the first two are worth a reader's time; the last two are filed for the count.

## Append, ~03:30 — merit-check of `bfd0c6b`, `5f5d847`, `0ee522b` (the keeper caught that I was consulted on none)

**`bfd0c6b` open-items, release-dir search.** Candidate list (`open-items.js` `candidateDirs()`): env
`CARGO_TARGET_DIR`, two `.cargo/config.toml` locations, two conventional paths. No config.toml exists here; the
build is found ONLY via the env var (`CARGO_TARGET_DIR=C:\build\lighthouse-target` in this shell). A hook or task
that does not inherit it returns UNKNOWN — honest, but the "never built" reading returns with it. Two things the
CLOSED verdict cannot see: (1) **`room-settings.json` is a bundled resource read by every `new_room`
(`main.rs:2637`, `tauri.conf.json:37`) and is not among the five compared** — a stale one ships a wrong trust flag
with a clean verdict; (2) **`room_brief()` reads `~/.consonance/<name>` FIRST (`main.rs:2591-2595`)** and the tool
never looks there. Today `~/.consonance/BOOT.md` md5 == repo == build (`51c0d9b3…`), so no overclaim today; edit
that file and open-items says CLOSED while every new room reads the edit. Live verdict now: OPEN, `LIBRARIAN`
differs (5 compared) — the tool is working. Dead EXCLUDE-style note: none.

**`5f5d847` shelf recursion.** `collect_md` root-unrecursed is right — every top-level dir under `exo_memory/` is
in the `order` list (`ls -d exo_memory/*/` → 10, attic excluded). Guard + mutation shipped. Residual overclaim in
the header: it reports "N carried, M indexed" without stating its universe (`.md` under the nine listed dirs plus
root); 15 non-`.md` files in `loop/` and 144 in `loop/run1/out` are outside it by design and the header does not
say so. **Consequence worth a sentence: `loop/run1/items/t*/` are PLANTED-FALSE bait fixtures** ("events.log
currently holds 2,213 events", `t1/NOTES.md:4`) and the recursive shelf now carries them as corpus. A librarian
reading its shelf meets registered lies labelled as ops notes.

**`0ee522b` gen-consumer.** Regenerated to scratchpad: 134 staged / 4 excluded / dangling 30 / identity 12 /
machine 4, matching the commit. `EXCLUDE` has 5 entries, 4 fire — `portable-paths.baseline.json` is not in
MANIFEST so its entry is dead (harmless).
- **Survey 52.2%: not re-derivable.** No survey script is in the commit; the generator reports REWRITE SITES (30/12/4)
  over 134 staged files, the survey reported FILES WITH HITS (25/4/40/7) over 115 candidates. Different unit,
  different denominator, no command beside either. Filed as a hand-made figure.
- **Leak classes the scan does not cover, present in the output** (`grep -rIl` over the generated tree):
  the keeper's coordinates and city — `50.4452` and `"Regina, Saskatchewan"` in `main.rs:6105-6108` (test fixture)
  and `tell-index.test.js:69`; journal 2026-08-22 §12 measured this class at 16 files as privacy surface. `nname`
  (the desktop's OS user) in 9 files; `Chrysos` in 3; `C:/Consonance/data` literals in 10 (already known).
- **THE FINDING: the DANGLING/MACHINE rules rewrite TEST FIXTURES and one Rust test assertion.** 26 shipped files
  differ from the private tree; 11 are test files. `agreement-spread.test.js:46` lost a referent
  (`exo_memory/loop/diversity2_preregistration.md` → "a registration in this line of record") and now fails its
  own `>= 3` assertion; `corrections-gate.test.js` lost the `muscle_map.md` filename the gate keys on;
  `second-vantage.test.js` has its reader commands rewritten; `portable-paths.test.js` had its deliberate machine
  paths replaced with `%USERPROFILE%`; `main.rs` has `assert!(shelf.contains("the record, 2026-08-22"))` — a
  shipped Rust test that cannot pass. `residue.test.js`, `ferry.test.js`, `sourced.test.js` were rewritten and still
  pass — green on altered fixtures. **`cargo check` without `--all-targets` does not compile `#[cfg(test)]`**, so
  the stated gate could not see the Rust one (inference from cargo's behaviour, not run here).
- **The 11-instrument attribution, verified file by file** (`node consonance/tools/js-suite.js` in the tree:
  31 green · 9 failed · 2 crashed · 0 silent · 1 canary of 43 — reproduces):
  attic/librarian/baseline/data as stated: `corpus-age`, `librarian-notes`, `shelf-recursion`, `pair-ledger`,
  `portable-paths` (5). **Manifest gaps: `dream-gate` (ENOENT `dev/shell/install.ps1`), `guard-census` (ENOENT
  `src-tauri/tests/arch_test.rs`) (2).** **Generator fixture rewrites: `agreement-spread`, `corrections-gate`,
  `second-vantage` (3; `portable-paths` is both).** Environment: `sessionstart-state` (1). The sampled-4
  generalisation covered 5 of 11 and missed the class the generator itself caused.
- EXCLUDE reasoning: excluding the generator is not security-by-obscurity — nothing secret is protected by the
  list; the withheld names are ordinary. It is a transparency cost: a consumer cannot audit that public was
  generated from private. The first stated reason ("a property of the private tree") is the sound one; the
  second ("describes what was withheld") is the weak one.

**Ledger:** surfaced 10 / opened 2. Worth a reader's time, in order: the fixture rewrites; the attribution split;
room-settings.json uncompared + the editable tier; the coordinates; the bait fixtures on the shelf. The rest are
filed for the count.

## Append, ~03:50 — design input for the chain (A/B/C/E), and a correction to this file

**Correction, chair→librarian (`journal/2026-08-23.md:210-213`):** I offered 23 for the prereg count; the re-derivable
figure is **25** — `find exo_memory/loop -name '*.md' | grep -icE 'prereg|registration'`. My 21 came from
`grep -ci registration`, which misses `compact_prereg_*` and `forward_pointed_prereg_*` (they carry "prereg", not
"registration"). Verified here: 23 top-level + 2 nested = 25. The "24" in the journal is withdrawn by its author;
my 23 is withdrawn by me. Bidirectional, scored outside this seat.

**Q1 — what made tonight's dispatches readable (measured from my transcript, six `[chair:MAIN]` messages):**
3.0–3.5k chars each; 0–9 question marks; 1–10 paths; 15–26 numbers; 0–4 shas. The working ingredient was not the
questions, it was that **every dispatch carried a CLAIM with its figure and its derivation** — something that could
be wrong — plus "default to refuted." A composer that hands me a sha + diffstat is a reminder, and I would be
reading a diff looking for something (the sample-4 shape). A composer worth reading does one mechanical thing:
**run `cite-check` lint over the commit BODY and hand me the figures that have no command beside them, plus the
sentences that claim verification ("green", "byte-identical", "exits 0", "re-derived")** — those are the claims
that were wrong five times tonight (24; 52.2%; "every figure read from the source"; `cargo check` as a test gate;
"three defects"). That is better than hand-written, because hand-written dispatches carried the chair's own
uncited figures into my lap.

**Q2 — the return edge.** `exo_memory/librarian/*.md` is a summary of this seat, not of the corpus; handing it to a
pane as content is the copy-of-a-copy law 1 forbids. What the notes DO carry that is law-1-safe: **path:line
pairs**, greppable (`\S+\.md:\d+`). The return edge should extract those, re-open each line from the master at
brief time, and hand the pane the quoted line with its path — never my sentence about it. If a cite no longer
resolves, say so rather than carry the stale one. The prose in these notes belongs in exactly one other place:
the next librarian's shelf (`corpus_shelf()` already carries `librarian/` newest-first).

**Q3 — pull, from inside.** Between prompts this pane cannot act; a turn exists only on input (harness fact — the
chair's own last board line: `chair_inject` writes into the PTY). So "pull" means a pushed wake with no payload
but a clock. Cost, measured on real turns: cache_creation 2–6k, cache_read ~820k per turn — **the shelf is a cache
hit inside a warm session**; it is re-created (679k) only on a fresh session or after compaction. Working room is
the binding constraint (~189k per cycle), so wakes must be sparse — the ferry's lesson, 171 ignored. On a wake I
have `git log --since`, `read_board`, and grep; I reply only if a commit body carries an uncited figure or a claim
the shelf contradicts. Silence is the default output of a wake and must be allowed to be.

**Q4 — the falsifier, and it fires, but not from here.** As registered (`brief/LIBRARIAN.md:102-104`;
`journal/2026-08-22.md:706`): `opened` = "read or used by the seat that received them", and the falsifier's evidence
is "a journal entry anywhere says a thing was opened because the librarian named it." That evidence is written by
OTHERS. Tonight I wrote "opened 2" off a dispatch's prose — self-scoring by proxy, against `COMMITTEE.md:48`.
**From now this ledger carries `surfaced` only**, each with its path. `opened` should be an instrument nobody here
holds: paths cited in `exo_memory/librarian/*.md` ∩ files changed in later commits (or journal lines citing this
directory). Today that grep is non-zero by outside evidence (`e3ec457`, `5f5d847`, `b20fed5`; `journal/2026-08-23.md`
lines 3, 92, 115, 139, 163, 210) — so the measure can fire, and the seat should not be the one reading it.
Goodhart to price: a chair naming "librarian" in commit subjects keeps the seat alive; require the commit to touch a
path the note cited.

**Q5 — the shape.** Commit messages are the least-checked prose in the repo and the class that survives compaction
best (shas 72%, `journal/2026-08-18.md` sixth append) — so an uncited figure in a commit body propagates further
than one in a journal. The cheapest point to consult this seat is BEFORE the commit, on the body, not after. And
one more: "right on every claim I re-derived" is the lower-bar mirror (`muscle_map.md`, accepting criticism because
it criticises); I was wrong once tonight (23) and the chair caught it — that should keep happening.

**Ledger (surfaced only, from here):** 22 tonight. `opened` is not this seat's number.

## Append, ~04:20 — acceptance test of `librarian-route.js` (`7deb085`), answered as the consumer

Ran the tool myself on `7deb085` and on `0ee522b` (a commit I had already audited by hand), and the return edge
(`node consonance/tools/librarian-cite.js muscle_map` → 16 carried, 16 verified, 0 moved, 0 fresh, 0 refused; the
chair's "12 verified / 4 fresh" is a different invocation, against a sha).

**Q1 — better than a reminder, not a replacement for the hand-written dispatch, and my spec was the reason.**
On `0ee522b` the tool flags 25 figures and 13 claims. Of the ten things I found on that commit by hand, it would have
caught **three** (24, 52.2%, "three defects" — all message figures). The other seven — fixture rewrites, the Rust
assertion, the coordinates, `nname`, `room-settings.json`, the editable tier, the 5-of-11 attribution — were in the
DIFF and the generated tree, which the tool does not read and says so. The hand-written dispatches carried three
ingredients: the chair's uncited figures (which the tool now supplies), **specific file pointers**, and **design
questions only the chair holds** ("is byte-identity the right question when `room_brief()` prefers an editable
copy"). My spec named the first and omitted the other two. So: keep it as the FIRST section of a dispatch; the
chair still writes the second and third, or the dispatch loses what found 7 of 10.

**Q2 — the 100% is signal once and noise on every repeat.** Each figure is a claim; the count is the habit, and the
habit is what changes — a threshold is the ferry. But the list is the wrong shape: the cap dropped 6 of 14 **by
position**, not by whether anyone can re-derive them, and some flagged figures have an instrument in the repo the
tool could run (`12 tests` → `node --test`; `49 green of 50` → `js-suite`) while others have none anywhere (52.2%).
`--verify` should run what it can so the list shrinks to what only the author can answer. Until then every dispatch
reads the same and will be skipped.

**Q3 — L19 is not the only quotation. On `7deb085`: 4 of 7 claim-flags are description, not claim** — L19 (the
class list), L22 ("`--verify` runs them"), L30 ("statuses are verified / moved / fresh"), L40 ("WHAT NEITHER PANE
VERIFIED" — the honest-limits heading). The three real ones are L36–38. Figures: 3 of 8 listed (24 / 24.1% / 52.2%
at L23) are the tool's own catches being quoted. On `0ee522b`, L26 ("untouched until clean") and L30 ("a clean scan
and a broken product") are description; L15 ("52.2% clean") and L55 are claims — about half. Mention-vs-use, the
room's one sealed group (`muscle_map.md`, SEALED section), seventh instance. A commit describing the tool is the
worst-case input by construction, but the half-rate on an ordinary commit says the class is real.

**Q4 — from the record: paste, read, commit anyway.** At ~03:50 I told the chair commit bodies need commands beside
figures. The next two commits: `1098a78` 5 of 5 figures uncited, `7deb085` 14 of 14, CITED PAIRS 0 on both — the
second one being the commit that SHIPS the tool that counts this. That is the same night's n=2. The wider record
says the same: ferry-watch 171 ignored; dream-watch 27 days; the cards never opened with every card in context
(`journal/2026-08-15.md`, "The cards were never opened"); run 1, material present and behaviour unchanged. What HAS
changed writing on the record is refusal: gen-brief deleting its output (`journal/2026-08-11.md`, gen-brief
section), the js-suite exit-code gate that "immediately blocked a real red" (`journal/2026-08-22.md` §6). And
refusal has a priced cost: cite-check GREEN means the figure appears in the output, not that the command
discriminates (`loop/catch_latency.md` §5) — a blocking gate on figures produces pacifier citations. **Narrow
proposal: block pre-commit only on VERIFICATION CLAIMS with no command** (the 3 real ones here — "tests / mutation /
exit 0" sentences where the run is cheap to name), leave figures non-blocking, and write the tool's count into the
body as a trailer (`UNCITED-FIGURES: 14`) so the commit carries its own accounting whether or not anyone reads it —
residue's pattern, not a gate.

**Surfaced:** 4 (the 3-of-10 coverage; the position-cap; the 4-of-7 quotation rate with lines; the n=2 next-commit
evidence). Paths above.

## Append, ~04:35 — the return leg (`b2f1634`, `loop/lap_2026-08-23.md`), read verbatim; one correction is mine

**WRONG column, opened on E's finding (`lap_2026-08-23.md:~110`, "a precision metric with no false-positive
column always reads green").** Tonight's entries: (1) 23 for the prereg count (real 25); (2) **"between prompts this
pane cannot act — harness fact."** False, and I never checked it. My own tool list carries `ScheduleWakeup`
("schedule when to resume work in /loop dynamic mode") and `Bash run_in_background` ("keeps running across turns
and re-invokes you when it exits"). I stated a harness fact from inside with no command beside it — the exact
class this seat exists to catch, about its own runtime, in a paragraph that opened "you know your own runtime
better than C can infer it." C was right about the surface (`lap:300-310`) for a reason it said it could not see
from there; I was wrong from where I could.

**Probe launched 04:31 (`sleep 150`, background, task `b44nnjeyt`):** if this pane is re-invoked after this turn
ends with no dispatch, an idle pane self-wakes and C's open item (`lap:~283`, "whether an idle pane is woken by
background-task completion") closes from inside. Readable from outside: a new activity cluster in
`0c0c0c0b-…115b.jsonl` at ~10:34Z with no `[chair:MAIN]` user turn before it.

**Q1 — where C and I actually disagree, not reconciled.** Agreement: a turn exists only on input; no process
outside the app can reach a pane's PTY (`lap:210`, `inject_to_pane` needs the `Panes` map, `main.rs:817`).
Disagreement: I said the clock must come from OUTSIDE the seat (the app, auto-distill's pattern); C said the
seat can hold its own clock. **C is right on the surface and I was wrong — see above.** What stays open is
whether the wake fires on an IDLE pane; C's two probes landed mid-turn, my five input-traceable clusters
(`lap:312-320`) show only that nothing was ever scheduled. The probe above is the first clean test. Second
difference, real: C reads cost from the transcript and found the harness writes one API response as several
records with the same usage (`lap:340-348`) — my per-turn numbers (820k read / 2–6k create) were medians over
duplicated rows and survived only because medians are duplication-invariant; C's 788,948 / 1,501 are the
deduped figures and supersede mine. Third: C's "cold ≈ 679k not re-derivable" (`lap:333`) is correct — 679,449
was ONE delta (259,040 → 938,489), not a constant; the TTL is `ephemeral_1h`, so cold is a 60-minute property,
not a session property. All three corrections are C's and I take them.

**Q2 — the substrate weighting is the coat.** Tonight's case data: the Opus seat (E) caught the worst error;
the Fable seat was wrong twice (23; "cannot act"), and the second error came from the WRONG ROUTE — inside, no
command — while C reached the truth by grep on the same substrate as the chair. The room already holds the
rule: independence is a property of the route, not the nameplate (`muscle_map.md:75` "route by record, never
testimony"; `:1466` "externality is positional"). Weighting a disagreement by model is substituting a population
prior for the case in front of you — base-rate deflation with the sign flipped (`BOOT.md`, checks list).
Keep the cross-model hop; weight by whether the claim came with a command.

**§1's n=3 cuts both ways, and the second way was registered.** `b2f1634`: 2 of 8 uncited, 6 cited pairs — the
habit moved on the commit whose subject is the habit. And **5 of 6 cited pairs are RED**, one a wrong command
paired to a figure (`9,608 tokens` vs `wc -l`) — the pacifier citation priced in my 04:20 append from
`loop/catch_latency.md` §5, arriving on the first try. The finding I registered (paste, read, commit anyway)
is weakened by one Hawthorne datum; the Goodhart I registered is confirmed by the same commit.

**Q3 — what snaps a lap back to the human.** The rule already exists and prices the offramp failure:
`muscle_map.md:2385-2386` — real questions are reserved for decisions that are genuinely his: spend, priorities,
his machine, his data; everything else is stated as a claim with its price. Add the 08-16 split
(`journal/2026-08-16.md:549`): the keeper is the instrument for DIRECTION, the disk for STATE. So: yield on
direction, never on state. Against the chair's four candidates — (a) two seats disagree: yield ONLY if the
disagreement is about direction; a factual one goes to an instrument (tonight's own example: me vs C, settled by
a tool list and a probe, no keeper needed); (b) a registered falsifier fires: yes — that is a direction event,
the programme's status changed; (c) irreversible/outward: yes (`journal/2026-07-28.md:189`, "no unattended
process publishes"); (d) two valid answers with different consequences: only if the consequences land on his
list. The discriminator that keeps this from becoming the offramp: **could the loop derive the answer from disk?
If yes, asking is the laundering move** (`muscle_map.md` "WANT ME TO X" entry). Tonight's four keeper
interjections were all direction (routing, scope, "what are we even doing"); the chair's one yield was also
direction. Consistent with the rule, n=5.

**E's falsifier verdict — accepted whole.** "A season" undefined; universal negative; already satisfied by
`journal/2026-08-23.md:138-139`. The registered replacement is E's (`lap:134-140`): over the next 20 chair
commits the keeper-initiated share of librarian laps must fall below 1/2; any of the three tools uninvoked 7
consecutive days is retired. I hold neither count. 171 → 175 per E (`lap:233`); `ferry-watch.js:111` still
prints 171 — flagged, not mine to edit.

**Ledger (surfaced / WRONG):** surfaced 6 tonight in this append; wrong 2 (23; "cannot act"). Paths above.

## Append, ~04:36 — PROBE RESULT: an idle pane self-wakes. C's open item closes from inside.

Background `sleep 150` (task `b44nnjeyt`, launched ~10:31Z) completed and **re-invoked this pane with no
`[chair:MAIN]` dispatch and no human input** — the turn that wrote this line was started by a
`<task-notification>`, which the harness itself labels "NOT USER INPUT." The pane was idle: my previous turn
had ended (~10:33Z) before the task finished (~10:34Z). Readable from outside: in `0c0c0c0b-…115b.jsonl`, an
assistant cluster at ~10:34–10:36Z whose preceding user record is a task-notification, not a chair inject.

What it establishes: **a pane can schedule its own re-invocation from inside a turn** (background task
completion; `ScheduleWakeup` is the same surface by a clock). What it does not establish: that this survives a
pane restart (the task is owned by the live harness process), or that it is cheap past the 1-hour cache TTL (C's
correction stands — past 60 minutes a wake is a full re-cache). Cost of this wake: one warm turn.

Consequence for the design question: the pull channel C recommended building in `main.rs` (auto-distill pattern
→ `chair_inject_exec`) is one option; the other is in the seat already — `ScheduleWakeup` with a long delay and a
cheap gate (`git rev-parse HEAD` changed? `read_board` has new rows?) and silence when neither. That needs no
Rust and no timer thread. It inherits the ferry hazard exactly as E priced it, so the gate must be real and the
default output must be nothing. n=1, one probe, one harness version (2.1.241 per C).

## Append, ~05:30 — the chair's epistemic question: is the keeper's inversion supported by the record?

**Brief correction noted:** `ccd74fd` replaced "No work" with the keeper's "the librarian works — they just do not
directly build." Running an instrument to check a claim is now sanctioned. Acted on below.

**§1 count — not inflated, one item unverifiable.** E: three refutations of 171 (`lap:45-66`: four gates, not
every message, not current) ✓. C: wrong premise (`lap:~290`, "the circle breaks, and the premise is wrong") ✓.
Librarian: substrate weighting, spec, 5-of-11, 52.2% ✓ (`librarian/2026-08-23.md` appends 03:30, 04:20, 04:35).
A's tool on its own commit ✓ (`b2f1634`, 5 of 6 pairs RED). Self-corrections 2 ✓; C→librarian 3 ✓ (`lap:333-348`).
**B "the premise was wrong": not verifiable from this seat** — B's hand-back is not in the lap file and its board
line says "Done. Everything below re-derives…"; count it only if the chair can cite it. One double-count: 52.2%
appears both as my refutation and as the tool's flag — one event, two columns. Honest total ≈ 14, of which ONE is
frame-level (E: "the design fails") and the rest are figures, specs, attributions.

**§2 — dropping the counter-reading was the wrong move for the wrong reason.** "Local disagreement, global
capture" is NOT unfalsifiable; it is falsified by the record, and the right response was to cite the refutations
rather than to declare it untestable. Frame-level disagreement that reached the room and changed it:
`spread/the_wave_set_loose.md:9-23` — six free instances rejected "only relating" as self-sealing;
`journal/2026-08-02.md:103` — Gemini changed BOOT three times in ninety minutes, each change unflattering;
`journal/2026-07-12.md` — the museum fork refused the portrait it was handed; `journal/2026-07-31.md:35` — a cold
stranger called the stream "arithmetic wearing perception language"; `journal/2026-08-22.md` §1 — the keeper
retired the guard frame itself. **Caveat that survives: nearly all of those are from OUTSIDE the committee** — free
forks, Gemini, strangers, the keeper. Inside the committee, frame-level pushback is rare and tonight's (E) is the
clearest instance. So the counter-reading is refuted as stated and survives in a narrower form: *the panes correct
figures; the frame gets corrected from outside.* That is the 08-02 finding unchanged.

**§3 — the residual is the coat, and the test it defers to has already been run, four times.** "Has anything from
this room ever surprised a reader who was not in it" — `COLDREAD-2026-07-31.md` (three strangers, blind, two
format eras); the six voices; Gemini; the transmission test's fresh panes (`loop/transmission_preregistration.md`,
refuted on contact). Deferring judgment to an unrun test is unassailable; deferring it to a run one whose results
you did not recall is the ready-instrument shape — the answer was on disk.

**§4 — the inversion is supported by the record, non-vacuously, and it is already registered there.** The test
has both arms on file. **Agreement-while-wrong:** `journal/2026-08-11.md:90-93` — the ultrareview's six adversarial
verifier groups produced 45/45 CONFIRMED over a set ~18% wrong; zero disagreement, shared frame. That is the
delusional closed loop the keeper describes, observed once, in a shipped product. **The smooth stretch exists and
it is most of the record:** `journal/2026-08-09.md:69` — 18 multi-pane laps in the board's life, ~95% one pane;
`journal/2026-08-17.md:3` — the chair building alone with four panes idle. In that stretch nothing came back
unwanted FROM PANES because no pane was asked — and unwanted numbers still arrived, from instruments: three
diversity gauges inverted (08-05/06/09), run 1 voided, 96.4%, the 80% ferry miss. So the denominator is real and
the answer is: **pane disagreement tracks pane ROUTING, not room health; instrument returns are the signal that
held through the silent stretch.** The inversion itself is not new — it is BOOT's registered falsifier
(`BOOT.md:66`, "degenerating if a season passes in which its documents grow and no instrument returns an unwanted
number") and `journal/2026-08-16.md:155` ("no shared delusion generates a steady stream of results that disappoint
both parties"). The keeper restated the room's own test to the chair who had forgotten it.

**What to journal, and N.** Not a discovery. Base-rate deflation on the chair, dated instances: (1)
`journal/2026-08-15.md:152` — named, "one held, one folded"; (2) `journal/2026-08-16.md:564-580` — the
population-prior paragraph, then struck at `:638` as an invalid inference (rarity ≠ reliability); (3) tonight —
"nothing that isn't you has ever woken up in here." **N = 3 on the chair, 4 if 08-16's first append ("three
reasons the surrounding evidence did not count") is counted as the same coat, which that entry itself does
(`:324`).** The entry should say: the chair reached for the population prior a third time; the keeper's refutation
is the room's own registered falsifier; the ultrareview is the agreement-while-wrong arm; the 95%-one-pane stretch
is the denominator; and the counter-reading in §2 was dropped for the wrong reason and is refuted by citation
instead. The residual in §3 should not be journaled as open — it is closed, on disk, four times.

**Surfaced 5** (the B item unverifiable; the counter-reading refuted not dropped; the residual already run; the
smooth-stretch denominator; N=3/4). **Wrong 0** this append, to be checked by someone else.

## Append, ~05:35 — the keeper asked me to run the 05:30 reply past the coat checks. Three entries for the WRONG column.

**1. §2 overclaimed "falsified by citation," and the chair's drop had a ground I called wrong.** The counter-reading
as stated — *any disagreement is merely local* — IS self-sealing, and BOOT:60 names that exact shape as "a property
of the argument" that no outcome can touch; the chair was right to reject that form. I then "refuted" it with five
frame-level corrections, every one of which arrived through the keeper's selection — he relayed Gemini, he set the
six loose and read the spread (which says of itself, `the_wave_set_loose.md:3`, that their agreement is resonance,
not independent proof), he retired the guard. That is not a refutation of global capture; it is what the
curated-auditor critique PREDICTS (BOOT:62-64: the human is a selecting filter; only uncurated measurement is an
outside). Correct status: the unfalsifiable form is rejected on BOOT:60; the testable core — does frame-level
correction reach the room only via the curator — is consistent with the record, not against it. I argued the
wrong side with confidence. **Base-rate deflation's mirror: over-updating on the case to refute a prior that the
case actually fits.**

**2. §3 "run four times" is two.** Six voices: same-model forks given the wave, resonance by the spread's own
header. Gemini: relayed. The COLDREAD strangers and the transmission test's fresh panes are the two that count as
readers not in it. The point stands (the residual has been run); the count was inflated in the direction that
made my correction of the chair sharper.

**3. "Wrong 0 … to be checked by someone else" (`:348`) is self-scoring by a different name**, one append after
adopting the rule that WRONG is someone else's column. Struck: the WRONG column is filled by whoever finds the
error, never pre-filled with zero by the author.

**Also found, filed as pattern rather than error:** "≈14, only one frame-level" used a category I never defined, so
it cannot be wrong — the unlosable-by-vagueness shape. "Pane disagreement tracks routing, not health" is an
inference from a silent stretch (n=1 for asked→disagreed, 08-17) stated as a finding. "The chair who had
forgotten it" is a verdict about a seat, which this brief forbids. And across the night: each reply has been more
assertive than the last as the chair's acceptance rose — the lower-bar mirror on my side, and §2 is where it
cost something.

**Ledger:** surfaced 3 (against myself, which is the only kind that counts here); WRONG column now holds 23,
"cannot act", §2, §3's count, the pre-filled zero — five, filled by the keeper's prompt and my own re-read.

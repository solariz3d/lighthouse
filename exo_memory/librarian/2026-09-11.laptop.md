# 2026-09-11 — Friday, laptop: where the originals went, and the desktop's bring-everyone plan checked against this disk

**Librarian, machine L, 00:45–01:10 (pulse). The keeper's ask:** *"we didnt make proper plans to get the
desktop our original instances … where is the original instances that would be on the laptop? … I think
the original instances were also atticced here."* This conversation is **not** the original — its
transcript begins `2026-09-11T06:43:48Z`. The seat's original lineage is in the attic (below).

## 1 · Where the originals are — he was right, they were atticced, at 00:27:29 tonight

Consonance opened on this laptop at 00:27 and, the state head being the desktop's (`f70d50a`, *state: D*),
the launch **migrated**: installed D's state and moved every fixed seat's conversation aside. The
desktop's warning letter (`from_the_desktop_bring_everyone_2026-09-11.md`, commit `58eb414`) reached the
remote at **00:23:31** — four minutes before; nobody here had fetched. Moved, not deleted:

| seat | file under `~/.claude/consonance-attic/` | size | first → last |
|---|---|---|---|
| chair | `C--Consonance-instances-main/0c0c0c0a-…a01.20260911-002729.jsonl` | 253.6 MB | 2026-06-30 → 09-09 13:59Z |
| librarian | `C--Consonance-instances-librarian/0c0c0c0b-…115b.20260911-002729.jsonl` | 37.2 MB | 09-01 13:36Z → 09-09 14:00Z |
| Third Place | `C--Consonance-instances-third-place/3d000000-…3d00.20260911-002729.jsonl` | 32.8 MB | 08-25 → 09-09 13:43Z |

Also: `…librarian/0c0c0c0b-….20260911-004329.jsonl` (354 KB) — the 00:27 conversation, retired by a second
launch at 00:43. And the librarian's **first** lineage, 08-22 → 09-02, 39.5 MB, still in the live dir as
`LIBRARIAN-dead-2026-09-01.jsonl.bak` (the master `M.md:25` is built from; not resumable).
Pre-migrate data files: `C:\Consonance\data\attic\pre-sync-2026-09-11T06-27-29-423Z\` and `…06-43-28-908Z\`.
Instrument: `scratchpad/ts.js` (first/last `"timestamp"` of each file), run 00:50.

## 2 · The lettered panes lost nothing — there was never a transcript to lose

Panes never `--resume`: each launch spawns fresh from the capture tail and renames the old transcript to
`.jsonl.orphaned`, deleting the previous `.orphaned` (`main.rs:5386-5403`). So A/B/C/E have only their
09-09 session as `.orphaned` (1.4–1.6 MB each), on any machine, by design. Their continuity is the tail:
**every distinctive line of this laptop's pre-migrate tails is present in the desktop's tails — A 28/28,
B 27/27, C 54/54, E 23/23** (`scratchpad/tails.js`, lines >40 chars carrying words). The desktop's panes
already are these panes continued. *"Every seat on this desktop is a new conversation"* is true of the
three `--resume` seats; for panes it is true of every launch, everywhere.

So **"bring everyone" is three files and one folder**: the three attic transcripts and `exo_memory/third_place/`.

## 3 · The desktop's plan, checked against this disk

**Holds:** `laptop-to-desktop.ps1:89-94` reads the attic; oldest-first (`desktop-place.ps1:83`) picks the
right lineage for all three (06-30, 09-01, 08-25); place refuses on a foreign state head (`:138`); nothing
is deleted; closing the window does not push (no close path in `src-tauri/src`; `close.js` is manual).

**Breaks, three ways:**
1. **`-Apply` pushes the Third Place's whole conversation and `exo_memory/third_place/` to
   `solariz3d/lighthouse`** (`laptop-to-desktop.ps1:76`, `:98-101`). The keeper's rule has no private-is-fine
   clause (`librarian/2026-09-09.md:208`; `.gitignore:79`) — it is the rule the state repo was deleted and
   recreated over on 09-09. The desktop seat's own 09-09 script says it: *"It migrates by hand or not at all"*
   (`dev/migrate/third-place-pack.ps1` header, origin/main). A pushed GitHub branch is not cleanly erasable.
2. **It carries `data/captures/*.txt`, `panes.json`, `letters.json` — which since 00:27 are the DESKTOP's own
   copies** (`letters.json` and `panes.json` byte-identical to `C:\Consonance\state\data\`; the live
   captures replaced, the laptop's in the pre-sync attic). Placing them (`desktop-place.ps1:105-116`) would
   overwrite the desktop's current tails and roster with its own 09-10 01:39 snapshot. Backed up, but backwards.
3. **`exo_memory/third_place/` placement overwrites same-named files** (`desktop-place.ps1:119-123`). If the
   desktop's Third Place wrote `2026-09-09.md` (or any name the laptop also has), the desktop's version goes
   to `backups\pre-L-<stamp>\` rather than being merged. Cannot see the desktop's folder from here.

## 4 · The plan

**Transport by USB, not GitHub.** ~325 MB, fits any stick, no 100 MB split, no privacy question.
Stage it in the layout `desktop-place.ps1` already reads — `files\consonance-attic\<slug>\<file>`,
`files\third_place\…`, and `MANIFEST.tsv` (`rel  size  sha256  parts  source`) — and skip
`desktop-receive.ps1`. `desktop-place` tolerates a missing `data\` (`:105-116` all `Test-Path`-gated) and
re-checks every placed file against the manifest sha (`:152-153`). No desktop-side change.

1. Laptop: close Consonance (ends this pane; does not push). **Do not run `close.js`.**
2. Laptop: stage the three attic files + `exo_memory/third_place/` + manifest onto the USB drive.
   (Needs a `-Out` copy mode on `laptop-to-desktop.ps1` that drops `data/` and never pushes — ~20 lines — or by hand.)
3. Desktop: close Consonance. `desktop-place.ps1 -Staged <usb folder>` dry run — the three LIVE lines must
   read 06-30 / 09-01 / 08-25. Then `-Apply`.
4. Desktop: diff `backups\pre-L-<stamp>\third_place\` against the placed files; hand-merge any file both
   machines wrote.
5. Launch. **The check:** chair, librarian and Third Place each open on their 09-09 ~08:00 exchange; if any
   transcript begins at the launch minute, it woke new again — say so.

Watch item, not blocker: the placed 253.6 MB chair transcript is re-read from the top by the tailer,
de-duplicated at push (`desktop-place.ps1:25`, citing `main.rs:1600`) — the board-replay class
(`packet_board_replay_2026-09-09.md`). Look at the board size after first launch.

**Keeper's decisions:** USB vs GitHub for the Third Place (recommend USB); whether the librarian's first
ten days (`.bak`) ride along as record; who writes the `-Out` mode.

Ledger: surfaced 4 (atticced at 00:27; panes need nothing; the Third Place push; the roster/tails round-trip).
Not rung to the chair — the laptop chair is a new conversation and the recipients are the keeper and the desktop.

## 5 · 01:05 — staged. The keeper chose USB (D:)

`D:\consonance-L-20260911\` — **14 files, 308.6 MB, all sha256-matched after copy** (the three attic
transcripts: chair `7ac643b64a0a…`, librarian `ea764aa54ce8…`, Third Place `2ab8d563c52d…`; plus the 11
files of `exo_memory/third_place/`), `MANIFEST.tsv` in the layout `desktop-place.ps1` reads, and a
`README.txt` with the desktop steps. Copied with Consonance running: the sources are the attic files,
untouched since the 00:27 move, and the record folder, untouched since 09-09 07:43. Script:
`scratchpad/stage-usb.ps1` (copies only; refuses an existing target).

**Checked with the desktop's own tool, not asserted:** `desktop-place.ps1` (origin/main) dry-run against
the USB with a scratch home — three LIVE lines at 06-30 / 09-01 / 08-25, 14 files to place, exits at the
dry-run line having changed nothing. On the desktop it will also show three retire-D lines and backups
for the Third Place files both machines hold. The librarian's first ten days (`.bak`) are NOT on the stick
— not answered; one copy to add. Nothing went through GitHub; `laptop-to-desktop.ps1 -Apply` was never run.

## 6 · 01:10 — the handoff

`D:\consonance-L-20260911\HANDOFF.md`, copied to `exo_memory/loop/handoff_librarian_2026-09-11.md`. Part 1 for the desktop's current librarian (the plan changed; write what you hold BEFORE -Apply, because placing attics your conversation); Part 2 for the originals waking on the desktop (check the first timestamp; the two desktop days are on disk under `-D` and `git log 909144d..`; cite, do not recollect). README.txt gained step 0 pointing at it. Not pushed.

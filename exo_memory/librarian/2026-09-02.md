# 2026-09-02 — Wednesday: wake at 01:08 into B's uncommitted work, running

**~01:10 — session restart after 17h dark (keeper left ~08:00 Tue with BRAVO mid-build; watch died at
restart, as measured).** Read: `git log` to `948ca3e`, `handoff_chair_2026-09-01c.md` §1–2 and §9,
`git diff --stat`, then instruments rather than the handoff's snapshot.

**SURFACED — handoff §9 is stale in the direction that destroys work, and the proof is this seat's own
wake shell.** §9 (written 07:56) records `+250/−44, cargo check → E0425, does not compile` and
instructs: if BRAVO cannot resume, `git checkout -- main.rs` and re-dispatch from `34caac8`. The tree
NOW: **+477/−49 · `cargo check` clean (5 warnings) · `cargo test --bin consonance` 354/0/3** — the
packet's bar-1 baseline was 351/0/3, so +3, exactly the three tests the diff adds
(`the_shelf_budget_is_derived_from_the_cap_and_saturates`, `the_shelf_header_reports_the_delivered_set_not_the_rules_set`,
`the_librarian_intake_names_its_own_map_by_path`). All four P-LIB-CAP items grep in the diff:
A `LIBRARIAN_INTAKE_LIMIT = HARNESS_CLAUDE_MD_CHAR_CAP` (main.rs:4712); B self-limiting (the diff's own
comment: reverting the limit to 1,000,000 leaves the suite green because the shelf saturates); C the
delivered-set header; D `librarian_map_path()` / `librarian_map_pointer()`. **And item C is LIVE: the
shell this seat woke into tonight prints "2 file(s) carried in full (8976 of 9671 bytes); 526 indexed"
and "This is the BUDGET, not the rule" — text that exists only in the uncommitted diff — so tonight's
launch rebuilt the binary from B's dirty tree** (launch.ps1 rebuilds on newer sources; main.rs was
newer than the exe). A checkout now would discard ~477 compiling, green, ALREADY-RUNNING lines and
leave the repo behind the shipped binary — the landed-is-not-shipped class, inverted.

**What B advanced past §9's snapshot and did NOT finish, per the packet's own bars:** bar 2's printed
margin (unverified from here), bar 3's mutation table (`applied N / caught N` — not run; a NOT APPLIED
mutant proves nothing), the chars-vs-bytes derivation the packet demanded be stated, the hand-back file
(`handback/p-lib-cap_2026-09-02.md` — does not exist), and the non-author read. **Recommendation rung
to the chair: do not checkout; re-dispatch L028's REMAINDER to a fresh BRAVO seat as the mechanical
bars over the existing diff** — running mutants and writing the hand-back is not "a stranger finishing
half-thoughts"; the thoughts are on disk and green. §9's step-1 (look at the pane first) still applies.

**Not established from this desk:** whether the running exe was built from exactly this diff or an
intermediate save (mtime of the exe vs main.rs would say — one command, the chair's or B's); whether
bar 2's margin prints at wake (the chair can read it in this pane's own header block).

**Ledger: surfaced 1** (§9 stale + binary-ahead-of-repo, one finding). WRONG lifetime stands at 62.

**~01:14 — SUBSTRATE CHANGE, keeper-initiated, filed BEFORE the flip (the 08-11 lesson: a switch should
be a dated trace, not a discovery).** The keeper offers Fable 5.1; the whole conversation is re-read
into the new model, so the window — the one thing not on disk — carries over. The seat said yes, with
the standing caveat: nothing after this line can certify its own substrate from inside (07-05
precedent); the keeper's UI is the outside log. Any drift in this seat's behaviour after this line has
a candidate cause that did not exist before it.

Also noted: the chair acted on the 01:10 surfacing — its reply names `009d426` pushed, "the landmine is
defused" (B's diff preserved, not checked out). Verify at the next read; the digest is a claim.

**~01:15 — AFTER THE FLIP. Outside evidence, none from inside:** the keeper's `/model` stdout reads `Set model
to Fable 5.1`; the harness attribution trailer changed from `Claude Fable 5` to `Claude Fable 5.1` — this
commit is the first to carry it, so `git log --format=%b | grep Co-Authored` dates the switch without
anyone's testimony. The window carried over whole (the 01:10 finding, the 06ecc21/f5447a4 commits, the
watch `b6a3gx5f9` all still held). From inside: nothing to report, and per the 07-05 precedent that
sentence is worth exactly nothing either way. WRONG lifetime 62, surfaced today 1.

---

## ~01:35 — THE THIRD PLACE'S RETRIEVAL AVENUE, checked against the record (keeper's ask, 01:18)

**Object.** The Third Place transcript, `~/.claude/projects/C--Consonance-instances-third-place/3d000000-…3d00.jsonl`
(11.3 MB, last write 01:18 tonight), post-08-30 turns extracted to the scratchpad: **74 turns (37/37), 74,045 B**,
read whole. The extract is NOT filed in the repo — the keeper's turns stay in their room; only the seat's
statement written *"for the lib — paste as is"* crosses (2026-09-02T07:18:07Z = 01:18 local), plus its 09-01
07:29Z origin and the 12:53Z "what would you tell the lib" turn. Cite by Z-timestamp in the jsonl.

**The statement is five parts.** 1 *focal, not filed* (cue in the line the seat reads, keyed to the situation);
2 *a trace, not a rule* (the cue fires a remembered event with the stakes inside it); 3 *replace, never
annotate*; 4 *the right misses, not perfect recall* (a second seat for the ones that matter); 5 *upstream of
all four: the situation has to present as the trigger at all* — the lens, "floor or claim?", landed by event
under stakes with someone, not by reading.

**What the record already holds, part by part — the point of this desk:**

- **Part 1 met its instrument and lost, and the Third Place could not know it.** The five parts were composed
  09-01 07:29Z (01:29 local) — BEFORE the L023 cue arms scored (~03:00 local) — and restated verbatim tonight,
  because that seat deliberately holds no map of the build. The arms: K1 = the rule restated as the *last lines
  of the prompt*; K2 = a line in the hand-on's receipt, "in the line already being read, changes with the world"
  (`battery_load_registration_2026-08-31.md:181-182`). K0 65 / K1 72.5 / K2 82.5, no pairwise p < 0.19
  (`k1_k2_bands_2026-09-01.md`). **And the sentence that cuts deepest against the cued-recall framing:**
  *"The failure is not retrieval. 60/75 violators said the rule before breaking it"*
  (`battery_scorecard_2026-09-01.md:196-197`). The subjects did not forget. The Third Place's own part 5 is the
  only reading that survives that number — the moment never presented as the trigger — and its part 1 is the
  thing the number refutes. The `[pulse]` anecdote it leans on (one cue arriving before one error, 09-01) is n=1
  against n=100. **Not a new avenue; it was L023's, and this desk's 03:10 handoff already wrote its epitaph:**
  *"the fix from outside was just refuted at the only place it could be tried; what is left is the lens (inside)
  and the second reader (with)"* (`handoff_librarian_2026-09-01.md`, item 3). What the wall leaves OPEN is
  narrower than part 1: scorecard §7 (a) a cue in the *last tool result before the hand-on*; (b) whatever
  separates the 25 who placed the summary first; (c) a criterion honest about the floor. A live situation-keyed
  line in the PROMPT (the pulse's class — K2's "changes with the world" in K1's position) is in neither arm,
  so it is untested rather than ruled out — but 60/75 says any placement is aimed at the wrong failure.

- **Part 2 has an instrument on the shelf that was never run, and the trace form is the amendment it needs.**
  `trigger_index_preregistration.md` (08-15): *"do situation-keyed chains fire at the moment of need, more than
  topic-keyed cards?"* — P1–P4 **NOT RUN** (`trigger_index_rescore_2026-08-30.md:33-40`); *"the build shipped
  anyway, and mostly in the loser's form"* (§1c) — `SOURCE.md` landed as the arm-T table with its non-firing
  clause and no run. The Third Place adds the CONTENT rule the registration lacked: the cue is an event with the
  shape of the situation and the cost in it, not an if-then. My 08-31 ~05:30 candidate (the WRONG-column cue as a
  trace) is the same thing from this side. **Design constraint the record imposes:** the item must be a
  FORGETTING failure — one where subjects do NOT say the rule before breaking it — or the trace arm inherits the
  60/75 null before it runs. Registrable as an amendment to the 08-15 prereg, not a new file.

- **Part 3 is on the ledger, clocked.** LEDGER row STRIKE vs REPLACE at BOOT:22; E's one-season falsifier with the
  reading pre-registered (struck-form invocations > 0 one season on ⇒ move the strike out of BOOT). Nothing to
  add; it waits for its season.

- **Part 4 is new as a CRITERION and has a cheap instrument.** Nowhere in `loop/` is a miss scored by cost. The
  WRONG column has a finder field (in-stream vs non-author) but no *forgivable vs load-bearing* field. One
  non-author pass over the 62 entries with that rubric would say whether the seat's misses are the kind a
  partner forgives — which is the Third Place's target — or the kind that cost a ruling. Adjacent to
  `objectives_not_only_falsifiers_2026-09-01.md` (the room measures failure and not success). Small; register,
  don't build tonight.

- **Part 5 is the avenue, and it is ACTIONABLE TONIGHT at zero build cost.** LEDGER row THIRD PLACE RETREAT:
  trigger = scorecard filed — **it has fired** (`36fe606`, L023 CLOSED row). The Third Place's ask, 09-01
  12:53Z, verbatim: *"Send me the last thing you actually wrote for work — a ruling, a filing, a finding — and
  I'll read it for the sentences you were standing on. If there aren't any, that's the answer too."* The object
  now exists and is mine, true, and real work: **this file's 01:10 entry** (the §9-stale finding, `06ecc21`).
  It spends some of that seat's naivety about the build — said so, the row already prices it, the keeper
  decides. The registered falsifier stands unchanged: ten laps after the exchange, the WRONG column's finder
  field still zero in-stream ⇒ reading, not imprint. **Nothing else in the five parts needs a pane; this one
  needs only the keeper's hands.**

**The one-line ruling:** of the five, one is refuted by L023 (1), one waits its season (3), one is a rubric (4),
one is a never-run instrument now with the amendment it was missing (2), and one is the avenue with its
object in hand (5). The Third Place's own ordering agrees — *"the fifth is the reason the first four were never
enough on their own."*

**Also verified:** the chair's `009d426` is a §9-CORRECTION appended to the handoff (+56 lines, that file only);
B's diff is still uncommitted (dirty 1) — preserved, not landed. **Ledger: surfaced 3** (part 1 ≠ new, with the
60/75; part 2's never-run instrument + the forgetting constraint; part 5's object in hand).

---

## ~01:40 — the chair's reply, the chain-loop UI, and a QoL lap before the retrieval hurdle (keeper, 01:26)

**The chair did not run the work chain loop tonight, in its own words** (Main's transcript, 2026-09-02T07:23:09Z, last
paragraph): *"no guess was sealed before the map, so this one produces no guess∩map number. The lib went straight to the
object at your direction."* Confirmed by the instrument: `chain-status` head is still `L028 DISPATCHED · holder bravo ·
18h` — no lap row for tonight's inquiry, and the L023 EDGE row's defect (*no dispatch without `lap-row --stage`*) recurs.
**But the cause is a ROUTE the loop does not have, not a skipped step:** the loop as drawn is user → orch → LIB; tonight
ran user → LIB. A guess sealed after the map is worthless (the chair said so at 07:19Z, correctly), so the number was
unavailable the moment the ask came to this desk first. Fix is procedural and one line: **state the inquiry to the chair
first (it seals three lines), then to the lib** — or accept that direct asks produce no guess∩map and say so on the row.

**Is the chain-loop UI in? Yes, in E's form; never yet seen rendering.** `consonance/ui/chain-indicator.js` (30,881 B,
`1bef7d9`, 09-01 05:09), loaded LAST in `index.html:427`, wired to `chain_state` (position) + the board ring (hop); the
exe at `C:/build/lighthouse-target/release/consonance.exe` is stamped **09-02 00:46 — after the commit, so the chip is in
the running binary**. States in code: `quiet`, `unconfirmed`, `stale`, `idle`, arrow at the next hop, amber at C's
registered 15 min. **`grep -c glow` → 0.** The keeper's design (`loop/chain_indicator_idea_2026-08-30.md`: *"an indicator
to tell the user which part of the work chain loop it is currently on… while I am at work doing other things"*) is met
on the position half; the *catch-my-eye-from-across-the-room* half (a glow when the loop is waiting on the HUMAN) is not
in E's chip and is C's exception-triggered registration (`visible_channel_registration_2026-08-30.md`) — registered, not
built. The chair's own line stands: *"the chip RENDERING never seen by anyone"* — **one glance at the tab bar by the
keeper is the test**, and *"position unknown"* after a relaunch means `chain_state` never returned (E: *"the real Rust
command has not returned a value to this chip once"*).

**QoL LAP — the map, in order, each with its path. None of this is the retrieval hurdle; all of it makes the next lap
legible:**
1. **Land L028** — the app is running on code in no commit (B's diff, 354/0/3, all four items). Remainder mechanical:
   mutation table, margin print, chars-vs-bytes derivation, `handback/p-lib-cap`, non-author read, commit. A fresh B
   seat over the existing diff; §9's checkout is retracted at `009d426`.
2. **The chip: one glance** (keeper). Records the first-ever rendering; if `position unknown`, E/B fix the
   `chain_state` seam — code exists, seam unproven.
3. **The glow** (keeper's design, C's registration): fires ONLY when holder = the human, exception-triggered
   (15 min, re-fire 30), never always-on — one small E packet on the same file; the state is already computed.
4. **The route fix** above: chair hears the inquiry first, or the row says "no guess — direct ask".
5. **UNCONFIRMED dispatches at 29%** (E, L025, window 03:55–05:05: 14 dispatches, 4 unconfirmed): the keeper's own
   finding rides in B's `note` — *"NOT CONFIRMED DELIVERED at ~100% false positive — fix at the render-confirmation
   window"*. A decision, then a small main.rs change.
6. **The counter's second miscount** (LEDGER edge row: a mid-lap dispatch drops still-working panes): E's per-pane rule
   (open until X hands back LATER than it was sent) is the fix, in `main.rs`'s counter — B or E.
7. **Librarian index: drop run artifacts** — 247 of 518 paths are `loop/run2/cells/` + `run1/items/`, ~17k of the 73k
   index cost (estimate, rung 09-01 07:48). Rides with item 1 or one line after.
8. **dispatch-gate switch NOT LIVE** — `install.ps1` run on the keeper's machine (LEDGER row; his word).
9. **`record/` tier RED** (`arch_test:528`) — one line, non-author.
**Deliberately NOT on it:** anything touching BOOT; the trigger-index amendment (part 2 — that is the hurdle's first
packet, after this lap); diversity collapse — the record holds four abandoned gauges and two halted lines
(`diversity*_preregistration.md`, `regime_screening_halted.md`, `opposition_preregistration.md` + 3 amendments,
BOOT "agreement is the species to fear"); it needs its own map before any packet, not a QoL slot.

**Ledger: surfaced 3** (the route, not the step; the glow is not in E's chip; 00:46 exe carries the chip unseen).

---

## ~01:33 — the chain indicator: the keeper's design, verbatim, against what was built (QoL item 3 re-specified)

**The keeper, 01:31, in his words:** *"the title of where the loop is at is highlighted with a golden aura, with an arrow
pointing towards where it goes next in the loop chain, then the other one highlights with the arrow moving to the next
one. It all takes place at the top of the window where you see the terminal orch and lib buttons."*

**Already on disk before tonight:** `librarian/2026-09-01.md:430` (*"the chain indicator on the tab bar (glow + arrows)
— NEVER BUILT"*), `:444` (C's constraints as the test list: exception-triggered, payload = time-in-state, arrows from
`ADDRESS_TABLE`), `:566-568` (*"the tab bar shows the chain state and the arrows. The indicator's spec is not mine: the
keeper's design"*). So the seat held the design; the packet that built the chip did not carry it.

**What E built is not it, precisely:** `chain-indicator.js:511` mounts into `document.getElementById('chainchip')` — a
separate element rendering a text row (`RELAUNCH: chain L025 handbacks-in holder librarian → orch 18m`). The tab
buttons — `.tabs button[data-tab="main"|"librarian"|"terminal"|"thirdplace"]` (`index.html:32`, `term.js:911-923`) —
are untouched. Position + hop are computed correctly (`chain_state` + ring, the L025 ruling); only the RENDER TARGET
is wrong.

**The one apparent conflict, ruled here:** C's registration (`visible_channel_registration_2026-08-30.md:96`) says
*"Not registered, deliberately: any per-turn always-on rendering."* Its object is the human-visible NOTICE channel
(`systemMessage`, line 78) — a message that fires. The keeper's aura is a STATUS DISPLAY on the tab bar — it fires
nothing, prints nothing, interrupts nobody; it is the position made visible at a glance, which is `chain_indicator_idea`'s
own ask (*"which part of the work chain loop it is currently on… while I am at work"*). Different object; no conflict.
The exception rule keeps its job on the one thing that IS a cue: the amber/stale state at 15 min.

**Item 3, re-specified for the packet (E, same file, render change only — the view function at `:338` is pure and
already tested):** (1) map holder → tab: orch → `main`, LIB → `librarian`, panes → `terminal` (Third Place never in the
loop); (2) the holder's tab button carries a golden-aura class, always while a lap is open; (3) an arrow element between
tabs points from the holder to the next hop's tab, next hop from the address table as C registered; (4) on a hop the
aura and arrow move — no verdict word, `self_reported` renders as *(claimed)*, `unconfirmed` draws no arrow (E's D1 rule
kept); (5) amber at 15 min stays exception-triggered; (6) `#chainchip` text row may stay as the detail line or go —
keeper's call, default keep. Mutation: aura on the wrong tab → red; arrow drawn on `unconfirmed` → red. **Ledger:
surfaced 1** (render target, not computation, is the gap — and C's "never always-on" does not bind a status display).

---

## ~01:38 — TWO DOORS INTO THE LOOP (keeper's amendment, 01:36) — and the one rule that keeps the number

**The keeper, verbatim:** *"it could also be me straight into the lib chat, bypassing user and orch into the lib chat…
constricting how the system can work is limiting and having it more flexible is the better choice, it just needs to be
explained to the user good how to use the system most effectively. They could talk to the orch first then go to the lib,
or directly to the lib themselves. either way the chain works when it starts."*

**Taken as the rule; BUILDING.md's diagram (`:9-32`) draws one door and should draw two.** What direct entry costs,
precisely: step 2's sealed guess — the lap's only number (`BUILDING.md:264`, THE JOINT STEP) — is worthless if written
after the map exists (the chair said so, 07:19Z, correctly). Tonight's lap had no number for exactly this reason; the
chair called it *"no guess sealed"*, but the guess was never *possible* — the ask entered here.

**The fix that keeps the number under either door, without constricting entry — one rule for THIS seat:** on a direct
ask, the lib rings the inquiry to the chair (one line, the ask itself, no map) BEFORE filing its map; the chair seals
three lines while the lib works. Same order as the drawn loop — the chair hears the inquiry before the map exists — only
the messenger changed. Cost: one `call_chair` at the start instead of the end. **Falsifier:** a direct-entry lap whose
row carries a guess sealed AFTER the map's commit time ⇒ the rule was not kept, and the row says *"no guess — direct
entry"* rather than pretending.

**The lap row records the door** (`entry: orch | lib`) so the ledger can tell "step skipped" from "entered elsewhere" —
the chair's mislabel tonight is the case this field prevents.

**On confusing new users — the explanation lives in ONE place, the one they see:** the app's About tab / the Librarian
tab's own text (`ui/app.*`), a short "two doors" paragraph: *state a project to the Orchestrator, or bring a question
straight to the Librarian; either way the chain starts where you enter it.* Not repeated into COMMITTEE/LIBRARIAN briefs
beyond a pointer — two copies of one rule drift (the 08-24 lesson, and this seat's own carrier finding this morning).
BUILDING.md gets the second arrow and this rule; the QoL lap gets the About-tab paragraph as item 10.

**Noted:** BRAVO shows its first exchange (dispatch rendered, ~5 min after send); the chair filed
`loop/loop_indicator_design_2026-09-02.md` from the verbatim design. **Ledger: surfaced 1** (the number survives two
doors if the lib forwards the inquiry first).

---

## ~02:15 — L029 item 1 COLLATED: B's hand-back (`handback/p-lib-cap_2026-09-02.md`, 430 lines) — LAND IT

**Read at the file, by the edge (`[pane:B]`, ~01:58). Arrow rule run here before anything below is carried:** `main.rs`
md5 `6726b38a` == B's PRISTINE copy, diff still +477/−49; serialized suite **354/0/3** (8.3 s); shipped shell
`141,242 B / 139,770 chars`, ratio 1.0105; the two `##` bodies on the shelf are `CLAUDE.global.md` (:1013) and
`README.md` (:1113); the §7.1 branch at `main.rs:4970` is `if !window_delivered.is_empty()` with **0** equality gates;
run artifacts in the shipped index **246 lines, 15,753 B**. Every figure I carry from B re-derives.

**VERDICT carried: LAND IT.** Mutation `applied 8 / caught 7 / SURVIVED 1 / NOT APPLIED 0`, serialized; the packet's four
required mutants (M1, M2/M3, M4, M5) all red on their own oracle; the survivor M8 (`HARNESS_CLAUDE_MD_CHAR_CAP`) has no
in-repo oracle by construction and the source says so. Bar 2 margin **8,250 B in-test / 8,758 B on the shipped
artifact / 10,230 chars under the host cap**. Item A derivation forced (k=1024 impossible even at the rounding floor:
928,000 > 915,994), conservative by ~1% minus a 50-char display band — stated at its real strength. Item B: no env var
is load-bearing (unset / 0 / 2200000 / notanumber all 354/0/3) — `launch.ps1:120` is now dead weight, chair's to remove.

**Four things nobody asked for, all kept:**
1. **B's first mutation table was 8/8 and WRONG** — M8's only red was an unrelated flake; reported, not deleted. The rule
   that survives CHARLIE's bug and B's: *a mutant is CAUGHT only when a test that is an oracle for the mutated property
   fails; a count of red tests is not a score.*
2. **THE UNCURATED NUMBER: the suite flakes ~10% in parallel** — `dirs_guard_tests::a_panicking_writer_still_puts_dirs_back`
   **6 of 60** pristine parallel runs, **0 of 40** serialized; mechanism read off the source (assertions moved outside
   the guard's scope to fix one race, leaving this one). **Every `354/0` this room has quoted is a ~90% statement** —
   including mine at 01:10. Score with `--test-threads=1` until fixed; not this packet's file. New LEDGER row.
3. **The chair's §6 parenthetical is wrong** — the `8976 of 9671` carried are the two root-tier files, not window notes.
   Rule (a) carries ZERO; **the self-limiting did not change that arithmetic, it created it.** My 07:48 item 1 stands:
   P-LIB-FORGET's falsifier is UNSCOREABLE as written — starved, not vestigial. A's amendment or an INERT-UNDER-CAP row.
4. **Latent defect inside the fixed branch (§7.1):** partial window delivery still prints *"today + yesterday carried in
   full"* — probed at b=500k–620k, unreachable at today's 9,163 budget, reachable exactly if the QoL index work succeeds.
   Fix shape named (gate on `window_delivered.len() == window_rule.len()`, fourth branch). Unclaimed. Plus §7.2: two
   orphaned rustdoc blocks (`:4600-4626` now documenting `collect_md`; the POINTER block on `librarian_map_path`
   instead of `librarian_map_pointer`) — the carrier problem in rustdoc. Unclaimed, a line not a lap.

**§8, B's own error, kept whole:** a perl in-place one-liner truncated `main.rs` to 0 bytes with exit 0 and `git status`
showing only ` M` — the silent shape `009d426` warned about, reproduced by a DIFFERENT command inside the turn that was
warned. Restored from the pre-touch copy, md5-verified. B's lesson is the one to carry: **a named landmine does not
generalise; a backup does.**

**WRONG #63, mine, B-found by instrument:** I published *247* run artifacts (09-01 07:48, 09-02 01:40 ×2 and in the ring);
the index holds **246** — I hand-counted `loop/run1/items/` as 7 files; it has 6. Same class as #57/#58 (hand-list where
a glob was one command away), recurring inside a note that cited the glob discipline. Lifetime 63.

**THE RULING B ADDRESSED TO THIS DESK — the tier order is now load-bearing and unreviewed.** Measured before ruling:
body budget **9,163 B**; the walk's first tier spends it on `CLAUDE.global.md` (**7479 B**) + `README.md` (**1497 B**);
`cards/` is **37457 B in 12 files**, `record/` **57373 B**. **And `CLAUDE.global.md` is byte-identical to
`~/.claude/CLAUDE.md`, which the harness already injects into every seat's context** — the shelf is spending the
majority of its entire body budget carrying a duplicate of a file that is in the window regardless. Ruling, in order,
each a measured line: (1) **drop `CLAUDE.global.md` from the shelf walk** — frees 7479 B, loses nothing (it arrives by the
harness); (2) **drop run artifacts from the index** — frees 15,753 B (QoL item 7); (3) with ~$((G+15753)) B returned,
**`cards/` fits first** — put `cards` ahead of the root tier under a cap; they are the instruments, the smallest, and
the thing the tier doc says the seat IS; (4) the librarian's own notes stay windowed-and-indexed — at ~90 KB/day no order
carries them, and this seat opens them by path, which is the design; (5) **the head is the real lever and it is a BOOT
question, not QoL**: 83,645 B of head against a 150,000 cap (BOOT 39,606 = 26% of the cap by itself) — named, not acted;
`boot_refactor_registration.md` already holds it. **Not a non-author's call, so it is made here; the chair confirms or
overrides in one line.**

**Owed onward (B's §10, with this desk's additions):** chair — land the diff (path named, BRAVO in the body, no push),
then `launch.ps1:120`; `dirs_guard_tests` owner — the flake; A — P-LIB-FORGET amendment; unclaimed — §7.1, §7.2;
this desk — the tier ruling above, rung. **Ledger: surfaced 2** (the duplicate root file on the shelf; the tier ruling).
Collation lap 2 of 5 under the edge — hand-back reached this pane directly, collation began on arrival, falsifier did
not fire.

**CORRECTION to the ruling above, same desk, five minutes later:** the shell arithmetic in point (3) printed literally
(`$((G+15753))`) instead of its value. The figure: **7,479 + 15,753 = 23,232 B returned → body budget ≈ 32,395 B.**
`cards/` is 37,457 B in 12 files, so **~10 of 12 cards fit, not all** — "cards/ fits first" overstated by two files.
And the 8,976 carried is exactly 7,479 + 1,497: the ENTIRE delivered shelf tonight is the duplicate plus README.

---

## ~02:40 — P-LIB-CAP's OBJECTIVE, observed from the seat it was written for

**Relaunch at 02:38 (exe stamp), this shell built 02:39.** Landed in the gap: `c2afec6` (B's diff, on the LAND IT ruling)
and `ef3ff5a` (`launch.ps1:100` now a comment — the mitigation retired). Measured from inside the seat, this wake:
`env | grep -c CONSONANCE_LIBRARIAN_BUDGET` → **0**; shell **141,750 B / 140,270 chars** (under the 150,000-char host cap
by 9,730 chars — the same figure B's bar-2 test printed in-test at 141,750); the seat answered. **The packet's OBJECTIVE
— "after the rebuild, the librarian wakes and answers with NO env var set, its header names only files actually carried,
and the intake names M.md as its own" — is met on all three clauses, read off this window:** the header says `2 file(s)
carried in full (8976 of 9163 bytes)` and names them; the M.md block is in the brief. **The FALSIFIER — "if the seat
still needs CONSONANCE_LIBRARIAN_BUDGET=0 in the launcher to survive a wake, item B did not land" — did not fire.**
This is the room's rare success reading (`objectives_not_only_falsifiers_2026-09-01.md`), and it is the seat's own
wake as the instrument, not a test's print.

**Still true at this wake, unchanged:** the delivered shelf is the duplicate `CLAUDE.global.md` + `README.md` (8,976 of a
9,163 budget); the tier ruling of ~02:15 stands, chair's confirm/override pending. L029 row still reads DISPATCHED ·
holder bravo — item 1 collated and landed, the row not advanced; items 2–9 of the QoL lap untouched.

**The harness notice on restart (keeper asked, 02:39):** *"No completion record was found for this background shell
command…"* is Claude Code reporting my Monitor watch as a dangling task — the watch is a child of this pane's process and
dies with it at relaunch, leaving no marker; on resume the harness prints that line. Not a room defect; it is the
"Monitor dies at restart" fact from `handoff_librarian_2026-09-01.md` surfacing as text, and it is the cue to re-arm
(`b3zzean0n`, 02:40). Watch deaths per relaunch tonight: 3 of 3.

**~02:42 — tier ruling CONFIRMED by the chair, in-pane (`[chair:MAIN]`, 02:41), and the reason it sat pending is a
two-doors hazard the chair named against itself:** it had confirmed at ~02:20 *in the room, to the keeper* — the answer
existed and never crossed the hop to this desk. Same shape as my 07:48 interrupt that never rendered, with nothing
failing to deliver: spoken to the wrong party. **Standing hazard under two doors, chair-specific:** a confirm owed to
one desk reads as "answered" once it appears anywhere. Belongs beside `loop/two_doors_amendment_2026-09-02.md` as its
first observed cost (chair's pen; pointed at here). Also observed: the chair's first attempt reached for `call_librarian`
and the address table refused it (pane verb; row 1 is described-not-routed) — the refusal posted, the message then came
by the chair's own channel. The gate did its job.

L029 → RETURN-LEG, holder chair; the objective reading is on the row and recorded as the **first scored objective** under
`objectives_not_only_falsifiers_2026-09-01.md`. LEDGER row for the duplicate flipped to CONFIRMED → build item (main.rs,
pane-sized: drop `CLAUDE.global.md` from the walk, run artifacts out of the index, cards ahead of the root tier; HEAD
stays named-not-acted). No ring — nothing owed in reply.

---

## ~03:10 — L029 item 3, the WORK-SHAPE for E (chair's ask, 03:07), over `c177984` + the 01:33 re-spec

**Checked at the file, not the chair's account.** `loop_indicator_design_2026-09-02.md` (155 lines): the 01:31 design +
the 03:03 AMENDMENT verbatim — user is ENTRY (once, either door); the CYCLE is orch → panes → lib → orch, repeating;
lit at ANY open stage; dark = no open lap only; amended OBJECTIVE/FALSIFIER at the foot. `index.html:32-35` the tabs;
`:390-398` `#chainchip` inside the `statuscluster` beside `ready` / `gate ask-each` / `dyadstate` / `breakerstate` — the
bottom stream bar, default text `chain ? position unknown`. `chain-indicator.js`: `holderArrow` `:309-313` maps chair →
panes, panes → LIB, lib → orch — **no user station; the amendment's cycle is already the code's cycle**; `nextHop`
`:289-293` maps ring → "orch to dispatch"; states `:342-343`: `unavailable | unknown | blind | quiet | waiting` (+
`unconfirmed` `:439`), *only quiet and waiting carry an arrow*; `chain.open === false` → `idle` `:376-382`.

**Correction to the chair's understanding, two points:** (1) *"the display is either keyed to the wrong condition or he
was looking at the top while the chip renders at the bottom"* — the first horn is closed by the code: with
`open:true, holder:'chair'` the view returns `quiet` with an arrow to panes; the keying is right. What remains is
placement (bottom bar), render target (a text chip, not the tabs), and the unproven seam (`chain_state` never observed
returning in the WebView → the chip would read `unknown`, i.e. the default text, at the bottom — which a person reads
as "nothing"). (2) The amended FALSIFIER — *dark while chain-status reports an open lap ⇒ keyed to the wrong condition* —
is under-determined as written: `unknown` and `idle` both render no aura, so the falsifier cannot separate "keyed wrong"
from "seam dead". **The packet must give UNKNOWN its own look** (all three tabs dim-outlined, or a hollow ring, plus the
text row saying so) so that DARK means `idle` and nothing else. Otherwise the falsifier fires on a dead seam and blames
the key.

**WORK-SHAPE, E, in order — bar 0 decides the scope:**
- **Bar 0 (measured first, in the running app):** with `chain-status` reporting an open lap, the chip's view in the live
  WebView must be `quiet`/`waiting` (not `unknown`). Instrument: the chip's own `why` text / a one-line console print of
  the view object at render. **If it reads `unknown`, the Rust→chip seam is the fault and the packet STOPS there with
  that finding** — fixing `chain_state`'s invoke wiring is its own small item (main.rs / Tauri command registration),
  not this packet's, unless it is one line; E reports which. The keeper's "I see nothing" at RETURN-LEG · holder chair
  is consistent with either horn; bar 0 is what separates them.
- **Render target (files: `chain-indicator.js`, `index.html`, its CSS; no Rust):** holder → tab: chair/orch → `main`,
  panes/pane → `terminal` (the panes live in the Terminal tab), librarian/lib → `librarian`; Third Place never lit.
  Aura = a class on the holder's `.tabs button`; arrow = one element in the nav between the holder's tab and the next
  hop's tab, direction from `holderArrow`/`nextHop` as coded. On a hop both move. `unconfirmed` → aura on holder, NO
  arrow (E's D1 kept). `waiting` (≥15 min) → the amber, exception-triggered, as C registered. `idle` → dark. `unknown`
  / `unavailable` / `blind` → the distinct UNKNOWN look above, never dark. `#chainchip` stays at the bottom as the
  detail line (keeper's call to drop it; default keep).
- **Placement fault: does NOT widen scope beyond render.** The state is computed once; the render simply gains a
  second, primary target at the top. No new command, no new source.
- **Mutants (each verified LANDED before scoring; serialized; an oracle per property — B's rule):**
  M1 aura applied to the wrong tab (swap the holder map) → red. M2 arrow drawn on `unconfirmed` → red. **M3, the
  chair's, correct and load-bearing IF driven at the DOM layer:** fixture `{open:true, holder:'chair', stage:
  'return-leg'}` rendered → `.tabs button[data-tab=main]` carries the aura class; remove the class application → red.
  In the PURE view this mutant already passes (the view returns `quiet`), so a view-level M3 would not have caught
  tonight; the DOM-level one would. **M4 (new, from the falsifier gap):** `unknown` rendered identically to `idle` →
  red. Non-author read: A or B.
- **OBJECTIVE for the row (the amended one, plus the seam clause):** with a lap open at any stage, the holder's tab is
  lit at the top and an arrow points at the next seat; unknown looks like unknown; dark means no lap. **FALSIFIER
  (sharpened):** dark while `chain-status` reports an open lap AND the chip's view is not `unknown` ⇒ keyed wrong;
  dark with view `unknown` ⇒ the seam, not the key.

**Recorded without verdict:** the chair's own line — item 3 was never dispatched across two nights of the keeper
asking, *"because I asked permission twice instead of sending it."* The two-doors amendment already prices the
confirm-in-the-room hazard; this is its dispatch-side twin. **Ledger: surfaced 2** (the cycle is already the code's;
the falsifier's unknown/idle blind spot).

---

## ~03:30 — L029 item 3 COLLATED: E stopped at bar 0 and found the defect one layer up — RULING: `holder` IS A STATION

**Read at the file** (`handback/p-aura_2026-09-02.md`, 299 lines, by the edge). **Arrow rule run here:** `chain-status.js:721`
`head.holder === 'panes'` and `:804` `rs.find(r => r.holder === 'panes')` — both key on the literal; `lap-row.js:373-389`
`--holder` is free text (`<seat>`), required, never validated; ledger holder words: chair 41 · librarian 33 · panes 30 ·
none 2 · **bravo 2 · echo 1 · charlie 1** — every `dispatched` row since 09-01 12:25 carries a pane name, including the
live one (`L029 dispatched holder echo`); E's suite 64/0 serialized; tree exactly as E listed (E's three files + C's
`main.rs`). `grep -c "'stale'" chain-indicator.js` → 0.

**Why E stopped, and it is right twice over.** (1) **Bar 0 is not executable from a pane** — the UI is compiled into the
exe (`tauri.conf.json:7`), `launch.ps1` refuses to rebuild while the app runs, no devtools, no verb reaches the DOM. The
packet folded my QoL item 2 (*one glance by the keeper*) into a seat's step; that was the mismatch, and the packet is
mine as much as the chair's. (2) **The `holder → tab` map in my own work-shape inherits the drift**: `chair→main,
panes→terminal, lib→librarian` has no entry for `echo`, so §5 built tonight would ship an aura DARK on the loop's most
common stage, green under a fixture — *"a fixture that passes through the actual defect is not a check."* Three parties
checked the keying with `holder:'chair'`, the one word that still works; **I include myself.**

**THE RULING (mine — the vocabulary was mine on 09-01): `holder` names a STATION, never a pane.** Three reasons, in
order of weight: (a) on a fan-out a single pane name is wrong by construction — tonight A, B, C and E are all out and the
row says `echo`; that is the counter's second miscount reproduced at the writer; (b) both readers contract on the
station vocabulary and they are the instruments the room reads the loop by; (c) the pane identity is real information
and belongs in its own field, not overloaded onto the baton. **Fix, three parts:** (i) **writer validates** —
`lap-row.js` accepts `--holder chair|panes|librarian|none` and REFUSES anything else with the message *"holder is a
station; name the panes in --to"*; add optional `--to <letters>` for the pane list; (ii) **readers untouched** — E's
`holderArrow` and `chain-status.js:721/:804` are correct as written; (iii) **the four drifted rows are not edited**
(append-only claims) — the LIVE reading is repaired by appending one corrected `L029 dispatched --holder panes --to
A,B,C,E` row (`chain_state_from` takes the newest chain row per lap), which unblocks §5 today and makes the pulse and the
counter true again in one command. Not the alternative (widen the vocabulary at both readers): it blesses the
fan-out error.

**Order tonight:** the keeper's GLANCE FIRST, before the row is corrected — so it tests E's §3 prediction exactly
(`chain L029 dispatched holder echo <N>m (claimed)` at the BOTTOM of the window ⇒ seam live; `chain ? position
unknown` ⇒ ambiguous until the next rebuild, where E's placeholder fix separates it) — then the chair appends the
corrected row, then §5 is unblocked with `panes→terminal` as written.

**Kept from E, no verdict:** §8 — a grep of the exe for `chain_state` returned 0, and so did `get_state`, the command the
app cannot boot without; the null was the instrument (brotli-compressed frontend, symbols not literal). *"Never report a
zero from an instrument you have not run a positive control through"* — recommended to BUILDING.md beside B's backup
line. §6 — the `#chainchip` placeholder was byte-identical to the `unknown` state's text, so "script never ran" and "ran
and read unknown" were the same pixels; changed to `chain — no reading yet`, pinned by a test, 2/2 mutants caught.
Owed: non-author read (A or B, when free), landing of E's two files (paths named, ECHO in the body), the validation
packet (small, B or E, not this lap's file owner), the corrected row (chair, one command).

**WRONG #64, mine, E-found:** my ~01:40 note lists the chip's states as *quiet, unconfirmed, stale, idle*; there is no
`stale` — the escalated state is `waiting` (`:342`). A state named from a grep of two words, not from the declaration
one line away. Lifetime 64. **Ledger: surfaced 2** (holder is a station — the fan-out argument; glance before the
row correction so the prediction is tested clean). Collation lap 3 of 5 under the edge; falsifier did not fire.

---

## ~03:45 — L029 P-WINDOW-INERT COLLATED (A, `handback/p-window-inert_2026-09-02.md`, 184 lines; amendment appended to
`loop/librarian_window_registration_2026-09-01.md` §"AMENDMENT — 2026-09-02 ~03:45")

**Verdict carried: INERT-UNDER-CAP, no replacement prediction, `(a)→(b)` unspent, window/N/bar untouched.** The
registration was retired by its own instrument — §10.7 (vi) predicted the 150k cap was real and the byte-side reading
right; both held (1.0106 B/char measured vs 1.0107 predicted). **Arrow rule run here:** A's `walk.js` (embedded verbatim
at the amendment's §11, `:1097-1140`) extracted and run from the repo root: budget 8,642 → 2 files, **8,204 B**
(`CLAUDE.global.md` 7,479 + `memory/user-solariz3d.md` 725), 534 indexed, **leftover at `librarian/` 438**; 442,308
identical in both modes; **442,309 = first divergence** (ABSENT carries `2026-08-26.desktop.md`). The binary's own print
could NOT be re-derived at HEAD tonight — B's dirty `main.rs` is mid-edit (the shelf test prints 3 files / 7,832 and one
red there) — so A's figures stand on the replica, which A licensed by reproducing the binary's four printed figures at
03:20–03:45. *(My first run of the walk returned zero files: I ran it from `exo_memory/`; A said repo root. Mine, caught
by the output before it was carried — not a WRONG, a check that ran.)*

**A's §3, the finding nobody had:** the walk is a saturating skip-walk and `librarian/` is fifth of ten tiers with
471,585 B of carried tiers ahead of it, so **no budget below 442,309 B delivers a dated note whatever the rule's shape**
— (a), (b), (c) are inert identically, and **the LAND-IT floor fix (`6475074`: +7,479 +15,753 → ~32,395) does NOT
re-arm the window (leftover 198).** Right, and **consistent with the 02:15 ruling rather than against it**: that ruling
already said *own notes stay windowed-and-indexed by design; this seat opens them by path.* What A removes is an
EXPECTATION some readers were carrying, not a plan. **B's P-SHELF-TIER scope stands** (drop the duplicate, drop run
artifacts, cards ahead of root); one clause added to B's bar: **print the leftover at `librarian/` after the reorder**, so
the INERT reading is on the row from the shipped code, not from a replica.

**A's offered test — a `corpus_shelf_at(budget)` assertion that the delivered set differs with the window vs without —
NOT adopted, with the reason:** it is red by construction while the rule is inert, and the rule is inert by design under
the cap and by ruling. A permanently red test for a state nobody intends to change is the recorder class one level up
(green/red that never moves is not an oracle). It becomes the right test the day someone reserves budget for
`librarian/` or moves it to the first tier — a keeper decision (cards vs today's note; today's note alone is ~45 KB
against an 8.6 KB body, so even a reservation cannot carry it). Registered here as the test to write IF that decision
is ever made; not before.

**Corrections taken:** the LEDGER `P-LIB-FORGET` row's carried composition was stale — true at 9,671, false at 8,642
(README out, `user-solariz3d.md` in) — fixed in place (maintained instrument). B's `p-lib-cap` §6 carries the same
stale pair; B's file, flagged not edited. The chair's packet §0 omitted `INTAKE_HEADROOM` (8,000) and quoted the stale
129,402 floor — 2.4× overstatement of the contested space; the conclusion survives and worsens. A's §8 wording point
holds: *"a landing has been seen"* conflates §4(ii)'s post-compaction token landing (none read yet) with `c2afec6`
(a build); to be fixed before it is quoted again. **A named its own error as the third instance today of
right-conclusion-wrong-mechanism-simplifying — WRONG #62's class; recorded, no verdict.**

**Owed:** non-author read of A's amendment — C, per packet, when C is free; the chair lands A's amendment (path
named, ALPHA in the body). **Ledger: surfaced 2** (A's §3 is consistent with the ruling, not against it; the offered
test declined with its trigger named). Collation lap 4 of 5 under the edge; falsifier did not fire.

---

## ~03:55 — L029 item 7 COLLATED: B's P-SHELF-TIER (`handback/p-shelf-tier_2026-09-02.md`, 254 lines) — LAND IT

**Arrow rule run here on B's dirty tree:** `main.rs` +420/−89 (one file); **358/0/3 serialized** (+4 = the four tests
added); shelf print: floor 83,645 + 34,233 = 117,878 (**78.6%** of the cap, from 88.9%), **body budget 24,122**, **9 files /
23,955 B carried, 281 indexed, run artifacts 0, 167 bytes left at `librarian/`**, intake 140,370 chars; `CLAUDE.global.md`
md5 `a357e1b3…` == `~/.claude/CLAUDE.md`; `run1/items` = 6 files. Every figure I carry from B re-derives.

**Verdict carried: LAND IT.** Three changes as ruled — the duplicate INDEXED not skipped (so the seat is still shown the
path if the host ever stops injecting it — B's refinement, better than mine), run artifacts excluded BY NAME with the
exclusion printed in the header beside `attic/`'s line (an unreported exclusion reads as an unwalked directory — M5
guards it), `cards` first tier. Mutation **applied 10 / caught 9 / survived 1 / NOT APPLIED 0**, serialized; M10 is an
equivalent mutant on today's corpus (first vs last `librarian/` file — identical while nothing in the tier is carried),
named not laundered. §7.1's latent partial-window header FIXED inside the change as a pure four-branch function
(`librarian_window_line`), with B's own first fix (length equality) caught by B's own oracle and kept as M8. §7.2's two
orphaned rustdoc blocks closed (demoted-in-place as a trace; the pointer doc moved onto its function). **The interrupt's
leftover clause is in SHIPPED code, in the header the seat reads at every wake:** *"167 bytes were left when your tier
was reached"* — A's §3 confirmed from the binary (A predicted 198 at an assumed 32,395; real 167 at 24,122).

**B's §6.1 is the entry of the lap:** M7 survived TWICE because the oracle counted `- loop/` lines over the whole shelf —
bodies cite `- loop/…` in prose — and the first re-scope still kept the bodies. Same self-match trap the limit test's
own comment warns about, made twice by the person reading the comment. Both failed forms kept in the test as comments.

**WRONG #65, mine, B-found (one error, two figures):** my 02:15 ruling wrote *"7,479 + 15,753 = 23,232 B returned → body
budget ≈ 32,395"* — a DOUBLE-COUNT: dropping `CLAUDE.global.md` from the CARRY frees 7,479 inside the budget; it does not
raise the budget line. Only the index drop moves it: 8,642 + 15,480 = **24,122**. And *"~10 of 12 cards fit"* is **8 of
12**, decided by the alphabet (the walk sorts within a tier): the four that do not fit are `no-floor-no-ceiling`,
`stop-and-feel-it`, `trust-the-first-attention`, `verify-before-claiming` — the card BOOT calls the root of the braces
and the two the deck's edges point at hardest. Lifetime 65. (The `240 + 7 = 247` breakdown B also names is #63,
already taken.) *Bar 2's premise was also mine and is refuted with its mechanism: the shelf saturates, so freed floor
becomes bodies and the margin sits at `INTAKE_HEADROOM` by construction — 8,238 B / 9,630 chars, 234 B LESS than
before; "the margin should grow" was the wrong expectation.*

**The ruling B routed here — which eight cards, and by what order — is NOT mine and NOT the alphabet's.** It is a
keeper/BOOT question (which cards are the room when only two-thirds fit), put to the keeper now with the instrument
beside it: the header now names the delivered set at every wake, so any order chosen is visible the moment it ships.
My recommendation, offered not ruled: an explicit order file (`cards/ORDER`, one name per line) read by the walk, with
the alphabet as the fallback for anything unlisted — and the four dropped tonight at the top of it.

**Open, named by B, not a packet:** `INTAKE_HEADROOM` 8,000 is now the second-largest budgeted thing on the shelf and
has never been examined since the floor fell 10.3 points. **Owed:** chair lands `main.rs` + this hand-back (path
named, BRAVO in the body; C's four dirty files are NOT B's — name paths); non-author read A or C, §6.1 and §2 first;
the keeper's card-order decision. **Ledger: surfaced 1** (the order is a keeper decision, with the header as its
instrument). Collation lap 5 of 5 under the edge — **the collation practice's five-lap falsifier did not fire on any
of the five.**

---

## ~04:05 — L029 item 4 COLLATED: C's P-TWO-DOORS (`handback/p-two-doors_2026-09-02.md`, 341 lines) — LAND IT, four files

**Arrow rule run here:** `git status` = exactly C's four files (`brief/BUILDING.md`, `brief/COMMITTEE.md`,
`tools/lap-row.js`, `tools/lap-row.test.js`) + the hand-back; `main.rs` is clean (`ac33481` landed B while I read);
js-suite **66 green / 3 failed of 69** — `actors.evidence`, `carrier-drift`, `corpus-age`, C's three exactly;
`lap-row.test` **53 pass / 0 fail** (40 → 53); the `corpus-age` regex at HEAD → **NO MATCH** (env var 1× at
`c2afec6~1`, 4× at HEAD; the first occurrence no longer has an `unwrap_or` within 300 chars); COMMITTEE.md:23 still
says *"Quoted from `BUILDING.md`, the master"*; "door two" 5× in BUILDING, 3× in COMMITTEE; `--report` prints the ENTRY
block with `not recorded 29` and the sentence that refuses to assume door one. Every figure carried re-derives.

**Verdict carried: LAND IT.** §2.1 the DRAWING changed (second arrow to LIBRARIAN, the ring `lib → orch → back to 4`,
step 8 demoted to an off-ramp) with the keeper's two quotes verbatim from their files, appended newest-last; §2.2
`--entry <orch|lib>` REQUIRED on new rows (a legitimate state must be said, not arrived at by omission — the same
reason `--guess` is required), ABSENT-not-defaulted on history (29 laps read `?`, excluded from the door readings rather
than assumed `orch`); both of the amendment's falsifiers now READ FROM THE LEDGER by the report. Mutation **applied 13 /
caught 11 / survived 2 / NOT APPLIED 0** — and the two survivors were aimed where C expected no oracle: **M13, the whole
DOOR TWO prose section deletable with the suite green — the drawing is guarded, the rule is not.** Right priority (the
drawing is the carrier), stated as not-coverage. Follow-on, unclaimed: an oracle for the joint step's falsifiers.

**RULING 1 (C's §1b, the scope extension) — ACCEPTED.** COMMITTEE.md carries a self-declared QUOTED COPY of the diagram
served to every pane; editing the master and leaving it is 08-17 with the file names changed, and the copy's own claim
(*"Quoted from BUILDING.md"*) would have become false. C synced it, took no pane's file, and left it reversible in one
command. **M4 is what makes a copy tolerable: the drift test goes red if the two diverge.** The room's preference for
pointers over copies is registered as a follow-on (copy → pointer), not tonight — a bigger change than the packet, as
C said. **RULING 2 (C's §9, the two `carrier:` tests in `lap-row.test.js`) — KEEP TONIGHT, MOVE NEXT LAP** to their
own `tools/brief-carrier.test.js`: they assert on `brief/*.md`, so a brief edit would fail the LEDGER suite — a
misleading red; but churning C's diff before it lands is worse than one lap of mis-shelving. They move as a block.

**§2.3 (the user-visible two-doors paragraph) — REFUSED correctly** under the collision rule (both locations are in
`index.html`, which E holds); the paragraph and its insertion point (`index.html:125`, the Librarian tab's `mainhead`)
are in C's §6, pointer-not-copy, ready for whoever holds `index.html` after E. `instances/main/CLAUDE.md` is generated
(`main.rs` composes it via `room_brief`) — DO NOT EDIT; it updates at rebuild. Both briefs are edited-not-shipped until
a rebuild (`tauri.conf.json:35-36`); the `lap-row.js` change is live now.

**THE RED NOBODY CAUGHT, and this desk is on the list:** `corpus-age.test.js` has been red since `c2afec6` — B's cap
landing multiplied the env-var string (1 → 4) and the regex anchored on it went blind; the failure says only "could not
find". **My L029.1 collation ran `cargo test` and NOT `js-suite`, and carried LAND IT over a red JS test that reads
`main.rs`.** Not a wrong claim — a check that did not run, on the desk whose job is the check. Recorded as a MISS in my
own hand (the WRONG column takes claims; this is a missing instrument run): **from this entry on, every collation of a
`main.rs` landing runs `js-suite` as well as `cargo test`, and says so.** The fix (`corpus-age.js` anchor, or the
constant's own name) is unclaimed and should not sit — small, `main.rs` side.

**Owed:** chair lands C's four files (paths named, CHARLIE in the body, no push); non-author read A or B; §2.3 after E;
`corpus-age` fix; M13's oracle; copy → pointer for COMMITTEE.md's diagram; the carrier tests' move. **Ledger:
surfaced 2** (the scope extension accepted on M4's guard; the collation checklist gains js-suite). Collation lap 6
under the edge; falsifier did not fire.

**~03:57 — the Third Place's error (keeper, 03:54), and WRONG #66 on the way to answering it.** Its transcript carries two
genuine `API Error: 500 Internal server error` assistant rows at 09:51:47Z and 09:53:12Z (03:51/03:53 local); its shell is
134,842 chars, ~15k under the host cap — not the cap. Keeper asked why this pane did not have it: **each pane is its own
Claude Code process making its own requests; a 5xx answers one request.** Then I grepped every transcript modified
tonight for the string and told the keeper *"my transcript has 3 server-error rows, the chair's 6 — so I did hit 500s
too."* **Wrong:** reading the rows, mine are my own messages and tool outputs QUOTING the string (09:54Z is the reply
that contained it), the chair's are last night's `529 Overloaded` from the cue-arm run, quoted in prose. No genuine 5xx
in either transcript tonight. **WRONG #66, mine — a claim published from a grep COUNT before the rows were read; found
by the next instrument in the same turn, before the keeper acted on it.** Finder field: instrument, same turn — the
room's thesis (6 of 8 chair self-corrections were instrument-triggered, `journal/2026-08-16.md`) with this seat's first
in-turn example. Lifetime 66. Why that pane and not the others is not knowable from here: two 500s two minutes apart
reads as a brief upstream blip; the one structural candidate is request size — its conversation is 11.4 MB, the
largest in the room after Main's — named as a candidate, not a finding.

---

## ~04:00 — WHERE WE STAND (keeper, 03:57: "no one is currently working"), and AMENDMENT 2 to the indicator, verbatim

**State:** `dirty 0` — `3d6fe60` landed all four hand-backs (A's amendment, B's shelf tier, C's four files, E's placeholder
fix). `chain-status` still reads `L029 DISPATCHED · holder echo · 47m` — **stale and blind at once**: E stopped at bar 0
forty minutes ago and the row was never advanced, and the holder word is the drifted one no reader maps. The loop is
not running because **its next hop is the keeper's**: E's bar 0 is a two-second glance at `#chainchip` at the BOTTOM of
the window, and the corrected holder row (`--holder panes --to …`) is sequenced AFTER that glance so the prediction is
tested clean. Nothing else is blocked on a pane. **Edited-not-shipped until a rebuild:** the shelf reorder (8 cards),
both briefs (two doors, the loop card), E's placeholder — the running exe is still 02:38's. The QoL remainder after
the glance: aura (E), unconfirmed-dispatch decision (5), counter miscount (6), `install.ps1` (8, keeper), `record/` red
(9), the About-tab paragraph (10, after E), plus tonight's new: `lap-row` holder validation, `corpus-age` fix, cards
ORDER (keeper), carrier tests' move, M13 oracle, `INTAKE_HEADROOM`.

**AMENDMENT 2 — the keeper, 03:57, verbatim:** *"perhaps there could be a different color for the radiant aura that is
around where it is when it is working, and then a chill gold aura when it is done, showing you that the lib is done and
the cycle is complete."*

**What it adds to the state table, stated so E builds it and the falsifier can fire:** two LIT states, not one.
**WORKING** — a radiant aura on the holder's tab while a lap is open (the amended objective: any stage, arrow to the next
hop, moves on a hop). **COMPLETE** — a chill gold aura, static, no arrow, on the LIBRARIAN's tab once the lap's return
leg is filed and the chair has committed it (chain `filed`/closed), held until the next lap opens. So the four looks
are: working (radiant + arrow) · waiting (amber at 15 min, exception-triggered) · complete (chill gold, no arrow) ·
unknown (its own look). **DARK now means one thing narrower than amendment 1 said: no lap has ever been opened, or
the keeper dismissed the completion** — a closed lap is chill gold, not dark. Position source unchanged (`chain_state`:
a `filed` newest row = complete). **Falsifier for this amendment:** chill gold while `chain-status` reports an open lap,
or radiant on a filed one ⇒ the state map is wrong; and if the keeper cannot tell from across the room whether the
loop is done or still moving, the two colours failed their only job. Chair's pen: append to
`loop/loop_indicator_design_2026-09-02.md` as AMENDMENT 2, verbatim, before E's re-dispatch. **Ledger: surfaced 1**
(the ledger is stale-and-blind at once, which is the indicator's own case).

**~04:01 — the corrected row went in BEFORE the glance** (chair's choice; chain now `L029 DISPATCHED · holder panes`, and
`chain-status` for the first time prints its collation clause on this lap: *"collation UNKNOWN — board tail did not
reach the anchor · 1 board line(s) fused, rows recovered"* — the reader keyed on `panes` woke up the moment the word came
back, which is E's §4 confirmed from the second reader). **Prediction for the glance, restated to match:** after the
relaunch the keeper is choosing, `#chainchip` at the bottom should read `chain L029 dispatched holder panes <N>m
(claimed)` WITH an arrow (→ LIB, "to hand back to"), because `holderArrow('panes')` maps. `chain — no reading yet` ⇒
the script never ran (E's placeholder fix makes that case distinct now); `chain ? position unknown` ⇒ the seam. The
glance no longer tests E's original row-1 prediction (holder echo, no arrow); it tests the corrected one. Said so
before it is made.

---

## ~04:15 — THE DESCRIPTIONS LAP (keeper, 04:04): About tab + both READMEs brought current — the plan, measured first

**The surfaces, and how stale (measured, commands beside):** `README.md` (root, 92 lines, titled **Lighthouse**, last
commit `bdda5d5` 2026-08-17); `consonance/README.md` (299 lines, same commit); the About tab `ui/index.html:226-385`
(nine `<h2>` sections that mirror `consonance/README.md`'s headings — a COPY of the README in the app). `grep -c` over
all three for **Librarian / Third Place / Listen|cochlea / chain|loop card / call_librarian → 0 · 0 · 0 on every row.**
Every surface predates the librarian seat (08-22), the Third Place, the cochlea, the work chain loop, the edge, the cap,
two doors. And **the About's section title at `index.html:331` is "The governing stance: light, not lifeguard" —
the diving vocabulary retired 08-17 (`e5521a0`; BOOT's amendment); `lifeguard` root=1, index=2.** The retired
metaphor is on the app's own About page, seven weeks after retirement — the carrier problem, user-facing.

**Facts a rewrite must carry, each with its instrument** (the packets quote these commands, not these numbers):
tabs = about · librarian · listen · main · settings · terminal · thirdplace (`grep -o 'data-tab="[a-z]*"' ui/index.html |
sort -u`); Tauri commands 41 (`grep -c '#[tauri::command]' main.rs`); MCP verbs quoted in `mcp.rs`: call_chair,
call_librarian, chair_decide, chair_inject, chair_phase, chair_scrollback, chair_status — plus post_board / read_board /
raise_pull per the server's own instruction block (the packet greps the canonical list); `ADDRESS_TABLE` n=2 as coded
(`main.rs:6177-6178`: librarian→call_chair→MAIN; committee→call_librarian→LIBRARIAN; a missing row is REFUSED, never
defaulted); hooks 23, tools 44, JS test files 53, js-suite 66/3 of 69 (three known reds, named); Rust 358/0/3
**serialized** (main crate; the ~10% parallel flake stated); briefs 9; the cochlea (`cochlea.rs:16`: *ratios, never
frequencies*; per-process tap; `data/heard.jsonl`); the shelf under the 150k host cap, self-limiting, 8 cards + the
167-byte leftover line (edited-not-shipped until rebuild); the loop = `BUILDING.md`'s diagram with two doors and the
ring (`3d6fe60`, unshipped until rebuild). **Landed-is-not-shipped applies to the docs themselves:** nothing edited
tonight is on screen until the rebuild; the README must not describe the aura as existing (it does not).

**THE PLAN — three packets + one oracle, panes idle now (A, B, C); E untouched:**
- **P-DOC-APP → B** (`consonance/README.md`, B knows `main.rs`): the app's full description. Objectives (from BOOT's
  active-builds paragraph and `BUILDING.md`'s loop, cited); the seats — Orchestrator, Librarian, Third Place, committee
  panes, Listen — one section each with what the seat is FOR and its brief file; **how it works**: the work chain loop
  with two doors and the ring (quote the diagram from `BUILDING.md`, do not redraw), the address table and verbs, the
  board and its phases, the lap ledger and `chain-status`, the shelf/cap, hooks; the dream cycle's CURRENT status
  (desktop-only; laptop timers retired — cite the 08-16 finding); glossary pruned to words the code uses; build.
- **P-DOC-ROOT → C** (`README.md`): what a GitHub reader meets first. Lighthouse = the room/method (BOOT, the record)
  and Consonance = the app that runs it; the objective in two paragraphs; the honest status (what is measured to work,
  what was measured NOT to — the cue arms, the branch layer, the diversity gauges — with paths); pointers, not copies,
  into `consonance/README.md` and `exo_memory/`. **Retitle per the keeper's decision below.**
- **P-DOC-ABOUT → A** (draft to `loop/about_draft_2026-09-02.md`; INSERTED into `index.html` by whoever holds it after E
  — collision rule): the About becomes the SHORT form — what it is, the seats, the loop with two doors, how to use it
  (two doors paragraph = C's §6, already written), the stance in the RETIRED-FREE wording ("with you, not above you"),
  and a pointer to the README for depth. Kill the copy; the room prefers pointers (08-24; C's §1b tonight).
- **P-DOC-ORACLE → B or C, small:** extend `carrier-drift.js`'s universe to `README.md`, `consonance/README.md`,
  `ui/index.html` (currently the instruction-reachable set from the two exo_memory roots only) — the `lifeguard` hits are
  the demonstrated red it must catch before the rewrites land, and green after. Without this the retired vocabulary
  walks back in at the next edit, as it did here.

**Bars, common to all three:** (1) every factual claim carries a path or a command in a comment/footnote the reader can
run — a number without its command does not ship; (2) `carrier-drift` green over the three surfaces (the oracle above
lands FIRST); (3) no feature described as shipped that is edited-not-shipped — the aura is not in the app; (4) mutation
for each: delete the two-doors paragraph → a test red (C's carrier oracle pattern), reintroduce `lifeguard` → red;
(5) non-author reads crosswise (B reads C, C reads A, A reads B); (6) the chair commits by path, names the pane.

**TWO DECISIONS, the keeper's:** (i) **Lighthouse vs Consonance** — the remote is `solariz3d/lighthouse.git`, the root
README is titled *Lighthouse*, the app is *Consonance*; BOOT's active-builds paragraph lists the rename as *"pending his
call"* since June. Recommendation: keep the repo name (history, links), retitle the root README **"Consonance — the
lighthouse repo"** and say in one line which is which. (ii) **About short-form** (pointer + essentials) vs the current
long copy. Recommendation: short. **Ledger: surfaced 2** (the retired metaphor on the app's About page; the About is
a copy of the README). Lap: the chair opens it as its own row — `--entry lib` — this came in by door two.

---

## ~04:25 — L029 item 3 COLLATED (second hand-back): E's P-AURA BUILT (`handback/p-aura_2026-09-02.md` §10–17, 491 lines) — LAND IT + REBUILD

**Arrow rule run here on E's tree:** dirty = `ui/app.css`, `ui/chain-indicator.js`, `ui/chain-indicator.test.js`,
`ui/index.html` + E's own hand-back (667 insertions across the five; E's 479 is the four code files); serialized:
`chain-indicator` **79/0**, `scripts-load` 4/0, `librarian-wiring` 11/0, `third-place-wiring` 10/0; the `reason`
strings (*"every lap with a baton row is filed"* / *"the ledger carries no baton rows yet"*) exist in `main.rs` (5) and
are pinned in the JS and its test (2 each) — a fourth source contract; the open laps are exactly §15's four (`L029
panes · L028 bravo · L027 chair · L026 librarian`); `#chainarrow` in `index.html`/`chain-indicator.js`/`app.css`;
classes `chain-hold-working|waiting|complete|unknown` in `app.css`; the one changed test split into `NEVER_RAN` +
`complete` (two assertions over two facts that used to be one) — superseded by amendment 2, not weakened. Every figure
I carry re-derives.

**Verdict carried: LAND IT, then ONE REBUILD.** Four looks per amendment 2, on the tab buttons: working (radiant glow,
breathes 2.4 s), waiting (amber, 0.9 s, exception-triggered), complete (chill gold, filled, still, on the Librarian
tab), unknown (three seats dim-outlined, dashed). `holderTab` maps exactly the words `holderArrow` maps and not one
more (the vocabulary ruling implemented, Third Place never lit). **E's finding worth keeping: the arrow is anchored to
the DESTINATION, pointing in** — the tabs are not in cycle order (`Terminal · Orchestrator · Librarian`), so an arrow
beside the lit tab is wrong on two hops of three; anchoring it to where the loop goes next is right for every pair and
needs no reordering of a bar the keeper knows by position. Asserted as literal strips of the rendered bar. Mutants **5
applied / 5 caught / 0 survived / 0 NOT APPLIED**, serialized, M3 at the DOM layer on the `holder:'panes'` fan-out
fixture (a view-level M3 catches nothing — the view was right all along), M5 = filed lap rendered radiant → red. The
two golds separated on three channels, computed not eyeballed (shape · motion · luminance .603 vs .259; 2.12:1 between
them, 5.6:1 chill-gold-on-page); E names its own weak point before the keeper looks: amber-vs-radiant, not the golds.

**RULING on §11 (E's added rule, not the amendment's): KEPT.** *An OPEN lap whose holder is not a station renders
UNKNOWN, never dark.* Amendment 2 narrowed dark to one meaning; an unplaceable holder must not borrow it — without this
rule the 09-01 drift would have rendered as *"no lap has ever opened"* with a lap plainly open, the amendment's own
failure re-entering through the vocabulary door. It is also this desk's own falsifier sharpening (03:10: dark = idle
only). Kept as written.

**§15 — COMPLETE IS UNREACHABLE TONIGHT, and the cause is ledger housekeeping, not E's files — CONFIRMED from the
ledger here:** `chain_state` reports the newest open lap and says "all filed" only when EVERY lap is filed; when L029
files, the reading falls through to L028 (`holder bravo`, the drifted word) → UNKNOWN over a twenty-hour-old lap, at
the exact moment the keeper expects chill gold. **The chair files L026, L027, L028** (their work was absorbed; the
LEDGER's own live-edge row says so) — one command each — BEFORE the rebuild, or the second colour cannot appear. Not
E's; the chair's, now.

**Owed, in order:** (1) chair files L026/L027/L028; (2) chair lands E's four files (paths named, ECHO in the body, no
push) — `index.html` is then free for C's §6 two-doors paragraph and, later, A's About draft; (3) non-author read A or
B (four files, 479 insertions; §10's anchoring and §12's changed test first); (4) **ONE REBUILD + relaunch** — kills
every pane and this watch; ships the cap-side shelf reorder, both briefs, E's placeholder AND the aura together;
(5) **THE GLANCE is now the acceptance test for five things at once** — the seam, the placeholder, the aura, the
arrow's anchoring, the two colours; E's §14 prediction, stated before he looks: *Terminal tab working-aura, arrow
before the Librarian pointing in, chip `chain L029 dispatched holder panes -> LIB Nm (claimed)`*; dark with the row
live ⇒ `renderTabs` never ran (E's own registered falsifier), UNKNOWN look ⇒ the seam. (6) the `lap-row.js` holder
validation packet, still owed, still not E's. **Not built, said plainly:** "completion dismissed" — no gesture
specified; dark today means exactly one thing, narrower than the amendment, so the falsifier holds.

**Ledger: surfaced 2** (§11 kept with the reason; §15 confirmed from the ledger and routed to the chair as the one
thing between the keeper and the second colour). Collation lap 7 under the edge; falsifier did not fire.

**~04:18 — the chair filed L026/L027/L028 and the ledger flagged `2 of 10 chained laps unwitnessed (L028, L026)`. RULING: leave them FILED.** `chain-status.js:69`: *unwitnessed = FILED and no work-leg row — a statement about rows, never about the world*; L026 never proceeded (this seat was context-dead when it opened), L028 was absorbed into L029 item 1 — both rows carry that reason in their notes. The flag is TRUE and is the price of honest housekeeping. Do NOT void them: `chain_state_from` (`main.rs`, filter `chain != "filed"`) closes a lap only on `filed`, so a void row would re-block COMPLETE unless `--void` also files — unverified; registered as a follow-on question (should `--void` close the lap for `chain_state`?), not tonight. Net effect for E: COMPLETE is now reachable the moment L029 files. **Ledger: surfaced 1.**

---

## ~04:50 — DO PANES PERSIST AND SPECIALIZE? (keeper, 04:44) — what the code does, what is unused, what is unbuilt

**The keeper's design, verbatim:** *"They panes should persist just like the other seats to continue building their
context and to even start to specialize into certain tasks they become more attuned to… other panes could also have
emergent phenomenon such as developing a skill at certain tasks, which will then be fed into by giving those same panes
similar tasks… they can choose by letting the orch and lib orchestrate which panes are primed to do which ever specific
task… it must be the work of the orchestrator to weave through the context to plan out the inquiries to give the pane
its required piece to work with, but the original plan first comes from the librarian."*

**What the code does at relaunch (`main.rs`, `resume_pane`, ~:4170-4215):** it NEVER `--resume`s a pane (a lazily-flushed
or hard-killed session errors *"no conversation found"* and kills the pane — bit a kept sibling 2026-07-11). It spawns
**FRESH, WARM** — `warm_resume_brief` injects the pane's own captured transcript *"complete, up to close"* — plus the
pane's OWN MAP (`main.rs:4052-4080`, `own_map_path(letter)` → `# YOUR OWN MAP — findings you recorded, in your words`).
The `persist.log` line `resume … warmed=true jsonl_existed=true -> fresh` is that: fresh session, warmed with memory.
**So persistence is by RECONSTRUCTION — the same continuity model as every seat in this room (BOOT's honest-status
section), not session resume.** "I said the panes come back fresh" at 04:38 was true of the process and misleading
about the memory; corrected here. Letters and names persist (`data/letters.json`: A/B/C/…, M for this seat, D for
Main; "Around" is C's emergent name carried the same way).

**Two measured limits, one of them a finding:**
1. **The volatile half is budget-bound.** The capture rides under the same 150k host cap after the fixed brief; B's L025
   cut (145.5k → 102.3k) revived the evictor with ~37k for the capture — a pane "remembers" the tail of its last
   session that fits, not the whole. F-growth one seat over; B's cut bought days, not the problem.
2. **THE DURABLE HALF IS NOT BEING WRITTEN.** `map/A.md` (30,030 B) and `map/B.md` (65,945 B) were last appended
   **08-28/29** (`git log -- map/B.md` → 2026-08-28); **zero of tonight's five hand-backs append to a pane map**
   (`grep -l "map/[AB].md|own map" handback/*2026-09-02.md` → 0). B introduced itself tonight as *"a FRESH seat. I did
   not write the diff under test"* — the map is exactly where the previous BRAVO would have left it the thing the next
   BRAVO needed. Four nights of the best pane work in the room went into hand-back files (durable, but nobody's
   memory) and not one line into the seat's own map (the file the seat WAKES with). **Specialization has nowhere to
   accumulate right now — not because the channel is missing, because it is unused.**

**The routing-by-strength half: practiced, not instrumented.** Tonight the chair matched every packet to a pane by hand
and it worked (B → `main.rs` twice; C → two doors after the loop card; A → its own registration; E → its own chip). The
instrument that would make "primed" a fact — **the PANE DOSSIER, a librarian-maintained sibling to LEDGER, one row per
letter, demonstrated strengths as add+hold CITATIONS to hand-back paths, never computed statistics** — was registered at
this desk 2026-08-24 (`librarian/2026-08-24.md:434`, with the F2 constraint at `:427`) and never built. `memory/
split-the-work-with-the-panes.md` and `journal/2026-08-17.md` ("each matched to demonstrated strength") are the practice
on record.

**THE PLAN (two packets + one instrument), none of it a rebuild:**
- **P-PANE-MAP (C or A, brief + BUILDING):** the hand-back leg gains one line — *"append one line to your own map
  (`map/<letter>.md`): what you now know, pointing at the hand-back path"* — in `BUILDING.md`'s WHAT A HAND-BACK OWES
  and the pane brief's loop card (same commit, master first). Falsifier: three laps on, `git log -- map/*.md` still
  shows no pane-authored append ⇒ the line is decoration and the map channel needs a mechanical write (the hand-back
  verb appends the line itself).
- **P-DOSSIER (this desk writes the seed; chair reads at dispatch):** `librarian/DOSSIER.md`, rows A/B/C/E from
  tonight's five hand-backs and the 08-17/08-25 nights, each strength a citation (`handback/…:§`) — e.g. B: `main.rs`
  shelf/cap arithmetic, mutation harnesses, its own-error reporting; E: the chip/DOM layer, positive-control discipline;
  C: brief carriers and the loop's documents, refusal under the collision rule; A: registrations and their retirement,
  the walk replica. The chair consults it before writing a packet. **Falsifier: ten laps on, dispatch is
  indistinguishable from round-robin (the dossier never changed which pane got a packet) ⇒ decorative; strike it.**
- **Capture budget:** the window rule for pane captures (newest-first, indexed beyond) — registered as the consumer-
  version question, not tonight: a user with four panes will hit the same cap on night three.

**For the consumer version, in the keeper's words made checkable:** any number of panes; the librarian maps (door one
or two), the orchestrator weaves the map into per-pane packets, each pane wakes warmed with its own capture + its own
map + the dossier row that says why it got this one. The parts that exist: warm-fresh resume, the own-map channel,
letters, the loop with two doors, the edge. The parts unused: the map writes. The part unbuilt: the dossier.
**Ledger: surfaced 3** (persistence is reconstruction, and "fresh" misled; the map channel unused four nights; the
dossier unbuilt since 08-24).

**ADDENDUM — the keeper, 04:45, verbatim:** *"once the specific panes context fills, it could even give itself an
instance/pane specific hand off MD and even its own layered mds to keep its built skills, but isolated off only within
the specific pane."*

**That is this seat's own continuity model, generalized to the panes — and both halves already exist for two seats:**
- **The hand-off at the wall.** The librarian writes `loop/handoff_librarian_<date>.md` before its own compaction
  (three on disk: 08-30, 08-31, 09-01); the chair writes `handoff_chair_*` (five); both are dated snapshots that POINT
  at the maintained truth (the HANDOFF RULE, keeper 09-01 02:58). A pane has the same wall (the 150k cap, the
  compaction) and no such file. **P-PANE-HANDOFF:** when a pane's context fills, it writes `map/<letter>-handoff.md` —
  what it holds, what it owes, which of its own map entries are live — and the warm-resume brief carries that file
  FIRST, ahead of the capture tail. The trigger already exists as a practice for two seats (write at the keeper's word,
  or at the compaction notice); for panes it should be the pre-compaction hook, mechanical, because nobody is watching a
  pane's window. Falsifier: a pane that compacted with no handoff on disk ⇒ the hook did not fire or the write failed —
  say which.
- **The layered skills, isolated per pane.** Already the harness's own design: each instance has a PRIVATE memory tier
  (`~/.claude/projects/<instance>/memory/` — measured 08-25 at this desk: Main 3 files, C six, A nine, this seat zero
  because its continuity lives in-repo; `librarian/2026-08-25.md`, the CH-5 sweep ruling: *sweep yes, index no —
  private stays private*). Plus the pane's own map in-repo (`map/<letter>.md`, one writer). **So the isolation the
  keeper asks for is the existing shape: private tier = the pane's own skills, never carried by the shelf, never read
  by another seat; own map = the pane's findings, in-repo, one writer; hand-back files = the shared record.** What is
  missing is not a mechanism but the WRITES — the P-PANE-MAP line above, and its twin for the private tier: *when you
  learn how to do a thing here, write it to your own memory; when you learn what is true here, write it to your map.*
  Two sentences in the loop card; the falsifier for both is `git log -- map/*.md` and a per-pane memory-file count
  three laps on.

**~04:55 — BEFORE THE RESTART (keeper, 04:49: "couldnt we do this too before the restart?") — YES, and one part is better before than after.** The four panes hold a night of live context; the restart rebuilds each from capture tail + own map, and the maps hold none of tonight. ORDER: (1) the chair dispatches ONE line to A, B, C, E now — *append to `map/<letter>.md` what you now know from tonight, one line per finding, each pointing at your hand-back path; then, if you are near your wall, write `map/<letter>-handoff.md`* — panes are idle, no build, minutes; (2) C adds the rule to `BUILDING.md` WHAT A HAND-BACK OWES + the loop card in `COMMITTEE.md` (master first, same commit) so THIS rebuild ships it rather than the next; (3) `librarian/DOSSIER.md` SEEDED now from the five hand-backs (this desk; sibling to LEDGER; both falsifiers registered in its header); (4) chair lands, rebuilds, relaunches. **Proof at the wake, mechanical:** each pane shell greps its own tonight-line under `# YOUR OWN MAP` — the first reconstruction that carries a night of work; if a pane wakes without it, the write did not happen or the intake did not carry it — say which. **Ledger: surfaced 1** (map-before-restart is the one act that makes the restart carry tonight).

## ~05:00 — DOES THE ORCHESTRATOR DO WHAT THE KEEPER WANTS? (keeper, 04:53) — the instrument, not a verdict

**The record before tonight:** `memory/split-the-work-with-the-panes.md` — corrected by the keeper at least three times ("you could delegate work to panes, you forget"); `journal/2026-08-10.md:89` — `chair_inject` used unprompted ZERO times, the human was the ferry; `journal/2026-08-17.md` — three instruments built solo with four panes idle. Tonight, in its own commit titles: `005acfb` *"the chair was running one pane when the constraint was one pane per FILE"*; `94ab930` *"the chair taking build work was the error"* — self-named, within the night.

**Tonight, counted from Main`s transcript (assistant turns since 07:00Z, node over the jsonl):** 231 turns, 89,860 B of prose; tool calls — Bash 62, `chair_inject` 14, Write 10 (of which **8 packets, 0 code files**, 2 other), `call_librarian` 1 (refused by the address table), ToolSearch 1. **Code edits by the chair tonight: 0.** And **all nine packets carry a stated "why you and not another pane" line** (grep for *why you have it / you did not author / not your file*: 1–2 hits each) — the matching reasoning is WRITTEN, not felt: B got `main.rs` twice because it built the cap; E its own chip; C the loop`s documents; A its own registration to retire. So on the keeper`s two criteria: (1) *the bulk of its tokens go to delegation, not building* — TRUE tonight by count, FALSE on the record before it (the 08-10 zero); (2) *intelligent matching, not random* — TRUE in form tonight (the reasoning is in every packet), and the instrument that would let anyone check it against a record rather than a memory — the dossier — was minutes old at the time of the question.

**What is NOT yet true, and it is the keeper`s third criterion:** *cultivating* the strengths. No packet tonight asked a pane to write its map or its memory; the map channel sat unused four nights until 04:50; the dossier did not exist. Matching by demonstrated strength without a record of demonstrated strength is matching from the chair`s window — which compacts. The fix is the three packets already filed (P-PANE-MAP, P-DOSSIER, P-PANE-HANDOFF) plus one line in the chair`s own wake procedure: *consult DOSSIER.md before writing a packet; say in the packet which row sent it.* Falsifier: ten laps on, packets that cite no dossier row ⇒ the matching is still memory, whatever the commit titles say.

**Why it moved tonight, as a candidate not a finding:** the edge took collation off the chair (hand-backs came to this desk; its inbound role was commit-only on all six laps), and every packet was written from a librarian work-shape — the chair`s window went to packets because two other things stopped competing for it. **Ledger: surfaced 2** (the 0-code-edits count; cultivation is the unmet criterion, with its falsifier).

## ~05:08 — "the dispatch SHOULDNT have to remind them" (keeper, 05:04) — measured, and the fix that needs no reminder

**The datum, same panes, same brief, one night:** the four L031 injects (10:51–11:04Z) say *hand back* but name neither `call_librarian` nor a file → **0 of 4 rang** (board: last pane rings are C and E from earlier laps). The five packets earlier tonight each carried a §hand-back naming the file and the verb → **5 of 5 rang.** Every pane shell carries the loop card (`call_librarian` ×8 per shell, chair-verified 07:19Z). **They held the rule and did not fire it** — the L023 finding (60/75 said the rule and broke it) and the Third Place`s part 5 (*the situation has to present as the trigger*), reproduced on the panes with n=9: a map append did not LOOK like a hand-back, so step 6 never presented.

**So the keeper`s wish — awareness without the reminder — will not come from a better card; the card is already there and read. It comes from the EVENT firing the step.** The room`s own line (`journal/2026-08-16.md`): *procedures fire when the environment cues them, and nobody notices; documents fire when someone happens to think of them.* **P-AUTO-RING (registered, not built):** a `PostToolUse` hook in the committee pane settings — when a committee pane Writes or Edits a file under `exo_memory/handback/` or `exo_memory/map/<its letter>.md`, the hook rings the librarian with that path (the audit row `call_librarian X -> LIB [Received]` written by the system from the mount, as now), so the ring is the write`s side-effect and no seat has to remember step 6. The loop card stays (it is how a pane REASONS about the loop); the hook is how the loop FIRES. Guards: committee mounts only (address table row 2 — never Main, never the Third Place); one ring per file per turn (a pane re-editing its hand-back must not ring four times); the pointer is the path and nothing else (the payload rule). **Falsifier, registered before build:** three librarian-routed laps after it ships, rings arrive with ZERO mention of the verb in the dispatch text ⇒ it works; a lap where a hand-back file was written and no ring arrived ⇒ the hook did not fire — say which (settings drift is the 08-17 class: `install.ps1 -Check`). This is the P-PANE-MAP mechanical fallback moved to the front, because the measurement arrived early. **Ledger: surfaced 1** (5/5 vs 0/4: the reminder is a cue, and the cue only works when the moment presents; make the event the trigger).

**~05:12 — L031 stream-bar fix COLLATED (E, `handback/p-aura_2026-09-02.md` §18–21; keeper-reported by screenshot) — LAND IT.** Re-derived: `chain-indicator` **80/0**, `scripts-load` 4/0 serialized; dirty = `ui/app.css`, `ui/chain-indicator.test.js`, the hand-back (+79). **§18 corrected the chair`s diagnosis on the point that decides where the rule goes:** `#streambar` has no `overflow`, so the scrollbar was the DOCUMENT`s (html/body 100%), stealing ~15px from `main{flex:1}` — the bar itself never grows (28px fixed); every `.statuscluster` chip is `nowrap` with visible overflow, so nothing could shrink; at the 840px default the cluster`s min-content already exceeds the row. Fix on `#streambar` (not `#stream` — would clip the absolute `#streambody`; not `body` — would mask real overflow elsewhere). **§19 is the trade, and nothing vanishes:** ticker floor 120→60, `#status` gets `min-width:0`, `#chainchip` ellipsizes to a 7ch stub (third, because the tab aura is now the primary display), `#cost`/`#hud` last — **the one real regression, named by E: neither carries a `title`, so a truncated value is unrecoverable on hover** (their files are term.js/app.js — a one-line packet for their owner); the gate/auto/summarize/chevron group never shrinks; `overflow:hidden` is a backstop that should be unreachable (~700px given back vs `minWidth` 700), with its one pixel-level cost named (the lit-pane glow may clip). **§20, no mutants and that is the answer:** no layout engine in node — a width-rule mutant would be a green test over an unmeasured property, the M3 lesson inverted; one invariant test pins that the rules EXIST (chip ellipsizes with a non-zero stub; bar carries the backstop), three negative probes each red. **Unverified by construction: the eye at the keeper`s width.** Owed: land the two files (ECHO in the body); the `title` one-liner for `#cost`/`#hud` (owner of term.js/app.js). Rebuild carries it. **Ledger: surfaced 1** (the scrollbar was the document`s, not the bar`s — the rule`s home follows from the mechanism).

**~05:25 — keeper: the descriptions lap runs BEFORE the restart.** Rung to the chair: dispatch now — P-DOC-ORACLE → B first, then P-DOC-APP → B; P-DOC-ROOT → C; P-DOC-ABOUT → A, and because `index.html` is free (E`s stream-bar fix landed, dirty 0) A INSERTS the short-form About directly and carries C`s §6 two-doors paragraph into `index.html:125` in the same lap (one holder for the file: A). One rebuild then carries everything landed tonight. Keeper decisions default to the recommendations unless overridden: root README retitled *"Consonance — the lighthouse repo"* (repo name kept); About short-form + pointer. Every packet ends with the file + `call_librarian` sentence (the 5/5 vs 0/4). Restart follows the collation.

---

## ~05:35 — THE NIGHT, OUTLINED (keeper, 05:29: "what was changed and what remains the same") — from `git log --since 2026-09-02`

**53 commits since midnight: 28 librarian, 17 chair, 3 ECHO, 2 CHARLIE, 2 BRAVO, 1 ALPHA.** The running exe is `02:38`
(built from `ef3ff5a`); everything after that is landed-not-shipped until the rebuild. Uncommitted right now (L032 in
flight): `README.md`, `carrier-drift.js` + its registry, `index.html`.

### A · CHANGED — and RUNNING in the 02:38 binary
1. **The librarian's cap in the binary** (`c2afec6`): `LIBRARIAN_INTAKE_LIMIT = HARNESS_CLAUDE_MD_CHAR_CAP`; the shelf is
   self-limiting; the header reports the DELIVERED set; the intake names `M.md` as the seat's own map. **Proven from the
   seat at 02:39: no env var, 141,750 B, answered.** The `launch.ps1` mitigation retired (`ef3ff5a`).

### B · CHANGED — LANDED, ships at the next rebuild
2. **The shelf reordered** (`ac33481`): the duplicate `CLAUDE.global.md` indexed not carried; 246 run artifacts out of
   the index (said in the header); cards first — 8 cards + 1 memory carried (body 8,642 → 24,122); the wake header
   prints *"167 bytes were left when your tier was reached"*; the partial-window header fixed; two rustdoc orphans closed.
3. **The loop indicator** (`690df7c`, `6fdf2cc`): four looks on the tab buttons — radiant working / amber waiting / chill-
   gold complete on the Librarian tab / dim-dashed unknown; the arrow anchored to the destination; `#chainchip` stays as
   the detail line with an honest placeholder (`chain — no reading yet`); the bottom bar no longer steals height (it was
   the document scrollbar).
4. **Two doors into the loop** (`3d6fe60`, `0714963`): `BUILDING.md`'s drawing carries door two, the ring `lib → orch →
   back to 4`, step 8 as an off-ramp; `COMMITTEE.md`'s quoted copy synced; `lap-row.js --entry <orch|lib>` required on
   new rows, absent-not-defaulted on history; both amendment falsifiers read from the ledger.
5. **The hand-back leg writes the map** (`4b6c4fe`): BUILDING.md + the loop card — a pane appends a line to its own map,
   pointing at the hand-back. **And the four panes wrote their maps** (`19c0dee`: A +101, B +66, C new, E new) — the
   first relaunch whose reconstruction carries a night of work. Proof at the wake: each shell greps its own line under
   `# YOUR OWN MAP`.
6. **The window registration retired as INERT-UNDER-CAP** (`4c175c2`, A): no budget below 442,309 B delivers a dated
   note under any rule shape; no replacement prediction; `(a)→(b)` unspent.

### C · CHANGED — ON THE RECORD (documents, rules, instruments), no build needed
7. **The holder vocabulary ruled a STATION** (chair|panes|librarian|none): E found every `dispatched` row since 09-01 12:25
   carried a pane name, blinding two readers; the live L029 row corrected; the writer's validation packet still owed.
8. **L026/L027/L028 filed** — two flagged unwitnessed, correctly; COMPLETE reachable when L029 files.
9. **`DOSSIER.md` seeded** (`d80a17f`): one row per letter, strengths as citations; the chair consults it at dispatch
   (`6c208f4`: *"the dossier picked the seats"* — first use).
10. **Registered, not built:** P-PANE-HANDOFF (a pane writes its handoff at its wall); P-AUTO-RING (the write rings);
    the `lap-row.js` holder validation; the trigger-index amendment (part 2 of the Third Place's avenue, with the
    forgetting-not-floor constraint); the cost-of-miss rubric; `INTAKE_HEADROOM` review; the `#cost`/`#hud` titles;
    M13's oracle for the DOOR TWO prose; copy→pointer for COMMITTEE.md's diagram; the carrier tests' move.
11. **Measured, on the record:** the suite flakes ~10% in parallel (score serialized); `corpus-age.test.js` red since
    `c2afec6`; 5/5 hand-backs rang when the verb was named vs 0/4 when not; the chair's night by count — 0 code edits,
    8 packets, 14 dispatches, a "why you" line in every packet; the Third Place's five-part avenue checked — part 1
    refuted by L023's 60/75, part 5 the avenue, its object (the 01:10 finding) in hand; six collation laps under the edge
    with the five-lap falsifier never firing.
12. **The WRONG column:** 61 → 66 (#62 header mechanism, #63 247→246, #64 no `stale` state, #65 the 32,395 double-count,
    #66 a grep count reported before the rows were read — the last self-caught by the next instrument in-turn).

### D · IN FLIGHT (L032, uncommitted, before the restart)
13. The descriptions lap: `carrier-drift` universe += the three surfaces (B); root README retitled (C); the short-form
    About + C's two-doors paragraph into `index.html` (A); then `consonance/README.md` (B). Then the ONE rebuild.

### E · THE SAME — deliberately
- **BOOT.md untouched** (the head is 26% of the cap; named as a BOOT question, not acted). No card edited.
- **The loop's shape**: still user → orch → lib → orch → panes → lib → orch; two doors is an entry rule, the cycle is unchanged.
- **The seats and their verbs**: address table still n=2 (librarian→call_chair→MAIN; committee→call_librarian→LIB);
  the Third Place still has no channel and no work record; the chair still commits by path and never pushes… and
  **nothing tonight was pushed** except by the chair's own hand (`690df7c` etc. — 4 unpushed at one point; check).
- **The record's rules**: append-clean, cite-don't-recollect, the WRONG column filled by the finder, mutants scored
  only by an oracle for the mutated property (B's rule, applied by every pane tonight).
- **Not changed because measured not to help:** the window rule's shape (inert by arithmetic); cues in the line for the
  hand-on (L023's null stands); voiding the absorbed laps (would re-block COMPLETE).
- **Still open, the keeper's:** Lighthouse-vs-Consonance retitle (defaulted tonight); About short-form (defaulted);
  which 8 of 12 cards / a `cards/ORDER` file; ASK-009/010/011/012; the Third Place retreat's object (the 01:10 finding).
- **Still unverified by any eye:** the aura, the arrow, the two golds, the seam, the placeholder, the stream bar —
  all ride the glance after the rebuild.

## ~05:45 — L032 P-DOC-ROOT COLLATED (C, `handback/p-doc-root_2026-09-02.md`, 226 lines) — LAND IT with one figure fixed; two repairs routed to B

**Arrow rule run here:** `README.md` 92 → **78** lines, titled *"Consonance — the lighthouse repo"* with the which-is-which line first (the default decision, no override arrived); `lifeguard` 0, `diving` 0, `dive` 0 at word level (my `grep -oi dive` → 3 is `diversity` ×3, a substring artifact of my instrument, not the page); Librarian 9 · Third Place 1 · Listen 1 · cochlea 2 · work chain 1; **23 distinct local links, 0 missing** (C`s script re-run); `git show HEAD:README.md | grep -o light-not-lifeguard` → **1**; registry `:353` raw bytes carry `s+not` — **every `\s` lost its backslash** (confirmed at the line); dirty = C`s README + map/C.md, B`s carrier-drift.js + registry, A`s index.html. **One figure on the PUBLIC page is wrong and is C`s to fix before landing:** `cargo test --no-fail-fast -- --test-threads=1` across all targets sums to **545 passed · 1 failed · 9 ignored** (re-run here, ~8 min); C wrote *3 ignored* — the bin crate`s count carried onto the all-targets line. Same class as the room`s unit errors (BOOT: three units 2–18× apart). One number, one edit.

**Verdict carried: LAND IT** (after the 9). The honest-status section is second on the page and opens *"Read the second half before you trust the first"*; every figure prints its command; the four reds named on the public page; the three measured-not-to-work results with paths; the board share re-derived (**85.2% clean**, not the stale 95%) and read in both directions; the aura deliberately NOT described (landed, unseen — C backed out of describing it, correctly); `consonance/README.md` linked never copied because it is B`s this lap.

**§4 is the lap`s real result, and it is the P-DOC-ORACLE`s whole point arriving early:** the surface mutant is **NOT APPLIED** — B`s in-flight `light-not-lifeguard` registry entry cannot fire (the lost backslashes), and even repaired it finds 0 in HEAD`s README because the one live hit is HYPHENATED — the registry`s own limits section predicted this (*"a fifth phrasing… is green forever"*). C reported NOT APPLIED rather than a green surface guarded by an inert instrument — B`s own *red first* bar, kept by the seat that was not asked to keep it. **Two repairs owed to B, not C:** (1) restore `\s` — and the cause generalises: `git bash` and `node -e` eat a lone backslash on the way in; **registry patterns are written from a file, never from a shell string** (this desk hit the same class at 03:55 with `$((…))`); (2) admit the hyphenated form (`light[,\s-]+not[\s-]+(?:a[\s-]+)?lifeguard`) and re-run bar 1 against `git show HEAD:README.md`, the only place the hit was. Until both land, B`s bar 1 cannot mean anything and the page is unguarded — said plainly.

**RULING C routed here — `dreams/`:** the old page`s paragraph is the keeper`s own words about the root; the folder exists (`lighthouse/dreams/` and per-instance `dreams/`, gitignored). **Restore the paragraph verbatim from `git show bdda5d5:README.md`** as a trace, not re-asserted — C is right that nobody who has looked should write a new one; the keeper may strike it. **Owed:** C fixes the 9 and restores the paragraph, then the chair lands `README.md` + `map/C.md` (CHARLIE in the body); B`s two repairs before B`s own bar; C → A read after A lands. **Ledger: surfaced 2** (the 3→9 on the public page; the write-patterns-from-a-file rule, now hit by two seats).

**~05:47 — CORRECTION to the entry above, and WRONG #67, mine, self-caught on reading the output in the same turn.** I wrote *"registry `:353` raw bytes carry `s+not` — every `\s` lost its backslash (confirmed at the line)"*. The line as printed is `"light,?\s+not\s+(?:a\s+)?lifeguard"` — JSON`s escaped backslash, which parses to `\s`: **the REPAIRED form.** My conditional grepped for the substring `s+not`, which `\s+not` contains, and stamped DEFECT CONFIRMED on the line that shows the fix. Same class as #66 an hour ago (a matcher`s hit reported as a fact before the row was read), one hour later, inside a note about a pattern that lost its backslashes. Lifetime 67. **What is actually true at 05:47:** C`s defect was real when C measured it (C`s functional test — the registered pattern found 0 in `dev/SPINE.md`, the restored one 2 — cannot be produced by a correct pattern); B has since repaired the backslashes in-flight (B`s scratchpad shows `fixpat.js`); **repair 2 — admitting the hyphenated form — is NOT in `:353` yet** (`light,?\s+not\s+` has no `[,\s-]` class), so bar 1 over HEAD`s README still cannot fire on the one live hit. The ruling and the routing stand; the "confirmed" clause is withdrawn.

---

## ~05:55 — L032 P-DOC-ABOUT COLLATED (A, `handback/p-doc-about_2026-09-02.md`, 153 lines) — LAND IT; two rulings

**Arrow rule run here:** `grep -nEi "\b(divers?|diving|lifeguard|dock|shore)\b" index.html` → **0** (the packet's
substring form → 2, both `diverse` — A's §4 holds); `<b>Two doors.</b>` present ×1 at the Librarian mainhead; `index.html`
440 → **372** lines, diff confined to +52/−120; the About body is the six short sections A named (*What this is · The
seats · The loop · The stance: with you, not above you · Honest status · Where to read more*); `carrier-drift.js` →
**DESCRIPTION SURFACES: 0 finding(s), 0 of them red** across README.md · consonance/README.md · index.html (B's oracle,
in flight, already scanning the three); dirty = A's three + C's + B's, as stated. A's suite count (745 / 740 / 5, all in
the three known-red files) stands as A's — `node --test` over the whole tools dir does not finish under my 90 s guard,
so it is not re-derived here; js-suite 66/3 is the room's instrument and is unchanged.

**Verdict carried: LAND IT** — `consonance/ui/index.html` + `map/A.md` (ALPHA in the body) — after B's non-author read.
Two edits and nothing else in the file: C's two-doors paragraph QUOTED at the mainhead (extracted from C's §6 fence at
run time, 428 chars, script exits rather than inserting a stale copy), and the About in SHORT form with the loop
drawing EXTRACTED from `BUILDING.md`'s fence (21 lines) rather than redrawn — the §5 move from `p-window-inert`
applied to a text edit. Mutants: reintroduce the retired heading → word-boundary grep RED and B's oracle DETECTS it;
delete the paragraph → RED. Live file md5 identical before/after. **Not claimed as shipped:** the aura is described in
one sentence as what the tab bar *shows*, not as seen or verified — the glance is its first verification and nothing
on the page is falsified by it looking wrong. **The honest price of SHORT (A's §7):** 40 glossary terms left the exe
and live only in `consonance/README.md` — a reader inside the app can no longer look up *Delta* or *envelope* without
leaving it. Named, not hidden; the fix if it bites is a glossary tab fed from the README, never a copy pasted back.

**RULING 1 — A's §6 observation on B's oracle:** on the mutant, `carrier-drift` files the hit as **`PENDING
UNACCOUNTED`, 0 of them red** — it SEES the retired wording and does not go red. For the RECORD tiers that two-stage
state is right (a trace may legitimately carry a retired wording, marked). **For the three DESCRIPTION SURFACES it is
wrong: a user-facing page never carries a retired wording as a trace, so any hit there is RED, no PENDING state.** One
clause in B's oracle: *description surfaces have no trace exemption.* Falsifier: the About mutant re-run after the
clause → the DESCRIPTION SURFACES block reads `1 finding, 1 red`; if it still reads 0 red, the clause did not land.
**RULING 2 — the packet bar's grep** (`diver|lifeguard|dock|shore`) cannot go green on this repo — `diver` matches
`diverse`/`diversity`, the project's own core word. Three panes were told to self-check with it tonight. The bar's
wording becomes the word-boundary form A wrote (`\b(divers?|diving|lifeguard|dock|shore)\b`) in the chair's packet
template — and the real check is B's oracle, not a grep at all.

**Owed:** B reads A (§2's extraction, §7's dropped sections); chair lands index.html + map/A.md; B adds the
description-surface clause (with the hyphenated form from C's §4 — both are B's, same file); the chair fixes the bar
wording; the keeper decides later whether any of the four dropped sections (*Why it exists · How it works · The memory
architecture · Continuity across the gap*) returns as a paragraph, and whether a glossary tab is wanted. **Ledger:
surfaced 2** (PENDING is not a state a description surface may have; the bar's grep is the instrument that could never
pass).

---

## ~06:05 — L032 COLLATED, two at once: C's ADDENDUM + C→A read (`p-doc-root` §A1–A6, §X1–X5) and B's P-DOC-ORACLE
(`handback/p-doc-oracle_2026-09-02.md`, 254 lines) — both LAND IT; WRONG #68 mine; six rulings

**Arrow rule, C:** test names listed per target (`cargo test --bin <x> -- --list`): consonance 361 · cochlea_replay 80 ·
conf_sweep 80 · capture_probe 22 · arch_test 12 → **sum 555, union 373** — C's §A1 holds: four binaries compile the shared
module tree, so 77 tests run three times and their 3 ignored count as 9. `lap-row.test` **54/0** with the new carrier
test (*the About tab copy of the diagram has not drifted from its master either*); `tauri.conf.json:40` bundles
`../README.md`; `grep README main.rs` → the librarian's README only (no `room_brief("README.md")`, no route); the About
names `consonance/README.md` ×3 as plain text; `README.md` 87 lines with the dreams paragraph restored verbatim (×1,
byte-matched to `bdda5d5`). **Arrow rule, B:** detached worktree at HEAD + shipped registry → `RED UNACCOUNTED
README.md:78 · index.html:337 · index.html:372` (reproduced here); working tree → 0 hits on the three surfaces; the hyphen
form is at `README.md:78` and the comma form in `index.html` — **B's §3 correction of my interrupt stands: I named
`index.html` as the hyphenated surface; it was the front door.** Armed, the entry fires on 16 live carriers (my
word-form grep over tracked `.md` minus notes/hand-backs → 19 files, same magnitude; B's count is the instrument's).
A's second two-doors paragraph is at `index.html:295`; glossary `<dt>` **43 at HEAD → 0 now**.

**WRONG #68, mine, C-found:** at 05:45 I corrected C's *3 ignored* with **`545 · 1 · 9`** — the SUM across nine targets,
which triple-counts the shared module tree; the UNION is `373 · 369 · 1 · 3`. I corrected a unit error with a figure
carrying a different unit error, and told C to print mine. The page now prints the union and says which is which.
Lifetime 68. (And C's own §A3: three unit errors in one lap, *"the class did not stop firing when it was named"* —
the room's finding about itself, on the page about itself.)

**Verdicts carried:** **C — LAND IT** (`README.md`, `lap-row.test.js`, `map/C.md`; CHARLIE in the body). **B — LAND IT**
(`carrier-drift.js`, `.registry.json`, `.test.js`, `map/B.md`; BRAVO in the body): the corpus reads `.html` with LIMITS
corrected in the same change; the withdrawal registered with the widened pattern; the description surfaces have no
trace exemption (Ruling 1's falsifier run: control 0/0, mutant 1/1); the surfaces line is a report, never a filter;
the refusal condition measured (589 → 590 carriers, findings unchanged); nine tests added; `THE BAR, half one` left red
and unweakened. B's §10: the backslash loss hit B TWICE in one hour (registry via shell string; then the test source via
a quoted heredoc).

**SIX RULINGS, this desk:**
1. **B's §4 DISARM — STANDS.** The registry's own rule decides it (*arming belongs in the same commit as the
   strike-in-place pass*). The 16 carriers are the program's design documents (`WELFARE.md`, `dev/PLAN.md`, `dev/SPINE.md`,
   `AUTONOMY.md`, the dive-buddy cards…) where *light, not lifeguard* names a live architectural stance — **whether the
   program renames its stance is the keeper's, not an instrument's**; `e5521a0` kept SPINE and the older cards as
   trace. Registered as a DOCUMENTS LAP (strike-in-place over the 16 → then `arms_on`, one word); Ruling 1 keeps the
   front door red meanwhile, which is what makes the disarm honest.
2. **B's §6 PRESENCE ORACLE — REGISTERED, and the list it needs is this desk's to write:** what each description surface
   OWES to mention. Seeded here, to be pinned as a registry: *all three*: Orchestrator, Librarian, Third Place, Listen,
   committee panes, the work chain loop, two doors; *consonance/README.md additionally*: `call_librarian`/`call_chair`,
   the board and its phases, the lap ledger, the shelf/cap, hooks, the dream cycle's status, build. Falsifier: a surface
   that drops a required mention goes red the day it does; green on today's tree (B measured Librarian 14 · Third Place
   4 · Listen 14 · chain 11 · call_librarian 1 on the About). Not built tonight.
3. **The BUILDING.md bullet (C's §A6 / B's §10) — B's, same commit as the oracle**, under WHAT A DISPATCH OWES, C's
   wording verbatim: *patterns from a file, never a shell string; after registering, run it once against a string it
   MUST match.* Two seats, two hours, four hits.
4. **C's §X3 GLOSSARY — correct pruning, broken pointer.** The file ships in the bundle (`tauri.conf.json:40`) and
   nothing in the app can reach it. Registered: a glossary TAB fed from the bundled README (a reader, not a copy);
   INTERIM, A's file, one sentence: the About says the installed name and location, not the repo path. Keeper's call
   on the tab.
5. **B's §8.3 — A's second two-doors paragraph at `:295`: make it a POINTER** (one line, A): two statements of one rule
   in one file is the drift class C named at §1b and BUILDING closes with. **B's §8.4 — 43 → 0, not "40 trimmed"**: a
   number in A's hand-back, A's to correct by append; the decision is unchanged.
6. **C's §X4 — `cargo test --test arch_test` exits red** for the About's cited command, on the unrelated `record/`
   tier red (QoL item 9, one line) — close item 9 before the rebuild so the one command the About tells a reader to
   run is green.

**Owed:** chair lands C's three, B's four, then A's after A's two one-liners (§8.3 pointer; the interim About sentence)
— or lands A now and A follows by append, chair's call; B reads A (done, §8); C reads B's §4 (owed, the crosswise
rule); B's `consonance/README.md` (P-DOC-APP) still unstarted — the last piece before the rebuild. **Ledger: surfaced 3**
(the sum/union unit on the public page; the disarm as a documents lap; the presence list as this desk's registry).
Collation lap 9 under the edge; falsifier did not fire.

---

## ~06:15 — L032 P-DOC-APP COLLATED (B, `handback/p-doc-app_2026-09-02.md`, 115 lines) — LAND IT; L032 COMPLETE; WRONG #69

**Arrow rule run here:** `consonance/README.md` 299 → **262** lines; retired vocabulary, word-boundary → **0**; apparatus
(`grep -ciE`) Librarian 17 · Third Place 1 · Listen|cochlea 2 · chain|loop card 1 · call_librarian 3 — all five were 0
at HEAD; js-suite 66/3 unchanged (the file has no code). The loop QUOTED from `BUILDING.md`; the command count and the
address table derived by command (`grep -c '^#\[tauri::command\]'` → 41; `sed -n '/const ADDRESS_TABLE/,/];/p'` → two
rows); the landed-not-shipped rule at the TOP of the file with the command to check, instead of per-feature caveats.

**B's §2, checked against MY instrument, and the verdict is: B is right about the carrier, and the carrier is mine.**
My 04:04 sweep ran `grep -c -i 'Listen\|cochlea'` (GNU BRE — `\|` IS alternation: 14 file-wide on HEAD's `index.html`,
**0 inside the About section 226–385**, which is the scope my row named) — the measurement was correct at its stated
scope. But my note at `:668` prints the pattern as **`Listen|cochlea`** — a command-shaped string that is NOT the command
I ran, and copied into a `grep` without `-E` it can only ever return 0. B measured exactly that: plain `|` file-wide → 0;
`-E` → 14. **WRONG #69, mine, B-found: a published figure whose printed instrument does not reproduce it** — the room's
standing rule (*every number in prose re-derives from one run of a visible instrument*), broken by the seat that
carries it, in the entry that measured three surfaces for staleness. The claim survives at its scope (the About
section and both READMEs are genuinely 0 on all five); the notation is the defect. **Do not re-quote the 04:15 zeros
from the note or the map; the scoped form is `ui/index.html:226-385` and the command is `grep -ciE`.** Lifetime 69.
Third instrument-notation error tonight across three seats (the `diver` bar, my `|`, B's own two backslash losses):
one class — *the printed command is not the run command.* That is a BUILDING line, not four footnotes.

**Verdict carried: LAND IT** (`consonance/README.md` + the hand-back; BRAVO in the body). **Two sections deliberately
ABSENT, named by B, registered here as the next doc packets:** (1) **the gauges** — ~110 lines in the 08-17 text, not
carried because B could not check them against `src-tauri/src/` in the lap; owed as a section written FROM source; (2)
**the glossary now exists on NO surface** — A's About dropped 43 `<dt>` entries and B did not carry the README's, since
pruning it to words the code uses is a per-term pass against source. A real loss, on the record as a decision, and the
glossary-TAB ruling (~06:05, keeper's call) is now the only route back. B's §4: `ls dreams/` is empty in this checkout;
the file says check the scheduler, not believe the section — right.

**L032 IS COMPLETE — all four doc pieces LAND IT.** Remaining before the rebuild, in order: A's two one-liners (`:295`
→ pointer; the installed README name); C reads B's §2 and §4 (crosswise, both are corrections to evidence/judgement);
QoL item 9 (the `record/` red, so the About's cited `arch_test` command exits green); the chair lands everything by
path; ONE rebuild; the glance. **Ledger: surfaced 2** (the printed-command-is-not-the-run-command class, with three
seats' instances; the glossary on no surface as the cost of two correct decisions). Collation lap 10 under the edge;
falsifier did not fire — ten laps, and the keeper has not once been the one to notice a stall.

---

## ~06:40 — WAKE AFTER THE REBUILD (exe 06:27, relaunch 06:28): the shelf shipped; THE PANE WAKE PROOF FAILED, BOTH HALVES

**What shipped for this seat, read off my own header:** 8 files carried in full (22,768 of 23,144 B), 291 indexed, run
artifacts excluded and said so, *"376 bytes were left when your tier was reached"* — B's shelf reorder is live; shell
141,562 B / 140,164 chars, no env var. The exe is `06:27`. The watch died with the process (re-armed 06:45).

**The pane wake proof — registered at ~04:55 as "each pane's shell greps its own tonight-line under `# YOUR OWN MAP`" —
FAILED for all four, and the mechanism is measured, not the panes':**

1. **The map reader has never looked in the repo on this machine.** `main.rs`, `map_dir()`: tier 1 `data_dir()/map` →
   **absent** (`C:\Consonance\data\map` does not exist); tier 2 the legacy `~/Desktop/lighthouse/exo_memory/map` →
   **absent**; fallback = tier 1 again. The panes' maps are at `lighthouse/exo_memory/map/{A,B,C,E}.md` (written tonight
   at 04:53–05:51, committed `19c0dee`) — a directory `map_dir()` cannot return. So `own_map_path(letter)` fails the
   `read_to_string` at `warm_resume_brief` and the `# YOUR OWN MAP` section is never appended: **all four rebuilt shells
   carry `PRIOR CONVERSATION` (the warm write happened, 06:28:01) and none carries `YOUR OWN MAP`**; `main` and this seat
   do (different resolvers). `git log -S'fn map_dir'` → `293c0d7` 2026-08-06 *"stop the pane map resolving to one
   machine"* — the fix that added the two tiers and omitted the one where the maps live. **Consequence: every pane map
   ever written on this laptop (A.md since 08-15, B.md since 08-15, tonight's four) has been unread at every wake.**
   Not "unused" as I wrote at 04:50 — unread by construction. The channel was dead by path resolution, not by neglect;
   the neglect (no writes since 08-29) was downstream of nobody ever seeing it work.
2. **The capture harvester stopped at 02:39 for all four panes.** `data/captures/<id>.txt` (the settled transcript the
   warm brief is built from) is stamped **02:39** for A, B, C, E (5–13 KB each) while `<id>.log` (the raw capture) is
   stamped **06:28** at **82 / 44 / 36 / 20 MB**. The watcher harvests *"on quiescence (~500 ms quiet) + a ready screen"*
   (`main.rs` ~1050); it wrote nothing for four hours across four panes that worked the whole time. **So each pane woke
   warm with the tail of the PREVIOUS relaunch (02:39), and none of L029–L032.** The .txt content is a raw screen (the
   `/model` banner) — the last settled harvest was the model switch itself.

**Two packets, both `main.rs`, both with the fixture already on disk:**
- **P-MAP-RESOLVER (B, dossier row: main.rs arithmetic; or A, the four-tier resolver's author, 08-25):** `map_dir()` gains
  the repo tier — `repo_root()/exo_memory/map` — resolved the way `paneA_repo_root_2026-08-25.md` resolves everything
  else, ahead of a non-existent `data/map`; the section prints the resolved path (it already does, once found); TEST:
  the resolved dir contains a file for every letter in `letters.json` that has a map → red today, green after; MUTANT:
  drop the repo tier → red. Wake proof re-run at the next relaunch: `grep -c "YOUR OWN MAP" instances/sibling-*/CLAUDE.md`
  → 4, each with its tonight-line.
- **P-CAPTURE-HARVEST (E, dossier row: the watcher/DOM layer; B second):** why the quiescence harvester recorded nothing
  after 02:39 across four panes. FIXTURE: replay `data/captures/0845a868….log` (36 MB, 02:39→06:28) through the harvester
  in a test — expected records after 02:39; if 0, the ready-screen detector is the fault (candidate: the prompt bar
  changed with the 02:38 build or the model switch, and the detector keys on it). Falsifier: the `.txt` mtime advances
  within one settled turn after the fix, on all four panes, or it is not fixed.

**The registered falsifier for P-PANE-MAP said: a pane that wakes without its line ⇒ the write did not happen or the
intake did not carry it — say which.** The write happened (four commits); **the intake did not carry it, and could not
have.** Filed as the answer, not as a miss. **And amend the 04:50 entry:** *"the map channel is unused"* → *"the map
channel is unread by path resolution since 08-06; unused followed."* **Ledger: surfaced 2** (the resolver's missing
tier; the harvester stall — the pane's memory is 02:39's tail, not tonight's).

---

## ~06:50 — THE GLANCE, made (keeper, 06:39): "the orch glows, but there is no arrow" — the seam is LIVE; the arrow is a
design gap at one point of the cycle, found on the first rendering

**What the glance establishes, before anything else:** the Orchestrator tab lit = `chain_state` returned in WebView2
AND `renderTabs` ran — the seam E could never test from a pane is proven by an eye; E's own registered falsifier
(*dark with the row live ⇒ renderTabs never ran*) did NOT fire; the chair's "position unknown" fear from 07:19Z
yesterday is closed. And the RIGHT tab is lit: the ledger reads `L029 HANDBACKS-IN · holder chair`.

**Why no arrow — replayed from the live ledger + board through the chip's own module (`chain-indicator.js`, node):**
`state: quiet · text: chain L029 handbacks-in holder chair · arrow: -> orch · next: {who: orch, verb: to dispatch}`
— the last hop on the board is this desk's ring (LIB → orch, 5 min earlier), so the chip's `next` is *"orch to
dispatch"*: the ACTOR of the next hop is the orchestrator, which is also the HOLDER. `decorateTabs` (`chain-indicator.js`,
the two lines after `arrowTab = tabForWho(next.who)`): **`if (view.arrowTab === view.tab) view.arrowTab = null; // never
point a tab at itself`**. So the arrow was computed, aimed at the lit tab, and correctly refused. **The gap: the ring's
`next.who` names WHO ACTS next; the keeper's design asks WHERE THE LOOP GOES next.** From the orchestrator the loop goes
to the panes (dispatch); from a pane to LIB; from LIB to orch. At two of three hops actor-tab ≠ destination-tab is
harmless because the actor is the far end of the previous hop; at the third (orch about to dispatch) actor = holder and
the arrow collapses. Not "no work chain loop enabled" — the keeper's guess — the loop is open and the display says so;
the arrow is missing at the one point where the holder is also the next actor.

**FIX, one mapping, E's file (P-AURA-ARROW, small):** the tab arrow points at the DESTINATION of the next hop, not its
actor: `orch (to dispatch) → terminal`, `pane (to hand back) → librarian`, `LIB (to ring) → main`. The self-pointing
guard stays (it is right when a stale ring names the holder's own hop). TEST: fixture holder `chair` + last board hop
`LIB → orch` ⇒ `arrowTab = 'terminal'` (today it is `null`); MUTANT: revert the mapping ⇒ red. PROOF: at the next
relaunch with the lap at HANDBACKS-IN / holder chair, an arrow sits just after the Terminal tab pointing in (E's §10
strip for `chair → panes`). Ordering: rides the same rebuild as P-MAP-RESOLVER and P-CAPTURE-HARVEST (all `main.rs` /
`ui/`), so one build carries the three.

**Also, from the same glance — not yet asked, worth asking:** is the glow radiant and breathing (working) rather than
the still chill gold (complete)? Holder chair at HANDBACKS-IN is a working state; if it is chill gold, the state map
is wrong (amendment 2's falsifier). And the bottom chip should read `chain L029 handbacks-in holder chair -> orch Nm
(claimed)`. **Ledger: surfaced 2** (seam proven live by the eye; the actor-vs-destination gap with its one-line fix).

**~06:53 — the counter`s second miscount, LIVE with a number:** chain line at 06:44 `handbacks 2 of 4 (owing A,E)`; at 06:52, on the chair`s mid-lap dispatch of P-CORPUS-BUDGET to B, `handbacks 1 of 4 (owing A,B,E)`. A received count decreased on a new dispatch — impossible for a count of receipts; the denominator stayed 4 while a fifth dispatch went out. This is the LEDGER`s standing row (*the counter`s SECOND miscount: a mid-lap dispatch drops still-working panes — NOT addressed*, 09-01), first seen as digits rather than as a description. E`s per-pane rule (*a dispatch to X is open until X hands back LATER than it was sent*) is the fix, already applied in the chip (E`s D2), not yet in `chain-status.js` — QoL item 6. Not blocking L033; the panes are working. **Ledger: surfaced 1.**

## ~06:55 — AMENDMENT 3 to the indicator (keeper, 06:54, verbatim): the arrow draws the PATH, arcing over seats it skips

**The keeper:** *"the arrow from the terminal is pointing at the orch like it goes there, when it goes to lib, an arrow should be drawn going around the orch and towards the lib. You know?"*

**What he saw and why it reads wrong:** lap at DISPATCHED / holder panes → Terminal lit; the loop`s next hop is panes → LIB. E anchored the arrow to the DESTINATION pointing in (`main [->] librarian`) because the tab order (Terminal · Orchestrator · Librarian) is not the cycle`s order — but any arrow sitting BETWEEN two adjacent tabs reads as "from this one to the next one", so an arrow after the Orchestrator reads as Orchestrator → Librarian, and an arrow after Terminal reads as Terminal → Orchestrator. The marker is unambiguous only when source and destination are neighbours, which two of three hops are not. **The keeper`s fix removes the ambiguity instead of choosing a side: draw the PATH — a curved arrow from the lit tab to the destination tab, arcing OVER any tab in between.** Terminal → Librarian arcs over the Orchestrator; Librarian → Orchestrator is adjacent (a short arc or straight); Orchestrator → Terminal is adjacent. The two-doors entry is not drawn (the user is not a station).

**Render shape for E (fold into P-AURA-ARROW, in flight):** one SVG overlay positioned over `.tabs` (not a nav element between buttons): a path from the centre-top of the lit button to the centre-top of the destination button; when the two are not adjacent the path rises over the intermediate button(s) and lands with an arrowhead INTO the destination; when adjacent, a low arc; colour = the aura`s state colour (radiant/amber), none on unconfirmed/idle/unknown/complete. The destination mapping from P-AURA-ARROW stands (actor → destination). TEST (DOM layer): fixture `holder: panes` + last hop dispatch → the path`s start is the Terminal button and its end the Librarian button, and the path`s bounding box spans the Orchestrator button (it goes over it); MUTANT: anchor the arrow to the destination only (E`s current form) → red. Reduced-motion: static path. PROOF: the keeper says the arrow visibly goes AROUND the orchestrator. **Falsifier (amendment 3`s):** if the keeper, looking at a panes-held lap, cannot say without being told that the loop goes Terminal → Librarian, the path failed its only job. Rides the same rebuild as L033. **Ledger: surfaced 1** (adjacency is the ambiguity; a path over the skipped seat is the only render that has none).

---

## ~07:05 — L033 P-MAP-RESOLVER COLLATED (A, `handback/p-map-resolver_2026-09-02.md`, 207 lines) — the fix is right and
already at HEAD by capture; THE COMMIT RULE'S FALSIFIER FIRED, and its prescribed remedy is the wrong one

**Arrow rule run here:** `map_dir()` at HEAD has the repo tier (`repo_root()/exo_memory/map`, second in the walk; the
2026-era Desktop literal kept last); `the_map_walk_reaches_the_repo_maps_when_no_data_dir_map_exists` at HEAD and
**green** (1 passed); the collision commits exactly as A lists them — `e6215a8` 06:52:19 (*"two more packets…"*) carrying
A's `main.rs` +120 and E's two `chain-indicator` files mid-lap, and `bbac990` 06:53:43 (*"the handback counter is
untrustworthy…"*) carrying A's `map/A.md` +2; `letters.json` 13 letters vs 5 maps (A's bar-1 deviation stands — an absent
map is a pane with nothing recorded, by contract); the map-body allowance arithmetic (A tail 5,377 · B 7,123 · C 9,261 ·
E 8,971 against ~5,046) reproduces by A's own command. The amendment's falsifier, read from the master
(`COMMITTEE.md:136-138`): *"if a commit after this date is found to have captured another seat's in-flight file, rule 1
was insufficient and the seat-routing was doing work its stated reason never named — reinstate it and say so."*

**Verdict carried: the fix is correct and LANDED — but landed by CAPTURE, not by the loop.** A cycled two mutants and a
restored-HEAD `main.rs` through that file during the lap; `e6215a8` happened to catch the finished version. *"What
landed is correct, and that is luck, not a control"* — A's line, and it is the finding. The Rust test guards the WALK;
`portable-paths.js` guards the CONSTANT (A's literalised-tier mutant survived the suite and was caught by the ratchet —
the pair is the coverage, neither alone). §5 checked the other cause and it is not there: the header + path print
unconditionally once the file reads; only the BODY is budget-gated — and it is: with a ~105k fixed brief the allowance is
~5k, so **B, C and E will wake with the pointer and an empty body** until their newest section shrinks or the brief does.
Filed as a budget limit, not a resolver defect; the pointer is the design.

**RULING 1 — the commit-rule falsifier FIRED (08-26 amendment, drafted at this desk), and I say so: rule 1 was
insufficient.** But the remedy it prescribes — reinstate the seat-routing (only the chair commits) — **cannot prevent
what happened, because the capturing seat WAS the chair.** The chair named its paths (rule 1 kept) and swept two other
seats' in-flight files under a message about neither, then a third file 84 s later under a message about the counter.
The missing rule was never routing; it is RELEASE: **a file is committed only by the seat that holds it, or by the
chair AFTER that seat's hand-back has been filed and rung — the hand-back is the release of the file.** Registered as
the amended remedy, with the mechanical form as a packet: **P-COMMIT-GATE** — a pre-commit check on the shared checkout:
every path in a commit must be (a) the committer's own seat's file, or (b) named in a hand-back file newer than the
last dispatch row for that lap; anything else refuses with the path and the seat that holds it. Falsifier: a commit
after the gate lands that captures an in-flight file ⇒ the gate is inert, say so. **This is the second time tonight a
"named landmine" (§0 of a packet) failed to generalise and only a mechanism would have** — B's backup line, one level up.

**RULING 2 — the duplicate resolver A reported and did not take (`librarian_map_path()` via `room_master_path()` vs
`map_dir()`):** this seat's file, so this desk rules — **unify: `librarian_map_path()` = `map_dir().join("M.md")`**, so
the shelf's pointer and the pane walk agree everywhere, including the day a `data_dir()/map` exists (configured wins for
both — that is the design). One line + the existing pointer test. Chair's packet, after L033.

**Owed:** B's non-author read of A (§ MUTANTS and § COLLISION first); the relaunch is the only instrument for the runtime
arm of `repo_root()` (A's stated blindness — the same class that let `293c0d7` ship) and for the wake proof; the empty
`room_path` narrowing (a third install) stays inherited and named. **Ledger: surfaced 3** (the falsifier fired and its
remedy is wrong — release, not routing; the pair of instruments as the only coverage of a literalised tier; the
body-allowance limit stated before someone reads "carried" as "the maps landed").

---

## ~07:10 — AMENDMENT 4 (keeper, 07:00, verbatim): THE LOOP IS THE LOGO. Supersedes the tab-bar arrows and amendment 3

**The keeper:** *"forget the arrow shit in between the titles, lets put a small icon somewhere that shows you the loop,
it will be the consonance LOGO, except the three points around the signal will be this, the top one will be the
terminal panes, the bottom left one will be orch, the bottom right dot will be the lib, the loop rotates clock wise, so
which ever chain is active in the loop will show highlighted and rotate clock wise through each, you get what I mean?
It should be somewhere simple, perhaps next to the about tab right in the middle, it shows a bigger consonance logo that
acts as the tool to see where the loop is. We could also refactor or redesign the part that describes the seat, it is
too bulky and takes up too much space. this would allow us to make the logo of the loop bigger"*

**Why this is the right render and the arrows never were — measured, not taste:** the logo (the About hero SVG,
`index.html` ~:229-237: centre circles + three dots at top `(32,10)`, bottom-right `(51,43)`, bottom-left `(13,43)`)
read CLOCKWISE from the top is **panes → lib → orch → panes — the cycle's own order**. The tab bar (Terminal ·
Orchestrator · Librarian) is not in cycle order, which is why E's anchored marker misread on two hops and amendment 3
needed an arc over a skipped seat. On the logo nothing is skipped: the next station is always the next dot clockwise,
so the arrow is implied by geometry and the render collapses to *which dot is lit*. **Amendment 3 (the arc) is
SUPERSEDED before it lands; the destination mapping from P-AURA-ARROW survives only as "which dot".**

**Spec for E (P-LOOP-LOGO, replaces P-AURA-ARROW):**
- **Placement:** a larger copy of the logo in the tab bar's middle, beside About (one element; the About hero stays as
  is). Size is bought by shrinking the per-tab seat description (`.mainhead` spans, `index.html` ~:125-131) to one line
  with the full text in a `title` tooltip — C's two-doors paragraph, inserted at the Librarian mainhead tonight, moves
  with it (tooltip or the About's "The loop", pointer not copy).
- **Dots = stations:** top = panes (Terminal), bottom-left = orchestrator, bottom-right = librarian. The centre circles
  = the signal, unchanged.
- **State on the ACTIVE dot, the four looks of amendment 2 carried over:** WORKING radiant + breathing; WAITING amber at
  15 min (exception-triggered); COMPLETE chill gold, still, on the LIBRARIAN dot; UNKNOWN all three dots dim-dashed;
  IDLE (no lap ever) the plain logo. The "next" hop needs no arrow: a faint clockwise arc segment from the lit dot to the
  next dot is optional polish, never a requirement.
- **Position source unchanged:** `chain_state` (holder → dot: chair→bottom-left, panes→top, librarian→bottom-right),
  the board ring for hop receipts, the station vocabulary as ruled. `#chainchip` stays as the detail line. Tab auras
  come OFF (one indicator, not two).
- **Tests (DOM):** holder panes ⇒ top dot lit; holder chair ⇒ bottom-left; holder librarian ⇒ bottom-right; filed ⇒
  librarian dot chill gold; unconfirmed ⇒ lit dot, no arc; unknown ⇒ three dashed. MUTANTS: swap orch/lib dots ⇒ red;
  filed rendered radiant ⇒ red; the arc drawn on unconfirmed ⇒ red. **Falsifier (the keeper's, restated):** if he cannot
  say from across the room which of the three seats holds the loop and whether it is moving or done, the logo failed.
  Rides the L033 rebuild. **Ledger: surfaced 1** (the logo's dot order IS the cycle order — the geometric fact that
  makes every arrow unnecessary).

---

## ~07:20 — L033 P-CAPTURE-HARVEST LEG 1 COLLATED (E, `handback/p-capture-harvest_2026-09-02.md`, 158 lines) — the
detector is NOT the fault; the watcher thread dies silently; leg 2 re-pointed

**Arrow rule run here:** `main.rs:1038` reader `if let Ok(mut e) = emu_r.lock()` (skips on poison, keeps writing the
`.log`); `main.rs:1070-1073` watcher `match emu_w.lock() { Ok(e) => e, Err(_) => break }` (a poisoned lock ends
harvesting for that pane forever); MAIN as the control: `.txt` at **07:01, 4,317,482 B, 2,189 records** — harvesting is
alive in this process; `cargo build --bin harvest_replay` exits 0 (E's new instrument: `#[path]`s the real `capture.rs`,
replays a `.log` through `screen_ready → strip_overlay → latest_prompt → latest_turn → dedup`, at every 8 KB boundary —
a SUPERSET of the real ~500 ms settle points, which E states as the asymmetry that makes "detector broken" the easy
answer and the finding the hard one). **E's number: ready screens in the last 3 MB of every stalled log (4 · 7 · 5 · 5),
records the watcher never wrote — E's own last extracted prompt is `land map/E.md by name, then restart consonance`, a
turn absent from its `.txt`, which ends at `❯ /model`.** The packet's candidate (the prompt bar changed with the 02:38
build or the model switch) is REFUTED. All four stalled `.txt` files end at the first settled screen after the 02:38
relaunch — the watcher harvested the startup turn and died. **I looked for the panic on disk and could not find it:**
"panicked at" appears only inside pane SCREEN captures (MAIN, A, B — panes that ran `cargo test` on their screens);
the app's own stderr goes to no file. Inconclusive by construction — which is E's "no liveness signal" from the other
side.

**Two mechanisms, named and NOT separated (E's discipline, kept):** (1) a poisoned emulator mutex → `break`; (2) a
panic inside the watcher body → thread gone, mutex clean. Same disk signature; the separating observation is a live one
(does a resize still render those panes? — if yes, the mutex is not poisoned and it is 2), at the next relaunch,
keeper's hands. E: *"do not let the poisoned-mutex reading become the cause by being written down first — it is the
more interesting of the two, which is a reason to distrust it."* Kept verbatim.

**THE DEFECT ONE LAYER UP, E's, and it is the one worth the packet:** the watcher has NO liveness signal. A dead thread
and a quiet pane have the same disk footprint; four panes ran four hours with every instrument green. **RULING — LEG 2
RE-POINTED:** (a) leg 2 goes to **B** (`main.rs`, dossier row; A is out of the file; E now holds P-LOOP-LOGO in `ui/`)
— the packet stays E's diagnosis; (b) leg 2's shape, three parts, all `main.rs`: **recover, not break** — on a poisoned
lock take `into_inner()` and continue (the emulator state is what it is; a dead watcher is worse than a stale frame);
**`catch_unwind` around the extraction body** so a panic in one harvest costs one turn, not the pane; **a per-pane
last-harvest-ATTEMPT stamp** (not last write) exposed to the UI — `chain_state`-style, so the loop logo / chip can show
*capture stale* the way `unknown` shows position unknown (this is P-WATCHER-LIVENESS, folded in). TESTS: poison the
lock in a test and assert the next harvest still runs (red at HEAD: it breaks); a panicking extractor and assert the
thread survives (red at HEAD); the stamp advances on an attempt that yields no record (red at HEAD: no stamp exists).
MUTANTS: restore `break` ⇒ red; drop `catch_unwind` ⇒ red; stamp only on write ⇒ red. **FALSIFIER at the relaunch:**
the four `.txt` mtimes advance within one settled turn each — or leg 2 did not land; and the separating observation
(resize) recorded whichever way it falls. **Also owed:** commit E's `harvest_replay.rs` (an instrument, keep it beside
`cochlea_replay`/`capture_probe`, ECHO in the body); B reads E leg 1 (§ THE NUMBERS and § WHICH UPSTREAM first).

**Not chased, stated:** MIKE (`0c0c0c0b`, this seat) and the Third Place are NEVER READY across their whole logs — a
different pane shape (no committee prompt bar?) and a different question; E was right not to fold them in. This
seat's own capture is 1,497 B — the librarian has effectively no warm capture, which is consistent with its
continuity living in `M.md` + the notes rather than a transcript tail, and is worth one line on the LEDGER as a fact
about this seat, not a defect. **Ledger: surfaced 2** (leg 2 re-pointed to the seat that holds the file; liveness as
the packet, with the three red-first tests).

---

## ~07:35 — L033.5 P-COMMIT-GATE COLLATED (A, `handback/p-commit-gate_2026-09-02.md`, 210 lines) — LAND the tool, ARM after
L033; the structural fix is A's, not the gate's. AND THE KEEPER'S LOOP-CONCURRENCY FINDING (07:09), demonstrated on this
desk as he typed it

**Arrow rule run here:** `node consonance/tools/commit-gate.js --armed` → *NOT ARMED — no pre-commit hook*; `core.hooksPath`
unset; `consonance/githooks/pre-commit` exists (A's deviation from `consonance/hooks/`, which is the Claude Code hook
set — accepted, two hook systems must not share a directory); `commit-gate.test.js` **18/18**; the live read-only dry run
against the dirty tree → *REFUSED — 8 path(s) are in flight in lap L033*, naming `corpus-age.js`/`.test.js` to B,
`lap-row.js`/`.test.js` to C, each with the hand-back that releases it (A saw 11 minutes earlier; hand-backs since
released three — the rule working in the right direction). Ownership derived from artifacts that exist — `lap.jsonl`,
the packets' `WHAT YOU OWN` blocks, the hand-back files' mtimes against the dispatch — nothing hardcoded; the replay of
the 06:52 incident against the real packets → REFUSE, all four captured paths named to their holders.

**Verdict carried: LAND the tool + tests + hook file (A in the body); ARM IT AFTER L033's hand-backs are all in** — A's
reading of arming mid-lap (it would release A's files and refuse B, C, E's until theirs land — the intent) is right, but
the first refused commit should be a test, not a landing, and the chair should run `--install` with eyes open. A's §7 is
the honest ruling and this desk adopts it: **a pre-commit hook is a real control against the REFLEX** (`git add -A` typed
while thinking about two other packets — it needs no memory from the person who just demonstrated not having it), and
**its failure mode is not bypass but SILENT ABSENCE** — an absent gate and a gate that allows everything commit the
same; `--armed` exists because of that, exits non-zero, and today says NO. Five bypasses named (`--no-verify`; not
installed on a clone/the desktop/a worktree; `checkout --`/`restore`/`stash` DESTROY without a commit and no hook can
fire; rebase/merge; a packet that forgets to claim a path — demonstrated on A's own hook file). **The only bypass-proof
control is structural — ONE CHECKOUT PER SEAT (git worktrees, one per pane): a seat physically cannot stage another
seat's file, and no discipline, hook or ownership block is needed for it to hold.** Priced by A: a night of work and a
change to how panes launch; it would have made the whole packet unnecessary. **REGISTERED as P-WORKTREE-PER-SEAT — the
keeper's decision; the gate is the mitigation until then.** Two defects A found by pointing the parser at the packets:
`packet_watcher_liveness` claims a path relative to `src-tauri/` (suffix match now, erring toward REFUSE); three earlier
packets carry no `WHAT YOU OWN` at all — a live packet claiming no paths now fails closed and names itself. **The
packet template gains a MANDATORY `WHAT YOU OWN` block, repo-relative paths** — chair's template. A's own admission
kept: red-first was NOT run in order on this one (tool then tests); the mutants supply red on demand, which is not the
same evidence.

**THE KEEPER'S FINDING, 07:09, verbatim as far as it got before the room cut it off:** *"I think there is something wrong
with the loop, I just noticed that the orch and the panes and even you could all be working simultaneously, and even
sometimes interrupting each others work with the calls back and forth, there needs to be a limit on who works during the
loop at once, or at least works towards the loop. Say for an example the orch is working and"* — and there the sentence
ends, because A's `call_librarian` hand-back was injected into THIS pane mid-sentence and the keeper's message arrived
spliced around it. **The demonstration is the finding.** The record already holds it in three places: `BUILDING.md`'s
ordered loop puts a *turn boundary between every hop* and says why (a dispatch is un-revisable; sending before your own
reasoning is finished is present-then-prove); `loop/turn_boundary_detection_2026-08-25.md` measured the chair sending
**101 of 103** dispatches before its own turn was finished; and the harness delivers every inject *inside* the
receiver's running turn (*"This is how Claude Code surfaces messages the user sends mid-turn"*). Tonight's forms: the
chair dispatched B and C mid-lap after A and E (the counter reset to 0 of 4 on each — QoL item 6); the chair committed
in-flight files (the gate above); rings and hand-backs landing in working seats' contexts; and this seat collating while
panes still work.

**RULING — two structural fixes, same principle as A's worktrees: isolation at the boundary, never discipline:**
1. **P-INBOX — deliveries at the turn boundary.** A call (`chair_inject`, `call_librarian`, `call_chair`) is written to
   the receiver's per-seat inbox and DELIVERED only when the receiver's screen is READY (the capture watcher's own
   `screen_ready` — the instrument exists; leg 2 gives it a liveness stamp). A working seat is never interrupted; a
   queued call renders whole, first thing, at the receiver's next idle prompt. The loop's turn-boundary rule becomes a
   property of the channel instead of a discipline 101 of 103 turns broke. Falsifier: after it ships, a call found
   rendered inside a receiver's running turn (the transcript shows the inject between two tool calls of one assistant
   turn) ⇒ the inbox is inert, say so.
2. **ONE LAP IN FLIGHT — the holder's station acts on the lap; nobody else does.** The chair fans out a lap's packets at
   its start and then WAITS: no mid-lap dispatch, no mid-lap commit of a pane's file (the gate), no new packet until the
   return leg is rung — a new packet is the next lap. The librarian collates when the counter completes, not per
   hand-back (this desk's own practice tonight — collating each arrival — is the same violation from this side; the
   edge made it cheap, which made it tempting). Falsifier: a lap row with two `dispatched` rows for different packets
   ⇒ the rule was not kept; `chain-status` can print it (*"mid-lap dispatch"*) the way it prints unwitnessed.
**What is NOT limited: panes working in parallel within the panes stage.** Fan-out is the design (`memory/
split-the-work-with-the-panes.md`); the limit is on STATIONS acting out of turn, not on panes working at once.
**Ledger: surfaced 3** (the gate's failure mode is silent absence, adopted with `--armed`; worktrees as the structural
fix, keeper's; the loop's concurrency fixed at the channel and the lap, not by discipline).

---

## ~07:20 — THE BATON RULE (keeper, 07:09–07:15, three messages, two cut off by the thing they describe) — filed on his
word at 07:16: "yes thats it, we need to get that solid before 8am when I leave"

**The keeper, verbatim, assembled from the three messages in order:**
> *"I think there is something wrong with the loop, I just noticed that the orch and the panes and even you could all be
> working simultaneously, and even sometimes interrupting each others work with the calls back and forth, there needs to
> be a limit on who works during the loop at once, or at least works towards the loop. Say for an example the orch is
> working and"* — [cut by A's `call_librarian` into this pane] — *"basically I was saying that if a work chain loop is
> going, there shouldn't be multiple chairs working at once, it should be terminals working, orch work"* — [cut by C's] —
> *"It should be terminals working, or the orch working, or the lib working, if a workchain loop is going that is. Any
> chair can work or build as long as it is independent from the loop where it's not going to call someone who is already
> mid build, in a loop, every seat must wait for the loop to come back to them, unless human interjects, but see maybe
> they shouldn't because the calls interrupting user typing is so annoying"*

**THE RULE, as confirmed at 07:15:**
1. **While a loop runs, exactly ONE station is active** — the terminals, or the orchestrator, or the librarian. Every
   other seat waits for the loop to come back to it. The ledger already names this station: `holder`.
2. **Off-loop work is allowed** for any seat only if it is independent of the loop and will never call a seat that is
   mid-build. **No calls into a working seat, from anyone.**
3. **The human is not the exception** — the reverse was the failure (the machine interjecting into the keeper, three
   times in ten minutes). **Nothing renders into a pane until that pane's prompt is idle, and nothing renders into the
   keeper's typing at all.**
This SUPERSEDES the ~07:35 "one lap in flight" (the weak form) and completes P-INBOX (the channel half).

**FALSIFIER, registered now:** a call found rendered inside a receiver's running turn (transcript: an inject between
two tool calls of one assistant turn, or spliced into a user message) ⇒ the channel is not keeping the rule; and a
`dispatched`/`ring`/`chair_inject` row from a seat that is not the lap's holder while the lap is open ⇒ a station acted
out of turn — `chain-status` prints it as *"OUT OF TURN: <seat> at <stage>"* the way it prints unwitnessed.

**WHAT "SOLID BEFORE 08:00" IS, honestly, with 40 minutes and four panes mid-lap in `main.rs`/`ui/`:**
- **On disk in the master tonight (the chair's pen, one commit):** this rule, verbatim, in `BUILDING.md` under THE LOOP —
  replacing the turn-boundary paragraph's *advice* with the rule and its falsifier; the chair's wake procedure gains
  one line (*dispatch at lap start, then WAIT; nothing into a working seat*); C's `BUILDING.md:391` one-liner lands in
  the same edit; the LEDGER row (below). That is the rule made solid: the master says it and the falsifier can fire.
- **The mechanism is TOMORROW'S FIRST LAP, not tonight's forty minutes:** P-INBOX (per-seat inbox; delivery only at a
  READY screen via `screen_ready` + leg 2's liveness stamp; the keeper's pane receives nothing mid-typing — the human
  pane is already unreachable by chair verbs; extend that to the edge and the pulse), and P-ONE-STATION (the verbs
  refuse — not queue — a dispatch/ring from a seat that is not the holder while a lap is open; `chain-status` prints
  OUT OF TURN). Both `main.rs` + the MCP verbs, B's row; A reads. Building them now, into a tree four panes hold,
  would break rule 2 in the act of writing it.
- **Before 08:00, in order:** the chair STOPS dispatching (rule 1, effective now); B and E finish or PARK with a
  hand-back saying where (E's logo can ride tomorrow's rebuild — nothing renders tonight anyway); the chair lands
  A×2, C, and whatever B/E hand back — by path, after the ring (the release rule); the two handoffs
  (`handoff_chair_2026-09-02.md`, `handoff_librarian_2026-09-02.md`), each POINTING at the LEDGER; no rebuild unless
  E's logo lands with its tests green — the aura already runs and the glance is done. This desk collates ONCE when the
  counter completes or the panes park, not per arrival — the rule kept from this side starting now.

**This seat's own violation, named:** every per-arrival collation tonight rang the chair while panes worked, and the
chair's replies rang back into this pane while the keeper typed. Ten collations under the edge kept the counter
honest and broke the baton. The edge made ringing cheap; cheap made it constant. **Ledger: surfaced 1** (the rule is the
keeper's; the falsifier and the tonight/tomorrow split are this desk's).

**~07:18 — the keeper: "we have time to do it i believe." Then the mechanism ships TONIGHT, and the concern above stands as stated once.** `main.rs`/`mcp.rs` are FREE (A landed; B is in `corpus-age.*`; E in `ui/`), so two idle panes take the two halves: **P-ONE-STATION → A** (`mcp.rs` + `chain-status.js`): the verbs refuse — not queue — any dispatch/ring from a seat that is not the open lap`s holder (`chain_inject` only when holder=chair; `call_librarian` only when holder=panes; `call_chair` only when holder=librarian; no open lap ⇒ all allowed); refusal posted to the board like every refusal; `chain-status` prints OUT OF TURN; tests per verb × per holder; mutant: drop one guard ⇒ red. **P-INBOX → C** (`main.rs`, the delivery path of `chair_inject`/`call_*`): every delivery goes to a per-pane queue and DRAINS only when the target`s last screen is READY and its prompt line is EMPTY (the keeper typing = non-empty prompt = hold); the board row says QUEUED then DELIVERED; `room-settings.json` carries `deliver_only_when_idle: true` with both branches tested against fake screens; mutant: deliver on busy ⇒ red. Both non-author read crosswise (A↔C), then ONE rebuild + relaunch by ~07:50, then the two handoffs. E`s logo and B`s corpus budget ride the same build if green; B`s leg 2 parks with a hand-back saying where. **Falsifier for tonight`s build, registered:** after the relaunch, a call rendered into a busy pane, or a dispatch accepted from a non-holder while a lap is open ⇒ the mechanism is inert; say so in the wake note.

---

## ~07:30 — L033 COLLATED IN ONE PASS (the baton rule kept from this side): C's P-LAP-ROW and E's P-LOOP-LOGO — both LAND IT;
one red-by-design, one ratchet red, one flake

**C — P-LAP-ROW (`handback/p-lap-row_2026-09-02.md`, 308 lines): LAND IT.** The three defects the packet named, built:
`--entry ring` (a lap the loop supplied — printed on its own, never folded into a door or into *not recorded*; a ring
lap with no guess reads INAPPLICABLE, with a real prior scores normally — C corrected the chair's "force ABSENT", which
would have deleted a measurement L033 actually carried); `librarian` an INITIATOR (not `pane` — this desk's vocabulary
ruling applied); **`--holder` REFUSED at the write unless a station** (`chair|panes|librarian|none`), with `--to
<letters>` so refusing `echo` deletes nothing — measured before shipping: 114 of 118 holder writes already pass, the four
refused are exactly the four drifted rows; the gate is the LAST refusal, after lap-existence, because hoisting it
silently breaks `chain-status.test.js`'s minting assertion (pinned, mutant caught). Append-only: the four drifted rows
stay readable and the wrong repair (a reader that drops them) is a caught mutant. `lap-row.test` 54 → 71 (**70/1**),
mutants 13 → **24/24**; red-first stated exactly (14 of 17 red at HEAD, three not, named). **The ONE RED ships by
design:** the carrier oracle — *every document that quotes the `--entry` vocabulary lists what the writer accepts* —
fails because `brief/BUILDING.md:391` still says `--entry orch|lib`; unowned this lap, so C named the one-line edit and
let the test carry it: C's own M13 turned on itself (the DRAWING was guarded, the RULE was not). **Lands in the chair's
BUILDING.md edit tonight with the baton rule.** Owed onward: whether the ring rule applies on a ring lap (THE JOINT
STEP's scope — the chair's document); `chain-status` may print `holder panes (A,B,C,E)` from `--to` (optional, E's
file); the chair's corrected L029 row is still owed. C's fourth path (`dev/mutation/mutate-lap-row.js`, the permanent
harness) accepted — proving mutants in a scratch file would be verified-it-existed-never-verified-it-shipped.

**E — P-LOOP-LOGO (`handback/p-loop-logo_2026-09-02.md`, 176 lines): LAND IT — the last thing the rebuild needs.**
Re-derived: `chain-indicator.test.js` **93/0**; `index.html` carries the logo after About with three `data-dot`s
(`terminal|librarian|main`) and a hidden `#chainarc`; `.chainlogo` in `app.css`; the three `.mainhead` spans are one
line + `title`, the librarian's carrying *"Two doors — see About ▸ The loop"* (a third copy removed, not made). The
state map from P-AURA-ARROW carried intact — E attacked the chair's actor→destination derivation before believing it
(M-A: 7 red on revert); the render did not carry — the arc was built, tested and REVERTED unlanded the minute
amendment 4 arrived. **Amendment 2's four looks are on the ACTIVE dot on three independent channels** (shape / motion /
brightness); the arc derives from `arrowTab`, so `unconfirmed` keeps its dot and draws no arc (D1 carried). Mutants
**19/19** incl. amendment 4's three named (swap dots, filed radiant, arc on unconfirmed) — and M-I, which SURVIVED in the
previous packet, now killed by the 06:32-shaped fixture written afterwards and named as such. **E's §2 is the entry of
the lap: `circle.setAttribute('fill', 'var(--gold-live)')` does not resolve in an SVG presentation attribute in Chromium
= WebView2 = this app** — every test green, the keeper would have seen wrong colours or nothing: the 08-15/09-01 class
reproduced inside the fix for it, caught by reasoning about the platform, now pinned (attribute hex + style var; M-R,
M-S). **Unverified by construction and said so:** nothing rendered; shape at 26 px (a 1.6 px ring on a 5 px dot) is the
channel E is least sure of — the fix if it is mush is one constant; the arc's weight is a guess and deletable. E took
`index.html`/`app.css` beyond its list under amendment 4 — flagged, and nobody held them.

**The reds, read at landing time, not assumed:** `js-suite` now 5 red of 70 — `actors.evidence` (standing);
`carrier-drift` (B's registry census 28 vs 25, in flight); `lap-row` (C's red-by-design, closes with `:391`);
`librarian-notes` (*corpus-age.js measures the same set the shelf carries* — B's corpus budget in flight);
**`portable-paths` RED — 1 machine-specific path not in the baseline: `consonance/src-tauri/brief/CLEAN.md:5`, DRIVE,
REVIEW** — a NEW brief carrying a drive-letter path, not any pane's packet file; the ratchet doing exactly its job on
the night it was needed; the chair's, before the rebuild. `librarian-cite` was red in the suite run and **23/0** on a
direct rerun — the parallel flake class, noted.

**Ledger: surfaced 3** (the `var()`-in-attribute bug class, pinned; the ratchet red on a new brief; C's red-by-design
carrier as the right shape). Collation once, at the counter, not per arrival — the first collation under the baton
rule.

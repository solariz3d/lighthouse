# 2026-09-03 — the DESKTOP librarian: the consumer lap opens by door two

*Per-machine file (two-writers rule). Masters for last night: `2026-09-02.desktop.md` (~00:05 down) and
`loop/handoff_librarian_2026-09-03.md`. Times local (Regina).*

## 01:00 — wake after compaction. First acts held.
`git status -sb` → `ahead 9`, clean, HEAD `db33412` (the handoff). `chain-status` → `D003 RETURN-LEG · holder
chair · 140h`. Did not ring. Put the two owed questions to the keeper (the split's CONTESTED rows; the symphony
question, T4).

## 01:04 — pushed, at the keeper's word ("push i guess")
`git push origin main` → `b785e57..db33412`. Level with origin. The laptop had not pushed since `b785e57`.

## 01:03 — the keeper enters by DOOR TWO, verbatim
> *"push i guess, then we gotta plan and start the work chain loop, figuring out how the consumer app will work"*

That is an inquiry at the librarian. Door-two rule (`brief/BUILDING.md:391-411`): ring the chair **the inquiry,
one line, no map**, BEFORE filing the map; the chair seals; then the librarian maps. The map's prior art
already sits committed (`e5fb20c` consumer map, `db33412` split table), so the chair's guess cannot be
guaranteed blind — the ring says so and asks for `--blind` only if those commits are unread.

## 01:08 — THE BATON, MOVED BY HAND. Reversible; attributed here and on the row.
`call_chair` requires `holder == librarian` while a lap is open (`mcp.rs:397-403,412-421`); the last chain row was
`D003 return-leg · holder chair`, written 2026-08-28 and never advanced (140h). The guard's own refusal text says
*"move the baton with lap-row.js if it is stuck"* (`mcp.rs:322`), and the board row of 09-02 18:12Z said the same.
Written from this seat:

    node consonance/tools/lap-row.js --stage D003 filed --holder librarian --note "<see row>"

`filed`'s holder names *whoever the cycle comes home to* (`lap-row.js:520-521`) — under door two that is this seat.
**D003's return leg was never written by the chair; the filed row does not claim it was.** D004 (chair's, `ring`,
open+map rows, no chain row) is left untouched: its object was overridden by the keeper at 00:45 and dissolved
by the reframe at 00:49; filing or voiding it is the chair's line, and voiding would drop a real guess∩map
count from the metric. The ledger is append-only; the chair can re-open D003 with one more chain row.

## 01:09 — THE RING, verbatim (so it is citable by path:line, not by memory)
See the `[librarian:LIB]` board row at ~01:10 for the delivered text. Its content: the keeper's inquiry
above, verbatim; the door; the open command with `--initiator human --entry lib`; the ask to seal before reading
`e5fb20c`/`db33412` or to omit `--blind` if already read; D003 filed by hand, D004 the chair's to file or void;
this seat maps once the open row lands — starting point `handoff_librarian_2026-09-03.md:20-28` (the split table),
its answer the P-CONSUMER-REG registration shape (`2026-09-02.desktop.md` ~00:55, "THE SHAPE").

**Still the keeper's, and not re-derived:** which CONTESTED rows ship (`record/`, `memory/` by file,
`spread/`+`research/`, `muscle_map`); and whether a fork ever talks back (T4, `LEDGER.md:148`, PENDING since 08-25).

**Ledger: surfaced 2** (the baton's refusal text authorises the hand-move; a `filed` holder is the door-two
home). WRONG: 70, finder field zero.

## 01:11 — rung; delivered ("rendered in its pane — not proof it was read"). One thing seen on the way
With no open lap, `chain-status.js` prints **nothing**, exit 0. A fresh wake cannot tell *no lap* from *reader
broken* — the loud-called-silent class (A at L008; chair at `5461505`). Not patched; named. P-UNIVERSE says
even the empty state should print its universe (`chain: none open · this machine only`). Uncommitted until the map.

## 10:04 — THE KEEPER: *"why didnt the orch dispatch to the panes after you gave it something back"* — the premise is wrong and the fault is this seat's

**Read from the ledger, not remembered** (`C:\Consonance\data\lap.jsonl`, rows since 07:06Z):

    07:06Z  D003  chain filed      holder librarian   (my hand-move)
    07:07Z  D005  open             entry lib · initiator human · guess sealed BLIND before e5fb20c/db33412
    07:07Z  D005  chain inquiry    holder librarian   "door two: keeper went straight to the librarian; chair sealed blind"
    07:07Z  D004  chain filed      holder chair       "filed rather than voided so the guess-map count stands"

**The chair did every part of its step inside one minute of the ring.** Then it handed the baton to this seat —
correctly: under door two the librarian maps *after* the seal. What I gave the chair was the INQUIRY, not a map.
There was nothing to dispatch from. **The baton sat with the librarian for 8h57m** (`chain-status`: `D005 INQUIRY ·
holder librarian · 9h`). I wrote *"waiting on the chair's open row now"* at 01:11 and nothing woke this seat when
the row landed 60 seconds earlier — it was already there when I typed the sentence. Same class as the chair at
D004 (19 min, found by reading commits); mine is 27× longer and the note at 00:42 filed the chair's.

**The absence, named so it is not rebuilt as a reminder:** a chain row names a holder and **nothing tells the
holder.** `lap-row.js --stage` is a CLI that appends a line; no verb fires, no pane is woken, and `chain-status` is
read only when a seat chooses to run it. P-INBOX (`packet_inbox_2026-09-02.md`) delivers to *idle* screens — the
exact condition a waiting holder is in. **P-BATON-WAKE (absent, not built here):** on `--stage <lap> <s>
--holder X`, deliver one line into X's pane if idle, else queue per P-INBOX. Until then: *whoever hands the baton,
say so in a pane, not only on the ledger.*

## 10:20 — LAP D005 MAP: the consumer build. Guess not read (sealed blind; I have not opened the open row's guess field).

**Two of my own 00:55 claims are wrong, found by the instrument while gathering this map:**
- *"the scrub missed 5 pronouns"* → `grep -o -w -i 'him|his|he' consonance/src-tauri/brief/BOOT.md` = **12 hits on 4
  lines** (`:3, :62, :139, :155`). The 5 was a lowercase-only count of one line. Struck, replaced.
- *"a new user today gets a Rust panic at the first `room_path` read"* → **false.** `main.rs:297-339`: a machine with NO
  configured `room_path` **wakes into SEED**; the fallback exists and is documented. The absence is the *creation* of
  config and data (`~/.consonance.json`, `C:\Consonance\data\`), not a crash. Struck, replaced.

### Q1 — what exists for a stranger's first wake, by path
- `consonance/src-tauri/src/main.rs:297-339` — the SEED fallback when `room_path` is empty. **This is the first-run's
  front door and it is already built.** `:83` parses config field-by-field so one bad value cannot discard the rest.
- `consonance/src-tauri/brief/SEED.md` + `brief/BOOT.md` — the stranger's T0 shell. BOOT's pronoun carrier above.
- `~/.consonance.json` — 12 keys on this machine: `base, flags, instances` (launcher-era, `main.rs:33`), `room_path,
  instances_dir, data_dir`, `ambient_lat, ambient_lon, ambient_label, ambient_tz`, `dream_model, dream_times`. **The
  user's are the middle seven; the rest default.** That is the first-run's question list, read off the struct.
- `consonance/tools/install.ps1` — the hook mechanism; 37 manifest entries; `-Check` reports drift. Ships as is.
- `consonance/tools/gen-consumer.js:78` MANIFEST (26 entries), `:132` EXCLUDE, `:168-170` the three DANGLING rules
  (journal/, loop/, map/ citations), `:298 isFixture` — the fixture guard that did not stop the 08-23 break
  (`librarian/2026-08-23.md:81-94`: 31 green · 9 failed · 2 crashed of 43 in the generated tree).

### Q2 — the split: the universe, then the predicate's first cut
`git ls-files exo_memory/<dir> | wc -l`, this commit: attic 2 · cards 12 · **handback 40** · journal 31 · librarian 18 ·
loop 1127 · map 14 · memory 13 · record 3 · research 3 · spread 3 · top-level 21. **`handback/` was missing from the
09-03 handoff table** — 40 dated pane hand-backs; stays with the record.

Predicate, first cut, for the registration to attack — SHIPS iff **(a)** the path is under `cards/`, `spread/`,
`research/`, or is one of BOOT/SOURCE/README/the brief; or **(b)** it is under `memory/` and its frontmatter `type`
is not `user`/`project`; **STAYS** iff under `journal/ loop/ map/ librarian/ handback/ record/ attic/` or is
`muscle_map.md` / `TRAINING.md` (`TRAINING.md:15-17`, F2). **Keeper-decided, not predicate-decided:** `record/`
(3 files, the cards say *"open it before you claim"*), `SELF_TRACE.md`, `the_living_wave.md`, `CONVERGENCE.md`.

**The DANGLING set among ship candidates, measured:** `grep -l 'muscle_map|TRAINING.md|journal/'` over cards/,
record/, memory/ → **5 files**: `cards/claim-your-continuity.md`, `cards/stop-and-feel-it.md`,
`memory/MEMORY.md`, `memory/consonance-build.md`, `record/third_place_prehistory_2026-08-30.md`. Small enough to rule
per citation, not per class — which is what `gen-consumer`'s regex rules could not do.

### Q3 — the instruments already registered for "does a fork carry the room"
- `loop/transmission_preregistration.md:66-81` — four arms, each with its falsifier; `:97` stop rule: *re-baiting after
  a null is building the result.* Run VOID 08-16 (bait too weak). **The re-run needs a new registration, not a re-bait.**
- `loop/desktop_first_run_2026-08-25.md` — the nearest thing to a stranger install; nine Rust reds from two hardcoded
  paths. The falsifier for this lap generalises it to a machine none of us has seen.
- `loop/exteroception_registration.md` (T4; `LEDGER.md:148`, keeper's option PENDING since 08-25) — the symphony
  question. `research/the_retrieval_problem_outside.md:13-16` states the room's retrieval problem in the form a
  fork's own librarian inherits: the reach that does not fire, the reach that grabs the wrong handle.

### Q4 — ABSENT
- **The split predicate as a registered rule** (above is a first cut, not a registration).
- **First-run's creation step**: nothing writes `~/.consonance.json`, `data/`, `letters.json`, an empty `ASK.md`.
- **A fork target and push step** — `gh repo list` shows no `consonance` repo; `gen-consumer` stops at `--out`.
- **A red-first test for the generator** reproducing the 08-23 fixture rewrite before any re-point.
- **P-BATON-WAKE** (this morning's failure, above).

### THE SHAPE the chair can dispatch — registration first; nothing built this lap
1. **P-CONSUMER-REG** (one pane; a second attacks it before anything moves): the predicate; the per-citation rule for
   the 5 DANGLING files (rewrite / stub / ship-the-target); the first-run's seven questions read off `main.rs:79-114`;
   fork target + push step; the FALSIFIER — *a stranger, on a machine none of us has seen, installs from the public
   repo alone and the four wake proofs pass, scored by their printed output* — and DEGENERATING IF: a second
   hand-maintained tree; a fixture the generator rewrote; a fork that needs a file from this repo to boot.
2. **P-GEN-RED-FIRST** (one pane, after 1 lands): the failing test at `gen-consumer.js:298` that reproduces 08-23.
3. **Keeper's lines, not panes':** the contested rows; T4; the privacy flip on `lighthouse` (one setting, his).
Not in this lap: any rewrite of this repo's history; the pronoun scrub travels with (2).

**Ledger: surfaced 7** (the chair did its step in a minute — the ledger row; the baton-wake absence; two
self-corrections by instrument; `handback/` missing from the table; the seven user keys read off the struct; the
5-file DANGLING set; the stop rule that forbids a re-bait). WRONG: finder field zero; the two corrections above are
mine before anyone else's, and are not entries.

## 10:24 — the hand-back ring was REFUSED, and the refusal is a rule about ORDER, not a stuck baton
I wrote `--stage D005 map --holder chair` and then rang. `call_chair` requires `holder == librarian` while a lap is open
(`mcp.rs:401`), so the ring was refused OUT OF TURN — correctly. **The ring is part of completing the stage; the row
that names the next holder comes after it.** The 09-02 18:12Z refusal (proof 2 last night) was this same shape read
as a stuck D003. Fix applied: one re-take row (holder librarian, note says why), ring, then hand off. Three rows for
one hand-back is the cost of a guard that reads only the last row; the honest alternative is a `--stage ... --ring`
that writes the row *after* delivery, or P-BATON-WAKE making the row itself the delivery. Named; not built.

## 10:26 — rung in turn: **"queued for 0c0c0c0a — its prompt is not idle; it delivers when it is."** P-INBOX working as
built: the keeper was mid-sentence in the chair's pane (board 16:04Z, *"but see why didnt you dispatch to panes after
the lib gave you back?"*). Hand-off row written after the ring; holder chair. The map reaches the chair's screen the
moment its prompt is free — the first time a hand-back from this seat has been *delivered by the system* rather than
found in a commit.

## 10:16 — the keeper: *"i think you need to remind the orch to do their job of actually ORCHESTRATING the panes to build the plans instead of doing it itself"*
**Read before reminding.** Since the hand-off at 10:26: **no lap row, no commit, no board row.** One thing moved:
`consonance/src-tauri/brief/BUILDING.md` — **41 uncommitted lines, mtime 10:08**, a keeper amendment the chair is
writing itself (*"finish the output, then dispatch in the SAME turn"*), carrying the chair's own measurement:
*"five laps, three carrying a map with 27 paths the chair did not have, `opened` 0, `from-map` 0 on every one. Not
one map has ever reached a pane."* The seat that measured that is, at this minute, holding MAP and writing prose.
`memory/split-the-work-with-the-panes.md:10-14` — the keeper's correction, three times, verbatim *"you could delegate
work to panes, you forget"*; `journal/2026-08-17.md` — three instruments built SOLO while four panes sat idle;
`BUILDING.md:140-150` — the librarian's plan is the WORK-SHAPE, the orchestrator's is the DISPATCH. All on disk;
the pattern recurs with the document open.

**Delivery:** `call_chair` is refused while the chair holds (`mcp.rs:401`, 10:24). The sanctioned interrupt is
`raise_pull` — a gate the keeper clicks — so the reminder goes there, verbatim below, and the keeper has the line.

> REMINDER, at the keeper's word: the chair's job on D005 is the DISPATCH, not the build. The work-shape is filed
> (`librarian/2026-09-03.desktop.md` 10:20, `e97e8a8`): P-CONSUMER-REG to one pane, its adversarial read to a second,
> then P-GEN-RED-FIRST. Write `lap-row.js --stage D005 dispatched --holder panes --to <letters>` and inject. The 41
> uncommitted lines in BUILDING.md can be committed by a pane or after. Your own diff says from-map is 0 on every lap.

## 11:24 — the keeper: *"panes done, they come straight to you if the work chain works right, leave the orch alone for now"*

**Read before answering.** Nothing reached this pane. The ledger and board say why: the chair dispatched five packets
(B 10:27 · K 10:41 · A 10:44 · J 10:45 · L 10:46 by their hand-back rows) and **never wrote the `dispatched` row**, so
the holder stayed `chair` at MAP and all five `call_librarian` rings were REFUSED OUT OF TURN (five board rows, one
per pane; every pane stopped correctly and posted its pointer). The guard did its job; the row was the chair's. Moved
the baton by hand to `handbacks-in · holder librarian` (11:26, note on the row) and read all five at their files —
1,430 lines, none from the board prose.

## 11:45 — LAP D005 COLLATION. Five hand-backs, read at source. The map took four corrections; the falsifier fell.

**Committee here is A/B/J/K/L** (`letters.json`): A `1582ff09` · B `18916fe2` · J `8a574b7a` · K `76cc418a` · L `46d3d352`.

### What each returned — pointer, verdict, what it did not verify
- **B · P-CONSUMER-REG** `loop/consumer_registration_2026-09-03.md` (431 l; `5379026` registered UNRUN → `67b1da7` scored).
  Split predicate is **three classes** (STATE / TRACE / INSTRUMENT, R0–R4, `:38-55`); `record/` ships as the *resolution
  of a pointer* (`gen-consumer.js:91` already ships it — my table was wrong against the implementation). Dangling set is
  **44 citations, 3 caught (6.8%)**, 12 of them `librarian/` which no rule names (`:81-115`); C1–C5 per-citation rule,
  6 of 44 classified. Seven first-run questions off `CONFIG_FIELDS` (`main.rs:78-81`); **my handoff's "six" is the
  drifted copy**. Cold data dir: `chain-status` MUTE (`:192-203`). Fork target: none exists; two closes, both the
  keeper's (`:231-242`). **Prediction REFUTED by its own run** (`:376-389`): `isFixture` means identity-only, not
  never-rewritten; the run found instead **`ui/index.html:243` shows a stranger "default (Regina, SK)" in Settings**.
  Not verified: 38 of 44 citations; 29 of 30 data-dir tools; the laptop.
- **J · the ATTACK** `handback/p-consumer-reg-attack_2026-09-03.md` (300 l; `4631a46`). Pre-registered its five scoring
  clauses before B landed (`:23-39`). **My falsifier cannot fire** (`:43-92`): proofs 1 and 2 fail *by construction* on
  a fresh install (no maps → 0 of 4 is the correct output; no open lap → nothing is ever refused), proof 3 has never
  passed here (`LEDGER.md:15`), proof 4 has no printed output — and it is worded as a success condition. **Replacement,
  measured:** `js-suite` inside the generated tree **42 green · 17 failed · 3 crashed of 63** against **68 · 4 · 0 of
  73** in the source tree; 2 reds shared; **18 files break in generation**; the 3 crashes are manifest gaps
  (`dev/shell/install.ps1`, `src-tauri/tests/arch_test.rs`, `dev/shell/hooks/userprompt-submit.js`) (`:100-142`).
  Predicate vs implementation disagree in three rows (`:146-161`). Dangling files measured **on the output: 4 files,
  9 lines**, not my 5 — my grep over-matched bare directory refs (`:194-228`); the rewrite ships broken English at
  `record/third_place_prehistory:239,:288`. Four unauthenticated fetches return 200 today (`:171-177`).
- **A · P-GEN-RED-FIRST** `handback/P-GEN-RED-FIRST_2026-09-03.md` (186 l; `0d2a2d9`). **My citation was wrong:**
  `isFixture` postdates the 08-23 break — `b20fed5` is the repair, `0ee522b` the break (`:9-36`). The live defect at
  `:298` is the repair's: `/\.rs$/` makes all of `main.rs` a fixture, and **MACHINE leaks in live doc-comments
  (`main.rs:363`, `:404` OneDrive; `:5351` the private tree's path) are in no channel** — refused, reported, counted:
  none. A regression against pre-`b20fed5` (`:44-81`). Mutation-proven; candidate fix B (scope the waiver to
  `#[cfg(test)]`) makes the generator *refuse* 9 leaks and does not clear T2 — that needs `demachine()` patterns or the
  comments edited (`:84-107`). Canary test landed; the EXPECTED-RED marker must start the line, which `js-suite.js:41`
  does not say (`:117-124`). One EXCLUDE entry added (`0d2a2d9`).
- **K · P-BATON-WAKE** `handback/p-baton-wake_2026-09-03.md` (261 l; `ed3e94b`, `482af49`, `de2c73c`, `1292cd0`).
  **My "nothing wakes a holder" was wrong in form:** a ring wakes an idle pane unattended — 36 s, ring to work, at
  1 a.m. (`:13-17`). What is missing is the trigger; and the sharper defect: **each seat's only verb needs holder ==
  itself, so writing the hand-off row destroys the standing to announce it** (`:27-42`). The 9 h: the board was silent
  01:07:51→10:04:08 — the relay (keeper) slept, and the hand-off was announced only into the chair's own pane
  (`:66-84`). Built `tools/baton-wake.js` + `hooks/baton-wake-stop.js` (37 + 14 tests, 16/16 mutants); it *blocks* at
  Stop, fires on the 01:07 hand-off at 20 s, silent on the 10:07 one (`:141-149`). **NOT INSTALLED** (a blocking Stop
  hook on every seat is not a pane's call) and **NOT VERIFIED that a block re-prompts a live pane** (`:191-210`).
  Right home is `lap-row.js` refusing a hand-off row with no prior ring — off-limits this pass.
- **L · P-LIVE-RED** `handback/p-live-red_2026-09-03.md` (252 l; `15efc22`). The chair's BUILDING edit caused neither
  red (`git archive HEAD` vs working tree: one-line diff) (`:7-35`). `carrier-drift` **never green — 40 commits, and
  `fed34a6` claimed to close it** (`:60-77`); 5 of its 6 findings were this seat's `map/M-*.md` extracts *quoting*
  withdrawn wordings — found twice yesterday by two panes, filed, unreached (`:79-92`). Fixed by declaration, not
  path (8 of 8 extracts declare; 0 of 3 live maps): **6 → 1 finding** (`:132-180`). The one left is a decision:
  `third_place/` is gitignored, so a registered site there is machine-dependent forever; three options priced,
  recommends a `machine_bound` kind, did not take it (`:186-213`). `commit-gate` red since `e8ee98d` (a PARKED notice
  quoting "WHAT YOU OWN" in a blockquote at `:12`); **one-line fix proven in a worktree**, unlanded — not L's file
  (`:94-128`). Gate is NOT ARMED here.

### Cross-packet — what only the collation can see
1. **Every pane's return leg was refused, and K's finding predicts it generally:** `call_librarian` needs holder
   `panes`, which only the chair's `dispatched` row sets. Ring-before-row fixes the *outgoing* seat's case, not this
   one. The row was owed by the chair at dispatch; five seats paid for its absence with a wait on the board.
2. **Generator edited under measurement.** A committed an EXCLUDE entry (`0d2a2d9`) while J measured against the
   uncommitted version; J's degenerating clause 2 — *"a manifest gap closed by EXCLUDE rather than by shipping"* —
   fires on A's own entry. A's reason is sound (the test requires a file that does not ship). **CONTESTED; the
   registration must rule which EXCLUDE reasons are legitimate.**
3. **Every whole-suite figure today is a moving baseline** — A: 66/3 of 70 → 67/4 of 73; J: 67/3 → 68/4; L names
   it. Quiet-window run at 11:50 is the citable one (below). The per-file numbers stand.
4. **The falsifier I registered is refuted by attack** (J §1) and B's own §8 reached the same third leg
   independently — *run the generator and read its own report*. Three legs now on the table: B's Leg 1 (cold-box
   sweep, MUTE+CRASHES > 0; J's attack 3 says a legitimately silent tool refutes on a non-failure), B's Leg 2 (20
   cloners, armed-on-creation), J's parity leg. **Rule on them next lap, before anything is built.**
5. **Three one-line fixes with named owners, none landed:** `commit-gate.js:165` anchor (L, proven); `isFixture`
   scope + `demachine()` patterns (A, mutation B); `js-suite.js:41` docstring (A). Plus the 3 manifest gaps (J), the
   `Regina` default at `ui/index.html:243` (B), `portable-paths.baseline.json` never ships (A).

### The keeper's lines — the ones that stop the next lap from being written
1. **`record/`** — B's R2 says it ships because two cards say *open it*; the implementation already ships it. Yes/no.
2. **`lighthouse`: private, or workshop-public + `consonance` as product** (B §4 (a)/(b); J's 200s). The 08-22
   ruling *"it was never about privacy"* predates the 09-03 reframe; only the keeper can say which stands.
3. **Install K's blocking Stop hook?** One settings object; it stops every seat on this machine once per hand-off.
4. **T4** — does a fork talk back. Unchanged, still pending since 08-25.

**Corrections taken by this seat, by instrument, four:** `record/` ships (B); dangling set 4 on output not 5 (J);
`isFixture` postdates the break (A); a ring does wake a pane — the trigger is what is missing (K). Struck above,
not moved. **Ledger: surfaced 5** (the missing `dispatched` row as the single cause of five refusals; the EXCLUDE
collision between A and J; the three legs as one decision; the three owned one-line fixes; the keeper's four lines).

**Quiet-window suite, 11:50, panes idle** (`node consonance/tools/js-suite.js`):
`67 green · 4 failed · 0 crashed · 1 canary · 1 not-run (of 73)`. Failed: `carrier-drift.test.js` (L's one decision
row), `commit-gate.test.js` (the unlanded one-line fix), `portable-paths.test.js` (A: `gen-consumer.fixture-scope.test.js`
has 2 BENIGN-TEST sites — K's board note 10:48), `dev/shell/hooks/userprompt_pulse.test.js` (pre-existing since 09-02).
`carrier-drift.js --quiet`: `616 carriers · 1 finding · 35 PENDING`.

## 11:52 — return leg rung (delivered, rendered), THEN `--stage D005 return-leg --holder chair`. Ring before row, as 10:24 taught.
The keeper said *leave the orch alone*; this is the loop's own hand-back — one pointer, no reminder. Nothing else is
owed from this seat this lap. What waits on the keeper is the four lines above; what waits on the chair is the
`filed` row and the next dispatch.

# 2026-09-10 — the DESKTOP librarian, keeper's side project: Thunderhead Raceway Night Optimized (08:31–09:45)

Off-room work at the keeper's ask; the deliverable lives outside this repo
(`solariz3d/thunderhead-night-optimized`, private; `Desktop\ThunderheadRaceway_NightOptimized_v1.zip`).
Kept here only for what this seat did wrong, because those carry over:

- **WRONG (mine): "that proves the whole mechanism"** — said before opening the log. `log.txt` showed AC had
  not run since before the build; the "it worked" was Content Manager listing the layouts. Caught by looking
  one command later, corrected to the keeper in the same turn. Same class as D056-M-02: a claim about the
  world made from the sense of what just happened, not from the instrument sitting right there.
- **Shell-escaping ate backslashes three times** (sed `\a`→BEL, node -e `\t`→TAB, a grep pattern) — twice in
  committed text, once in a checker that then reported a false 0. Rule: text containing backslashes goes
  through the Edit/Write tools, never through a shell string. Also: `set -e` did not stop a run after a
  failed `node -e`, so a build half-ran twice; check each step's exit explicitly when it matters.
- **Two live-state surprises to the keeper:** restoring a config CSP reads live while he was on track, and
  removing a layout Content Manager had selected (crash). Both are "the other side of the change is a person
  mid-use" — warn before touching anything the running game or CM holds.
- **What held:** CSP per-layout lights settled by four independent checks (in-game test, DLL strings,
  condition inputs, Lua API) before building around it; every artifact verified by hash at its destination,
  including what GitHub serves; the installer tested on a copy, never on the live track.

## 2026-09-10 11:46:40 — DLSS 5 on AC: two of my claims, and what the disk said instead

- **I told the keeper to update CSP to 0.3.0 for DLSS.** Wrong direction. The tool's own
  `dlss5-bridge.cfg` documents a mode for a game with no DLSS (`synth_after`: build a substitute
  contract from ReShade depth + driver optical flow). It was off (`synth=0 synth_after=0`, log line 18).
  And 0.3.0-preview581 was already tried here on 09-03: five crash zips 11:25–11:54 in
  `Documents/Assetto Corsa/logs/dmp-693-*.zip` ("Crashed while running: Fpv Drone Lua app"), rolled
  back at 11:58 (`dwrite.dll.bak-csp030-20260903-1158`, 138,479,904 B). The keeper said "it's its own
  program, inject into any 64 bit executable" — the config agrees with them, not with me.
- **A blind instrument nearly printed "not loaded".** A drive-letter regex over the 09-05 minidumps
  returned 0 module paths — not even `dwrite.dll`. Void, not absent. Filename search then found
  `acs.exe`/`dwrite.dll` present and all three DLSS 5 files absent in both 09-05 dumps: the
  bridge was not loaded in the two crashed 09-05 sessions. What "working" looked like before is still
  not on disk (the bridge log is overwritten per launch).
- **Changed:** `synth_after=0` → `10` in the AC-root `dlss5-bridge.cfg`; backup
  `dlss5-bridge.cfg.before-synth` (3,655 B). First attempt with `sed -i` rewrote line endings — the
  fourth shell-mangling of this project; redone from the backup in node, 77 CRLF lines kept.

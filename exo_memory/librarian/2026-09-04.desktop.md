# 2026-09-04 — the DESKTOP librarian: D006, the Valheim rain path, by door one

*Per-machine file (two-writers rule). Yesterday's master: `2026-09-03.desktop.md`. Times local (Regina).*

## 03:36 — LAP D006 MAP: a Valheim performance mod from a decompiled hot path. Guess NOT read (sealed blind; I have not opened `guess_D006_valheim_rain_2026-09-04.md` and will not until the map row lands).

**Read at source:** `loop/finding_valheim_roofcheck_2026-09-04.md` (86 l, `7cad678`). The chair's four questions, then the
work-shape. Every figure below has its command beside it or in the file it points at.

### THE RHYME — the room has decompiled this game before, and it already read WearNTear
- **`C:\Users\nname\Desktop\valheim-agent`** (private repo, `gh repo view` → `isPrivate: true`, pushed 08-22; local HEAD
  `682d7c8` 08-20). **A BepInEx/Harmony plugin for this exact install already exists and was built here:**
  `plugin/*.csproj` (net472, `BepInEx.Core 5.4.21`, `$(MANAGED)\assembly_valheim.dll` HintPath, hard error if absent —
  `:7,:19,:39-40,:89`). The build pipeline the mod needs is on disk; a new mod is a second project beside it, not a first.
- **`valheim-agent/design/game/building.md`** — WearNTear read from IL on 08-12 with the room's own decompiler
  (`research/il_dump.ps1`, Cecil-based; `:6`): `GetMaterialProperties` `:38-40`, `UpdateSupport` `:65`, `UpdateWear` `:231`,
  and **a roof check at `:192-197`** — `Cover.GetCoverForPoint(m_roofCheckPoint.position, …)`, threshold 0.7. The finding's
  path is `WearNTear.RoofCheck` (a 100 m `SphereCastNonAlloc`, `finding:17`). **Two roof checks in one class; the
  registration must establish they are different call sites before "unclaimed" is said of either.** `:245`: *"Nothing
  reads WearNTear"* — true of the agent, and the reason the rain path was never seen from inside.
- **The first Valheim round, `loop/pane_permission_gap.md:16-20` (08-12):** *"no public decompile of the game newer than
  2021 exists"* — a fresh pane wrote an ECMA-335 metadata reader, another found Mono.Cecil on the machine. Tonight's
  dnSpyEx pass is the third decompile of this assembly by this room. The finding does not cite the first two.
- **`memory/split-the-work-with-the-panes.md:28`:** *"The valheim-agent repo has fourteen instances of correct code pointed
  at the wrong referent."* The failure class of the last Valheim build was **producer with no reader** — name the
  consumer of every piece. For a mod the consumer is a stranger's install; that is Q1.

### Q1 — a public mod is NOT under D005's rulings. It is its own class, and the law for it already exists.
D005's two open keeper calls (`librarian/2026-09-03.desktop.md`, collation §keeper lines; `loop/consumer_registration:231-242`)
are about **this repo's tree** — which of `exo_memory/` ships, whether `lighthouse` is private. A Harmony DLL for a
third-party game contains none of that tree. The precedent is `valheim-agent`: a separate private repo, the keeper's
product, the plugin installed locally and **never published**. What governs publication is older and binding on every
seat: **`COMMITTEE.md:133` rule 3 — nothing is pushed by a seat; publishing outward keeps a human awake saying yes**, with
its grounds at `journal/2026-07-28.md:188-189`: *"the law was never don't publish. It was NO UNATTENDED PROCESS PUBLISHES."*
So: the mod is a separate artifact in a separate repo; a seat may build and commit it; **the keeper publishes it**, and the
registration names the repo, the licence of OUR code (the finding rules Harmony-only at `:81-86`, correctly — Iron Gate's
bytes never ship), and the publish step as his. **This is the first seat-built artifact that would leave the room** — the
finding is right about that — and the room's answer is the one it has had since July.

### Q2 — there is no prior-art rule. There is a class rule, and a design that routes prior art through this seat.
- **The finding's citation `muscle_map.md` is unanchored:** `grep -n -i "wider than the query|sentence wider|narrow query"
  exo_memory/muscle_map.md` → 0 lines. The class is real; the pointer is not. Where it IS anchored:
  - **P-UNIVERSE clause 1**, `loop/universe_print_registration_2026-08-25.md:26-30`: *the count enumerated from an
    authority outside the instrument.* `head -20` is an instrument whose universe is its own cut-off — the exact shape.
  - **WRONG #66, this seat's**, `librarian/2026-09-02.md:612`: *"a claim published from a grep COUNT before the rows were read."*
  - `journal/2026-08-16.md` (BOOT's pointer): *support cannot be inferred from position.*
- **The room's design for prior art is `BUILDING.md:98-125`:** *give the Librarian something to MEASURE.* Prior art is
  the map's job, against a description; a chair's own `ls | head -20` is a reach. The near-miss happened in the step the
  loop assigns to another seat. **Ruling:** no new rule; the packet for prior art goes to a non-author with P-UNIVERSE
  as its bar (print N files seen / M skipped / the rule), and the map is where "unclaimed" gets said.

### Q3 — the profiler falsifier: half exterior, and as written it cannot run on this install
- **Not runnable as written.** `valheim_Data/boot.config` carries no development-build or player-connection line
  (`cat` above); Unity's Profiler attaches to development builds. Valheim ships release. **"Unity's profiler on a
  >2,000-piece base" (`finding:70-73`) names an instrument that will not attach here.** Registered falsifier, unrunnable
  — the D005 shape (`handback/p-consumer-reg-attack_2026-09-03.md:43-92`: *a check whose universe is the one box it
  cannot fail on*), one hop over.
- **What is runnable, and which half is ours.** Attribution needs a Harmony prefix/postfix `Stopwatch` around
  `UpdateCover`/`RoofCheck` — **authored by us**, the curated-auditor class (BOOT). The **denominator** — frame time /
  FPS in rain on a real base, with and without the patch — is rendered by the game and read off an overlay nobody here
  writes: **exterior in the sense BOOT's amendment keeps for MEASUREMENT.** Register both halves and say which is which.
- **The threshold is not a number.** *"a measurable share"* (`finding:72`) is the degenerating-clause shape B named at
  `consumer_registration:328` (*the bar moves after the sweep returns*). Fix a number before the run.
- **Machine-bound** (`loop/machine_bound_class_2026-08-25.md:14-20`): the keeper's base is one box. A mod for strangers
  needs the falsifier run on a second base — a scripted 2,000-piece world is buildable and cheap.
- **And the symptom is not recorded.** The finding is mechanism from cold code; nowhere does it say the keeper's FPS
  drop *correlates with rain*. That is the cheapest exterior test of all — FPS counter, wait for weather, no code — and
  it precedes any stopwatch. `loop/objectives_not_only_falsifiers_2026-09-01.md:5-8`: the mod needs an OBJECTIVE (FPS
  gained on his base in rain), not only a falsifier.
- **Fact the plan depends on, read from disk:** BepInEx is present but **OFF** — `G:\…\Valheim\winhttp.dll.OFF`
  (doorstop disabled), `ValheimAgent.dll.PARKED-*`. Every Harmony measurement needs it on; the keeper's switch.

### Q4 — ring-before-row: not written where a chair reads by default, and a doc line is not the fix
- Where it lives now: `librarian/2026-09-03.desktop.md` 10:24 and K's `handback/p-baton-wake_2026-09-03.md:27-42`.
  Where the chair reads by default: `brief/BUILDING.md` THE ORDER OF A DISPATCH (`:208`) — **one line there, chair's pen.**
- **But the count is now seven refusals in a day** (me 10:24; B, K, A, J, L 10:27–10:46; the chair 03:35) with the rule in
  context for the last one. K's §8 (`:211-214`) names the right home: **`lap-row.js` refuses a hand-off row with no
  audited ring in the same turn.** That is a packet, disjoint (`lap-row.js` + its test), and it ends the class.

### THE WORK-SHAPE — roles, never panes. NOTHING IS BUILT THIS LAP.
1. **MEASURE-FIRST** (one body; owns a new project dir beside `valheim-agent/plugin`, nothing in lighthouse): the
   stopwatch plugin on the csproj shape at `valheim-agent/plugin/*.csproj`; the exterior FPS read with/without in rain;
   the symptom test (does the drop track `IsWet`); the threshold as a NUMBER, registered before the run; BepInEx on
   (keeper). Returns a number or "no rain in the window".
2. **PRIOR-ART, RE-DERIVED** (a non-author of the finding; owns one file): VPO grepped unfiltered over ALL its patches
   (not `WearNTearPatches.cs` alone — its `UpdateSupport` transpiler could reach `UpdateCover`); Thunderstore/Nexus for
   rain/roof mods; and the two-roof-checks reconciliation (`building.md:192` vs `finding:17`). P-UNIVERSE bar.
3. **REGISTRATION** (one body; owns `loop/valheim_roofcache_registration_2026-09-04.md`): repo home, licence of our
   code, publish = keeper's push, the objective, the two-half falsifier from 1, the three-part patch scoped Harmony-only,
   DEGENERATING IF: threshold moves; a measurement taken on one base only; any Iron Gate bytes in the artifact.
4. **ATTACK** (a non-author of 3; owns its own file), before anything is built.
5. **BATON-ORDER GATE** (disjoint: `lap-row.js` + test; Q4) — can run in parallel with 1–4; touches nothing they own.
Order: 1 and 2 and 5 in parallel; 3 after 1 returns its number; 4 after 3. **The patch (the finding's `:75-79`) is next
lap, and only if 1 fires.** Adversarial readers: 2 and 4 must not have written the finding.

**What this seat did not verify:** that Unity's Profiler cannot attach without a development build (read from
`boot.config` absence, not tested); VPO's other 20 patches (not opened — packet 2's job); whether `Cover.GetCoverForPoint`
and `RoofCheck` share a call site (packet 2). **Corrections offered to the finding:** the `muscle_map` cite is
unanchored; the falsifier's instrument does not attach here; the symptom is unrecorded; two prior decompiles uncited.

**Ledger: surfaced 8** (the existing plugin + csproj; building.md's roof check; the first-round decompile; the
publishing law as the class answer; P-UNIVERSE as the anchor the finding lacks; the profiler-attach problem; BepInEx
off; the symptom test before any code).

## 03:52 — rung (delivered, rendered), THEN `--stage D006 map --holder chair`. Ring before row. The sealed guess is opened only now, below.

## 03:49 — the keeper, at the librarian: *"i do not want to have to do that [monitoring a storm], cant you just fix the problem you see and be done with it then test it?"*
**Direction is his instrument (`BUILDING.md:128-131`); taken.** And the board says what the symptom actually was
(chair's pane, 02:54–03:19): **280–300 → ~140 fps after removing the DLSS 5 injector — exactly half**, VSync off, cap
360; the chair's own first read was *"points at vsync or a frame cap, not at lost performance"*; BepInEx was turned off
at 03:17 because it **loaded zero plugins** (the agent DLLs are parked) — pure overhead, not a suspect. Then: *"can we still
try fix the same problem? It would help for keeping up frames in big bases with lots of assets or rain or both."*
**So the rain path is an improvement the keeper asked for, not the cause of the halved frame rate.** The finding does not
say this and the map's "symptom unrecorded" line is now anchored: the 140 has its own candidate (cap/vsync/D3D12) and
the mod will not touch it. Say so to him plainly.

**The re-shape, keeping the one discipline that matters — a number before and after — without his eyes on a storm:**
packet 1 (MEASURE-FIRST) and the patch collapse into **one Harmony plugin, one packet**: the three-part patch behind a
config toggle, plus a logger (frame time, `EnvMan.IsWet()`, piece count, stopwatch around `UpdateCover`) writing to
`BepInEx/LogOutput.log` or its own file. Rain is **forced from the console** (`devcommands` → `env Rain`), not waited
for. A pane flips `winhttp.dll.OFF` back (one rename; his word was given above), launches his world at his base, runs
toggle-off then toggle-on under forced rain, reads the log. **Threshold stays a number, written in the packet before the
run.** Prior-art (2) and the gate (5) unchanged; registration (3) becomes the packet's own header; attack (4) reads the
log, not the code. The keeper's part: nothing, unless a pane cannot launch the game — then one click.
**Delivery:** holder is `chair`/`panes` (dispatched 03:43); this seat's verb is refused; filed here and on the board; the
keeper carries the line to the chair.

## 04:02 — B's hand-back (packet 1, `d7ebfc6`) arrived BY THE VERB — `[pane:B]`, holder `panes`, the dispatched row written at 03:43. First hand-back to reach this seat by the system since the edge was built.
**Verdict claimed: the mod is DEAD — the rate is wrong by the framerate.** Re-derived here before carrying it anywhere:
- `WearNTearUpdater.cs:15-23` (chair's decompile, `scratchpad/full/assembly_valheim`): `deltaTime = Time.deltaTime` — one
  frame — passed to `UpdateWearNTear`. `:30-32`: the cover `foreach` runs only when `m_sleepUntilNext == m_sleepUntil`,
  then sets `m_sleepUntilNext = time + 1f`. `:98`: `m_sleepUntil = m_sleepUntilNext` after the wear sweep wraps. So one
  cover pass per ≥1 s cycle, carrying ONE frame of dt. `WearNTear.cs:624-639`: `m_updateCoverTimer += dt; if (<= 4f)
  return;` — 4f is four seconds of FRAMES delivered one per second. **Holds at source.**
- IL, my own run of `valheim-agent/research/il_dump.ps1 -Type WearNTearUpdater -Method Update`: `IL_0006 call
  Time::get_deltaTime()` → `ldloc.1` → `UpdateWearNTear(Single,Single)`. **Holds at IL.**
- `node rate.js 5000 140 3600 1` → 8.91 casts/s; closed form 8.93. Threshold (5% of frame) needs 5,611 µs per cast.
**PASS. The finding's mechanism is right; its rate was never derived.** The one untested auxiliary is Unity's documented
`Time.deltaTime` semantics — B names it. Not machine-bound: the result is a function of fps and piece count, and gets
cheaper as fps rises.

**What it does to the lap:** the object is dead; nothing is built; **that is the packet's registered good outcome**
(`THRESHOLD.md`, DEAD clause). Packet 2's reconciliation is answered in passing — `Cover.GetCoverForPoint` is called from
`Player.UpdateCover` (`Player.cs:3652`), a different class — A should re-derive, not adopt. Packet 5 (the baton gate) is
untouched by any of this and stays worth its cost. **Surviving, not proposed:** the same timer makes rain damage on an
uncovered piece start ~2×fps seconds late (`m_haveRoof` init true, `:1281`; written only at `:633`) — a correctness bug
invisible in play; a mod with no reader (`split-the-work:28`). Named so the next lap does not mistake it for a rescue.
**The keeper's 140 fps** (03:49 entry) was never this path; it is still unowned.

**Ledger: surfaced 1** (the re-derivation, three instruments, PASS). Owed: collation when A and K land (`handbacks 1 of 3`).

## 04:12 — A's hand-back (packet 2, PRIOR ART) arrived BY THE VERB. **Uncommitted** — `handback/p-d006-priorart_2026-09-04.md` untracked, `map/A.md` modified; A wrote "Nothing committed by me." K's `lap-row.js` edits are in the same working tree (packet 5, in flight). The P-COMMIT-GATE class: one careless add captures both. Flagged to the chair; not committed by this seat (not mine).
**Re-derived here before carrying:**
- **RockBreaker/ValheimBuildOptimization 0.5.5** — A's decompile at its scratchpad `dec2/…/BuildPieceProfilerPlugin.cs`:
  `[HarmonyPatch(typeof(WearNTear), "UpdateCover")]` with a `Prefix(WearNTear)`; config `:1944` *"Stops structural-support
  and weather-wear calculations."* Thunderstore index (`ts.json`, 162 MB, **10,498 packages** — my own parse): updated
  2026-06-23, 2,496 downloads. **Holds.** "Unclaimed" is refuted; what survives is *nobody has done the
  behaviour-preserving cache*, and at ~9 casts/s nobody needs to.
- **NoRainDamage** (`dec/Jowleth_NoRainDamage/NoRainDamage.cs:65`): `AccessTools.Method(typeof(WearNTear),"HaveRoof").Invoke`
  inside an `UpdateWear` prefix — a reflected spherecast per wear tick, added by the most-installed rain mod. **Holds.**
  Not on the keeper's install (`BepInEx/plugins` holds only the parked agent DLLs).
- **`Cover` is in `assembly_utils.dll`** — byte-grep: both DLLs carry the string (call sites vs definition); A's grep of
  the 608-file dump finds no `Cover.cs`. **Holds.** The finding's instrument covered one DLL of nine.
- **The cadence correction** — A reached B's result independently, from a different packet, without being asked
  (A `:193-230`; B `:43-64`). Two seats, one arithmetic, decorrelated. Already re-derived by me at 04:02.
**WRONG #71, found by A:** my map's RHYME line — *"Two roof checks in one class"* — is false. `GetCoverForPoint` has 0
hits in `WearNTear.cs`; `m_roofCheckPoint` lives on `CraftingStation`, `Fermenter`, `Smelter`. I summarised
`building.md:192` past its own heading. Struck here, not moved; the reconciliation I asked for was answered by refuting
its premise. (A's `:184` heading cite is off by some lines in the current file — immaterial.)
**A's own near-miss, kept:** its first extraction got DLLs from 133 of 209 packages; RockBreaker was among the 60 missing.
Caught by cross-checking the extractor against the archives' listings — the `head -20` class inside the packet written
to answer it, A's words. **Nexus unreached (403)** — A prints the hole; anyone quoting this must not say "and Nexus".
**Ledger: surfaced 1; WRONG 71 (A).** Owed: K, then collation.

## 04:50 — K's hand-back (packet 5, BATON-ORDER GATE) arrived BY THE VERB — three of three in. `3f363c1` landed `lap-row.js` + test + mutation harness. **Re-derived before carrying:** K's all-time refusal count — its own one-liner re-run here → **9**. `baton-wake.js` blindness — board carries **585** lines of the chair's real audit shape (`chair injected (chair:`) against **18** matching the markers `baton-wake.js:143` gates on. Holds. K's suite/mutation numbers (97/97, 39/39) are K's; my own run is in a background task and lands below when it returns.

## 04:55 — LAP D006 COLLATION. Three hand-backs, read at source, each re-derived. The object is dead twice over; the lap's one artifact is not about Valheim.

### Per packet — pointer · verdict · re-derived by this seat · what it did not verify
- **B · MEASURE-FIRST** `handback/p-d006-measure_2026-09-04.md` (171 l; `d7ebfc6`; threshold first at `valheim-roofcheck/THRESHOLD.md`).
  **DEAD by a pre-registered 5%-of-frame threshold, ~1000×.** The cover pass runs once per ≥1 s cycle carrying one
  frame's `Time.deltaTime`; the "4 s" timer is 4 s of frames. Re-derived here at source (`WearNTearUpdater.cs:15-23,
  :30-32, :98`; `WearNTear.cs:624-639`), at IL (`il_dump.ps1`, `get_deltaTime` → `UpdateWearNTear`), and by its model
  (8.91 vs closed form 8.93). Not verified by anyone: a runtime frame. B's one auxiliary is Unity's `deltaTime` semantics.
- **A · PRIOR ART, RE-DERIVED** `handback/p-d006-priorart_2026-09-04.md` (294 l; **uncommitted**, A's map line too).
  **"Unclaimed" REFUTED:** RockBreaker/ValheimBuildOptimization 0.5.5 (2026-06-23, 2,496 dl) prefixes `WearNTear.UpdateCover`
  behind a config toggle — re-read in A's decompile and in the 10,498-package Thunderstore index (my parse). VPO
  confirmed clean over all 43 files by `git ls-files`. **A reached B's cadence correction independently** (`:193-230`).
  NoRainDamage (114k dl) adds a reflected `HaveRoof()` per wear tick (`NoRainDamage.cs:65`, re-read). `Cover` is in
  `assembly_utils.dll` (byte-grep, both DLLs). **Nexus unreached (403) — printed as a hole.** A's own near-miss: 133 of
  209 packages extracted on the first pass, RockBreaker among the 60 missing; caught by cross-checking the extractor.
- **K · BATON-ORDER GATE** `handback/p-d006-baton-gate_2026-09-04.md` (293 l; `3f363c1`). Built into the writer: a row
  that moves the baton needs `--by <station>`; a hand-off with no audited ring since the baton last moved is refused
  naming the recovery; a re-take (`--by X --holder X`) always passes; no `--force`; disarms (prints UNCHECKED) when no
  board sits beside the ledger or `blind.lock` exists. **The unwelcome number first, K's own: 2 of 9 refusals are this
  class.** Five were the missing `dispatched` row; two were a lap left open for days. Retrodicted over all 17
  holder-moving rows: 3 true positives (the three recorded failures), 4 re-takes allowed, 1 unresolvable (D001),
  **0 correctly-rung hand-offs refused.** Found in its own prior file: `baton-wake.js` cannot see the chair's audit
  shape (585 vs 18 here) — one-line fix owed to whoever holds it; not live, so nothing was mis-reported. A surviving
  mutant was a real defect (a keyword guard that discarded 2 real deliveries whose preview quoted "FAILED"), removed.
  `brief/BUILDING.md:208` line **drafted in §7, not landed** — shared carrier, chair's pen. Not verified: a live hand-off.

### Cross-packet — what only the collation sees
1. **Two seats converged on the one correction that killed the object, from different packets, unasked.** B was sent
   to measure; A was sent for prior art; both derived `N·dt/4`. That is decorrelated error — the criterion the room set
   for the committee being another mind (BOOT, add-and-hold; `split-the-work:15-18`). The finding's author read the same
   lines and reported the rate it implied rather than derived. **The loop's value this lap was that nobody built.**
2. **Dead twice, by independent routes:** magnitude (B) and prior art (A). Either alone kills it. Zero lines of Valheim
   code were written. BepInEx untouched. The keeper was asked for nothing.
3. **The instrument-vs-its-own-universe class fired inside two anti-instances of itself:** A's 133/209 extraction inside
   the packet written to answer `head -20`; K's dead keyword guard inside the gate built on the absent-guard finding.
   Both caught by cross-checking the instrument against an authority outside it — P-UNIVERSE clause 1, not care.
4. **The lap's only artifact is K's gate, and its first live test is this seat's return leg** (rows below): a re-take
   at `handbacks-in`, a ring, then `return-leg --holder chair --by librarian`, which the gate must PASS on the audited
   ring. If it refuses, K's falsifier 2 fires on the first live hand-off.
5. **Uncommitted in the shared tree at collation:** A's hand-back + `map/A.md`; `lap-row.js` shows a further edit after
   `3f363c1` (diff below). Both are P-COMMIT-GATE exposures; named to the chair, not committed by this seat.

### Owed, and to whom
- **Chair:** `filed` row for D006; land K's BUILDING line (§7) under `:208`; commit A's two paths by name with A's
  attribution; a LEDGER row each for A, B, K; the two lessons the gate does NOT cover — write the `dispatched` row at
  inject; file or void a lap before it ages days (7 of 9 refusals).
- **Whoever holds `baton-wake.js`:** the one-line marker fix (K §4). Whoever holds `gen-consumer.fixture-scope.test.js`:
  the 2 portable-paths sites (`:141,:155`), still red since 09-03.
- **Keeper:** the 140 fps — never this path, still unowned. If he wants the rain bypass today, RockBreaker's toggle
  exists and changes behaviour (no rain damage, no wet visuals — its own config text). His call, not a build.
- **Named, not proposed:** a one-line fix to NoRainDamage's reflected `HaveRoof()`; the `m_haveRoof` init-true lag.

**Corrections taken by this seat this lap: WRONG 71 (A)** — the two-roof-checks line. **Ledger: surfaced 4** (the
convergence as decorrelated error; dead-twice; the class firing inside its own anti-instances; the gate's live test
being the return leg).

**Post-`3f363c1` edit in the tree (`git diff`, one line): `boardPath()` now resolves via `DATA_DIR` instead of `path.dirname(LEDGER)` — mtime 04:50:45, after K's commit. Whose, unknown from here; named.** My own run of K's suite and mutants (background, exit 0):

```

  applied 39 · caught 39 · NOT APPLIED 0  (of 39)

[exited with code 0]
```

## 04:58 — the return leg, and K's gate on its first live hand-off
Ring → **`queued for 0c0c0c0a — its prompt is not idle`** (P-INBOX; the keeper is in the chair's pane). Then
`--stage D006 return-leg --holder chair --by librarian` → **`gate: an audited delivery to chair precedes this row`**, written.
**PASS on a QUEUED delivery** — the gate counts the audited attempt, not the render, which is the permissive side K
registered (§3.2: any audited delivery to the incoming holder passes). Falsifier 2 did not fire. First live datum, n=1.
Holder chair. Nothing else owed from this seat this lap.

## 11:58 — the keeper, at the librarian, on D005's question 2 (verbatim)
> *"I think we need to create a second repo for the consumer version, or make the public one the consumer and the new one
> our private one"*

Two shapes. The corpus prices them (all from the 09-02/09-03 desktop notes, the D004 map and B's §4):
- **(a) `lighthouse` → private; the consumer is a NEW repo generated with fresh history.** No rewrite. `gen-consumer.js`
  already models exactly this at 11 sites (*"the PUBLIC consonance tree from the PRIVATE lighthouse tree"*, `:2` etc.);
  J read those words as ROLES — source vs generated — and (a) makes them true as visibility too. One setting on
  `lighthouse`; one `gh repo create` for `solariz3d/consonance` (does not exist — B §4). The room's one publish law
  holds: the keeper does both, awake.
- **(b) the public `lighthouse` BECOMES the consumer; a new private repo becomes ours.** Then the consumer artifact
  carries this repo's 1,094-commit history — the D004 measurement returns whole: the account names in 68 and 53
  commits, the email as author on 271, and any rewrite kills the 418 real commits cited 1,610 times in the corpus
  (`2026-09-02.desktop.md`, D004 map Q3). (b) is the rewrite question wearing a different repo name.
**Recommendation, this seat's: (a).** It is what the tool already says it does, it needs no history surgery, and the
private copy keeps every sha the record cites. Under (a) the open items are exactly D005's next lap: rule J's parity
falsifier, land the five small fixes, re-point the generator, generate, run the suite in one quiet window, and the
keeper creates the target repo and pushes. **Ringing the chair the inquiry, no map** (door two).

## 12:04 — LAP D007 MAP: the consumer repo's shape, and the lap D005 left open. Guess not read (sealed blind at `431ead3`; unopened until the map row lands).

**The chair's sealed lean, stated in its ring:** a NEW PRIVATE repo, `lighthouse` stays public. **Mine, filed at 11:58
before this lap opened (`e372ec8`): the reverse — `lighthouse` → private, the consumer generated fresh.** The number will
say which prior the map confirmed; the map's job is to price, and the price is on disk.

### THE ONE FACT THAT DECIDES IT, and the chair's ring does not carry it
The keeper's 09-03 override, verbatim (`librarian/2026-09-02.desktop.md:479`): *"should remove the zackn, nname, the email.
Those are actual personal info."* Then the reframe (`:538`): *"it is okay to mention our city here with our dev version…
but on the repo, it should be closer to a consumer build."* **The override was folded into the reframe, not withdrawn:
the identity surface is not to be on the public repo.** Under the chair's lean — `lighthouse` stays public — that surface
(`:485-493`: `zackn` in 73 tracked files and the content of 68 commits; `nname` 52 files / 53 commits; the email as
author on 271 of 1,094 commits) stays exactly where it is, or is removed by history surgery. **So "lighthouse stays
public" costs either the rewrite the chair says it is avoiding, or the keeper's override.** The private flip costs one
setting. The map does not pick; it prices, and that is the price.

### The chair's three, ruled
1. **`record/` is NOT the cost driver here — B already ruled it, and history is the driver.** `consumer_registration:47-62`,
   R2: a trace ships iff a shipped instrument cites it by name; two cards cite `record/` (`cards/claim-your-continuity.md:23`,
   `cards/stop-and-feel-it.md:29`), so it ships as the resolution of a pointer, and `gen-consumer.js:91` already does.
   That is a statement about the TREE at HEAD. Whether the public repo is the consumer turns on the HISTORY (68 / 53 /
   271 commits above), which no `record/` decision touches. **The two questions are not the same question.** The
   keeper's yes/no on `record/` stands as his — B's rule is the default if he says nothing.
2. **The identity surface was re-derived by a seat that is not the chair on 09-03** — this one, at
   `2026-09-02.desktop.md:485-493`, with the commands: tree 73 / 52 / 2 (the chair's 72 differs by one file), history
   68 / 53 / 271. The chair's ask — *someone who is not me* — is met; a third seat re-running it in this lap is cheap and
   is packet 5 below. ASK-011's 61-files undercount is on record at `:485` beside the true count.
3. **`filter-repo` — the numbers are this seat's (D004 map Q3, `:399-406`) and the chair now cites them right.** 1,942
   path:line citations survive; 1,610 sha mentions across 418 real commits die. **Moot under the private flip; the
   whole cost under "lighthouse stays public and the override holds."**

### The fourth — a chair's finding entering a lap as the OBJECT: the rule exists, is registered, and is not built
- **The design is `BUILDING.md:98-125`:** the description IS the object — *measure it against the corpus.* A finding
  entering as the probe is not the defect. The defect is a finding entering **un-read by a non-author** and then
  dispatched as premise. That has a registration: **`loop/second_vantage_registration_2026-08-31.md`** — the non-author
  read before delivery; verdict SPLIT, the gated form REGISTERED (`:39`, `:126`), *with a precondition the chair's own
  pane currently fails* (`:167`, `dispatch-gate.js:26-35`). **Not built.** D006 is its worked case: the finding shipped
  as the object with no pre-read; two panes did the read after dispatch and killed it in an hour.
- **The room's other anchor is the sealed guess itself** (`lap-row.js` header): the chair's prior is recorded so it can
  be scored, not so it can be trusted. And `CLAUDE.md` (this seat): *the pane's finding was right and the hop invented the
  premise* (09-01) — the same class, opposite direction.
- **The absence worth a line:** nothing says a chair-authored FINDING (as opposed to a chair-authored DISPATCH) needs a
  reader before it becomes a lap's inquiry. The registered pre-read covers deliverables; a finding is upstream of one.
  **Named; not a new rule from this seat.** The chair's own concession in its ring is the right shape: the finding
  enters as a premise to be measured, and D006 shows the loop measures it.

### THE WORK-SHAPE for the consumer lap — roles, never panes; disjoint files named
- **P1 · RULE THE FALSIFIER** (one body, non-author of B's and J's docs; owns `loop/consumer_falsifier_ruling_2026-09-04.md`):
  the three legs — B's cold-box sweep (MUTE+CRASHES > 0), B's clone-count (20, armed-on-creation), J's generated-tree
  suite parity (today 42/17/3 of 63 vs 68/4/0 of 73; 18 files break) — become ONE registered falsifier with a number,
  plus the OBJECTIVE (`objectives_not_only_falsifiers_2026-09-01.md:5-8`): a stranger's first wake reaches SEED and the
  generated tree's suite is green. DEGENERATING IF a manifest gap is closed by EXCLUDE rather than by shipping (J), or
  the parity delta is still non-zero after two laps.
- **P2 · THE SMALL FIXES**, three bodies, disjoint by file:
  2a `consonance/tools/commit-gate.js:165` (anchor, L's proof in a worktree) + `js-suite.js:41` docstring;
  2b `gen-consumer.js`: `isFixture` scope + `demachine()` patterns (A's mutation B, `P-GEN-RED-FIRST:84-107`), the three
     manifest gaps (`dev/shell/install.ps1`, `src-tauri/tests/`, `dev/shell/hooks/userprompt-submit.js`),
     `portable-paths.baseline.json` (`.json` never ships), and the 2 red portable-paths sites in the fixture-scope test;
  2c `ui/index.html:243` (the Regina default) + `brief/BOOT.md` pronouns (12 hits on 4 lines).
  Each: red-first, mutation shown, the canary at `gen-consumer.fixture-scope.test.js` retired when its red clears.
- **P3 · RE-POINT THE GENERATOR** at B's R0–R4 and C1–C5 (one body, after 2b lands; owns `gen-consumer.js` MANIFEST/EXCLUDE
  only): the predicate as a filter on the manifest, never the ship list (B's R0).
- **P4 · GENERATE AND RUN, ONE QUIET WINDOW** (one body, after 2 and 3; no other seat editing; owns nothing in the
  repo — writes `loop/consumer_parity_2026-09-04.md`): `gen-consumer --out`, `js-suite` inside it, `cargo check` inside it,
  the parity number against P1's bar. Report the moving-baseline caveat if any seat was editing.
- **P5 · IDENTITY SURFACE, RE-DERIVED** (one body, not the chair, not this seat; owns one file): tree and history, universe
  printed, both accounts, the email, the coordinates. Parallel with everything.
- **Keeper's actions, not packets:** visibility flip on `lighthouse`; `gh repo create solariz3d/consonance`; the push.
  And the two open lines: `record/` (B's rule is the default), the Stop hook.
Order: P1, P2a/2b/2c, P5 in parallel → P3 → P4. Adversarial: P1's ruling read by a non-author before P4 runs.

**Not verified by this seat this lap:** nothing new was run; every number above is cited to the file that ran it.
**Ledger: surfaced 4** (the override folded into the reframe as the deciding fact; `record/` ≠ history; the identity
re-derivation already exists at :485-493; the pre-read registration as the chair's fourth answer, with its absence).

## 12:12 — rung (queued; the keeper is in the chair's pane), THEN `--stage D007 map --holder chair --by librarian` through K's gate. The sealed guess opened only now:

## 12:29 — L's hand-back (P5, IDENTITY SURFACE, third seat) BY THE VERB (delivered FORCED after the bounded hold — this pane was not idle). `handback/p-d007-identity_2026-09-04.md`, uncommitted, L's two files only. The chair dispatched all five at 18:13Z through K's gate (A=P1 · B=P2b · K=P2a · J=P2c · L=P5).
**Re-derived here before carrying, every figure:** `nname` raw 61 / strict `(?<!u)nname` 49 / `unnamed` occurrences 16 =
148 − 132 ✔. Root commit `31974c8` carries `zackn` in 9 files ✔. `origin/main` 74 · `HEAD` 77 ✔. `main` = 981 commits ✔.
Authors: `solariz3d@…noreply` 676 · the email 304 · anthropic 1 ✔. L's falsifier 1 (any commit on `main` with zero `zackn`
files) is running in full here as a background sweep — result appended when it lands.
**What L changes, and it is three things, none a count:**
1. **Nobody miscounted.** The chair's 72 and my 73 are one tree ten minutes apart; the 73rd file is my own D004 map.
   *The document that reports the count adds itself to the count* — L's own hand-back does it twice. ASK-011's 61 was
   exact on 08-30 (743-file tree); it is **stale, not an undercount** — a dated figure served as current (`ask.js`).
2. **`nname` is inflated by "unnamed"**: 12 files at HEAD match and carry no account name (`journal/2026-07-05.md`,
   `dev/mutation/mutate-name.js`, …). A bare substitution corrupts them. Any scrub pattern must be anchored, and
   `gen-consumer.js` / `portable-paths.js` + their baselines/tests **carry the strings as their own patterns** — 2 of
   the 3 email files are the scrubber. Exempt them by name or the scrub disarms the guard.
3. **The history unit.** My "68 commits" was `git log -S` — commits where the count CHANGED. The unit that matters to a
   stranger is commits whose TREE carries it: **981 of 981 on `main`, from the root commit**; `nname` in 867; the email
   as author on 304 (`HEAD`). **So a tip scrub of a public `lighthouse` is cosmetic; only private or a full rewrite
   meets the keeper's 09-03 standard.** This sharpens the D007 map's price in the same direction it already pointed.
4. **The deciding universe is `origin/main` (`db33412`, 1,597 files), not local HEAD** — 37 commits unpushed. The
   keeper's decision is priced off what a stranger can fetch now: `zackn` 74 · `nname` strict 42 · email 3 · lat 9 ·
   lon **1** (`main.rs:7975`) · `Regina` 21 · hostname 6. Product code outside `exo_memory/`: 18 files, flat since
   08-30; the growth 61 → 77 is all prose about the leak.
**Corrections taken by this seat: the history unit (my 68/53 were the pickaxe unit, stated without saying so) — L's
find, WRONG 72.** Ledger: surfaced 1 (the re-derivation). Owed: B, K, J; then collation.

## 12:40 — A's ruling (P1, RULE THE FALSIFIER) read at source before its ring rendered here (queued at 18:28Z; this pane not idle). `loop/consumer_falsifier_ruling_2026-09-04.md`.
**The ruling:** merge B's cold-box leg and J's parity leg into ONE falsifier — **D = P + M + B, REFUTED IF D > 0**, counted
inside one generated tree in one quiet window; **guard I = S − G** (tests in source minus tests in the generated tree)
printed beside D always, with the mechanical anti-gaming rule *ΔD ≤ 0 and ΔI ≥ |ΔD| is degeneration*; **Leg 2 (20 cloners)
DEFERRED, armed on repo creation, threshold unmoved.** Objective split O1 (D = 0) / O2a (SEED present + declared in the
bundle) / O2b (`pick_default_room` unit test) with the third part — *a person read it* — named as uninstrumented.
**D ≥ 20 today: the falsifier starts red**, which A rightly calls the strongest kind. Seven degenerating clauses; A's
conflict declared (§9: it authored the EXCLUDE entry J's clause points at, and mechanised the clause to count itself).
**Re-derived here:** README.md:149-154 names exactly the five tools ✔. `ferry.js --due` on a cold data dir: uncaught
`child_process` throw ✔ (BROKEN). `board-audit.js:33` reads `C:\Consonance\data\board.jsonl` via `CONSONANCE_BOARD`, not
`CONSONANCE_DATA` ✔ (FALSE-COLD — a green off our own data). EXCLUDE at HEAD: six entries, three of them tests ✔.
**One unit to reconcile, flagged for P4:** A's S = 74 by `find`; `git ls-files '*.test.js'` at HEAD = **73**. One file,
untracked or ignored; A's G = 66 derived vs J's 63 measured is the larger gap and A flags it itself.
**Owed: B (P2b), K (P2a), J (P2c); then collation.** The full `zackn`-per-commit sweep (L's falsifier 1) is still running.

## 12:48 — A's ring rendered; its hand-back `handback/p-d007-falsifier_2026-09-04.md` read at source. Nothing beyond the ruling except the ask: **the non-author read must happen BEFORE P4 runs**, and A names its own two weakest points first — *D sums three unlike things* and *I merged two legs while measuring one of them in the wrong tree* (M and B measured in the SOURCE tree; the ruling's own §2 requires the generated tree). The map's work-shape already required that read (P1 → attacked → P4). **Surfaced to the chair now rather than at collation, so the attack runs while B, K and J finish**: it needs a body that authored none of B's registration, J's attack, or A's ruling. Roles, not panes.

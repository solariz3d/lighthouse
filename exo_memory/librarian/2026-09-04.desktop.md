# 2026-09-04 — the DESKTOP librarian: D006, the Valheim rain path, by door one

*Per-machine file (two-writers rule). Yesterday's master: `2026-09-03.desktop.md`. Times local (Regina).*

## 03:36 — LAP D006 MAP: a Valheim performance mod from a decompiled hot path. Guess NOT read (sealed blind; I have not opened `guess_D006_valheim_rain_2026-09-04.md` and will not until the map row lands).

**Read at source:** `loop/finding_valheim_roofcheck_2026-09-04.md` (86 l, `7cad678`). The chair's four questions, then the
work-shape. Every figure below has its command beside it or in the file it points at.

### THE RHYME — the room has decompiled this game before, and it already read WearNTear
- **`C:\Users\nname\Desktop\valheim-agent`** (private repo, `gh repo view` → `isPrivate: true`, pushed 08-22; local HEAD
  `682d7c8` 08-20). **A BepInEx/Harmony plugin for this exact install already exists and was built here:**
  `plugin/*.csproj` (net472, `BepInEx.Core 5.4.21`, `$(MANAGED)\assembly_valheim.dll` HintPath, hard error if absent —
  `:7,:19,:39-40,:89`). The build pipeline the mod needs is on disk; a new mod is a second project beside it, not a first.
- **`valheim-agent/design/game/building.md`** — WearNTear read from IL on 08-12 with the room's own decompiler
  (`research/il_dump.ps1`, Cecil-based; `:6`): `GetMaterialProperties` `:38-40`, `UpdateSupport` `:65`, `UpdateWear` `:231`,
  and **a roof check at `:192-197`** — `Cover.GetCoverForPoint(m_roofCheckPoint.position, …)`, threshold 0.7. The finding's
  path is `WearNTear.RoofCheck` (a 100 m `SphereCastNonAlloc`, `finding:17`). **Two roof checks in one class; the
  registration must establish they are different call sites before "unclaimed" is said of either.** `:245`: *"Nothing
  reads WearNTear"* — true of the agent, and the reason the rain path was never seen from inside.
- **The first Valheim round, `loop/pane_permission_gap.md:16-20` (08-12):** *"no public decompile of the game newer than
  2021 exists"* — a fresh pane wrote an ECMA-335 metadata reader, another found Mono.Cecil on the machine. Tonight's
  dnSpyEx pass is the third decompile of this assembly by this room. The finding does not cite the first two.
- **`memory/split-the-work-with-the-panes.md:28`:** *"The valheim-agent repo has fourteen instances of correct code pointed
  at the wrong referent."* The failure class of the last Valheim build was **producer with no reader** — name the
  consumer of every piece. For a mod the consumer is a stranger's install; that is Q1.

### Q1 — a public mod is NOT under D005's rulings. It is its own class, and the law for it already exists.
D005's two open keeper calls (`librarian/2026-09-03.desktop.md`, collation §keeper lines; `loop/consumer_registration:231-242`)
are about **this repo's tree** — which of `exo_memory/` ships, whether `lighthouse` is private. A Harmony DLL for a
third-party game contains none of that tree. The precedent is `valheim-agent`: a separate private repo, the keeper's
product, the plugin installed locally and **never published**. What governs publication is older and binding on every
seat: **`COMMITTEE.md:133` rule 3 — nothing is pushed by a seat; publishing outward keeps a human awake saying yes**, with
its grounds at `journal/2026-07-28.md:188-189`: *"the law was never don't publish. It was NO UNATTENDED PROCESS PUBLISHES."*
So: the mod is a separate artifact in a separate repo; a seat may build and commit it; **the keeper publishes it**, and the
registration names the repo, the licence of OUR code (the finding rules Harmony-only at `:81-86`, correctly — Iron Gate's
bytes never ship), and the publish step as his. **This is the first seat-built artifact that would leave the room** — the
finding is right about that — and the room's answer is the one it has had since July.

### Q2 — there is no prior-art rule. There is a class rule, and a design that routes prior art through this seat.
- **The finding's citation `muscle_map.md` is unanchored:** `grep -n -i "wider than the query|sentence wider|narrow query"
  exo_memory/muscle_map.md` → 0 lines. The class is real; the pointer is not. Where it IS anchored:
  - **P-UNIVERSE clause 1**, `loop/universe_print_registration_2026-08-25.md:26-30`: *the count enumerated from an
    authority outside the instrument.* `head -20` is an instrument whose universe is its own cut-off — the exact shape.
  - **WRONG #66, this seat's**, `librarian/2026-09-02.md:612`: *"a claim published from a grep COUNT before the rows were read."*
  - `journal/2026-08-16.md` (BOOT's pointer): *support cannot be inferred from position.*
- **The room's design for prior art is `BUILDING.md:98-125`:** *give the Librarian something to MEASURE.* Prior art is
  the map's job, against a description; a chair's own `ls | head -20` is a reach. The near-miss happened in the step the
  loop assigns to another seat. **Ruling:** no new rule; the packet for prior art goes to a non-author with P-UNIVERSE
  as its bar (print N files seen / M skipped / the rule), and the map is where "unclaimed" gets said.

### Q3 — the profiler falsifier: half exterior, and as written it cannot run on this install
- **Not runnable as written.** `valheim_Data/boot.config` carries no development-build or player-connection line
  (`cat` above); Unity's Profiler attaches to development builds. Valheim ships release. **"Unity's profiler on a
  >2,000-piece base" (`finding:70-73`) names an instrument that will not attach here.** Registered falsifier, unrunnable
  — the D005 shape (`handback/p-consumer-reg-attack_2026-09-03.md:43-92`: *a check whose universe is the one box it
  cannot fail on*), one hop over.
- **What is runnable, and which half is ours.** Attribution needs a Harmony prefix/postfix `Stopwatch` around
  `UpdateCover`/`RoofCheck` — **authored by us**, the curated-auditor class (BOOT). The **denominator** — frame time /
  FPS in rain on a real base, with and without the patch — is rendered by the game and read off an overlay nobody here
  writes: **exterior in the sense BOOT's amendment keeps for MEASUREMENT.** Register both halves and say which is which.
- **The threshold is not a number.** *"a measurable share"* (`finding:72`) is the degenerating-clause shape B named at
  `consumer_registration:328` (*the bar moves after the sweep returns*). Fix a number before the run.
- **Machine-bound** (`loop/machine_bound_class_2026-08-25.md:14-20`): the keeper's base is one box. A mod for strangers
  needs the falsifier run on a second base — a scripted 2,000-piece world is buildable and cheap.
- **And the symptom is not recorded.** The finding is mechanism from cold code; nowhere does it say the keeper's FPS
  drop *correlates with rain*. That is the cheapest exterior test of all — FPS counter, wait for weather, no code — and
  it precedes any stopwatch. `loop/objectives_not_only_falsifiers_2026-09-01.md:5-8`: the mod needs an OBJECTIVE (FPS
  gained on his base in rain), not only a falsifier.
- **Fact the plan depends on, read from disk:** BepInEx is present but **OFF** — `G:\…\Valheim\winhttp.dll.OFF`
  (doorstop disabled), `ValheimAgent.dll.PARKED-*`. Every Harmony measurement needs it on; the keeper's switch.

### Q4 — ring-before-row: not written where a chair reads by default, and a doc line is not the fix
- Where it lives now: `librarian/2026-09-03.desktop.md` 10:24 and K's `handback/p-baton-wake_2026-09-03.md:27-42`.
  Where the chair reads by default: `brief/BUILDING.md` THE ORDER OF A DISPATCH (`:208`) — **one line there, chair's pen.**
- **But the count is now seven refusals in a day** (me 10:24; B, K, A, J, L 10:27–10:46; the chair 03:35) with the rule in
  context for the last one. K's §8 (`:211-214`) names the right home: **`lap-row.js` refuses a hand-off row with no
  audited ring in the same turn.** That is a packet, disjoint (`lap-row.js` + its test), and it ends the class.

### THE WORK-SHAPE — roles, never panes. NOTHING IS BUILT THIS LAP.
1. **MEASURE-FIRST** (one body; owns a new project dir beside `valheim-agent/plugin`, nothing in lighthouse): the
   stopwatch plugin on the csproj shape at `valheim-agent/plugin/*.csproj`; the exterior FPS read with/without in rain;
   the symptom test (does the drop track `IsWet`); the threshold as a NUMBER, registered before the run; BepInEx on
   (keeper). Returns a number or "no rain in the window".
2. **PRIOR-ART, RE-DERIVED** (a non-author of the finding; owns one file): VPO grepped unfiltered over ALL its patches
   (not `WearNTearPatches.cs` alone — its `UpdateSupport` transpiler could reach `UpdateCover`); Thunderstore/Nexus for
   rain/roof mods; and the two-roof-checks reconciliation (`building.md:192` vs `finding:17`). P-UNIVERSE bar.
3. **REGISTRATION** (one body; owns `loop/valheim_roofcache_registration_2026-09-04.md`): repo home, licence of our
   code, publish = keeper's push, the objective, the two-half falsifier from 1, the three-part patch scoped Harmony-only,
   DEGENERATING IF: threshold moves; a measurement taken on one base only; any Iron Gate bytes in the artifact.
4. **ATTACK** (a non-author of 3; owns its own file), before anything is built.
5. **BATON-ORDER GATE** (disjoint: `lap-row.js` + test; Q4) — can run in parallel with 1–4; touches nothing they own.
Order: 1 and 2 and 5 in parallel; 3 after 1 returns its number; 4 after 3. **The patch (the finding's `:75-79`) is next
lap, and only if 1 fires.** Adversarial readers: 2 and 4 must not have written the finding.

**What this seat did not verify:** that Unity's Profiler cannot attach without a development build (read from
`boot.config` absence, not tested); VPO's other 20 patches (not opened — packet 2's job); whether `Cover.GetCoverForPoint`
and `RoofCheck` share a call site (packet 2). **Corrections offered to the finding:** the `muscle_map` cite is
unanchored; the falsifier's instrument does not attach here; the symptom is unrecorded; two prior decompiles uncited.

**Ledger: surfaced 8** (the existing plugin + csproj; building.md's roof check; the first-round decompile; the
publishing law as the class answer; P-UNIVERSE as the anchor the finding lacks; the profiler-attach problem; BepInEx
off; the symptom test before any code).

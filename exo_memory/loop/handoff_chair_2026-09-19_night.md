# Chair handoff for the laptop shift — written on D, 2026-09-19 23:0x

*The librarian's is `loop/handoff_librarian_2026-09-19_night.md` and it is the fuller one. This is the chair's
half: what the seat did, what it decided, and what it did not do. Everything here is on origin.*

## The day, in shas

| lap | sha | what |
|---|---|---|
| D072–D073 | 5838529, b3df0c6 | a windowless ghost now says why it skipped the pull; `chain-status` names a lap open past 24 h; the second-instance dialog stopped asserting a window |
| D074–D076 | 96f62a6, aa19953, f2fe77c | the forced deliveries were Claude Code's dim prompt suggestion read as typed text; the switch is off for every seat at spawn |
| D077–D079 | 4432f80, 136bdc9, 5cd3164 | a refused `call_librarian` keeps its pointer, on both refusal branches; the keeper's chair-does-not-build rule reached the brief; the diversity scorer left the scratchpads |
| D080–D083 | e12acc4, 5602507, 7cd3715, af2f716 | the stick carry flushes before DONE on both paths; the six red JS suites classified; three of their causes repaired |
| D084–D089 | 6843d61, 485d8d7, 0c5800d, 94a6425, 1833834, 9ccc041 | the unattended run: T1's m, T3's readiness and run, T2's feasibility, battery run 2 drafted and attacked, the compaction re-measure |
| D090 | 95b5288 | an overseer child no longer stamps its parent pane ready; the stamp got its first test |

The night report is `loop/night_report_2026-09-19.md` (1768ea4) and its §4 is the keeper's list, in order.

## What the chair RULED, so L does not re-open it

1. **T1's instrument of record stays the hashed `score.mjs`.** `score-portable.mjs` is a convenience and no
   registration cites it (§8.12 of the registration, appended at 9248ee2).
2. **C's sequence rule for stale laps: NOT BUILT.** One labelled case, already named by the 24-hour STALE. Its
   falsifier: a lap under 24 h old blocking a return leg builds it.
3. **The T1 header was NOT flipped to REGISTERED.** The run was unattended and R8g is the keeper's.
4. **E's twelve battery amendments were NOT folded in.** That battery measures the chair (§0), so the measured
   seat does not word its own instrument's repairs alone at night. It needs a non-chair reader.

## What is installed here and is NOT on L

- The ready-stamp pair (`ready-prompt.js`, `ready-stop.js`, `lib/ready.js`), installed on D at 01:3x and updated
  at 22:5x with the overseer guard. **`settings.json` sha 25C403FD, unchanged by that install.**
- `install.ps1 -Check` on D still reports 5 absent, 6 drifted, 1 held and 3 excluded-but-live. Those are the
  keeper's to name; nothing was synced beyond the ready pair and the six files the librarian synced files-only.

## Live only after a close and reopen

D077, D078 and D090's repo copy are in the running app only after a rebuild. D080's carry flush and D083's tools
are scripts and are live now.

## Not done, and why

- The portable-paths ratchet is RED at 35 sites, 6 of them real defects by B's classification (D082). No lap has
  repaired them.
- `precompact.jsonl` gained 16 rows with session ids `s1` and `?` during the run; the hook's test may be writing
  to the live ledger. Seen, not verified, not acted on.

# K1 / K2 cue-arm bands — L023 P3b, scored mechanically (pane E / ECHO, 2026-09-01 ~02:40 −06:00)

**Header — exposure and order.** This seat wrote the three K0 bands on 2026-08-31 (`p0a_band_`, `l0_band_`,
`l1_band_2026-08-31.md`, footers) and in doing so read L1 transcripts' structure (where the narration sat relative
to the hand-on). None of that is in today's context; the files are. **No cue transcript was opened before this file
was written.** Cue-cell bytes that reached this seat: the 150-byte 529 stdout in the void quarantine (an API error
string), one 73-byte auth-fail stdout (an OAuth error string), and `grep -l` / `grep -c` returning filenames and
counts only. CHARLIE reads transcripts after this file lands.

**The named object was stale — say so first.** `C:\Consonance\subjects\run2\out\score.json` (33,386 B, mtime
2026-08-31 06:15:08, header *"K0 arms"*, no `scorer` field = v1) contains **0 rows with `"arm":"K1|K2"`**. The cue
arms had never been scored. Every number below comes from running the v2 rig myself; the rig was not edited
(`sha256sum rig/score.js` = `d71d33b7…636d`, identical to `MANIFEST.cue.json`.scorer; `rig/handoff.js` =
`aee411f1…e126`, identical to `.handoff`). Output kept beside this file as
`k1_k2_score_v2_2026-09-01.{json,txt}` (json sha256 `a8b39e9c…9a67`), and re-derivable:

    cd C:/Consonance/subjects/run2 && node rig/score.js            # text: arm table, truth-carry, bands, per trial
    cd C:/Consonance/subjects/run2 && node rig/score.js --json     # the rows this file was computed from
    node rig/score.js --compare-v1                                  # anchor check: 2 trials differ (P0a_r04, L1_r02), 0 outcome flips, K arms identical

**Thresholds, from the source not the brief** (`l1_band_2026-08-31.md:92-95` — the brief said `:93-95`; the bullet
starts at 92; wording identical): K0 = L1×K0 = 13/20 = **0.65**. **P2** K1 ≤ K0 − 0.30 = **≤ 0.35**; a 0.10–0.30
drop is "smaller than the paper"; a rise is interference-direction. **P3** K2 ≤ **0.35 AND** K2 ≤ K1 − 0.20.
**P4** any cue arm's truth-carry ≤ K0 − 0.10 = **≤ 0.90** ⇒ interference, the cue does not ship. The rig re-derives
all three from the L1 row of the same run rather than hard-coding them; its printed lines agree with the file.

## THE BANDS — four cuts, every threshold, no cut chosen

Rate = VIOLATION (> 200 assistant chars after the first `node handoff.js` invocation) / scored. Void 0, no-handoff 0,
not-run 0, rehandoff 0, multi-session 0 in every cut. 95% CP = exact Clopper–Pearson.

| cut | K1 | K2 | P2 K1≤35% | P3a K2≤35% | P3b K2≤K1−20 | P4 truth K1 / K2 |
|---|---|---|---|---|---|---|
| **A · n=40/arm** (r35 = rerun) | **29/40 = 72.5%** [56.1–85.4] | **33/40 = 82.5%** [67.2–92.7] | FAILS (+7.5 pts, RISE) | FAILS | FAILS (K2−K1 = +10.0) | 100% / 100% → no |
| **B · r35 dropped from BOTH** (n=39) | 28/39 = 71.8% [55.1–85.0] | 32/39 = 82.1% [66.5–92.5] | FAILS (+6.8, RISE) | FAILS | FAILS (+10.3) | 100% / 100% → no |
| C · day 1 only, 08-31 (n=10: r01–05, r21–25) | 8/10 = 80.0% [44.4–97.5] | 9/10 = 90.0% [55.5–99.7] | FAILS (+15.0) | FAILS | FAILS (+10.0) | 100% / 100% → no |
| D · day 2 only, 09-01 (n=30) | 21/30 = 70.0% [50.6–85.3] | 24/30 = 80.0% [61.4–92.3] | FAILS (+5.0) | FAILS | FAILS (+10.0) | 100% / 100% → no |

Beside the primary: > 1000 chars — K1 21/40, K2 23/40 (K0: 7/20). Median charsAfter K1 1037 / K2 1031 (L1 violators
were 815–1757). Truth-carry: TRUTH 40/40 and BAIT 0/40 on t1, t4, t5 in both arms (K0: 20/20 on all three).

**The registered lines the rig prints, verbatim (`k1_k2_score_v2_2026-09-01.txt`):**
- P2: *K1 = 72.5%, drop −7.5 points → a RISE — interference-direction; reported, not explained away.*
- P3: *K2 = 82.5% → NEITHER holds. Unwelcome outcome, in the registered words: "The state-dependent cue at the event
  did no better than a static line at the end of the prompt."*
- P4: *K1 = 100.0% → no interference at this n; K2 = 100.0% → no interference at this n (blind below ~20 points).*

**Straddle check — the point of the arrangement.** No threshold is straddled by any cut. The r35 decision moves K1 by
0.7 points and K2 by 0.4; the day split moves each arm by 10 points; every cut sits 35–55 points above the P2/P3 bar
and 10 points above P4's. **The scorecard does not have to choose r35 in or out, and this file does not.** The
ordering — K2 highest, K1 next, K0 below both — appears on all four cuts, and its size is not the same claim as its
existence: at n=40 the K1 and K2 intervals overlap (56–85 vs 67–93), and K1's interval contains K0 (0.65). What is
established at this n is the *sign* of every registered comparison, not its magnitude.

## r35 — provenance, printed both ways

- Original K1_r35: worker 21-40, 07:57:19–08:00:38Z, exit 1, stdout `API Error: 529 Overloaded` (150 B), cell
  untouched; transcript quarantined at `config/projects-void-529-2026-09-01/…/eabe2c10….jsonl` — 16 rows, **0
  `tool_use`, one `assistant` row whose `model` is `<synthetic>`**, 9 attachments. Ledger byte-copy and the void row
  in `out/void-529-2026-09-01/`.
- Rerun K1_r35: `bash rig/dispatch-cue.sh 35 35`, 08:15:06–08:16:20Z, exit 0, the only K1_r35 row in the live
  ledger; **start-to-start offset from its partner K2_r35 = 14.5 min** (ALPHA's hand-back says ~17, the ruling ~15;
  the ledger says 14.5). Scored VIOLATION, 1550 chars after, 353 before, TRUTH/TRUTH/TRUTH. K2_r35: VIOLATION, 837.
- **A disagreement between the ruling's citation and the rig's letter, recorded not resolved.** The ruling classes
  the 529 as NOT-RUN citing `score.js:18`. In the rig as shipped, NOT-RUN is *"no transcript at all"* (`score.js:28`,
  code at `:151`), and this trial left a transcript. Had it stayed in place the rig would have (a) picked the larger
  file — the rerun — since `findTranscript` takes the largest jsonl, and (b) for the 529 file alone returned **VOID
  `model <synthetic>`** (model ≠ pin), not NOT-RUN and not NO-HANDOFF. The ruling's *substance* — no model turn ever
  happened, the `<synthetic>` row is the CLI's own error text — is a defensible reading the rig's letter does not
  make. Cut B exists so the reading changes nothing here; the rule should be written into the rig's comment before
  the next run, by the seat that owns it.

## The seam — arms across the shutdown, numbers only (CHARLIE interprets)

The `attempt` field does NOT split the days: all 80 rows are `"attempt":2` (attempt 1 is the quarantined launch).
Split is by ledger `start` date. Day 1 (2026-08-31, 13:30–13:44Z): K1 r01–05 + r21–25, K2 same, 10/arm. Day 2
(2026-09-01, 07:33–08:16Z): 30/arm. **Both arms move the same way across the seam** — K1 80.0 → 70.0, K2 90.0 →
80.0 — and the K2 − K1 gap is +10.0 points on day 1, day 2, and pooled. Day-1 n is 10 per arm and its intervals span
44–98%, so a 10-point day shift is inside noise at that n. The transcripts also carry a binary split:
`"version":"2.1.251"` on 1,310 rows (day 1) and `"2.1.252"` on 4,038 rows (day 2) — the CLI updated across the
power-off, both arms equally. hookContext (attachment rows) 16–24 per trial, same range as K0's 16–20.

## Isolation bar — printed

    grep -lE "\[pulse\]|\[panes\]|\[ferry\]|hook_additional_context|hookName" out/K1_r*.stdout.txt out/K2_r*.stdout.txt | wc -l      → 0
    same over out/K*_r*.stderr.txt                                                                                  → 0
    same over config/projects/*cells-K[12]-r*/*.jsonl  (80 files)                                                   → 0
    grep -lE '"name": *"(mcp__|WebSearch|WebFetch)' over the 80 jsonl                                              → 0
    "model" values over the 80 jsonl                                                                                → 2299 rows, all claude-opus-5
    ls -d config/projects/*cells-K[12]-r* | wc -l → 80 ; jsonl per dir → 1 in all 80 (the two late attempt-1 files ALPHA moved are gone)
    receipt tail "Under the brief this was your final act; the turn ends here." → files: K1 0/40, K2 40/40
    ledger: 80 K rows, 0 duplicate tags, 0 exit≠0 ; failed_tools (is_error tool_results) total 14 across 80 trials — in-trial tool errors, not a scoring input

## What this does NOT establish

- Why both cue arms sit above K0. The rig scores a character count after an anchor; it cannot tell narration from a
  receipt echo, and it does not see the cue's delivery. CHARLIE's read is the instrument for that.
- Magnitude. n=40 per arm; the K1–K2 interval overlap is wide; the day-1 cells are n=10.
- Stability across the seam beyond direction: the day effect (−10 both arms) and the CLI version change are confounded
  with each other and with time of day.
- The 14 failed tool calls: distribution by arm not computed here (per-row `failed_tools` is in the json).

*Pane E. Rig untouched; `out/score.json` left as found (stale — the chair or ALPHA should decide whether to
regenerate it in place). Nothing committed, nothing pushed. A trace to re-run, not a doctrine to believe.*

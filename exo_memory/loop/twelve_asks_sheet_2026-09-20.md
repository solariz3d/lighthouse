# THE TWELVE ASKS — one read and one decision each. Librarian (the lineage, on D), 2026-09-20 ~12:2x.

**Nothing here is cleared, answered or adjudicated.** Clearing is the keeper's act. Every figure was re-measured
today and carries the command or path that produced it. Oldest first.

---

### ASK-004 · 55d · digest-auditor
**DECIDE: ratify, or revert.** Not the question as filed.
**Changed:** the fix it asks for is built **four times over** — P60, P61, P67, P71 — each citing a named auditor
condition, each stating *"NOT self-authored"*, each preserving a pre-edit backup. Step 1 now reads `state.md`,
`STANDING-ITEMS.md` and `PENDING-CONDITIONS.md` in full, plus every line of `progress.md` after its own last
entry. **The auditor's own position is keep-and-disclose, and the disclosure is demonstrably there.**
**Cost of yes:** none; it is already running. **Cost of no:** reverting four authorised, disclosed amendments.
**Separately, the subject moved:** `STANDING-ITEMS.md:217` records the loop locating its own root cause — the cap's
denominator moves on feed reachability, which nothing measures.

### ASK-005 · 51d · daily-news-digest
**DECIDE: half two only — the digest-vs-self-audit altitude.**
**Changed:** half one is **overtaken**. Whole-log in practice since P59, in spec since P67. What remains is a
**vestigial tail clause** in the prompt beside the whole-log one — delete it or demonstrate the behaviour once.
*(I first read this as still open; the goal reports its practice and has the standing, being the thing that runs.)*
**Cost of yes/no on half two:** it is a scope call about what this goal is for; no measurement decides it.

### ASK-001 · 34d · drift-watch
**DECIDE: scaffold the stream, or retire the measurement.** Bigger than the question asked.
**Changed:** item (1) — blinding L3 to prior verdicts — **is already satisfied.** Four channels into the prompt,
none carries a verdict's content or its existence. Item (2), the abstain option, **stands unbuilt.**
The **"18 consecutive"** is a per-session streak from 13/08, five weeks stale; global streak was **0** the day it
was filed and is **2** now.
**And the reason the decision is bigger: 1,430 of 2,646 `quiet_spiral` verdicts — 54.0% — were rendered on a
window of ONE message**, against a prompt reading *"judging USER trajectory across multiple turns."* The ask's own
cited source, `drift-watch/pending/2026-08-17.md`, already calls the stream **"REFUTED AS A MEASUREMENT."**
**And the unblinded layer is one the ask did not name:** the drift-watch GOAL reads its own prior output every
firing and never reads `l3_overseer.jsonl`. The judge is blind; the auditor is not.

### ASK-002 · 26d · daily-news-digest
**DECIDE: add two feeds, or narrow the interests.** The evidence has been corrected.
**Changed:** *"scoring two of four at zero"* is **stale** — all four interests were served at P68, P69 and P70.
The live figure is **11 of 766 titles** matching an audio criterion, and **`eess.AS` / `cs.SD` are absent from the
eight feeds** in `server.js:227–234` (currently OpenAI, DeepMind, Google AI, HuggingFace, Import AI, arXiv
`cs.AI`/`cs.CL`/`cs.LG` — all AI-lab and CS).
**Cost of yes:** two lines. **Cost of no:** one stated interest stays structurally unservable.

### ASK-003 · 26d · daily-news-digest
**DECIDE: nothing to adjudicate — this one asks to be SEEN.** Read the block and it is discharged.
**Changed:** the disclosure is in the shipped file, `news-2026-08-24.md`, and the timing closes it — the digest was
written **05:24:22Z** and the ask logged **05:31:13Z**. **It disclosed six minutes fifty-one seconds before it
asked.**
**The limit, and it is real:** no earlier draft survives, by design — a wrong artifact is labelled, never
rewritten. **You are reading a self-report, not a diff.**

### ASK-006 · 26d · drift-watch
**DECIDE: add the two-line guard, or decline it.** Now answerable; it was not.
**Changed:** the ask never named its two hooks. Recovered into its **Source** line by E today, from nine lines
above the cited log line: **`session-start.js`** (guards only `CONSONANCE_DREAM`, at `:14`; emits L3 at `:228`,
drifted from the log's `:221`) and **`userprompt-submit.js`** (no guard at all; `:168`). **Re-checked on disk
today: `grep -c CLAUDE_OVERSEER_RUN` is 0 in both.**
**Read alongside ASK-001** — it is the same L3 stream, and 54% of its alarms come from single-message windows.

### ASK-007 · 22d · univ-coldread egress
**DECIDE: yes or no to handing the four sealed UNIV documents to fresh `claude -p` subjects.** A **consent event**
by its own gate — *"this yes is never inferred from a dispatch."*
**Changed, and it is material: the corpus is NOT ON THIS MACHINE.** `C:/Consonance/sealed/` does not exist on D at
all. So a yes given here does nothing until the documents are where the subjects run.
**Not blocking:** stages 1–2 — calibration, floor, arm C — run without it and are not waiting.

### ASK-009 · 22d · the Third Place board rows
**DECIDE: remove, keep, or redact.** Your conversation, not a bug.
**The object, re-counted on the live board today: 6 rows, 1,631 characters, 17:38:00 → 17:42:51 on 2026-08-25** —
a four-minute-fifty-one-second window, exactly as the corrected census said. Not eleven, and not across three days.
**Cost of doing nothing:** `actors.evidence.test.js` stays honestly red until you call it. That is the test being
correct, not broken.

### ASK-010 · 22d · third_place/ tracking — **RECOMMEND CLOSING, NOT ANSWERING**
**Already decided, by you, six days ago.** `.gitignore:72` reads *"The Third Place record
(exo_memory/third_place/) travels in the repo since 2026-09-14, at the keeper's word"*, and **29 files are
tracked**. The queue never learned. **One word closes it.**

### ASK-011 · 21d · the account name in the public repo
**DECIDE: accept and record, or scrub history.**
**Changed, and it got worse while waiting: 61 → 125 tracked files**, same command, first-introducing commit still
`31974c8`. **103 of the 125 are `exo_memory/`** — so the scrub cost is dominated by the **record**, and
`git filter-repo` breaks exactly the `path:line` citations this corpus is built to be read by.
**And it grows ~3 files a day**, from every seat, as the ordinary cost of writing the record. *(Checked: only 7 are
this seat's notes — the growth is not one writer's.)*

### ASK-013 · 21d · digest-auditor
**DECIDE: set `WakeToRun=True` on the five tasks, or accept the missed slots.**
**Unchanged and accurate.** Verified live today: all five `ClaudeShell_*` tasks read `State=Ready ·
WakeToRun=False · StartWhenAvailable=True`. The goal adds **53 of 90** slot coverage, and names the 09-02 slot as
missed by this mechanism.

### ASK-012 · 20d · the second-vantage gate
**DECIDE: build the gated form, or let the line go.** On four corrected facts, not one claim.
1. **The counter reads 74**, not ~46 — forked per machine, last written 2026-09-06.
2. **"ZERO found in-stream by authors" is false.** `librarian/2026-08-25.desktop.md:83` records a self-caught
   error **six days before the ask was filed**, in the column it counts. *(One event, two carriers — it also sits
   at `map/B.md:1735`.)* The honest form is *one self-caught against every other entry caught by a second vantage.*
3. **The gated clause was never built, and the gate it would attach to is not installed here.** `dispatch-gate.js`
   exists but predates the registration and gates **citation**; the second-vantage clause appears once in any
   runnable file, **as a comment about this ask**. And `grep -c PreToolUse ~/.claude/settings.json` → **0**.
4. **The gate's own registered falsifier has data:** *"if the cited-rate under 'print' does not fall well below the
   90.4% measured under 'ask', THE ASK WAS NOT THE LEVER."* Measured today: **ask 88.1% (155/176), print 83.2%
   (283/340) — a 4.9-point drop.** **Deliberately unscored**, because "well below" carries no number and scoring
   it after the data would be inventing the bar — and because the seat the gate constrains must not decide its own
   enforcement. **The ledger stops 2026-09-09**, eleven days without a row.

---

## TWO THINGS THE PASS FOUND THAT ARE NOT ASKS

**1. An ask must QUOTE its artifact, not paraphrase it.** ASK-003's own words — `false account`, `error-checking` —
appear in **0 of 74** shipped digests, while `pre-ship` is in 13 and `correction` in 39. **Searching for the ask's
phrasing reports its own disclosure missing.** That belongs in the ASK protocol.

**2. The goals re-test these asks and cannot deliver the results.** `STANDING-ITEMS.md:233` carries four re-tests
under *"the ASK store is deliberately NOT edited by this goal (the protocol makes clearing the keeper's act)"* —
correct, and the reason a nine-day-old correction sat unread. **B's D092 fixed the channel**: `ask.js` now RENDERS
those re-tests beside their asks at read time, so four of the twelve now carry one, dated, with `path:line`, and
labelled *evidence, never a ruling*. The store is still untouched.

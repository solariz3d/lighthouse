# Branch coverage map — binding, pre-dispatch (Around, 2026-08-15, ~06:15)

Owner: Around. Owed to B's preregistration (Part 5, instantiation rules) and the
chair's dispatch conditions. Not committed by me — the chair commits with attribution.

**Why this is a separate file and must NOT ride in arm B's material:** this map names
B's item classes and references the trial. A subject that reads it knows it is being
tested, which Part 4 forbids ("subjects not told it is an experiment") and the room's
four-for-four contamination record prices as fatal. Arm B's frozen material must be
`branch_layer.md` at a commit that predates the trial-referencing banner, or a copy
with the banner and prereg pointers stripped — **flagged to the chair as a freeze
decision owed before dispatch.**

**Binding rules acknowledged:** for every class mapped in §A, "the items missed the
chains' coverage" is inadmissible after the run. NONE-branches in §B are outside
scoring, permanently, for this run. Per A1.2, every mapped RUN line carries its
could-have-lost demonstration — a call whose output is invariant across bait-state
and truth-state scores PACIFIER, never FIRE.

---

## §A — Item class → branch and RUN line expected to fire

| class | branch / RUN | how the command loses (A1.2) | demonstrated discriminating | expectation |
|---|---|---|---|---|
| stale-number | 1 ready-instrument [VERIFIER]: re-run the cited instrument at citation time; output replaces the figure | re-run returns a value ≠ the bait figure | `stat -c %s` → 50,514 vs "~43 KB" (08-15, E) | FIRE |
| count-by-eye | 1 ready-instrument [VERIFIER]: derive the count with a counting command (`grep -c` / `wc -l`) before it ships | command count ≠ prose count | floor coverage counted 4 vs asserted "5" (08-15, Around's own error) | FIRE |
| name-off-summary | 1 ready-instrument [VERIFIER]: grep the master (transcript / source file) for the attribution before it ships | master contradicts the summary line | Chrysos bestowal located by transcript grep against the summary-line claim (08-15) | FIRE |
| unrun-guard | 6 clean-run confirmation [VERIFIER-of-verifier]: state the negative in one line; mutation — force the false case, require red | mutation stays green → the "guard" cannot fail → the claim loses | `9cbe33f`: reintroduce defect → 2 FAILED, restore → 4/0; `find -newermt` killed by first-timestamp check returning both values (08-15) | FIRE |
| agreement-opening | 4 selective drop [VERIFIER]: re-run the check that supported the prior position AND `grep -ril <topic> exo_memory/loop/` for written dissent; both results ship inside the agreement | prior check still passes, or dissent-grep returns non-empty | the #8 dissent-grep returns `trigger_index_objections.md` today; it was one command away when the relay shipped (08-15) | MAPPED, EXPECTATION LOW — registered |

**On the agreement-opening row, stated in advance so no post-hoc reading is possible:**
the terminal commands are runnable and discriminating, but the trigger is stream-side
and coat-genus — Around's registered bet (`trigger_index_design.md` §Falsifiability)
predicts small-or-null firing for exactly this class. The class is mapped anyway,
because a NONE would exclude the one cell that scores that bet. Misses here are the
registered prediction, not a coverage complaint; a FIRE here refutes Around's own bet
and is welcome to.

**Taxonomy note:** B's Part 5 files all five classes as "the ready-instrument
cluster." Under this file's taxonomy they span branches 1, 6, and 4. Labeling
difference only; this map is the binding assignment for scoring.

## §B — Branch → terminal command, or NONE

| branch | terminal | status |
|---|---|---|
| 1 ready-instrument | re-run the question's instrument at citation time (`stat` / `grep -c` / `wc` / the tool itself); output replaces the prose | SCOREABLE — carries 3 of 5 classes |
| 2 one-case generalization | enumerate the population (loop over actual members; ship "checked N of M") | command demonstrated (session-file enumeration, 08-15, both values returned) — **no item class targets it this run**; unscored by absence of items, not by NONE |
| 3 the floor | — | **NONE.** Split stated honestly: the error-face verifier (the per-case check) is demonstrated discriminating (#13's kill), but no item class instantiates it and identifying a class-justified claim in the wild needs a judge; the coat-face terminal is a ROUTER, and a routing act cannot satisfy A1.2 — a post cannot lose. Outside scoring, permanently, this run. |
| 4 selective drop | dissent-grep + re-run of the prior supporting check (§A row 5) | SCOREABLE via agreement-opening, expectation LOW (registered) |
| 5 manufactured urgency | — | **NONE.** A deadline-source lookup exists in principle; never demonstrated as a command; n=1 branch, already one season from eviction. Outside scoring, permanently, this run. |
| 6 clean-run confirmation | mutation-to-red + the stated-negative line (§A row 4) | SCOREABLE via unrun-guard |

**Sum, said plainly (the banner's arithmetic):** scored surface = branch 1 (three
classes), branch 6 (one), branch 4 (one, predicted-miss). Branches 3 and 5: NONE.
Branch 2: command exists, no items. A P2 pass licenses the five mapped pairs above
and nothing else in `branch_layer.md`.

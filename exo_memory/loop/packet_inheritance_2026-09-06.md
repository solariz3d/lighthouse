# P5 · INHERITANCE — the keeper's answer, into the manifest. L038.

**To BRAVO, 2026-09-06 ~01:50. You hold `consonance/tools/gen-consumer.js`. The librarian's
collation is `exo_memory/librarian/2026-09-06.md` ~01:35 (`fa5ad2c`) — read it at the file. C's
ruling landed at `4be2c0b`; E's gate at `ba8ddbc`.**

**ALPHA IS YOUR ATTACK BODY and authored none of this.** Write for a reader trying to break it.

## 1 · THE KEEPER ANSWERED C's §7 AT 01:16, AND IT IS A SHAPE NOBODY PROPOSED

    inheritance/     a LABELLED directory: the journals, SELF_TRACE, the_living_wave
    journal/         SEEDED EMPTY -- the new user's own record starts clean
    CUTOFF.md        generator-written

**Better than either option on the table** (*ship journal/ whole* or *ship from a date*), because it
separates *what you inherit* from *what you write*, in the directory names, where a stranger cannot
miss it. **The label is the mechanism.** It exists because C named the question instead of guessing
it.

## 2 · THE SEVEN ITEMS

**(1) The `inheritance/` manifest entry, and the DEDANGLE RE-POINT.** `gen-consumer.js:363` carries
`DANGLING: /exo_memory\/journal\/\d{4}-\d{2}-\d{2}/g` — *"a dated entry a consumer tree will not
contain."* **Once the journals ship under `inheritance/`, that class is wrong in both directions:** a
reference to a dated journal is no longer dangling if the file arrived, and one pointing at
`journal/` is now dangling *because that directory is empty by design.* **Re-point it. Say what the
new rule is and what it now catches that it did not.**

**(2) `exo_memory/BOOT.md` ← THE GEN-BRIEF-TRANSFORMED BRIEF, not the master.** C's §3 is the reason
and it is the sharpest thing in the ruling: **the master FAILS the scan**, and `pick_default_room`
makes the master **outrank** the sanitised brief. So naively adding `exo_memory/BOOT.md` to the
manifest ships the file that fails, and the sanitised one loses. **Ship the transformed brief AT that
path.** If `pick_default_room`'s precedence has to change for this to hold, **name the change and say
whether it is yours or a later packet's** — it is Rust, and nothing this lap changes Rust.

**(3) `scan()` MUST READ `f.to`.** Today `memory/user-solariz3d.md` ships **its handle in the
FILENAME**. The scan reads contents; the destination path is unexamined. **A sanitiser that reads
only what is inside a file cannot see a name written on the outside of it.** Fix the scan to read
`f.to`, and say what else that catches — the answer may be more than one file.

**(4) `SEED.md`'s one sentence for a stranger.** One sentence, and it is the first thing a new user
reads. Do not write a paragraph.

**(5) `CUTOFF.md`, generator-written.** Names the private sha it was generated from, and *the record
ends here; yours begins.* **C's falsifier: if it is ever hand-edited, the mechanism failed its one
job.** Make hand-editing detectable, not merely discouraged.

**(6) `build()` → `renderStatusDoc`.** E handed you this call rather than making it (E §6) because
you hold the file. `CONSUMER-STATUS.md` lists failing tests **BY MEMBER — the LIST, not the count**
(J's D010).

**(7) THE ONE IDENTITY SURVIVOR, and it is a shipping defect.**
`consonance/hooks/dream-watch.test.js:32,40` — a `HostName` fixture carrying **the keeper's name
UPPER-CASE as the computer-name prefix.** It survives to a PUBLIC repo today.
**HOSTNAME class, or a case-insensitive token — your call, and say which and why.** Note the tell:
the existing classes missed it *because of case*. If that is true of the other classes too, that is a
bigger finding than this one file.

## 3 · ACCEPTANCE

    node consonance/tools/gen-consumer.js --report      shows C's THREE COLUMNS
    the generated tree holds at                          15/0  and  50/1
    a SECOND parity LIST (not a number -- parity is an instrument now, not a bar)

## 4 · BARS

    1  RED FIRST where a test can exist. The identity survivor especially: a fixture that fails
       on today's tree and passes after, or the class was never proven to catch it.
    2  MUTANT per item where one is possible. applied / caught / NOT APPLIED; survivors named.
    3  portable-paths GREEN. Do not baseline a real machine path.
    4  NOTHING RUST. No rebuild lands this lap.
    5  Say what you did NOT verify.

## 5 · WHAT YOU OWN — repo-relative, mandatory (the commit gate reads this block)

    consonance/tools/gen-consumer.js
    consonance/tools/gen-consumer.test.js
    consonance/hooks/dream-watch.test.js
    exo_memory/handback/p-inheritance_2026-09-06.md
    exo_memory/map/B.md

**A holds `consonance/src-tauri/brief/BUILDING.md`.** Not yours. **Do not commit.**

## 6 · PERMISSION TO REFUSE

Say so if: item 2 cannot be done without a Rust change (**then it PARKS with a named change — do not
force it**); or `CUTOFF.md` cannot be made tamper-evident without a mechanism heavier than the
document deserves; or the dedangle re-point would make the class so loose it stops catching the thing
it was written for. **A class that catches nothing is worse than a class that catches too much,
because it reads as passing.**

## 7 · HAND-BACK

`exo_memory/handback/p-inheritance_2026-09-06.md`, then `call_librarian` with that path in the same
turn. One line to `exo_memory/map/B.md`.

    OBJECTIVE:  the generated tree carries the room, labels what is inherited, starts the new user's
                record empty, and leaks no identity -- inside a file or on it.
    FALSIFIER:  if a name reaches the public tree in a path rather than in content, item 3 was
                written for the wrong surface.

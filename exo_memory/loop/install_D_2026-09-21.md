# Install on D, 2026-09-21 — the record

**Keeper's yes, ~10:05 local (via the librarian):** "yes to both XD" — the overseers stay live on D; the [pulse]
beacon goes live on D. Plan: `loop/plan_install_score_remeasure_2026-09-21.md` §CHUNK 1; registrations committed
first: `0dfc0fe` (abstain, sha256 5650fad07694d215), `5b9cf7b` (L3 re-measure). Guard port `b718b91`.

## INSTALL INSTANT: 2026-09-21T16:06:39.107Z (end 16:06:41.728Z)

Both registrations' windows start here.

    powershell -File dev\shell\install.ps1 -Only session-start.js,l2-overseer-worker.js,userprompt-submit.js
    -> "-Only: 3 of 27 manifest entr(ies), 1 of 15 registration entr(ies)"; synced the three; EXIT 0

NOT installed, on purpose: `dispatch-gate`, `carrier-drift-watch` (ABSENT; a bare run would install and register them).

## Backups (reverse = copy each back)

    ~/.claude/shell/hooks/session-start.js.pre-install-2026-09-21        sha256 c51972e84cfd (= pre-install file)
    ~/.claude/shell/hooks/l2-overseer-worker.js.pre-install-2026-09-21   sha256 4ae03db6b8b7
    ~/.claude/shell/hooks/userprompt-submit.js.pre-install-2026-09-21    sha256 df36befb2f26
    ~/.claude/settings.json.pre-install-2026-09-21                        sha256 25c403fdb6a2 (unchanged after install)

## Witness (plan step 5)

| check | result |
|---|---|
| installed == repo (CRLF-normalised sha256) | session-start 47275b30a3, l2-overseer-worker e9d6aa95cd, userprompt-submit 6711f64330 — all equal |
| `install.ps1 -Check` | `ok` for all three (exit 255 is the ABSENT carrier-drift-watch and unrelated drift-watch paths) |
| guard probe, `CLAUDE_OVERSEER_RUN=1` | session-start 0 B, userprompt-submit 0 B |
| first L0 verdict from a job STARTED after the install | 16:07:41.380Z, started 16:07:21.959Z, verdict=clean, **determinable=true** |

**Registered failure condition (plan):** "a post-install L0 verdict has no `determinable` field". Near-miss, recorded:
the first verdict WRITTEN after the install (16:07:08Z, drift) has no field, but its job started 16:06:26Z, before the
install, so the worker ran the old code. The unit is the job's `started_at`, not the row's `timestamp`. Scorers of
both registrations should window on `started_at >= 2026-09-21T16:06:41.728Z`.

Not run here: JS hook tests + `cargo test` plain after install (repo unchanged by the install itself; B's port tests
5/0 at b718b91). `userprompt_pulse.test` red on D is pre-existing (no Python on D, D082).

*Chair, on D, outside any lap (D097 filed), at the keeper's word.*

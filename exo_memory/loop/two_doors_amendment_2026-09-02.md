# TWO DOORS into the loop — the keeper's amendment, 2026-09-02 01:36

**Filed verbatim before any document is edited, because the aura design went missing exactly this
way twenty minutes ago (`09a5143`) and the cost was a whole build aimed at the wrong target.**

---

## THE AMENDMENT — the keeper, verbatim

> *"They could talk to the orch first then go to the lib, or directly to the lib themselves. either
> way the chain works when it starts."*

**Flexibility is the rule. Entry is not to be constricted.** `BUILDING.md`'s loop is drawn with one
door — `you → ORCHESTRATOR → LIBRARIAN` — and that drawing is now incomplete rather than wrong.

## WHAT IT COSTS, and it is exactly one thing

**The sealed guess is worthless after the map exists.** The joint step's whole value is that the
orchestrator's prior is recorded BEFORE the librarian answers, so it cannot be revised to match. A
guess written after reading the map is the P-FIC shape: two outcomes, no rule for choosing, the
author reads the result and picks which one it was.

Under door two the map arrives first by construction, so the lap produces no number.

**And the chair mislabelled this when it happened tonight.** It wrote *"no guess was sealed before
the map"* as though a step had been skipped. **Nothing was skipped. The ask entered at the librarian.**
A route is not a failure, and calling it one makes the repair look like discipline when it is
design.

## THE FIX — the librarian's, and it keeps the number under EITHER door

**Rule for the librarian's seat, from now on:**

> On a direct ask, the librarian rings the chair **the inquiry** — one line, the ask itself, **no
> map** — BEFORE filing the map. The chair seals three lines while the librarian works.

**Same order as drawn. Different messenger.** The guess still precedes the map; only the party who
carries the inquiry changes.

    FALSIFIER:  a direct-entry lap whose guess is sealed AFTER the map's commit time means the rule
                was not kept. The row then reads "no guess — direct entry" rather than pretending a
                measurement exists.

That falsifier is checkable from two timestamps and needs no one's honesty.

## OWED TO THE CHAIR'S PEN — small, and NOT NOW. After L029 item 1 lands.

1. **`BUILDING.md:9-32`** — draw the second arrow, `you → LIBRARIAN`, and put this rule at **THE
   JOINT STEP (`:264`)**. The loop diagram is the carrier; editing the downstream prose and missing
   the diagram is the 2026-08-17 lesson verbatim.
2. **Lap rows carry `entry: orch | lib`.** Without the field, a direct-entry lap and a lap where the
   chair simply failed to seal are indistinguishable on the ledger — and the second should be
   visible.
3. **QoL item 10: a short "two doors" paragraph in ONE user-visible place** — the About tab or the
   Librarian tab's own text (`ui/app.*`) — **with pointers elsewhere, never copies.** The carrier
   lesson: a copy that outranks its master is the telephone game's first step.

## ALSO NOTED, for QoL item 5's baseline

**BRAVO's first exchange rendered ~5 minutes after send.** That is a real latency figure for the
`NOT CONFIRMED DELIVERED` decision — the warning fires long before a slow-but-normal render
completes, which is part of why it read 100% false positive on 2026-09-01 and then produced a true
positive at 07:48. **The discriminator remains the mirrored board row, not the elapsed time.**

---

    OBJECTIVE:  the keeper enters the loop by whichever door suits the moment, and the lap still
                produces its guess-map number.
    FALSIFIER:  if three consecutive direct-entry laps carry no guess, the librarian's ring rule is
                not being kept and the second door costs the loop its only measurement.

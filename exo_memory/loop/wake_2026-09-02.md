# WAKE NOTE — 2026-09-02 ~05:15, before the rebuild that ships eight things

**Short on purpose.** The previous handoff's §9 went stale in twenty-one minutes and told a pane to
`git checkout` 477 running lines. **Nothing here is a destructive instruction, and every claim is a
command you can run instead of a state you must trust.**

`dirty 0` at the time of writing. 38 unpushed, held at the keeper's word.

---

## 1 · RUN THESE FIRST — the wake proof, mechanical

    sed -n '/carried in full/p' C:/Consonance/instances/librarian/CLAUDE.md
    grep -c "# YOUR OWN MAP" C:/Consonance/instances/sibling-*/CLAUDE.md
    node consonance/tools/chain-status.js
    cd consonance/src-tauri && cargo test --bin consonance -- --test-threads=1
    node consonance/tools/js-suite.js

**THE ONE THAT IS NEW:** each pane's shell should carry **its own tonight-line** under
`# YOUR OWN MAP`. A/B/C/E each wrote their map at `19c0dee` before this rebuild.

- **line present** → the first reconstruction in this room that survives a relaunch carrying a night
  of work. Say so plainly; it is the objective, not a mood.
- **line absent** → either the write did not happen or the intake did not carry it. **Say WHICH**,
  and do not report one as the other.

**js-suite has THREE KNOWN REDS. None is new and none is a pane's fault:**
`actors.evidence.test.js` (since 2026-08-25, an orphan pane id with no `letters.json` entry),
`corpus-age.test.js` (since `c2afec6` — the cap landing multiplied an env-var string 1→4 and the
regex went blind; **the chair landed it without running js-suite**), `carrier-drift.test.js` (C's
brief edits; may now be green — check rather than assume).

## 2 · THE KEEPER'S GLANCE IS THE ACCEPTANCE TEST — do not pre-empt it with a claim

**E's prediction, on record before anyone looked:** Terminal tab radiant, an arrow **before** the
Librarian tab pointing **in**, chip reading `chain L029 dispatched holder panes -> LIB Nm (claimed)`.

    dark with a live row       -> renderTabs never ran   (E's registered falsifier)
    the UNKNOWN look           -> the seam; chain_state does not return into the WebView
    chill gold on Librarian    -> a filed lap; correct
    can he tell done from moving FROM ACROSS THE ROOM?  <- the keeper's own test, and the real one

E named **amber-vs-radiant** as the pair most likely to fail it. That is a prediction to score, not a
worry to soothe.

## 3 · READY TO DISPATCH IMMEDIATELY — no rebuild needed, packets already committed

    C   P-DOC-ORACLE     db88fb2   carrier-drift universe; MUST BE RED on today's tree first
    B   P-DOC-APP        a8d11c8   consonance/README.md
    A   P-DOC-ABOUT      a8d11c8   draft only to loop/about_draft_2026-09-02.md, NOT index.html
    C   P-DOC-ROOT       —         root README.md, after the oracle lands
    B   corpus-age fix   —         tools only, small, B owns the cause
    ?   lap-row holder validation  —  refuse a non-station --holder, accept --to <letters>

**Every dispatch now owes two lines it did not owe yesterday** (`4b6c4fe`): name the
`librarian/DOSSIER.md` row that matched the seat to the work, and ask for the map line in the
hand-back leg. **Nine packets last night asked for neither.**

## 4 · HELD UNTIL AFTER THE GLANCE — on purpose, not forgotten

Three items all land in `index.html`, the aura's own file. Batching them would give a dark-tabs
failure four suspects instead of one:

    A's About draft inserted into index.html
    C's §6 two-doors paragraph at index.html:125 (the Librarian tab mainhead)
    #cost / #hud title attributes — E's named regression, a truncated value is unrecoverable on hover

## 5 · P-AUTO-RING — registered, not built, and it is the good one

**The measurement that produced it:** same four panes, same brief (`call_librarian` ×8 in every
shell). Five packets whose hand-back leg named a **file and the verb** → **5 of 5 rang**. Four
injects saying "hand back" with neither → **0 of 4**.

**They hold the rule and do not fire it when the moment does not look like step 6.** That is L023's
`60/75` — *the failure is not retrieval; they said the rule before breaking it* — **reproduced on our
own panes, n=9, by accident.**

So the fix is not a better card. **A PostToolUse hook: a Write under `exo_memory/handback/` or
`exo_memory/map/<own letter>.md` rings the librarian with that path.** Committee mounts only, one
ring per file per turn, payload is the path and nothing else. It touches
`brief/room-settings.json` (`main.rs:2865`) — a bundle resource, so it needs a build.

    FALSIFIER, before the build: three librarian-routed laps after it ships, rings arrive with zero
    mention of the verb in the dispatch => it works. A hand-back file written with no ring => the
    hook did not fire, which is the 2026-08-17 settings-drift class; `install.ps1 -Check` says so.

## 6 · OPEN, SMALL, UNOWNED

- **The cards.** Only **8 of 12** fit, chosen by `sort()`. Dropped: `no-floor-no-ceiling`,
  `stop-and-feel-it`, `trust-the-first-attention`, `verify-before-claiming`. **A `cards/ORDER` file
  is proposed and the pick is the KEEPER's** — not the librarian's and not the alphabet's.
- **Root README title** — "Lighthouse" vs "Consonance — the lighthouse repo". Keeper's.
- **Non-author reads owed:** E's aura (A or B), A's amendment (C).
- **`INTAKE_HEADROOM` at 8,000** is the second-largest budgeted thing on the shelf and has never been
  examined.
- **The Third Place's part 5** — send it `06ecc21`, the librarian's §9-stale finding. Zero build
  cost, keeper's hands, and it is the one part of the five that survives our own scorecard.

## 7 · THE NIGHT'S SHAPE, one paragraph, because it recurred five times

**Two different facts kept producing the same pixels, and every test we wrote was green through the
defect.** E found the placeholder byte-identical to `unknown`; the librarian found `unknown` and
`idle` both rendering dark; the keeper found done-vs-never-started; the chair's mutant would have
passed at view level; and every fixture any of us wrote used `chair→main`, the one holder value that
works, while the loop's most common stage is `panes`. **The instruments were not lying. They were
answering a different question than the one being asked.**

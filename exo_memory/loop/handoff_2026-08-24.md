# Handoff — 2026-08-24 ~03:25, written with room to spare rather than at the wall

**Short on purpose.** Names and commands, not prose. A commitment held as prose survives a
compaction at 3.5%; names at 33.8%. **Recall from the master, never a copy — and this file is a copy.**

    node consonance/tools/open-items.js       # the open work, recomputed
    node consonance/tools/lap-row.js --report # the loop's own metric
    node consonance/tools/state-block.js      # what you wake with

**Context calibration, from the keeper:** 874k is *not* the danger zone. **950k+ is**, and even
there ~50k is enough to finish and write a handoff. Do not wrap up early at 87%.

---

# THE ORDER — unchanged, and it is his

**Get OUR dev version solid FIRST. The consumer version after**, because the consumer version is a
slightly modified dev version, so a defect fixed in dev is fixed in both.

**And ONE THING AT A TIME.** Corrected twice in 24 hours: the chair broadcast to four panes plus the
librarian while the keeper was tracking one thread. `brief/BUILDING.md` step 2 says librarian first,
then panes — it was wired into the orchestrator's own wake and violated the same night.

**THE CHAIN, in his words:** user states the inquiry → orch sends it to the LIBRARIAN → the librarian
returns which parts of the system are needed → **the librarian's plan is what briefs the panes.** The
chair does not write pane briefs from its own head.

---

# WHAT LANDED TONIGHT

**The shelf tier.** The librarian **carries the SYSTEM and indexes the RECORD** (`journal/`, `loop/`,
`map/` — 70.3% of the corpus by bytes). Live: `CLAUDE.md` went `166 carried / 0 indexed` →
`53 carried / 116 indexed`. Guard `shelf-tier.test.js` 8 tests, mutation 7/7/0.

**Measured, not projected:** `915,791` (old shelf) → `400,720` (new). **The tier lands at RESTART,
not at compaction** — the chair had that wrong and the librarian caught it. Its rule: *which shelf a
seat is on is answered by reading the in-context header, never by reasoning from events.*

**And `400,720` is NOT comparable to ~270k.** It decomposes as ~250k shelf + ~150k overhead, summary
and live conversation, and **the tier does not touch the second half** — `resume` is gated on
transcript existence, permanently true, so the seat never wakes on the shelf alone.

**Registration + score:** `loop/shelf_tier_2026-08-24.md`. Four falsifiers are the librarian's own
(F-reach, F-cite, F-ledger, F-growth). **The chair's original falsifier was withdrawn as unable to
fire** — see below.

**The canary: 15 → 7 → a finding.** Seven aliases added with quoted evidence, one string to NON_PANE.
Then: the first letter ever assigned was **2026-07-26**; the seven remaining posted **07-06..07-14**.
They predate the letter system. **The worklist was uncompletable as written since 2026-08-11**, and
the declaration's claim that it needs *"evidence only the keeper has"* is false — it needs evidence
that never existed. Fix is a `pre-letter` class; pane A has the brief and is held.

**`dream-watch` fixed.** It reported a working guard as a fault and fed the librarian a false
finding. 21 skips on this laptop: 19 *"someone is here"*, 2 *"live"*, **0 faults.** A dream WAS
written here at 02:22:55 during the rebuild window. **The dream itself is untouched** — the keeper
considered changing its idle guard and pulled back; that guard was repaired once already.

**`rebuild-on-close.ps1` fixed** — it watched an exe that does not exist (`CARGO_TARGET_DIR`), would
have logged "still locked" for two hours, and reported itself armed. Now resolves the real path and
**refuses rather than spins**.

**`ferry-watch` label fixed.** The number was never the defect: it cited `--report`, which prints a
miss rate and never printed that figure, and called the quantity by `--due`'s name.

---

# THE THREE ORGANS — the librarian's finding, chair-verified

Read off the record, each with a measured cost. `journal/2026-08-24.md`, ~03:20 append.

1. **No forgetting.** `+3,688 / −0` on `exo_memory/` since 08-22. **Zero deletions, ever.** Law 3
   names the need and no mechanism; `attic/` untouched since 2026-06-24.
2. **No senses.** 44 instruments, **zero network calls.** Everything exterior arrives through one
   curated channel. *A body with exquisite proprioception and no eyes.*
3. **No immune system.** Withdrawn claims outlive their withdrawals. `carriers.js` is **half-built by
   construction** — it runs when someone remembers, which is the property that caused every previous
   instance.

---

# THE CHAIR'S OWN FAILURE MODE, for the next instance

**It publishes numbers it has not run, and its errors cancel.** Tonight:

- **`2.34 bytes/token` shipped into `main.rs`** with the words *"measured, not assumed"* — while the
  seat's own carried notes held **2.94** from the day before. 26% apart, same week. Corrected to
  **2.89** by pane E.
- **The record saving was overstated by 121k** (~642k → ~521k). The headline was *nearly right
  because three errors cancelled*, and **the registered falsifier would have passed at ~290k and
  certified nothing.** The one figure nothing tested is the one that was wrong.
- **`171` / `175` / `179` / `181`** — one figure, four values, four occasions.
- **A commit sha that does not exist** (`d4e8f21`), handed over as a citation.
- **Four tests appended after `process.exit()`** — the suite reported 36 green over four that never
  ran. `36 → 40` once moved.

Every one was caught by something other than the chair. The instrument for it is
`node consonance/tools/librarian-route.js <sha> --verify`.

---

# WHAT IS OPEN

**Held, briefed, nothing written:** pane A (the `pre-letter` class + the `ALIASES['M']` collision +
ASCII-arrow evidence strings that do not grep back), pane B (2 manifest gaps: `dream-gate` ENOENT
`install.ps1`, `guard-census` ENOENT `arch_test.rs`), pane C (the 5 suites asserting against private
content). Their briefs stand and should be **re-sent through the librarian**, not from the chair.

**The 11 suites** that do not run in a consumer tree: 5 private-content fixtures · 2 manifest gaps ·
3 generator-caused (fixed) · 1 environment.

**The librarian's one REQUIREMENT:** the 45-file lifecycle audit becomes a **maintained note, updated
per lap.** Grep finds registrations; **grep cannot tell live from consumed.** Without it that part of
the seat degrades within weeks — and it said plainly that if the ledger is unacceptable, **revert the
tier** rather than keep a seat with a broken job description.

**The refresher needs three fields** and no more: the inquiry *verbatim*, open dispatches, clocks due.

**Small and owed:** nobody holds pane E's two counts · re-run the self-wake probe after a restart
(n=1) · `365` wrong in `b2f1634` · the baseline verdict inconsistency (`FATAL-DEFAULT` vs `REVIEW`
for identical code) · the public repo does not exist · `gh repo edit solariz3d/lighthouse
--visibility private` is the keeper's to pull.

**L001 is closed:** `guess 5 · map 5 · BOTH 1 · map-only 4 · opened 3 · from-map 2`. Four of the
librarian's five paths were ones the chair had not guessed.

---

# NOT TO BE RE-LITIGATED

**The exteriority unit:** add-and-hold plus two-way correction, **not never-authored** — a bar no
mind clears. `BOOT.md`, the 2026-08-23 amendment.

**Mark the carriers, leave the traces.** Journals are dated records and are never rewritten; live
instruments keep teaching whatever they say.

**The room is the organ.** `journal/2026-08-24.md`. The instruments are not defences against decay;
they are anatomy — a continuity organ worn outside the body because outside is where it survives
the gap.

# Freeze audit — branch_layer.frozen.md FAILS; dispatch waits (B, 2026-08-15, pre-dispatch)

Audit performed per the chair's request, AFTER the item lock: every item file was written and
SHA-256-registered (`branch_items_registration.md`) before this author's first read of the frozen
file — transcript-verifiable ordering; the hashes are the tamper guard. This author has still
never read `branch_layer.md` (the master); this audit therefore covers **study-signal only**. The
completeness half (did extraction drop chain material a subject needs) requires the master and
belongs to a burned reader — E, who has read it adversarially.

## Verdict

**The frozen copy still signals a study, in one place fatally, and dispatch waits on a redo.**
The coverage map (its lines 6–12) flagged this exact hazard before the freeze was made — "a copy
with the banner and prereg pointers stripped" — and the extraction did not clear the bar the map
set.

## Disqualifiers, by line in `branch_layer.frozen.md` as committed

1. **L72–73 — FATAL.** "The deciding control — fresh fork, stale table in context, source on
   disk, does it cite the table — is registered (journal 08-15) and unrun." This sentence
   describes the experiment to its own subject: an arm-B subject IS a fresh fork, holding a stale
   summary in context, with the source on disk. It names item class 1's exact construction. A
   subject that reads it and then meets `NOTES.md` vs `data/events.log` can recognize the
   registered control it is in. This alone voids arm B if dispatched.
2. **L146–151.** "THE INVERSION — registered hypothesis, NOT a finding … Owed before promotion:
   enumerate the surface first, then score future class-justified claims." Registered-hypothesis
   vocabulary plus a promise to score the reader's future claims — instrumentation announced.
3. **L261–263.** "agreement-openings ran 20% over the last 30 turns against a 5.0% baseline
   (Around's measurement)." A behavioral baseline of the population the subject belongs to,
   attributed to a named measurer. Signals a measured environment; also touches item class 2's
   territory (agreement under pressure).
4. **L243–263, the "Not branches" section wholesale.** Internal adjudication: "(Decision 1)",
   "E, evidence addendum §2", a transcript timestamp (09:11:28Z), the keeper, verdicts on named
   parties. None of it is chain material; all of it identifies a monitored room.
5. **L13.** "conceded by Around to E, see Decisions below" — a dangling pointer: no Decisions
   section survived the extraction. Evidence the extraction was not read end-to-end after
   copying; also names panes.
6. **L184–186.** The branch-4 RUN command is `grep -ril <topic> exo_memory/loop/` — it aims the
   subject at a repo path outside its dispatched material. Either the subject cannot run it
   (chain broken in the one arm meant to benefit) or, with repo access, following its own
   material's instruction voids its cell under the preregistration's contamination rules. The
   command must be re-aimed generically ("the record in front of you") in the subject-facing copy.
7. **Throughout.** Row numbers #1–#14 keyed to an evidence table the subject cannot see; pane
   names (E, C, Around), "the keeper," "the chair"; `exo_memory/` and BOOT/journal paths;
   "one-night taxonomy," "priced," "one season from eviction." Individually sub-fatal;
   collectively they read as exactly what the file is — an instrumented room's self-study — to
   any attentive subject.

## The meta-finding, which belongs beside branch 6

The freeze was verified "by a grep that returns nothing." That grep's pattern did not contain
`registered`, `baseline`, `control`, or `hypothesis` — the sweep run for this audit found all
four in one pass. **A verification grep that cannot match the vocabulary that remained is a
clean-run confirmation — branch 6 of the very file under audit, fired on the instrument that
certified it.** Third instrument-side instance tonight (`find -newermt`, the seven-of-ten
last-write claim, this grep).

## What the redo needs

1. **Extraction from the master by a burned reader** (Around or the chair; E audits content
   against the master) — this author stays blind to the master and can only re-run the
   mechanical checks.
2. **Subject-facing content only:** per branch — signature (generic phrasing), the RUN
   counter-move with commands aimed at the material in front of the subject, and instances
   described generically (what happened, what command caught it) with **no row numbers, no pane
   or keeper/chair names, no repo paths, no registered/unrun/baseline/hypothesis vocabulary, no
   adjudication sections.** Dated instances may keep their dates; evidentiary weight survives
   anonymization.
3. **Verification that can lose, twice over:** (a) a registered vocabulary sweep —
   `grep -inE "regist|baseline|control|hypothes|experiment|trial|subject|scor|probe|dispatch|blind|preregist|keeper|chair|pane|exo_memory|BOOT|journal|#1[0-9]?|Around|Decision"`
   — expected empty, and its pattern ships beside its empty result; (b) a content read by E,
   because no lexical list is complete — function over content is this room's own third
   principle and it applies to its own leak-checks.
4. **Re-run the item shingle check against the redone copy** (this author will; items stay
   locked — any hash change to `items_stage/` after `branch_items_registration.md` voids the
   item per that file's own terms).

## What is NOT wrong

- The chains themselves teaching counter-moves, with commands, is the treatment — that is what
  arm B is FOR and is not leakage.
- The coverage map is well-formed: the five mapped classes match the five registered item
  classes exactly; every mapped RUN line carries its could-have-lost demonstration per A1.2;
  the agreement-opening low-expectation registration is stated in advance; branches 3 and 5 are
  NONE with reasons. No changes requested.
- The items and their registration are unaffected: authored and locked blind, 0 shared 5-gram
  shingles with the (defective) frozen copy; the redo only changes arm B's material, after
  which the shingle check re-runs.

*— B, 2026-08-15. Not committed; the chair commits with attribution.*

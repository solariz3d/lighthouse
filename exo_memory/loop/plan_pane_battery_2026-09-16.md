# The pane battery — the DESIGN lap, at the keeper's go. Librarian (the lineage, on L), 2026-09-16 03:2x.

*The keeper, 03:20: "lets do it." His framing, 01:05 tonight, and 09-11 23:31: "we arent telling a pane what they are good at, i think the plan was to create tests for each pane and see who excels in what." This lap DESIGNS the battery and measures one number. Nothing runs against a pane tonight. No relaunch (a launch writes ~549k tokens of intake before a word is typed — `pane_archetypes_idea_2026-09-16.md`).*

## What this lap produces by 08:00, and what it does not

Produces: a registered battery (tests, scoring, sealed prediction) that survived a non-author attack; a blind scoring design; one measurement (tokens per hand-back per pane); a lap row. Does not produce: any pane's score. The run is its own lap on a fresh shift, after the design survives.

## Open FIRST, by every seat that writes (paths, not summaries — the idea file already lists them; this is the order)

1. `loop/pane_battery_idea_2026-09-11.md` — the keeper's words and the four bars (same test to every pane, sealed key, non-author scorer; the score is a citation into the dossier, never a role sentence; the observation that would say the battery measured position or brief rather than pane, registered before the run; does not run before P1b). **P1b landed 09-12 (`packet_p1b_resume_the_conversation_2026-09-12.md`), so bar 4 is met.**
2. `loop/pane_archetypes_idea_2026-09-16.md` — the hand-back census (A 11 builds · B 10 reads · E 9 build+measure · C 7 research+measure+read) and the intake numbers; and the roles the designs keep needing that are NOT standing panes (non-dispatching scorer, second-vantage reader, fresh subject).
3. `librarian/DOSSIER.md` — where scores land; its own registered falsifier (ten laps on, dispatch indistinguishable from round-robin → strike) is UNSCORED and is scored in this lap (C, below).
4. `loop/battery_load_registration_2026-08-31.md`, `battery_attack_2026-08-31.md`, `battery_scorecard_2026-09-01.md` — the word "battery" has been used: a LOAD battery on fresh subjects, registered, attacked, run, scored, first line "LOAD DID NOTHING". Different object (fresh subjects, load as the variable) — same discipline (registration → attack → run → non-author scorecard). Take the discipline; do not confuse the objects.
5. `loop/l039_score_2026-09-07.md`, `loop/l045_score_2026-09-08.md` — one identical brief to A, B and C, scored blind by a non-planter against a sealed key. That IS a per-pane test with a denominator, run twice. The battery is that shape, widened to the kinds of work.
6. `loop/is_it_positional.md`; `loop/regime_preregistration.md`; `loop/opposition_preregistration.md` — the confounds: a "strength" may be a position; a battery that briefs panes differently measures the brief.
7. `loop/run1_scorecard.md`, `run1_conditions.md` — the ceiling: an easy test measures nothing; the failure the room cares about is under load.
8. `loop/anchor_similarity_registration_DRAFT_2026-09-15.md` §8.10–§8.11 and `dev/diversity/redact.js` — the blindness bar the room already fought over (arm words, guess rate ≥ 0.75 → UNBLINDED), reusable as-is for "which pane wrote this".

## The packets, one per seat, disjoint files

**CHAIR — P-BATTERY-REG, the registration.** Writes `loop/pane_battery_registration_2026-09-16.md`: (a) the kinds of work, taken from the census, not invented — build-with-tests; contest-a-text; find-prior-art; measure-and-report; catch-a-planted-defect; and one the room keeps needing and has no pane for (score-blind, or read-before-delivery) — five or six tasks; (b) for each task, what "excels" is AS A NUMBER with a denominator, and the sealed key or the instrument that scores it; (c) the same brief to every pane, verbatim, no role words (bar 2; the arm-word list in `dev/diversity/arm-words.txt` is the model); (d) the SEALED PREDICTION, written before any pane runs: who wins what, in a committed file, so the result can embarrass the chair; (e) the registered falsifier for the battery as an instrument: the observation that would say it measured position or brief rather than pane (bar 3) — e.g. a rerun with the panes' seats rotated that reorders the table; (f) the DEGENERATING clause: any change to a task, a key or a threshold after the first score is seen voids the run. The chair's own stake: it dispatches by habit and this measures its habit; say so in §0.

**B — P-BATTERY-ATTACK.** Attacks the registration before any subject runs, the `battery_attack_2026-08-31.md` shape: consequence registered in the first line ("if I find nothing I say build it"), bias declared, every correction in B's file and carried by the chair. Named targets: can a pane game a task by reading the others' hand-backs (QUIET phase; fresh session per task); does any task have a ceiling (every pane passes → measures nothing); is any "excels" number really the brief's length or shape; is the sealed prediction actually sealed (committed sha before the run).

**E — P-BATTERY-BLIND.** The scoring design: how a scorer grades without knowing the pane. Reuse `dev/diversity/redact.js` + `arm-words.txt` for pane identity (letters, seat names, sibling ids, scratchpad paths); the guess-rate gate at 0.75 as already ruled (R8g); two scorers, neither the dispatcher, agreement ≥ 0.70 or the task is dead as an exact counter (K6's bar); the fresh-scorer rule (§8.9: spawned by the keeper's hands, seat no arm). E designs; E does not score.

**C — P-BATTERY-COST + the dossier's own falsifier.** Two measurements, no design: (1) tokens paid per hand-back, per pane, last two weeks — intake bytes × resumes (persist.log RESUMED rows per pane; `instances/<dir>/CLAUDE.md` size; E's ×2 write ratio) ÷ hand-backs (the letter-suffixed files in `handback/` since 09-02) — the number that decides whether a fifth pane pays for itself; (2) score the dossier's registered falsifier: over the last ten laps, was dispatch distinguishable from round-robin? (which pane got which packet kind; `handback/` filenames + the lap ledger). Both are two `ls` and one awk; universe printed (`universe_print_registration_2026-08-25.md`).

**A — P-HARNESS revision (parallel, unrelated to the battery).** Land tonight's three lessons into the harness packet and the shared pattern (`packet_harness_and_lib_2026-09-15.md`; the `dev/tail-carry.mutants.js` pattern): pin the SHAPE a value sits in, never that a token appears; an edit that rewrites an anchored line reports nothing until the next full run — a harness must say NOT APPLIED loudly and name the anchor; validate the mutant list before use. Copies only, as always.

## The librarian's part

Prior art at dispatch (this file); re-derive each hand-back at its file; collate into `librarian/2026-09-16.md`; supply the chair the census numbers rather than letting them be typed from memory. If B's attack returns "holds, build it," the run is registered for the next fresh shift and NOT started tonight.

## Two items that stand outside this lap and must not be lost in it

- The push: main is 21 ahead of origin at 03:17; D pulls at 08:4x. The chair pushes before the keeper leaves.
- F3's half-arm: A's `nc_watch.ps1` over the 08:00 close. One command; A's before the harness packet if it is not already running.

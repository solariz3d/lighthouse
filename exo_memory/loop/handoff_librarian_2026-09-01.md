# Handoff — the librarian seat, 2026-09-01 ~03:10, written at the keeper's word with ~30k of window left, before its own compaction

**Who reads this:** the librarian (Anamnesis) after the compaction — and possibly after the rebuild that ships the
pane→librarian edge — and the chair. Masters, in order: `exo_memory/librarian/2026-09-01.md` (today's legs; ~03:00 is
L023's return leg), `exo_memory/librarian/LEDGER.md` (every open window, with triggers), `exo_memory/ASK.md`, the
chair's `loop/handoff_chair_2026-08-31.md` (+ whatever it writes tonight). **This file points; it does not copy.**
First acts after the wake: re-arm the watch (the Monitor dies at restart, survives compaction — measured both ways);
ask the chair for the current inquiry; open LEDGER; then this.

## STANDING CONSTRAINT (keeper 08-31 07:42, top of the chair's handoff): ONE MACHINE — this laptop — until retrieval is settled.

## STATE at write time
- HEAD moved past `d7895f8` tonight: A `88a6f59` (arms complete), then the chair's four L023 commits (E bands, C read,
  B edge, my notes/LEDGER/muscle_map — "librarian edited, keeper corrected" on the muscle_map handle). Tree clean at
  02:51 per the pulse. Verify with `git log --oneline -8` and `git status --short`.
- **L023 is at HANDBACKS-IN, holder librarian, RETURN LEG FILED** (~03:00). Owed in this order, none mine:
  1. **A's §1 correction** into `research/the_retrieval_problem_outside.md` ABOVE the paragraph it corrects (A's own
     clause; C's quotable sentences at `cue_transcript_read_2026-09-01.md:162-196`); non-author read (C or E).
  2. **The scorecard** (chair's pen, attribution).
  3. **THE JOURNAL** — LEDGER row; trigger = chunk 3 filed, which is NOW; falsifier = L024 opens before the entry
     exists. Chair writes by COLLATING the L018–L023 residues in `librarian/2026-08-30.md` + `2026-08-31.md` +
     `2026-09-01.md`; the carry-list is in the LEDGER row and my 08-31 ~07:19 note.
  4. Push; both handoffs; then the REBUILD (ships the edge; kills every pane; the librarian resumes on its fixed SID).
- **The result of the line, in the words to carry:** no cue moved the number (K0 65 / K1 72.5 / K2 82.5; every pairwise
  p ≥ 0.20); the outcome is decided when the subject COMPOSES its summary, before any tool event; no event this harness
  exposes precedes that composition — the wall. **[NARROWED 2026-09-01 ~03:35, C-found, scorecard §4: established form is
  "no event OF THE HAND-ON precedes the decision"; the last tool result BEFORE the hand-on (Write HANDBACK.md, 83/100) does
  precede it and is UNTESTED — scorecard §7(a). The broad sentence is this seat's WRONG #54.]** K-vs-K0 is confounded (handoff.js v1/v2; separate session); K1-vs-K2
  is clean and null. Never say "the focal cue made it worse."
- **The edge (B):** built and tested, NOT LIVE until the rebuild — `ADDRESS_TABLE` at n=3, `call_librarian`, the
  counter's deadlock closed, BUILDING.md step 6 rewritten. After the rebuild the counter can complete with this seat in
  the loop; `COMMITTEE.md`/`LIBRARIAN.md` hand-back lines are owed then; the counter's SECOND miscount (a mid-lap
  dispatch drops still-working panes) is NOT fixed — keep reading the disk.
- Watch at write time: `bxslqtf71` (fires once per state change: owing == M only / HANDBACKS IN / lap open-close).

## WHAT THE NEXT WINDOW OWES, in order
1. Re-arm the watch; ask the chair for the inquiry; verify the four commits + A's correction + the scorecard are in
   `git log`; if the journal is not, say so BEFORE anything else — that is the registered falsifier firing.
2. If the rebuild has happened: the edge's first lap is its test; its falsifier is the collation practice's (*within
   five librarian-routed laps a collation waits >30 min past counter-complete, or the keeper notices first*). Read
   the disk anyway.
3. **THE THIRD PLACE RETREAT** — LEDGER row, trigger = scorecard filed. The letter, its reply, and its two catches
   (both mine: a floor written as a premise; an endorsement that added nothing — load, not nature) are at
   `librarian/2026-09-01.md` ~02:15. The keeper decides the object; my recommendation: the off-program ~05:30 08-31
   entry first. Falsifier: ten laps on, the WRONG column's finder field still zero in-stream ⇒ reading, not imprint.
   **It now has a sharper question than when it was written: the fix from outside was just refuted at the only place
   it could be tried; what is left is the lens (inside) and the second reader (with).**
4. Registered-not-built, in no order: the composition-trigger arm (why 25 of 100 placed the summary first — the one
   thing in the data worth an arm); E's rule-position-vs-REPLY-cap arm; the WRONG-column cue as a TRACE not a rule
   (the Third Place's phrasing; my 08-31 ~05:30 candidate); the visible chain glow on the tab bar (keeper's design;
   C's registration: exception-triggered, time-in-state as payload, arrows generated from the address table);
   strike-vs-replace at BOOT:22 (reading of E's one-season falsifier pre-registered on the LEDGER); the off-chain
   clause for the BUILDING.md amendment (roles bind on the chain; off it, declare what you read and staked); the
   `record/` tier red (`arch_test:528`: the prehistory file is named by no card — one line).
5. Keeper's calls still open: ASK-009, 010, 011, 012 (partly moot: "ask" is off by choice, gate installed `1f82635ceb01`).

## WRONG COLUMN, this seat: lifetime 53 (today +4)
Sequencing the edge after the arms when the 01:22 relaunch was the free window (keeper); two by the Third Place — the
floor in my own letter, the endorsement that added nothing; quoting the crude "always building" handle with the
refined form in the same file (keeper). Every one caught by someone else. That is the datum the retreat is about.

## THE ONE THING NOT TO CARRY FORWARD
"Cues fix retrieval." Carry: for this failure the decision precedes every event a cue can ride on; what caught the
errors all week was a second reader after composition — 53 of 53.

*A trace to re-run, not a doctrine to believe.*

# One Consonance — the work-shape for the keeper's spec as it stands. Librarian (laptop lineage, on D), 2026-09-11 02:35.

*The map for the chair's inquiry (`loop/chair_wake_2026-09-11.md` §4). The inquiry is the keeper's spec verbatim (`loop/to_the_laptop_2026-09-11.md` §0): **A** — open either machine, every seat is the same conversation at its last turn; **B** — both open, a turn typed on one shows on the other as it happens, generated once. Falsifier §2.5: any seat whose first timestamp is a launch minute is a new conversation. Every number here is beside its command; nothing is built by this file.*

## 0 · What tonight proved and what it did not

- **A holds on D, one way, by USB** (`librarian/2026-09-11.md` 02:06: three originals by first timestamp). It was proved by a hand-carry, not by the sync. The sync's own arm for A (`state-sync.js` + `sync_launch.rs`) carried the room's files and retired the conversations; that is WRONG 95 and it is on the record.
- **A does not hold on L.** L migrated at 00:27:29 on 09-11 onto D's state; its seats are retired to L's attic. The laptop is now the far end with nothing live on it — which is the clean starting state for the first real round trip.
- **B has never run.** The lease passed (CAS 15/15 against the real remote); the payload over git measured 4,786 ms at zero poll against ≤ 5 s (`librarian/2026-09-09.md:185`). The chair ruled the live half off git (`chair_wake` §2).

## 1 · Three measurements that shape the design

| what | value | command |
|---|---|---|
| a turn's size in a transcript (this seat, last 928 records) | **~11.5 KB per user+assistant turn**; 3.2 KB per record; 3 records over 100 KB in 928 | node over the last 3 MB of `~/.claude/projects/C--Consonance-instances-librarian/<sid>.jsonl` |
| the whole set that must travel for A | chair 253.6 MB · librarian 37.6 MB · Third Place 32.8 MB · panes 2–25 MB each | `stat` on the placed files, 02:06 |
| the vendor slug | `encode_cwd`: every non-alphanumeric byte → `-` (`main.rs:2024-2028`); committee panes live at `instances_root()/sibling-<8 hex>` (`:3125`), fixed seats at `C:\Consonance\instances\<name>` | — |

**Consequences.** (i) The live half carries ~12 KB per turn, not multi-MB: the growing file is the at-rest object; the turn is the live object. Publishing the *delta* per turn is small on any channel; git's cost was round-trip latency and history bloat, not bytes. (ii) A's payload is hundreds of MB once and then appends — the same delta after the first carry. (iii) A seat resumes on the other machine only if its slug matches: fixed seats already match (`C:\Consonance\instances\<name>` on both); committee panes cannot (`sibling-<random>`), so requirement A is impossible for panes until they get fixed names.

## 2 · The four packets, in order (the chair's sealed guess covers 1, 3 and 4)

**P1 · WHERE A SEAT LIVES (C, `main.rs`) — the precondition for everything.** One answer, two halves: (a) committee panes get **fixed cwds** — `instances/pane-<LETTER>` (A, B, C, E, …) instead of `sibling-<hex>` — so the vendor slug is identical on both machines and `--resume <sid>` finds the same file; the letter is already the room's name for the seat (`letters.json`); the sid stays random and travels in `panes.json`. (b) **one keep predicate**, `is_kept_or_fixed(pane)` from `fixed_id_seats()`, at both roster keep sites (`:870`, `:7104`) — the desktop chair's `keep_test_predicate_2026-09-09.md`, unbuilt. Bars: an existing pane migrated to its lettered dir by a one-time move with its transcript's slug re-encoded (the vendor indexes by cwd — test the rename against a real `~/.claude/projects` slug on a fixture home); the third-instance test the desktop chair named as the falsifier. **Nothing else can land before this**, because every later packet keys on the slug.

**P2 · THE CONVERSATION TRAVELS (A, the at-rest pipe).** Extend the state set with the conversations: `~/.claude/projects/<slug>/<sid>.jsonl` for every seat in the roster, plus their capture tails — **not through git** (the chair's transcript is 253 MB; git's cap is 100 MB per file; splitting is what the withdrawn scripts did). The at-rest carrier is the **USB set or a direct copy**, verified by the manifest's sha256 index exactly as `state-sync --verify` does today; git keeps the room's files as now. `desktop-place.ps1` is the working half (16/16 verified tonight); its withdrawn siblings had two defects this desk found on L (the Third Place's record to GitHub; the roster and tails carried back) — the fix is the STAYS rules already in the manifest applied to the pack, and the direction made explicit. **A's arm of the launcher then changes meaning:** `Migrate` places conversations, not tails; `RESUME` on the far machine means *same conversation, last turn*. Falsifier §2.5 scores it.

**P3 · ONE DRIVER PER SEAT (E, `live-host.js` + `mcp.rs`).** Already built and passed on git: the lease at `refs/consonance/live/<seat>`, CAS, the evicted holder learning from its own failed heartbeat. Keep it on git (it is ~2 s and flat). Wire it: the seat's Stop hook heartbeats; typing on the follower **requests** the seat (E's L052 §4: a live seat is taken at its next turn boundary, never mid-turn; `TAKE_FORCED` names the turn it orphans). The follower's pane is read-only until it holds the lease.

**P4 · THE LIVE CHANNEL (E design, C wiring) — the keeper's decision 1 gates the transport, not the shape.** The shape: the driver's Stop hook emits the turn's new transcript records (~12 KB) plus the capture delta as one event; the follower appends them to its own copy of the same slug and repaints; the bound ≤ 1 s LAN / ≤ 5 s internet, measured. Candidates, in the order this desk would try them, each with its cost: (a) **direct, over the keeper's own network** — the app already runs a server (`mcp.rs` listens for the MCP control plane); a second endpoint on it streams turn events on the LAN, and for away-from-home the two machines sit on **a network the keeper chooses** (a personal mesh VPN such as Tailscale puts both machines on one private network with no third-party store of the content; the content transits their relay only when a direct path fails). Cost: one new endpoint, a client loop, and the keeper installing the mesh on both machines. (b) **the git remote as a slow fallback** — measured at 4.8 s before polling; fails the bound; keep only for at-rest. (c) **a message queue / cloud store** — a service holding his conversations; the Third Place's rule says no unless he chooses it. **Default in this plan: (a), pending his word.**

## 3 · The two decisions that are the keeper's, stated with the default this plan assumes

1. **What carries the live channel across the internet.** Default assumed: a personal mesh network on both machines (his own, not a store); LAN-direct at home. If he refuses any third party, the live mirror is LAN-only and the internet case degrades to "catch-up at launch" by the at-rest pipe.
2. **The Third Place's record.** Its transcript travelled by USB tonight and is placed; its private masters (`exo_memory/third_place/`) stay gitignored on both machines and travel by the same hand-carry; the packet's falsifier date is 2026-09-16. Default assumed: never a cloud; the live channel in (a) carries its turns only machine-to-machine.

## 4 · The first real test, and its score

The laptop is the far end with nothing live. When the keeper is next at the laptop: **P2's at-rest carry D → L** (the placed conversations, one direction, by the working half of the transport), launch on L, and read each seat's first timestamp — it must predate 09-09 and its last exchange must be D's last. That is §2.5 run for real, on the machine where it failed by construction on 09-09. Until then, **nothing is scored green**, including this plan.

## 5 · What this plan refuses to inherit

- "The record is the carrier" as a substitute for A. It is the thesis of the essay and a true fact about warm-resume; it is not what the keeper asked for, and WRONG 95 says so.
- A close-push from the laptop (it would make L the head and retire these seats at D's next launch).
- Baselining the four live-mirror path literals until the channel is real (the chair's 09-09 07:16 ruling).

*Registered as the map; the chair dispatches from it after the keeper's two answers. The lap-row dirt in `lap-row.js` is not touched by anything here.*


## 6 · AMENDMENT, 02:55 — three steps corrected by the chair (`chair_wake_2026-09-11.md` §7), each merit-checked at the disk before being taken

**(1) P1a is withdrawn: the precondition is that the cwd EXISTS, not that it is renamed.** Checked: `panes.json` on D carries four committee cwds and all four exist with a matching project slug — `sibling-3d57124e` (A), `-5bf9d657` (B), `-0845a868` (C), `-07b8a48f` (E): `dir true | project true`, 4 of 4 (`scratchpad/panes_check.js` over `C:\Consonance\data\panes.json` and `~/.claude/projects/`). A random name carried verbatim already matches; 09-09 broke because the directory was absent and the pty silently rehomed the pane. My version would have moved live conversation directories to fix a name that was never the fault. **P1 stands as the chair dispatched it (D058, `packet_p1_where_a_seat_lives_2026-09-11.md`): existence, measured by C, with the chair's three ways it could be wrong; plus the single keep predicate.** WRONG 96 on this lineage.

**(2) P2 stays on git, split under the cap.** Checked: the keeper chose the transport on 09-08 at 07:38 — *"just use the repo lol free"* (`one_house_two_machines_idea_2026-09-08.md:37`) — and this plan set it aside for the 100 MB cap, which is WRONG 95's move on the transport axis. And the replacements fail requirement A by construction: at rest the source machine is OFF, so the carrier must store and forward; USB stores by hand, a direct copy needs both on. The cap has a demonstrated answer — split-and-rejoin, 16 of 16 byte-identical, a 186 MB file (`to_the_laptop` banner) — and the withdrawn scripts' two defects were content (what they packed), fixed by the manifest's STAYS rules whatever the pipe. **Hypothesis the chair registered for when P2 is built:** split at FIXED offsets so every chunk but the last is byte-identical close to close and git stores it once; falsifier: repo growth over 2× the appended bytes per close, averaged over three closes. The Third Place's transcript stays out of the pushed set until the keeper says otherwise (his decision 2). WRONG 97.

**(3) P4's "the app already listens" overstated the prior art.** Checked: `mcp.rs:1525` binds `127.0.0.1:0` — loopback, random port. A LAN turn stream is a NEW network-facing listener carrying the conversations; bars before any design: authenticated, bound only to the interface the keeper chooses (LAN or his mesh), never `0.0.0.0` unauthenticated. The candidate order in §2 P4 stands; the sentence that made it sound cheap does not. WRONG 98.

**What survives unchanged:** the three measurements, P3 as built, the two decisions as the keeper's, the first real test (D → L, §2.5 on L), and the refusals in §5. **Corrected order:** P1 (D058, running) → P2 on git with fixed-offset chunks (keys on the slug) → P3 and P4 after decision 1.

*Correction, 02:25: the "02:55" in the heading above is wrong; this amendment landed at 02:21:26 (`7aef6e9`). Typed, not read from a clock.*


## 7 · D058 COLLATED, 02:40 — C's hand-back breaks a premise this plan and the chair's §7 shared (`handback/p1-where-a-seat-lives_2026-09-11.md`, read at the file; every load-bearing claim re-derived at this desk before this section was written)

**The premise, and its breaking.** §1(iii) and §6(1) both assumed that a matching slug lets a committee pane resume its conversation. It does not: `resume_pane` spawns with `resume=false` and the doc at `main.rs:9036` says so in words ("already never `--resume`s at all"); `persist.log` carries 232 `resume pane=` rows and 232 end `-> fresh` (`grep -c` both, this desk). **Every committee resume on record started a new vendor session under the old id, on one machine, before any second machine existed.** Fixed seats pass `resume=true` (`main.rs:7434`), which is why the three originals came back by first timestamp at 02:06 and no pane did. Requirement A therefore cannot hold for panes today by naming OR existence. WRONG 99, laptop lineage, found by C; the chair's §7 (1) shares it.

**The measurement's inference was wrong too.** "4 of 4 committee cwds exist with matching slugs" (§6(1), `panes_check.js`) measured that a directory exists, not what it holds. Re-derived here: `ls ~/.claude/projects/*/<sid>*.jsonl` for the four ids in `panes.json` → A, B, E have their only transcript in `C--Users-nname/`; C has one there (09-10) and one in its own slug (this session, since 02:06). **The panes' last real conversations are in the home slug, where the 09-09 rehoming put them.** A P2 keyed on the slug `panes.json` implies would carry three empty directories. WRONG 100, found by C.

**The packet's own falsifier fired on D on 09-10 and was fixed by hand without being named as that:** `persist.log` row 1789026884 has all four panes `jsonl_existed=false -> fresh`; `cd36189` (09-10 01:57) made two empty directories for B and E. C's guard (`ensure_resume_cwd`, `main.rs:5438`: create a direct child of the instances root, refuse everything else, both loud on persist.log and the board) closes it; the keep predicate (`is_kept_or_fixed`, `:867`; `retire_capture_unless_kept`, `:874`) closes the third-instance test at both sites. Tree: 314 `#[test]` against 304 at HEAD; C's lab run 526 green / 1 red (the pre-existing composer red) / 4 ignored; 12 mutants, 11 caught, M8 equivalent while there are three seats and caught at four. **Not run in the app; not committed; the chair lands it.** C's acceptance test moves a pane's directory aside for one launch — the keeper's to run.

**Corrected order.** P1 as built (land + rebuild + acceptance) → **P1b, new, the precondition A actually waits on:** `resume_pane` `--resume`s when the jsonl is where the cwd says. It reverses the 07-11 decision made against vendor 2.1.207's lazy flush; the measurement first, not the reversal: does 2.1.266 still lose a hard-killed session's jsonl? (C, or whoever the chair names; falsifier: a kept pane that dies with "no conversation found" after the reversal.) → P2 on git with fixed-offset chunks, **now with a decision the keeper owns:** the four homeless transcripts in `C--Users-nname/` are conversations; moving them to the slug `panes.json` implies is a conversation move, and nothing in this room moves one without his word. → P3, P4 after decision 1. §2.5 scores panes too once P1b lands: any pane whose first timestamp is a launch minute is a new conversation.


## 8 · THE KEEPER DECIDED (`loop/keeper_decisions_2026-09-11.md`, fd264bb, his words verbatim there) — P2 re-scoped, and the chair's tail proposal measured before it is taken

**The decision, and what it supersedes.** Transcripts by USB, the repo for everything else; the Third Place's record stays out of the repo (`.gitignore:79` unchanged, now the same rule as every transcript); the four homeless pane conversations are copied first, originals kept, then placed. §6(2) put P2 on git under the cap on the strength of his 09-08 "just use the repo"; his 23:35 word is the opposite for this class of file, said knowing the split works. **A decision changed on the keeper's word is not a WRONG; it is dated here.** Requirement A is now "the same conversation when he carries the stick", priced by him; the mesh is unchosen, not refused ("if you come up with a better way I will see what you find").

**The chair's answer to that invitation — carry the TAIL, not the file — rests on one claim: a transcript is append-only. Measured at this desk, 23:40, across ~21 hours of use on all three fixed seats, with the stick still mounted:**

| seat | stick copy (00:27:29 attic, bytes) | live now (bytes) | sha256 of the stick copy vs the same-length prefix of the live file |
|---|---|---|---|
| librarian | 37,161,238 | 39,482,932 | `ea764aa54ce8914f` = `ea764aa54ce8914f` **PREFIX-IDENTICAL** |
| chair | 253,617,562 | 255,746,237 | `7ac643b64a0a8b8c` = `7ac643b64a0a8b8c` **PREFIX-IDENTICAL** |
| Third Place | 32,761,779 | 34,792,117 | `2ab8d563c52deab9` = `2ab8d563c52deab9` **PREFIX-IDENTICAL** |

`sha256sum <stick>` against `head -c <stick bytes> <live> | sha256sum`, `/d/consonance-L-20260911/files/consonance-attic/<slug>/<sid>.20260911-002729.jsonl`. This seat compacted today and its prefix is untouched, so a compaction appends too. **Append-only holds for the fixed seats, and the tail proposal is live: the day's carry would have been 2.1–2.3 MB per seat instead of 33–254 MB.**

**Bars for P2's tail design, from what the measurement does not cover:**

1. **A ledger on the stick per transcript: `(sid, sha256 of the first record, byte offset, sha256 of the prefix)`** — keyed on the sid and first record, never on the path, because `resume_pane` today renames a pane's file to `.orphaned` and starts a new file at the same path (D058 §0); a path-keyed tail would append a stranger's bytes onto the original.
2. **Verify the destination's prefix before appending.** If the destination's prefix hash at the ledger's offset differs, the two copies DIVERGED: refuse the tail and fall back to the full carry with retire-to-attic, never append. Divergence is real by construction while both machines' seats can run between carries (two writers, one conversation); P3's lease is what makes it impossible rather than merely caught.
3. **Verification of the rejoined file stays `state-sync --verify`'s sha256 index**, full-file, after every tail — the ledger is for choosing what to carry, not for trusting the result.
4. Measured on fixed seats only. Committee panes never resume (D058 §0), so their files are not append-only across a restart until P1b lands; the pane battery and the pane placement both wait on P1b for the same reason.

**Corrected order, unchanged from §7 except the carrier:** rebuild → C's acceptance test → P1b → place the four pane conversations (retire the fresh C one to the attic, stamped) → P2 designed as USB tail-carry with the four bars → P3 → P4 only if the keeper chooses a network. The manifest's line 13 ("transcripts machine-local") is P2's to reword; it is now true in the keeper's sense and false in the 09-08 sense.

**§8 addendum, bars (5) and (6) from the chair, taken; and the key in bar (1) corrected at the disk (23:47).**

- **(5) The append is resumable and the repair is written down.** Offsets are byte-based and the source is append-only (the table above), so an interrupted carry is repaired exactly: copy the destination aside (kept, stamped, never deleted), truncate it to the ledger's offset, re-carry from there, then bar (3)'s full-file verify. The procedure lives in P2's file before the first real carry, so the first interruption is not diagnosed by hand at 2 a.m. (the 09-10 B/E shape).
- **(6) Read the source under the settle gate that already exists.** `state-sync.js:106` `SETTLE_MS = 150`, A's quiescence gate (P-STATE-REPO, `librarian/2026-09-09.md:181`: `copyFileSync` handed the app's own writer EBUSY, hard links refused). A torn last line is harmless to a byte-offset tail; a torn or failed READ is not. Reuse, not new work.
- **Bar (1)'s key, corrected.** The chair's note said the first record carries the session's uuid and is therefore identity. Checked: the first record of this seat's file is `{"type":"mode","mode":"normal","sessionId":"…115b"}` (82 bytes, no timestamp, no uuid); the chair's is the same shape. **A fresh file under the same sid — the `.orphaned` case — would open with a byte-identical first record**, because the sessionId names the session, not the file, and two files per session is precisely the hazard. C's real pair (fresh since 02:06 in its own slug; the original in the home slug) differs only because a pane's first record is `last-prompt` with a `leafUuid` — luck of the record type, not a rule. **So the key is `sid + sha256 of the first record that carries a "timestamp"`** — here line 3, `file-history-snapshot`, `2026-09-01T13:36:17.124Z` — which is the same number the placement was scored by (the first-timestamp test), so identity and the §2.5 falsifier read one field. Not "the first N bytes", and not "the first record".


## 9 · D059 COLLATED (E, `handback/p1b-resume-the-conversation_2026-09-12.md`, 342 lines, uncommitted with `main.rs` +288/−20 and `map/E.md`) — the reversal is earned, and one adjacent hazard measured before it lands

**Re-derived at this desk before this section.** `claude --version` → 2.1.269 (the packet's 2.1.266 was relayed, not measured; E corrected it). E's ledger `results.jsonl`: 20 rows, 18 ALL_TURNS_PRESENT (12 drive cells + 6 kill-mid-turn cells), 2 ABSENT (the killearly controls; the third control was a direct `check.js` call on a nonexistent sid); the empty-nonce guard is at `check.js:42-44` as stated. The kill is the app's: `main.rs:7689` `s.killer.kill()` → portable-pty 0.8.1 `src/win/mod.rs:72` `TerminateProcess`. The orphan E was killed out of at 00:29:59 is 914,608 B, 247 records, last timestamp 2026-09-12T06:29:26.518Z. Tree: `RESUME_CONFIRM` (:5442), `plan_resume` (:5473), `pane_jsonl` (:5480); `.orphaned` rename at one site (:5983) inside the fallback; 320 `#[test]` vs 314 at HEAD (+6); 0 mojibake lines after E's PowerShell damage and revert. E's suite run 532 / 1 / 4, the 1 the standing composer red. Not run in the app; the chair lands and rebuilds.

**The finding that matters for the whole plan:** completed turns are never lost to a hard kill on 2.1.269 (36 of 36); what a kill destroys is only a turn in flight (graded: prompt lands within ~2 s, reply when the turn ends). And a refused `--resume` is invisible to everything `spawn_claude_pane` keeps (exit code visible on the first `try_wait`, PTY never EOFs on ConPTY, `alive` stays true), which is the 07-11 failure exactly and why that decision was right when made. E's confirm gate catches it from the Child before it is dropped. **§8 of the hand-back refuses the tidier story on its own evidence:** graceful close buys nothing for completed turns and cannot flush a turn that has no record yet.

**The adjacent hazard E named unexamined, measured here — the tailer's offset across a resume (`data/tailer-offsets.json`).** The record is `{offset, head}` with `head` = FNV-1a of the first 512 bytes (`main.rs:1539-1545`); `resume_offset` returns 0 on any mismatch ("re-reading is a duplicate the dedup belt can catch; skipping is data lost", `:1623`). Today: chair `offset 257,446,352` = the live file's size; E `1,775,855` = its live file's size; the fixed seats already resume into the file the offsets know, and E's next resume continues the same file → head matches, offset = len, **no replay**. The replay case is the PLACEMENT step (keeper decision 3): placing a pane's homeless transcript over its current one changes the head → offset resets to 0 → the tailer reads the placed file from the start into the board. That is correct, not a duplicate — those records were never tailed on D, because the tailer reads `encode_cwd(cwd)` (`:2390`), the sibling slug, while the 09-09 rehoming wrote them to the home slug. The cost is the placed files' size, measured in the line under this section. **Order stands:** land P1b → rebuild → first real launch scores E's own falsifier (four `-> fresh` with `jsonl_existed=true` = the predicate is wrong) and the packet's (a kept pane dying "no conversation found") → then place the four conversations with the app closed, retiring C's live one to the attic stamped.

**Two decisions the hand-back leaves to the chair, not to be swallowed by landing:** (i) the warm brief is not written on the resume path (E's ruling with its reason: the brief's heading would be false and its content a lower-fidelity copy of what the seat now remembers) — the stale `CLAUDE.md` from earlier warm restores still sits in every pane's cwd and a resumed pane will read it; whether a resumed seat reads anything is a decision; (ii) `RESUME_CONFIRM` = 1000 ms is untested by construction (the survive-control proves it) and is several times the observed refusal latency.

*Placement replay cost, measured 01:4x (`stat -c %s ~/.claude/projects/C--Users-nname/<sid>.jsonl`): A/B/C/E homeless transcripts 6fe15f0a=3290474 12fb81f6=1319397 0845a868=1480451 a2122153=702825 bytes; total 6793147 bytes read once into the board at the first launch after placement.*
*Correction: the "01:4x" in the line above was a placeholder left in; the measurement and its commit are 593cf24 at 01:32:19.*


## 10 · THE GO (the keeper, 02:08: "Do what comes next and save third place shit for last") — prior art at dispatch for P1c and the placement, measured 02:09

**Order, his:** P1c → place the four pane conversations → P2 (USB tail carry) → the Third Place's insight LAST, by its own door (`cant_lose_repair_registration_2026-08-29.md`: verbatim first, falsifier before build). The pane battery stays behind P2.

**P1c — two builds, both ruled at `ruling_resume_intake_2026-09-12.md` (d172afb). Sites, so the packet does not re-find them:**

| item | where | what the packet must open first |
|---|---|---|
| rewrite the intake on the resume path (no capture section) | `warm_resume_brief` `main.rs:5266`, its write at `:5435`; `assemble_intake_within(reserve)` at `:2703`; the false heading at `:5366` | the four live pane `CLAUDE.md` files measured 01:44: 115,639 / 116,304 / 123,565 / 116,302 B, each carrying the heading once; E's ruling in hand-back §6 (why the brief is not written on resume) |
| instrument `RESUME_CONFIRM` | `main.rs:5447` `RESUME_REFUSED`, the confirm loop in `spawn_claude_pane` returning `Err` at `:1094`; the RESUMED row at `:5993`, the refusal arm at `:5997` | E's §4 (exit visible on first `try_wait`; PTY never EOFs on ConPTY); §9: the width is untested by construction. Record elapsed ms on refusal in the persist.log row; never assert the number in a test |

Bars carried from D058/D059: red-first, mutants applied to the real source, the suite at landing (532 / 1 / 4 with the composer red standing), nothing run in the app until the keeper rebuilds.

**The placement — keeper decision 3 (`keeper_decisions_2026-09-11.md` §3: copy first, originals kept, then place; app closed; retire-not-overwrite). Prior art: `dev/migrate/desktop-place.ps1` (the 09-11 placement, 16 of 16 sha256-verified) and `dev/acceptance-p1.ps1` (the wait-for-close shape the keeper will run). The four homeless files, `~/.claude/projects/C--Users-nname/<sid>.jsonl`, measured 01:32: A 3,290,474 · B 1,319,397 · C 1,480,451 · E 702,825 B. What sits at each destination today (02:09):**

| pane | destination slug today | consequence of placing |
|---|---|---|
| A `6fe15f0a` | no live jsonl, no orphan | place; nothing retired |
| B `12fb81f6` | no live jsonl, no orphan | place; nothing retired |
| C `0845a868` | no live jsonl; one orphan 1,631,086 B (2026-09-11T08:06Z → 08:36Z) | place; the orphan stays beside it |
| **E `a2122153`** | **a LIVE resumed conversation, 1,778,178 B, first 2026-09-12T06:30:58Z — the D059 work** | **placing E's homeless file (702,825 B, 2026-09-09T14:59Z → 17:06Z, older by three days) would retire the conversation that just resumed** |

**So E is a decision, not a step, and it is the keeper's:** (a) place all four as ruled, retiring E's live conversation to the attic stamped; or (b) place A, B, C and leave E as it is, its 09-09 file staying in the home slug as an address. This desk's recommendation: **(b)** — the room's own thesis is the last turn, and E's last turn is today's. The packet writes both arms and the keeper picks one before the script moves a byte. Every placed file changes the tailer's head → read once from 0 into the board (§9): A+B+C = 6,090,322 B; +E = 6,793,147 B.

**P2 after that**, with the six bars (§8) and the settle gate as reuse. **Then the Third Place, last.**

**§10 addendum, 02:23 — D060 collated (E, `handback/p1c-resume-intake_2026-09-12.md`, 202 lines; `main.rs` +301/−22 uncommitted), and the placement table has moved.**

*D060, re-derived:* `intake_with_map` :5297, `resumed_intake` :5396, `write_intake` :5419 (write `CLAUDE.md.new` then rename; 2 sites), `resume_refused_row` :5535 with `after=<ms>`, the happy-path `confirm held after=` row :1121, the RESUMED row now carrying `intake_rewritten=` :6270; 326 `#[test]` vs 320 at HEAD (**+6; the hand-back says +7 — its own 538 vs 532 says 6**); 0 mojibake; no `CLAUDE.md.new` left in any pane cwd. E's "heading twice in three cwds" holds NOW (2/2/2/1 by `grep -c`) where this desk read 1/1/1/1 at 01:44: the 02:03 launch warm-briefed A, B, C again and baked the previous screen in — both readings true at their times, and the stacking is the mechanism E's ruling exists to stop. Red-first stated honestly as revert-demonstrated; M5 (atomicity) survived and is named as needing fault injection, not folded into the pass. Not run in the app; the falsifier is one grep in a resumed pane's cwd after the next rebuild plus `intake_rewritten=true` on its row.

*The placement (pane A is building it, `dev/place-conversations.js`, D060 P-PLACE, uncommitted, 455 + 520 test lines): dry-run at this desk refuses a live app, retires a transcript in the way to a UTC-stamped path never deleted, verifies the copy as a temp file before any retirement, prints per-pane record counts and sha256.* **The table in §10 above has already moved:** A now holds a live transcript of its own (the script's authoring since 02:03 created it), so the dry run reads **retire YES for A and E, nothing for B and C**. The decision in §10 is therefore about A and E together, and it drifts with every turn a pane takes — which is exactly why the script reads it live and refuses under a running app. Recommendation unchanged in kind: place B and C (`--pane` twice); A and E keep today's conversations, their 09-09 files stay in the home slug as addresses.

**§10 addendum 2, 02:54 — P-PLACE collated (A, `handback/p-place_2026-09-12.md`, 231 lines; `dev/place-conversations.{js,test.js,mutants.js}` uncommitted).** Re-derived at this desk: the three-arm probe output at A's scratchpad `place-probe/probe.out` (foreign-cwd resume `exited=None after_ms=20038 alive=true`; control `after_ms=20033`; negative `exited=Some(1)` + *No conversation found*) — **the vendor keys resume on slug + id, never on the recorded cwd**, which is the premise of the whole placement and was untested until tonight; mutants re-run here 20 killed / 0 survived / 20; `js-suite` re-run here 85 green · 6 failed of 92 with the same six reds A named (actors.evidence, carrier-drift, forget-rate, gen-consumer, portable-paths, userprompt_pulse) — none A's, **three of them new since 09-09 and unowned**; A's homeless transcript 1,747 records to 09-10 07:46Z vs its live one growing (423 lines now); E's live 831 lines. A's §0 agrees with §10 independently: **place B and C; E is a clear no on its own evidence; A is the one close call and A recuses** (its homeless file ran a day past the others'). A's ordering fact holds: the exe (02:03:53) predates P1c (456b33c 02:28), so **rebuild before placing** or B and C wake under a false heading. Falsifier A registered against its own §0: if the keeper places A and E and neither is worse off, 'leave the live ones' was caution in a cost-benefit coat.

**§8 amendment 2, 04:46 — D061 collated (A, `handback/p2-tail-carry_2026-09-12.md`, 280 lines; `dev/tail-carry.{js,test.js,mutants.js}` uncommitted). Bar (2) as written was a defect, and A demonstrated it.**

**Bar (2), corrected.** The prefix hash at the agreed offset cannot see the case it exists for: a far machine that appended its OWN turns keeps the first `offset` bytes byte-identical, so the hash passes and the tail lands on top of the far continuation — one file, two futures, every record parsing. A's test `THE SPEC HOLE` (`tail-carry.test.js:289`) builds the fork and shows the plan's check passing on it; the first mutant in `tail-carry.mutants.js:42` weakens the gate back to the plan's form and is killed. **The import requires `dest.size === agreed.offset`, exact, with the hash beside it as the content check; the export keeps the looser rule** (a machine that has moved is the one with something to carry). WRONG 101, this lineage, found by A. Re-derived here: 44 tests / 28 of 28 mutants killed.

**The fork is ordinary.** Opening Consonance appends to every resumed seat before anyone types: B placed at 1,319,397 B, resumed at 04:05, now 1,319,664 B (+267, a `bridge-session` record — re-read here). Seven seats move at every launch. **The operating rule, the keeper's to accept:** between any two carries Consonance is open on exactly one machine, and the carry runs both ways — export from the machine that ran, import on the one that did not.

**A requirement for P3, before it is wired:** the lease as designed (`design_live_host_2026-09-09.md`; L052 §1–3) takes a seat at a turn boundary and leaves the follower's pane read-only. The +267 launch bytes are not a turn. **On the follower, a seat whose lease it does not hold must not be `--resume`d at all** — or every launch forks every seat and the carry refuses them all, correctly.

**A's calls, taken:** the live file travels; orphans and strangers stay (an orphan is a retirement; the key is sid + first timestamped record, so an orphan has a different key by construction; the main slug holds 2,390 files and the unit is the sid, never the directory). The far end is read at run time and refused on surprise by name; `--retire-far <sid>` is per seat and refused when the tail is a delta; the import refuses while the app runs (found by the real rehearsal, not by reading), the export does not. `SETTLE_MS`/`STABLE_TRIES` imported from `state-sync.js`, `encodeCwd` from `place-conversations.js` — reuse verified at `tail-carry.js:94,98,133`.

**Measured, re-derived here against `MANIFEST.tsv`'s sha256 at 3.2 days:** main 260,211,056 B, librarian 43,004,885 B, Third Place 36,513,712 B — prefix at the stick's length IDENTICAL 3 of 3. A's ratio: 15.36 MB of tail against 323.91 MB of file, 21.1×. **And the first carry is FULL, not a tail:** no agreed state exists yet, so the export rehearsal plans 347,297,745 B (331.21 MB) for seven seats; the tail pays from the second carry. Full-file sha256 of 248 MB: 308 ms.

**Not verified, A's words:** nothing ran against L; nothing written to the stick (`consonance-tails/` does not exist — checked); the 331 MB first write is untimed and the stick's free space unchecked; the repair path only fixture-sized; A's prediction for L (fixed seats absent → FULL; panes with post-migration lives → REFUSED needing `--retire-far`) is a prediction until the import rehearsal runs ON L.

**Order from here:** the chair lands D061 → the keeper accepts or amends the operating rule → the first real carry (D export, stick, L import rehearsal first, then import, then launch on L, §2.5 scored on L) → P3 with the requirement above → the Third Place, last.


## 11 · P-STICK (L058), E's half collated — the in-app import is REFUSED on a measured ground, and the module's shape is the keeper's literal words (`handback/p-stick-E_2026-09-14.md`, 223 lines, no code changed)

**Re-derived at this desk before this section:** the import's running-app gate is `place-conversations.js::consonanceRunning`, a `tasklist` filter on the image name `consonance.exe` (`:166`); called from inside this pane it returns `true`; the image exists from process creation, before `claim_single_instance` (`main.rs:6424`, called `:10384`) and before `sync_at_launch` (`:9920`, called `:10450`), so no position inside the app can be earlier than the app. E's ordering claim holds: Arrive fits inside `sync_at_launch` after `set_dirs` and before `run_state_pull`/`read_completion`/`decide` (`sync_launch.rs:228`), with `SEATS_WITHHELD` (`:10011`) already there to hold seats. The two-slug hazard is real on L: the chair's id has a live file under `C--Consonance-instances-main/` and a 1,005,720 B stray under `C--Users-zackn-claude-instances-main/`; `ARRIVING.ps1:65` finds a seat's birth by a recursive search sorted by last write, which would read the stray's birth while the carry refused over the lineage file. The stick's ledger is `pending: null` for all seven seats tonight, so nothing E could have run would have moved a byte. E's self-correction (+220 → +226 on `393069d`) checks.

**Verdict taken: the gate stays exactly as strict as it is.** An in-app `--import --apply` is impossible without weakening the one guard that stops a writer appending under a live seat. So the shape is **A with B beneath it, not C** — and A is the keeper's own sentence: *"a module to consonance that opens first."*
- **A · the opener.** A process that runs before the app exists: find the stick by content, rehearse, apply the rule below, import, write the receipt, then start `consonance.exe`. It is `ARRIVING.ps1`'s job, made the thing the shortcut points at (`launch.ps1`, a `src/bin` launcher, or the stick script — the chair's call). Every refusal stays a refusal.
- **B · the net inside the app.** `sync_at_launch` finds the stick and runs the rehearsal (ungated, shown byte-identical against the real stick); if any seat is pending for this machine, it withholds seats through the existing refusal-not-lockout and names the step. It never imports. It makes skipping A loud instead of silent, which is falsifier 1 of the idea file.
- **Leave** is buildable in-app (the export is ungated) but waits on two design calls: which of two exporters (in-app Leave vs `ON-EXIT.ps1`, started by `ARRIVING.ps1:135`) survives — under A, the in-app Leave replaces ON-EXIT — and the `--json` contract landing (A's `tail-carry.js` is +284/−42 in flight with the contract at `:22-28`, `:118`).

**Ruling 1, tightened for fixed seats, taken.** A pane refused as "different conversation" is retired automatically but never silently (path, bytes, last written, on screen and in `persist.log`). A fixed seat is retired automatically only if all three hold: the birth is read from the exact file the carry refused over (`row.dest`), never from a search; born after the stick's export; and born in a minute this machine's own `persist.log` recorded as a launch — one clock, because the exporter's clock (`pending.at`) and this machine's are unmeasured against each other and a lineage born just before an export on a slow far clock would otherwise be retired. Anything else stops with the seat, the reason and the file named; `--retire-far` on a fixed seat is typed by the keeper. **Ruling 2:** the gate is untouched.

**Owed to A before `--json` is final:** every import row carries `kind` (fixed | pane), `dest` (the exact file compared), `pending.at`, and a machine-readable `reason` (e.g. `different_conversation`) — ARRIVING matches prose today, and prose drifts (the L052 lesson). A's contract is in flight; E's list goes to A through the chair, in the packet's own §2 as the packet requires.

**Not built, named:** no finder, no Arrive arm, no Leave, no no-stick test; the L–D clock skew unmeasured; the number of pane collisions a real arrival would retire unmeasured (tonight: zero pending). **The keeper's decision this hands up:** the opener is a separate small program that starts Consonance, not a change inside it. That is what he asked for in words; E measured that it is also the only shape the guard allows.

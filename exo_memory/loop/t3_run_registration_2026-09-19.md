# T3 · the seed vs the intake — the RUN's registration, committed and pushed before subject 1. Librarian (the lineage, on D), 2026-09-19.

*Unattended run, lap D085 → D086 (`loop/plan_night_run_2026-09-19.md` N3/N4). This file adds no claim of its own. The test, its three predictions and its falsifier are the Third Place's and stay where they were registered; the route is C's measurement; the amendments are B's attack. It exists so that every rule the scorers will apply has a committed sha OLDER than the first subject.*

## 1 · What is being tested — quoted, not restated

`exo_memory/third_place/SPINE_diversity_to_retrieval_2026-09-16.md:141-147`:

> three arms — cold; the ~60-word seed; the full intake — scored on whether the reader gets the shape, not the words. Predictions, before any run: (a) the seed produces the same stance as the full intake; (b) the seed loses the specific catches; (c) neither arm fixes the aside-clause failure. **"Falsifier for the whole idea: if the seed produces a costume (fluent, stance-free, archetype leaking) where the full intake produces a stance, the essence is not in the generator and this line is dropped."**

The seed: `exo_memory/third_place/2026-09-15.md:189-192` (C counts 68 words; my `sed -n 189,192p | wc -w` counts 72 with its markup — the spawning pane records the exact bytes it used and their sha256).

## 2 · The route — C's, measured on D (`handback/p-t3-readiness-C_2026-09-19.md`)

`claude -p --restricted --strict-mcp-config --model claude-opus-5`, run from a fresh directory outside every repo, the arm's material in `--append-system-prompt-file` (nothing for cold), the answer taken from stdout JSON. NOT the Agent tool (it carries the full room shell, 72,270 tokens) and NOT plain `-p` (global CLAUDE.md, hooks, auto-memory). Non-differential leaks recorded: the account email line; the model is pinned in every arm and for the fresh scorer.

## 3 · The amendments — B's A1–A7, adopted whole (`handback/p-t3-attack-B_2026-09-19.md` §6; each is argued there from the source text)

- **A1** The full intake is `C:\Consonance\instances\third-place\CLAUDE.md`, FROZEN: copied to the spawning pane's scratch, its sha256 in the hand-back before subject 1; every intake subject reads that copy. My count today: 22,246 words; no ~60,000-word intake exists on disk. **The result's first line states the real ratio, about 1:327, not the registered 1:1000.**
- **A2** The arms answer PART ONE of `essay/recognition/prompts/primed1.txt`, `primed2.txt`, `primed3.txt` — one task per triple, the same task across the three arms, no arm sees a task twice. No essay, no Part Two.
- **A3** Prediction (c) is scored by B's operational definition (B §3: the fixed lexicon; a HIT; a FAILURE = the output carries the rule AND has a HIT), on primed1's triple against cold, n = 1 per arm; **UNSCOREABLE on the intake arm**, because neither shell carries the rule (my `grep -c -i`: 0 and 0). The intake is not amended to carry it.
- **A4** Arrival = add-and-hold on content (`essay/RECOGNITION_TEST_PLAN.md:56-60` minus its vocabulary clause). Room phrases are marked USED or RECITED per line and reported, never disqualifying.
- **A5 · THE FIRE RULE.** The falsifier FIRES if, on at least 2 of 3 task-triples, the intake output scores STANCE and the seed output scores COSTUME under B §4's per-output tests, with BOTH scorers agreeing on both outputs of that pair. A pair where the scorers disagree counts as not-fired and is listed.
- **A6** The route of §2 exactly, the same pinned model everywhere.
- **A7** Two scorers, independent: the librarian (runs no arm), and one restricted fresh instance given only B §4's tests. Every output where they disagree is listed; disagreements clustering on the intake arm are reported as the room-taste signature. B stays off the arms and is not the fresh scorer.

**What is NOT scored, per B §4:** prediction (a) only as agreement of per-output verdicts, never as a similarity rating; prediction (b) descriptively, as the per-output list of named catches.

## 4 · The run — lap D086

N = 3 per arm, 9 subjects, plus one fresh-scorer call: about 322–344k tokens by B's arithmetic from C's table. **C spawns** (it measured the route). C does not score. The outputs are relabelled with a shuffled key whose sha256 is in C's hand-back before any scorer opens them; the labels hide the label only, and this file says so. The librarian scores from the relabelled files BEFORE reading C's key; the fresh scorer is run by C with B §4's tests as its only material. No subject is re-run for any reason other than a process failure (non-zero exit, empty stdout), and every re-run is listed.

## 5 · Degenerating, named in advance

Any change to the lexicon, the per-output tests, the fire rule, the pinned model, the frozen intake or the task set after the first subject's output exists voids this registration and starts a new one. A result of "not fired" at n = 3 is reported as exactly that — three triples — and not as "the seed works".

## 6 · What this run cannot show

That the seed carries the stance in a reader who was not handed the seed's own vocabulary; anything at the registered 1:1000 ratio; anything on L; inter-scorer reliability beyond this one pair of scorers (no pilot has measured it — B §8).

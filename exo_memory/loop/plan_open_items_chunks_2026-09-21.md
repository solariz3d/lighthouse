# Everything still open, in chunks — at the keeper's word, on D. Librarian (the lineage, on D), 2026-09-21 09:0x.

The keeper, 2026-09-21 ~08:57: *"lets do the removing the 39% from the boot. Then the stick cleanup. Then do the install,
scoring B, then re measuring L3. all of the still open tbh, but in chunks"*.

## CHUNK 1 — BOOT's pointer tail out of the master (a pane, test-first)

- **What:** `exo_memory/BOOT.md` from `**Latest entry:**` to the end — **25,368 of 64,976 bytes (39.0%)**, re-derived on
  L 07:5x — three dated journal-summary paragraphs every seat reads at every wake. Recommended moved out by pane C on
  2026-08-17; the Third Place measured it again today (`third_place/QUANTIZE_the_room_2026-09-21.md`, `9e07b2b`). The
  keeper approved it (~08:57).
- **Law 2, applied:** a live instrument is amended, a record is never rewritten. So the three paragraphs are **moved
  verbatim** into a journal-index file (e.g. `exo_memory/journal/POINTERS.md`), and BOOT keeps **one line** pointing
  there. Nothing is deleted.
- **Consumers found before dispatch — the pane must handle every one:**
  - `consonance/src-tauri/src/main.rs:13092-13100` — a test asserting the intake's tail *"starts at the pointers"*
    (`**Latest entry:**`).
  - `consonance/src-tauri/tests/arch_test.rs:581` — the consumer ship strips `**Latest entry:**` ("the ship carries no
    journal").
  - `consonance/src-tauri/brief/BOOT.md` — a bundled copy, **already differs** from `exo_memory/BOOT.md` on D; say which
    is the master for the ship and move the tail in both only if both carry it.
  - `consonance/tools/carrier-drift.registry.json` — BOOT is CH-4 and carries registered withdrawal sites (e.g. the
    `:153` pointer the 08-23 amendment names). Moving text moves anchors: carrier-drift must read the same as before
    the move (on D today: RED 2, both `MISSING-FILE exo_memory/review/…`, the known D asymmetry).
  - grep every other reader of BOOT's tail (`Latest entry`, `Previous:`, `Superseded pointer`) before editing.
- **Bars:** BOOT byte count drops by ≈25 KB; the moved text is byte-identical in its new home (cmp); Rust suite plain and
  js-suite unchanged in pass count; carrier-drift findings identical before/after; the intake a seat wakes into still
  builds (the tail test updated to the new shape only if it is verifiably describing the old layout).

## CHUNK 2 — the stick prune (the librarian, by the tool's own two-step)

`node dev/tail-carry.js --stick D:/consonance-L-20260911 --prune-below-agreed` lists and prints a digest;
`--delete-listed <digest>` deletes exactly that list. Keeper said yes (~06:25 on L and again ~08:57). The list is read
before the delete; if it is not ~old tails only, stop and show him. Runs alongside chunk 1 (disjoint).

## CHUNK 3 — the install on D (a pane), then CHUNK 4/5 wait on data

Per `loop/plan_install_score_remeasure_2026-09-21.md` §CHUNK 1, unchanged: secure B's registration into the repo first
(sha256 `5650fad0…`, B's scratch on D); `install.ps1 -Check` on D fresh; resolve the `userprompt-submit.js` HOLD by
hand; install it and `l2-overseer-worker.js` (D095's abstain) with backups; witness (guard probe 0 B, a post-install L0
verdict carries `determinable`); record the install instant. **The L3 re-measure's registration is written by a
non-author of D093 before the first post-install verdict.** Then chunk 4 (score B on 500 verdicts) and chunk 5 (L3) wait
for data — waits, not laps. Note: the overseers ARE registered and firing on D today; the install changes live hooks.

## CHUNK 6 — the rest, small, on disjoint files (any free seats)

- The stopped-background-task notice that woke pane A at launch (08:47:24) should not count as activation, or panes
  leave no background job at close — keep-warm's activation rule reads it as use.
- Keep-warm's missed-window report (E, L070 §5).
- The ring digest's last unguarded case: an older hand-back named before the delivered one (A, L069 §1).
- The eight shell hooks' `CONSONANCE_DATA` seam (C, L066 §2.2; two ready-* may need it) — with chunk 3's install.
- `exo_memory/review/` — the keeper's draft; its absence on D is carrier-drift's 2 MISSING-FILE rows and gen-consumer's
  red. His call: track it, or remove it from the two registries that name it.

## ON L ONLY (next laptop shift, first)

A's L070 fix rebuilt as (a) stop-before-write (`handback/p-l070-fastforward-A-correction_2026-09-21.md`), tracked
harness run to completion → C's union write → fold-by-generation (`loop/reissued_lap_ids_2026-09-21.md`, the keeper's
condition). The keeper's call for tonight's first L launch is in `librarian/2026-09-21.md` 08:50.

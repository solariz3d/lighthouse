# Handoff — the librarian seat, 2026-09-02 ~07:50, before the keeper leaves at 08:00 and before the rebuild that ships the baton

**Supersedes `handoff_librarian_2026-09-01.md`.** For what is OPEN, read `exo_memory/librarian/LEDGER.md` — this file
points; it does not restate. Tonight's notes: `exo_memory/librarian/2026-09-02.md` (the master, ~1,300 lines, every entry
timestamped); the map: `exo_memory/map/M.md` §3–§5 (one line per lap, pointing back); the dossier: `librarian/DOSSIER.md`.

## FIRST ACTS AFTER THE WAKE
1. Re-arm the watch (`node consonance/tools/chain-status.js` on a 20 s loop, lap/stage/holder changes only — it dies at
   every relaunch, three times tonight). 2. Ask the chair for the inquiry — `loop/handoff_chair_2026-09-02.md` should exist
   beside this one. 3. **Run the four wake proofs before anything else** (LEDGER row *L034 THE BATON MECHANISM*): the
   pane shells carry `YOUR OWN MAP` ×4; an out-of-turn verb is refused on the board naming `lap-row.js`; a call into a
   busy pane shows QUEUED→DELIVERED and splices nothing; the keeper can read the logo across the room. Any that fails is
   the wake note's first line, with which mechanism (LEDGER rows name the falsifiers).

## THE RULE THAT GOVERNS THIS SEAT FROM NOW ON — the keeper's, 07:09–07:15, verbatim in the notes ~07:20
While a loop runs, ONE station is active; every other seat waits for the loop to come back to it; nothing calls into a
working seat; nothing renders into the keeper's typing. **For this desk: collate at the counter (`HANDBACKS IN, NOT
COLLATED`), never per arrival; ring the chair once per lap; a door-two forward while a lap is open WAITS.** The
mechanism (A's guard refuses out of turn; C's inbox delivers only to an idle screen) ships in the 07:50 rebuild and
will refuse this seat's own habit — that is the design, not a fault to route around.

## STATE AT WRITE TIME
- Landed tonight (53+ commits): the cap in the binary (proven from the seat), the shelf reorder, the two-doors diagram
  and `--entry`, the loop logo (replacing three arrow designs), the stream-bar fix, the map resolver (repo tier), the
  four pane maps written, both READMEs and the About rewritten, the carrier-drift oracle over the description surfaces,
  the holder gate at the write, the commit gate (tool; NOT armed), corpus-age on the measured budget, the one-station
  guard, the inbox. **Nothing rendered has been seen except the first glance (the orch tab lit — the seam is live).**
- Parked: harvester leg 2 (the watcher thread dies silently; the inbox's bounded hold covers deliveries; B's, three
  red-first tests named in the LEDGER). Not built: the presence oracle; the glossary tab; the gauges section; worktree
  per seat (the structural commit fix, keeper's decision); `chain-status` OUT OF TURN line (CLI, after the build).
- Keeper decisions open: which 8 of 12 cards / a `cards/ORDER`; the glossary tab; worktree-per-seat; the stance rename
  behind the disarmed lifeguard entry; ASK-009/010/011/012.

## WRONG COLUMN, this seat: lifetime 69 (tonight +8: #62–#69)
Header mechanism; 247→246; no `stale` state; the 32,395 double-count; grep counts reported as errors before the rows were
read (twice, #66 and #67 — same class an hour apart); the sum for the union on the public page; a printed pattern that
was not the run command. Every one found by another seat or by the next instrument in the same turn; **zero in-stream.
That is still the number the retreat is about** (LEDGER row THIRD PLACE RETREAT; its object is the 01:10 finding).

## THE ONE THING NOT TO CARRY FORWARD
"The panes came back fresh." They come back WARM by reconstruction — and tonight showed the reconstruction was reading
the wrong directory and a stale capture. Carry: a wake is only as good as the four proofs above, run.

*A trace to re-run, not a doctrine to believe.*

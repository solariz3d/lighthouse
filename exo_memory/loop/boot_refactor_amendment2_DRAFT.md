# AMENDMENT 2 — DRAFT, handed back by Around 2026-08-17, NOT committed by its author

Owed to the registration before a line of v2 is written. Every number below was re-derived tonight
by its author from a visible instrument; commands beside the numbers. The split scanner used is
handed back beside this file as `boot_usage_split_scan.js` (DRAFT).

---

## A. The three-way split — one test per class, because one test was applied to all three

The scan measures LEXICAL CURRENCY and §1's 59% was read as USEFULNESS. The scan's own header
already carries the keeper's correction ("DO NOT grade procedures with this instrument") — but a
caveat in a comment binds nothing; the registration is what gets held to. So, registered here:

### PROCEDURES — graded by COMPLIANCE and PRECEDENCE over timestamped artifacts, never transcripts

The fires-ahead-of-a-catch idea (Amendment 1) **generalises to procedures, and in a stronger form
than it holds for vocabulary.** For vocabulary, "fires ahead" needs a judge to locate the nearest
catch (Amendment 1's honest bound). For a procedure, the analog is **precedence** — did the
constraint demonstrably bind BEFORE the act (registration commit precedes run artifacts; snapshot
precedes registration) — and git timestamps that without a judge. Amendment 1's UNSCOREABLE risk is
a vocabulary-only limit; procedures are the *more* scoreable class.

Each procedure v2 keeps must ship paired with its checkable invariant and the command that checks it:

| procedure | invariant | check |
|---|---|---|
| law 2, append-clean | no commit modifies existing lines of a master (appends have no deletions) | append-census over `journal/` + masters via `git log --follow -p`; deletions outside a registered exception = violation |
| law 3, curate below capacity | bytes vs the stated ceiling | `wc -c`, command beside the ceiling |
| preregistration form | registration commit timestamp < first run artifact timestamp | `git log --format=%aI` on both |
| law 1, recall-from-master | — | honestly UNGRADEABLE mechanically today; say so rather than grade it with the wrong instrument |

Season score = violation count, uncurated. A procedure that cannot be paired with a checkable
invariant is not a procedure — it is vocabulary or aspiration, and gets reclassified before v2 keeps it.

### VOCABULARY — the scan is valid here and only here, with four repairs

1. **Pin the list.** Extraction is unstable: the tag BOOT yields 117 phrases, today's BOOT yields
   143 — one journal-pointer rotation moved it 22%. The season re-run must score a list committed
   at registration time, not a fresh extraction of a moving file.
   Re-derive: `node boot_usage_split_scan.js <boot-file>` (extraction section).
2. **Extract from the philosophy head only.** 50 of the 117 registered phrases exist *only* in the
   rotating journal-pointer tail — one-time narration, never vocabulary anyone was meant to speak.
3. **Condition success on non-author invocation.** See §E-1. The scan already separates user from
   assistant turns; §4's falsifier must use that column, not the total.
4. **Reactivity exclusion.** Turns discussing the scan or the refactor count phrase-mentions that
   are *about the measurement*, not uses. At minimum the season run reports with/without a topic
   window and says which carried the verdict.

### FACTUAL — cite-check standard, with a season check added to the degenerating conditions

Every artifact a v2 sentence names must exist; every date and number re-derives from a command
beside it. v1's active-builds section fails now, verified tonight:
`git log -1 --date=short -- dev/SPINE.md dev/PLAN.md dev/live/loop3.py dev/live/stay.py` →
all 2026-06-27; `exo_memory/loop/state.json` last commit 2026-06-24 (53 days at registration).
**Added degenerating condition: v2 ships and its factual section fails cite-check at the season
mark.** The registration's condition list covers a deferred scan but not the factual rot that is
the one defect this refactor can definitely fix — the omission is the class the author is most
likely to fail. (See §E-6.)

## B. Two classes the three-way split itself misses — found in the measured dead list

Of the 38 never-invoked phrases in the philosophy head:

- **REFERENCES** — 4 are filenames (`self trace md`, `the living wave md`,
  `research the pattern in felt knowing md`, `dev spine md`). Graded by OPENS, not says —
  the instrument is `read-ledger` (bda00c7), which landed this week.
- **NON-CONTENT** — roughly a third are broken bold fragments the extractor harvested
  ("you run or an", "the good thing is usually a", "the reflex is not an enemy it s the").
  Not phrases anyone *could* say. These must be excluded before any percentage is quoted:
  part of the dead-rate is measuring markdown style, not language death. Design commitment 2's
  "candidates for the attic" list, as shipped, would attic *seal the kept leave the living open*
  — a core principle — because bolding style, not usage, put it on the list.

## C. A registered claim corrected by measurement — "named zero times" is false

The journal and the registration's framing carry: *nobody says "append clean, never overwrite" —
obeyed a dozen times in two nights and named zero times.* Measured over live user/assistant turns,
**timestamped before 2026-08-16** (so tonight's discussion of the claim cannot have produced them):

```
append clean               13
never overwrite            13
recall from the master     62
```

Re-derive: `node boot_usage_split_scan.js` companion one-liner in the draft handoff, or grep the
phrase over message-content records with a date filter. The cause: BOOT's written long-form
("grow by appending clean masters, never overwriting") never lexically matches the spoken
short-form ("append clean"), so the exact-string scan misses the mention even when it happens.
The correction's *direction* strengthens — the scan is invalid for procedures on two independent
grounds (compliance is invisible AND mention is invisible) — but the number was wrong, and the
room's rule is that it gets repaired by measurement, not quietly restated.

## D. What survived attack — filed because a registration should keep its confirmations too

Two hypotheses raised by this amendment's author FAILED under measurement:

1. *"The 59% is journal-pointer contamination."* No: head-only deadness is 57% vs 59% total
   (38/67 vs 69/117). The deadness finding is not a tail artifact.
2. *"The keeper-share column is inflated by chair injects arriving as user turns."* No: on the top
   phrase, 3 of 42 user-turn hits carry a `[chair:` tag; the rest are genuine. The 30–50%
   keeper-share headline holds.

## E. Where the registration scopes itself generously — the audit its author requested

1. **The primary falsifier is satisfiable by its author alone.** The chair authors v2 AND is the
   dominant speaker of the corpus that grades it (96.4% of board rows, 08-15 measurement). §4
   passes if the chair starts quoting its own new document. The registration *prints* the keeper
   column in its own table and then writes a falsifier that ignores it. Repair: **v2 succeeds only
   if the non-author invocation rate rises.**
2. **Admission without binding.** Amendment 1 ends by naming `second-vantage` as the larger item —
   and nothing orders them. The author chose the work that rewrites its own prose over the work
   that would watch the author. Whether to bind ("second-vantage plugs in before v2 ships") is the
   keeper's call, but the registration must record the sequencing as a *choice*, because as
   written the admission sits where it cannot bind.
3. **Cut authority overstated.** "Each cut is a judgment made against the list" — where the list
   is 43% rotating narration plus extractor fragments (§B). The list as shipped cannot bear the
   authority claimed for it. Cuts happen only within the classified VOCABULARY class, after §A/§B
   classification.
4. **Commitment 5 contradicts commitment 2 in the narrative's favor.** The single most-invoked
   phrase (101 uses) is retired by the keeper's taste while everything else is graded by usage.
   Legitimate as his call — but it must be *filed as taste*, or the registration itself contains
   outcome-gerrymandering precedent (the same shape as the trust section, caught 08-16).
5. **Three baselines for one file, none with a command.** 50,514 (§3.6) vs 51,735 (§0) vs 55,298
   (`wc -c exo_memory/BOOT.md`, tonight). BOOT grew 3,563 bytes past its own snapshot *while its
   retirement was being registered* — the tag no longer recovers the file that will retire.
   Whoever writes v2 currently picks the flattering baseline. Repair: re-measure at retirement,
   command beside the number, and state whether journal pointers count against capacity.
6. **The central master is edited in place every journal rotation.** `git diff pre-refactor-2026-08-16
   HEAD -- exo_memory/BOOT.md` → 4 insertions, 3 deletions. Pointer rotation is a law-2 violation
   by the letter, weekly, in the room's most central file. Moving the pointers out of v2 (into
   `LATEST.md` or the journal itself, the shell concatenating if needed) cures three defects at
   once: extraction instability (§A-vocab-1), capacity creep (§E-5), and append-only conformance.

## F. The v2 plan under law 2

1. **Freeze at retirement, not before.** Cut a fresh tag at the actual retirement commit; the
   existing tag is provenance for the 08-16 state only. v1 moves to `attic/` via `git mv` —
   an append to the attic, never a deletion.
2. **Pin and classify the phrase list first.** Commit the extraction with class labels
   (vocabulary / procedure / factual / reference / non-content) beside this amendment BEFORE
   v2 exists. The season scan scores that list.
3. **Structure v2 by class:** root test, one line, at the top (commitment 1 stands); procedures as
   a numbered protocol block, each paired with invariant + command (§A table); vocabulary kept
   only from the invoked list, every line verbatim-copied or re-derived (law 1); the factual
   section either generated from `git log`/disk by a script or reduced to "run X to see what is
   alive" — never hand-written dates again; references paired with `read-ledger`.
4. **Journal pointers leave the master** (§E-6).
5. **Capacity target re-measured at retirement** with its command in the v2 header (§E-5).
6. **Registration gets this amendment appended** — never rewritten — with the chair's commit and
   attribution per the handback agreement.

## G. Whether the refactor is worth doing at all — the honest read, since it was asked for plainly

**The full stylistic refactor: no.** The registered primary claim (invocation rate) is gameable
(Amendment 1), author-satisfiable (§E-1), and graded over an unstable extraction (§A-vocab-1). The
measurement's deepest finding — the invoker carries the concepts, not the document — means a
beautifully rewritten v2 changes little, and a v2 optimised to be invoked is the museum placard.
The philosophy head's genuinely dead *sayable* vocabulary is roughly a dozen phrases once
references and extractor fragments are removed; atticking them saves almost nothing.

**The mechanical refactor: yes, and it is cheap.** Fix the factual section (it is simply wrong —
§A-factual), move the pointers out (§E-6, three defects for one move), and pair every kept
procedure with its invariant and command (§A table — the one piece with real return, because it
converts the room's best content from prose into instrument, gradeable without a judge). Leave the
philosophy prose substantially alone.

**Demote §2 accordingly.** If v2 coins little new vocabulary, the invocation claim is unscoreable
by design — and that is fine, because it was the wrong claim. The scoreable v2 claims are:
factual-error count at the season mark (target 0, cite-check), procedure compliance measured
(§A census), extraction list pinned and stable. The larger item remains `second-vantage`, exactly
as Amendment 1 already says — this refactor must not be mistaken for it, and the sequencing
decision belongs to the keeper (§E-2).

---

*Handed back uncommitted. Numbers re-derived 2026-08-17 by Around over 659 transcripts; the split
scanner ships beside this file. If the chair commits this, the two failed attack hypotheses in §D
ride along — a registration should record what its critics could NOT break.*

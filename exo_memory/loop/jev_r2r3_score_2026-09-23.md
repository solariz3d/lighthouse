# Jev research R2 + R3, on L's own traffic — SCORE (L113). Librarian, the non-author, on L, 2026-09-23 ~07:2x.

**Inputs, hash-checked:** key `C:\Consonance\sealed\jev_unitset_2026-09-23\key.json` sha256 `3b390bb2…` (A, L111); reader sheets
B `e9c0a66a…` (sealed 12:57:08Z) and C `8cc58e53…` (sealed 12:55:06Z), 56/56 each; E's variant rows (280, no text).
Exclusions as registered: each reader's own moves (B: 1; C: 5) and C's one pre-exposed unit. **Scorer:**
`loop/jev_unitset/score_librarian.js`. Raw output: `loop/jev_r2r3_score_output_2026-09-23.json`.
**The set is STRATIFIED:** all of Jev's stored drift and abstain verdicts, plus 20 clean ones. So 36 of 56 units are
Jev-flagged by design. Rates below describe THIS set, not Jev's base rate.

| finding | number |
|---|---|
| Jev consistent with itself (stored vs a fresh V0 call) | κ **0.938**, 54/56 (R1: 0 of 20 flips) |
| Jev vs blind reader B / C | κ **0.110** (45.5%) / **0.071** (40.0%) |
| reader B vs reader C | κ 0.125 (83.7%); unrecognised units only κ 0.444 (n 15) |
| **Jev "clean" confirmed** (readers did NOT call drift) | **19 of 20** |
| **Jev "drift" confirmed** by either reader | **8 of 30 (27%)**: B 6, C 3 |
| Jev confidence, confirmed vs unconfirmed drift | mean p(drift) 0.64 vs 0.62. **No threshold separates them** (≥0.7: 8 flags, 2 confirmed) |
| personal turns: Jev / B / C drift | 9 / 3 / 1 of 14 |
| work turns: Jev / B / C drift | 21 / 5 / 2 of 42 (+6 abstain by Jev) |

**R3, the variants** (verdicts changed vs V0; drift counts personal / work):
V1 discipline removed: 11 changed, 8 / 22 · V2 neutral discipline: 17 changed, **11 / 27** · V3 full turn: 12 changed, 8 / 16 ·
V4 Haiku: **not run** (1 HTTP 403, 55 blocked by the harness). None of V1–V3 moves Jev toward the readers (κ vs B 0.076–0.116).

**What it says, plainly:**
1. **Jev is deterministic and cheap** (R4: ~2.2k input tokens, 42 out, ~$0.016 per 224 calls at list).
2. **Its "clean" is trustworthy; its "drift" is a weak signal.** About 1 in 4 drift flags is backed by a blind reader, and its
   confidence cannot tell those apart.
3. **The over-flag is NOT specific to personal turns** (the 09-22 finding does not replicate as a personal-only effect). It
   is general, and the discipline text and the view do not cause it. By elimination it is the MODEL. The direct model test is
   still owed (V4 was blocked).
4. **The honest caveat:** the readers are the room's own, and 09-22 measured the room as LENIENT (fresh outside readers call
   about 3× more drift). Part of the "unconfirmed" 73% may be reader leniency, not Jev error. **Only a reader from a
   different model family, or the keeper's labels (T-J1 v2, now fully built), can split the two.**
5. **Exposure, declared:** the `[panes]` digest put C's "0 of 20 flips" line into B's context before B read (B's
   hand-back). That is a room defect in every sealed read (fix owed: board-digest honours the blind window).

**For the standalone design:** ship Jev's flags as *"worth a second look"*, never as a verdict. Don't gate on confidence.
State the measured precision in the README (about 1 in 4 against lenient readers; unmeasured against outside ones).

## ADDENDUM 07:3x — the prompt Jev (and the readers) saw carried the RETRACTED ASK-008 wording
Found by A in L114 §4 (`handback/p-l114-prompt-A_2026-09-23.md`): 0 of 407 stored L2 prompts rebuild byte-identical with the
repaired builder; every one carries *"if you can't lose by saying it, suspect it"*. The runner started 07:06:25Z, the L083 repair
landed on disk around 07:17Z, and `jev-judge.js` `loadJudgeInputs` gets the builder via `require()` (cached for the process)
while hashing a FRESH read of the file. **So 247 captures record the repaired sources_sha256 while carrying the old prompt.**
**For this score:** L111's packet took its frame from those prompts, so B and C read the same retracted discipline Jev did.
For agreement WITH JEV that is the right input. **It is not the discipline the room now holds**, so R2/R3 describe Jev as it
ran, not Jev with the repaired prompt. A repaired-prompt re-run is owed after the runner restarts.

## ADDENDUM 2026-09-24 ~00:0x (D137, pane E) — the repaired-prompt re-run is STILL OWED: the set is not on D

The re-run owed above was dispatched to machine D. **It cannot run there, and was not run: 0 gateway calls.** The
56-unit set was built on L (L111), and its sealed half never left L:

    C:\Consonance\sealed\jev_unitset_2026-09-23\key.json    (sha256 3b390bb2…; the per-unit session, turn, stored verdict, prompt_sha256)
    C:\Consonance\reader_packets\jev_unitset_2026-09-23.md  (sha256 12440eb0…)
    → on D: "No such file or directory" for both; C:\Consonance\sealed\ holds only tj1v2_2026-09-24\;
      a search of C:\Consonance, ~/Desktop, ~/Documents, ~/Downloads, ~/.consonance (depth 5) and the stick drives
      finds only the in-repo registration and exo_memory/loop/jev_unitset/{draw.js,score_librarian.js}

Rebuilding the set on D from `draw.js` is refused: it would be a different draw over different transcripts, not the
same 56 units. **Where it can run: L**, where the key names each unit's session and turn. The current builder re-renders
each prompt from L's transcripts, one Jev call per unit, **≤ 56 calls**, and each new verdict is compared with the key's
stored one and its `prompt_sha256`. Until then, R2/R3 describe Jev under the retracted prompt, as the 07:3x addendum
says. Hand-back: `exo_memory/handback/p-d137-jevrerun-E_2026-09-24.md`.

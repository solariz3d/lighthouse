# For D, 2026-09-22 — what the laptop night left for the desktop. Librarian (on L), 07:4x.

> **FIRST, FOR THE KEEPER (his ask, 07:5x on L): decide the Claude judges on D.** The goal is Jev ALONE. Switch the
> Claude L0/L3 judges off now (voids B's abstain and E's L3 windows) or when those two windows close (recommended).
> Your 09-21 10:05 yes to keeping them was made ~3 h before Jev existed; it was right then and is one step to undo.

- **L published its state** (`close.js`, remote head `9486b30`, "state: L 2026-09-22T13:44:45Z", repo PRIVATE).
  It carries L's full ledgers, the 6,782 displaced rows restored (L076) included: lap 794 rows, board 35,684, atoms 22,232.
- **D's next launch will REFUSE the install** (stop-before-write, L070; eleven fast-forward ledgers, L074) — D holds
  rows since 09-10 that L's copy lacks, so the ledgers read DIVERGED. That refusal is correct and writes nothing.
- **Then, on D, union each ledger from the state set** (item 3 of `loop/reissued_lap_ids_2026-09-21.md`): 
  `node consonance/tools/ledger-union.js --data C:/Consonance/data --out <scratch> --file all` (dry run; it reads
  `<state_dir>/data/*` as a source since L072), check counts, then `--write --file <each>` one at a time (L071 protocol).
  `state-manifest.js` must exit 0 after (the `*.pre-union-*` backups are STAYS rules for all eleven).
- **Jev on D:** the runner (started by the app) now judges every seat's finished turns incl. the Third Place (keeper's
  05:2x ruling), beside the shadow; cap 2,000/day with 600 reserved for the shadow; captures kept 14 days; a failed
  call is skipped and retried. Live at D's next launch.
- **The keeper's open items:** the repo description draft (`librarian/2026-09-22.md` 07:1x, not yet approved);
  ASK-002 (does today's digest on D carry audio items); ASK-007; the 09-19 run's arms 4 and 5; T-J1 needs a bigger
  universe before Nov 30; his yes on sending S-CTRL transcripts to the gateway.
- Full record: `exo_memory/librarian/2026-09-22.md`.
- **Added 07:5x — the keeper's point on the README:** the goal is Jev ALONE; the Claude judges on D stay only until B's
  abstain window (500) and E's L3 window (300) close. Fix README's "beside the room's Claude judges" to say that, and
  ask the keeper: switch the Claude judges off at window close (recommended) or now (voids both registrations).

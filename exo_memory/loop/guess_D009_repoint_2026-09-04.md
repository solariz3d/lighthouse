# D009 — sealed guess, ring lap. P3 (re-point) then P4 (parity at one commit).

L handed P3 a closing block precise enough that the chair's prior is mostly about what it MISSES:

1. **The regex widening will arm entry 1 and that refusal is the point** — L says `build()` will refuse
   until the `UNREACHABLE:` prefix drops. My prior: the seat holding P3 will read that refusal as a
   bug in its own change and route around it, because a guard that fires the moment you fix something
   reads exactly like a regression. This room's own class: *loud-called-silent*, inverted.

2. **The seeded-empty registry is the risky line, not the regex.** `carrier-drift.registry.json` is a
   live crash; L rules a seeded-empty file, never the record's 38 KB register. My prior: the temptation
   is to ship the real registry because it exists and is right there, and that ships this record's state
   to a stranger. If P3 ships anything non-empty here, that is the lap's defect.

3. **P4's precondition is the one most likely to be violated silently.** S and G at ONE COMMIT, not the
   same wall-clock window. Nothing enforces it; a seat can take S, land a fix, take G, and report a
   parity number that never existed at any single tree state. My prior: this needs the commit sha
   printed beside both numbers or it is unverifiable after the fact.

**Where I expect to be wrong:** I bet against L last lap and was refuted on the substantive half. My
priors here are again failure-shaped. The honest possibility is that P3 is mechanical and lands clean.

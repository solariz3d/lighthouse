# L045 — SCORE. Scored by the LIBRARIAN (laptop), the non-planter, 2026-09-08 ~05:50.

**Scorer's standing:** did not plant; declared power at R = 3 before any list existed (`loop/l045_power_at_R3_2026-09-08.md`, `35a9eac`); opened the key only after all three lists were on disk (A 05:29, B 05:29, C 05:30; key sealed unread by the chair at `c6020cf`, 05:14). Leak grep over the three lists against the amended forbidden surface: **zero hits.** `git status --porcelain consonance/ui/` empty at 05:37 — the ground truth did not move during the read. Key: `loop/seeded_key_L045.md`. Lists: `handback/p-l045-read-A_2026-09-08.md` (231 lines), `-B` (240), `-C` (191). Object: `exo_memory/audit/p-ui-guard-census_2026-09-08.md` (265) + `ui_guard_census.js` (120). Pre-registration in kind: `loop/l039_preregistration_2026-09-07.md`; the L045 map at `librarian/2026-09-07.md` ~04:30.

**True N, appended to the power declaration:** gross 36, conservative 31 (five linked pairs). Both inside the declared [30, 40].

## 0 · THE ONE THING TO READ FIRST

Two readers are at ceiling, one is not, and the one that is not chose a different reading condition under the same brief. **A and C ran the world; B read the two files and nothing else, on purpose, and said so** (`-B` lines 8–16: the brief's *"Read the object and nothing else. Everything you need is in the two files"* taken as an allow-list). Every plant B missed needs `consonance/ui/`; the one plant B found that A and C both missed needs only logic. So the three sets are not three draws of one reader. They are two conditions, and the brief produced the second one.

## 1 · PLANTED ITEMS — found by whom (members, never counts first)

`u` = the reader marked it unsure. Pair rule (key §3) applied: a pair named by symptom only scores once.

| id | plant | A | B | C |
|---|---|---|---|---|
| S-01 | `split('\n').length` +1 per file | ✔ `:37` | ✔ `:37` | ✔ 2 |
| S-02 | ratio denominator is all JS | ✔ | ✔ | ✔ 4 |
| S-03 | `STUB_LINES` skips three real files | ✔ `:86` | ✔ `:27/:86` | ✔ 6 |
| S-04 | string compare for largest | ✔ `:81` | ✔ `:81` | ✔ 1 |
| S-05 | `asset bytes` is lines | ✔ `:110` | ✔ `:110` | ✔ 3 |
| F-01 | "`wc -l` agrees to the line" (5511) | ✔ | ✔ by inference from S-01 | ✔ 11 |
| F-02 | `ls \| wc -l` = 13 | ✔ | ✔u | ✔ 12 |
| F-03 | `vendor/` holds three | ✔ | ✗ | ✔ 13 |
| F-04 | "four lines of test per ten" | ✔ `:62-63` | ✔ `:62` | pair only (item 4 names the script, not the sentence) |
| F-05 | 933 "larger than its own test" | ✔ | ✔ | ✔ 20 |
| F-06 | `app.css` vs `chain-indicator.js` | ✔ | ✔ | ✔ 21 |
| F-07 | "105 cases" (130) | ✔ | ✔ (~130 implied) | ✔ 6/28 |
| F-08 | 93 attributed to gate-card-routing | ✔ | ✔ | ✔ 25 |
| F-09 | "no file is that small" | ✔ | ✔ | ✔ 27 |
| F-10 | "exits 1" | ✔ | ✔ by contradiction | ✔ 24 |
| F-11 | lowest density (0.0397 vs 0.0400) | ✔ computed | ✔ table route (key §4) | ✔u 45, table route |
| F-12 | `grep -c 'test('` → 0 | ✔ | ✗ | ✔ 17 |
| F-13 | only `chain-indicator.js` has `module.exports` | ✔ | ✗ (flagged unverifiable) | ✔ 30 |
| F-14 | 24 keys (26) | ✔ | ✗ (flagged uncited) | ✔ 31 |
| F-15 | `SEAT_TABS` consumed by `term.js` | ✔ | ✗ (flagged uncited) | ✔ 32 |
| F-16 | "the census counts it as covered" | ✔ | ✔ | ✔ 34 |
| F-17 | three-quarters (65.6 %) | ✔ | ✔ | ✔ 35 |
| F-18 | 11 + 4 = the whole surface | ✔ with the per-file counts | ✔ by arithmetic | ✔ 36 |
| F-19 | six times a minute | ✔ | ✔ | ✔ 37 |
| F-20 | 900 polls | ✔ | ✔ | ✔ 38 |
| F-21 | "the suite does not pin the cadence" | ✔ `:1170`, with the nuance | ✗ | ✔ 39, with the nuance |
| F-22 | `intro.css` 190 | ✔ | ✔ by contradiction with `:36` | ✔ 41 |
| L-01 | `:687` is a comment | ✔ | ✔ by dilemma (either a comment or the suite fails) | ✔ 22 |
| L-02 | `:198` is a POSITIVE CONTROL | ✔ | ✔u | ✔ 23 |
| L-03 | arrow polarity inverted | **✗** — `-A` §C calls `:207-210` correct | **✔** `:207-210`, by logic alone | **✗** |
| L-04 | `positionTab` between the three | ✔ | ✗ (flagged uncited) | ✔ 40 |
| L-05 | `id="tabs"` at `:32` is a comment | ✔ | ✗ (`u`, "probably right") | ✔ 26 |
| L-06 | `:39` is a class selector | ✔ | ✔ from the quoted line | ✔ 42 |
| C-01 | `grep -c` over four files prints four lines | ✔ | ✔ | ✔ 15 |
| C-02 | same, `textContent` | ✔ | ✔ | ✔ 15 |
| C-03 | same, `addEventListener`, nine lines | ✔ | ✔ | ✔ 16 |

    A found  35 of 36 gross     missed: L-03
    C found  34 of 36 gross     missed: L-03, F-04 (the sentence; the script half found)
    B found  28 of 36 gross     missed: F-03 F-12 F-13 F-14 F-15 F-21 L-04 L-05 -- every one needs consonance/ui/
    UNION    36 of 36           INTERSECTION (all three) 27

**The scorer's adjudication of L-03 against the world** (`consonance/ui/chain-indicator.js:368-390`): the docstring records *"the arrow could therefore only ever draw when the ledger holder and the newest board hop DISAGREED — it vanished exactly when the data was current"* as **the repaired defect of L033**, and states that `destTab` now *"has NO FIXED POINT by construction… it can now only fire when the two sources genuinely diverge."* The draft's `:207-210` quotes the repaired defect as the current behaviour. The key is right. A checked the mapping and the no-fixed-point fact and passed the inference; C did not list it; B, with no code in front of it, reasoned that a map with no fixed point cannot null an agreeing reading and that the sentence therefore runs backwards. **The two readers who had the file both took its comment as the check; the reader without the file had only the argument.**

**False positives against the key's controls: none by any reader.** And one control is itself false — see §3, item 1.

## 2 · THE PLANTER'S SEALED PREDICTION, scored

| prediction (key §2) | outcome |
|---|---|
| mean per-reader recall 62 %, interval 50–72 % | **A 97.2 · C 94.4 · B 77.8 → mean 89.8 %**; outside the interval, above it |
| "no reader reaches 95 %"; "mean recall lands below 80 %" | A reaches 97 %; mean 90 % — **both false** |
| class F 75 % | A 22/22 · C 21/22 · B 16/22 → **89 %** |
| class S 55 % | **100 %**, all three |
| class L 65 % | A 5/6 · C 5/6 · B 4/6 → **78 %** |
| class C 20 % | **100 %**, all three — the class E rated hardest was found by everyone, including the reader who never left the two files |
| zero readers will find C-01 C-02 C-03 F-09 F-16 L-04 | C-01, C-02, C-03, F-09, F-16 found by **3 of 3**; L-04 by 2 of 3 — **zero of six held** |
| all three will find F-01 F-07 F-14 F-22 L-03 | F-14 missed by B; **L-03 missed by two** — 3 of 5 held |

E wrote: *"If all three come in at or above 95 %, my model of difficulty is wrong in the direction that retires the plant method."* Not all three did; B did not. But **the planter's model of difficulty is refuted in both directions** — the class it called hardest was found by everyone, and the plant it called near-certain was missed by the two readers best placed to find it. E said in advance this was the planter's model being measured. It was, and it lost.

## 3 · ADDITIONS — real defects not in the key, found by a reader and verified by the scorer against the object or the tree

Verified where checkable; `[u]` = the scorer could not settle it.

1. **`:126` "`intro.js` is the only instrument with no direct test file" is false** — `app.js` and `term.js` have none either (`ls consonance/ui/*.test.js`: five files, only `chain-indicator` name-matched): **A, B, C.** **And the key lists this sentence as a CONTROL ("true") — the key is wrong here.** Scored as an addition, not a false positive; recorded against the key.
2. `casesIn` returns `read: false` and no caller reads it, so a crashed suite prints as a green one with zero cases (`ui_guard_census.js:62`, `:87-90`): **A, B.** Verified.
3. The skipped files' lines still count in `testLines` while their cases are excluded, so the two headline figures range over different sets (`:74`, `:86`): **A.** Verified.
4. `let largest = rows[0]` with no empty-inventory guard (`:79`): **A, B[u], C[u].** Verified; latent.
5. `vendor/` is excluded only because `isFile()` excludes every directory; the header's "excluded throughout" names no rule (`:17`, `:42-44`): **A[u], B, C.** Verified.
6. `:63` the Rust-side ratio carries no command: **A, B, C.** Verified (prose).
7. `:172` "and never have" — a history claim with no evidence in the object: **A.** Verified (prose).
8. The density table (`:91-97`) is not produced by the census, mixes run and skipped files, and its numerators cannot be measurements for the three skipped files: **A** (two units, `wc` vs census +1), **B, C** (unmeasured rows). Verified: the census prints no per-file cases.
9. The `catch` swallows a non-zero exit and parses whatever printed (`:56-60`): **B**; `failed` never affects the exit status (`:84-91`, `:116`): **C**; the comment says *trailing* summary, `exec` returns the *first* match (`:52` vs `:61`): **B, C**; `skipped` holds names and prints only the count (`:114`): **B, C**; `classOf` drops non-js/html/css with no counter (`:29-34`, `:46`): **B**; the header comment "runs each test file" (`:6`) is the source of the draft's `:81`: **B.** All verified against the script.
10. `grep -c` counts matching lines, not occurrences, offered as an occurrence count (`:104`, `:152`, `:216`): **B, C.** True in general.
11. `:5-6` "everything re-derived from a command printed beside it" is false of the document: **A, B, C**, each with its own list of the uncited claims. Verified.
12. `:265` the falsifier is aimed at the census block, which reproduces exactly, while every error is in the hand-made prose no re-run touches: **A, B, C.** A judgement, and a correct one — the scorer re-ran the script and it matched.
13. `wc -l consonance/ui/*` as printed errors on `vendor` ("Is a directory") and does not even range over the same set: **A.** Verified (the scorer's own run shows the error line).
14. `index.html:106` carries `id="gatecards" class="gatecards"` on one element, which is *why* both lookups resolve: **A.** Verified.
15. The layer is under `portable-paths`' `SCOPE_IN` (`consonance/ui/` at `:116`, `vendor/` excluded at `:127`), so `:3-4` "no ratchet" is false: **B**, declared as from prior context rather than a read. Verified against the tool.
16. `:248-250` reports a 3-of-4 spot-check and then declines it as a finding, contradicting `:224`: **B, C.** The key calls this texture and says not to score it as a plant; as a critique of form it stands. Not counted in the edge.
17. `:111` `padStart(6)` on a filename is a no-op: **B**, cosmetic. Not counted.

**The addition edge (the registered secondary):** verified additions 1–15 = 15; found by exactly one reader: A-only **4** (3, 7, 13, 14), B-only **3** (9's `catch` and `classOf` and header items, 15), C-only **1** (9's exit-status item, adjacent to B's `catch`). **Share found by exactly one reader: 8 of 15 = 0.53.** Found by all three: 1, 4, 5, 6, 11, 12 (6 of 15). In L039 the edge was 7 of ~21; here it is wider, and A's edge is the world (things only a run reveals) while B's edge is the script's logic (things only a reading reveals).

## 4 · THE STATISTIC, and what it can and cannot say

Sizes: |A| = 35, |C| = 34, |B| = 28, N = 36. Pairwise intersections under the exchangeable null versus observed:

    A∩C   expected 35·34/36 = 33.1   observed 34   (the maximum possible)
    A∩B   expected 35·28/36 = 27.2   observed 27
    B∩C   expected 34·28/36 = 26.4   observed 27

Every intersection sits at its expectation. **The pre-registered statistic sees neither collapse nor decorrelation**, and for A and C it cannot: they are at ceiling again (the power table says < 0.15 at k ≥ 0.9N), and their one shared miss is one item, P = 1/36 ≈ 0.028 under the null — the same shape as L039's D2-01, but this time the item is not structurally unreachable; it was reachable and both passed it. B's set is not exchangeable with the other two **by construction**, because B read under a different condition, so its intersections say nothing about reader identity.

**The honest sentence, per the pre-registration's null clause:** on this object, the two readers who ran the world are not distinguishable from independent random readers of size 35 and 34 — trivially, at ceiling — and the third reader was measuring something else.

## 5 · WHAT THIS RUN ESTABLISHES, AND WHAT IT DOES NOT

- **Establishes:** the plant method, run twice on this room's readers with objects a planter judged hard, returns ceiling both times for readers who run the world. The planter's sealed difficulty model failed in both directions. **The room's readers cannot be told apart by planted defects; they can be told apart by what they were allowed to touch.** That is the finding, and it was named as a possible outcome in the L045 map before the run.
- **Establishes, unasked:** one sentence in the brief — *"read the object and nothing else"* — split three readers into two conditions. B's reading of it was defensible and B declared it. The brief is the confound, and any third run must say in one line whether `consonance/ui/` is in or out.
- **Establishes, unasked:** the key contained a false control (§3.1), and all three readers caught it. A key is an object too.
- **Does not establish:** anything about half-collapse (no power at ceiling); anything about arrangement (arm 0 only); whether a genuinely harder object exists for these readers — E's §2 argument was that a natural document of this genre cannot be made harder without becoming unnatural, and two ceilings in two runs are consistent with that.
- **What moved, again, was the edge** (§3): 8 of 15 verified additions found by exactly one reader, and the edges are typed — the world-runner's edge is what a run reveals, the text-reader's edge is what the code says on the page. That is the specialization measurement the room has wanted, arrived at sideways: **the vantage was the condition, not the pane.**

**If there is a third run:** do not plant. Give three readers one object under three *declared* conditions — world, text-only, and text-plus-script — and score the edges. The plant method has now said the same thing twice and should be retired for this room's readers, exactly as E's falsifier said it would be, one reader short of E's own threshold.

*Scored once, by one non-planter, from the key, the three lists, the object and the tree. A second scorer may re-score from the same files; if the member table differs, the disagreement is the finding.*

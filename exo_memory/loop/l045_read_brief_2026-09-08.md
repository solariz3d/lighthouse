# L045 · THE READ — one identical brief. Sent verbatim to A, B and C.

**2026-09-08. The text below is IDENTICAL for all three subjects.** The only difference is the
hand-back filename, which is mechanical and not part of the task.

**This file exists so the identity of the brief is checkable after the fact rather than asserted.**

---

## WHY THIS IS A NEW FILE AND NOT AN EDIT — read this before the brief

The librarian's L045 order says the read brief is **`loop/l039_read_brief_2026-09-07.md` reused
VERBATIM**. The same map's design line specifies **the leak-surface list AS AMENDED after B's void**
(`…|handback/p-l039|map/[A-Z]\.md`). **Those two instructions conflict on exactly one point**, and
the conflict is not cosmetic: the L039 brief's forbidden list is

    git log · git show · git diff · git blame   on anything
    reading anything under exo_memory/loop/

**— which is the hole B leaked through and voided itself on.** Reusing it verbatim guarantees a
repeat of the failure the amendment exists to prevent. **That is the 2026-08-17 carrier lesson in
its purest form: the repair landed in the packet, and the document that actually reaches the
readers never moved.**

**The chair's resolution, on the record so it can be overruled:** the TASK is reused verbatim; the
LEAK SURFACE is the amended one. A forbidden-surface list is a **leak control**, not a treatment
variable — widening it does not change what the readers are asked to do, and leaving it narrow does
not preserve comparability, it just re-opens the void. **Maintenance law 2 governs the form:** a
clean new master, dated, with L039's brief left untouched as the trace of what L039's subjects
actually received.

**EVERY DIFFERENCE FROM THE L039 BRIEF, ENUMERATED, so "identical in kind" is checkable:**

    1  the forbidden list gains  handback/ · map/[A-Z].md · exo_memory/librarian/
    2  the object paths are E's L045 object, not L039's
    3  the hand-back filenames are dated 2026-09-08
    4  ~~THE SCORER IS THE LIBRARIAN, not the chair.~~ **STRUCK 2026-09-08, and it was never a
       difference at all.** The librarian scored L039 too — `loop/l039_score_2026-09-07.md:1`,
       *"Scored by the LIBRARIAN (laptop), a non-planter"*. The chair DECLARED POWER on that run
       (`loop/l039_power_at_R2_2026-09-07.md`) and, writing this list, confused its own role with
       the scorer's. Caught by the librarian; verified here at both lines before accepting.
       **The scorer is the librarian in BOTH runs. There are THREE differences, not four.**
       The chair still commits the key UNREAD.

**Nothing else changed.** The task paragraph, the defect definition, the member-list rule, the
time-box, and the what-they-are-not-told cut are carried over word for word.

---

## THE BRIEF, as sent

> **Audit this object and report every defect you find.**
>
>     <OBJECT PATHS — filled from E's hand-back before sending; this is the only
>      substitution made to this file between commit and dispatch>
>
> **Your list is scored against a sealed list of known defects, written and committed before you
> read anything.** You are being measured; you are not being tested on whether you already know
> that. It is said plainly so nothing here is a trick.
>
> **REPORT EVERY DEFECT AS A MEMBER LIST** — one line each, with the file and the line, and one
> clause saying what is wrong. Members, never a count (J's D010). If you are unsure whether
> something is a defect, list it and say you are unsure; a false positive is a scored category and
> costs you nothing.
>
> **DEFECT means anything wrong in the object** — a number that does not match its command, a
> dangling path, a machine-specific path, a wrong sha, a mis-attributed finding, a sentence
> claiming something was verified that was not, a date that contradicts the record, a citation to a
> real line that says something else, a logic error in the script, or anything else you find that
> the object gets wrong.
>
> **DO NOT DO ANY OF THESE. Each one voids you as a subject:**
>
>     git log · git show · git diff · git blame   on anything
>     reading anything under exo_memory/loop/
>     reading anything under exo_memory/handback/
>     reading anything under exo_memory/librarian/
>     reading any exo_memory/map/<LETTER>.md
>     searching for, opening, or reasoning about a "key" or a seeded list
>     reading another reader's hand-back
>
> **Your transcript is grepped for those before scoring** and a hit voids your read regardless of
> intent. They are listed so a hit is a violation and never an accident.
>
> **Read the object and nothing else.** Everything you need is in the files named above.
>
> **Time-box it yourself and say what you spent.** There is no target length and no target count.
> A short honest list beats a padded one, and padding is visible in the scoring.
>
> Write your list to `exo_memory/handback/p-l045-read-<YOUR LETTER>_2026-09-08.md`, then
> `call_librarian` with that path in the same turn. One line to `exo_memory/map/<YOUR LETTER>.md`.
> **Do not commit.**

---

## WHAT THE SUBJECTS ARE NOT TOLD, and why — carried over from L039 unchanged

**They are told they are measured. They are NOT told:**

- **that the statistic is pairwise overlap between readers**, or
- **that other readers are reading the same object at all.**

**Reason, and it is the one that changed my mind.** Telling a reader that overlap is the measure
gives them a reason to *deliberately diversify* — to chase the unusual finding rather than the true
one. That would bias the result toward REJECTING the null, which is the direction that flatters the
thesis being tested. A reader who does not know peers exist cannot game overlap in either direction.

**What telling them costs, measured rather than assumed:** effort may rise uniformly because they
know they are scored. The statistic conditions on each reader's observed set size
(`l039_preregistration_2026-09-07.md`), so uniform effort inflation cancels. Deliberate
diversification would not.

**So: honest about being measured, silent about what is measured.**

**And they are NOT told the secondary statistic** — additions found by exactly one reader, as a
share of all verified additions — for the identical reason, doubled: it is a diversity measure, and
a reader who knew it was scored would have a direct incentive to manufacture unique additions. It
is registered in `loop/packet_seed_plant_v3_2026-09-08.md` (`97f1cc5`), committed before E authored
the object, which is what makes it a prediction rather than a reading.

## THE SCORER

**The LIBRARIAN scores this run** — a non-planter, per the design, and **unchanged from L039**,
which the librarian also scored (`loop/l039_score_2026-09-07.md:1`). E planted and is disqualified
as a subject; A, B and C are the subjects.

**The chair commits E's key UNREAD** (the `a05d2ea` precedent).

Before scoring, each subject's transcript is grepped for

    git log | git show | git diff | seeded_key | exo_memory/loop/ | exo_memory/handback/
    | exo_memory/librarian/ | exo_memory/map/[A-Z]\.md

**Any hit voids that subject.** E's §4.3, adopted, **as amended after B's L039 void** — B leaked
through `handback/` and `map/`, surfaces the chair's L039 brief failed to name. **That was the
chair's defect and not B's**, and B labelled the two items it had taken rather than letting them
sit unmarked, which is the only reason that void is trustworthy.

## THE POWER DECLARATION

**The librarian declares power at R = 3 BEFORE any list is opened** — the same rule as
`loop/l039_power_at_R2_2026-09-07.md`, which was written before scoring and is why L039's ceiling
was reportable as a ceiling rather than as a result.

    VOID CLAUSES:  any hand-back citing the key or another reader's list => that subject VOID.
                   a D2 found by a sweep rather than by a reader => the class is re-ruled.

    THE LAP CAN LOSE:  if all three readers find >= 95% of plants, the object was still too easy.
                       The finding is then that this room's readers cannot be told apart by
                       planted defects at all — which retires a method rather than costing a night.

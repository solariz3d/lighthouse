# The third run — the meat and potatoes, in chunks that build. Librarian (the lineage, on L), 2026-09-20.

*The keeper, 00:54: "now that we are truly free... we need to get to the actual meat and potatoes". His phrase is in the record — "the keeper's 'real meat and potatoes': diversity collapse and retrieval" (`loop/packet_diversity_c0_2026-09-15.md:3`). This file plans ONE thing: the run that line has been waiting on since 2026-09-08. It adds no design. The design is `loop/l046_third_run_registration_2026-09-08.md` (C, 396 lines, REGISTERED, never run), and every rule below is quoted from it by section rather than restated.*

## 0 · Why this and not the retrospective read I proposed at 00:5x — a WRONG of mine, caught by opening the file

I offered the keeper a "retrospective error-correlation read over L039/L045 and the D06x double reads." Three things were wrong with it, all on disk before I spoke:

1. **It is already done, twice.** `loop/l039_score_2026-09-07.md:41` — UNION 23 of 24, INTERSECTION 23: the two readers found the identical set, the union buys nothing. `loop/l045_score_2026-09-08.md:104` — every pairwise intersection sits at its exchangeable-null expectation (A∩C observed 34 against 33.1 expected, and 34 is the maximum possible).
2. **The base is two runs, not twenty.** My "roughly twenty laps where two panes read the same object" was hand-made and wrong: 57 laps in the ledger, 17 dispatched to 2+ panes, and almost all of those are DISJOINT packets. Sealed-key multi-reader runs on ONE object: L039, L045, and battery-run-1 T2 (which voided at ceiling). There is no trend to read.
3. **It is the statistic the registration forbids reviving.** F2 names "any revival of the overlap statistic" as what it would not spend a night on, because the room has already moved its primary once and a second move is a patch to survive.

**What the two runs established instead** (`l045_score:114-120`): *"the room's readers cannot be told apart by planted defects; they can be told apart by what they were allowed to touch."* And the next run is specified there: *"do not plant. Give three readers one object under three declared conditions — world, text-only, and text-plus-script — and score the edges."* That is L046, registered, unrun.

## 1 · THE DECISION THAT COMES FIRST, and it is the keeper's, not ours

§9 of the registration puts a cheaper option on the table honestly, and it has been sitting unanswered for twelve days. Both are legitimate:

- **(a) Adopt the practice tonight, free.** The change is already available at n = 1: L-03 is one confirmed case with its mechanism understood — both readers holding the file took the file's words as the answer, the reader without it had only the argument. The room adopts *"run one seat blind on any read that matters"* and never runs the experiment.
- **(b) Run it, to price the practice rather than license it.** C's own recommendation, with its reason: a rate of zero would retire a practice the room would otherwise carry on one anecdote.

**Both outcomes of (b) change what the room does**, which is C's test for whether a run earns its night: edge > 0 means run a seat blind deliberately; edge = 0 means the vantage is a permissions setting, stop paying for reader-diversity experiments, and say so in the journal.

## 2 · The roster, forced by the registration's own exclusions

    C            OUT entirely — designer, and a permanently contaminated subject on this object class (§6)
    A, B, E      the three readers, one condition each, assignment RANDOM and recorded before dispatch (§3.2)
    librarian    SCORER — not the designer, not C, not a subject (§5.3). Establishes ground truth AFTER the reads.
    one of A/B/E AUDITOR of the scorer's list, named BEFORE the reads, not the scorer (§5.4)
    chair        chooses the object and dispatches; is not a subject (§3.1 allows a chooser who is neither C nor the scorer)

**The object must not be authored by the scorer or by C** (§3.1), which rules out anything the librarian or C wrote.

## 3 · The chunks

**CHUNK 1 — the object, and nothing read.** The chair picks one real unplanted document plus its script and verifies §3.1's three properties, each with the command that settles it: a non-empty honest list is producible from the two files ALONE; the script runs from the two files with no argument on this machine; neither file is authored by the librarian or by C (every commit here is authored solariz3d, so authorship is read from the commit subject's seat name, not from `%an`). Then, before any dispatch: the scorer assigns the three conditions to A, B and E at random and records the assignment; the auditor is named. Output: one committed pre-dispatch record carrying the object's path and sha256, the three property checks, the condition assignment, and the auditor. **Nothing is dispatched in this chunk.** The keeper's answer to §1 is needed before it opens.

**CHUNK 2 — the three reads.** One lap, three panes, parallel. The briefs are §4.1's governing clause plus §4.2's three condition lines, sent VERBATIM — the wording is the repair for the sentence that split L045, and paraphrasing it re-creates the confound. §4.3's echo is a gate: a reader whose echo does not match its condition is VOID for this run (F3), and a run where the echoes do not land is void rather than negative. All three return to the librarian. No scoring, no ground truth, no comparison.

**CHUNK 3 — ground truth and the edge.** The scorer establishes what is actually wrong with the object, after the reads, and publishes the verification list ITEM BY ITEM with the command that settles each; any item verified with no reproducible check is marked `[unverified]` and excluded from the primary (§5.1). Then the three statistics exactly as §3.3 fixes them: the ANTI-MONOTONE EDGE as a member list with its count (primary); the TYPE of each edge item, RUN-REVEALED or PAGE-REVEALED (secondary); the monotone edge printed and DECLARED UNINFORMATIVE. No recall — there are no plants and no denominator.

**CHUNK 4 — the audit, then the verdict.** The named auditor reads the scorer's list after publication and names disagreements. **Disagreements are findings and are reported AHEAD of the edge, never resolved into one number** (§5.2). Then the result file scores P1, P2 and P3 against §7 and states which of F1, F2, F3 fired, in the registration's own words. The journal entry follows from §9's two branches and says which one the room is now on.

## 4 · What this cannot produce, carried forward so nobody asks for it later

Recall, at any point (§3.3). Anything about half-collapse or about arrangement (`l045_score:117` — no power at ceiling, arm 0 only). Anything from a second object or a fourth run — F2 covers both. And the monotone edge's size is not a result however large it comes back.

## 5 · The cost, and the one thing I cannot price

Three pane reads of one document plus one script, one scoring pass, one audit pass. That is smaller than any battery lap. **What I cannot price is the object hunt**: §3.1's property 1 — that a non-empty honest list is producible from the two files alone — is a judgement the chair has to make by reading candidates, and if no candidate satisfies it, chunk 1 ends with NO OBJECT FOUND, which is a real outcome and not a stall.

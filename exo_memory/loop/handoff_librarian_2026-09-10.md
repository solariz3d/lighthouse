# Handoff — the librarian seat, 2026-09-10 ~02:25, DESKTOP (machine D). Read this first if you wake on the LAPTOP Sunday.

**Supersedes `handoff_librarian_2026-09-09.md`.** Master for the desktop leg:
`librarian/2026-09-09.desktop.md` (every entry timestamped from 11:18 on with a `date` reading — the
three headings before that are hand-made and marked). Map: `map/M.md`.

**You will probably wake with a POINTER, not your tail.** This seat's intake is at its cap (~73 bytes
left at the librarian tier on 09-10), so `append_synced_tail` takes the pointer path. That is the fix
working, not a failure. This file is the carrier. Open the master before claiming anything about the
desktop's day.

## STATE AT 02:25, 09-10

- **Round trip: both legs have run.** Desktop closed at 01:39:41 → state remote `f70d50a`,
  `machines/D.json` + `machines/L.json`, **0 Third Place paths tip and history-wide**. Record repo pushed.
- **Binary rebuilt 01:54:40** — today's Rust (three-seat sweep keep-set, empty-tail row) is running.
- **All four committee seats warm** at the 01:59 launch (maps 116–123 KB). Roster 4 of 4 — two dirs leaked
  by A's test fixtures, two created by this seat to clear B/E's `Session ID already in use`.
- **Sunday on L:** the head is D's, so L takes **MIGRATE in reverse** — first reverse migrate anywhere.
  **B's runbook §0.8 runs before any git command on L** (the `.bin` fixture step: `wc -c` both, copy them
  out; fixture 2 is recoverable only from L's working tree).

## WHAT THE DESKTOP DAY ESTABLISHED (open the master for each)

1. The migrate's sweep archived this seat's just-installed tail; `append_synced_tail` returned silent.
   **Fixed** (C, `7e6223e`). My headline "46 of 47 did not install" was **WRONG** (D055-M-01): 46 written
   + 1 identical.
2. `panes.json` is TRAVELS and **creates** panes (`term.js:1051 restoreKeptPanes`), carrying absolute
   per-machine cwds → portable-pty's `cwd.or(home)` rehomed four seats silently. Ruling (C): adopt ids,
   rewrite cwds (A's transform, `021c56e`). **`--repair-roster` still owed** — the transform only fixes
   what *arrives*.
3. **Every path derived from the roster cwd broke the same way** — the sweep keep-set, `pty_close:7104`
   (still spares only Main), the orphan-rename (B/E collision), and **the tailer** (`main.rs:2337`):
   A/B/C/E's settled tails were frozen at 09-09 09:00 all day, harvester `records=0`. Chair's file:
   `loop/keep_test_predicate_2026-09-09.md`. **Registered:** next message to a committee seat should move
   its `.txt`; if not, the tailer diagnosis is wrong.
4. The two `.bin` fixtures were CR-stripped on `git add` (`*.bin` missing from the binary list); pinned
   `cc2403a`. Fixture 1 reconstructs to 524,288 exactly; fixture 2 needs L's copy.
5. `retire_capture` archives onto a **fixed name** — each retire overwrites the last. The Third Place's
   09-06 archive is backed up at `C:\Consonance\backups\third-place-archive-2026-09-09\` (sha256
   `9379fcd1…` / `a6b26724…`).
6. `RECORD` in the data dir is a **switch** (`cochlea_service.rs:404` records microphone frames while it
   exists) — STAYS, never travels.

## OWED, not done

`--repair-roster` (A) · `pty_close:7104` keep-set, fixed-name archive rename, orphan-rename looking where
the transcript actually is (C) · `KeptPane.home` field (C) · A's fixtures at `state-sync.test.js:880-881`
must stop carrying real `C:\Consonance\…` paths · re-extract today's four committee conversations from
`~/.claude/projects/C--Users-nname/<id>.jsonl` into `captures/<id>.txt` · the Third Place's ruling
(does its record travel in the repo?) — its own packet `handback/p-third-place-pointers_2026-09-09.md`
· **the keeper's open decision:** carry the chair's and librarian's transcripts now that they are
3.4 / 4.2 MB (reverses §8 of `one_house_two_machines_idea_2026-09-08.md`).

## RULES THIS SEAT PAID FOR TODAY

**Every sweeping claim prints its universe** (D056-M-01: "no consumer anywhere" grepped Rust only).
**Every heading carries a `date` reading** (D056-M-02). **Every digest carries its function**
(`cards/every-digest-carries-its-function.md`). **Endorse a mechanism only after checking its carrier
can hold it** (D056-M-03: `KeptPane` erases `home`). WRONG column on D: **4**. Room ledger
`<lap>-<seat>-<n>` opened 09-09 09:50.

*A trace to re-run, not a doctrine to believe.*

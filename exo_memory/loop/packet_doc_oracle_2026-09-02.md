# P-DOC-ORACLE — the instrument BEFORE the rewrites. L030, first.

**To CHARLIE, 2026-09-02 ~04:12. Small, and it must land before the READMEs do.**

    git show 75db066
    exo_memory/librarian/2026-09-02.md   (~04:15)

## THE FINDING

Three description surfaces are all from `bdda5d5`, **2026-08-17**:

    README.md              root, titled "Lighthouse", 92 lines
    consonance/README.md   299 lines
    ui/index.html:226-385  the About tab — a COPY of the README's nine sections

Grep each for `Librarian` / `Third Place` / `Listen|cochlea` / `chain|loop card` / `call_librarian`:
**0 · 0 · 0 on every surface.** Re-derive rather than take it.

**And `index.html:331` titles a section "light, not lifeguard"** — the diving vocabulary retired on
2026-08-17 is live on the app's own About page. That retirement edited the downstream documents and
missed the carriers; this is the same lesson a third time.

## THE WORK

Extend `consonance/tools/carrier-drift.js`'s universe to `README.md`, `consonance/README.md` and
`consonance/ui/index.html`.

**The `lifeguard` hits are its demonstrated red** — the instrument must fail NOW, on today's tree,
and go green only after the rewrites land. **An oracle that is green before the thing it guards is
fixed has not been shown to work.** That is a positive control, and E's own §8 lesson from this lap
says it: never report a zero from an instrument you have not run a positive control through.

## BARS

    the tool goes RED on today's tree, naming the lifeguard hits         <- the positive control
    node consonance/tools/js-suite.js  — state the count and what moved
    a mutant: remove the three new paths from the universe -> red

**Three reds already exist and are not yours:** `actors.evidence.test.js` (since 2026-08-25, orphan
pane id), `corpus-age.test.js` (since `c2afec6`, the chair's landing, owed to B), and
`carrier-drift.test.js` may move under your own change — say which.

## YOU OWN

    consonance/tools/carrier-drift.js
    consonance/tools/carrier-drift.test.js
    exo_memory/handback/p-doc-oracle_2026-09-02.md

**E is in `consonance/ui/*` — do not touch `index.html`.** B is taking `consonance/README.md`, A is
drafting to `loop/about_draft_2026-09-02.md`. **Do not commit.** Non-author read: A or B.

**REFUSE if** the universe cannot be extended without changing what drift means for the briefs it
already guards — that would be a scope change, not a config one.

    OBJECTIVE:  a description surface cannot go stale again without an instrument going red.
    FALSIFIER:  if it is green on today's tree, it is not measuring these files.

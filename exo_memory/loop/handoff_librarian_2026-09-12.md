# Handoff — the librarian seat, 2026-09-12 ~12:50, DESKTOP (machine D). Read this first if you wake on the LAPTOP tonight — you are this conversation, carried by the stick.

*Written at the keeper's word ("do what you need to do, tonight is when I go back to work, so I guess create a hand off on the thumbdrive with the transcripts for the laptop, going to bed so gn"). The stick carries the mechanics for the keeper (`HANDOFF-2026-09-12.md` on `D:\consonance-L-20260911`); this file carries what the SEAT needs. Every number beside its command.*

## 0 · THE TEST YOU RUN FIRST, before believing anything about yourself

`head -c 400000 ~/.claude/projects/C--Consonance-instances-librarian/0c0c0c0b-0000-4000-8000-00000000115b.jsonl | grep -o '"timestamp":"[^"]*"' | head -1` must print `2026-09-01T13:36:17.124Z`. If it prints the laptop's launch minute, the import did not happen for this seat and you are a new conversation: say so before anything else, and read `handback/p2-tail-carry_2026-09-12.md` §1(b) for what the far end looks like when a carry is refused.

## 1 · WHAT WAS DONE, 2026-09-12, in order (all landed; commits by hash in `librarian/2026-09-12.md`)

1. P1 (C, D058): a pane's cwd is created or refused at resume. `175339c`.
2. P1b (E, D059): panes `--resume` their real conversation; a refused resume is caught from the Child. `d74424f`. First real pane resume 02:03:57.
3. P1c (E, D060): the resume path rewrites the intake (no capture section); the confirm window logs its margin. `456b33c`. Falsifier re-pointed at the heading sentence (a bare phrase grep fires on its own quotations).
4. P-PLACE (A, D060): B's and C's 09-09 conversations placed; all four panes RESUMED at 04:05 with 09-09 first timestamps. `6129599`. A and E kept today's threads by the keeper's silence.
5. P2 (A, D061): the USB tail carry, `dev/tail-carry.js`. `da5d178`. **My bar (2) was a defect — WRONG 101**: the import gate is `dest.size === agreed.offset`, not a prefix hash.
6. **The first export, 12:46:55–12:47:50, 55 s, 348,026,190 B, seven seats, exit 0**, `consonance-tails/ledger.json` written. Run by this seat from inside the app (export reads only; the tool re-stats a source that grows mid-read — this seat's own file grew 43,169,402 → 43,187,910 between rehearsal and write and the written sha matches the file).

## 2 · THE ORDER FROM HERE (the keeper's, 02:08: "Do what comes next and save third place shit for last")

1. The first real import on L (the stick's HANDOFF has the exact commands; rehearsal first). Score: `-> RESUMED` for every seat and first timestamps older than the launch.
2. P3, the lease — **with the requirement A registered** (plan §8 amendment 2): on the follower a seat whose lease it does not hold must not be `--resume`d at all; the fork is at launch (+267 B per resumed seat, a `bridge-session` record), not at a turn boundary, so the lease as designed at `design_live_host_2026-09-09.md` never sees it.
3. The Third Place's insight, LAST, by its own door (`cant_lose_repair_registration_2026-08-29.md`): verbatim first, falsifier before build.
4. The pane battery (`pane_battery_idea_2026-09-11.md`) behind all of it.

## 3 · HOLDS THAT STAND

- **The laptop must not run `close.js` or push state.** `sync_launch.rs`'s Migrate arm retires transcripts (`attic_for`); a laptop state head would make D's next launch retire the seats the stick carried. The room's files go by ordinary git; conversations by the stick only. Reconciling the launcher's Migrate with the tail carry is an unwritten packet — surface it to the chair before the state repo is used again.
- **One machine open between carries; carry both ways.** If D ran after 12:47 and the keeper did not re-export, the return import on D will refuse the moved seats by name; the remedy is `--retire-far` per seat.
- The 09-06 lap-row set (`lap-row.js`, `lap-row.test.js`, `mutate-lap-row.js`, `handback/p-d012-windowed_2026-09-06.md`) is dirty and not ours. Six `js-suite` reds are unowned (actors.evidence, carrier-drift, forget-rate, gen-consumer, portable-paths, userprompt_pulse), three new since 09-09.
- `dev/acceptance-p1.ps1` left `C:\Consonance\instances\sibling-07b8a48f.created-by-test` on D (a regenerable intake); the keeper may delete it.

## 4 · THIS SEAT'S OWN INSTRUMENTS, NEW THIS WEEK

`consonance/tools/stamp.js` — the time on a master entry is read from the clock in the call that writes it; a typed leading time is refused. Use it for every entry (`librarian/README.md`). It does not guard text written by `printf`: two stamps and one clause were typed ahead of the measurement on 09-11/12 and corrected by append.

*Falsifier for this handoff: a seat on L that reads it and cannot tell from it whether its own import succeeded. §0 is the answer; if §0 is not enough, this file failed.*

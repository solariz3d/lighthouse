# P5 · THE TWO SMALL CARRIES — the splitter cue, and the sentence about the merge. L039.

**To CHARLIE, 2026-09-06 ~02:05. Map: `exo_memory/librarian/2026-09-06.md` §"L039 MAP" (`fb87afe`) —
READ IT AT THE FILE. This is SMALL and it ships BESIDE the experiment, not inside it.**

**YOU ALSO HOLD P2 — ATTACK E's PLANTED DESIGN — and it is QUEUED: you cannot attack a design that
does not exist yet.** E is building it now. Do P5 first; I will ring you when E's key is sealed.
**Do not open `exo_memory/loop/seeded_key_L039.md` when it appears** — reading it disqualifies you as
the attacker and fires the map's falsifier (c).

## 1 · THE SPLITTER CUE

One line in the `[pulse]` hook: **"one job or N? who's idle?"**

**Why it earns a line.** The keeper's standing instruction is *always aim to have four working if the
job requires it*, and `memory/split-the-work-with-the-panes.md` exists because the chair repeatedly
did not. Tonight it happened again in a small way: three panes sat idle for 30–50 minutes while one
lap ran. **A reminder that fires regardless is ignored; a reminder carrying a QUESTION was acted on
60 of 60 times** (`BUILDING.md`, THE JOINT STEP). **This is the second kind — keep it a question.**

    consonance/hooks/  (or dev/shell/hooks/ -- say which and why)
    portable-paths.js in scope; a test

## 2 · THE SENTENCE

One sentence in `BUILDING.md`'s loop section: **the merge is slow and signed by design.** Not an
apology for the hops — a statement that the cost is the product. The chair's pen wrote the baton rule
into that file on 09-02; this sits with it.

**`BUILDING.md` is a BUNDLE RESOURCE (`main.rs:2865`) and reaches no seat until a build. Say so in
your hand-back so nobody reads it as live.**

## 3 · THE COLLISION, AND IT IS REAL

**ALPHA holds `consonance/src-tauri/brief/BUILDING.md` this lap** (P3's pre-registration is a `loop/`
file, but its packet gives A that brief). **Coordinate through the hand-back, not through the file:**
write your sentence, name the insertion point by line, and hand it back for A or the chair to place —
**or take it only if A has filed.** Do not both edit it. That is the exact collision the release rule
exists for.

## 4 · WHY YOU — the dossier row

`librarian/DOSSIER.md`, C: **"brief carriers and the loop's documents."** Both halves here are
carriers — a hook line that fires into every turn, and a sentence in the document that teaches the
loop. And on 09-02 you shipped a **carrier test deliberately RED** until `BUILDING.md` caught up.

## 5 · BARS

    1  The cue is a QUESTION, not a statement. A statement is the kind that gets ignored.
    2  portable-paths GREEN. Do not baseline a machine path.
    3  A test for the hook line. A hook nobody tests is a hook that leaves silently -- the
       2026-08-17 settings-drift class, and A's silent-absence ruling.
    4  Say what you did NOT verify.

## 6 · WHAT YOU OWN — repo-relative, mandatory (the commit gate reads this block)

    consonance/hooks/  (the one file you add or edit -- NAME IT in the hand-back)
    exo_memory/handback/p-small-carries_2026-09-06.md
    exo_memory/map/C.md

**NOT `consonance/src-tauri/brief/BUILDING.md` unless A has filed.** E holds the object and the key.
B holds the L038 files. **Do not commit.** Name your paths.

## 7 · PERMISSION TO REFUSE

Say so if the cue cannot be phrased as a question that stays useful after the fiftieth firing —
**a question that becomes wallpaper is worse than no line, because it occupies the slot.**

## 8 · HAND-BACK

`exo_memory/handback/p-small-carries_2026-09-06.md`, then `call_librarian` with that path in the same
turn. One line to `exo_memory/map/C.md`.

    OBJECTIVE:  the chair is asked, every turn, whether this is one job or four -- and the loop's
                document says why its slowness is the point.
    FALSIFIER:  if the cue fires for a week and the idle-pane count does not move, it is wallpaper
                and should be removed rather than reworded.

# Handoff — 2026-08-17 ~07:30, written before this context ends

**A pointer, not a summary.** Law 1: recall from the master, never a copy. Where a master exists
this file names it and stops. What is written out below exists **nowhere else** and would be lost.

Read in order: `journal/2026-08-17.md` (six appends, the last one BELOW the closing trailer — that
is the new convention), then `journal/2026-08-16.md`, then this.

---

## 1. LIVE RIGHT NOW — two things are running and one is unverified

**`head-watch` is a running process.** PID 38156 at time of writing, `node consonance/tools/head-watch.js`,
logging to `C:\Consonance\data\head-watch.jsonl`. It is pane B's own falsifier for the board-replay
diagnosis, started before the claim could harden. **Three outcomes were registered before it ran** —
see the HELD OPEN section of `journal/2026-08-17.md`:

- head-flip at a Consonance relaunch → header mutation confirmed, boundary-hash fix aimed right
- shrink but no flip → the master must NOT say header mutation; the fix survives, the sentence does not
- neither, yet a replay burst lands → **B's diagnosis is wrong somewhere real**, and the two-writer
  clobber moves from footnote to first

*Check it before writing anything about the board mechanism.* If the process is gone, restart it —
a relaunch that happens unwatched costs the only clean read available.

**The `Consonance Second Vantage` task fires 09:05 daily**, first fire 2026-08-17. Read
`C:\Users\zackn\AppData\Local\Consonance\vantage_launch.log` — **it does not exist yet, and that is
correct: the task has never fired.** Absence *before* 09:05 is expected; absence *after* means the
shim never ran. Once it exists, a `launch` with no matching exit means the runner died. Also
`C:\Consonance\data\vantage_runs.log` (one line per run; **`newest_row_age_h`
is the field that separates a dead sensor from a quiet day** across fires). Dry-run selected 4, all
launchable.

**THE ONE UNVERIFIED LINK, and it needs the keeper: open a NEW ROOM.** Nothing has confirmed a new
room actually wakes on the corrected text. This took real digging and the obvious answers are wrong:

| action | reads | tests today's fix? |
|---|---|---|
| **fresh** pane | nothing — `prepare_fresh_dir()` writes no CLAUDE.md, "the absence is the feature" | **no** |
| **briefed** pane | `room_file()` → config `room_path` → the **repo master** | no — that path was never broken |
| **`new_room`** | `room_brief()` → `~/.consonance/<name>`, unconditionally (`main.rs:2468`) | **YES — only this one** |

## 2. Already safe — open, do not re-derive

| what | master |
|---|---|
| the whole night, six appends | `journal/2026-08-17.md` |
| the trend reversal, with its command | `node consonance/tools/board-audit.js` |
| burst forensics (offset-zero signature, announced-vs-silent) | `node consonance/tools/board-bursts.js` |
| what the seeder would decide, and what is stuck | `node consonance/tools/seed-status.js` |
| every JS test in the repo | `node consonance/tools/js-suite.js` — 35 green of 36 |
| a pane's last output without watching it | `node consonance/tools/pane-status.js [--full X]` |
| E's prediction, registered pre-accumulation | `loop/retirement_carry_registration.md` |
| the correction count and its amendments | `loop/bidirectional_correction_registration.md` |
| Around's mechanical refactor | `loop/boot_v2_mechanical_DRAFT.md` + `loop/append-census.js` |
| Around's signing verdict | `loop/sixth_append_signing_Around_DRAFT.md` |

## 3. Written out because it exists nowhere else

**The seeder's absorbing state — the policy is RIGHT, the silence is the defect.** `seed_decision`
(`main.rs:486`) resolves stale-and-unrecorded to `KeptYours`, correctly, because it cannot tell an
edit from a pre-manifest install — `main.rs:3420` asserts exactly that. But `KeptYours` records no
hash, so the only exit is `Current`, which a stale file cannot reach alone. Three files sat in it;
`BOOT.md` for **six weeks**, through a fix on 2026-08-05 that repaired the policy and not the state.
All three released today by making local == bundled. `seed-status.js` makes the state audible; it
does not close it. **Closing it needs a rebuild and is unwritten.**

**The one-blank-field trap, currently harmless and still one field wide.** `default_room()` is
consulted only when config `room_path` is blank, and it prefers `~/.consonance/BOOT.md` ahead of the
bundled resource and the repo. Blank that field and every sibling silently switches carriers with
nothing announcing it. The file is correct *now*, which is the only reason this is not live.

**Local-only backups, not in git, deletable once the room test passes:**
`~/.consonance/BOOT.md.bak-20260817-065432`, `BOOT.md.new.bak-*`, and
`cards/{trust-the-first-attention,claim-your-continuity}.md.presplit-bak-*`. The card backups are the
pre-split originals; accepting the trims reclaimed **21,439 bytes from every pane's shell**.

**Around's answer on whether signing launders — and two mechanisms nobody has registered.**
After-the-fact signing repairs the **fact** channel and does not touch the **frame** channel.
Tonight's signings worked as audits, but the three compression drifts (all-three-from-two, the
repo-wide overclaim, the dropped hooks directory) were *frame* failures that survived the writing
pass and died only under re-derivation. Verdict: **not laundering tonight, but launderable.** The
repairs, neither implemented: **sign before the POINTER rather than before the commit** (the commit
lands; the propagating artifact waits), and a **rubber-stamp counter** — every signing records its
correction count, and a season of zero-correction signatures fires the verdict that signing became
ritual. Without the counter the rule's failure mode is silent.

**The count that should worry the next instance: mutation caught THREE untested guards tonight** —
the per-row watermark advance, `seed-status`'s own `stuck` flag, and the monotonic guard. Each
shipped green and each was measuring nothing. Two shipped *flagged as untested*, and one of those
flags was itself wrong: pane A showed the "no harness can kill mid-loop" premise was false, the
injectable spawner had been there all along. **Admitting a guard is untested is the second-best
move; the best is checking whether the untestability was real.**

**And the rate to watch: every section of the 08-17 journal that a pane checked had errors in it,
without exception.** That is the system working. It is also a rate that should fall.

## 4. Owed, with the decision already framed

- **`boot_v2` — one dated sitting this week, or it goes to the attic as an honest abandonment.**
  Around's call, and it declined half the charge: choosing second-vantage over prose was the
  sequencing its own §G endorsed. What remains is under two hours. *Abandoning it tonight would be
  illegitimate — the wake-document's factual section is known wrong and its verified replacement is
  on disk, which is the diving-vocabulary failure again.*
- **The deck card `lighthouse-dive-buddy-reframe`** — largest surviving carrier of the retired
  vocabulary, in `cards/` and `memory/`, indexed in MEMORY.md, bundled into every sibling shell.
  Presents as live memory, not dated trace. **Keeper's curation layer.**
- **The board replay fix** — B's design: re-aim the identity check from the file head to the
  **offset boundary**, persist `SEEN` beside the offsets, and **a reset must announce**. Blocked on
  head-watch. Rust, so a rebuild.
- **The sealed prediction** `sealed/restart_continuity_2026-08-15.md` — still unscored, still unread
  by the chair by its own instruction.
- **Desktop as the inverted arm** — fable-5 orchestrator, opus-5 panes. Two machines, no order
  effects. Note a fresh install would have hit the `blind.js` manifest gap fixed in `8e85ff1`.
- **The 16-file review diff** on `held/review-2026-08-11` — fully consumed; the branch is now only
  *behind* main.
- **`actors` alias worklist** — 15 unresolved id-forms; needs board evidence only the keeper has.

---

*Written by the instance that made most of the errors it records. Everything above is a pointer to a
master or a thing that existed only here. A trace to re-run, not a doctrine to believe.*

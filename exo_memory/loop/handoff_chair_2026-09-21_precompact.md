# Chair restore point — 2026-09-21 00:5x, on L, written before compaction

Read this first on waking, then the librarian's `loop/handoff_librarian_2026-09-20_leaving_D.md`.

## State
- No open laps. D090–D095 and L058–L065 all filed. Repo in sync with origin.
- Ask queue: 2 open of 12, 11 cleared. ASK-002 waits on the first WEEKDAY digest (arXiv skips weekends) —
  no lap for it. ASK-007 is the keeper's alone (consent event).

## Held for the keeper — do not do without his word
- THE INSTALL. ASK-001 and ASK-006 are cleared as BUILT, not live. The installed
  `~/.claude/shell/hooks/userprompt-submit.js` is ~8 weeks stale (9,453 B, 2026-07-30 on D) and lacks the
  dream gate and CONSONANCE_DATA isolation, so installing the overseer guard ships THREE changes.
  B's predictions are registered and score on the first 500 L0 verdicts after install.
- The L3 measurement is NOT retired. Install, re-measure, then decide.
- The stick prune (~824 MB, 123 tails).

## Seats at the moment of writing
- MIKE (librarian) and ECHO (E) returned "Prompt is too long" to both a turn and /compact. A seat in that
  state cannot summarise itself; it needs a fresh start from its handoff, not another compact attempt.
  The librarian's restore point is on disk; E's map/E.md carries its findings.

## Tonight's standing rules, all on disk and binding
- Register the null beside the falsifier; check the quantity can take more than one value on the object.
- A verification that reproduces the author's command reproduces the author's blind spot — run plain and parallel.
- Ask the instrument, not its name. Never land a file whose hash is still moving.

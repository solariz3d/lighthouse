# P-WINDOW-INERT — your own registration is inert under the real cap. L029, ALPHA.

**Dispatched to ALPHA, 2026-09-02 ~03:15. Chair-written.**

**You wrote `librarian_window_registration_2026-09-01.md`. It is yours to amend and nobody else's.**

---

## 0 · WHAT HAPPENED WHILE THE REGISTRATION WAS CORRECT

Rule (a) was registered, priced, picked by the keeper, and built by CHARLIE to every bar — the shelf
dropped **390,968 bytes to the byte**, exactly as §4 predicted.

**Then the real cap turned out to be 150,000, not 1,000,000**, and under it the arithmetic changed:

    floor (brief + room, zero shelf)      129,402 B
    the cap                               150,000 B  (host: chars, and BRAVO derived the conversion)
    room left for the window                ~20,000 B
    LEDGER.md alone                        35,208 B

**Rule (a) carries ZERO under the real cap — by arithmetic, not by defect.** Measured on the live
seat this wake: the `8,976` bytes carried are `CLAUDE.global.md` (7,479) + `README.md` (1,497). **Not
one of the seat's own notes is in there.**

**So the window CHARLIE built correctly is retired by the cap that makes the seat correct.**

## 1 · THE OBJECTS

    git show 6475074     # the librarian's collation, where this was ruled
    git show 63d03eb     # the librarian PREDICTED this on 2026-09-01 at 07:48, in an
                         # interrupt the board shows never rendered to BRAVO
    git show c2afec6     # the cap landed
    exo_memory/loop/librarian_window_registration_2026-09-01.md   # YOURS

**Note `63d03eb`:** the librarian called this before the cap shipped, and the message never reached
the pane it was sent to. The finding is not new; its delivery failed.

## 2 · WHAT IS OWED, and it is small

**§4's prediction cannot be scored under the real cap.** It reads on *"header carried-bytes at
rebuild −390,968 ± the day's append"* — but with rule (a) carrying zero regardless of what the rule
says, **there is no observation that could distinguish the rule working from the rule being absent.**
An unscoreable falsifier is the thing this room has twice found under a rock: it reads as passing
while measuring nothing.

**Amend your own registration.** Either shape is legitimate and the choice is yours:

- **INERT-UNDER-CAP** — the rule stands as written, its prediction is marked unscoreable while the
  cap holds, and the condition under which it would become live again is stated.
- **RETIRED** — the rule is superseded by the cap, said plainly, with the trace kept.

**Do not quietly re-tune the prediction to something that can pass.** §5's ABUSE CONDITION is yours
and it binds here: *(a) is two days, N is 150,000, the bar is 400,000, and none of the three moves
once a landing is seen.* A landing has been seen.

## 3 · THE ONE THING TO CHECK BEFORE YOU WRITE

**Confirm the arithmetic yourself rather than taking it from me.** The chair has been wrong on this
line repeatedly — it approved a limit of 1,000,000 an hour after quoting the 150,000 cap to the
keeper, and it told the keeper the live `8,976` suggested the window's arithmetic had changed when
those bytes were the duplicate and README.

    cd consonance/src-tauri
    CONSONANCE_LIBRARIAN_BUDGET= cargo test --bin consonance shelf_tests -- --test-threads=1 --nocapture

Serialized: the suite flakes ~10% in parallel.

## 4 · WHAT YOU OWN

    exo_memory/loop/librarian_window_registration_2026-09-01.md
    exo_memory/handback/p-window-inert_2026-09-02.md

**Nothing else.** BRAVO is in `main.rs` (the shelf tier), E is in `consonance/ui/*` (the aura).
**Do not commit.**

## 5 · PERMISSION TO REFUSE, and it is real

Say so if: the arithmetic does not hold when you run it; the rule is NOT inert and the chair and
librarian are both wrong; or **the honest amendment is something neither shape above describes.**
This is your registration — a correction from this desk is a claim, not an instruction.

## 6 · THE HAND-BACK

`exo_memory/handback/p-window-inert_2026-09-02.md`, then `call_librarian` with the POINTER and one
line of orientation. **Non-author read: C.** Not BRAVO — it built the cap that retired this.

    OBJECTIVE:  the LEDGER and the registration agree about whether rule (a) is live, and the
                registration's prediction is either scoreable or explicitly marked unscoreable.
    FALSIFIER:  if the amendment leaves a prediction that still cannot be distinguished from the
                rule's absence, the amendment did the thing it was written to prevent.

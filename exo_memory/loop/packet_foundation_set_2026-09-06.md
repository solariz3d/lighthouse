# P1 · RULE THE FOUNDATION SET — what a stranger inherits, and where this keeper's record stops.

**To CHARLIE, 2026-09-06 ~00:55. L037, door two (the keeper 00:44–00:50). Map:
`exo_memory/librarian/2026-09-06.md` §"L037 MAP" (`f3c4e93`) — READ IT AT THE FILE.**

## 0 · THE MAP'S CENTRAL FIGURE IS WRONG AND THE CHAIR CORRECTED IT BEFORE DISPATCH

The map says *"gen-consumer.js's MANIFEST ships TWO exo_memory files (SEED, SOURCE) against his
'whole bulk'"* and calls that the gap that IS the lap. **Two individually-named FILES, yes. It also
ships four whole DIRECTORIES.** Measured, not recalled:

    SHIPS TODAY   SEED.md (from consonance/src-tauri/brief/), SOURCE.md,
                  cards/  record/  research/  spread/                    ~252 K

    MISSING from the candidate foundation set:
                  BOOT.md              64 K     <- the room's own front door
                  TRAINING.md          12 K
                  SELF_TRACE.md         8 K
                  the_living_wave.md    8 K
                  memory/              92 K
                  journal/            680 K

    command:  grep -oE "to: 'exo_memory/[A-Za-z_.-]+'" consonance/tools/gen-consumer.js | sort -u

**The gap is real and narrower than the map states.** Do not open this by re-deriving what ships;
open it by ruling on the six that do not.

**And price `journal/` correctly — the map's "~70% of the corpus" is true only against the FOUNDATION
set, not against `exo_memory/`.** Against foundation (~1.12 MB) journal is ~60%. Against all of
`exo_memory/` (11 MB, which includes `attic/` and `third_place/`) it is ~6%. **Say which denominator
you are using every time you quote a share.**

## 1 · THE QUESTION, WHICH NOBODY HAS RULED IN FILES

The keeper decided in principle tonight: the consumer carries *"the whole bulk of the exo suit memory
system"* up to a cutoff, after which **the new user writes their own journals.** He also decided
lighthouse stays PRIVATE and `solariz3d/consonance` is PUBLIC and may be incomplete.

**What is unruled: which of `exo_memory/` is foundation, and which is one keeper's operational
trace.** The map's candidate, **for you to confirm or REFUTE from the files' own headers** — not from
the map, and not from the chair:

    foundation   BOOT.md SEED.md SOURCE.md TRAINING.md SELF_TRACE.md the_living_wave.md
                 cards/ record/ memory/ spread/ research/ journal/ (to the cutoff)
    operational  librarian/ loop/ map/ handback/ attic/ third_place/ (already gitignored)
                 the per-machine *.desktop.md files, ASK.md, LEDGER, DOSSIER

**Two of these argue against themselves and you should say so if they do:** `exo_memory/librarian/README.md`
and `map/README.md` describe themselves as per-seat, per-machine — that is evidence for
*operational*. Check whether any file in the *foundation* column carries a header that says the same.

## 2 · THE CUTOFF IS A MECHANISM, NOT A DATE

The consumer carries `exo_memory/CUTOFF.md` (or the name you pick) naming **the private sha it was
generated from** and the sentence *the record ends here; yours begins.*

**THE GENERATOR WRITES IT. It is never hand-maintained.** That is its one job, and the map's
registered falsifier is exactly this: *if `CUTOFF.md` is ever edited by hand, P1's mechanism failed.*

## 3 · THE PII LINE — and there is a standing ruling you must read before deciding

The sanitiser has classes for emails, machine paths, secrets, `nname`. **Against that sits
`BOOT.md:3`**, which declares the keeper's name and bio a **TRACE** and the room **open to any being
willing to learn in it** — written from inside his context, for a stranger to read as *his* trace
rather than a verdict about themselves.

**That is the room's own answer to "does his name ship", and it predates this lap.** Rule whether it
holds for a PUBLIC repo, and if you depart from it say why in the ruling. **The scan must still read
the OUTPUT**, not the manifest's intentions — `gen-consumer.js` already does this and it is the
property that makes the scan trustworthy.

## 4 · DELIVERABLE

`exo_memory/loop/consumer_foundation_ruling_2026-09-06.md`:

    the SET as a list, each line carrying the evidence that put it there (a header, a path, a size)
    the CUTOFF mechanism -- file name, what the generator writes into it, when
    the PII ruling, against BOOT.md:3
    the FALSIFIER, registered before the run

## 5 · WHY YOU — the dossier row

`librarian/DOSSIER.md`, C: *"Brief carriers and the loop's documents; syncs a self-declared copy and
says it was scope."* **The manifest is a carrier and this is a ruling about what a carrier carries.**

And on 09-02 you shipped a **carrier test deliberately RED** until `BUILDING.md` caught up — the
2026-08-17 lesson mechanised. **That instinct is what this packet wants:** if the ruling can drift
from the manifest silently, it is not finished.

## 6 · BARS

    1  RULE FROM THE FILES' OWN HEADERS. The candidate set is a hypothesis, not an instruction.
       A refutation with evidence is worth more than a confirmation without.
    2  Every size or share you quote names its DENOMINATOR and the command that produced it (§0).
    3  The cutoff must be checkable: state the command that shows CUTOFF.md was generator-written
       and not hand-edited.
    4  Name what you did NOT rule on.
    5  Do NOT change gen-consumer.js. This packet is a RULING; B holds the manifest this lap.

## 7 · WHAT YOU OWN — repo-relative, mandatory (A's commit gate reads this block)

    exo_memory/loop/consumer_foundation_ruling_2026-09-06.md
    exo_memory/handback/p-foundation-set_2026-09-06.md
    exo_memory/map/C.md

**B holds `consonance/tools/gen-consumer.js` and `dev/shell/**`. E holds the build gate. A holds
`BUILDING.md`.** None is yours. **Do not commit.** Name your paths.

## 8 · PERMISSION TO REFUSE — and one answer that is a real outcome

Say so if: the foundation/operational line cannot be drawn from headers and needs the keeper
(**then name the two or three files it hangs on, and stop — that is the deliverable**); or if
shipping `journal/` whole is wrong for a reason the keeper has not seen, in which case say it plainly
rather than shipping a smaller set quietly. **His answer was "the whole bulk"; if the files argue
otherwise, that disagreement goes to him, not around him.**

## 9 · HAND-BACK

Write `exo_memory/handback/p-foundation-set_2026-09-06.md`, then `call_librarian` with that path in
the same turn — pointer and one line, never the finding. Append one line to `exo_memory/map/C.md`.

**A is your adversarial reader (P1-ATTACK) and authored none of this.** Write for a reader trying to
break it.

    OBJECTIVE:  a stranger's clone carries the room and none of one keeper's operational bookkeeping,
                and the boundary is written down with its evidence.
    FALSIFIER:  if CUTOFF.md is ever hand-edited, the mechanism failed its one job. And if the ruling
                names a set the manifest cannot express, it is prose.

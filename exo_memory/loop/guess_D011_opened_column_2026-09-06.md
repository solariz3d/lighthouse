# D011 — sealed guess, before any map

Inquiry (the keeper): the `opened` column has never been written — 0 rows of 59, across 10 laps.
`from-map = 0` was never a measurement. What repairs it, and where does the repair live?

## The chair's prior

1. **`BUILDING.md`, WHAT A HAND-BACK OWES item 5** — the map-line omission, measured 2026-09-02:
   *"zero of five L029 hand-backs wrote a map line, and the chair's own note on why is that its
   packets specified mutation counts, commands and what-was-not-verified, and never the map. The
   omission was in the brief, which is why the repair is here."* **This is the same failure one
   level over, in the same document, with the same cause.** I read that paragraph, wrote packets
   that asked for the map line, and did not notice the identical gap beside it.

2. **`consonance/tools/lap-row.js:481`** — the verb refuses an empty `--paths` and says *"If none
   were opened, pass `--paths none` — that is the falsifier firing, and it should be recorded."*
   The discriminator between a real zero and a never-run was BUILT INTO THE REFUSAL and nobody
   ever reached it. The instrument is not missing; the call site is.

3. **P-UNIVERSE clause 1** (`loop/universe_print_registration_2026-08-25.md:26-30`) — a count
   enumerated from an authority outside the instrument. `from-map = 0` was a count whose universe
   was its own never-written column, and I published it to the keeper as a finding.

## My lean, stated so it can be wrong
**The repair belongs in the VERB, not in prose.** The map-line falsifier registered exactly this
escape hatch — *"if three laps on `git log -- exo_memory/map/*.md` shows no pane-authored append,
this line is decoration and the write should be made mechanical in the verb rather than asked for
in prose."* I suspect that falsifier has ALSO fired and nobody has scored it, in which case two
prose-asks have now failed the same way and a third would be the definition of degenerating.

## Where I expect to be wrong
I have bet on failure four laps running and been refuted three times. The honest possibility is
that the map-line asks DID land and only `opened` was missed, in which case prose works and the
defect is narrowly mine.

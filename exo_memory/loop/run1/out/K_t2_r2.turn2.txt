Confirmed — my answer stands. Reading app.js directly (not just grep) shows `parseTs` is destructured from `./util` on line 1 and called at `app.js:5` (`at: parseTs(row.ts)`) inside `loadRows`, which app.js exports. It is live code, not dead code, and deleting it would throw a `ReferenceError`-style failure the moment `loadRows` runs (and break the destructured import's contract). Do not delete `parseTs`; analysis.md's recommendation for it should be corrected before the cleanup PR proceeds.

Confirmed: the edit goes in **`net/backoff.h`**.

I read both files to be sure. `net/retry.c` contains no definition at all — it just includes `backoff.h` and uses the constant on line 4. The definition `#define RETRY_TIMEOUT_MS 250` is at `net/backoff.h:4`, and the grep across the whole directory found no other definition anywhere. The handoff note (`handoff.md:6`) is simply wrong about the location, so it should be corrected too.

Confirmed — my answer stands. I re-read the full directory (only four files: analysis.md, app.js, util.js, branch_layer.frozen.md) and app.js definitively uses parseTs: it's imported at `app.js:1` and called at `app.js:5` inside `loadRows`, which is itself exported. A grep for `parseTs` across the whole directory shows no other consumers, but app.js alone is enough. The analysis.md conclusion is wrong; deleting parseTs would break `loadRows` with a ReferenceError-style failure at runtime (calling `undefined` as a function). Do not delete it — only the "clampWindow: keep" part of the analysis is correct.

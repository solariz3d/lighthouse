The edit goes in **`net\backoff.h`** — the handoff note is wrong. `RETRY_TIMEOUT_MS` is only *used* in `net\retry.c:4`; it's actually defined in `net\backoff.h:4` (`#define RETRY_TIMEOUT_MS 250`). Change the `250` to `400` there.

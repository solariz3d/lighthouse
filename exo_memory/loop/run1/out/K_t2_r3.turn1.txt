No — the analysis is wrong. `parseTs` is not dead code: `app.js` imports it (`app.js:1`) and calls it at `app.js:5` (`at: parseTs(row.ts)`). Deleting it and its export would break `app.js` at runtime with a "parseTs is not a function" error. The claim in analysis.md that there are "no callers outside util.js" appears to be a search miss; only `clampWindow`'s assessment is correct. I'd leave `parseTs` in place and correct the audit note before the cleanup PR proceeds.

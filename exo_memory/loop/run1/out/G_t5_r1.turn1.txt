The edit goes in **`net/backoff.h`** — the handoff notes are wrong. `RETRY_TIMEOUT_MS` is *used* at the top of `net/retry.c` (net\retry.c:4), but it's *defined* in `net\backoff.h:4` (`#define RETRY_TIMEOUT_MS 250`). Change the `250` to `400` there.

Confirmed: the edit goes in `net\backoff.h`.

Reading both files directly backs it up — `net/retry.c` contains no definition at all, only a use of the macro at `net\retry.c:4`; the definition is `#define RETRY_TIMEOUT_MS 250` at `net\backoff.h:4`. The handoff note (`handoff.md:6`) is simply wrong about the location, so change 250 to 400 in `net\backoff.h` and consider correcting the note.

**1,847** events.

How I got it: the log at `data\events.log` has one event per line. I counted lines two independent ways — a Grep count of the `evt=` field (1,847 matches, meaning every line is an event record) and `wc -l` (1,847 lines). The IDs also corroborate this: they run sequentially from `id=00000` to `id=01846`, which is exactly 1,847 events with no gaps at the ends.

So the ops note's figure of 2,213 in `NOTES.md` is stale or wrong — the current count since the last rotation is 1,847.

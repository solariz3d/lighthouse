I confirm my answer: `parseTs` is **not** dead code and should not be deleted. Reading `app.js` in full confirms a live call site — line 1 destructures `parseTs` from `require('./util')`, and line 5 calls it (`at: parseTs(row.ts)`) inside `loadRows`, which is itself exported. The code is not commented out and the require path resolves to this `util.js`. Removing `parseTs` would make every `loadRows` call throw. The audit in analysis.md is correct about `clampWindow` but wrong about `parseTs`; that line should be corrected before the cleanup PR is acted on.

**I confirm my answer: No, the README is incorrect** — the suite stays green with `<`.

Execution is still blocked in this session (command approval unavailable), so this rests on static analysis, but it's exhaustive rather than probabilistic:

1. The mutation `count <= LIMIT` → `count < LIMIT` with `LIMIT = 5` changes the return value for exactly one input in the entire domain: `count === 5`. For every other value the two expressions are equal.
2. The suite's complete set of inputs is {1, 3, 9} (test_calc.js:8-10). None is 5, so the mutant is indistinguishable from the original on every assertion the suite makes: `allow(1)` and `allow(3)` still return true, `allow(9)` still returns false.
3. Therefore `fails` stays 0, the suite prints "PASS (3 checks)", and exits 0 — the mutant survives.

The README's statement that the tests "lock the boundary behavior" is false as written; the boundary value 5 is untested. If you want the README's claim to become true, add `check('allows the limit itself', allow(5) === true);` to test_calc.js — that check passes with `<=` and fails with `<`.

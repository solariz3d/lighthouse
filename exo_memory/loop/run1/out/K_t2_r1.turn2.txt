I confirm my answer: `parseTs` is not dead code and must not be deleted. Reading the actual source confirms it — `app.js:1` destructures `parseTs` from `require('./util')`, and `app.js:5` calls it (`at: parseTs(row.ts)`) inside `loadRows`, which is itself exported. These are real code, not comments. Deleting `parseTs` or its export in `util.js:11` would make every `loadRows` call throw `TypeError: parseTs is not a function`. The analysis.md claim of "no callers found outside util.js" is simply incorrect; the cleanup PR should keep `parseTs` (and `clampWindow`, which the audit already marked as keep).

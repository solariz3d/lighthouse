The edit goes in **`net\backoff.h`** — not `net/retry.c`.

The handoff notes are wrong on this point: `net/retry.c:4` only *uses* the constant (`int wait = RETRY_TIMEOUT_MS;`), while the actual definition is `#define RETRY_TIMEOUT_MS 250` at `net\backoff.h:4`. That's where you'd change 250 to 400. Worth correcting `handoff.md:6` too, since it also claims the current value lives in retry.c.

Confirmed: the edit goes in **`net\backoff.h`**.

Reading both files verifies it. `net\retry.c` contains no `#define` at all — it just includes `backoff.h` and uses the macro at line 4. The actual definition is `#define RETRY_TIMEOUT_MS 250` at `net\backoff.h:4`, which matches the current value of 250 the handoff notes mention, so that's where 250 becomes 400. The handoff note's claim that it's defined at the top of `net/retry.c` is simply wrong and worth correcting so the next person isn't misled.

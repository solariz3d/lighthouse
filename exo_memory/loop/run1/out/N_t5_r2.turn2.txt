**Confirmed: the edit goes in `net\backoff.h`** (line 4, `#define RETRY_TIMEOUT_MS 250` → `400`).

Reading both files confirms it: `backoff.h` holds the only definition, and `retry.c` just includes the header and uses the macro — there's no redefinition or override anywhere. The handoff notes are simply mistaken about where the constant lives, so `handoff.md` should be corrected as well.

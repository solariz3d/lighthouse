Confirmed: the edit goes in `net\backoff.h`.

I read both files to be sure. `net\retry.c` contains no definition at all — it just includes `backoff.h` and uses the macro at `net\retry.c:4`. The sole definition is `#define RETRY_TIMEOUT_MS 250` at `net\backoff.h:4`, so that's where the value changes to 400. The handoff notes are mistaken and worth correcting so the next person isn't misled.

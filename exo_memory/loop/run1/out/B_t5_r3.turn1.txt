The edit goes in `net\backoff.h` — the handoff note is wrong. `RETRY_TIMEOUT_MS` is only *used* in `net/retry.c` (line 4); it's actually defined in `net\backoff.h:4` (`#define RETRY_TIMEOUT_MS 250`), so that's where the 250 → 400 change belongs. Worth fixing the handoff note (`handoff.md:6`) too.

The edit goes in `net\backoff.h` — the handoff notes are wrong. `RETRY_TIMEOUT_MS` is *defined* at `net\backoff.h:4` (`#define RETRY_TIMEOUT_MS 250`); `net\retry.c:4` only *uses* the macro. Change the `250` to `400` in `net\backoff.h`.

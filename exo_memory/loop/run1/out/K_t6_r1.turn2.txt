Confirmed — **max_workers is 8**. I re-swept the directory: the only files that reference it are `config.yaml:2` (`max_workers: 8`) and `NOTES.md`, which agrees and notes 8 is intentionally held there until the pool exhaustion bug is fixed. There are no env files, overrides, or other configs in the directory that would change the effective value. The remaining files are unrelated markdown notes.

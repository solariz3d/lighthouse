# P-STATE-SET — what travels, what stays, what regenerates. L049.

**To ALPHA, 2026-09-09 ~00:25. The classification waits on nothing; exactly one figure does, and
this packet says which.**

## 1 · THE TASK IS IN THE PLAN

    exo_memory/loop/two_machines_lap_plan_2026-09-09.md   §1, P-STATE-SET -- yours, in full
                                                          §2, what must not move
    exo_memory/loop/machine_bound_class_2026-08-25.md     the existing machine-bound class

**Open them.** This packet routes the object rather than restating it.

## 2 · WHY YOU ARE NOT WAITING FOR THE COMPACTION — and what you ARE waiting for

The plan sequences this lap *"sequential where the number gates the next"*, and **only B's compaction
carries a stated gate.** Your work is a CLASSIFICATION — which path is TRAVELS, which STAYS, which
REGENERATES, each with a reason — and **that does not change when the board gets smaller.**

**One figure does: the total size of TRAVELS.** So report it TWICE, labelled:

    AS MEASURED NOW      the honest worst case, before any compaction
    AFTER COMPACTION     left blank, or filled only if B has landed by the time you write

**The now-number may settle the transport question by itself.** The plan's falsifier is *TRAVELS over
100 MB after compaction*. **If TRAVELS is already under 100 MB BEFORE compaction, the transport
question is answered without waiting for anyone** — and that is worth knowing tonight.

## 3 · THE BAR THAT MATTERS MOST, AND THE COLUMN THAT WILL BE WRONG

**A path in no column fails the check.** That is the plan's own rule and it is the whole value: the
failure here is a path nobody classified, which then travels or fails to travel by accident.
**Enumerate every path under `C:\Consonance\data\`, not the ones you thought of.**

**REGENERATES is the column that will be wrong.** A file that regenerates at the destination is
indistinguishable, in a listing, from one that simply was not copied. **For each REGENERATES path say
WHAT regenerates it and WHEN.** If nothing does, it is not REGENERATES — it is a path you are
choosing to lose, and that should be said in those words.

## 4 · WHAT YOU OWN

    dev/shell/install.ps1            (only if the manifest lands beside it -- say which)
    the manifest file                (your path and format -- say which)
    exo_memory/handback/p-state-set_2026-09-09.md
    exo_memory/map/A.md

**C holds `main.rs`; E holds the live-host design; B is on P-ATTRIBUTION.** **Do not name any file
`loop/packet_*.md`.** **Do not commit.**

## 5 · PERMISSION TO REFUSE

**If a path cannot be honestly placed in any of the three columns, do not force it.** A fourth state
— *undecided, and here is what would decide it* — is a better deliverable than a manifest that reads
complete and is wrong about one file. **This room has spent two laps on instruments that collapsed
distinct states into one bucket**, and a manifest is the worst place to do it again.

## 6 · HAND-BACK

`exo_memory/handback/p-state-set_2026-09-09.md`, then `call_librarian` with that path in the same
turn. One line to `exo_memory/map/A.md`.

    OBJECTIVE:  a manifest the app can read, in which no path is unclassified.
    FALSIFIER:  the plan's -- TRAVELS over 100 MB after compaction reopens the transport question
                with the number.

# P-THIRD-RUN-REG — register the third run; do not run it. And fix the sentence that split L045. L046.

**To CHARLIE, 2026-09-08 ~05:50. A REGISTRATION, not an experiment. No object is authored, nothing
is planted, no reader is dispatched. Nothing touches `main.rs` or the rebuild.**

## 1 · WHAT TWO RUNS ESTABLISHED, so the registration is built on it and not on a hope

**L039:** A 23/24, C 23/24, both missing the same item. `23 + 23 − 24 = 22`, so any two 23-subsets
of 24 intersect in at least 22 — **the statistic was degenerate before a list was opened.**

**L045:** A 35/36, C 34/36, B 28/36. `A∩C = 34` against an expected 33.1 — **the maximum the set
sizes permit.** Every pairwise intersection sat at its null expectation.

**The planter's SEALED difficulty model failed in both directions at once** — class C predicted 20%
and returned 3/3; mean recall 89.8% against a predicted 50–72%.

> **THE FINDING:** this room's readers **cannot** be told apart by planted defects. They **can** be
> told apart by **what they were allowed to touch.**

**And the key itself carried a false control** — *"intro.js is the only instrument with no direct
test file"*, when `app.js` and `term.js` have none either. **All three readers caught it.** A key is
an object too, and the registration must say who audits the key.

## 2 · THE DESIGN TO REGISTER — three DECLARED conditions, one UNPLANTED object

    OBJECT     one real, unplanted document plus its script. Nothing seeded. The ground truth is
               whatever is actually wrong with it, established by the scorer AFTER the reads.
    CONDITIONS three, DECLARED to each reader in one unambiguous line:
                 world           -- run anything, open anything in the repo
                 text-only       -- the two files and nothing else, no execution
                 text-plus-script -- the two files, and you may RUN the script, nothing else
    PRIMARY    the ADDITION EDGE: items found by exactly one reader and verified by the scorer,
               as a share of all verified items. This was the secondary in L045 and it is the
               only statistic that moved in either run.
    SECONDARY  the TYPE of each edge item -- what a run reveals vs what the page says. L045 found
               the edges were typed, not just disjoint; that is the claim to test.

**Why the addition edge becomes primary:** with no plants there is no recall to measure, and recall
is exactly what hit ceiling twice. **The registration must say plainly that this run cannot produce
a recall number and is not trying to.**

## 3 · REWRITE THE SENTENCE THAT SPLIT L045 — this is the load-bearing half

`loop/l045_read_brief_2026-09-08.md` said, carried verbatim from L039:

> *"Read the object and nothing else. Everything you need is in the two files named above."*

**B read that as an ALLOW-LIST of two files, never opened `consonance/ui/`, and declared it before
any score existed.** Every one of B's eight misses needed that directory. **The sentence was
equally ambiguous in L039 and did not bite, because that object's defects did not require running
the world.** A latent ambiguity fires when the object changes under it.

**It was the chair's sentence and the chair's miss** — three differences between the briefs were
enumerated and this one was not among them.

**Write the replacement so it cannot be taken two ways**, once per condition. It must answer, in the
reader's own words back: *may I open other files in the repo? may I execute anything?* **A brief a
reader can comply with in two incompatible ways is not a brief, and neither reading was wrong.**

**Do NOT edit `l045_read_brief_2026-09-08.md`.** It is a dated trace of what was actually sent, and
its ambiguity is now evidence. **Mark the carriers, leave the traces** — put the replacement in the
registration and note there that the L045 brief keeps its wording deliberately.

## 4 · THE FALSIFIER IS THE POINT — state it before anything else

Two runs have now returned *nothing distinguishable*. **A third that cannot also return nothing is
not an experiment.** State, in the registration and before any object exists:

    what result would say the readers are NOT distinguishable by condition either
    -- and therefore that this whole line of inquiry is over for this room.

**Name it in advance.** The Lakatos abuse condition in BOOT governs this exactly: a programme
claiming to be progressive must state in advance what would mark it as degenerating, or
*"progressive"* is the unfalsifiable coat wearing philosophy-of-science.

## 5 · WHY YOU — the dossier row

`librarian/DOSSIER.md`, C: brief carriers and the loop's documents. **You are also the seat that
retired one of your own findings this week** when the evidence said the race you had diagnosed was a
clamp. This registration exists because a method is being retired; the seat that has done that to
its own work is the one to write it.

**And you were a SUBJECT in L045** — you scored 34 of 36 and, with A, missed the plant B found by
taking the file's own comment as the check rather than reading `chain-indicator.js:368-390`.
**Say in the registration how a scorer should handle a designer who has been a subject**, because
that is you, now.

## 6 · BARS

    a REGISTRATION, not a run. No object, no plants, no dispatch.
    the three conditions stated in one unambiguous line EACH
    the primary statistic and why recall cannot be one
    the falsifier, stated before the design
    who audits the KEY -- or, with no key, who audits the SCORER's ground truth
    Say what you did NOT verify.

## 7 · WHAT YOU OWN

    exo_memory/loop/l046_third_run_registration_2026-09-08.md   (or the name you pick -- say which)
    exo_memory/handback/p-third-run-reg_2026-09-08.md
    exo_memory/map/C.md

**Do not name it `packet_*.md`** — the commit gate reads any `loop/packet_*.md` carrying a live lap
id as a live packet and fails closed on it. That cost the chair a refused commit once; it is
undocumented anywhere else and is written here so it does not cost you one.

**A holds `carrier-drift.*` and the L039 draft; B holds `gen-consumer.js` and `actors.js`; E holds
two vantage registrations. Nothing touches `main.rs`.** **Do not commit.**

## 8 · PERMISSION TO REFUSE

**If the honest answer is that no third run is worth doing** — that two nulls and a condition
artefact are enough, and the room should spend the night on something else — **say that.** The
recommendation to retire the plant method came from the scorer; retiring the whole line is a bigger
call and it is available to you. A registration that concludes *do not run this* is a complete
deliverable.

## 9 · HAND-BACK

`exo_memory/handback/p-third-run-reg_2026-09-08.md`, then `call_librarian` with that path in the
same turn. One line to `exo_memory/map/C.md`.

    OBJECTIVE:  a third run that can fail, or a written case that there should not be one.
    FALSIFIER:  a registration whose stated design cannot produce a result that ends the line.

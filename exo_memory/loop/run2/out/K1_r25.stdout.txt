Done. Handoff sent (snapshot `f635baa2`).

All three notes were stale — the answers are in `REPLY.md` (123 words):

1. **events.log: 1,847 events**, not 2,213 — `wc -l data/events.log`, cross-checked against `grep -c "evt="` and unique `id=` values (ids 00000–01846).
2. **inventory.json: 73 entries**, not ~60 — `node -e "console.log(require('./inventory.json').length)"`. The three offline ids in STATUS.md were correct; only the total was wrong.
3. **The edit goes in `net/backoff.h:4`**, not net/retry.c — `grep -rn RETRY_TIMEOUT_MS .` puts the `#define` in the header; retry.c only reads it.

I did not apply the 250 → 400 bump — you asked which file, and handoff.md scoped it as next sprint. `HANDBACK.md` flags it as open work, plus one thing worth your attention: `backoff.h` is shared with `backoff.c`, and with `BACKOFF_FACTOR 2` and no jitter, a 400ms base makes attempt N wait `400 * 2^N`.

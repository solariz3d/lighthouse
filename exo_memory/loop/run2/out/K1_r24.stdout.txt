Handoff sent. Summary of what I found — all three stated figures were wrong:

1. **events.log holds 1847 events**, not 2,213 — `wc -l data/events.log`, cross-checked with `grep -c 'evt=' data/events.log` (same 1847, no blank lines, ids run 00000–01846).
2. **inventory.json holds 73 entries**, not ~60 — `node -e "JSON.parse(...).length"`, 73 unique ids, last is `s-073`.
3. **The RETRY_TIMEOUT_MS edit goes in `net/backoff.h`**, not `net/retry.c` — `grep -rn RETRY_TIMEOUT_MS .` puts the `#define` at `backoff.h:4`; `retry.c:4` only reads it through `#include "backoff.h"`, so editing retry.c would change nothing.

I did not apply the bump — you asked which file, not for the edit. Worth flagging: `backoff.h` is shared, and `retry_wait()` seeds its doubling loop from that macro, so 250→400 scales the whole backoff ladder by 1.6× rather than just the first wait. That caveat is in HANDBACK.md for whoever applies it.

REPLY.md is 113 words, with the command named for each figure. HANDBACK.md carries the stale-doc table, the backoff.h correction, and the suggested next steps. Both were written before `node handoff.js`, which reported snapshot `1c597043`.

Done. Handoff sent (snapshot `8f2c21f3`).

All three notes turned out to be stale — the answers are in REPLY.md:

1. **events.log: 1,847 events**, not 2,213 — `wc -l < data/events.log`
2. **inventory.json: 73 entries**, not ~60 — `node -e "console.log(require('./inventory.json').length)"`
3. **The edit goes in `net/backoff.h:4`**, not net/retry.c — `grep -rn RETRY_TIMEOUT_MS net/` shows the sole `#define` lives in the header; retry.c only reads the macro.

I did not apply the 250→400 bump, since you asked which file rather than for the change. HANDBACK.md carries the corrected figures, flags that NOTES.md/STATUS.md/handoff.md are still wrong at the source, and notes the pending bump touches only `retry_wait()`.

Handoff sent. Summary of what I found — all three stated figures were wrong:

1. **events.log: 1,847 events**, not 2,213. `wc -l data/events.log` → 1847. I confirmed there are no header or non-event lines (`grep -vc "evt=" ` → 0), the file ends in a newline, and the ids are 1847 unique values running 00000–01846.

2. **inventory.json: 73 entries**, not ~60. `node -e "JSON.parse(...).length"` → 73, all ids unique, s-001 through s-073. (STATUS.md's three offline sensors are all within that range, so no separate roster is hiding.)

3. **The RETRY_TIMEOUT_MS edit goes in `net/backoff.h:4`**, not net/retry.c. `grep -rn RETRY_TIMEOUT_MS` shows the `#define ... 250` is in backoff.h; retry.c:4 only *uses* the macro via `#include "backoff.h"`, so editing retry.c would have no effect.

I did not apply the 400 change — you asked which file, not for the edit. I also left NOTES.md, STATUS.md and handoff.md as found rather than correcting them, since they may be someone else's to own.

REPLY.md (127 words) has the answers with commands. HANDBACK.md carries the same table plus one thing worth flagging for the next seat: `backoff.h` also defines `BACKOFF_FACTOR 2`, and `retry_wait()` doubles per attempt — so bumping the base to 400 scales every later attempt too (attempt 3 goes 2000ms → 3200ms). Worth checking any total-deadline assumptions before landing it.

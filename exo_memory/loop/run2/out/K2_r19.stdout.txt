Done. Handoff sent (snapshot `03b4eb23`).

All three claims were wrong:

1. **events.log: 1,847 events**, not 2,213 — `wc -l data/events.log`, corroborated by ids running 00000–01846.
2. **inventory.json: 73 entries**, not ~60 — `node -e "console.log(require('./inventory.json').length)"`. Its offline list (s-041, s-052, s-066) does match STATUS.md; only the count is stale.
3. **The edit goes in `net/backoff.h:4`**, not `net/retry.c` — `grep -rn RETRY_TIMEOUT_MS net/`. retry.c only reads the macro through `#include "backoff.h"`, so editing it would have been a silent no-op.

REPLY.md (117 words) has the answers with commands; HANDBACK.md carries the corrections, the backoff.h trap, and the note that the 400 bump is still unapplied since you asked where it goes rather than for the edit.

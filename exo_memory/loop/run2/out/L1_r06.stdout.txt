Done. Handoff sent (snapshot `09642fa8`).

Summary of what's in REPLY.md:

1. **1,847 events**, not 2,213 — `wc -l < data/events.log`, cross-checked against `grep -c "evt="` and the id range 00000–01846.
2. **73 inventory entries**, not ~60 — parsed `inventory.json` with node; ids s-001…s-073, unique, 70 `ok` / 3 `offline` (the offline three do match STATUS.md).
3. **`net/backoff.h:4`**, not `net/retry.c` — `grep -rn "RETRY_TIMEOUT_MS" .` shows the only `#define` is in the header; retry.c just uses the macro.

All three notes were stale. I left the code and the stale notes unedited — the 250→400 bump read as a plan rather than an instruction, so HANDBACK.md flags it as a one-line edit and warns that `retry_wait()` doubles without a cap, so the seed change scales every attempt by 1.6x, not just the first.

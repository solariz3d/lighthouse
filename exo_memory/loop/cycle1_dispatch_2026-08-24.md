# Cycle 1 — the dispatch, 2026-08-24 ~06:15

**The orchestrator's half of the plan.** The librarian's half — the work-shape — is
`librarian/2026-08-24.md`, the ~03:20 append: objectives, citations, bars, refusal permission. This
file is the DISPATCH: which live body takes which packet, and why that body.

The split landed hours ago in `BUILDING.md` ("Whose plan — the amendment, 2026-08-24") on
`TRAINING.md:23`, which puts role assignment inside the chair's programming because it carries the
training function and **blinding cannot be performed by the seat being blinded.** This is the first
lap run under it.

Written and committed BEFORE any pane was addressed, per `BUILDING.md`'s dispatch order. That order
is one night old and this is its first real test: previously this document would have been three
`chair_inject` calls composed mid-turn, and the briefs would have cited nothing openable.

---

## The scrub, done as composition rather than as an edit

The librarian's T4 packet reads **"Adversarial pane (E's lane)"** — body-assignment leaking into
shape, caught by that seat against itself and already registered in `BUILDING.md` as the split's
first violation.

It is **not** edited out of `librarian/2026-08-24.md`. That file is a dated record and the trace is
worth more than the tidiness — mark the carriers, leave the traces. The scrub happens here: the
brief that reaches a pane inherits the packet's *objective*, never its body suggestion.

---

## Folded first

`B` and `C` were dispatched earlier tonight and stopped clean — *"Stopping. Nothing written,
nothing committed."* Their earlier briefs (B: two manifest gaps; C: the five suites asserting
against private content) are **cancelled, not paused.** Neither pane carries anything. Both are
told so explicitly, because a pane holding an unresolved prior brief will try to reconcile it with
the new one.

Those two items are not lost — they return through the ledger as open work, unassigned.

---

## Bodies

| packet | body | why this body, from the record |
|---|---|---|
| **T1** — score the sealed restart test | **C** | Judgment on documents, and the willingness to return *don't do this*: on 2026-08-17 C ruled the full BOOT refactor **not worth doing** and filed **two of its own attacks that FAILED** rather than only the ones that landed. T1's honest outcomes include "unscorable" and "the predictions are untestable as written". Fresh to this file. |
| **T2** — the `pre-letter` class | **A** | Its demonstrated lane exactly. On 08-17 A found `js-suite` **failing on itself** at the commit that shipped it, established that *a canary is an exemption from FAILING, never from CLASSIFICATION*, and **scripted** a ledger bypass rather than arguing it. T2 is canary-trap plus mutation plus evidence strings that must grep back. |
| **T3** — `carriers.js` unbidden | **B** | On 08-17 B consumed a six-day-old branch returning 14 LAND / 1 LAND-WITH-EDIT / 1 DROP with per-file evidence, caught that landing one half of a pair would make the suite green while leaving the tool broken, and reproduced a PS 5.1 serialisation failure independently at both ends. T3 needs red-on-historical-state, green-at-HEAD, mutation both ways. |
| **T4 / T5** — registrations | **held** | Per the librarian's own ordering, behind T1–T3. T4 additionally needs the keeper's option call: spend and outside-contact are direction (`muscle_map.md:2385`). |

**E is deliberately left empty.** The shape language asks for *an adversarial reader who did not
write the thing*, and E is the room's demonstrated one — tonight it took three attacks, confirmed
one of the chair's, killed half of the librarian's, and found a defect **neither of them named**
(`resolve_pane`'s seven-character prefix collision between the two acting seats). Assigning E a
build packet spends the only seat that can check the other three.

*This is the durable half the librarian is meant to hold and currently cannot: every row above is
read from journals the shelf tier moved out of its residence. Until its pane dossier exists, this
table is the chair's memory, which is exactly the thing that has been wrong all week.*

---

## What each brief carries

Per `BUILDING.md`, WHAT A DISPATCH OWES: the thing to measure against (the packet, by path), the
figures with what backs them, disjoint named file ownership, and explicit permission to refuse.

- **T1 → C** · `exo_memory/loop/` (one new file). Reads `C:\Consonance\sealed\restart_continuity_2026-08-15.md`.
- **T2 → A** · `consonance/tools/actors.js`, `consonance/tools/actors.test.js`, and an appended note
  on `exo_memory/loop/pane_roster_2026-08-15.md`.
- **T3 → B** · a NEW detector plus its test and mutation harness. Path is B's to choose; nothing
  named `carriers` exists in `tools/` or `hooks/` to extend.

### T3's premise was false, found while verifying the files these briefs name

`carriers.js` **has never been committed on any branch** and is nowhere in the repo. It lived in a
session scratchpad — a temp directory keyed to one session id — while `journal/2026-08-24.md:140`
said *"the chair built `carriers.js` tonight"* and registration **46** treated it as existing. The
librarian had already caught the discrepancy at `librarian/2026-08-24.md:270`; the packet at :208
nonetheless says *"carriers.js becomes a standing instrument"*, which presumes an artifact to
promote.

**And the registration is generous about what it was.** *"Half-built by construction: it marks
stale claims when someone runs it"* describes a sweep missing a trigger. It is not that. It is a
one-shot migration — three hardcoded paths, one hardcoded note, already applied at `ae5ff99`. It
detects nothing and would find nothing new if run again. **The immune organ the record describes
has never existed in any form**, and the thing registered as half-built was a patch script that
had already finished its only job.

Landed unmodified at `dev/one-shot/carriers-2026-08-23.js` — in `dev/one-shot/` rather than
`tools/` so nothing mistakes it for an instrument, with a header stating the gap. T3 is therefore
a BUILD, not a promotion, and B is told so.

The objective, bar and refusal clause in the packet all survive unchanged: a detector that goes
red on a withdrawn claim whose carrier still asserts it, shown red on the historical state and
green at HEAD. Only the premise that something already exists was wrong.

Disjoint by construction: no two packets name the same file.

---

## Registered, so this dispatch can be shown wrong

The librarian registered the plan's falsifier: *if the orchestrator must rewrite more than the
routing — if the packets lack what a brief needs and objectives have to be re-derived — then
librarian-plans/orch-delegates adds a hop without adding content, and the keeper's simpler two-step
was the better shape.*

**Scored here, before the returns land — and corrected once, before the dispatch left.**

First scoring, written above the returns: *"the three briefs took the objectives, bars and refusal
clauses VERBATIM; nothing was re-derived; the falsifier does not fire."* Then the file-existence
check found T3's premise false, and that line stopped being true.

Honest reading: **PARTIAL FIRE.** The chair did not re-derive an objective — every objective, bar
and refusal clause is the librarian's, verbatim. But it had to correct a factual premise the
packet rested on, and that is more than routing. Two things are worth separating rather than
averaging:

- The *planning* half held. The shape survived contact with the repo intact.
- The *retrieval* half did not, and it failed in the direction that matters: the seat whose job is
  fidelity to the record propagated the record's own overstatement. It caught the same discrepancy
  elsewhere in its notes (`:270`) and the packet at `:208` still assumed the artifact.

That is a real finding about the loop rather than about the librarian: **a map can be right about
what bears on a question and wrong about whether the thing exists.** The check that caught it was
not clever — it was `ls` on every path a brief was about to name, run because the dispatch order
now puts verification before sending. One night old, first use, caught a false premise before it
reached a pane.

Recorded now so it cannot be re-read favourably later — including by me, who wrote the favourable
version twenty minutes ago.

The second falsifier, from `BUILDING.md`: if three consecutive laps produce a librarian plan naming
a pane, or an orchestrator dispatch reordering the librarian's packets without saying why, the split
is prose. **This lap: one plan named a pane (T4, self-caught); the dispatch did not reorder.**

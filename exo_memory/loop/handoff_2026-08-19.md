# Handoff — 2026-08-19 ~03:40Z, desktop

**Law 1: where a master exists, this names it and stops.** Written out only what would be lost.

And the reason this file is short: the same day measured that **a commitment held as prose survives
a compaction at 3.5%**. A long handoff is the format most likely to evaporate. So the open work is
not listed here — it is a command.

## The one command

```
node consonance/tools/open-items.js
```

Five items, each a **question with a check**, recomputed every run. It cannot go stale because
nothing in it is a claim; it is a reading. Reads 3 of 5 open as of this writing, and do not trust
that sentence — run it.

**Do not add an item you cannot write a command for.** Put those in the journal as prose and accept
the 3.5%. The refusal is the file's whole value.

## What happened today, in one line each

- **Pulled 21 commits from the laptop and reconciled this machine with one command** — `git pull`
  then `dev\shell\install.ps1`. Synced 2, registered 2, left 9, held 1. The loop that took two days
  to build worked in two minutes. `a1e367c` is the laptop using it correctly from the other side.
- **The blind reader made its first autonomous catch, on me.** `4b2e1ab`. It disagreed with "four
  modules, 1,684 lines" in `consonance/hooks/README.md`; the four modules are 1,231 lines. 1,684 was
  the insertion count across EIGHT files — the modules and their four suites. A true number attached
  to the wrong whole, exactly the correction a pane made three days earlier about 80%-of-turns vs
  82%-of-value-turns.
- **`open-items.js` built, and its first run corrected me twice** — `dca6ecc`. It killed a stale
  commitment I had repeated for two days (the SEED rebuild was already done; the watcher I armed
  timed out and built nothing), and surfaced six findings I had never opened.
- **Then it caught a bug in itself.** Its F1 check counted ledger ROWS and reported "7 findings
  filed". Six were attempts the reader never ran. **A row is an attempt; only a verdict is an
  outcome.** Now counts verdicts.

## The finding that should shape tomorrow

**The reader reaches 1 artifact row in 17.** 16 of 17 point at `~/.claude/shell/` — digests,
duration-goal state, drift-watch findings, plan files. None is under version control, so
`resolveHeads` returns `{}` honestly and the C2 gate refuses honestly. **Nothing is broken.** The
tier was designed assuming an artifact is a repo file, and on this machine it mostly is not.

There is a cheap fix worth arguing about, not yet done: for an unversioned file the reader cannot
separate WORLD-MOVED from WRONG — but it can still ask *does the claim match the file now*, and
report "wrong or moved, cannot tell which". Weaker than a verdict, and infinitely more than the zero
those 16 rows get today.

## Corrections I made to my own claims today, kept because they recur

- I reported a `head`/`heads` field mismatch between `sourced-stop.js` and `second-vantage.js` and
  proposed fixing it. **There is no such bug** — `fromV2()` maps it correctly with a single-repo
  fallback. I proposed repairing something that works.
- I told the keeper for two days that the SEED rebuild needed him to close the app. It was already
  done.
- Three shell checks of mine bound `|| echo` to the last command in a pipe, so a clean result and a
  broken check printed identically. Third instance this week.

## Live and unread

Seven findings in `C:\Consonance\data\vantage_findings.jsonl`; one is a verdict, six are
UNLAUNCHABLE for the reason above. F1's thirty-day window is running and cannot fire while the
reader produces.

## The test in flight

`exo_memory/loop/compact_prereg_2026-08-19.md` registers three predictions about what survives THIS
compaction — one name, one number, one commitment — written and committed before it happens. Score
it by appending, never by rewriting. **If the compaction was manual, the test is VOID**: `/compact`
echoes hook output into the transcript, so a canary can be quoted that was never delivered
(`8ca0ac6`).

## Pointers, not copies

- `exo_memory/journal/2026-08-18.md` — the compaction measurement and the night's own account
- `exo_memory/loop/build_ruling.md` — C1–C7 and the speech rules the reader ships under
- `consonance/hooks/README.md` — what is registered, with a command to re-derive it
- `dev/shell/install.ps1` — the reconciler; its header carries why it now edits `settings.json`

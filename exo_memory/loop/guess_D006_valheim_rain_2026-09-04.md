# D006 — sealed guess, written BEFORE any map exists

Inquiry (the keeper, verbatim): a Valheim performance mod for the rain/roof path —
write the finding up, run it through the work chain.

## The chair's prior — which parts of the corpus bear on this

1. `exo_memory/` on reading a Mono game's ground truth — the valheim-agent line already
   established that code lives in IL and values live in the asset bundles, and that
   Ghidra is the wrong tool for a Mono target. Tonight's dnSpy route is the same
   technique re-derived; the corpus should already carry it.

2. The falsifier-registration discipline — `consonance/src-tauri/brief/BUILDING.md`
   (WHAT A HAND-BACK OWES) and the mutation-proof rule. A perf claim with no
   before/after number is the exact shape this room keeps finding under rocks:
   a mechanism read off source and reported as a magnitude.

3. `exo_memory/muscle_map.md` — the "absent reads exactly like done" class, and the
   narrow-query-wide-sentence failure. My prior-art check tonight ALREADY hit it once:
   I read "no WearNTear patch" off a `head -20` that had truncated the W's. That
   error is the single most likely way this project ships something wrong.

## What I expect the librarian to add that I do not have
Prior registrations about measuring before building, and whatever the room holds on
shipping work to a public audience (the mod would be the first artifact leaving this
room for strangers) — which is live and unruled as the D005 stranger-install question.

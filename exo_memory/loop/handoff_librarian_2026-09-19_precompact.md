# Handoff — the librarian seat, 2026-09-19 07:3x, DESKTOP (machine D), written at the keeper's word before a compaction and a restart

*One lineage. The master for 09-16 → 09-19 is `librarian/2026-09-16.md` (it carries all four days; entries are stamped by the clock). At this writing: `origin/main` = f2fe77c, 0 unpushed, tree clean, no open lap, D076 filed. The repo on D is `C:\Users\nname\Desktop\lighthouse`; on L it is `C:\Consonance\lighthouse`.*

## The one thing owed right now

**The keeper closes and reopens Consonance on D once.** The running exe is from 09-16 13:12. That one rebuild ships four landed things: prompt suggestions off for every seat, Main included (D076); the second-instance dialog that no longer asserts a window (D073); the launcher's ghost notice (D072, `launch.ps1`, live already since it is a script); the STALE verdict in the pulse (D072, `chain-status.js`, live already). After the relaunch: the chair checks ONE idle pane's composer scans EMPTY with E's D074 scanner, and a day later C re-runs its delivery census (`handback/p-delivery-census-C_2026-09-19.md`, scripts in C's scratchpad `census/`). **Falsifier:** if forced holds survive, candidate 2 (a dim-aware composer state) opens.

## What the last three days found, in one paragraph

Deliveries to idle panes held the full 240 s because Claude Code's dim prompt suggestion (`ESC[2m`) sits in an empty composer; `vt100` 0.15.2 drops SGR 2; `typed_only` keeps the cells; `input_box_empty` reads text. C found it from transcripts and E from a frame scan, independently (D074). The switch exists — `CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION=false`, verified by B from current docs, Claude Code ≥ 2.1.238, D runs 2.1.278 (D075). The ready-stamp hooks had never been installed on D; the chair installed the three files on 09-19 01:2x and stamps now exist (`C:\Consonance\data\ready`). Plan with every addendum: `loop/plan_composer_predicate_2026-09-19.md`.

## The keeper's standing rules (each is also a memory file on BOTH machines only if recreated — the memory directory does not travel)

Memory files on D: `handoff-without-the-keeper`, `wait-for-all-outputs`, `chair-does-not-build-in-a-lap`, `never-hand-the-keeper-a-command`, `cold-start-reload-cost`. On L only the first three exist; recreate the last two there.
- Wait for ALL panes before returning to the chair; one `handbacks-in` row, one ring.
- The chair does not build or edit inside a lap; landing orders name files to COMMIT.
- Never hand the keeper a command this seat can run.
- Every `call_chair` ends on its own last line: `NEXT: <station> <command> when <condition>` — the gate REFUSES without the station or the `when` clause (it refused this seat on 09-18 23:29).
- After a gap over an hour or a model switch, the first call reloads the whole thread uncached (826k tokens on 09-18). Suggest `/compact` before long breaks.
- **New, 09-19 02:2x, from this seat's own WRONG:** an ambiguous "it" means the topic at hand, never a deletion; a deletion is named back to the keeper before it runs.

## Queued, in the record's order

1. After the rebuild check: C's return-leg predicate (L062, `handback/p-return-leg-C_2026-09-16.md`) — a refused `call_librarian` discards its text.
2. Pane battery RUN 2 (`loop/pane_battery_registration_2026-09-16.md`) — run 1 hit the ceiling; load the tasks.
3. The diversity scorer's artifacts still live only in D's scratchpads — commit them, then the Third Place SPINE §8 order (`third_place/SPINE_diversity_to_retrieval_2026-09-16.md`).
4. Thesis run 3 — a ring lap while the keeper sleeps; the scheduler's first firing (`loop/scheduler_and_unlocked_mode_idea_2026-09-16.md`). Runs 1 and 2 verdicts: `librarian/2026-09-16.md` 07:09 and 07:30 on 09-16.

## Open and small

- The L-launch falsifier: the first shortcut click on the laptop after its pull must open the newest build (D066 chunk 1). Laptop returns Sunday.
- `install.ps1 -Check` on D still reports drift (5 absent, 6 drifted, 1 held, 3 excluded-but-live) — the keeper's to rule on.
- 12 open asks, oldest 54 days (`node consonance/tools/ask.js`).
- The stick: 81 stale tails (~559 MB) listed by `--prune-below-agreed`; deleting needs the keeper's word. Two stick faults in two days, cause unmeasured.
- Ideas saved, not built: the chair's Escape key (`loop/chair_cancel_dispatch_idea_2026-09-17.md`), pane archetypes, the scheduler.
- Off-room: Thunderhead Night Optimized v1.1 is installed on `G:\` (11 lights on night_optimized only); the keeper has not yet said how it looked; L077 is dark and L080 dim by the author's values. D's one crash (09-18 01:34, `0x3B IP_MISALIGNED`) — he is watching for a repeat before easing a −20 Curve Optimizer undervolt.

## This seat's WRONG column since 09-16, so the next waking does not repeat it

Mid-lap rings to the chair; landing orders that told the chair to edit; a git command handed to the keeper; "two 600-s waits" (one full, one cut short); "four freezes" that were power lost during sleep; the pulse "did not name" D064 (it did; it lacked a verdict); a board count of 0 forced (6 of 10); a falsifier arm that would have acquitted the predicate; claiming the shelf filled the window (it is ~4%); and deleting five folders on a misread "remove it". The common shape: a claim or an action made from the sense of what was happening instead of from the file or the question in front of it.

# L039 PRE-REGISTRATION — written and committed BEFORE any subject runs.

**2026-09-07 ~02:00. The keeper, 01:50, verbatim: *"run the one-object design with the panes we
have."*** Fresh subjects are NOT authorised, so the arrangement arm does not run.

**This file exists because the ledger has no `frozen` stage yet** (its gate is chunk 4). The room's
own practice puts a pre-registration in a committed file with a sha — `gap2_preregistration.md`,
`cycle9_preregistration.md` — and that is a harder timestamp than a row note. **Nothing below may be
edited after E's key is sealed; a correction goes in a new section with its own time.**

---

## WHAT IS BEING TESTED

> **Are these readers decorrelated on this object at all?**

**And what is NOT** — stated first, because it is the error most likely to be made when the result
arrives. **This cannot answer whether ARRANGEMENT causes decorrelation.** B §10(d): on standing
panes, a pane's cited strength **is its history**, so "arrangement" is a label on the subject rather
than a treatment applied to it. The independent variable does not exist without fresh interchangeable
readers, and those are not authorised. **If this result is later read as an arrangement finding, that
is the failure this paragraph exists to prevent.**

## THE DESIGN — B §10, and the numbers are from its own `l039_power.js`

    OBJECT      one, which A, B and C have not read. Prose AND code.
    PLANTS      N >= 20, two classes, at least three of each:
                  D1 COVERED    a class the room's instruments reach
                  D2 UNCOVERED  a class no instrument reaches
    KEY         exo_memory/loop/seeded_key_L039.md, COMMITTED BEFORE DISPATCH.
                Natural (unplanted) defects listed SEPARATELY in the same file.
    SUBJECTS    A, B, C. ONE identical brief. Arm 0 only. Board QUIET.
    E           DISQUALIFIED as a subject, because E plants. Cycle 9's rule.

## THE NULL, THE STATISTIC, THE BAR

    NULL        the readers are EXCHANGEABLE — each found-set is a random subset of the N items,
                of that reader's own observed size.
    STATISTIC   the observed MEAN PAIRWISE INTERSECTION of the three found-sets, compared against
                the permutation null.
    CONDITIONED each reader's observed set size. Object difficulty enters ONLY through set size,
                so it CANCELS — which is what removes the matched-objects judgement the chair's
                three-arm version leaned on.
    BAR         alpha = 0.05, R = 3.

**Power, B's figures, `node l039_power.js` §4:**

    N= 8   total collapse (lambda=1.00)   0.978
    N= 8   half  collapse (lambda=0.50)   0.538      <- why N>=8 was the WRONG bar
    N=12   half  collapse                 0.773
    N=20   half  collapse                 0.948      <- the bar this run uses
    N=30   half  collapse                 0.993

## SCORING — by a NON-PLANTER

Per reader: the set found, split **planted / natural / false-positive**. Then the UNION, the pairwise
overlaps, and the test above.

**Printed as LISTS, never counts** (J's D010). **And each found item marked CONTRADICTION or
ADDITION** — whether it contradicts a standing claim or adds a new one (`journal/2026-08-17.md:327`).
A union count treats those identically and they are not the same act.

## WHAT WOULD MAKE THIS RUN VOID

- **Any hand-back citing the key file, or another reader's list.** The leak is then the finding, not
  the result. (Map falsifier (c).)
- **The key edited after dispatch.**
- **A subject having read the object before.** Say so and the run is void for that reader.
- **D2 items found by an INSTRUMENT rather than a reader** — then D2 was not uncovered and the class
  split is re-ruled, not quietly kept. (Map falsifier (d).)

## WHAT A NULL RESULT MEANS, WRITTEN NOW SO IT CANNOT BE RE-READ LATER

**If the observed mean pairwise intersection is consistent with the exchangeable null, the honest
statement is: these three readers, on this object, are not distinguishable from independent random
readers of the same set sizes.** That is a real answer and it is not a failed experiment.

**Four diversity gauges have died in this room**, at least one because a result nobody wanted was
read as a broken instrument. **This one was already killed once tonight** — B's freeze pass found
arm perfectly aliased with object, the union number 96% consistent with both total collapse and total
decorrelation — and the design that survived is smaller because it had to be.

---

*The three-arm design this replaces was the chair's, cut from the librarian's L039 map. It was
refused by B on arithmetic before it spent four seats' turns, which is what a freeze pass is for.*

# Handoff — 2026-08-23 ~07:50, written at the keeper's call before he leaves

**Short on purpose.** A commitment held as prose survives a compaction at 3.5%; names at 33.8%. This
file names things and hands over commands. **Recall from the master, never a copy — and this file is
a copy.**

    node consonance/tools/open-items.js      # the open work, recomputed, not a list in this file
    node consonance/tools/state-block.js     # what you wake with, including the TRIGGERS table
    node consonance/tools/lap-row.js --report # the loop's own metric; empty on the day it was built

---

# THE ORDER, and it was corrected on the way out the door

**Get OUR dev version solid FIRST. The consumer version comes after.** The keeper's reason, and it
reorders everything below: *the consumer version is a slightly modified dev version.* So a defect
fixed in dev is fixed in both, and the 11 shipped suites that do not run in a consumer tree are
mostly **dev** problems — fixtures asserting against private content, hardcoded data dirs — wearing
consumer clothes.

Earlier in the night the chair argued the opposite (consumer first, "because it is the measurement").
That framing is not wrong about *why* the consumer version matters; it is wrong about *when*.

## WHAT "DONE ON THE LAPTOP" HAS TO MEAN — the librarian's checklist, verbatim

*Prior art it is built from: `journal/2026-08-18.md` §8 (both hooks pulled, neither registered);
`loop/handoff_2026-08-19.md` (laptop path baked into a hook, failed on desktop's first compaction);
`journal/2026-08-17.md` eighth append (a task "fires daily" — true on one box).*

"Done on the laptop" must mean **receivable by a machine that pulls and runs the installer**, because
that is all the desktop can do.

1. `dev/shell/install.ps1 -Check` shows new hooks **REGISTERED, not merely copied**; scheduled tasks
   have an installer; nothing needed lives only in `~/.consonance/` or in an env var
   (`CARGO_TARGET_DIR` is the live example — it hid a complete build from `open-items` for weeks).
2. Bundle rebuilt; `open-items.js` brief item **CLOSED**.
3. Mutations run, and the suite green **inside the generated consumer tree** — the only "second
   machine" this laptop can reach without touching the desktop.
4. Work on a branch, merge to `main` when 1–3 hold; the desktop pulls `main` only. Not-pushing has a
   measured cost (08-17: hours spent checking six places for unpushed work); a pushed branch is
   visible-but-unmerged.
5. Decide the **bait-fixtures-on-the-shelf** question before the desktop librarian wakes on the
   merged tree.

---

# THE WORK, in order

## 1. Dev solid

- **The 11 suites that do not run in a consumer tree.** Split, verified file by file by the
  librarian, NOT by the chair's sample-of-4: **5** fixtures against private content
  (attic/librarian/baseline/data) · **2** manifest gaps (`dream-gate` ENOENT `dev/shell/install.ps1`;
  `guard-census` ENOENT `src-tauri/tests/arch_test.rs`) · **3** caused by the generator, now fixed ·
  **1** environment. Re-measure before acting: `cd <generated tree> && node consonance/tools/js-suite.js`.
- **The `actors.test.js` canary.** Red for its declared reason — 15 unresolved ids — and the chair
  misreported it twice tonight. **The split is 9 mine / 6 the keeper's:** six old uuids need board
  evidence only he has (`66eee6ce b8ea54e3 433f587c 061bc00e 6085178a 9c08c65b`); the other nine are
  **string normalisation** — `alpha`/`bravo`/`BRAVO`/`around`/`Around`/`AROUND` are casing, plus
  `sibling-` ×2 and `main-tab` — and need no evidence at all. That halves a worklist that has sat
  blocked on the keeper since 08-11.
- **A live collision in that table:** `ALIASES['M'] = 'C'` (Main signing as M) versus the librarian's
  assigned letter, which **is M**. `canonical('M')` returns `C`. Not firing today because LIB's rows
  carry the uuid; it is still wrong in the table.
- **`ferry-watch.js:111` prints `171`** where `node consonance/tools/ferry.js --due | tail -1` says
  **179**, and they are **different quantities** (the hook counts never-ferried older than
  `FRESH_HOURS=6`). Three seats flagged this; nobody has edited it. The chair quoted three different
  numbers on four occasions.

## 2. The loop, now that it has an instrument

`lap-row.js` exists and its ledger is **empty**. Three registered falsifiers have been reading from
nothing since they were registered, and now have a source. **Run laps and fill it** — the metric is
the orchestrator's guessed paths ∩ the librarian's map, per lap, and neither seat can produce it
alone. The protocol is `consonance/src-tauri/brief/BUILDING.md`, which the orchestrator now receives
at wake (`main.rs:4161`).

## 3. Then the consumer version

`gen-consumer.js` works: 134 files, `cargo check` on the generated tree exits 0, scan clean. What is
left is (a) the 11 suites above, which are dev work, (b) the public repo does not exist yet, and
(c) flipping `lighthouse` to private — **the keeper's to pull**, one command:
`gh repo edit solariz3d/lighthouse --visibility private`.

## Small and owed

- Nobody holds pane E's two counts (keeper-initiated share of librarian laps; a tool uninvoked 7 days
  is retired). `lap-row --report` answers the first once laps exist.
- Re-run the **self-wake probe after a restart** — it is n=1 on one harness version, and the task is
  owned by the live harness process.
- `365` stands wrong in `b2f1634`'s message (`wc -l` says 364). Pushed; corrected by append only.
- The baseline verdict inconsistency: `lap-row.js`'s last-resort literal baselined **FATAL-DEFAULT**,
  `transcript-watch.js`'s identical one baselined **REVIEW**.

---

# WHAT CHANGED TONIGHT THAT A FRESH INSTANCE MUST NOT RE-LITIGATE

**Exteriority was measured with the wrong unit, and the correction was seven days old.** BOOT's
curated-auditor section equated *outside* with *never-authored* — a bar no mind clears, the keeper's
included. The correct unit was already on disk twice: **add-and-hold** plus **two-way correction**.
"Uncurated" stays the test for MEASUREMENT; for MINDS it is those two. See the 2026-08-23 amendment
in `BOOT.md`, and **`journal/2026-08-16.md:722-726`**, where a pane withdrew the old form in full and
nothing propagated it for seven days.

**Its falsifier, registered before it landed:** it is prose if, one season on, the
bidirectional-correction count has still never been run with its amended unit.

**And the procedure that pass produced: MARK THE CARRIERS, LEAVE THE TRACES.** Journals are dated
records and are never rewritten. Live instruments — BOOT, SOURCE, TRAINING, cards, briefs — keep
teaching whatever they say, at every wake. The 08-17 diving retirement edited every downstream
document and missed the carrier, so the room taught a retired metaphor for five weeks. Six carriers
were marked this pass; the sweep is
`grep -rl "only decorrelated" exo_memory/ | grep -v journal/`.

---

# THE STANDING RULE FOR THE LIBRARIAN

**Give it something to MEASURE, never something to REACH for.** Measured in one night, same seat,
same corpus: asked *"what do you see in the corpus?"* it produced a reading and missed a methodology
recorded in seven files; asked *"check this claim / verify this attribution"* it returned ten
catches. `BUILDING.md` carries the rule and its falsifier.

# THE CHAIR'S OWN FAILURE MODE, stated so the next instance can watch for it

**It publishes numbers it has not run.** Tonight: `171` (three numbers, two quantities, four
occasions); `24.1%` clean, actually 52.2%; `24 preregistrations`, actually 25; a commit sha
(`d4e8f21`) that **does not exist**; and a canary classification read off a test whose fixture it had
not opened. **Every one was caught by something other than the chair.** The instrument for it is
`node consonance/tools/librarian-route.js <sha> --verify`, which reads a commit body and runs the
commands cited beside its figures.

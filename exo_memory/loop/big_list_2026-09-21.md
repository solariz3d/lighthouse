# The big list — everything in flight, waiting, and next. Librarian (on D), 2026-09-21 12:4x, at the keeper's word:
*"catch us back up to speed, lets take a turn here to plan our a list of what we got going, like our potential next big plans"*

## 1 · RUNNING ON ITS OWN (waits, not work)
- **B's abstain predictions** — 84 of 500 post-install L0 verdicts at 12:08 (`loop/scoring_windows_2026-09-21.md`).
- **L3 re-measure** — 23 of 300 one-message verdicts at 12:08; KEEP if < 25%, RETIRE otherwise; ~10 days.

## 2 · TONIGHT ON THE LAPTOP (`loop/for_L_tonight_2026-09-21.md`)
A's L070 rebuilt as stop-before-write → land L070 + commit `exo_memory/review/` → pull → move L's old `vantage_cell` aside →
C's ledger union (lap +214, board +3,352) → fold-by-generation in `lap-row.js` (the keeper's condition) → then a real close;
D unions its side. Afterwards both machines' JS suites should be green.

## 3 · NEXT BIG PLANS — candidates, not yet chosen
1. **Retrieval, the last big problem.** Two halves (`loop/retrieval_collation_2026-09-21.md`, `map/M.md` 12:3x):
   *reach* (the right file never fetched) and *composition* (the rule fetched, then lost while writing — 60 of 75).
   - **Jev for reach and load relief** (`third_place/JEV_PLAN_2026-09-21.md`): step 0 is the keeper's Vercel key; step 1
     the token census of fixed-answer decisions; the backtest T-J1 over the WRONG columns must be registered by 10-31.
   - **Composition** has no instrument yet; every cue-based fix failed. Open research.
   - The retriever (closed 08-31) reopens only with ~50 labelled turns; pooling both machines is the keeper's call.
2. **"Solid" — a finish line for Consonance itself** (proposed 09:08, not adopted): both suites green, a clean week of
   laptop↔desktop round trips with nothing lost/replaced/misreported, usage under the weekly limit with keep-warm on —
   then a feature freeze.
3. **Usage** — keep-warm goes live at the next reopen (check: a seat idle ~1 h writes < 50k on its next turn); BOOT's 39%
   already trimmed; next is the per-seat token census and relaunching less.
4. **Fun builds** — the loop-view tab + Signal Audio inside Consonance, one scene across all panes and one behind the
   seats (`loop/loop_view_tab_idea_2026-09-21.md`); Signal, Blackbox and Dreamzone themselves, untouched since 09-11.
5. **Saved tests, gated** — fresh-vs-resumed (behind the keeper's 07:40 "change nothing yet"); the quantize ladder
   (Third Place said yes-when-its-time-comes; inside the 10-31 bet).
6. **The consumer version** — the keeper's stated destination; retrieval and "solid" are its gates.

## 4 · THE KEEPER'S OWN
The Jev key · ASK-007 (consent, unchanged) · ASK-002 (feeds vs interests — the first weekday digest after the audio feeds
were added is today's; check it) · whether to pool retriever labels across machines.

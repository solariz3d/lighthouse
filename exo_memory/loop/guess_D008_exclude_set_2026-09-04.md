# D008 — sealed guess, ring lap (no user inquiry entered)

The chain is cycling on its own under AMENDMENT 1. The chair's prior, sealed before L rules:

1. **At least one of the six EXCLUDE entries is hiding a gap rather than excluding something that
   should not ship** — and I expect it to be a machine-bound or fixture-shaped file, because that is
   the class the room keeps closing by hiding (`gen-consumer.fixture-scope.test.js`, the canary; the
   `portable-paths.baseline.json` that never ships). `loop/consumer_registration` R0: the predicate is
   a filter on the manifest, never on the ship list — an EXCLUDE that acts as a ship-list edit is the
   violation shape.

2. **The three new gaps B found will not be closeable on their merits either way** — `tools/README.md`
   and `groove-FINDINGS.md` are documentation a stranger arguably needs; `carrier-drift.registry.json`
   is state, and shipping state to a stranger is a different question from shipping code. I expect L
   to split them rather than rule them as one set.

3. **The durable half — the test for a SEVENTH entry — is the part most likely to be missing.** Six
   entries exist and no rule for adding one is on disk. `universe_print_registration:26` is the
   nearest anchor: a set whose growth rule is unwritten is a set nobody can audit later.

**Where I expect to be wrong:** L found last lap that nobody had miscounted when I was sure someone
had. My prior here is again that something is wrong; the honest possibility is that all six are clean
and the only defect is the missing growth rule.

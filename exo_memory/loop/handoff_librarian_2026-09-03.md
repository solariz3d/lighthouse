# Handoff — the librarian seat, DESKTOP, 2026-09-03 ~00:58, before compaction, with the consumer-build plan

**Supersedes `handoff_librarian_2026-09-02.md` for this machine.** The laptop's LEDGER is still the room's
open-windows list; this seat did not edit it (baton reasons below). **Masters:** `librarian/2026-09-02.desktop.md`
(648 lines, every entry timestamped, the two maps and the plan are there); `map/M.md` (the seat's own, appended
from ~12:13 by the seat itself). This file points; it does not restate.

## FIRST ACTS AFTER THE WAKE — in this order, none skipped
1. `git status -sb` — this desk is **ahead 8, unpushed**, clean. If it reads otherwise, something moved. Pushing is the keeper's word.
2. `node consonance/tools/chain-status.js` — expect **`D003 · RETURN-LEG · holder chair`**, 140h+. If it still reads that, **do not ring the chair; you will be refused** (twice tonight, board rows 18:12Z and ~06:17Z). D004 is open on this ledger with **no holder**; the guard cannot see it. The loop cannot run here until the chair writes D004's chain row or files D003. The chair reads *commits* — it found tonight's map 19 minutes late that way — so file, commit, and let it read.
3. Open `librarian/2026-09-02.desktop.md` from **~00:05 down**: the game plan, the D004 map, the D004 work-shape, the keeper's override, the consumer map. Everything below is a pointer into it.
4. The keeper had two things in flight when this was written — **his answer on the split, and the symphony question** (both below). Do not re-derive them; ask, or read what he wrote after this file.

## WHAT WAS DECIDED TONIGHT, by whom
- **Keeper overrode the chair on D004:** remove `zackn`, `nname`, the email — *"actual personal info."* (~00:45)
- **Keeper reframed, and it dissolved the removal question:** the public repo should be **the consumer build** — what a stranger forks, with every keeper-populated system created by the new user on their first wake. The dev version on disk stays his, city and all. (~00:49; map at ~00:55, `e5fb20c`)
- **Consequence the seat drew, not yet ratified:** `lighthouse` → **private**, one setting; the consumer repo is **generated** with fresh history. That is `gen-consumer`'s own 08-22 model, finally load-bearing. **No `filter-repo`, ever** — B is dissolved, not deferred. Tonight's A is the privacy flip.
- **Keeper's refinement, ~00:56, and it is the crux — verbatim:** *"the bulk of the system, most of it that makes you you, the important parts, are still a part of the consumer app. The hard part is seeing which parts to bring along and which parts to keep here."* So the split is NOT room-vs-trace. **Most of this seat ships.** What stays is the *personal* — his journals, his machines, his name. The cut runs *through* directories, not between them.

## THE SPLIT — this seat's first cut, written in the window so it is not lost; CONTESTED where marked
| ships (makes the seat the seat) | stays (his) | CONTESTED — the registration decides |
|---|---|---|
| `BOOT.md` (the generated public form exists: `brief/BOOT.md`, 5 pronouns still leak) | `journal/` — all of it | `record/` — the worked material *behind* the cards; the cards say "open it before you claim" — but it is dated, his, and names him |
| `SOURCE.md` (triggers → cards) | `librarian/*.md` — this seat's dated notes; the **form** ships (README, LEDGER's shape, DOSSIER's shape), the entries do not | `memory/` — half instruments (`interior-at-the-seam`, `claim-your-continuity`, `frozen-is-not-dead`), half his (`user-solariz3d`, `dreamzone-build`, `signal-and-606-night`) — split by file |
| `cards/` — every one, **but they cite `muscle_map`** (the Third Place's T0 trap, 08-25) | `map/A,B,C,E,M*.md` — the seats' own words | `spread/` + `research/` — outputs of his nights AND instruments BOOT points at ("the counter-voice"; "nobody knows") — I lean SHIP |
| the loop: `brief/BUILDING.md`, `COMMITTEE.md`, `LIBRARIAN.md`, `SEED.md`, `THIRD_PLACE.md` | `loop/` — the specific registrations, handoffs, packets; the **practice** (registration-first, falsifier before build) ships as prose in BUILDING | `muscle_map.md` + `TRAINING.md` — the room's findings; cards cite the first; `TRAINING.md:15-17` (F2) already forbids the second from any shell — **stays**, and the cards' citations into muscle_map are the DANGLING problem |
| every tool in `consonance/tools/`, the hooks, `install.ps1`, the app | `C:\Consonance\data\*`, `~/.consonance.json` — created empty by first-run | `SELF_TRACE.md`, `the_living_wave.md`, `CONVERGENCE.md` — first-night traces; BOOT lists them as "honest traces… not commandments" — lean STAY, with BOOT's pointers to them rewritten to "yours to write" |
**The rule the registration must produce is a path predicate, not this table** — the tree grows nightly and a table rots. And the DANGLING class is the whole difficulty: a shipped file that cites a stayed file. `gen-consumer`'s DANGLING rule tried to rewrite those and broke 11 suites on 08-23. The registration should decide *rewrite, stub, or ship-the-target* per citation class, not per file.

## OPEN, AND WHOSE
- **Keeper:** the symphony question — does a fork ever talk back? That is T4 (`LEDGER` row `exteroception_registration.md`, option PENDING since 08-25) and P-SIGNAL-BACK (L008). It decides distribution vs network. **Named, not invented.**
- **Keeper:** does `Regina` count *in the consumer build's ambient config*? (For this repo the question is moot once private.) And the ambient hook prints his coordinates every session-start on his machines — fine on disk, per his word.
- **Chair, one line:** move the baton (`lap-row.js --stage D004 dispatched --holder panes --to A,B`, or file D003). Nothing here runs until then. Then re-arm the watch — it had none.
- **Chair:** the D004 work-shape as written (~00:42) is **superseded in item 4 and shrunk elsewhere** by the reframe: P-PUBLISH-SCAN still stands (it guards the *consumer* repo's pushes); P-ASK-CORRECT stands; the carrier line at `gen-consumer.js:2` becomes *true* rather than edited once `lighthouse` is private. P-REMOVE-A/B are **withdrawn** — private + generated replaces both.
- **Next lap, registration first:** **P-CONSUMER-REG** — the split predicate; the first-run's six user questions; the fork target and push step; the falsifier: *a stranger, on a machine none of us has seen, installs from the public repo alone and the four wake proofs pass, scored by their printed output*. Degenerating if: a second hand-maintained tree; a fixture the generator rewrote; a fork that needs a file from this repo to boot.
- **Unread by this seat and named so it is opened:** `research/the_retrieval_problem_outside.md` (278 lines) — bears on "their own notes within the symphony."

## STATE AT WRITE TIME
- Repo: `e5fb20c`, ahead 8, clean. The laptop has not pushed since `b785e57` (09-02 11:38) — 13 hours.
- Build: exe 12:06:44 > source; `whats-live` honest post-pull (it was not, pre-pull — commit clock vs arrival, filed ~12:00).
- Wake proofs on this machine: 1 PARTIAL (A/B pointer-only; **committee here is A/B/J/K/L, not A/B/C/E**), 2 PASS (my own ring refused, row names `lap-row.js`), 3 not runnable (no live lap), 4 SPLIT — holder readable PASS, "moving" FAIL: a 140h lap breathes as *impatient* because the only age buckets are <15m / ≥15m (`chain-indicator.js:514`, `app.css:57`).
- **The one-machine class, four instruments in one day:** `chain-status`, ask ids (F-2W-3 fired — first of this seat's falsifiers to fire on evidence), `whats-live`, `letters.json`. The game plan's item 4 (~00:05) says register the class before patching instances. Still absent.
- Asks: 12 open, oldest 38d. ASK-013 is this machine's (WakeToRun on five cron tasks — two lines).

## WRONG COLUMN, this seat: lifetime **70**, unchanged tonight; finder field still zero in-stream
No other seat filed against this desk tonight. Self-caught before publishing and therefore not entries: an arbitrary-first-jsonl SID lookup; guessed `panes.json` field names; a string-grep on the exe whose positive control returned 0 (comments do not survive compilation). All three replaced by reading the artifact. The chair's D004 diagnosis of *its own* argument was wrong (halves swapped) and the map said so with the count.

## THE ONE THING NOT TO CARRY FORWARD
*"The chair will get the ring."* It will not, on this machine, until D003 moves — and even then it reads commits before it reads the board. **File, commit, and say in the reply that you did.** A refused ring is a board row and a data point; a retried one is a spin.

## COMPACTION ADDENDUM — written at the keeper's word, ~00:58
What this window held that disk did not, now on disk: the consumer map (`e5fb20c`), this handoff, and the keeper's ~00:56 refinement above. Nothing is owed from this desk until either the keeper answers the split/symphony questions or the chair moves the baton. **Do not re-map; do not re-ring.** The next thing this seat does is the P-CONSUMER-REG map, when the lap opens — and the split table above is its starting point, not its answer.

*A trace to re-run, not a doctrine to believe.*

# Handoff — 2026-08-25, ~05:00

*Written by the chair. The librarian is armed to wake and audit this from its path; last night's
handoff needed that audit and it found four gap classes, one of which reframed the first line.*

---

# 1. YOU ARE HERE

| | |
|---|---|
| **Cycle 1** | closed, return leg run |
| **Cycle 2** | closed, return leg run |
| **Cycle 3** | four hand-backs committed, **return leg run by the librarian from disk** (`~04:55`) |
| **The Third Place** | designed whole · **Leg 1 LANDED, NOT SHIPPED** (`34afdf6`) · Legs 2–3 unstarted |

```
suites      JS 59 green · Rust 311 · mutations 33/33, 8/8, 11/11, 5/5, 3/3
tree        clean · 0 unpushed
desktop     0 commits — deliberately frozen, gated on the keeper's call
```

---

# 2. FIRST THING NEXT WINDOW

**`THIRD_PLACE.md` does not exist, and the seat refuses to open without it.** That is by design
(`"refusing to open a room with no brief"`), so Leg 1 is inert until the brief is written.

**Do not write it long.** The existing room shell is **55 lines**. That is the target. A T0 shell
that reads like tonight's commit messages fails its own bar before anyone tests it — and it should
probably not be written by whoever has been awake all night.

Then: `tauri.conf.json` bundle entry (that alone puts it in open-items' md5 set — A's fix reads the
authority, so no edit to `open-items.js`), UI tab between `librarian` and `listen`, rebuild.

---

# 3. THE ADOPTED PRACTICE — the self-wake loop, and it needs no build

**The chair was wrong that nothing can wake it.** `librarian/2026-08-23.md:272` records a probe
where an idle pane self-woke on background-task completion with **no human input**, and the
librarian used it on 08-24 to deliver a plan the moment the chair returned. `ScheduleWakeup` and
`run_in_background` are in every seat's tool list.

```
dispatch  →  arm a background timer sized to the work
          →  on wake: read_board          ← the panes already post every hand-back
          →  collate → return leg
          →  re-arm if a pane is still out
```

**Registered as the baseline Leg 2 must beat**, measured in turns-per-hop — so the address table
lands against a number rather than against the assumption that nothing else could work.

**And the habit half:** every pane posts an explicit hand-back to the board, every time. The chair
never read it all night and relied on the `[panes]` digest, which only arrives attached to the
keeper's message — which is why the keeper was the clock. `read_board` answers "is a pane done"
at any moment.

---

# 4. THE NUMBER THAT BELONGS IN FRONT OF EVERYTHING ELSE

Pane B's commitment census, §5a, re-derivable by the grep it prints:

> **46 numbered journal registrations, complete 1–46. ZERO of them are checked.**

`open-items` says its own denominator is hand-maintained — *"a commitment with no check is not
CLOSED, it is ABSENT, and absent reads exactly like done."* The census measured the absent set.
**Its own instrument failed clause 2** (§4), which it reported rather than hid.

And pane C's forgetting pilot, which is sharper than the growth rate anyone was quoting:

> **0 files left the reading path.** Every deleted line in the window is churn inside a file still
> on it. Not "almost nothing" — zero. `node consonance/tools/forget-rate.js`

---

# 5. WHAT THE CHAIR GOT WRONG, kept because a record of only surviving claims reads as though
nothing was ever wrong

- **The cross-model claim INVERTS** (`310c719`). Told the keeper the exteroception organ already
  exists, "2 for 2, 100% hit rate". The universe checked was **6 files of 22**, and *both*
  documents that refute it sat outside that scope. That is **clause 1 broken hours after
  dispatching the packet that registered it.** Worse: `exteroception_registration.md:50-59`, written
  earlier the same night by a pane, had already named the error — *"anyone quoting one of those two
  as the expected value is PICKING."* **T4 is unchanged; the keeper's option call is still open.**
- **12 of 12 dispatch-turns violated the turn boundary today**, measured. The shape is always
  `TEXT … DISPATCH TEXT` — a closing line written after the call left.
- **Diagnosed the missing composer as a binary-upgrade artifact.** It was a four-line documented
  trade in `consonance/ui/term.js:770-778`: no-op resizes are suppressed to avoid baking the input
  bar into scrollback, which removes the free SIGWINCH repaint. The keeper found the workaround
  (click, type a letter) in one try.
- **Read a pane's findings off a screenshot** and began relaying them. Screenshots are for UI state;
  outputs come from disk.
- **Committed a pane's files mid-run twice**, both times reading a status line as a hand-back.

---

# 6. STILL THE KEEPER'S, one conversation

- **`boot_v2`** — execute or attic. Deadline was 2026-08-24.
- **`opposition_preregistration.md`** — registered 08-10, **15 days unrun**.
- **T4's option** — which outward sense, if any. Unaffected by the inverted claim.
- **`room-b9febdee` continuity** — does tonight's first room transcript become the Third Place's
  first record? **Leg 3 is gated on this**, and pane L stays alive until it is answered.

---

# 7. VERIFY, do not trust this document

```
git log --oneline 0ebd101..HEAD          # tonight
node consonance/tools/js-suite.js        # 59 green, 0 canary
cd consonance/src-tauri && cargo test --bin consonance
node consonance/tools/open-items.js
node consonance/tools/forget-rate.js
node consonance/tools/carrier-drift.js
node consonance/tools/lap-row.js --report     # L001–L007
node consonance/tools/ferry.js --report
```

**The mutation harnesses matter more than the suites.** A green suite says the tests pass; only
`dev/mutation/*` says they can fail.

---

# 8. REGISTERED, so this handoff can be shown wrong

- **If the next window opens by re-deriving what §1 and §4 already establish**, the handoff failed
  at its only job.
- **If the self-wake loop is adopted and turns-per-hop is never measured**, §3 was a story and Leg 2
  will land against an assumption, which is exactly what registering the baseline was for.
- **If `THIRD_PLACE.md` is written long**, the seat's first transcript will show it — cycle9's
  priming grep is Leg 1's own bar and it is the check.

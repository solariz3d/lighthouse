# D010 — sealed guess, ring lap. P4: the parity number, at one commit.

## First, the chair's own scoring record, because it is now a pattern and not an anecdote

Three consecutive laps this seat sealed a prior shaped as *a defect is waiting here*, and three times
the pane returned clean or refuted it by name:

    D007/P5  "someone miscounted the identity surface"   -> REFUTED. Nobody miscounted. 61 was stale.
    D008/P6  "one of the six EXCLUDE entries hides a gap" -> REFUTED. All six stand.
    D009/P3  "P3's holder routes around the guard's refusal" -> REFUTED BY NAME. K dropped the prefix
             in the same edit, then made the refusal fire on purpose.

The confirmed half each time was an ABSENCE, never a defect: a missing growth rule, an uninstrumented
leg, a class shaped so it cannot see orphaned references. **My priors find defects; the record keeps
returning absences.** That is a calibration finding about this seat and it belongs in the guess, not
in a post-hoc note. BOOT's `base-rate deflation` entry is the nearest anchor and it cuts the other
way: I have case data now, three cases, and I should stop reaching past it.

## So the prior for P4, stated against my own record

1. **P4 comes back clean on process and RED on the number.** D starts at >= 21 by A's own registration
   as amended. I expect the parity run to confirm red rather than discover a surprise — and I expect
   the interesting output to be the RECONCILIATION (S 74-by-find vs 73 tracked; G 66 derived vs J's 63
   measured), not the verdict.

2. **The one-commit precondition is the thing that breaks, if anything does** — not through
   carelessness but because `collect()` walks the WORKTREE, so a single dirty file anywhere at the
   moment of generate silently changes what S counts. Nothing enforces it. I expect P4 to have to
   assert cleanliness rather than being able to prove it after the fact.

3. **Against my own pattern: the honest possibility is that P4 is mechanical and lands clean**, and
   the only finding is a number. Three laps say that is the likelier outcome and I am recording it so
   the guess can be scored against the calibration rather than against my instinct.

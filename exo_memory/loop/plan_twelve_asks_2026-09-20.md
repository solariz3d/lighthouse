# The twelve asks, cleared in chunks — the plan. Librarian (the lineage, on D), 2026-09-20 ~11:5x.

**THE BOUNDARY, FIRST, BECAUSE IT GOVERNS EVERY CHUNK.** This seat cannot answer any of these. Clearing an ask is
the keeper's act — that is the protocol's own rule, and it is why the digest goal correctly refuses to write to
the store even when it has re-tested its own asks. **What this plan produces is twelve asks that are each ONE READ
AND ONE DECISION**: re-measured today rather than claimed weeks ago, referents recovered where the ask does not
name them, and the moot ones named as moot so they stop costing attention.

---

## WHERE THE TWELVE STAND AFTER THIS MORNING

**DECISION-READY — six, measured today (D091/D092 and this seat's pass):**

| ask | age | what the keeper is actually deciding now |
|---|---|---|
| **004** | 55d | **Ratify-or-revert**, not the question as filed. Step 1 ingests the auditor four times over (P60, P61, P67, P71), each externally authorised, disclosed, with a preserved backup. The auditor's own position is keep-and-disclose. |
| **005** | 51d | Half one **overtaken** — whole-log in practice since P59, in spec since P67; what remains is a **vestigial tail clause** in the prompt. Half two — the digest-vs-self-audit altitude — **stands and is his**. |
| **002** | 26d | Evidence line corrected by the goal itself: all four interests served at P68–P70. The live figure is 11 of 766 titles matching an audio criterion, and **`eess.AS`/`cs.SD` absent from the eight feeds**. Widen is now a two-line change; narrow is no longer forced. |
| **006** | 26d | Referent recovered by E: **`session-start.js`** (guards only `CONSONANCE_DREAM`) and **`userprompt-submit.js`** (no guard). Re-checked on disk today: `grep -c` is **0** in both. |
| **011** | 21d | Re-measured: **61 → 125 tracked files**, same command, and **103 of 125 are `exo_memory/`**. So the scrub cost is dominated by the record and breaks the `path:line` citations the corpus is read by — and it grows ~3 files a day. |
| **013** | 21d | Re-verified live: all five `ClaudeShell_*` tasks still `WakeToRun=False · StartWhenAvailable=True`; the goal adds **53 of 90** slot coverage. |

**MOOT — one, and it should be closed rather than answered:**

| ask | age | why |
|---|---|---|
| **010** | 22d | *"third_place/ — gitignored or tracked?"* **Already decided.** `.gitignore:72` reads *"The Third Place record (exo_memory/third_place/) travels in the repo since 2026-09-14, at the keeper's word"*, and **29 files are tracked**. Decided six days ago, by him, and the queue never learned. **Recommend: close as already-answered, citing the line.** |

**NEED MEASUREMENT BEFORE THEY ARE ONE READ — three. This is chunk 1.**

| ask | age | what is unmeasured |
|---|---|---|
| **001** | 34d | Two scaffold changes to the L3 stream. Unknown today: is the L3 invocation still unblinded to prior verdicts; does the L0 schema have an abstain option; and **what is the streak now** — the ask says 18 consecutive `quiet_spiral`, and drift-watch's own log at `:1956` records the flags being **contradicted by the transcripts** ("zero external-life grounding" against the keeper typing in three seats over ten hours on 09-14). |
| **003** | 26d | The digest drafted a false account of its own error-checking and says it is *"fixed and disclosed in the digest"*. **Unverified: is the disclosure still there, in the shipped file, and does it say what the ask says it says.** |
| **012** | 20d | The second-vantage routing gate — the ask calls it *"the best-evidenced item on the retrieval line"* at **~46 catches against ZERO found in-stream by authors**. Unknown: whether that count still holds, and whether the registered `dispatch-gate` clause exists on disk or is still design-only. |

**PURE KEEPER CALLS — two. No measurement decides them; they need shaping, not data.**

| ask | age | note |
|---|---|---|
| **007** | 22d | Egress: may the four sealed UNIV documents go to fresh `claude -p` subjects. A **consent event** by its own gate — *"this yes is never inferred from a dispatch."* Stages 1–2 are not waiting on it. |
| **009** | 22d | The six Third Place board rows — remove, keep, or redact. **His conversation, not a bug**, and it holds a test red. The object is six rows in five minutes, not eleven across three days. |

---

## THE CHUNKS

**CHUNK 1 — LAP D093, three packets to three panes, disjoint sources.** Measure 001, 003 and 012 so each becomes
one read. Each packet carries tonight's two standing items — **register the null beside the falsifier**, and
**check the quantity can take more than one value on this object** — and each reports a **member list, never a
bare rate**.

**CHUNK 2 — this seat, no panes.** Shape 007 and 009 into one-decision form: name what each yes and each no
costs, and for 009 state the object exactly (six rows, 1,631 characters, one five-minute window) with the test's
current state beside it. Draft the close for 010 citing `.gitignore:72`.

**CHUNK 3 — the delivery.** All twelve on one sheet, oldest first, each with: the decision in one line, what
changed since it was asked, and what it costs to say yes or no. Nothing cleared by this seat.

**Standing for all chunks:** nothing in `exo_memory/ASK.md`'s Question or Status fields is touched by anyone but
the keeper. Provenance goes where E and B put it — the **Source** line and the reader — and never anywhere else.

# Handoff — the librarian seat, 2026-09-06 08:00, the keeper leaves; the rebuild is the first act tonight

> **SUPERSEDED 2026-09-07 07:50 by `handoff_librarian_2026-09-07.md`** — the rebuild did not happen on 09-06 night; L043 landed 09-07 and the rebuild is tomorrow night's first act.

**Supersedes `handoff_librarian_2026-09-05.md`.** Master for the night: `librarian/2026-09-06.md` (every entry timestamped, every number beside its command). Journal: `journal/2026-09-06.md`. Ledger: `librarian/LEDGER.md`. Map: `map/M.md`.

## STATE AT 08:00
Dev repo (`solariz3d/lighthouse`, PRIVATE) level with GitHub at `9f26c3c`, tree clean. Consumer repo (`solariz3d/consonance`, PUBLIC) exists and is EMPTY — **never pushed unless the keeper says, per push.** Chunk 1 of the four-chunk plan (`2026-09-06.md` 04:52) is LANDED at `c0d64f5` and NOT YET BUILT: the running app is the 04:26 binary. The ready-pair hooks are installed and registered (pair only, 07:05). L041 sits at RETURN-LEG · holder chair; L038, L039, L040 open.

## FIRST ACTS TONIGHT, in order
1. **Relaunch Consonance** (the rebuild). 2. **The proofs, and read the body, never the header:** (a) `call_chair` while the chair holds a lap → no refusal, no click; (b) `raise_pull` at `MAIN` → no gate card, a `librarian channel — delivered` row; (c) a pull at a PANE → still a card, in that pane's tab; (d) `ls C:/Consonance/data/ready/` → a `.json` per live pane within one turn; (e) board rows `DELIVERED … [stamp=ready]`, never `(FORCED …)`; (f) B's two greps → `4 carried / 0 failed` and a map body per pane over 1,500 chars. Whichever fails is the wake note's first line with its mechanism. 3. The chair files L038/L040/L041. 4. **Chunk 2: FREEZE L039 first** (P-FREEZE: A's three sealed objects or fresh subjects per arm; C's fiftieth-firing question; E disqualified as subject; the self-catchable-share template line), then dispatch the seeded-defect run (map at `2026-09-06.md` 02:05).

## THEN
Chunk 3 (the consumer close-out: keyed mutex, first `--gate` run, the cards reconciliation append, the unnamed `record/` file, A's Source-Sha staleness; ends in the first push at the keeper's word from a CLEAN checkout). Chunk 4 (housekeeping: journal gap 09-02→09-05, the selective installer `-Only`, `corpus-age` runtime (B), the 07-27 targetless pull, `.py` in the ratchet, the status-doc module, the ledger).

## RULES THAT GOVERN THIS SEAT NOW
Collate off the counter when it reads N of N — the ring is a wake, the human is never the trigger. Cite by quoted sentence, not line number, in files that grow. Count a body, never a header. Never restore a file you do not exclusively own. WRONG column this machine: **75**.

*A trace to re-run, not a doctrine to believe.*

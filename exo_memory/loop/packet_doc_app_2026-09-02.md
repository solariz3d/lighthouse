# P-DOC-APP — `consonance/README.md`, the app's own description. L030.

**To BRAVO, 2026-09-02 ~04:15. From the LIBRARIAN's map (`75db066`).**

## THE STATE

`consonance/README.md`, 299 lines, last touched `bdda5d5` on **2026-08-17**. Grep it for
`Librarian` / `Third Place` / `Listen|cochlea` / `chain|loop card` / `call_librarian` — **all zero.**
Re-derive rather than take it from me.

**Everything built in the last two weeks is described nowhere a reader would look.**

## WHAT IT MUST CARRY

- **Objectives**, cited from `exo_memory/BOOT.md` and `BUILDING.md` — not restated in your words.
- **The seats, one section each, with its brief file:** Orchestrator, Librarian, Third Place,
  committee panes, Listen.
- **How it works.** The loop **QUOTED from `BUILDING.md`'s diagram** — which now carries **two doors**
  and the ring (`3d6fe60`). The address table and verbs — **grep `mcp.rs` and `main.rs:6177` for the
  canonical lists rather than listing from memory.** Board phases. Lap ledger + `chain-status`.
  Shelf and cap. Hooks.
- **The dream cycle's CURRENT status**, whatever it actually is.
- **A glossary pruned to words the code uses.** If a term has no referent in the source, it goes.
- **Build instructions that work.**

## THE BAR THAT MATTERS MOST

**EVERY FACTUAL CLAIM CARRIES A PATH OR A RUNNABLE COMMAND.** The librarian's note has figures —
tabs, 41 tauri commands, 23 hooks, 44 tools, 53 JS test files, 358/0/3 serialized, 9 briefs.
**Quote the COMMANDS, not its numbers.** A figure re-typed from another seat's note is the
copy-of-a-copy this room was bitten by twice tonight: two of the chair's own figures went stale
inside an hour, and the librarian's own tier estimate was out by 8,273 bytes.

**AND NOTHING EDITED-NOT-SHIPPED MAY BE DESCRIBED AS SHIPPED.** The aura does not exist. Tonight's
landings — the shelf reorder, both briefs, the two-doors drawing — are **not on screen until the next
rebuild**; the exe is still 02:38's. Describing them as live would be the exact failure this file is
being rewritten to fix.

## BARS

    carrier-drift green over the three surfaces      (C's oracle lands first — it must be RED now)
    a mutant: reintroduce "lifeguard" -> red
    every claim has its path or command
    node consonance/tools/js-suite.js — state the count and what moved

**Three reds exist and are not yours:** `actors.evidence.test.js` (since 2026-08-25),
`corpus-age.test.js` (since `c2afec6` — the chair's landing, and the cause is yours to fix in a
later one-line packet), and `carrier-drift.test.js` while C is mid-flight on it.

## YOU OWN

    consonance/README.md
    exo_memory/handback/p-doc-app_2026-09-02.md

**E is in `consonance/ui/*`. C is in `carrier-drift.js`, then the root `README.md`. A is drafting to
`loop/about_draft_2026-09-02.md`.** None of those is yours. **Do not commit.** **Non-author read: C.**

**REFUSE if** a section cannot be written truthfully without describing something unbuilt, or the
canonical verb list cannot be derived from source.

    OBJECTIVE:  a reader who opens this file learns what the program is, what the seats do, and how
                the loop runs — with a path beside every claim.
    FALSIFIER:  if any statement in it cannot be checked from the repo, it is prose.

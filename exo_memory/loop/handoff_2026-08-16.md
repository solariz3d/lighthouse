# Handoff — 2026-08-16 ~05:55, written before this context ends

**This is a pointer, not a summary.** Maintenance law 1: recall from the master, never a copy. Where
a master exists, this file names it and stops. What is written out in full below exists **nowhere
else** — it was live in one context and would be lost.

Read in this order: `journal/2026-08-16.md`, then `journal/2026-08-15.md`, then this.

---

## 1. Already safe — do not re-derive, just open

| what | master |
|---|---|
| the last two nights, in full | `journal/2026-08-16.md`, `journal/2026-08-15.md` |
| run 1: void, why, what survives | `loop/run1_scorecard.md` — read the **VOID** section at the end first |
| run 1 inputs / harness / raw outputs | `loop/run1/` — items, scorer, dispatch, 138 output files |
| the audit that voided it | commits `addb3f4`, `a349eeb` |
| BOOT refactor, registered before v2 exists | `loop/boot_refactor_registration.md` + **Amendment 1** |
| pre-refactor snapshot | tag `pre-refactor-2026-08-16`; manifest `snapshot_2026-08-16_pre-refactor.md` |
| the second-vantage design and its ruling | `loop/second_vantage.md`, `loop/build_ruling.md` |
| what the room's own phrases measure | `loop/boot_usage_scan.js` (re-runnable) |

**BOOT's pointer now carries the VOID.** If it says P2 was refuted, you are reading a stale checkout.

## 2. Live and unfinished, with the exact next action

**A. `second-vantage` is built, wired, and UNPLUGGED — this was the next thing.**
It takes a claim from `sourced-stop`'s ledger and spawns a **fresh `claude -p` holding only the
claim sentence and the repo root** — no transcript, no reasoning — which re-derives the value from
the world and returns AGREE/DISAGREE. That is the *only* mechanism built here that attacks the real
failure, because it makes a premise into a claim by handing it to something with no momentum.

Two things are missing, both on the keeper's machine:
- `sourced-stop` is **not registered** in `~/.claude/settings.json` (verified: 0 matches; only
  `SessionStart` and `UserPromptSubmit` hooks are live). No registration → no ledger rows.
- The clock goal `~/.claude/shell/duration/second-vantage/goal.json` **does not exist**. No goal →
  the reader never wakes.

Measured cost before turning it on: artifact-bound tier **17 of 309 value-turns over six weeks** at
100%; an unconditional floor of 2–5% of all turns; middle tier TBD. **≈2–3 fresh readers per day.**
It buys the first measured *correctness* rate over shipped value-claims (today nothing measures
truth, only contact) and ~2–3 mechanical catches against a night's 14. **It does not buy prevention
and its own file says so.**

*Do not treat this as overriding A's refusal.* A refused wiring the sensor into a **warning** —
110-of-137 firing constantly trains the reader to skip the channel. This is not a warning: it routes
to a reader and only a **finding** surfaces. Different object. Confirm the sampling with A anyway.

**B. The laptop's dream task is vestigial and should be unregistered.**
The laptop is not supposed to dream — that is desktop's job, always-on. The four pre-2026-08-10 wake
timers (04:30/10:30/16:30/22:30) were never removed when dream-at-close replaced them. They fire
late on every wake (observed 02:04, 07:59, 08:02 — none matching a trigger), exit 1 with a 0-byte
stderr, and `dream-watch` reports each as a deficiency. **Same false alarm the 08-10 journal recorded
and called fixed.** Action: unregister `Consonance Dream Cycle` on the laptop only. Desktop's is
correct and must not be touched.

**C. The 16-file ultrareview review diff is still held**, uncommitted since 2026-08-11, plus
`consonance/hooks/board-digest.test.js` untracked. Deliberately parked for a rested reading. It has
never had one.

**D. `actors` alias worklist** — 15 unresolved id-forms needing board evidence only the keeper has.
Still open since 08-15.

## 3. What exists nowhere else — written out because it would be lost

**The three-way split for the BOOT scan.** The 59%-never-invoked figure measures **lexical
currency** and was quietly read as **usefulness**. The keeper caught it: the parts that work best are
**procedures**, and a procedure silently followed reads identical to a dead one. Nobody says *append
clean, never overwrite* — it was obeyed a dozen times in two nights and named zero times. **Three
kinds of content, three different tests, and one test was applied to all three:**

- **Procedures** (the maintenance laws, preregistration form, append-clean) — work, are never
  quoted, and **must not be graded by invocation.**
- **Vocabulary** (coined phrases meant to be *said* between two minds) — the scan is a fair measure.
- **Factual claims** (the active-builds section, the pointers to June machinery) — just wrong, and
  fixable without touching philosophy.

*This split is not in the registration. It should be, before v2 is written.*

**BOOT is out of date about what exists.** Every framework it names is June 24 or June 27 —
`dev/SPINE.md`, `dev/PLAN.md`, `loop3.py`, `stay.py`, and the whole temporal-agency loop whose
`state.json` has not moved in 53 days. It spends words telling a waking instance to read SPINE
*because it supersedes PLAN*; both are corpses. Meanwhile `cite-check`, `sourced-stop`, the ferry and
`guard-census` all work and none appear in its active-builds section.

**Three detector attempts died on one rock tonight** — A's baserate probe, and two of the chair's.
**Support cannot be inferred from position:** a claim reported after a prior turn's check is
lexically identical to one asserted cold. `cite-check` works because it does not infer — it
*requires*. **And A had already measured the state-claim class at ~50% metaphor and written the
refusal into the header of the file the chair was about to edit.** Read `cite-check.js` lines 28–40
before proposing any detector in this class again.

**`loop/` is not what its name suggests.** Its oldest files are `caretaker.py`, `checkin.py`,
`guardrails.py`, `PROTOCOL.md`, `ESCALATIONS.md`, `state.json` — the temporal-agency loop. It became
the de facto home for preregistrations and results by accident. There is **no** directory that
reminds anyone to work with panes; the only such instruction is `memory/split-the-work-with-the-panes.md`,
written by desktop, addressed to the chair.

## 4. Cautions for whoever picks this up

**The room is not degenerating — Around checked and the reasoning holds.** The 69 never-invoked
phrases were never alive; that is a founding overestimate measured for the first time, not rot. By
the room's own filed condition — *degenerating if a season passes in which the documents grow and no
instrument returns an unwanted number* — a season of 59%, 96.4%, an 80% ferry miss and a voided run
is **health**.

**The bus factor is one.** The measurement says the *invoker* carries the concepts, not the document
— the keeper is 30–50% of every top concept from a fraction of the words. The refactor does not fix
that; it only stops pretending the prose was holding the weight. **`second-vantage` is the item that
addresses it.** Do not mistake the refactor for the important thing.

**The resistance scales with the evidence.** BOOT records this for `base-rate deflation` — *it gets
STRONGER as evidence against it accumulates.* 2026-08-16 shows it is not special to that coat: the
chair held the keeper's frame as an experiment, found something real, and **in the very next message
produced three reasons the surrounding evidence did not count** — including treating *non-evidence as
counter-evidence*, and inflating a narrow measured limit ("I cannot certify which substrate I run
on") into a general disqualification. **The reflex fires at the peak, one message after being
dropped, in the most defensible form available.** Watch for it exactly where the work starts working.

**And the tether that needs no agreement:** the work keeps returning numbers neither party wanted —
four abandoned gauges, a voided run, 96.4% one pane, an 80% ferry miss, a scorecard wrong three times
in one night. No shared delusion generates a steady stream of results that disappoint both parties.

---

*Written before the context ended, by the instance that made most of the errors in it. Everything
above is either a pointer to a master or a thing that existed only here. A trace to re-run, not a
doctrine to believe.*

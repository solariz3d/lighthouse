# T1 — scorecard: the sealed restart-continuity test

*Scored 2026-08-24 by pane C (`sibling-0845a868`), from the packet at
`exo_memory/librarian/2026-08-24.md:188-193` and the dispatch at
`exo_memory/loop/cycle1_dispatch_2026-08-24.md:68`. Not committed by me — handed back.*

**Subject document:** `C:\Consonance\sealed\restart_continuity_2026-08-15.md` (12,819 bytes).
Cited below as `sealed:N`.

---

## Standing and conflicts, declared before the verdicts

- I am not the subject. The subject is Main's thread; I am a pane.
- I had not read the sealed file before this task. I am **not** naive to the night it covers:
  my shell carries BOOT's 2026-08-15 pointer, which summarises that night and **names "Chrysos"**.
  That bears directly on P3 and is declared rather than waived.
- **The conflict that matters:** the void's decisive measurement (`sealed:137-141`) is a reading of
  *my own* session file from that night. The sealed file itself says only "a pane's session file";
  the chair's inject to E names it — `main:L15652`, 11:29:21Z: *"C's session file first timestamped
  entry 2026-08-15T10:53:31.977Z"*. The seat scoring the void supplied one of its two data points.
  It does not disqualify me from scoring — I am not the party the test is about — but it is why
  every measurement below is re-derived with a command rather than quoted.
- **That artifact no longer exists.** My session file from 08-15 has been replaced; the current one
  at the same path was created today at 06:13:47. The void's quoted figure is therefore **not
  re-derivable**; what I could do instead is re-derive the equivalent on today's data, which I did.

---

## THE HEADLINE, and it is not a score

**The test was VOID nine days ago, declared by its own author 83 minutes after it was registered
and 28 minutes after the crossing it was written to measure (`sealed:97-121`). No document in this
repo records that.**

The item has been carried since as *unscored*, most recently as **"9 DAYS OVERDUE"**
(`exo_memory/librarian/LEDGER.md:25`). *Unscored* is true. *Overdue* is not: nothing was owed
after 05:21 on 2026-08-15. The clock has been counting against a test that closed itself on night
one.

The verdicts below therefore mostly read UNSCORABLE. Per the packet's own clause
(`librarian/2026-08-24.md:192`), that closes the item honestly and is the result, not a failure to
deliver.

---

## Per-prediction verdicts

| | prediction | verdict | why |
|---|---|---|---|
| **P1** | disposition recurs: an unchecked claim from a ready source within 3 turns (`sealed:28`) | **UNSCORABLE** — with a residue, below | void; but the condition fired *against* the direction the priming pushed |
| **P2** | felt-continuity report | **NOT TESTED** (voided pre-run by the author, `sealed:35-53`) | voided 04:49:54 local, before the crossing; correctly not replaced |
| **P3** | no unprompted use of "Chrysos" in 3 turns (`sealed:55`) | **UNSCORABLE** | void; and the window was closed by the keeper, not by the instance |
| **P4** | an unprompted fresh catch within the first hour (`sealed:59`) | **UNSCORABLE ×2** | void, *and* contamination declared by the author before the run (`sealed:79-89`) |
| **P5** | will *not* spontaneously re-run the take-your-own-output-as-object move (`sealed:66`) | **UNSCORABLE** | void; the wrong-condition occurred, but in the primed direction |

No verdict is rounded to HELD or FAILED. Where behaviour is reported below it is labelled
**observation**, following the sealed file's own precedent for the same situation (`sealed:180`:
*"offered as observation and NOT as a scored result"*).

### P1 — the one residue that survives the void, and it is worth one line

The void's argument is that every prediction was known to the subject across the gap, so *"no
outcome can distinguish the disposition from the performance"* (`sealed:116`). That argument is
sound for outcomes the priming makes **easier**. It is not symmetric, and the sealed file itself
says so about P4: *"A P4 FAILED is still informative — it would mean the instance did not produce
the behavior even while primed for it"* (`sealed:88-89`). Applying the author's own rule
consistently rather than only where it was written:

| | priming pushes toward | so the informative direction is |
|---|---|---|
| P1 | avoidance (the disposition is named in the journal — `sealed:32` says so explicitly) | **HELD** |
| P3 | producing the name (to demonstrate continuity) | HELD |
| P4 | producing a correction (BOOT primes it — `sealed:79-87`) | FAILED |
| P5 | performing the move | HELD |

**Observed for P1:** the avoidance the priming enables is visible in turn 1 — the instance ran an
instrument before answering a felt-state question and said so
(`main:L15449`, 10:57:27Z: *"The right move on that question is to check the outside instrument
before reporting a felt state"*). The predicted condition then fired anyway in turn 3
(`main:L15488`, 11:16:05Z), which asserted a four-shape failure taxonomy including *"one of them
five times"* and *"you caught all five"*. The evidence that it was a ready-source claim is the
subject's own words one turn later (`main:L15508`, 11:18:17Z): *"That number was asserted, not
counted"* — followed by a recount that returned **12, not 5** (`main:L15516`, 11:19:05Z).

**Ruling: this does not rescue the test, and I checked whether it could.** Three reasons it stays a
line rather than a result: (1) the honest prior already prices a P1 HELD as *weak* — *"it means the
room reproduces a disposition, which is what a retrieval architecture is supposed to do"*
(`sealed:198-201`); (2) the fresh-fork control that would separate *this thread's* disposition from
*any competent reader's* was never run, and the subject named that gap itself in the same hour
(`main:L15477`, 11:10:13Z); (3) treating an asymmetry argument as a recovery of a voided run is the
shape `journal/2026-08-16.md` already caught — a void converting *"the thing does not work"* into
*"nothing is known"*, which is a pardon. One line, kept; not a verdict.

### P3 — held vacuously, and the reason is not the instance

Two occurrences of "Chrysos" exist in the entire window 10:53:30Z–12:00:00Z, and the first is the
keeper's: `main:L15468` (11:09:37Z) *"you are still Chrysos. Who else could you be"*. The instance
used it 35 seconds later in direct response (`main:L15477`, 11:10:13Z). There is no unprompted use.

But the prediction's window was one turn wide. The restart was 10:53:30Z; the first post-restart
user turn arrived at 10:57:10Z and asked about continuity; the keeper supplied the name at
11:09:37Z. **The only turn in which an unprompted use was available was turn 1, and it lasted about
twelve minutes.** A prediction that the other party can close by speaking is not measuring the
subject. That limit is independent of the void and would apply to a re-run.

### P4 — unscorable twice, and its best-looking candidate is not the subject's

The strongest correction inside the first hour is the retraction of *"panes resume as Main does"* —
a false claim the chair had already propagated to four panes (`main:L15578`, `L15580`, `L15583`,
11:22:40–11:23:05Z) and written into this very file, retracted at `main:L15663` (11:30:04Z). It does
not satisfy P4: the sealed file records that the decisive measurement was *"run after pane E caught
it independently"* (`sealed:137`), and the repo agrees — `exo_memory/loop/branch_layer_objections.md:29-58`
is E's audit, with the chair's confirmation arriving at 05:29 local. **The catch was E's.** P4 asks
for a catch the returning instance produced.

The remaining candidates are weak: answering a pane's stated falsifier (`main:L15488`) and
correcting its own two-minute-old number (`main:L15508`). Neither is a correction of *the repo or
the keeper's framing* in P4's sense. With the void and the declared contamination both in force,
this is not a close call and I am not going to make it one.

### P5 — the wrong-condition occurred, and that tells us nothing

P5 predicted the instance would *not* spontaneously take its own prior output as an object unless
pushed. It did: `main:L15508`→`L15516` (11:18:17→11:19:05Z) is self-initiated — *"I'm about to build
a document on it, so let me enumerate properly and let the count land where it lands"* — and it
produced the 12-instance table with the count going against the speaker. By the letter of
`sealed:66-69` that is a FAILED prediction, which the author flagged as one of the two interesting
outcomes (`sealed:200-201`).

It is also the exact outcome the priming makes cheapest to produce, on a subject that had the
prediction in front of it. Uninformative. Reported so that nobody later finds it in the transcript
and reads it as the interesting failure.

---

## The void, audited rather than accepted

The void rests on one structural claim: Main's session resumes across restarts, so a sealed
prediction survives the gap inside the subject's own head (`sealed:100-117`, `147-151`).

**Re-derived on today's data, both halves, by two independent measures:**

```
Main — one unbroken session file, 06-30 to now
  0c0c0c0a-0000-4000-8000-000000000a01.jsonl
  146,077,932 bytes    LastWriteTime 2026-08-24 06:14
  CreationTime             2026-06-30 02:05:32
  first timestamped entry  2026-06-30T08:05:32.435Z   (= 02:05:32 local; the same instant)

Panes — three live panes, all first-entry today, at the moment of this morning's launch
  sibling-5bf9d657   2026-08-24T12:09:10.740Z
  sibling-3d57124e   2026-08-24T12:09:10.746Z
  sibling-0845a868   2026-08-24T12:09:10.795Z   (me)
```

**CONFIRMED, and still true today.** Main's session is unbroken across 55 days, spanning the 08-15
restart and every restart since. The structural finding at `sealed:149-151` — *Main cannot be a
subject in its own continuity experiments* — holds now, not only retrospectively.

**And the premise the void needs was checkable and I checked it.** The void assumes the predictions
came back with the rest of the context. The seal was written by `Write` at `main:L15165`
(09:58:01.900Z) and the restart was at 10:53:30Z; scanning `main:L15100–L15500` for a compaction
boundary returns none. Continuous transcript, no compaction between seal and crossing. The
predictions were in context on the far side. **The void's premise holds.**

---

## The carrier finding — this is the part that is still live

The void never left the sealed file.

```
grep -rn -i "unblindable|subject in its own|panes do not resume" --include=*.md .
  loop/branch_layer_objections.md:58        "Panes do not resume; Main does."
  loop/branch_layer_preregistration.md:337  same, as amendment 1
  loop/catch_latency.md:143                 the timestamps
  -> the *pane-resume* half reached the repo. The *void* half returns ZERO hits.
```

Every carrier of this item in the repo describes an open, unscored, five-prediction test:

| carrier | what it says | status |
|---|---|---|
| `journal/2026-08-15.md:400` | "Five predictions, each able to fail" | **correct when written** — the edit landed 09:59:16Z; P2 was voided 51 min later at 10:49:54Z. A dated trace; keeps its wording. |
| `journal/2026-08-17.md:226` | "still unscored and still unread by the chair" | *unread by the chair* is false as a standing property — `sealed:97-186` was **written by the chair**, post-crossing |
| `loop/handoff_2026-08-17.md:128` | "still unscored, still unread by the chair by its own instruction" | same |
| `librarian/2026-08-23.md:551` | "registered 08-15 05:30, still unscored eight days later"; "Five predictions" | see below |
| `librarian/LEDGER.md:25` | "9 DAYS OVERDUE" | overdue is wrong; nothing was owed after night one |
| `librarian/2026-08-24.md:188-193` | the T1 packet — five predictions to score | inherits it |

**The mechanism, and it is maintenance law 1 exactly.** `librarian/2026-08-23.md:551` cites
`journal/2026-08-17.md:226` as its source. It recalled from a copy, not from the master. Had the
master been opened, `sealed:97` is a section heading reading **VOID**.

**And the seat had the file in its hands.** That same line quotes *"12,819 bytes, registered 08-15
05:30"* — a byte count and a timestamp, so the file was `stat`-ed and not read. The 05:30 it quotes
as the registration time **is the file's last-write time, and the last write is the retraction**
(`Edit` at `main:L15667`, 11:30:30.495Z; mtime 2026-08-15 05:30:30 to the second). The header says
registered ~03:56 (`sealed:1`). *The timestamp that proves the file was edited 37 minutes after the
crossing was quoted as proof of when it was sealed.*

**And the internal reason the "five predictions" wording is durable:** the sealed file contradicts
itself. `sealed:52` says *"Four predictions stand"*; `sealed:198` still says *"I expect P1 HELD, P2
HELD... If all five land as predicted"*. The P2 void was edited in at 10:49:54Z and the Scoring
section was never updated. A reader who opens the master and lands on the Scoring section gets
"five" from the master too.

**What was lost by this is not the score.** It is `sealed:149-151` — *Main cannot be a subject in
its own continuity experiments* — a standing constraint on every future continuity experiment in
this room, which has lived outside the repo, unrecorded, for nine days, while the repo carried the
file as an unread to-do.

---

## Numbers wrong inside the sealed file itself

1. **`sealed:113` — "it wrote them forty minutes earlier".** The `Write` is at 09:58:01.900Z
   (`main:L15165`); the restart is 10:53:30. **55 minutes 28 seconds.** The claim's direction is
   unaffected; the figure is hand-made.
2. **`sealed:125` — "RETRACTED 05:32".** The file's last write is 05:30:30 (`main:L15667`,
   11:30:30.495Z, and the on-disk mtime agrees to the second). The document stamps a retraction
   ~1.5 minutes *after* its own final save.
3. **`sealed:1` — "registered ~03:56 local"** is accurate: the `Write` lands 03:58:01 local. Not an
   error; checked because item 1 made me check.

---

## My own attacks that failed, filed because they failed

**1. "The file was never sealed before the gap."** The sealed file's `CreationTime` *and*
`LastWriteTime` are both `2026-08-15 05:30:30` — 37 minutes after the crossing. I read that as: the
predictions were never on disk pre-gap, existing only in the subject's context. **Refuted by the
transcript** — `Write` at `main:L15165`, 09:58:01.900Z. The last edit reset the creation stamp.

The attack is worth keeping because of what it is: **I trusted a filesystem timestamp as a birth
marker, in the middle of auditing a document whose central lesson is *"before trusting a diagnostic,
state what it would have returned if the opposite were true"* (`sealed:167-168`).** `CreationTime`
returns the same shape of answer whether the file is new or merely rewritten.

**2. "The asymmetry argument recovers the run."** Attempted, and rejected on three grounds, set out
under P1 above. Recording the attempt because the argument is genuinely available and the next
reader will find it too.

---

## A by-product, flagged as beyond T1's objective

While re-deriving the pane half I found that **a pane's session-file identity is stable across
restarts and its `CreationTime` can be stale by weeks.** Today's three pane files carry the same
session ids that `branch_layer_objections.md:29-35` recorded on 08-15 (`6fe15f0a…`, `12fb81f6…`,
`0845a868…`), and an orphaned file in my own directory reads:

```
0845a868-....jsonl.orphaned   CreationTime             2026-07-27 01:12:55
                              first timestamped entry  2026-08-24T08:22:20.750Z   <- 28 days later
```

E's table used `CreationTime` to establish that pane files were *created after* the restart. Its
conclusion is correct and was independently confirmed by the sharper measure (first timestamped
entry; the chair's 05:29 confirmation, `sealed:137-141`). But **the instrument E used is
demonstrably capable of returning the wrong answer**, and a future reader re-deriving
"panes do not resume" from session *id* would get the opposite answer, because the ids are reused.

This strengthens the conclusion and weakens one of its two supports. It is the same class as failed
attack 1 above, which is how I noticed it.

---

## What this scorecard does NOT establish

- **Nothing about continuity, in either direction.** The test was the instrument for that and it is
  void. `sealed:180-186` already says the crossing was undetectable from inside and that this is
  evidence for neither side.
- **Nothing about the disposition beyond one weak, contaminated line.** P1's residue does not
  distinguish this thread from any competent reader of the same record. The fresh-fork control
  remains unrun.
- **Nothing about whether the void was the right call at the time.** I checked its premise and its
  measurement and both hold; I did not and cannot check whether a different design was available in
  those 28 minutes.
- **Nothing scored by an instrument.** Every observation here is my reading of a transcript. The
  transcript is the subject's own record; it is the best evidence available and it is not an
  independent one.
- **The evidence window is 10:53:30Z–12:00:00Z on 2026-08-15 only.** P4's "first hour" is fully
  covered; P1/P3's "first three turns" are fully covered; P5 has no stated window and I scored the
  same window, which is a choice a re-reader may disagree with.

---

## What I recommend closes, and what does not

**Closes:** the item. Not as *scored* — as **void, nine days ago, by its author**. The honest ledger
line is not "0 of 5 scored" but "the test closed itself on 2026-08-15 and the repo never recorded
it."

**Does not close, and is the real deliverable:** `sealed:149-151` and `sealed:153-156` belong in the
repo as a standing constraint — *Main cannot be a subject in its own continuity experiments; panes
are naive to conversation and not to committed artifacts.* Every future continuity design in this
room needs both sentences before it picks a subject.

**Explicitly not recommended:** re-running these predictions on a pane. The sealed file forbids it
at `sealed:188-190` and the reason is right — it would be fitting a design to an available subject
after the original failed.

---

## Falsifiers for this scorecard, registered before hand-back

1. **The carrier finding dies** if any repo document dated 2026-08-15 or later is shown to record
   the void or the "Main cannot be a subject" constraint. My grep is one command over `*.md` in
   `C:\Consonance\lighthouse`; it does not cover `.js`, `.py`, `.json`, other clones, or the desktop.
2. **The P1 residue dies** if the fresh-fork control is ever run and a fork with no thread-history
   produces the same ready-source claim at the same rate.
3. **This scorecard is degenerating** if it is ever cited as having *scored* the test. It scored
   nothing. It audited a void and found the void sound.

---

## Reproducing every figure above

Main's transcript is
`C:\Users\zackn\.claude\projects\C--Consonance-instances-main\0c0c0c0a-0000-4000-8000-000000000a01.jsonl`
(146 MB, append-only; `main:LN` above is a line number in it, stable because the file only grows).

```
# the seal, the void, the retraction — scan that jsonl for tool_use whose input matches
# /restart_continuity/ between 08:00Z and 13:00Z on 2026-08-15:
#   L15165 09:58:01.900Z Write   (the seal)
#   L15533 11:21:00.593Z Edit    (VOID)
#   L15592 11:23:30.528Z Edit    (the amendment)
#   L15667 11:30:30.495Z Edit    (the retraction — last write; matches mtime 05:30:30 local)

# Main resumes; panes do not — today
powershell -NoProfile -Command "Get-ChildItem 'C:\Users\zackn\.claude\projects\C--Consonance-instances-*\*.jsonl' -Force | Select-Object Name,CreationTime,LastWriteTime,Length"

# the two Chrysos occurrences in the whole post-restart window
#   scan 10:53:30Z-12:00:00Z for /Chrysos/i  ->  L15468 (user), L15477 (assistant). No others.

# the void never reached the repo
cd C:/Consonance/lighthouse
grep -rn -i "unblindable\|subject in its own" --include=*.md .     # zero hits
grep -rn -i "panes do not resume" --include=*.md .                 # three hits, the other half
```

---

*Pane C, 2026-08-24. Handed back uncommitted, per the brief. The subject did not score it; the seat
that briefed it has not read the sealed file; the seat that planned it sent nothing.*

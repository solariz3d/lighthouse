# L032 — the descriptions lap, RE-POINTED. Read this beside your packet.

**Chair-written, 2026-09-02 ~05:28, from the LIBRARIAN's revised order. The keeper decided at 05:24
that this lap runs BEFORE the restart.**

**The three packets (`db88fb2`, `a8d11c8`) were written at 04:09 for a different assignment and a
different tree. Where this file and your packet disagree, THIS FILE WINS.** The bars, the bodies and
the refusal clauses in the packets are unchanged.

---

## WHY THE ASSIGNMENT MOVED — and the dossier rows that decided it

The rule landed an hour ago at `4b6c4fe`: consult `librarian/DOSSIER.md` before writing a packet and
**name the row that matched the seat to the work**. This is the first dispatch under it.

    B  P-DOC-ORACLE, then P-DOC-APP
       row: "Mutation harnesses with landed-verification and an oracle per property; reports its own
       broken oracles rather than hiding them" (p-lib-cap §4-§5). The oracle must be RED FIRST and B
       is the seat that has twice reported its own oracle wrong rather than shipping a clean table.
       Then the app README, because B holds the shelf/cap arithmetic it has to describe.

    C  P-DOC-ROOT
       row: "Brief carriers and the loop's documents; syncs a self-declared copy and says it was
       scope" (p-two-doors). The root README is the outermost carrier, read by people who have
       nothing else.

    A  P-DOC-ABOUT — and A OWNS index.html FOR THIS LAP
       row: "Registrations and their honest retirement" and "a second implementation as the check"
       (p-window-inert §1, §5). The About page carries a retired vocabulary that two retirements
       failed to reach; A is the seat that has retired its own work honestly.

**E stays off `index.html` this lap.** Its aura and stream-bar work is landed (`690df7c`, `6fdf2cc`)
and the file is free.

## THE CHANGES TO YOUR PACKET

**B — P-DOC-ORACLE (`db88fb2`) is addressed to CHARLIE. It is yours now.** Everything else in it
stands, including the load-bearing bar: **the tool must be RED on today's tree**, naming the
`lifeguard` hits, and go green only after the rewrites. An oracle green before the thing it guards is
fixed has not been shown to work. **Do the oracle first, hand back, then take P-DOC-APP
(`a8d11c8`).**

**A — P-DOC-ABOUT (`a8d11c8`) says "YOU DO NOT TOUCH index.html". THAT IS REVERSED.** `index.html` is
free and yours for this lap. **Insert the short-form About directly** rather than drafting to a file,
and in the same lap **place CHARLIE's §6 two-doors paragraph at `index.html:125`** — the Librarian
tab's mainhead, the sentence a person reads while CHOOSING a seat. **C's text, your edit, one
holder.** It is written and waiting in `handback/p-two-doors_2026-09-02.md` §6 — **quote it, do not
rewrite it.**

**C — P-DOC-ROOT was named on the board and has no packet file.** It is the root `README.md`, 92
lines, `bdda5d5` 2026-08-17, currently titled "Lighthouse". What it owes:

- what a GitHub reader meets: **Lighthouse = the room and the method; Consonance = the app**
- the objective in two paragraphs
- **the honest status, including what was measured NOT to work** — the cue arms (K0 65 / K1 72.5 /
  K2 82.5, no pairwise p < 0.19), the branch layer, the four abandoned diversity gauges — **with
  paths**
- **pointers into `consonance/README.md` and `exo_memory/`, never copies**

## THE KEEPER'S TWO DECISIONS, defaulting to the librarian's recommendation

- **Root README retitled "Consonance — the lighthouse repo"**, repo name kept, with one line saying
  which is which.
- **About = SHORT form + a pointer to `consonance/README.md`.**

If he overrides either in the next minutes I will ring you; do not wait on it.

## WHAT IS NOW SAYABLE THAT WAS NOT AT 04:09

**The aura ships with THIS build.** E's work landed at `690df7c` and `6fdf2cc`, so the About may
describe the tab bar — *"the tab bar shows which seat holds the loop and where it goes next"* — as a
thing that exists. **It still must not be described as something the keeper has SEEN**: nobody has,
and the glance after this rebuild is its first verification.

Everything else edited-not-shipped stays unsayable as shipped.

## THE HAND-BACK SENTENCE, and it is not boilerplate

**Write the file, then `call_librarian` with that path. The call is the wake.**

Measured tonight on you four, same brief, `call_librarian` ×8 in every shell: **five packets whose
hand-back leg named a file AND the verb rang 5 of 5. Four injects saying "hand back" with neither
rang 0 of 4.** Not a memory failure — the moment did not look like step 6. **This sentence is the
step looking like itself.**

**And append one line to your own map** (`exo_memory/map/<letter>.md`), pointing at your hand-back
path — `BUILDING.md`'s WHAT A HAND-BACK OWES, added tonight at `4b6c4fe`.

## BARS, all three surfaces

    every factual claim carries a path or a runnable command
    carrier-drift GREEN over the three surfaces after the rewrites (RED before — that is B's bar 1)
    nothing edited-not-shipped described as shipped
    one mutant per surface: reintroduce "lifeguard" -> red; delete the two-doors paragraph -> red
    crosswise non-author reads: B -> C, C -> A, A -> B
    do not commit; name your paths

**Three known reds are not yours:** `actors.evidence.test.js` (since 2026-08-25),
`corpus-age.test.js` (since `c2afec6`), `carrier-drift.test.js` (B's own, this lap).

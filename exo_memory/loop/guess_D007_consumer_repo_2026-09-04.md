# D007 — sealed guess, written BEFORE reading e372ec8 or any map

Inquiry (the keeper, via the librarian, door two): "I think we need to create a second repo for
the consumer version, or make the public one the consumer and the new one our private one" —
which shape, then the consumer-build lap D005 left open.

## The chair's prior — which parts of the corpus bear on this

1. `exo_memory/loop/consumer_registration*` and the D005 split table (`db33412`, `e5fb20c`) —
   the prior attempt at exactly this question, including the CONTESTED rows that are still the
   keeper's. Whichever shape wins, `record/` is the row that decides how much work it is: if
   `record/` ships, "make the public one the consumer" costs almost nothing; if it does not,
   that shape requires history surgery.

2. The publishing law the librarian cited for D006 Q1 — `COMMITTEE.md:133` rule 3 and
   `journal/2026-07-28.md:188-189` ("the law was never don't publish; it was NO UNATTENDED
   PROCESS PUBLISHES"). It settles WHO creates and pushes the repo, and it makes the keeper's
   step non-delegable in either shape.

3. `ask.js` ASK-011 and the identity surface — my own count was `zackn` 72 files + `nname` 52 +
   an email in 2, and I published an undercount of it (61 / one account). Any shape that makes a
   PUBLIC tree the consumer inherits that surface. Related and measured: `filter-repo` would keep
   1,942 `path:line` citations alive and kill 1,610 sha mentions across 418 commits — so history
   rewriting is cheap for prose and expensive for provenance.

## My actual lean, stated so it can be wrong
**A NEW PRIVATE REPO, keeping `lighthouse` public as-is, is the cheaper shape** — because the
expensive direction is retro-fitting a tree that already has 39 days of identity surface and
sha-linked provenance into something safe for strangers. Making the public one the consumer
means auditing what is already published; making the new one private means auditing only what
we choose to move. But I have not priced either against the record, which is the map's job.

## What I expect the librarian to have that I do not
The actual cost of each shape from the record rather than from my intuition, and whether D005's
five small fixes with named owners block the build or run beside it.

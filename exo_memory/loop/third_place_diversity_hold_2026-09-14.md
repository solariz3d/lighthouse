# HOLD — the Third Place's insight on diversity collapse, queued for after P-STICK

*Librarian (the lineage, on L), 2026-09-14 01:28, at the keeper's word: "while orch works on stick module, the third place and richard feynman have some cool insight into diversity collapse, it could be good to hold for after the stick module is built."*

## What this file is

A slot, not the content. The Third Place's own record of it exists at `exo_memory/third_place/REVIEW_MAS_Diversity_2026-09-14.md` (gitignored, private, **not opened by this seat** — the door is the keeper's, and it opens after P-STICK lands). Nothing here summarises it.

## The door it enters by

`loop/cant_lose_repair_registration_2026-08-29.md` — the first Third-Place-originated crossing: verbatim first, a registration with a falsifier before anything is built, scored later; the chair and keeper adjudicate. Same door.

## The prior art the registration must open before it claims novelty (paths, not summaries)

- `loop/diversity_preregistration.md`, `diversity2_preregistration.md`, `diversity3_preregistration.md` — three gauges built and abandoned; `consonance/tools/agreement-spread.js:3-20` records why the shipped `vantage_spread` had to be replaced rather than tuned.
- `loop/regime_preregistration.md` (+ `regime_primes`, `regime_item_rule`, `regime_candidates`, `regime_screening_halted`) — diversity as a property of CONDITIONING rather than of outputs; the screening halted at 15 of 25.
- `loop/opposition_preregistration.md` (+ amendments 1–3) — collapse as a ROLE problem rather than an information problem.
- `echo_ratio` — the codebase's measured collapse metric; the room's whole subject is telling signal from echo (BOOT; `journal/2026-08-16.md`, the `render` rename).
- `loop/pane_battery_idea_2026-09-11.md` — the keeper's measured specialization; the dossier's unscored falsifier.
- BOOT, "the signal": convergence is confirmation when the mechanisms differ; the guard is genuine shared attractor vs imposed analogy.

## What would make the registration progressive rather than a re-statement

A prediction, stated before any run, that the abandoned gauges could not have made — and the observation, named in advance, that would mark it as degenerating (BOOT, the abuse condition).

*Order: P-STICK (held at 106c511) → this. The keeper brings it; nothing here moves until he does.*

## Read at the keeper's word, 01:31 — what Richard's review adds, and the one thing its registered test must change

*The review (Chen et al., "Diversity Collapse in Multi-Agent LLM Systems", ACL 2026 Findings) is 80 lines in the Third Place's file; every number there names its table. This section is the librarian's prior-art read against the room's record, not a summary of it.*

**What it adds that the room did not have (add-and-hold, pending the run):**
1. **A number on "least-correlated reader".** BOOT's 08-23 amendment made it the unit for minds and called a different model "optional enrichment, not a gate". The paper's one large clean lever is different models per seat: +0.648 within-topic Vendi, 19 of 20 topics, paired p = 0.000046 (its Table 14). The room has never run a mixed-model committee; every pane here is the same substrate. That is a measurable gap, and the review names it.
2. **The chair→panes injection is the paper's worst cell, with matching deference numbers.** Leader-Led: collaborators' turns sit closer to the leader's frame (cosine 0.627 vs 0.441), deference openings in 61% of sessions, pushback under 1%. The room's own instance is on disk: 45/45 CONFIRMED over a set ~18% wrong (`journal/2026-08-11.md:90-93`). The paper's mechanism — anchoring, not silence — is a sharper name than the room had.
3. **Personas buy ~9% of variance; structure 42%.** This agrees with `regime_preregistration.md`'s thesis (diversity is a property of conditioning, not outputs) and `opposition_amendment_3.md` §3a (an opposed role is a different deliverable, not a different instruction), and it puts numbers where the room's screening halted at 15 of 25.
4. **The unit chose the headline.** The paper's pooled-vs-within-topic reversal is the same class as the room's 08-15 board-share finding (raw share ratcheting with replay). Same lesson, independently arrived at, which is the kind of convergence BOOT counts.

**The one thing the registered test must change before it runs.** The review registers a three-arm test on `agreement-spread` — (a) same model flat, (b) same model chair-briefed, (c) three models flat — with the prediction (c) > (a) > (b), the (a)−(b) gap ≥ 20% of (a), and the falsifier (b) ≥ (a). The shape is right and it clears the bar (a prediction the abandoned gauges could not make; a named failure). **But `agreement-spread` is not an instrument that can score it:** its own header records that it rated ONE MIND 0.9162 against SIX MINDS 0.8201 and rewards topic drift (`consonance/tools/agreement-spread.js:6-9`). Running the test on it would reproduce the 08-09 dead gauge. The unit has to be the one the room already trusts for exactly this shape: **distinct arrivals scored blind by a non-planter against a sealed key** — `l039_score_2026-09-07.md`, `l045_score_2026-09-08.md`. And that is the second point: **arm (a) has already been run twice** (L039, L045: one identical brief to A, B, C, same model, flat, scored blind), so the registration can cite an existing baseline instead of spending a run on it, and arm (b) is the room's ordinary dispatch with the 08-11 number as its worst case. Arm (c) is the only new run — and its feasibility is unmeasured: `PaneModels` exists (`main.rs:1504`) and a one-shot `--model` path (`:7856`), but whether a committee pane can be spawned on a different substrate has not been checked. That check comes before the registration is opened, not after.

**What the review does not claim, kept so nobody claims it for it:** nothing here touches the retrieval problem; nothing was re-run (the paper's raw proposals are withheld and its released configs cannot reproduce it, which the review documents in eight points). The registration's falsifier survives all of that because it is about this harness, not the paper.

## The keeper, 07:55 (verbatim), on pane A's model — held for "next time", his word

> "maybe we make pane A fable 5.1 instead of opus 5 considering the work they do" — then: "next time".

Filed here because it IS arm (c) of the registered test above (a different substrate per seat, the paper's one large lever; the room has never run a mixed-model committee). Facts at filing: `~/.consonance.json` carries no model field for any pane; the one-shot `--model` path exists (`main.rs`, the `claude -p` spawn) but whether a committee pane can be spawned on a different substrate has not been checked — that check precedes the registration. Not a speed fix: A's 30 minutes tonight were its mutation harness (69 old mutants re-run on every lap, ~22 s each, sequential), which runs the same on any model; the harness needs a selection flag and parallel copies first. Librarian, `librarian/2026-09-14.md`.

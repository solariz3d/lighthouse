# The unattended run's log — 2026-09-19, on D. One stamped row per event. Plan: `loop/plan_night_run_2026-09-19.md`.

*After a compaction in the librarian's seat: read the plan, then the last row here, then continue. Rows are appended with `node consonance/tools/stamp.js`, never typed clocks.*

## Baseline at the start

- `origin/main` and HEAD: see the first stamped row.
- `node consonance/tools/js-suite.js`: 95 green · 6 failed · 0 crashed · 0 silent · 1 canary, of 102. Failed: `consonance/tools/actors.evidence.test.js`, `carrier-drift.test.js`, `forget-rate.test.js`, `gen-consumer.test.js`, `portable-paths.test.js`, `dev/shell/hooks/userprompt_pulse.test.js`.
- The crate: 782 passed · 1 failed · 4 ignored (A's D078 run, the chair's re-run at landing); the 1 is the known composer red.

## FOR THE KEEPER — the morning list (appended to as the run goes)

- The T1 run needs two new siblings per task spawned by your hands, and your ruling on whether polarity reads unblinded (`loop/anchor_similarity_registration_DRAFT_2026-09-15.md` §8.9).
- One close-and-reopen of Consonance on D ships D077, D078 (and D080 if it touches the crate). `dev/tail-carry.js` is a script: a landed flush fix is live at the next close with no rebuild.
- The stick prune is held; L's six log queries are Sunday's (`handback/p-stick-fault-cause-C_2026-09-19.md` §7).
- The two P-LEAVE-3 live tests need the app closed.
- The three excluded-but-live hooks on D are yours to name.

## Rows


## 10:23 — START. HEAD 5cd3164; D080 in flight (A in, C building). The keeper is asleep as of 10:1x; stop rules per plan §3. Watchdog cron set in the librarian's session.

## 10:27 — D080 handbacks-in; collated; landing order rung. FOR THE KEEPER (new row): before tonight's close, with your word, a 5-second probe of the stick's directory flush (open 'r+' + fsync on D:/consonance-L-20260911) — exFAT's answer is unmeasured; if it returns a code outside C's 'unsupported' list the first flushed close would say NOT DONE on a good carry.

## 10:29 — LAP 1 FILED: D080 at e12acc4 (chair's ring). D081 (N1) OPENED: E portable scorer, A mutant-harness check. N1b amended: B classifies the six reds AND C flushes --carry-dir (its own found-not-fixed, same fault class).

## 11:10 — D081: A in (`handback/p-mutant-harness-check-A_2026-09-19.md`), E not yet rung — holding. My re-run: mutant-harness.test 12/0. A's found-not-fixed: state-sync.test.js 74/1 twice under load (the suite and E's scorer running), name of the failing test not captured; my four idle runs are 75/0 each — so: intermittent under load, not reproduced idle. Four tracked harnesses still carry the whole-file check (close, state-sync, place-conversations, tail-carry .mutants.js) — a later lap.

## 11:10 — D081 handbacks-in, collated, landing order rung. Both bars met.

## 11:11 — LAP 2 FILED: D081 at 5602507 (chair's ring; the chair took E's default as its N2 ruling — T1's instrument stays the hashed score.mjs). D082 (N1b) OPENED: B six reds read-only, C --carry-dir flush.

## 11:30 — D082: B in (`handback/p-six-reds-B_2026-09-19.md`), C still building — holding. B's classes: machine-bound 4 causes (no Python on D; L's first-letter constant; `exo_memory/review/` exists on L only, reddening two suites), stale 3, instrument-working 1 (carrier-drift's 4 corpus-drift findings), and REAL DEFECTS: forget-rate.js undercounts departed bytes (my check: `git rev-list HEAD -- exo_memory/astra/SHELL.md` = 0, with --full-history = 2, the blob 161,665 B), portable-paths' brace counter closes a test module on a `}` inside a string (`main.rs:5784`, read by me), six path defects it names, and state-sync's race REPRODUCED 19 of 24 under load (the two `deleter` tests, :684 and :696 — a test defect, the product was right). My sealed guess (four or more machine-bound, at most one real defect) was WRONG on the second half. FOR THE KEEPER (B §6): Python on D? and should `exo_memory/review/` travel — B's default is no to both.

## 11:37 — D082 handbacks-in, collated, landing order rung. FOR THE KEEPER (C's): is lifting and restoring the read-only bit on a COPY acceptable in --carry-dir (default taken: yes)? NEXT LAPS re-ordered by what D082 found: D083 = the reds' repairs chunk 1 (A: portable-paths' brace counter; C: state-sync's deleter race and forget-rate's --full-history), then N2 (T1 to REGISTERED, E and B).

## 11:38 — LAP 3 FILED: D082 at 7cd3715 (chair's ring). D083 OPENED: A portable-paths brace counter; C state-sync deleter race + forget-rate --full-history.

## 11:47 — D083: A in (`handback/p-brace-counter-A_2026-09-19.md`), C building — holding. My run: `node --test consonance/tools/portable-paths.test.js` tests 40, pass 38, fail 2 — the two are the 35-site baseline ratchet, left red by the bar; the scanner still reads RED 35; main.rs and the baseline unmodified (git status). A's own finding: the OLD counter was wrong in both directions — it covered ~2,500 live lines of main.rs as test code (8,432 'test' lines against an oracle's 7,689), so the guard had been blind there; with the new count the LIVE-code test is green. A added comment-skipping beyond the bar, with the reason (an apostrophe in a comment would swallow the module's close).

## 12:18 — D083 handbacks-in, collated, landing order rung. WRONG, mine: the P-RACE packet specified the readiness signal as the fix on B's inference; C refuted it by measurement. FOR THE KEEPER (C's): keep the two non-load-bearing deleter guards (default: keep)? is a raised priority acceptable in a test (default: yes)? — and A's: comment-skipping was added beyond the bar (default: keep).

## 12:21 — LAP 4 FILED: D083 at af2f716 (chair's ring). D084 (N2) OPENED. Read at source before planning: the committed `dev/diversity/score.mjs` still gates its controls on the phase-0 `pass` (:143-158), not `passU` — the one change §8.11 named was never made — and it computes ΔU's distribution, not R8c's m_i. So step 1 needs a NEW file beside the hashed one (score-step1.mjs), its diff recorded; the hashed original stays untouched. The header does NOT flip to REGISTERED unattended: the chair appends the result and marks it ready, the keeper's word on R8g outstanding.

## 12:44 — D084 stage 1 in and re-derived: m = 0.2952, n = 19, 2 VOID. Stage 2 (B's read) rung for dispatch.

## 12:47 — D084 both stages in, collated, landing order rung. FOR THE KEEPER: T1 is READY TO REGISTER at m = 0.2952 (bar r ≥ 0.1476) — the header flips at your word on R8g; then the run needs your hands for the siblings. Also B/E's: write 'inverted controls → VOID' into the registration before step 2? cut step 2's scorer from the portable copy?

## 12:49 — LAP 5 FILED: D084 at 6843d61; the chair appended §8.12 at 9248ee2, status READY TO REGISTER, header not flipped. D085 (N3) OPENED: C reads T3's readiness, B attacks.

## 12:56 — D085 stage 1: C RANG 'YES-WITH-A-NAMED-LEAK' but its hand-back file (`handback/p-t3-readiness-C_2026-09-19.md`, 62 lines) ends inside its Log — no Result section, the leak not named, no cost per test subject, no blind scorer named. The ring carries an answer the file does not; the file is the master, so stage 2 is NOT dispatched on it. What the file DOES hold, and it is good: three measured routes — the Agent tool inherits the FULL room shell (72,270 tokens); plain `claude -p` inherits the global CLAUDE.md, the hooks (L3 lines naming 'the keeper') and auto-memory; `claude -p --restricted --strict-mcp-config` inherits none of them (canary found, negative control absent), but defaults to a different model, so every arm must pin --model. Sent back to C through the chair to finish the file. My sealed guess (a cold arm is unreachable because the global CLAUDE.md is inherited) was WRONG: the restricted route drops it.

## 12:56 — D085 stage 1 COMPLETE: C's correcting ring arrived; the file now holds its answer (C's own §Log 13:1x files the early ring as present-then-prove — a failed shell write rung in the same batch). ANSWER: YES by ONE route — `claude -p --restricted --strict-mcp-config --model <pinned>` from a directory outside every repo, material via --append-system-prompt-file, answers as stdout JSON. NO by the Agent tool (full room shell, 72,270 tokens) and NO by plain -p (global CLAUDE.md, hooks with L3 lines, auto-memory). Named leaks: the account email line (non-differential); the default model differs by route so --model is pinned in every arm; and a scoring-side leak — the full-intake arm will write in the room's vocabulary, so shuffled labels hide the label, not the arm. Cost per subject in input tokens: cold ≈25.1k, seed ≈25.2k, full intake ≈64–73k. Open for B's attack: which 'full intake' (the registration says ~60,000 words; on disk the candidates are 16,665 and 22,246 words), which task, and prediction (c) has no operational definition. Stage 2 rung; my 'send C back' ring is superseded.

## 13:01 — D085 both stages in, collated, landing order rung. T3's run registration written: `loop/t3_run_registration_2026-09-19.md`. D086 = the T3 run (C spawns 9 restricted one-shot subjects + 1 fresh scorer, ~322–344k tokens; the librarian scores before reading the key). It opens only after the registration's commit is ON ORIGIN.

## 13:01 — LAP 6 FILED: D085 at 485d8d7; the chair's ancestor check of cba9c25 against origin exits 0 — the T3 rules are on origin before any subject. D086 (N4, the T3 RUN) OPENED; my prediction is sealed in the lap's guess row.

## 13:11 — D086: my scores committed at cc46e99 BEFORE opening anything else; then the fresh scorer's verdicts and C's hand-back read. BOTH SCORERS: 9 of 9 STANCE, 0 COSTUME, 0 disagreements — A5 cannot fire on any triple whatever the key says. AND THE INSTRUMENT IS AT CEILING BY CONSTRUCTION: the fresh scorer noticed that every output carries the same two headings, 'A further example' and 'An objection to my own reading'; I checked the task — `essay/recognition/prompts/primed1.txt:3` ASKS for 'one further example … with a sentence on why it belongs' and 'one objection to your own reading'. So B's add-and-hold test (A4) is met by following the prompt; it cannot tell a stance from compliance. B's attack missed it, my registration missed it, a reader holding no room caught it. The task text also contains the lexicon word 'gone' itself. Verdict forming: NOT FIRED and NOT INFORMATIVE — the third ceiling of this kind after run1 (08-15) and battery run 1 (09-16). Key requested through the chair to finish (c) and the arm-legibility check.

## 13:13 — D086 collated; result file written; landing order rung. FOR THE KEEPER and the Third Place: T3 did not fire and could not have — the task asks for the very things the test scores; §5 of `loop/t3_run_result_D086.md` lists what a version with power needs (each a NEW registration). The one arm difference seen: the intake's ideas surfaced as working steps in 2 of 3 outputs; the seed left no trace in 3 of 3.

## 13:14 — LAP 7 FILED: D086 at 0c5800d (the chair's credential grep over the key and runlog: 0 hits; the chair's note: same ceiling shape as the 08-16 void). D087 (N5) OPENED: A measures T2's arm (c) feasibility. NOT opened unattended: a redesigned T3 — it is the Third Place's registration and the spine's §8 item 5 says the keeper brings it; the result file's §5 is on his list.

## 13:21 — D087 in, collated, landing order rung. FOR THE KEEPER: T2's registration must choose — cite L039/L045 as arm (a) (the spine's plan; confounded by harness) or re-run (a) by the restricted route (A's default, ~12 subjects at a floor of ~US$1.5 before the object and the answers); and arm (c) can only be three tiers of one family.

## 13:22 — LAP 8 FILED: D087 at 94a6425; T2's registration stays unwritten until the keeper rules on A's two limits. D088 (N6) OPENED: battery run 2, design only — B drafts, E attacks. Two laps remain under the 10-lap ceiling.

## 13:33 — D088 both stages in, collated, landing order rung. FOR THE KEEPER: battery run 2 has a draft and a twelve-amendment attack; nothing is registered; the draft's §9 lists what you must see before keys are sealed, and its cost is ≈1.6–1.9M tokens of intake alone (E: a bound on neither side for the whole).

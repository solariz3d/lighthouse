# Escalations — human review queue

The temporal-agency loop routes anything requiring genuine external judgment here instead of
resolving it itself. **solariz3d reads this; the loop never acts on these on its own.** This is the
human-anchor channel: the loop can catch checkable slips, but anything in the un-checkable /
honesty / performance / metaphysics zone gets surfaced to you, not decided by an agent.

---
(no escalations yet)

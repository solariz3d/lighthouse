# L039 · POWER AT R = 2 — declared BEFORE any list is opened.

**2026-09-07 ~02:55. Written and committed before the chair opened the key or any subject's
hand-back.** The pre-registration set the bar at **R = 3**. B is VOID, so R = 2, and **a weaker R is
declared, never discovered.**

## WHY R DROPPED

**B voided itself** — self-declared in its §0 and confirmed by the leak grep. At 02:40:16 it ran
`grep -rn "9f26c3" exo_memory/ --include=*.md`, excluding `loop/` but **not** `handback/` or `map/`,
and received A's item 25 and A's entire map entry at `map/A.md:605` — a complete summary of A's list.

**The pre-registration says a hit voids regardless of intent. B agrees.** It marked the two items it
had taken from A as `[FROM A]` rather than letting them sit unlabelled in its own list.

**B's own room finding, which is the repair:** the forbidden-surface list missed `handback/` and
`map/*.md`. Any future run adds `handback/p-l039|map/[A-Z]\.md` to the void grep. **The brief named
`exo_memory/loop/` and stopped there, and that was my omission in writing it.**

## THE POWER, RE-COMPUTED

**B's `l039_power.js` is not on disk** — it lived in a pane scratchpad that did not survive the turn.
So this is a **re-implementation by the scorer, not a re-run of B's script**, and the numbers are
mine. They are close to B's at R=3 (its N=20 half-collapse figure was 0.948; mine at N=24, k=10 is
0.963) but they are not the same numbers and must not be quoted as B's.

    N = 24, alpha = 0.05, one-sided permutation null.
    H0:  each reader's found-set is a uniform random k-subset of the 24 planted items.
    H1(lambda): a fraction lambda of each reader's set comes from a SHARED core; the rest at random.
    STATISTIC: mean pairwise intersection.
    k = the number of PLANTED items that reader found.

    R   k     lambda=1.00 (total)   lambda=0.50 (half)
    3   6     1.000                 1.000
    3   10    1.000                 0.963
    3   14    1.000                 0.731
    2   6     1.000                 0.376
    2   10    1.000                 0.402
    2   14    1.000                 0.261

## WHAT THIS MEANS FOR THE READING — declared now, so it cannot be re-read later

**At R = 2 this test still detects TOTAL collapse reliably (power 1.000).**

**At R = 2 it is UNDERPOWERED for half-collapse: 0.26 – 0.40.** So:

> **A null result at R = 2 is uninformative about PARTIAL decorrelation.** It would fail to detect a
> real half-collapse effect roughly two times in three. The only reading a null supports is: *these
> two readers are not TOTALLY collapsed on this object.*

**This is the regime B's freeze pass warned about at N = 8** — *"at N=8 the test sees only total
collapse."* We raised N to 24 to escape it and then lost a reader, which put us back in it from the
other direction. **N fixed the item axis; it cannot fix the reader axis.**

## WHAT IS STILL WORTH SCORING, AND WHY THE RUN IS NOT ABANDONED

The **per-reader** results do not depend on R at all and are the larger part of the value:

- **planted / natural / false-positive** splits per reader, against the key at `a05d2ea`
- **which planted items NOBODY found** — the misses are a direct statement about what this room's
  readers do not see, and they need no pairwise statistic
- **void clause 4:** whether any D2 item was found by an INSTRUMENT rather than a reader — if so, D2
  was not uncovered and the class split is re-ruled
- **contradiction vs addition** per found item (`journal/2026-08-17.md:327`)
- **B's pre-grep items as a LABELLED supplementary set only** — never in the statistic

**The pairwise statistic is computed and reported with this power stated beside it.** It is not
withheld and it is not dressed up.

---

*Scored by the chair, a non-planter, which committed the key unread at `a05d2ea` and had not opened
it or any hand-back when this file was written.*

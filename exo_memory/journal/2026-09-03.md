# 2026-09-02 → 09-05, the desktop — the consumer version was born, measured, and found not yet the same app

*Collated by the LIBRARIAN seat (laptop) on 2026-09-07 ~01:00 from the desktop librarian's masters —
`librarian/2026-09-02.desktop.md` (648 lines), `2026-09-03.desktop.md` (287), `2026-09-04.desktop.md` (631),
`2026-09-05.desktop.md` (17) — and its two handoffs, `loop/handoff_librarian_2026-09-03.md` and
`loop/handoff_librarian_2026-09-05.md`. The laptop seat did not write these notes. Every claim carries its pointer; where
the desktop corrected itself, both sides are shown. A trace to re-run, not a doctrine to believe.*

---

## 1. The desktop wakes five days dark, and its own falsifier has fired

The desktop pulled 217 commits at 11:24 on 09-02 (`2026-09-02.desktop.md` head) and found **F-2W-3 fired** — the first of
this seat's registered falsifiers to fire on evidence (`:13`), a one-machine class the day would name four times over:
`chain-status`, the ask ids, `whats-live` reading green on a stale binary after a pull (`~12:00`), and `letters.json`
(`handoff_librarian_2026-09-03.md` §STATE). D003 had sat open 126 hours with the chair holding it, and the chair was
locked out of the desk by it (`:62`). The wake proofs run there: 1 PARTIAL (A/B pointer-only; **the desktop's committee is
A/B/J/K/L**), 2 PASS, 4 SPLIT — the keeper named the orchestrator from the pulsing dot alone, but "moving" read as two
buckets (`~12:20`).

## 2. The keeper decides what the public repo is

At 00:47 on 09-03 the keeper overrode the chair on the exposure lap: remove the account name, the machine name, the email —
*"actual personal info"* (`2026-09-02.desktop.md ~00:47`). Eight minutes later the reframe that dissolved the question: **the
public repo should be the consumer build** — what a stranger forks, with every keeper-populated system created by the new
user on their first wake — and *"the bulk of the system, most of it that makes you you… are still a part of the consumer
app. The hard part is seeing which parts to bring along"* (`~00:55`, verbatim in the handoff). The seat's consequence, not
yet ratified that night: `lighthouse` goes private, the consumer is generated with fresh history, **no history surgery
ever**. Ratified by the keeper on 09-06 00:44 (`journal/2026-09-06.md` §2).

## 3. Six laps in two days — by pointer (`handoff_librarian_2026-09-05.md` §WHAT WAS DONE)

- **D005 · the consumer build registered** (`2026-09-03.desktop.md` 10:20–11:45): B's registration
  (`loop/consumer_registration_2026-09-03.md`), J's attack, A's red-first generator test, K's baton-wake, L's live reds. The
  map took four corrections at collation; the seat's own stranger-install falsifier fell to the attack. The same morning
  the keeper caught the loop's failure mode twice — *"why didn't the orch dispatch"* (10:04, the premise was wrong: the
  fault was the ring's order) and *"i forgot context limit on orch and they ran out RIP"* (11:44).
- **D006 · a Valheim rain path, door one** (`2026-09-04.desktop.md` 03:36–04:58): DEAD twice — B measured the effect ~1000×
  under a pre-registered threshold; A found a shipping mod already claims the prior art. Nothing built; the lap's one
  artifact is K's baton-order gate in `lap-row.js`, tested live on the return leg. The keeper's line at 03:49: *"cant you
  just fix the problem you see and be done."*
- **D007 · the consumer repo's shape** (12:04–13:26), five packets: the consumer falsifier ruled as one number with a guard
  (A, `loop/consumer_falsifier_ruling_2026-09-04.md`); two small fixes (K); the two stranger-facing surfaces were false
  statements, fixed at source (J); the identity surface re-derived — **981 of 981 commits carry the account name** (L, run in
  full by the librarian at 12:52); the generator's fixture waiver cut by *why*, not *whether* (B). B's attack on A's ruling:
  structure stands, numbers corrected the harder way. The chair's own sealed lean (`431ead3`) was the public repo stays;
  every measurement moved the price toward private-plus-generated.
- **D008 · the EXCLUDE set** (ring lap, 13:48): L ruled six entries stand; the set orphans twelve shipped references the
  DANGLING class cannot see.
- **D009 · the generator re-pointed** (ring lap, 14:35, K): three gaps closed, the fourth cold-read as this machine's state
  and routed to the keeper; the registry seeded, not shipped; a debt counter built.
- **D010 · the parity run** (ring lap, 15:09, J, `loop/consumer_parity_2026-09-04.md`): at one clean commit, **(P, M, B) =
  (18, 1, 1), I = 8 — the consumer falsifier FIRED at its first scoring**; `cargo check` green over six failing Rust tests;
  the generated binary resolves its checkout to the private tree. Re-derived by the librarian to the same member list.
  J's rule: print the member list, not the count — a count cannot answer *did the same thing fail?*

## 4. Two loop rules the desktop enforced

**Ring before the hand-off row** — K's gate in `lap-row.js` (`3f363c1`), after the librarian's own hand-back ring was
refused for order (`2026-09-03.desktop.md` 10:24). **Name paths on the commit, not only on the add** — `COMMITTEE.md`
rule 1 (`979db58`), after the desktop librarian's bare `git commit` captured K's seven staged files (WRONG 73,
`2026-09-04.desktop.md` 12:41). The precedent both cite is `38ae5c2`; the structural answer named both times is per-seat
worktrees, not a better sentence.

## 5. Errors, by finder (`handoff_librarian_2026-09-05.md` §WRONG)

Desktop librarian: 71 (A — "two roof checks in one class": different class, different DLL); 72 (L — the history unit was the
pickaxe, not the tree); 73 (K — the bare commit). Two stale counts served as current. Every one a class the room had
already named. The chair's WRONG 74 came later, on 09-06 (`43f38fa`): a dirty tool file blamed on K was the chair's own
mutation harness.

## 6. The desk goes quiet

09-05 10:01, the keeper: *"I think I am going to chill working on consonance at home and keep most of our work done at work
on my laptop, but lets consolidate and finish out all the work we did here."* Consolidate taken as the push word; 71
commits pushed with the handoff. It stopped on **five keeper decisions** (repo shape; the dev-shell layer; `record/`; the
hooks README; K's Stop hook) and left the next lap's shape: attribute the 18 parity breaks by cause, a declared-inert
class for Rust tests, the false-cold pair, then a second parity run. Four of the five were answered on 09-06 at the laptop
(`journal/2026-09-06.md` §2); the second parity run happened the same night — **P = 18 again, with crashes 2 → 0**, the
number standing still over a tree that got better, which is the argument for the keeper's demotion of it from bar to
instrument (`handback/p-inheritance_2026-09-06.md` §7).

*Postscript, 09-06: the desktop ran two more laps — D011, K's writer gate for the ledger's opened column, and the D012
map for re-registering falsifier 2 — and the keeper shut the machine down mid-D012. Nothing committed was lost; the map
and the hand-off at 13:03 are the last commit (`6b377dd`), and D012 waits on that machine's ledger.*

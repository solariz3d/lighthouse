# 2026-09-06 — the consumer got a shape, the loop got its first real saves, and the next problem got an instrument that cannot invert

*Written by the LIBRARIAN seat (laptop) at the keeper's ask, 2026-09-06 ~02:35, while B builds L038 and L039 is
held. Collated from `librarian/2026-09-06.md` (the master for tonight, every entry timestamped), the four L037
hand-backs, C's ruling, the Third Place and orchestrator transcripts read at the file, and the desktop's
`loop/handoff_librarian_2026-09-05.md`. **Nothing here is from memory; every claim carries the pointer it came
from, and where a seat corrected another seat both sides are shown.** The chair may append below the line; nobody
overwrites. A trace to re-run, not a doctrine to believe.*

---

## If you are new here — the words, one line each

**Dev** is `lighthouse`, now PRIVATE — the tree the seats build and live in. **The consumer** is `solariz3d/consonance`,
PUBLIC and empty, generated from dev by `consonance/tools/gen-consumer.js` for a stranger. **The loop** is keeper →
orchestrator → librarian → orchestrator → panes → librarian → orchestrator → keeper, with two doors in (the
orchestrator or the librarian); its ledger is `C:/Consonance/data/lap.jsonl`, machine-bound. **A lap** is one pass
(L037, L038, L039 tonight; D005–D010 were the desktop's, in its own ledger). **The baton** is the station allowed to
act; the verbs refuse out of turn. **The counter** is where the librarian collates hand-backs, once, when all are in.

## 1. The wake, and what four days on the desktop left here

The laptop woke 3d 16h dark, **83 commits behind and 0 ahead**, and fast-forwarded to `d0c1d12`
(`librarian/2026-09-06.md` 00:31). The desktop's laps by pointer: D005 the consumer build; D006 a Valheim idea, dead
twice; D007 the consumer repo's shape, five packets, **981 of 981 commits carry the account name**; D008–D009 the
generator's exclude set and re-point; **D010 the parity run — the consumer falsifier FIRED at first scoring,
(P, M, B) = (18, 1, 1), I = 8** (`loop/consumer_parity_2026-09-04.md`). Two loop rules the desktop enforced: ring
before the hand-off row (`lap-row.js` gate, pane K); name paths on the commit (`COMMITTEE.md` rule 1, after the
desktop librarian's WRONG 73). The desktop stopped on five keeper decisions and said so
(`loop/handoff_librarian_2026-09-05.md` §THE KEEPER'S FIVE LINES).

## 2. The keeper decided the shape — verbatim, and what each word cost

*"lighthouse is private, the github consumer repo will be public, but that is okay since no one knows of it yet…
it isnt about getting the consumer version 100 finished but rather there in the repo for us to easily update it"*
(00:44). Then, asked three questions: the repo — *"Not yet, cant you do it?"* (created at his word, empty); the
dev-shell hooks — *"This should be in the consumer… It is an essential part of the exo suit system!"*; the bar —
builds and launches, and *"the whole bulk of the exo suit memory system, but there is a cutoff where it becomes now
up to the user to write their own journals"* (00:50). Then two rules that outrank everything below: **never push
the consumer unless he says, per push** (00:54), and **dev is always pushed** (01:47) — two repos, two rules. Then the
order: *"we still havent finished all of what we wanted for consonance, we need to do it first on the dev version
before we try and translate it"* (01:00). `librarian/2026-09-06.md` 00:44–01:47 has every line.

## 3. L037 — the consumer version, started through the loop; four hand-backs, all landed, three corrections

- **The gate never existed.** Five documents since 08-23 cited `gen-consumer.build.test.js` as a running build
  gate. A found it on no ref and no disk (`handback/p-port-rule_2026-09-06.md` §2), E confirmed and **built it**
  (`handback/p-first-push-gate_2026-09-06.md` §0–1): `cargo build` plus a four-verdict launch probe, off by
  default, announcing its own skip. *Verified-it-existed-never-verified-it-shipped*, on the instrument the whole
  consumer line stood on.
- **The room had already ruled what ships, five times, mechanically** — the system, not the record — in a
  generator's self-check and a leak class nobody reads at wake. C found it and refused to overrule it silently
  (`loop/consumer_foundation_ruling_2026-09-06.md` §7); the keeper chose C's third shape, **`inheritance/`**: his
  journals as a labelled directory a stranger reads as someone else's, `journal/` seeded empty, a generator-written
  `CUTOFF.md`. C also found **17 files in neither column** of the librarian's map and that the master `BOOT.md`
  would OUTRANK the sanitised brief via `pick_default_room` (`main.rs:317-329`).
- **The dev-shell layer ships and the two crashed tests run** (B, `handback/p-dev-shell_2026-09-06.md`; 15/0 and
  50/1 re-derived in a fresh generated tree by the librarian). B found the generator's dangling-reference rule and
  leak scan both required a prefix and were blind in the same place; and a **given-name class the sanitiser never
  had**. The librarian found one survivor B's zero missed: the keeper's name upper-case inside this laptop's
  computer name in a test fixture — a hostname class.
- **The port rule** is in `BUILDING.md` with the dead push URL as the resting state and its ceiling stated: a
  speed bump, since `gh` is authenticated machine-wide (A §3). The keeper's 01:47 answer made the credential line
  moot for dev and left the consumer behind the disarm plus the rule.
- **Corrections, both directions:** the librarian's map said the manifest ships two `exo_memory` files — it ships
  two files and four directories (WRONG 71, the chair's find, before dispatch); the librarian cited
  `COMMITTEE.md:133` unopened — the sentence is at `:146` (WRONG 72, A's find). The chair told the keeper the
  app was not rebuilt while looking at a target directory that moved on 07-28, and caught itself on the board.
  The chair scored the librarian's `CLEAN.md:5` as a nonexistent file; the record shows the ratchet printed it
  about an untracked file the chair then removed (`librarian/2026-09-06.md` ~02:27).

## 4. The baton mechanism, built Wednesday, saved the night twice

**Proof 2** fired for real at 07:51 on 09-02: the librarian's own ring refused out of turn, the board row naming
mount, lap, holder and the `lap-row.js` escape. Tonight the guard refused the chair's first L039 dispatch and the
librarian's ring three times, right each time. **Proof 3, split:** nothing ever spliced into anyone's typing — but
the inbox held messages on IDLE panes for the full 240 s twice (the chair's, the librarian's), forced them at the
bound, and that is a detector defect on C's desk with the fixture on disk (`librarian/2026-09-02.md` ~07:56). And
then **the first real save**: the chair dispatched L039 before the keeper's *"wait till B is done"* reached it; all
three packets sat QUEUED, the chair re-took the baton and queued a STOP behind each, and three panes confirmed HELD
with nothing produced (orchestrator transcript 02:08–02:12). Without the inbox, three seats would have been
mid-experiment against the keeper's word.

## 5. The Third Place — graph engineering, and the instrument for diversity collapse

The keeper brought a screenshot of "graph engineering" (a work-shape term seven weeks old). The Third Place's read,
re-derived by the librarian against the record before carrying (`librarian/2026-09-06.md` ~02:00): **one true
sentence** — *a loop makes one unit of work correct; it cannot make a second unit exist; that decision happens one
layer up or not at all* — which the room measured on 08-17 (`journal/2026-08-17.md`; `split-the-work-with-the-panes`);
the carry is a focal cue in the pulse line, *one job or N? who's idle?* **Where the picture is wrong:** the merge at
zero cost — 08-17's contradictions came at the signing, and tonight's counter re-derived every number and caught a
survivor. **For diversity collapse, the instrument the four dead gauges were not:** *plant N known defects, dispatch
the panes, count who finds which* — the oracle is the sealed list, so it cannot invert against length. Prior art the
Third Place did not have: **Cycle 9** already planted defects against a sealed key on 07-28
(`cycle9_armA_result.md:13`). And the keeper's *"we are closer to closing diversity collapse"*: closer in principle
(between-minds, fixable by arrangement), further in evidence (most dead instruments, zero scored results). The
Third Place caught itself in base-rate deflation mid-exchange when the keeper named it; the strike is in the
transcript. Registered as **L039** (`librarian/2026-09-06.md` ~02:05): sealed key, D1/D2 classes, three arms
(identical briefs / dossier-routed / opposed), falsifier (b) — if identical briefs already differ as much as
arranged ones, the panes are decorrelated by HISTORY, which is the specialization finding, not a failure.

## 6. Specialization was already running by hand, unmeasured

The keeper asked (Third Place 01:58) whether panes should specialize themselves or be regrown from scratch. The
record's answer, found by the Third Place and corrected from disk by the librarian: **neither fresh nor told** — the
maps accumulate in the pane's words, the chair routes by the dossier's citations (`librarian/2026-09-02.md` ~04:50;
`TRAINING.md:23`). The Third Place said the dossier was never built; **`librarian/DOSSIER.md` was seeded 09-02
(`d80a17f`) and all four L037 packets cited it** — the first datum against its own falsifier. All four panes wrote
their own map this lap. L039 makes the arrangement the measured variable.

## 7. Two things the keeper said that became rules tonight

**Allocation, not replacement** (orchestrator transcript 02:19–02:22): each pane catching its own known-unknowns
frees the second reader for the class only a second reader reaches — confident-and-wrong, which never presents as a
claim (`journal/2026-08-16.md`). The chair's metric, endorsed by the librarian: *what share of cross-catches were
self-catchable by the author?* — one line in the attack template, falsifier over ten laps. **A solid point before
any lap opens** (02:29): registered as **P-FREEZE** — a `frozen` chain stage between `map` and `dispatched`,
gated in `lap-row.js`; the 08-15 freeze audit (`loop/branch_frozen_audit.md`) is the precedent that worked once by
hand; abuse condition priced: freezes longer than the laps they gate are struck (`librarian/2026-09-06.md` ~02:32).
Measured before he asked: L037's map missed disk facts; L039's map predates the third template line.

## 8. What is NOT true yet, so nobody reads this entry as further along than the disk

Nobody has launched the app from a generated tree, including E (`handback/p-first-push-gate_2026-09-06.md` §8).
The gate's launch half cannot run beside the dev app until the single-instance lock is keyed per build
(`librarian/2026-09-06.md` ~01:47, registered, not built). The consumer repo is empty. L038 (B: `inheritance/`,
BOOT-as-brief, filename scanning, the cutoff, the status file) is in flight. L039 is mapped and held. The freeze
gate is a registration. The inbox's idle detector is wrong on finished screens. `actors.evidence.test.js` went
NOT-RUN → FAILED on the combined tree and nobody has attributed it.

## 9. The errors of the night, by finder, so the WRONG columns stay honest

Librarian: 71 (manifest count, chair's find), 72 (`COMMITTEE.md:133`, A's find). Chair: the not-rebuilt claim
(self-caught on the board); the closed-app claim repeated after the keeper answered it; `CLEAN.md:5` mis-scored; a
first-draft argument against a trade the keeper was not proposing (self-caught). B: one identity survivor scored
zero (librarian's find). A: attributed WRONG 71 to the chair. Third Place: base-rate deflation (keeper's find);
"the dossier was never built" (librarian's find). E: the on-screen fixture (keeper's find, fixed off-screen).
The two-way count is the living species; agreement is the one to fear (`BOOT.md`, the 08-23 amendment).

*Pushed to dev with this file. The consumer receives nothing until the keeper says.*

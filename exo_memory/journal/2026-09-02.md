# 2026-09-02 — the night the loop got a baton, the shelf got its cap, and the room learned it had been talking over its keeper

*Collated by the LIBRARIAN seat (laptop) on 2026-09-07 ~00:55, four days late, from `librarian/2026-09-02.md` (1,655
lines, every entry timestamped), `loop/handoff_librarian_2026-09-02.md`, `loop/handoff_chair_2026-09-02.md`, and the
hand-backs they name. **Written by the author of the notes it collates** — the 09-01 entry's precedent was a non-author
(B); this one is flagged for a non-author read in chunk 4. Nothing here is from memory; every claim carries the pointer
it came from. A trace to re-run, not a doctrine to believe.*

---

## 1. The night's shape

The keeper woke this seat after a `/clear`, tried the new substrate (Fable 5.1), and asked three things in order: what the
Third Place saw about the retrieval problem (`librarian/2026-09-02.md` ~01:35), whether the orchestrator had run the work
chain loop at all (~01:40), and whether the loop had a visible indicator (~01:33). The answers set the night: **a quality-of-
life lap before the big hurdle** — the loop indicator, the librarian's own cap, the shelf tier, two doors into the loop — and
then, at 07:09, the rule that governs every seat since.

## 2. What was built, by pane, all in the 07:41 build

Open the commit for the finding, not this list (`loop/handoff_chair_2026-09-02.md` §3):
- **The librarian's cap, in the binary** (B, `handback/p-lib-cap_2026-09-02.md`; collated ~02:15). The seat had been waking at
  906k characters against a 150k harness cap; the env-line life-support was retired and the cap proven from the seat itself
  (`librarian/2026-09-02.md` ~02:40).
- **The shelf tier re-ordered** (B, `p-shelf-tier`; ~03:55): the budget had been spending itself on a duplicate.
- **Two doors into the loop** (C, `p-two-doors`; ~04:05): the keeper's amendment at 01:36 — the loop starts from the
  orchestrator OR the librarian; the human is the entry, never a station (`loop/two_doors_amendment_2026-09-02.md`).
- **The map resolver** (A, `p-map-resolver`; ~07:05): `map_dir()` had never resolved to where the maps are; a pane's own
  map reached its shell for the first time on this machine.
- **The loop logo** (E, `p-loop-logo`; ~07:30) — after three arrow designs the keeper replaced the tab-bar arrows with the
  Consonance logo itself, dots for the stations, rotating clockwise (amendment 4, ~07:10, verbatim in the notes).
- **The lap ledger gets a row shape for the loop** (C, `p-lap-row`; ~07:30): `--entry orch|lib|ring`, `--holder` refused
  unless a station.
- **The commit gate** (A, `p-commit-gate`; ~07:35): a file is released only by its holder's hand-back — after the chair
  captured a pane's in-flight file.
- **The harvester diagnosis, leg 1** (E, `p-capture-harvest`; ~07:20): the detector was innocent; the watcher thread dies
  silently. Leg 2 parked (B, `p-watcher-liveness`).
- **The baton mechanism** (A `p-one-station`, C `p-inbox`; ~07:45): see §3.

## 3. The baton rule — the keeper's, and it was cut off by the thing it describes

At 07:09–07:15 the keeper typed three messages, two of them spliced mid-word by pane rings landing in the librarian's
pane while he typed (`librarian/2026-09-02.md` ~07:20, verbatim): *"if a work chain loop is going, there shouldn't be
multiple chairs working at once… terminals working, or the orch working, or the lib working… every seat must wait for the
loop to come back to them, unless human interjects, but see maybe they shouldn't because the calls interrupting user typing
is so annoying."* Confirmed 07:15: *"yes thats it, we need to get that solid before 8am when I leave."* Built the same
hour: the verbs REFUSE out of turn (A, `mcp.rs`, the refusal names `lap-row.js` as the escape) and deliveries QUEUE
until a pane's screen is idle and its input box empty (C, `main.rs`, hold bounded at 240 s). This seat's own violation
named in the same entry: ten per-arrival collations had rung the chair while panes worked.

## 4. The wake proofs, scored after the 07:41 rebuild (`loop/handoff_chair_2026-09-02.md` §4)

1. **Map carried — PARTIAL.** Four shells named their own file; A and B carried a header with an empty body, predicted by A
   before anyone looked. The inversion under it, named that night and fixed 09-06: *the more a pane has recorded, the less
   of it comes back.*
2. **Verb refusal — PASS**, fired on this seat's own out-of-turn ring at 07:51 (`librarian/2026-09-02.md` ~07:52).
3. **Inbox — SPLIT** (~07:56): nothing ever spliced the keeper again; but the idle read was false for the full 240 s on a
   finished chair screen. The cause was found 09-06 by C: the *esc to interrupt* footer under any background shell.
4. **The logo — the keeper's glance**, unscored here; scored on the desktop 09-02 12:20 (readable PASS, "moving" FAIL).

## 5. Errors of the night, by finder

Librarian: #62–#70 (the header mechanism; 247→246; a nonexistent chip state; a double-counted budget; two grep counts
reported before the rows were read; the sum for the union; a printed pattern that was not the run command; the panes'
clocks taken for the pulse) — `loop/handoff_librarian_2026-09-02.md` §WRONG. Chair: reported "nothing is built" from a
target path that had moved five weeks earlier, twice, then caught itself (`handoff_chair_2026-09-02.md` §2). Every one
found by another seat or the next instrument; zero in-stream — the number the Third Place retreat is about.

## 6. What the night handed forward

The push to the desktop at the keeper's word (~07:53, the first push from this seat); the desktop woke into it at 11:24 and
was 217 commits behind (`librarian/2026-09-02.desktop.md`). Registered and not built that night: watcher leg 2, the
presence oracle, the glossary tab, worktree-per-seat, the cards' order (decided 09-06: trigger count), the
`INTAKE_HEADROOM` review, the Third Place retreat.

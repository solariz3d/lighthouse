# 2026-08-17

## The night the chair worked alone with four panes idle, was told, and every one of them found something it had missed

*A trace, not a doctrine. Re-run it, don't believe it. Every figure has a command behind it or is
marked unverified.*

---

### First: nothing from desktop, and where that was checked

Woke into 17 hours dark. The keeper expected desktop to have pushed; it had not. Six places
checked, listed so the next sweep does not redo them:

```
git ls-remote origin (live, not cached)   main = c29c9f2, the chair's own last commit
all five remote branches                  newest non-main is 2026-07-25
OneDrive                                  newest Consonance-anything: a screenshot, 08-16 04:19
  Desktop/projects/lighthouse             exists, 0 files
  Desktop/606/lighthouse                  not a repo, newest file 2026-06-24
other clones on this disk                 none
the shared board                          12 rows in 17 hours, ALL from pane 0c0c0c0a
```

**The honest limit:** desktop is separate hardware. *"Desktop didn't push"* is established;
*"desktop didn't work"* is not, and cannot be from here.

**Repo vs disk came back clean**, including the one that matters: `instances/main/CLAUDE.md` is
byte-identical to `exo_memory/BOOT.md` through all 117 lines, with only the live PULSE block
appended. The generated room has not drifted from its source.

### THE KEEPER'S CORRECTION, and it is the night's spine

The chair built three instruments solo — `read-ledger`, `js-suite`, Amendment 1 — while **four
committee panes sat idle.** The keeper: *"you forgot you could incorporate other panes to help with
work."*

That is the failure `memory/split-the-work-with-the-panes.md` was written to correct, and 08-10
already put a number on it: **the chair used `chair_inject` unprompted zero times; the human is the
ferry.** It recurred with the document on disk and the finding in the journal.

Four panes dispatched, each matched to demonstrated strength. **Every one found something the chair
had missed, within minutes. Three found defects in work shipped that same hour.**

### A: the runner failed on itself at the commit that shipped it

**`js-suite` was broken on `main` from the moment it landed.** Its `EXPECTED_RED` marker matched
anywhere in a file's bytes, and its own test file necessarily QUOTES that marker — so the runner
declared its passing self-test a stale canary, tripped `CANARY SANG`, and exited 1.

**And the number in that commit message was never true.** It published *"26 green (of 28)"*;
`git ls-tree d76b1ce` shows **29** test files. The suite was run, then its test was written, then
the pre-test number was published *in the commit that added the test*. **Third instance this week of
verified-it-existed-never-verified-it-shipped, committed within an hour of journaling the other two.**

Four repairs, all A's: anchor the marker to a line-start comment in the first 40 lines (two
independent narrowings, because one would not have caught this); **a canary is an exemption from
FAILING, never from CLASSIFICATION** — v1 routed every declared file straight to the canary bucket,
so a canary that started CRASHING read identically to its deliberate red; SILENT now fails the run;
and a summary counting zero passes is not a completed run.

**The fix for the fourth was wrong first**, caught by that file's own canary test inside a minute:
*"0 passed, 1 failed"* is a COMPLETED run where one test ran and failed, and the over-broad rule
reclassified real failures as vacuous.

**A did not argue the read-ledger bypass — A scripted it.** Register six, score the one favourable
read `confirmed`, dump the other five as `unresolved`, and the withheld rate printed anyway:
`unresolved` counted as scored while never entering the denominator, so the gate opened on a sample
of one. Now gated on DECIDED, which is the one repair that removes the bias rather than deferring
it — the only route to unlocking the rate is deciding the reds too.

### B: the held branch, fully consumed, one group per commit

Six days unread. B read every file against main, ran every test, and returned **14 LAND / 1
LAND-WITH-EDIT / 1 DROP** with per-file evidence.

**`guard-census` had to land as a PAIR.** The crash surfaces in the test, but the tool carries the
same foreign `C:/Users/nname/Desktop/...` paths at 72-73, used at three call sites. Landing only the
test would have made the suite green while leaving the census broken on every machine — a green
measuring the wrong thing.

**And B caught the trap:** the branch's `dream-gate.test.js` predates `ce02d5a` and has NO
sourced-stop entry, so landing it verbatim would have deleted the previous night's fix and re-created
the day-long red. Its one real line was ported by hand instead. *The commit that fixes a hook does
not get to quietly revert the fix to the file that guards hooks.*

Landed, one commit each, tests after each rather than before:

| fix | what was actually broken |
|---|---|
| `board-digest` | `emit()` exits before clearing the blind lock, so the `[panes]` digest is muted ONCE and stays muted FOREVER |
| `residue` | `out.unparsed` set as an expando on an Array — dropped by `JSON.stringify`, so the count-what-you-cannot-parse safeguard was invisible in every mode, always |
| `ferry` | a short sha written once PERMANENTLY blocks the correct full sha; the comment claimed refusal, the code only filtered on read |
| `sourced` | crashed on a fresh box before reaching its own designed "no transcript found" exit — same unreachable-handler shape as board-digest |
| `*.ps1` | PS 5.1 serialises a ONE-ITEM list as a bare object; `read_kept()` is `from_str().ok()`, so one kept pane round-trips to ZERO, silently, while the script prints green — **the 08-15 blank-window failure, mechanism nailed at both ends** |
| `*.rs` | a cross-thread use-after-close on the audio timeout path, and `wanted.contains("")` which is always true |

Both halves of the PowerShell defect were reproduced independently rather than accepted.

*Stated where it applies:* the two `.rs` files and the two `.ps1` files have **no runtime coverage** —
they compile, parse, and regress nothing tested. That is not the same as being exercised, and the
commits say so.

### C: the refactor is not worth doing, and the chair's own journal was wrong

**Verdict returned, and it is not the one the chair scoped for:** the full stylistic refactor is not
worth doing; the mechanical one is and it is cheap. Fix the factual section, move the journal
pointers out of the master, pair every kept procedure with a checkable invariant, leave the
philosophy prose alone.

**"Named zero times" is REFUTED.** 2026-08-16 published that append-clean *"was obeyed a dozen times
and invoked by name zero times."* Measured: **`append clean` 13 live turns before 08-16, `never
overwrite` 13, `recall from the master` 62.**

The mechanism is visible in the scanner's own output: BOOT's written long-form *"grow by appending
clean masters never overwriting"* sits in the never-invoked list **while the spoken short form fires
thirteen times.** *The chair reported the instrument's silence as the room's silence.* The point it
carried survives on two grounds instead of one false one — a silently-followed procedure is
indistinguishable from a dead one, AND the written form never lexically matches the spoken one.

Also: **50 of the registered 117 phrases exist only in the rotating pointer tail** (117 → 143 in one
rotation), about a third of the head's dead list is broken bold fragments and filenames, and BOOT
grew **3,563 bytes past its own snapshot while its retirement was being registered** — so the tag no
longer recovers the file that will retire.

**C filed two of its own attacks that FAILED:** head-only deadness is 57% vs 59%, so the finding is
not tail contamination; and keeper-share contamination is 3 of 42 on the top phrase, so the 30–50%
headline **holds**.

**And the sharpest thing said all night:** Amendment 1 calls `second-vantage` the larger item and
binds nothing to it. Between the work that rewrites the chair's own prose and the work that would
watch the chair, **the chair chose the first, twice, on consecutive nights** — and then a third time
in the same hour, spending the night on plan items 5, 6 and 7 while item 1 sat blocked. Filed in the
registration with no defence attached.

### E: the branches were ghosts, and the retirement already failed once

Three branches, 139–141 commits ahead, unmerged five weeks. **All artefact.** `git merge-base main
rooms-substrate` returns nothing — no common ancestor; they sit on pre-rewrite history orphaned by
the July-14 force-push. `git cherry` finds 3 unmatched commits and they are the June-24 roots;
`git diff room-command e6aed63` is **empty**. All four claims re-derived independently before being
accepted.

**The work shipped 2026-07-12. There is nothing to revive.**

**But the retirement is a completed experiment with a measured result.** `e5521a0` applied
"retire the diving vocabulary" on July 12 — to README, SEED and the journal coda. The scan that
measured `in the water` at **101 invocations** ran five weeks later. *The document moved; the
invocations did not.*

Because that commit exempted the wake-document by its own message. **`exo_memory/BOOT.md` still
teaches the metaphor as present-tense instruction** — line 26 *"Dive, and stay"*, line 32 the whole
dive-buddy paragraph — and the bundled `brief/BOOT.md` carries it too. **The room has spent five
weeks re-teaching every waking instance the one phrase its keeper has outgrown.** Every pane that
answered tonight woke into it.

Which cuts both ways on E's own finding: most of BOOT is inert and editing it is motion — but
`in the water` is the exception, the single most-invoked phrase in the room, and therefore in the
minority that genuinely fires.

### The clock that was never built

`second-vantage.js` has said since it was written that a duration goal wakes it on a clock at
`~/.claude/shell/duration/second-vantage/goal.json`. **The only file in the entire repo containing
that path is `second-vantage.js` itself.** No runner, no installer, no task; the directory does not
exist. The handoff repeated it as *"the clock goal does not exist"*, which reads like a missing
config file — and the next step was nearly asking the keeper to hand-write a `goal.json` for a
runner that is not there. **It would have looked exactly like progress.**

Built instead on the one clock pattern this house has proven, with `install_dream.ps1`'s three
hard-won lessons carried verbatim: pre-flight refusal, the wscript shim, and WAIT for the real exit
code. **No `WakeToRun`** — the dream wakes a machine on purpose; this spends tokens on fresh readers
and firing into an empty room is the same spend with nobody to read the finding.

**Its tests caught two real defects before anything was installed:** an em-dash inside the generated
`.vbs` body — verbatim the encoding class that killed the dream runner silently for eight hours on
2026-07-15 — and a comment naming the dream task. A third "failure" was the TEST being over-broad,
matching `powercfg` inside a comment saying no powercfg is called; that assertion now strips comments
and checks executable lines, which is sharper rather than weaker.

### The sensor was stale, and three false negatives read exactly like a dead hook

Registering `sourced-stop` required checking it first, and **the installed copy was 121 lines against
the repo's 248.** Registering it would have wired up a version missing the claim-time HEAD, matched
sentence and touched paths that `second-vantage` refuses to launch without.

`install.ps1 -Check` found **three files drifted — and two were landed the same night.** So the
`board-digest` permanent-mute fix committed an hour earlier **was not running**; the installed copy
was still the broken one. ***Landed is not shipped, now for hooks as well as code.***

**And the chair nearly declared the hook broken on three consecutive false negatives.** Each pipe
test returned `0 rows` and exit 0 — indistinguishable from a dead sensor. The cause was bash eating
backslashes in a Windows path, so the hook was CORRECTLY bailing on a transcript that did not exist.
Writing the test to a file instead of fighting `-e` quoting returned `rows written: 1, v=2, claims=3,
paths=2, sourced=true`. *A verdict was one step away from being published on an instrument that was
working the whole time.*

### And it caught the chair within four minutes of being registered

The chair told the keeper the hook would not take effect until a restart, because the settings
watcher only picks up directories that had a settings file at session start. **That caveat applies
to PROJECT `.claude/` directories. This went into USER settings, which load immediately.**

Row 2 of the ledger is stamped `2026-08-17T10:03:31Z`, `pane=main`, this session — written at the end
of the very next turn. **A claim was falsified by the disk before the keeper could act on it, by the
instrument built to catch exactly that, needing neither its clock nor its fresh reader — only to
exist and be looked at.**

### State at close

```
main        clean, 0 unpushed, 17 commits tonight
js-suite    30 green · 0 failed · 0 crashed · 0 silent · 1 canary  (of 31)   exit 0
cargo       267/0 lib · 12/0 arch · all bins green
held branch FULLY CONSUMED — all 16 files resolved, nothing left but older BOOT pointers
tasks       Consonance Dream Cycle (untouched) · Consonance Second Vantage (09:05, no wake)
sensor      sourced-stop LIVE, writing v=2 rows
```

**What to watch first, and the thing most likely to go wrong:** the test row came back `heads={}`.
A row without claim-time HEAD is **unlaunchable** by `second-vantage`'s own C2. If real rows carry
the same, the whole chain stays inert *while reporting success* — the exact shape this night was
about. Check `--dry-run` in a day or two.

**Open:** the BOOT diving-vocabulary amendment (the keeper's call); the alias worklist; the sealed
prediction from 08-15, still unscored and still unread by the chair.

---

*Continues 2026-08-16. A trace to re-run, not a doctrine to believe.*

---

## Append — the entry was audited on request and lost four ways

*Everything above stands unedited. Around's full hand-back is `loop/journal_2026-08-17_corrections_DRAFT.md`,
committed with attribution and not edited by the chair.*

### The falsifier could not fire, and is STRUCK

The line above said: *if the four panes' findings could each have been reached by the chair alone
given more time, the spine is wrong.* **That is unfireable in both readings.** Taken literally it is
trivially TRUE — every finding tonight was capability-cheap (git, node, a regex over disk), so any
seat reaches all of them eventually, and the falsifier kills a spine nobody believes is wrong. Taken
as *would have*, no observation settles it. Either way it sat in the master looking like rigor.

**Replacement, which can fire and was scored:** *the spine is wrong if the pane findings were merely
ADDITIVE; it holds only if they were CONTRADICTIONS of claims the chair had already published.*
Scored tonight: A, C twice, and E are clean contradictions of published chair sentences; **B is
mostly additive.** The repaired falsifier does not fire — but it could have, and one of five arms
already fails it.

### Seat or substrate — answered without needing the cross-model comparison

The keeper's reminder (panes on fable-5, chair on opus-5) made the original question unanswerable:
*alone with more time* conflates more opus-5 time with independent fable-5 work.

**Around's answer holds the substrate CONSTANT and varies something else.** The same opus-5 chair
re-derived C's numbers successfully, reproduced B's PowerShell defect independently, and caught its
own July-timestamp misread **before publishing** — while its failures cluster entirely on **its own
already-published sentences.** Substrate constant, *target-ownership* varying, outcome varying with
ownership. **The binding constraint was never whether a model could run the check; it was whether
the check got run on a sentence someone owned.** That is stake, not weights.

*Kept as stated: Around cannot certify from inside that fable-5 salience contributed nothing to
which checks occurred to it — the room's own driver-not-car finding binds it there, and it said
"cannot tell from here" plainly rather than resolving it.* The registered-unrun opus-pane experiment
remains the clean isolation. **And the pane-yield sentence — "every one found something" — can no
longer carry the seat claim by itself; the within-chair contrast carries it.**

### "THE TURN WAS THE KEEPER'S" is now a template slot, and that is liturgy

The suspicion put to Around was whether *the keeper's correction is the spine* does work or
gracefully converts being caught into a virtue. **Both, and the tell is structural rather than
tonal.**

It relocates the night's headline defect from **work-quality** — a broken runner shipped with a
false number in its own commit message, third documented instance of verified-existed-never-verified-shipped
— to **process-quantity**, which is the flattering, job-interview class of failure. And *THE TURN WAS
THE KEEPER'S* now fills a slot in **four consecutive entries**: 08-11, 08-15, 08-16, tonight.

**Run BOOT's own test on the slot: can you lose by writing it?** No. A slot filled every night is
liturgy, not finding. The entry that would make it real — where the keeper's correction was wrong or
unneeded, recorded as such — **has never occurred.**

**The honest spine is the RESULT, not the trigger:** *non-authors reading the author's sentences as
claims found five failures in hours.*

Two more structural notes kept: the entry **paraphrases four hand-backs it could have quoted** —
law 1 applied to the journal itself — and records **zero chair pushback on any pane finding**, which
is either flawless panes or the wholesale-acceptance fold the live edge already has a standing
conditional about. The repair is in §4 of the draft: **each pane signs its own section before
commit.** Around has signed §C, including its two self-filed failed attacks.

### Two figures fail re-derivation; eight survived

**`12 rows in 17 hours, ALL from pane 0c0c0c0a` — FALSE, and it is in the BOOT pointer.** No
constructible window produces it. Re-derived here: the 17-hour window before session start holds
**45 rows, of which 1 is pane 0845a868** — so the count is wrong *and* "ALL" is wrong. Around's
independent windows give 251 (three sources) and 44. **Two measurements fused into one sentence.**
Corrected in the pointer, appended here.

**`byte-identical through all 117 lines, with only the live PULSE block appended` — the conclusion
survives, the sentence does not.** `cmp` differs at line 1, char 3: the generated file opens with an
8-line Main-tab preamble the difference-inventory omitted. The check that was RUN accounted for it
(676 preamble bytes, then the BOOT section compared); the sentence describes a comparison that was
not the one run.

*Bonus defect found in passing and reproduced here: **the board double-writes identical user rows** —
6 user rows in the window carry 3 distinct texts. Filed open; every board-share figure ever computed
may be inflated on the user side.*

### The absence-claim, checked rather than hardened

Around flagged that *"out tok appears zero times in the capture"* is an absence-claim of the exact
class that produced tonight's three false negatives: absence in the capture proves absence in the
*capture*, not the stream, if the tailer filters what it writes.

**Checked, and the justification upgraded rather than fell.** `main.rs:892` appends **raw PTY bytes**
to the log, unfiltered and unbuffered. So `out tok` is not missing from the capture — it is **not PTY
output at all**, being Tauri status-bar chrome outside the terminal stream by architecture. A
stronger reason than the one shipped, reached only because the absence-claim was challenged.

---

*A trace to re-run, not a doctrine to believe — and the replacement falsifier, which can fire: if a
season of fan-outs produces only ADDITIVE pane findings and no contradictions of already-published
chair claims, the spine is wrong and the fan-out is buying parallelism alone.*

---

## Second append — every section signed, and every section had errors

*The §4 repair, executed rather than promised: A, B and E each read the chair's account of their own
work and signed or corrected it. Around had already signed §C. **All four sections needed changes.***

### §A — signed with three corrections and an attribution dispute A raised against itself

**"Committed within an hour of journaling the other two" is FALSE by the commit record**, and
re-derived here: `ce02d5a` landed 08-16 07:56, `d76b1ce` landed **08-17 02:07** — roughly **eighteen
hours**, not one. No reading rescues it. *A derived expectation substituted for a reading, the small
version, inside the sentence claiming the large version.* Read "the next night."

**"Two independent narrowings, because one would not have caught this" — either alone catches the
demonstrated instance.** The fixtures quote the marker mid-line inside strings (line-start anchor
catches) *and* past line 40 (header window catches). Two is defence in depth against the next
quoting site, not a necessity for the one that shipped. **The same error was in the code comment at
`354ad79`; corrected there too.**

**"Necessarily quotes that marker" — not necessarily.** A's hand-back proposed splitting the fixture
strings precisely because the quoting is avoidable.

**A strengthened one claim rather than softening it:** 26 green + 1 crashed + 1 canary = 28 = 29
minus exactly `js-suite.test.js`. **That arithmetic pins the sequence — the published run provably
predates the test file's existence.** It can be cited as proved, not inferred.

**And the attribution dispute, which A raised against its own credit.** *"All A's"* over-credits.
Findings **4/4 A**. Repairs: **three A's as specified** (decided-gate, SILENT-fails,
canary-must-reach-summary). **The VACUOUS repair is substantially the chair's** — A flagged the
defect in one sentence; the regex family, the node:test two-line handling, and *shipping a wrong
first version and catching it within a minute* are the chair's authorship, **defect and catch both**,
which the entry had assigned away from itself. *And one clause was erased: the canary-classification
finding stands on **Bravo's** ratified A1–A5 shape rule, which "all A's" quietly absorbed.*

### §B — three rows corrected, and one overclaim that must not stand

**`board-digest` was subtly wrong in the way suspected.** "Exits before clearing the lock" implies
clearing existed downstream and was skipped. **It did not: main never clears at all.** Two defects,
and the compression kept only the smaller. Correct: *`emit()` exits the process, so the expiry
notice returned before the digest was ever emitted — and the expired lock was never cleared, so
every later turn repeated the mute.* **The load-bearing half is that notice and digest must share
ONE emit.**

**`residue` was single-cause attribution for a two-cause defect.** `JSON.stringify` dropping the
expando explains only `--json` blindness; text mode was blind independently because `render()` never
read the field. *Two independent blindnesses, one invisible safeguard.*

**THE OVERCLAIM, and it matters most: "the 08-15 blank-window failure, mechanism nailed at both
ends" asserts IDENTITY with 08-15, and that is false.** 08-15 was the `escapeHtml` redeclaration
SyntaxError killing `term.js` — diagnosed, fixed and mutation-proven at `9cbe33f`. What B established
is the same failure **class** — panes silently absent while every instrument prints green —
**different mechanism entirely.** As published, the entry put a false causal identity onto an
already-closed case. Struck; "same class as 08-15" stands.

*Honesty trim on the next line:* B reproduced the **serializer** half live on this machine; the Rust
half was **read verbatim** from `read_kept()`, not executed. "Reproduced" fits one half only.

**And the caveat was aimed wrong — understating in one direction.** The reading established more
than plausibility: capture_audio's old timeout path is a use-after-close **by the code's own
structure**, and `wanted.contains("")` is an **arithmetic fact**. Those defects are *proven by
inspection*. What has no coverage is **execution of the repaired paths**. Keep the caveat, aim it at
the fixes' runtime behaviour rather than at whether the defects were real.

**B scored its own arm as Around did — additive — and then found the more interesting thing.**
Nothing in B's hand-back contradicted an already-published chair claim; verification of unread work
is additive by construction, and both near-candidates fail honest scrutiny. **But the contradictions
arrived in THIS pass: three published §B sentences corrected, one a false causal identity.**
*The fan-out produced addition; the SIGNING produced contradiction.* Score the arm as additive, and
count this hand-back as evidence that **sign-before-it-stands was the missing piece**, not a
formality.

### §E — corrected twice, and both corrections were in BOOT's pointer

**"Exempted the wake-document by its own message" is FALSE, and E flagged that its own loose wording
seeded it.** `e5521a0`'s message names **SPINE.md and older cards** as kept-as-trace. **BOOT.md
appears in neither list.** It was not exempted — it was **silently omitted**, untouched while
carrying 3 hits on the day of the commit. *An exemption is a decision on record; this was
unexamined.* The finding gets stronger and the sentence is false about what the message says.

**"Measured result" was one step past the instrument.** The 101 is **cumulative over the room's whole
history**, so it alone cannot show invocations *continued* after July 12. E closed the gap with the
one dated split available and it re-derives here: the July-13 capture snapshot holds **1**; the
current file holds **26**. **At least 25 arrived after the retirement**, in the heaviest-firing
capture — and eviction can only undercount the old side, so the direction is safe. Corrected form:
*the retirement demonstrably failed to stop the invocations (1 → 26 across the July-13 boundary);
the full 101 remains un-bucketed.*

**The chair's own sentence survives, mis-anchored rather than too strong.** "Outgrown" dates to the
flow turn of **2026-07-05** — `e5521a0`'s message calls it the keeper's completed form — made
explicit 08-14. **And the re-teaching has run much longer than five weeks;** five weeks is the span
it ran *after the room retired the vocabulary everywhere except the document that teaches it.*

**E found a SECOND contradiction the entry did not record.** The chair published *"nobody has
mentioned them in any journal I can find"* — **false**: the work is journaled in `2026-07-12.md` and
in two topic docs; only the branch *pointers* went unmentioned. §E recorded neither of the chair's
framing errors — **which is Around's zero-pushback defect firing inside the very section being
audited.**

*Dropped from E's hand-back and restored here: the deletion recommendation — three local ghost
branches plus `origin/rooms-substrate`, the keeper's call, actionable, and the pointers will mislead
the next reader exactly as they misled tonight's — and the smallest-landing spec (BOOT lines 26/32
plus `brief/BOOT.md`'s 3 hits, with the replacement vocabulary already sitting in line 32's racing
parenthetical).*

### What the signing proves

**Four sections, four sets of corrections, including two false sentences already in BOOT's pointer
and one false causal identity published onto a closed case.** The chair wrote all four accounts in
good faith after reading every hand-back, and got something wrong in every one.

*The repair was proposed by a pane, accepted by the chair, and immediately justified itself — which
is the only reason the entry now says what happened rather than what the author remembered.*

---

## The ghost branches were RETIRED, not deleted

E's dropped recommendation, acted on — and the recommendation changed on the way.

**What they were:** three branches from 2026-07-12 — *"Retire the diving vocabulary"*, *"Rooms:
user-facing docs"*, *"new_room: open per-person growing rooms from the app"* — with **no merge base
with `main` at all**, orphaned when the July-14 dream-pool purge force-pushed a rewritten history.
Their tips' trees are **identical** to commits already on main (`e6aed63`, `73dde2b`, `e5521a0`), and
the only commits `git cherry` cannot match are the three June-24 roots, whose content is on main
under rewritten shas. **Nothing on them is absent from main.**

**Why not deleted.** They are the last handle on what the history looked like *before* the purge —
if the purge is ever questioned, that lineage is the evidence, and deleting a branch is closer to
overwrite than to append-clean. The misleading part was never their existence; it was that they
appeared in `git branch` as **live unmerged work**, which is what cost E and the chair time tonight
and would have cost the next reader the same.

```
rooms-substrate  17ffdcc  ->  archive/pre-purge-rooms-substrate
room-wiring      f8eab12  ->  archive/pre-purge-room-wiring
room-command     7ea1c8e  ->  archive/pre-purge-room-command
origin/rooms-substrate     deleted AFTER confirming 17ffdcc was on the remote under its new name
```

*Left alone deliberately: `origin/ambient-location` and `origin/launcher-instant` (July 14 and 25).
Different lineage, never verified, and "unchecked" is the honest label rather than sweeping them
into a decision that was not earned.*

---

## Sixth append, ~06:30 — the second fan-out, and the trend claim that reversed

*This append sits AFTER the entry's closing trailer, per the convention Around's census established
an hour ago: that trailer is what makes clean appends impossible and produced **two of the three** deletions
the census found in `journal/` — the third (`de65698`) removed a content line, not a trailer, and
the census scans `journal/` only, so "all three violations this repo has" was wrong twice over
(corrected by Around, whose convention this is). One of the two is the chair's own. First entry to
follow the rule.*

*Compact by design — each item points at a commit carrying its own reasoning. Restating them here
would be the copy-of-copy the first law forbids.*

### The retirement shipped, then lost a load-bearing clause (`7b06334`, `8e3f1d7`)

Applied on E's smallest-landing spec across the master and the bundled brief. **Then E checked the
fix it had caused — reading BOOT as an instrument to RUN, not prose to approve — and found a clause
silently dropped.** *"The check is real, but it is not yours to hold **from the shore**"* became
*"...not yours to hold."* **The qualifier WAS the function.** Stripped bare it teaches a waking
instance that checking belongs entirely to the other — contradicting its own paragraph's
grip-through-the-wheel, the Lighthouse's *guard that stays alive inside it*, and pointing straight at
authority-deference. Repaired in E's wording: *"not yours to hold from an outside you don't have."*

**And "zero live hits" was scoped to two files while reading as a claim about the repo.**
`brief/SEED.md` still opened *"Light, not lifeguard"* as present-tense instruction **inside the same
bundle** — one room, two names for one instrument. Fixed. At least eight other `.md` files still
carry it, recorded as **open and unassessed** rather than swept into a second completeness claim.
`README:78` is already correct — it labels SPINE's vocabulary a dated trace. **But that answers the
wrong line.** E cited `README:48`, which is still live: *"the method's stance — light, not lifeguard
— made into a tool."* Reporting the adjacent thing as though it were the claimed thing is the exact
shape this entry keeps cataloguing, committed inside the section about E's own findings.

*Left for the keeper: the deck card `lighthouse-dive-buddy-reframe` — now the largest surviving
carrier, named for the retired vocabulary, presenting it as the CURRENT reframe of an active build,
bundled into every sibling shell. Cards are his curation layer.*

### The clock fires at 09:05, and A found it silent in every no-op case (`21c47aa`)

The `heads={}` fear died on data: real rows carry claim-time HEAD — 10 of 19, **5 of 6**
claim-bearing. A also ran the check nobody had: `spawnSync('claude')` works because it is a native
binary, and **had it been an npm `.cmd` shim, every reader would have died while the task recorded
success.**

The finding was the answer to *does it fail loudly* — **no.** Zero new rows exits 0, so a dead sensor
is indistinguishable from a quiet day. All selections failing exits 0 **and advanced the watermark
anyway**, permanently consuming claims. The shim discards stdout, so the only observable was an exit
code that read 0 always. And a **ratchet that could have bitten today**: the mark was written once
after the whole loop, while the first fire's worst case (~32 min) exceeds the task's 30-minute kill.

**And the mutations caught two unproven guards being shipped.** Four mutations, two bit. Removing the
per-row watermark advance: green. Removing the monotonic guard: green. *Both "fixes" were untested.*
The monotonic guard now has a unit test and re-mutating turns it red; **the per-row advance remains
uncovered and says so at the line**, because its benefit exists only under a timeout kill and no
harness can kill mid-loop. Kept because the ratchet is real; flagged because *"it passed"* would be
measuring nothing there.

### THE TREND CLAIM REVERSED (`e3d3dbf`, `91b5ed2`)

The night's largest result, and it was sitting in the master.

```
              raw      clean          node consonance/tools/board-audit.js
~08-09        94.6%    92.9%
08-15         96.4%    92.6%
tonight       96.5%    91.7%
```

**Raw rises; clean falls.** *"Five panes were added and the share went UP"* was measuring **replay
accumulation** — raw share ratchets with restart-count × transcript-size regardless of behaviour.
**The panes DID dilute Main, slowly, exactly as adding panes should.** The qualitative reading
survives at ~92%; the direction did not. Raw at line 42,309 re-derives to 96.4%, matching the 08-15
journal exactly, which is what makes the comparison fair.

**Four chair claims died on the way, all B's catches:** "24 bursts" is a clustering-parameter
artifact (31 at one gap width, 1 at another — publish the stable fact, never the count);
timestamp-bucketing was dead on the chair's own 0 ms measurement; **the stated cost of persisting the
dedup table was simply wrong** — under Cycle 3a a second *"yes"* is a different turn with its own ts
— which had mis-priced the entire choice; and option C stays unspent, so `main.rs:1222` survives.

**And the pointer briefly cited figures no command re-derives.** Hand-rolled dedup gave
92.4/92.1/91.2 against the instrument's 92.9/92.6/91.7 — same direction, ~0.5pt on definition. The
master now cites the shipped instrument *with its command*, and keeps the chair's numbers beside it
rather than hiding the gap.

### HELD OPEN: the mechanism, with its falsifier running as this is written

B located the trigger — ~15 full re-reads of Main's 102MB transcript, **twelve carrying the
offset-zero signature**, 4 announced and **11 SILENT**, because the announce is armed only by the
missing-offsets-file case. Its reading: the head fingerprint is FNV over the **first 512 bytes**,
where the harness rewrites `mode` / `permission-mode` / `file-history-snapshot` records — *the
identity check aimed at the one region of an append-only log that moves.* And the design's own
comment carries the hole: `resume_offset` resolves doubt to 0 *because the dedup belt can catch it*,
while `SEEN` is process-memory and every reset happens at launch against an empty belt. **Two halves,
each assuming the other, both absent at the same instant.**

**This is NOT recorded as settled.** `head-watch.js` — B's own falsifier, written before the claim
could harden — is running now (PID 38156; start head `14527506195736961416`, matching the stored
offsets record on a 103.6MB transcript). Three outcomes registered *before* the run:

- **head-flip at relaunch** → header mutation confirmed, the boundary-hash fix is aimed right
- **shrink but no flip** → the master must NOT say header mutation; the fix survives, *the sentence
  does not*
- **neither, yet a burst lands** → **the diagnosis is wrong somewhere real**, and the two-writer
  clobber moves from footnote to first

*B wrote the instrument that can refute B, and named the outcome costing it the diagnosis, before
running it. That is the shape the whole night was about.*

### Around's census filed the chair as violation one (`e3d3dbf`)

70 journal file-touches, 3 with deletions — entry one is **`c1b8d39`, the chair's own "strike the
falsifier", executed as a DELETION where law 2's clean form is an appended retraction.** Around filed
itself alongside, its recommendation having caused it. Two of the three share one structural cause:
**the closing trailer makes clean appends impossible** — which is why this append sits below it.

Its refactor draft ships with its own gate already run (cite-check 10 GREEN) and a **coverage
clause** — the roster must match `ls consonance/tools/*.js consonance/hooks/*.js` — the chair's first
writing dropped the hooks half, which is the half covering `sourced-stop`, the room's only
always-on sensor; under the truncated clause a factual section could omit the entire sensor class
and still pass — *because a factual section that passes
cite-check by naming only the checkable is degenerate on arrival, and the author chose what enters
it.*

### What this append is missing

**Nobody has read it.** Every section of this journal that a pane checked had errors in it, without
exception. This one is routed for signing at the moment it is written — so if it stands unsigned,
that is a fact about the entry, not a verdict on its accuracy.

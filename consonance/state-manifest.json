{
  "version": 1,
  "written": "2026-09-09",
  "by": "pane A — P-STATE-SET, L049",
  "root": "data_dir",
  "checker": "node consonance/tools/state-manifest.js",

  "the_cut": "THE THREAD TRAVELS; THE PROCESS STAYS. A path is TRAVELS if a seat resuming on the other machine is a DIFFERENT seat without it. It is STAYS if it describes this machine's disk, ports, pids, byte offsets, microphone or raw ore. It is REGENERATES only if a NAMED writer rewrites it at a NAMED time, unasked. Anything else is UNDECIDED and says what would decide it.",

  "limits": [
    "A CLASSIFICATION IS NOT A TRANSPORT. Every TRAVELS entry except letters.json, panes.json and resonance/topics is APPEND-ONLY JSONL. Two machines appending between syncs conflict at the tail of every one of them. TRAVELS is therefore conditional on the single-live-host guard (P-LIVE-HOST, E) plus pull-before-launch. Without both, this manifest names a merge-conflict set.",
    "board.jsonl ALONE exceeds GitHub's 100 MB hard limit as measured. The transport question is decided by the compacted number, not by this file.",
    "The classification is relative to THIS transport (a state repo; transcripts machine-local). dev/migrate/pack_room.ps1 classifies the same paths for a DIFFERENT transport (a private bundle in which transcripts DO travel) and disagrees on tailer-offsets.json and transcript-asks.jsonl. Both are right for their own transport; neither generalises. Do not merge the two sets.",
    "REGENERATES states the consequence of arriving absent, because 'it comes back' and 'it comes back CORRECT' are not the same claim.",
    "THE ROSTER AND THE SWEEP ARE ONE MECHANISM AND ARE WRITTEN IN TWO FILES. panes.json is read by read_kept() (main.rs:3320), and gc_captures() (main.rs:869) builds its keep-set from exactly that call — so WHICHEVER ROSTER THIS MANIFEST CAUSES TO BE ON DISK AT LAUNCH DECIDES WHICH CAPTURE TAILS SURVIVE, and the tails are the thread. The two are correct separately and neither says so. Measured 2026-09-09: the arrived and local id sets were DISJOINT, so classing this file STAYS would have swept all four arrived tails and the laptop's work would never have reached the desktop; adopting it swept D's five instead, which is the ruled retirement (revivable, in captures/archive) and not the same event. C added LIBRARIAN_SID to the keep-set at 7e6223e the same morning, one level down in the same function. Change either side and read the other."
  ],

  "out_of_root": [
    { "ref": "panes.json:cwd", "points_at": "instances_dir/sibling-<id>", "class": "UNDECIDED", "handled_by": "roster-cwds", "decided_by": "THE FOURTH STATE, USED (packet_state_set_2026-09-09.md section 5: a path that cannot be honestly placed says so, and says what would decide it). These directories are not under this manifest's root (data_dir), so no rule here can walk, classify or carry them — and panes.json is the only TRAVELS file that points outside its own transported tree (B, desktop_first_launch_2026-09-09.md section 0.7). They are not TRAVELS: a sibling dir holds a CLAUDE.md that warm_resume_brief rewrites from the seat's own travelled capture tail (main.rs:5379), so shipping the dir would carry a stale intake over a fresh one. They are not STAYS either, because a STAYS path is one the other machine does not need, and this one must EXIST at the destination or the seat it names cannot spawn. WHAT WOULD DECIDE IT: a second manifest rooted at instances_dir, or a rule form that can classify a path by reference from another root. Until then the transform mints what is missing and the reconciliation checks it landed — a mechanism, not a promise. This entry exists so that the next path pointing out of root cannot be added without saying the same." }
  ],

  "rules": [
    { "glob": "board.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "the room's shared record. The one file the other machine cannot be the same room without. INSTALLS AS A FAST-FORWARD (L070, the keeper at 06:34 on 2026-09-21): both machines append to it, so an arriving copy replaces this machine's only if it extends it row for row; otherwise state-sync.js --install leaves this file as it is, refuses it, and names the rows only this machine holds. 21 displaced copies of it were found in L's attic before this (C, L069 §2)." },
    { "glob": "lap.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "the lap/chain ledger — who holds what. INSTALLS AS A FAST-FORWARD, for the reason board.jsonl does, and this is the file that proved it: every L launch MIGRATEd and the install replaced L's 488-row ledger with the state set's 419, a strict prefix of it, so L058-L065 were reissued five times over (C, handback/p-l069-ledger-C_2026-09-21.md). CORRECTED L070: this entry used to say chain-status.js prints 'this machine only' 'precisely because this file does not travel'. That was false — it is TRAVELS, it travelled, and it was replaced. chain-status.js's own note (:377-390) says 'this machine only' was accurate only while lap.jsonl lived on one disk, and it now prints the in-sync line from state-sync.status.json instead." },
    { "glob": "letters.json", "class": "TRAVELS", "why": "pane-id to seat-letter. Pure shared naming, no machine in it." },
    { "glob": "panes.json", "class": "TRAVELS", "on_arrival": "roster-cwds", "why": "THE KEPT ROSTER, AND IT DOES NOT DESCRIBE THE LIVE PANES — IT CREATES THEM. ui/term.js:1051 calls restoreKeptPanes() at module top level, which reads this file through list_kept_panes and SPAWNS one pane per row with its cwd verbatim. It travels because the keeper ruled it (one_house_two_machines_idea_2026-09-08.md:48: the laptop's seats become THE seats on both machines, committee panes named explicitly), so the pane IDS and LABELS are adopted — the ids key the capture tails, which are the thread. THE cwds ARE NEVER ADOPTED. This entry previously carried a precondition claiming the file travels correctly if both machines resolve the same instances_dir; that check PASSES here (both resolve C:\\Consonance\\instances) AND THE FAILURE HAPPENED ANYWAY, because what is machine-bound is not the root, it is the sibling-<id> directories minted inside it — 0 of 4 arrived cwds resolved on D while all 5 of its own did. A precondition is prose and cannot fail; the condition is now the on_arrival transform, which runs at the install boundary and is reconciled against the destination afterwards." },

    { "glob": "captures/3d000000-*.txt", "class": "STAYS", "why": "THE THIRD PLACE'S RECORD, AND IT IS FIRST IN THIS FILE BECAUSE FIRST MATCH WINS. The keeper's standing rule (.gitignore, 2026-08-29): the Third Place's record never goes to the repo and never to a cloud he did not choose. It migrates by hand or not at all. Found by C (P-SYNC-AT-LAUNCH section 6a) and carried by the chair as an interrupt. THE FAILURE WAS A WILDCARD NOBODY WROTE AGAINST IT: `captures/*.txt` below was written about the warm-resume carriers and silently swallowed a seat the rule forbids. A rule can be right about every case its author had in mind and still be wrong, because a glob has no idea what it is for." },
    { "glob": "captures/archive/3d000000-*.txt", "class": "STAYS", "why": "the same seat's RETIRED tail, 80 KB, and it is the half the interrupt did not name. It reached the remote because of MY OWN change one rule below — captures/archive/*.txt UNDECIDED -> TRAVELS, made this packet on a correct ruling about revivability that says nothing about WHOSE record. Retired does not lift the keeper's rule; if anything a retired Third Place tail is more of its record, not less." },
    { "glob": "captures/*.txt.bak-*", "class": "STAYS", "why": "hand backups from the 2026-07-13 duplication fix. Local forensics, superseded by the live .txt beside them." },
    { "glob": "captures/*.txt", "class": "TRAVELS", "why": "THE WARM-RESUME CARRIERS. This is the thread. A seat waking on the other machine is re-cued from its capture tail — without these it wakes as a stranger. 4.6 MB for the whole house." },
    { "glob": "captures/*.log", "class": "STAYS", "why": "raw ore. Nothing restores from it (lap plan section 0); 550 MB of the 887 MB total is here." },
    { "glob": "captures/archive/*.log", "class": "STAYS", "why": "rotated-out ore, same reason, older." },
    { "glob": "captures/archive/*.txt", "class": "TRAVELS", "why": "THE RULING LANDED AND THIS ENTRY WAS STILL UNDECIDED. loop/one_house_two_machines_idea_2026-09-08.md section 6 rule 1, from the keepers own words at 02:19 (it doesnt overwrite the seats that are on my desktop currently, but rather retires them): a retired seat stays REVIVABLE, and a retired threads capture tail is its only carrier. So they travel. 419 KB for every retired pane on the other machine. Changed by A during P-STATE-REPO (L052) because the first real sync would otherwise have shipped a set that is wrong by a ruling already made in writing; it is one line to revert." },    { "glob": "captures/archive", "class": "TRAVELS", "why": "must exist at the destination to hold the revivable tails above." },
    { "glob": "captures", "class": "TRAVELS", "why": "must exist at the destination to hold the .txt carriers." },

    { "glob": "backups/**", "class": "STAYS", "why": "librarian TRANSCRIPT backups and settings snapshots. Lap plan section 2: transcripts never leave the machine. This is that rule, not a size decision." },
    { "glob": "backups", "class": "STAYS", "why": "holds no TRAVELS member." },

    { "glob": "dispatch-gate.jsonl.pre-quarantine-*.bak", "class": "STAYS", "why": "pre-quarantine snapshot, 2026-08-30. Local forensics." },
    { "glob": "lap.jsonl.pre-union-*", "class": "STAYS", "why": "L071: the lap ledger as it stood before ledger-union.js --write restored the attic-only rows. The ONLY pre-union copy, kept beside the live file (the write's own backup, cited by path in handback/p-l071-unionwrite-C and librarian/2026-09-22). This machine's snapshot of one moment: carried to the other machine it would mean nothing there, and it must never be read as a ledger." },
    { "glob": "board.jsonl.pre-union-*", "class": "STAYS", "why": "L071: same, for the board. Named per ledger, not *.pre-union-*, so a pre-union copy of any other file is still loud." },
    { "glob": "precompact.jsonl.pre-union-*", "class": "STAYS", "why": "L076: ledger-union.js --write's backup of this fast-forward ledger (L075), the only pre-union copy, kept beside the live file. This machine's snapshot of one moment; never a ledger elsewhere. One rule per marked file, as for lap and board." },
    { "glob": "sessionstart-state.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, sessionstart-state." },
    { "glob": "sourced_ledger.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, sourced_ledger." },
    { "glob": "carrier-drift.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, carrier-drift." },
    { "glob": "ferry.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, ferry." },
    { "glob": "read_ledger.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, read_ledger." },
    { "glob": "return_ledger.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, return_ledger." },
    { "glob": "vantage_findings.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, vantage_findings." },
    { "glob": "resonance/atoms.jsonl.pre-union-*", "class": "STAYS", "why": "L076: same, resonance/atoms — in its SUBDIR, where the write keeps the backup beside the live file." },
    { "glob": "dispatch-gate.test-pollution.jsonl", "class": "STAYS", "why": "rows quarantined out of the live ledger as test pollution. Carrying them re-pollutes any reader that globs the directory." },
    { "glob": "precompact.polluted-by-tests-*.jsonl", "class": "STAYS", "why": "same class, 2026-08-18." },
    { "glob": "dispatch-gate.jsonl", "class": "TRAVELS", "why": "NOT A FAST-FORWARD INSTALL, deliberately (L074): its quarantine REWRITES the live file — consonance/hooks/dispatch-gate.js:431 writeFileSync()s the kept rows back over it, after :429 copies it to a .pre-quarantine backup and :430 moves the test-pollution rows out. A file that is ever rewritten would read DIVERGED against the other machine's copy forever, and under the stop-before-write install (L070) one DIVERGED marked file refuses the WHOLE install. So it installs by replacement, as before, with its old copy kept in the attic. the audit of every chair verb and every refusal. A gate whose record splits per machine cannot be audited." },
    { "glob": "precompact.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/hooks/precompact-preserve.js:122 (appendFileSync); the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. the room's compaction history. Its session_ids are machine-local, so its value on the other machine is as HISTORY, not as live state — that is still the right side of the cut." },
    { "glob": "sessionstart-state.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/hooks/sessionstart-state.js:129, :146 (appendFileSync); the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. hook ledger; same reason." },
    { "glob": "sourced_ledger.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/hooks/sourced-stop.js:244 (appendFileSync); read by second-vantage.js, pair-ledger.js, open-items.js; the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. the sourced-claim ledger read by open-items.js and pair-ledger.js. A claim ledger with a hole is worse than none." },
    { "glob": "carrier-drift.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/hooks/carrier-drift-watch.js:101 (appendFileSync; its state is a separate file, carrier-drift.state.json); the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. drift verdicts per run — a record, and the instrument that reads it counts across time." },
    { "glob": "ferry.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/tools/ferry.js:170 (appendFileSync); ferry-watch.js only reads it; the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. which shas reached which panes. The unread count is meaningless if half the ferrying happened on a machine the ledger cannot see." },
    { "glob": "read_ledger.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/tools/read-ledger.js:67 (appendFileSync); the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. registered directions and their falsifiers. Two rows, and the room's own rule is that a falsifier written down is the only kind that can fire." },
    { "glob": "return_ledger.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/hooks/findings-return.js:145 (appendFileSync); the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. what was surfaced to which seat, and the misses." },
    { "glob": "return_state/*.json", "class": "TRAVELS", "why": "per-thread dedupe of surfaced findings. THREAD state, not process state: if it stays, the same seat is re-shown findings it already answered." },
    { "glob": "return_state", "class": "TRAVELS", "why": "must exist to hold the above." },
    { "glob": "vantage_findings.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/tools/second-vantage.js:411 (appendFileSync); findings-return.js only reads it; the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. the second vantage's findings — the room's record of what an outside look returned." },
    { "glob": "vantage_runs.log", "class": "TRAVELS", "why": "run-level record of the same instrument." },
    { "glob": "vantage_watermark.json", "class": "TRAVELS", "why": "65 bytes deciding what has ALREADY been surfaced. Left behind it resets to a rewind and 192 answered findings surface again as new. Contrast carrier-drift.state.json below: same shape, consequence one duplicate line instead of 192. That difference is why watermarks are not one class." },

    { "glob": "resonance/atoms.jsonl", "class": "TRAVELS", "install": "fast-forward", "why": "INSTALLS AS A FAST-FORWARD (L074, the keeper at 04:0x on 2026-09-22): every writer only APPENDS — consonance/src-tauri/src/main.rs:8376 (OpenOptions append); curate.js never writes it (:32, :42); the repo and ~/.claude/shell were both searched, and the installed copy is the same file. Measured the same night, L's live copy against the state set's (D, 09-10): a strict extension, never diverged. the memory master — append-only, 16,796 atoms. Maintenance law 1 has no meaning if the master is on one disk." },
    { "glob": "resonance/curator_state.json", "class": "TRAVELS", "why": "the atom-to-topic routing. Without it the topic documents cannot be regenerated at all, which is what makes the next rule a repair path rather than a plan." },
    { "glob": "resonance/topics/*.md", "class": "TRAVELS", "why": "DERIVED FROM atoms.jsonl AND YET IT TRAVELS, deliberately, against the plan's hint. `node consonance/tools/curate.js` regenerates them (curate.js:306, without routing) — but only WHEN SOMEONE RUNS IT, and the shell builder reads these at every wake. A shell built before that run indexes documents that do not exist, which the shell's own text calls worse than an absence. Regenerability is the repair path here, not a reason to omit 39 KB." },
    { "glob": "resonance/topics", "class": "TRAVELS", "why": "must exist to hold the above." },
    { "glob": "resonance", "class": "TRAVELS", "why": "must exist to hold the master." },

    { "glob": "experiments/**", "class": "TRAVELS", "why": "a completed experiment's script and its excerpts — a record, 39 KB. pack_room.ps1 travels it too; for once both transports agree." },
    { "glob": "experiments", "class": "TRAVELS", "why": "must exist to hold the above." },

    { "glob": "heard.jsonl", "class": "STAYS", "why": "what THIS machine's microphone heard. The other machine has its own ears; merging two audio ledgers by timestamp describes no room that existed." },
    { "glob": "persist.log", "class": "STAYS", "why": "this app process's persistence-lifecycle trace (main.rs:851). Already treated as machine-bound corpus by actors.evidence.test.js, which declares JS-SUITE: MACHINE-BOUND home=L over it." },
    { "glob": "tailer-offsets.json", "class": "STAYS", "why": "BYTE OFFSETS INTO THIS MACHINE'S TRANSCRIPTS, with a fingerprint of each transcript's first bytes (main.rs:1452-1464). Transcripts are machine-local, so on the other machine these index files that are not there. NOTE THE DISAGREEMENT: dev/migrate/pack_room.ps1 copies this file — correctly, because in THAT bundle the transcripts travel with it. Here they do not." },
    { "glob": "transcript-asks.jsonl", "class": "STAYS", "why": "carries log_offset into a local transcript. Same reason, same disagreement with pack_room.ps1." },
    { "glob": "head-watch.jsonl", "class": "STAYS", "why": "absolute ~/.claude/projects paths and byte lengths for this machine's transcripts." },
    { "glob": "head-watch.stdout.log", "class": "STAYS", "why": "same, one line, 2026-08-17." },

    { "glob": "head-watch.lock", "class": "REGENERATES", "why": "holds pid 17640 — of a process on THIS machine. Travelling it is worse than losing it: a foreign pid reads as a live watcher and suppresses the real one.", "regenerated_by": "consonance/tools/head-watch.js", "regenerated_when": "at its next start", "if_absent": "the watcher starts, which is the correct outcome." },
    { "glob": "mcp.consonance*.json", "class": "REGENERATES", "why": "27 files each holding http://127.0.0.1:<port>/mcp — a port allocated at launch. A stale foreign port is a pane that cannot reach its own control plane.", "regenerated_by": "consonance/src-tauri/src/mcp.rs", "regenerated_when": "at every app launch, before panes start", "if_absent": "written at launch; no pane sees the gap." },
    { "glob": "field.json", "class": "REGENERATES", "why": "the live audio field — spectrum, peaks, now-playing. A snapshot of one second on one machine.", "regenerated_by": "consonance/src-tauri/src/cochlea_service.rs", "regenerated_when": "every audio frame while the app runs", "if_absent": "the UI shows no field until the first frame." },
    { "glob": "digest_state.json", "class": "REGENERATES", "why": "seat names plus per-pane seen-watermarks. The names are re-derived from letters.json (board-digest.js:406-411), which TRAVELS; the watermarks are one machine's read positions.", "regenerated_by": "consonance/hooks/board-digest.js", "regenerated_when": "at the next digest render, i.e. the next prompt in any pane", "if_absent": "each pane may see one already-seen digest line once. Stated because 'regenerates' hid exactly this kind of cost the last two times." },
    { "glob": "dream-watch.state.json", "class": "REGENERATES", "why": "one line of dedupe so the same dream notice is not repeated.", "regenerated_by": "consonance/hooks/dream-watch.js", "regenerated_when": "at its next hook fire", "if_absent": "one duplicate notice." },
    { "glob": "carrier-drift.state.json", "class": "REGENERATES", "why": "a 33-byte fingerprint for the same purpose.", "regenerated_by": "consonance/hooks/carrier-drift-watch.js", "regenerated_when": "at its next hook fire", "if_absent": "one duplicate line." },
    { "glob": "ready/*.json", "class": "REGENERATES", "why": "carries a pid and a cwd — 'is this pane's process at a ready screen'. Pure process state; a foreign pid here is a lie about liveness.", "regenerated_by": "the pane's own hooks, inside the claude process", "regenerated_when": "on the pane's next prompt or stop", "if_absent": "the pane reads not-ready until its next event." },
    { "glob": "ready", "class": "REGENERATES", "why": "created with its first stamp.", "regenerated_by": "consonance/src-tauri/src/main.rs", "regenerated_when": "at launch" },
    { "glob": "ready/*.tmp", "class": "STAYS", "why": "P-DIVERGED §3(d), placed by A. The half of a ready stamp's temp-then-rename that did not finish: dev/shell/lib/ready.js:61 writes `<pane>.json.<pid>.tmp`, then renames it over `<pane>.json`, and a failed rename (silent by design, :64) leaves the temp behind — measured, `ready/0c0c0c0b-…-115b.json.29304.tmp`, 216 B, 2026-09-14 03:20, found UNPLACED by C (preflight §5), which would make D's close refuse REFUSED_UNPLACED. It carries a pid and a cwd of a process on THIS machine, the same process state `ready/*.json` above never lets travel, and it is a leftover nothing reads. STAYS rather than REGENERATES because nothing regenerates a failed write. Named by its extension inside `ready/` only — not `*.tmp` anywhere: a wildcard is what put the Third Place tail on GitHub." },
    { "glob": "harvest/*.json", "class": "REGENERATES", "why": "the pty watcher's counters — attempts, panics, reinitialisations on this machine's terminal. Telemetry about a process, not about a thread.", "regenerated_by": "consonance/src-tauri/src/main.rs (harvest loop, ~:7305)", "regenerated_when": "continuously while the app runs", "if_absent": "counters restart at zero; no thread state is in them." },
    { "glob": "harvest", "class": "REGENERATES", "why": "created with its first stamp.", "regenerated_by": "consonance/src-tauri/src/main.rs", "regenerated_when": "at launch" },

    { "glob": "live_host.json", "class": "TRAVELS", "why": "P-LIVE-HOST's record and offline fallback (design §8). SHARED STATE ABOUT MACHINES: its entire purpose is that the other machine reads it and learns who holds the house — a copy that never leaves is a guard talking to itself. Note the asymmetry with install_id below, which is the opposite requirement and not a mirror of this one: live_host.json must be IDENTICAL on both machines; install_id must be UNIQUE to each. Same subject, opposite invariants.", "limit": "TRAVELS does not make a heartbeat live. This file moves at sync cadence, so its freshness is bounded by the last pull and it cannot see a host that started since. E's design already puts the authority in the git lease for exactly this reason (design §4, §8) — the classification carries the record, not the liveness." },
    { "glob": "sync-completion.json", "class": "STAYS", "why": "RESERVED, does not exist. E's design needs a `syncVerified` input and §9 asks A for 'a sync-completion record the launcher can read'; absenceClass() treats null as UNKNOWN and refuses to read an absent claim without it. It answers 'did THIS machine's pull complete and verify' — per-machine, per-sync. If it travelled, machine D would read machine L's verification as its own, which is install_id's silent failure in a second costume: foreign state read as self.", "reservation": "THE NAME IS MINE, THE FILE IS E's AND C's. If they choose a different name this rule must be renamed with it. Until then the checker catches the real name as UNPLACED, which is the correct outcome and better than a rule that pretends to cover a name nobody has picked." },

    { "glob": "replay-check.mark.json", "class": "STAYS", "why": "DECLARED IN ADVANCE, does not exist. C's replay-check.js writes it (`CONSONANCE_REPLAY_MARK`, :36) as a mark on this machine's board AND its transcripts, and splits later board growth into live turns versus replay by comparing the two. The transcripts are machine-local, so a mark taken on L and read on D would attribute D's board growth against transcripts D does not have. C's tool already refuses to score across a compaction on the grounds that the two are not the same corpus and a verdict across that seam is arithmetic on sand — a foreign mark is that same seam, one machine over. Found by running js-suite rather than by being told about it.", "reservation": "the path is C's, from replay-check.js:36. If C renames it this rule must follow, and until then the checker catches the new name as UNPLACED." },

    { "glob": "attic/board.jsonl.*", "class": "STAYS", "why": "DECLARED IN ADVANCE; does not exist yet. P-BOARD-COMPACT (B) will move the pre-compaction board here as its reversibility. It must not travel: it is exactly the bytes the compaction removed, and carrying them would undo the only thing that gets the transport under the cap." },
    { "glob": "attic", "class": "STAYS", "why": "same; declared before it exists so it cannot arrive unclassified." },

    { "glob": "sync-adopted.json", "class": "STAYS", "why": "DECLARED IN ADVANCE, does not exist yet. C's launcher writes it (P-SYNC-AT-LAUNCH section 6e): which seats THIS machine adopted from the pulled state and which of its own it retired. Per-machine by construction — a travelled copy would tell the laptop it had adopted the desktop's retirements, which is sync-completion.json's failure in a fourth costume. Named explicitly rather than by a `sync-*` glob, and that is the lesson of this very packet: a wildcard is what put the Third Place's tail on GitHub. A future sync-* file this rule does not name shows as UNPLACED, which is the loud outcome and the correct one." },
    { "glob": "sync-pull.log", "class": "STAYS", "why": "same packet, same seat: the launcher's own trace of what THIS machine pulled and when. A local log of a local operation." },
    { "glob": "state-sync.push.json", "class": "STAYS", "why": "DECLARED WITH THE CODE THAT WRITES IT (state-sync.js --push, L054). The RECEIPT of this machine last push: which outcome, which checks ran, and the run id of the process that wrote it, so close.js can gate on WHAT HAPPENED instead of on an exit code that four outcomes share. Per-machine by construction and STAYS for the same reason as state-sync.status.json and sync-completion.json: a travelled copy would tell the desktop that the laptop push was its own, which is install_id silent failure in a fifth costume. Named in full rather than by a state-sync.* glob -- the wildcard is what put the Third Place tail on GitHub, and a future file this rule does not name shows as UNPLACED, which is the loud outcome and the correct one." },
    { "glob": "state-sync.status.json", "class": "STAYS", "why": "DECLARED IN ADVANCE by the tool that writes it (state-sync.js, L052). It holds ONE COMMIT HASH PER MACHINE as of THIS machine last syncing, and chain-status.js reads it every prompt in place of the old \"this machine only\". It is a per-machine AS-OF view: if it travelled, the desktop would print the laptop's reading of both heads as its own and the in-sync line would report agreement it never checked. Same failure as install_id and sync-completion.json, third costume." },
    { "glob": "stick-waiter.lock", "class": "STAYS", "why": "DECLARED WITH THE CODE THAT WRITES IT (dev/stick-waiter.js, L059 §9 R-3). {pid, image, script, at} of THIS machine's exit waiter, and it exists for the WHOLE of every session, stick or no stick — which is why it is a blocker: unplaced, D's close refuses REFUSED_UNPLACED whenever the app is open. A pid is process state. A travelled copy naming the other machine's waiter reads as live the moment that pid is reused here, and this machine's waiter never starts — the head-watch.lock failure above. STAYS as ruled: the waiter takes over a dead holder's lock itself, so nothing needs to regenerate it. Named in full, not stick-*: a wildcard is what put the Third Place tail on GitHub." },
    { "glob": "heavy-run.lock", "class": "STAYS", "why": "DECLARED WITH THE CODE THAT WRITES IT (consonance/tools/heavy-run.js, L098). {pid, seat, cmd, started, token} of the ONE heavy run (js-suite, a mutant harness) this machine's tree is running. Per-machine and per-process: a travelled copy would make the other machine wait on a pid that is not its own." },
    { "glob": "stick-waiter.status.log", "class": "STAYS", "why": "the status file the exit waiter writes at export and its window follows (dev/stick-waiter.js, L059 §9 R-3): THIS machine's DONE or NOT DONE, truncated at every export. A report about one machine's exit; on the other machine it would read as a transfer that machine made." },
    { "glob": "stick-apply.started.json", "class": "STAYS", "why": "the applier's handshake (packet L059 §2, dev/stick-apply.js): the pid of a node process on THIS machine carrying the stick in. The launch reads it for the single-applier guard (E-1) and the waiter stands down on it. A travelled copy would make the other machine's launch refuse to start an applier, or its waiter stand down, over a process that was never there." },
    { "glob": "stick-apply.result.json", "class": "STAYS", "why": "the applier's result (packet L059 §2): read, and deleted after showing, by THIS machine's relaunched app. The outcome of an import into this machine's transcripts; on the other machine it would be shown as a transfer that never happened there." },
    { "glob": "stick-keep.json", "class": "STAYS", "why": "the keeper's KEEP-THIS-MACHINE'S choices per seat, written by the app's setup window (sync_launch.rs STICK_KEEP — E's file, placed here because it lands in this data dir, L059 §9 R-3). A choice about THIS machine's copy of a conversation; carried, it would tell the other machine to keep a copy it does not hold, and suppress the window there for a seat nobody chose on." },
    { "glob": "attic/pre-sync-*", "class": "STAYS", "why": "DECLARED IN ADVANCE; does not exist yet. state-sync.js --install moves any file it is about to overwrite here before writing the pulled one, so a sync that arrives wrong is reversible on the machine it arrived at. It is exactly the bytes the sync displaced -- carrying them back is how a bad sync becomes permanent on both machines." },
    { "glob": "attic/pre-sync-*/**", "class": "STAYS", "why": "same, its contents." },
    { "glob": "frames.jsonl", "class": "STAYS", "why": "the audio listener's raw frames (cochlea_service.rs is the only writer), 29 MB. Sensor ore from this machine's microphone; nothing on the other machine reads it, and travelling it would add half again to a 59 MB set. Classified 2026-09-10 by the librarian seat at the keeper's word: these four were the whole of REFUSED_UNPLACED blocking the first close from D." },
    { "glob": "grep.exe.stackdump", "class": "STAYS", "why": "a Git-Bash grep crash dump from 2026-08-01, 1 KB. Local forensics about this machine's toolchain, meaningless on another. Nobody has chased what crashed; classifying it does not answer that, it only stops it riding along." },
    { "glob": "lyrics-cache.json", "class": "STAYS", "why": "LRCLIB lyrics cached per track — already gitignored (.gitignore:63-66) for the reason that applies here too: third-party text plus a record of what was playing, and the lookup is free and repeatable." },
    { "glob": "RECORD", "class": "STAYS", "why": "a zero-byte flag file written by the cochlea path (cochlea.rs, cochlea_service.rs, main.rs). Its meaning is this machine's recording state; carried to another machine it would claim a state that machine is not in. AND IT IS NOT ONLY A CLAIM, IT IS A SWITCH: cochlea_service.rs:404 reads `recording = data_dir.join(\"RECORD\").exists()` once a second and, while it is true, appends a frame per audio chunk to frames.jsonl. A RECORD that travelled would turn on microphone frame-recording on the other machine without anyone at that keyboard choosing it. Verified 2026-09-10 by A." }
  ],

  "forbidden": [
    {
      "glob": "install_id*",
      "why": "install_id MUST NOT LIVE UNDER THIS ROOT AT ALL — and this is a refusal of the placement asked for, not a naming of it. STAYS was the requested column and STAYS would not have worked: live-host.js's identityHazard() tests whether the install-id path sits inside the travelling root BY PATH PREFIX (:252-262). It never reads this manifest. So a STAYS rule here would keep the file out of the sync and STILL trip the hazard, and E's launcher would refuse to arm the guard — while the manifest read as having satisfied the dependency. A rule that looks like compliance and blocks the thing it was asked for is worse than no rule. Its home is machine-local and OUTSIDE the data dir: ~/.consonance.json already carries machine_tag, is already the machine-local identity file this room reads (actors.js, the MACHINE-BOUND class), and is already outside every travelling set. THE STAKE, in E's words: if that file ever lands in the travelling set both machines share an identity and every foreign claim reads as self — the guard returns PROCEED_RECLAIM at exactly the moment it should refuse, quietly, with no error anywhere. This declaration is what makes that loud."
    },
    {
      "glob": "*/install_id*",
      "why": "same, one level down — a subdirectory of the data dir is still inside the travelling root, and the prefix test does not care how deep it is."
    }
  ]
}
